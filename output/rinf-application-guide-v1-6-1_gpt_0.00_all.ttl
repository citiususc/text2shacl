@prefix era: <http://data.europa.eu/949/> .
@prefix era-sh: <http://data.europa.eu/949/shapes/> .
@prefix geosparql: <http://www.opengis.net/ont/geosparql#> .
@prefix owl: <http://www.w3.org/2002/07/owl#> .
@prefix rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#> .
@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .
@prefix sh: <http://www.w3.org/ns/shacl#> .
@prefix time: <http://www.w3.org/2006/time#> .
@prefix wgs1: <http://www.w3.org/2003/01/geo/wgs84_pos#> .
@prefix xsd: <http://www.w3.org/2001/XMLSchema#> .
@prefix org: <http://www.w3.org/ns/org#> .

era:TrackShape a sh:NodeShape ;
               sh:property era-sh:AccelerationLevelCrossing,
               era-sh:ConnectedTo ;
               sh:targetClass era:Track .

era:CommonCharacteristicsSubsetShape a sh:NodeShape ;
               sh:property era-sh:AdditionalBrakingInformationDocument,
               era-sh:AtoCommunicationSystem,
               era-sh:AtoGradeAutomation,
               era-sh:AtoSystemVersion,
               era-sh:CantDeficiencyBasicSSP,
               era-sh:CompatibilityProcedureDocument,
               era-sh:ConditionsSwitchClassBSystems,
               era-sh:ConditionsUseReflectivePlates,
               era-sh:ContactLineSystem,
               era-sh:ContactStripMaterial,
               era-sh:Contains,
               era-sh:DocumentRestrictionPositionContactLineSeparation,
               era-sh:DocumentRestrictionPowerConsumption,
               era-sh:EddyCurrentBraking,
               era-sh:EddyCurrentBrakingConditionsDocument,
               era-sh:ErrorCorrectionsOnboard,
               era-sh:ETCS,
               era-sh:EtcsDegradedSituation,
               era-sh:EtcsNationalPacket44Function,
               era-sh:EtcsRestrictionsConditionsDoc,
               era-sh:GaugingProfile,
               era-sh:GaugingTransversalDocument,
               era-sh:GsmRActiveMobiles,
               era-sh:GsmRAdditionalInfo,
               era-sh:GsmROptionalFunctions,
               era-sh:GsmRVersion,
               era-sh:GsmrConstraintsOperateOnlyInCircuitSwitch,
               era-sh:GsmrNetworkCoverage,
               era-sh:InCountry,
               era-sh:InfrastructureManager,
               era-sh:InstructionsSwitchRadioSystems,
               era-sh:LegacyRadioSystem,
               era-sh:LinesideDistanceIndication,
               era-sh:LocalRulesOrRestrictionsDoc,
               era-sh:MNvcontact,
               era-sh:MagneticBraking,
               era-sh:MagneticBrakingConditionsDocument,
               era-sh:MinimumVerticalRadius,
               era-sh:OsmClass,
               era-sh:OtherCantDeficiencyBasicSSP,
               era-sh:OtherPantographHead,
               era-sh:OtherTrainProtection,
               era-sh:ProfileNumberSemiTrailers,
               era-sh:ProfileNumberSwapBodies,
               era-sh:QNvdriverAdhes,
               era-sh:QNvemrrls,
               era-sh:SafeConsistLengthInformationNecessary,
               era-sh:StandardCombinedTransportContainers,
               era-sh:StandardCombinedTransportRollerUnits,
               era-sh:SubsetOf,
               era-sh:TemperatureRange,
               era-sh:TenClassification,
               era-sh:TrackSystemSeparationInfo,
               era-sh:TrainDetectionSystem,
               era-sh:TSIPantographHead,
               era-sh:VehicleTypesCompatibleTrafficLoad,
               era-sh:VehiclesCompatibleTrafficLoad,
               era-sh:WheelSetGauge,
               era-sh:AutomaticDroppingDeviceRequired,
               era-sh:BigMetalMass,
               era-sh:ConditionsSwitchTrainProtectionSystems,
               era-sh:ContactStripMaterialMetallicContent,
               era-sh:dNvovtrp,
               era-sh:dNvpotrp,
               era-sh:dNvroll,
               era-sh:DemonstrationENE,
               era-sh:DemonstrationINF,
               era-sh:DistSignToPhaseEnd,
               era-sh:ErrorCorrectionsOnboardExplanation,
               era-sh:EtcsImplementsLevelCrossingProcedure,
               era-sh:EtcsInfillLineAccess,
               era-sh:FlangeLubeForbidden,
               era-sh:GaugingCheckLocation,
               era-sh:GprsForETCS,
               era-sh:GprsImplementationArea,
               era-sh:GradientProfile,
               era-sh:GsmRNoCoverage,
               era-sh:GsmrForcedDeregistrationFunctionalNumber,
               era-sh:HasAdditionalBrakingInformation,
               era-sh:HasETCSRestrictionsConditions,
               era-sh:HasEtcsNationalPacket44,
               era-sh:HasHotAxleBoxDetector,
               era-sh:HasLevelCrossings,
               era-sh:HasPhaseSeparation,
               era-sh:HasPlatformCurvature,
               era-sh:HasSevereWeatherConditions,
               era-sh:HasSystemSeparation,
               era-sh:HasTSITrainDetection,
               era-sh:IsQuietRoute,
               era-sh:LocalRulesOrRestrictions,
               era-sh:MNvderun,
               era-sh:MaximumAltitude,
               era-sh:MaximumBrakingDistance,
               era-sh:MaximumPermittedSpeed,
               era-sh:NationalLoadCapability,
               era-sh:NationalValuesBrakeModel,
               era-sh:PermitUseReflectivePlates,
               era-sh:PermittedContactForce,
               era-sh:PublicNetworkRoaming,
               era-sh:PublicNetworkRoamingDetails,
               era-sh:QNvsbtsmperm,
               era-sh:RadioNetworkId,
               era-sh:RailSystemType,
               era-sh:RedLightsRequired,
               era-sh:SpecificInformation,
               era-sh:StructureCheckLocation,
               era-sh:SubsetName,
               era-sh:SwitchProtectControlWarning,
               era-sh:SwitchRadioSystem,
               era-sh:TNvcontact,
               era-sh:TNvovtrp,
               era-sh:TenGISId,
               era-sh:TrainIntegrityOnBoardRequired,
               era-sh:TSIMagneticFields,
               era-sh:TSISwitchCrossing,
               era-sh:TSITractionHarmonics,
               era-sh:UsesGroup555,
               era-sh:VNallowovtrp,
               era-sh:VNvsupovtrp,
               era-sh:VerificationCCS,
               era-sh:VerificationENE,
               era-sh:VerificationINF ;
               sh:targetClass era:CommonCharacteristicsSubset .

era:NonLinearElementShape a sh:NodeShape ;
               sh:property era-sh:Aggregates ;
               sh:targetClass era:NonLinearElement .

era:SubsidiaryLocationShape a sh:NodeShape ;
               sh:property era-sh:AllocationCompany,
               era-sh:LinkedToPrimaryLocation,
               era-sh:SubsidiaryLocationType,
               era-sh:SubsidiaryLocationCode,
               era-sh:SubsidiaryLocationName ;
               sh:targetClass era:SubsidiaryLocation .

era:NetPointReferenceShape a sh:NodeShape ;
               sh:property era-sh:AppliesToDirection,
               era-sh:HasLrsCoordinate,
               era-sh:HasTopoCoordinate ;
               sh:targetClass era:NetPointReference .

era:VehicleTypeShape a sh:NodeShape ;
               sh:property era-sh:AuthorizedCountry,
               era-sh:AxleBearingConditionMonitoring,
               era-sh:Category,
               era-sh:Certificate,
               era-sh:DataGSMRNetwork,
               era-sh:DataRadioCompatible,
               era-sh:EndCouplingType,
               era-sh:EtcsBaseline,
               era-sh:ETCSEquipmentOnBoardLevel,
               era-sh:EtcsInfill,
               era-sh:EtcsSystemCompatibility,
               era-sh:FireSafetyCategory,
               era-sh:GsmRRadioDataCommunication,
               era-sh:Manufacturer,
               era-sh:ParkingBrakeType,
               era-sh:PreviousVehicleType,
               era-sh:ProtectionLegacySystem,
               era-sh:SnowIceHailConditions,
               era-sh:SubCategory,
               era-sh:SupportedPlatformHeight,
               era-sh:ThermalCapacityTSIReference,
               era-sh:TypeVersionId,
               era-sh:VehicleCategory,
               era-sh:VehicleSubCategory,
               era-sh:VehicleTypeMaximumSpeedAndCantDeficiency,
               era-sh:VoiceGSMRNetwork,
               era-sh:VoiceRadioCompatible,
               era-sh:WheelSetGaugeChangeoverFacility,
               era-sh:AlternativeName,
               era-sh:AltitudeRange,
               era-sh:AltitudeRangeDetail,
               era-sh:AxleSpacing,
               era-sh:BoardingAids,
               era-sh:BrakeWeightPercentage,
               era-sh:CantDeficiency,
               era-sh:CatenaryMaxRatedCurrent,
               era-sh:ConditionsTrainFormation,
               era-sh:DangerousGoodsTankCode,
               era-sh:DesignMassExceptionalPayload,
               era-sh:DesignMassNormalPayload,
               era-sh:DesignMassWorkingOrder,
               era-sh:DrivingCabs,
               era-sh:EddyCurrentBrakePrevention,
               era-sh:EddyCurrentBrakingFitted,
               era-sh:EmergencyBrake,
               era-sh:EnergyMeterInstalled,
               era-sh:EnergySupplyMaxPower,
               era-sh:ETCSDataCommApp,
               era-sh:ETCSNationalApplications,
               era-sh:ETCSOnBoardImplementation,
               era-sh:FerromagneticWheelMaterial,
               era-sh:FixedSeats,
               era-sh:FlangeLubricationFitted,
               era-sh:GsmRSetsInDrivingCab,
               era-sh:HasAutomaticDroppingDevice,
               era-sh:HasCantDefficiencyCompensation,
               era-sh:HasCurrentLimitation,
               era-sh:HasLubricationDevicePrevention,
               era-sh:HasParkingBrake,
               era-sh:HasRegenerativeBrake,
               era-sh:HasSandingPrevention,
               era-sh:HasShuntingRestrictions,
               era-sh:HasTrainIntegrityConfirmation,
               era-sh:HasWheelSlideProtectionSystem,
               era-sh:LetterMarking,
               era-sh:LoadingPlatformHeight,
               era-sh:MagneticBrakePrevention,
               era-sh:MagneticBrakingFitted,
               era-sh:MassPerWheel,
               era-sh:MaxDistConsecutiveAxles,
               era-sh:MaxFlangeHeight,
               era-sh:MaxLengthVehicleNose,
               era-sh:MaximumAverageDeceleration,
               era-sh:MaximumBrakeThermalEnergyCapacity,
               era-sh:MaximumDesignSpeed,
               era-sh:MaximumLocomotivesCoupled,
               era-sh:MaximumServiceBrake,
               era-sh:MaximumSpeedAndCantDeficiency,
               era-sh:MaximumSpeedEmpty,
               era-sh:MeetsRequirementVehicleAuthorisation,
               era-sh:MinAxleLoad,
               era-sh:MinDistConsecutiveAxles,
               era-sh:MinDistFirstLastAxle,
               era-sh:MinFlangeHeight,
               era-sh:MinFlangeThickness,
               era-sh:MinRimWidth,
               era-sh:MinWheelDiameter,
               era-sh:MinimumConcaveVerticalRadius,
               era-sh:MinimumConvexVerticalRadius,
               era-sh:MinimumWheelDiameter,
               era-sh:NonCodedRestrictions,
               era-sh:NumberElementsRakeFreightWagons,
               era-sh:NumberOfPantographsInContactWithOCL,
               era-sh:NumberOfToilets,
               era-sh:OclType,
               era-sh:ParkingBrake,
               era-sh:ParkingBrakeMandatory,
               era-sh:ParkingBrakeMaximumGradient,
               era-sh:PassByNoiseLevel,
               era-sh:PermissiblePayload,
               era-sh:PortableBoardingAids,
               era-sh:PreventRegenerativeBrakeUse,
               era-sh:PrioritySeats,
               era-sh:PrmAccessibleToilets,
               era-sh:QuasiStaticGuidingForce,
               era-sh:RadioSwitchOverSpecialConditions,
               era-sh:ReferencePassByNoiseLevel,
               era-sh:ShortestDistanceBetweenPantographsInContactWithOCL,
               era-sh:SleepingPlaces,
               era-sh:StartingNoiseLevel,
               era-sh:StaticAxleLoadExceptionalPayload,
               era-sh:StaticAxleLoadNormalPayload,
               era-sh:StaticAxleLoadWorkingOrder,
               era-sh:StationaryNoiseLevel,
               era-sh:StructuralCategory,
               era-sh:ThermalCapacityDistance,
               era-sh:ThermalCapacityGradient,
               era-sh:ThermalCapacitySpeed,
               era-sh:ThermalCapacityTime,
               era-sh:TotalVehicleMass,
               era-sh:TrainControlSwitchOverSpecialConditions,
               era-sh:TransportableOnFerry,
               era-sh:TypeVersionNumber,
               era-sh:VehicleContactForce,
               era-sh:VehicleKinematicGaugeOther,
               era-sh:VehicleMaxSandingOutput,
               era-sh:VehiclePantographHead,
               era-sh:VehiclesComposingFixedFormation,
               era-sh:VoiceOperationalCommImpl,
               era-sh:WheelSetGaugeTransformationMethod,
               era-sh:WheelchairSleepingPlaces,
               era-sh:WheelchairSpaces ;
               sh:targetClass era:VehicleType .

era:InfrastructureElementShape a sh:NodeShape ;
               sh:property era-sh:BelongsTo,
               era-sh:HasAbstraction,
               era-sh:HasPart,
               era-sh:InfrastructureMgr,
               era-sh:IsPartOf,
               era-sh:LineReference,
               era-sh:NetReference,
               era-sh:OperatingLanguage,
               era-sh:ParameterApplicability,
               era-sh:PrimaryLocation,
               era-sh:ValidityEndDate,
               era-sh:ValidityStartDate ;
               sh:targetClass era:InfrastructureElement .

era:OperationalPointShape a sh:NodeShape ;
               sh:property era-sh:BorderPointOf,
               era-sh:DigitalSchematicOverview,
               era-sh:HasSchematicOverviewOPDigitalForm,
               era-sh:OpInfoPerCountry,
               era-sh:OpName,
               era-sh:OpType,
               era-sh:OpTypeGaugeChangeover,
               era-sh:ReferenceBorderPoint,
               era-sh:SchematicOverviewOP,
               era-sh:Siding,
               era-sh:Track,
               era-sh:Uopid,
               era-sh:TAFTAPCode ;
               sh:targetClass era:OperationalPoint .

era:OrientationShape a sh:NodeShape ;
               sh:property era-sh:CarrierLinearElement,
               era-sh:Direction,
               era-sh:Orientation,
               era-sh:Side ;
               sh:targetClass era:Orientation .

era:ContactLineSystemShape a sh:NodeShape ;
               sh:property era-sh:ConditionalRegenerativeBrake,
               era-sh:ConditionsAppliedRegenerativeBraking,
               era-sh:ContactLineSystemType,
               era-sh:EnergySupplySystem,
               era-sh:ConditionsChargingElectricEnergyStorage,
               era-sh:CurrentLimitationRequired,
               era-sh:EnergySupplySystemTSICompliant,
               era-sh:MaxCurrentStandstillPantograph,
               era-sh:MaxTrainCurrent,
               era-sh:MaximumContactWireHeight,
               era-sh:MinimumContactWireHeight,
               era-sh:PermissionChargingElectricEnergyTractionStandstill,
               era-sh:Umax2 ;
               sh:targetClass era:ContactLineSystem .

era:InfrastructureManagerShape a sh:NodeShape ;
               sh:property era-sh:DefinesSubset ;
               sh:targetClass era:InfrastructureManager .

era:NetRelationShape a sh:NodeShape ;
               sh:property era-sh:ElementA,
               era-sh:ElementB,
               era-sh:Navigability,
               era-sh:IsOnOriginOfElementA,
               era-sh:IsOnOriginOfElementB ;
               sh:targetClass era:NetRelation .

era:TunnelShape a sh:NodeShape ;
               sh:property era-sh:EndLocation,
               era-sh:LineReferenceTunnelEnd,
               era-sh:LineReferenceTunnelStart,
               era-sh:RollingStockFireCategory,
               era-sh:SpecialTunnelArea,
               era-sh:StartLocation,
               era-sh:TunnelDocRef,
               era-sh:ComplianceInfTsi,
               era-sh:CrossSectionArea,
               era-sh:DemonstrationSRT,
               era-sh:DieselThermalAllowed,
               era-sh:HasEmergencyPlan,
               era-sh:HasEvacuationAndRescuePoints,
               era-sh:HasWalkway,
               era-sh:LengthOfTunnel,
               era-sh:NationalRollingStockFireCategory,
               era-sh:TunnelIdentification,
               era-sh:VerificationSRT ;
               sh:targetClass era:Tunnel .

era:NetLinearReferenceShape a sh:NodeShape ;
               sh:property era-sh:EndsAt,
               era-sh:HasSequence,
               era-sh:StartsAt ;
               sh:targetClass era:NetLinearReference .

era:ETCSShape a sh:NodeShape ;
               sh:property era-sh:EtcsLevelType,
               era-sh:EtcsMVersion,
               era-sh:EtcsTransmittedTrackConditions,
               era-sh:EtcsTransmitsTrackConditions ;
               sh:targetClass era:ETCS .

era:RunningTrackShape a sh:NodeShape ;
               sh:property era-sh:FreightCorridor,
               era-sh:LineCategory,
               era-sh:PassesThroughTunnel,
               era-sh:PlatformEdge,
               era-sh:RailInclination,
               era-sh:TrackDirection,
               era-sh:TrackLoadCapability,
               era-sh:TrackPhaseInfo,
               era-sh:TrackRaisedPantographsDistanceAndSpeed,
               era-sh:TracksideHABD,
               era-sh:CantDeficiency,
               era-sh:HasBallast,
               era-sh:HasBridge,
               era-sh:HighSpeedLoadModelCompliance,
               era-sh:MaximumTrainDeceleration,
               era-sh:MinimumHorizontalRadius,
               era-sh:TrackId ;
               sh:targetClass era:RunningTrack .

era:TrainDetectionSystemShape a sh:NodeShape ;
               sh:property era-sh:FrenchTrainDetectionSystemLimitation,
               era-sh:FrequencyBandsForDetection,
               era-sh:MaxSandingOutput,
               era-sh:MinVehicleImpedance,
               era-sh:TdsFrenchTrainDetectionSystemLimitation,
               era-sh:TdsMaximumMagneticField,
               era-sh:TdsMinAxleLoadVehicleCategory,
               era-sh:TrainDetectionSystemSpecificCheck,
               era-sh:TrainDetectionSystemSpecificCheckDocument,
               era-sh:TrainDetectionSystemType,
               era-sh:TSICompliantCompositeBrakeBlocks,
               era-sh:TSICompliantFerromagneticWheel,
               era-sh:TSICompliantMaxDistConsecutiveAxles,
               era-sh:TSICompliantMaxImpedanceWheelset,
               era-sh:TSICompliantMetalConstruction,
               era-sh:TSICompliantMetalFreeSpace,
               era-sh:TSICompliantRSTShuntImpedance,
               era-sh:TSICompliantSandCharacteristics,
               era-sh:TSICompliantSanding,
               era-sh:TSICompliantShuntDevices,
               era-sh:FlangeLubeRules,
               era-sh:MaxDistEndTrainFirstAxle,
               era-sh:MaxImpedanceWheelset,
               era-sh:MaximumInterferenceCurrent,
               era-sh:MaximumInterferenceCurrentEvaluation,
               era-sh:RequiredSandingOverride ;
               sh:targetClass era:TrainDetectionSystem .

era:FrenchTrainDetectionSystemLimitationShape a sh:NodeShape ;
               sh:property era-sh:FrenchTrainDetectionSystemLimitationNumber,
               era-sh:FrenchTrainDetectionSystemLimitationApplicable ;
               sh:targetClass era:FrenchTrainDetectionSystemLimitation .

era:KilometricPostShape a sh:NodeShape ;
               sh:property era-sh:HasLRS,
               era-sh:Kilometer,
               era-sh:KmPostName,
               era-sh:MeasuredDistance ;
               sh:targetClass era:KilometricPost .

era:OrganisationRoleShape a sh:NodeShape ;
               sh:property era-sh:HasOrganisationRole,
               era-sh:RoleOf,
               era-sh:IMCode,
               era-sh:OrganisationCode ;
               sh:targetClass era:OrganisationRole .

era:SignalShape a sh:NodeShape ;
               sh:property era-sh:HasOrientation,
               era-sh:SignalOrientation,
               era-sh:SignalType,
               era-sh:RelativeDistanceDangerPoint,
               era-sh:SignalId ;
               sh:targetClass era:Signal .

era:HABDShape a sh:NodeShape ;
               sh:property era-sh:HotAxleBoxDetectorDirection,
               era-sh:HotAxleBoxDetectorGeneration,
               era-sh:HotAxleBoxDetectorIdentification,
               era-sh:HotAxleBoxDetectorLocation,
               era-sh:HotAxleBoxDetectorTSICompliant ;
               sh:targetClass era:HABD .

era:NetAreaReferenceShape a sh:NodeShape ;
               sh:property era-sh:Includes ;
               sh:targetClass era:NetAreaReference .

era:LinearPositioningSystemCoordinateShape a sh:NodeShape ;
               sh:property era-sh:KmPost,
               era-sh:OffsetFromKilometricPost ;
               sh:targetClass era:LinearPositioningSystemCoordinate .

era:LineReferenceShape a sh:NodeShape ;
               sh:property era-sh:LineNationalId ;
               sh:targetClass era:LineReference .

era:LinesideDistanceIndicationShape a sh:NodeShape ;
               sh:property era-sh:LinesideDistanceIndicationAppearance,
               era-sh:LinesideDistanceIndicationPositioning,
               era-sh:LinesideDistanceIndicationFrequency ;
               sh:targetClass era:LinesideDistanceIndication .

era:LoadCapabilityShape a sh:NodeShape ;
               sh:property era-sh:LoadCapabilityLineCategory,
               era-sh:LoadCapabilitySpeed ;
               sh:targetClass era:LoadCapability .

era:LinearPositioningSystemShape a sh:NodeShape ;
               sh:property era-sh:LrsMethod,
               era-sh:LineId ;
               sh:targetClass era:LinearPositioningSystem .

era:VehicleShape a sh:NodeShape ;
               sh:property era-sh:ManufacturingCountry,
               era-sh:OperationalRestriction,
               era-sh:QuieterRoutesExemptedCountry,
               era-sh:VehicleKeeper,
               era-sh:VehicleType,
               era-sh:CompositeBrakeBlockRetrofitted,
               era-sh:LengthOfVehicle,
               era-sh:VehicleNumber,
               era-sh:VehicleSeries ;
               sh:targetClass era:Vehicle .

era:MinAxleLoadVehicleCategoryShape a sh:NodeShape ;
               sh:property era-sh:MinAxleLoadVehicleCategory ;
               sh:targetClass era:MinAxleLoadVehicleCategory .

era:MinVehicleImpedanceShape a sh:NodeShape ;
               sh:property era-sh:MinVehicleImpedanceVoltages,
               era-sh:MinVehicleInputCapacitance,
               era-sh:MinVehicleInputImpedance ;
               sh:targetClass era:MinVehicleImpedance .

era:SectionOfLineShape a sh:NodeShape ;
               sh:property era-sh:NationalLine,
               era-sh:OpEnd,
               era-sh:OpStart,
               era-sh:OperationalRegimeType,
               era-sh:SolNature,
               era-sh:ExistanceOfIndustrialRisk,
               era-sh:LengthOfSectionOfLine ;
               sh:targetClass era:SectionOfLine .

era:ParameterShape a sh:NodeShape ;
               sh:property era-sh:NotApplicable ;
               sh:targetClass era:Parameter .

era:ParameterApplicabilityShape a sh:NodeShape ;
               sh:property era-sh:OfParameter,
               era-sh:ParameterValueType,
               era-sh:ParameterValue ;
               sh:targetClass era:ParameterApplicability .

era:LinearElementSectionShape a sh:NodeShape ;
               sh:property era-sh:OnElement,
               era-sh:AppliesInBothDirections,
               era-sh:EndOffsetFromOrigin,
               era-sh:StartOffsetFromOrigin ;
               sh:targetClass era:LinearElementSection .

era:TopologicalCoordinateShape a sh:NodeShape ;
               sh:property era-sh:OnLinearElement,
               era-sh:OffsetFromOrigin ;
               sh:targetClass era:TopologicalCoordinate .

era:PlatformEdgeShape a sh:NodeShape ;
               sh:property era-sh:PlatformHeight,
               era-sh:AreaBoardingAid,
               era-sh:AssistanceStartingTrain,
               era-sh:LengthOfPlatform,
               era-sh:PlatformId ;
               sh:targetClass era:PlatformEdge .

era:RadioBlockCenterShape a sh:NodeShape ;
               sh:property era-sh:ReasonsEtcsRadioBlockCenterReject,
               era-sh:RbcID,
               era-sh:RbcPhone ;
               sh:targetClass era:RadioBlockCenter .

era:BodyShape a sh:NodeShape ;
               sh:property era-sh:Role ;
               sh:targetClass era:Body .

era:SpecialAreaShape a sh:NodeShape ;
               sh:property era-sh:SpecialAreaType ;
               sh:targetClass era:SpecialArea .

era:CertificateShape a sh:NodeShape ;
               sh:property era-sh:State ;
               sh:targetClass era:Certificate .

era:FeatureShape a sh:NodeShape ;
               sh:property era-sh:Validity,
               era-sh:CurrentlyValid ;
               sh:targetClass era:Feature .

era:ReferenceBorderPointShape a sh:NodeShape ;
               sh:property era-sh:BorderPointId ;
               sh:targetClass era:ReferenceBorderPoint .

era:PrimaryLocationShape a sh:NodeShape ;
               sh:property era-sh:ContainerHandlingFlag,
               era-sh:FreightFlag,
               era-sh:HandoverPointFlag,
               era-sh:PrimaryLocationCode,
               era-sh:PrimaryLocationName ;
               sh:targetClass era:PrimaryLocation .

era:DocumentShape a sh:NodeShape ;
               sh:property era-sh:DocumentUrl ;
               sh:targetClass era:Document .

era:SidingShape a sh:NodeShape ;
               sh:property era-sh:Gradient,
               era-sh:HasElectricShoreSupply,
               era-sh:HasExternalCleaning,
               era-sh:HasRefuelling,
               era-sh:HasSandRestocking,
               era-sh:HasToiletDischarge,
               era-sh:HasWaterRestocking,
               era-sh:LengthOfSiding,
               era-sh:SidingId ;
               sh:targetClass era:Siding .

era:LinearElementShape a sh:NodeShape ;
               sh:property era-sh:LengthOfNetLinearElement ;
               sh:targetClass era:LinearElement .

era:MaximumMagneticFieldShape a sh:NodeShape ;
               sh:property era-sh:MaximumMagneticFieldDirectionX,
               era-sh:MaximumMagneticFieldDirectionY,
               era-sh:MaximumMagneticFieldDirectionZ ;
               sh:targetClass era:MaximumMagneticField .

era:MinimumVerticalRadiusShape a sh:NodeShape ;
               sh:property era-sh:MinimumVerticalRadiusCrest,
               era-sh:MinimumVerticalRadiusHollow ;
               sh:targetClass era:MinimumVerticalRadius .

era:PhaseInfoShape a sh:NodeShape ;
               sh:property era-sh:PhaseInfoChangeSupplySystem,
               era-sh:PhaseInfoDistanceType,
               era-sh:PhaseInfoKm,
               era-sh:PhaseInfoLength,
               era-sh:PhaseInfoPantographLowered,
               era-sh:PhaseInfoSwitchOffBreaker ;
               sh:targetClass era:PhaseInfo .

era:RaisedPantographsDistanceAndSpeedShape a sh:NodeShape ;
               sh:property era-sh:RaisedPantographsDistance,
               era-sh:RaisedPantographsNumber,
               era-sh:RaisedPantographsSpeed ;
               sh:targetClass era:RaisedPantographsDistanceAndSpeed .

era:SystemSeparationInfoShape a sh:NodeShape ;
               sh:property era-sh:SystemSeparationInfoChangeSupplySystem,
               era-sh:SystemSeparationInfoKm,
               era-sh:SystemSeparationInfoLength,
               era-sh:SystemSeparationInfoPantographLowered,
               era-sh:SystemSeparationInfoSwitchOffBreaker ;
               sh:targetClass era:SystemSeparationInfo .

era:MaximumSpeedAndCantDeficiencyShape a sh:NodeShape ;
               sh:property era-sh:VehicleTypeMaximumCantDeficiency,
               era-sh:VehicleTypeMaximumSpeed ;
               sh:targetClass era:MaximumSpeedAndCantDeficiency .













era-sh:AccelerationLevelCrossing a sh:PropertyShape ;
    era:affectedClass era:Track ;
    era:affectedProperty era:accelerationLevelCrossing ;
    era:rinfIndex "1.1.1.1.7.3" ;
    era:scope "local" ;
    rdfs:comment "Existence of limit for acceleration of train if stopping or recovering speed close to a level crossing expressed in a specific reference acceleration curve."@en ;
    # Source: ontology comment for accelerationLevelCrossing

    sh:class era:Document ;
    # Source: ontology range is http://data.europa.eu/949/Document

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "accelerationLevelCrossing (1.1.1.1.7.3): The track or subset with common characteristics must have at most one acceleration level crossing value that is a Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ;
    # Source: RAG indicates that the value must be a document reference

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to other identifiable documents (IRI references)

    sh:path era:accelerationLevelCrossing ;

    sh:severity sh:Violation .

era-sh:AccelerationLevelCrossingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Track ;
    era:affectedProperty era:accelerationLevelCrossing ;
    era:rinfIndex "1.1.1.1.7.3" ;
    era:scope "local" ;
    rdfs:comment "Applicable only when selected value of parameter 1.1.1.1.7.2 is ‘Y’."@en ;
    # Source: RAG applicability note

    sh:message "accelerationLevelCrossing (1.1.1.1.7.3): This error is due to the track or subset with common characteristics {?trackLabel}, violating the rule: Applicable only when selected value of parameter 1.1.1.1.7.2 is ‘Y’."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:accelerationLevelCrossing ?value } .
        FILTER EXISTS { $this era:hasLevelCrossings ?levelCrossingValue . FILTER(?levelCrossingValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AdditionalBrakingInformationDocument a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:additionalBrakingInformationDocument ; 
    era:rinfIndex "1.1.1.3.11.3" ; 
    era:scope "local" ; 

    rdfs:comment "Electronic document available in two EU languages from the IM stored by the Agency providing additional information as defined in point (2) of point 4.2.2.6.2 of TSI OPE."@en ; 
    # Source: ontology comment for additionalBrakingInformationDocument

    sh:class era:Document ; 
    # Source: ontology range is http://data.europa.eu/949/Document

    sh:maxCount 1 ; 
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "additionalBrakingInformationDocument (1.1.1.3.11.3): The CommonCharacteristicsSubset must have at most one additional braking information document that is a Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ; 
    # Source: RAG indicates that the value must be a document reference

    sh:nodeKind sh:IRI ; 
    # Inferred: links must refer to other identifiable documents (IRI references)

    sh:path era:additionalBrakingInformationDocument ; 

    sh:severity sh:Violation .

era-sh:AdditionalBrakingInformationDocumentApplicability a sh:SPARQLConstraint ; 
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:additionalBrakingInformationDocument ; 
    era:rinfIndex "1.1.1.3.11.3" ; 
    era:scope "local" ; 

    rdfs:comment "Applicable only when for parameter 1.1.1.3.11.2 True is selected."@en ; 
    # Source: RAG applicability note

    sh:message "additionalBrakingInformationDocument (1.1.1.3.11.3): This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when for parameter 1.1.1.3.11.2 True is selected."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:additionalBrakingInformationDocument ?value } .
        FILTER EXISTS { $this era:brakeRelatedObjParameter ?brakeValue . FILTER(?brakeValue = "True") } .
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:Aggregates a sh:PropertyShape ;
    era:affectedClass era:NonLinearElement ;
    era:affectedProperty era:aggregates ;
    era:scope "local" ;

    rdfs:comment "Specifies the linear elements aggregated by a non-linear element."@en ;
    # Source: ontology comment for aggregates

    sh:class era:LinearElement ;
    # Source: ontology range is http://data.europa.eu/949/LinearElement

    sh:maxCount 1 ;
    # Inferred: typically, a non-linear element aggregates a single linear element, based on the nature of aggregation.

    sh:message "aggregates: The NonLinearElement must have at most one aggregates reference that is an instance of LinearElement. This error may be due to having more than one value or having a value that is not an instance of LinearElement."@en ;
    # Source: inferred from the nature of aggregation

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable linear elements (IRI references)

    sh:path era:aggregates ;

    sh:severity sh:Violation .

era-sh:AggregatesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:NonLinearElement ;
    era:affectedProperty era:aggregates ;
    era:scope "local" ;

    rdfs:comment "Applicable when the non-linear element is defined and is expected to aggregate linear elements."@en ;
    # Source: inferred from the definition of NonLinearElement

    sh:message "aggregates: This error is due to the NonLinearElement {?elementLabel}, violating the rule: Aggregates must be defined for non-linear elements."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?elementLabel WHERE {
        $this rdfs:label ?elementLabel .
        FILTER NOT EXISTS { $this era:aggregates ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AllocationCompany a sh:PropertyShape ;
    era:affectedClass era:SubsidiaryLocation ;
    era:affectedProperty era:allocationCompany ;
    era:scope "local" ;

    rdfs:comment "The organisation in charge to allocate the code for the subsidiary location."@en ;
    # Source: ontology comment for allocationCompany

    sh:class era:OrganisationRole ;
    # Source: ontology range is http://data.europa.eu/949/OrganisationRole

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "allocationCompany: The SubsidiaryLocation must have at most one allocation company value that is an instance of OrganisationRole. This error may be due to having more than one value or having a value that is not an instance of OrganisationRole."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable organisation roles (IRI references)

    sh:path era:allocationCompany ;

    sh:severity sh:Violation .

era-sh:AllocationCompanyApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SubsidiaryLocation ;
    era:affectedProperty era:allocationCompany ;
    era:scope "local" ;

    rdfs:comment "Applicable when the subsidiary location is defined and requires an allocation company."@en ;
    # Source: inferred from the definition of SubsidiaryLocation and its relationship to allocation

    sh:message "allocationCompany: This error is due to the SubsidiaryLocation {?locationLabel}, violating the rule: An allocation company must be defined for the subsidiary location."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?locationLabel WHERE {
        $this rdfs:label ?locationLabel .
        FILTER NOT EXISTS { $this era:allocationCompany ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AppliesToDirection a sh:PropertyShape ;
    era:affectedClass era:NetPointReference ;
    era:affectedProperty era:appliesToDirection ;
    era:scope "local" ;

    rdfs:comment "The direction relative to the origin of the linear element towards the point reference applies."@en ;
    # Source: ontology comment for appliesToDirection

    sh:class geosparql:Concept ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "appliesToDirection: The NetPointReference must have at most one appliesToDirection value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:appliesToDirection ;

    sh:severity sh:Violation .

era-sh:AppliesToDirectionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:NetPointReference ;
    era:affectedProperty era:appliesToDirection ;
    era:scope "local" ;

    rdfs:comment "Applicable when the NetPointReference is defined and requires a direction to be specified."@en ;
    # Source: inferred from the definition of NetPointReference and its relationship to direction

    sh:message "appliesToDirection: This error is due to the NetPointReference {?pointLabel}, violating the rule: A direction must be defined for the NetPointReference."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?pointLabel WHERE {
        $this rdfs:label ?pointLabel .
        FILTER NOT EXISTS { $this era:appliesToDirection ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AtoCommunicationSystem a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:atoCommunicationSystem ; 
    era:rinfIndex "1.1.1.3.13.3" ; 
    era:scope "local" ; 

    rdfs:comment "Supported ATO communication systems from trackside."@en ; 
    # Source: ontology comment for atoCommunicationSystem

    sh:class geosparql:Concept ; 
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ; 
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "atoCommunicationSystem: The CommonCharacteristicsSubset must have at most one ATO communication system value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ; 
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ; 
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:atoCommunicationSystem ; 

    sh:severity sh:Violation .

era-sh:AtoCommunicationSystemApplicability a sh:SPARQLConstraint ; 
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:atoCommunicationSystem ; 
    era:rinfIndex "1.1.1.3.13.3" ; 
    era:scope "local" ; 

    rdfs:comment "Applicable when 'ATO Grade of Automation' is applicable."@en ; 
    # Source: RAG dependency note

    sh:message "atoCommunicationSystem: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: ATO communication system must be defined when 'ATO Grade of Automation' is applicable."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:atoCommunicationSystem ?value } .
        FILTER EXISTS { $this era:atoGradeAutomation ?gradeValue . FILTER(?gradeValue = "Y") } .
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:AtoGradeAutomation a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:atoGradeAutomation ;
    era:rinfIndex "1.1.1.3.13.1" ;
    era:scope "local" ;

    rdfs:comment "ATO grade of automation installed lineside."@en ;
    # Source: ontology comment for atoGradeAutomation

    sh:class geosparql:Concept ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "atoGradeAutomation: The CommonCharacteristicsSubset must have at most one ATO grade of automation value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:atoGradeAutomation ;

    sh:severity sh:Violation .

era-sh:AtoGradeAutomationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:atoGradeAutomation ;
    era:rinfIndex "1.1.1.3.13.1" ;
    era:scope "local" ;

    rdfs:comment "Parameter only applicable when ETCS Baseline > 4 MR1 and ATO is implemented."@en ;
    # Source: RAG dependency note

    sh:message "atoGradeAutomation: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: ATO Grade of Automation must be defined when ETCS Baseline > 4 MR1 and ATO is implemented."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:atoGradeAutomation ?value } .
        FILTER EXISTS { $this era:etcsBaseline ?baselineValue . FILTER(?baselineValue > 4) } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AtoSystemVersion a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:atoSystemVersion ;
    era:rinfIndex "1.1.1.3.13.2" ;
    era:scope "local" ;

    rdfs:comment "ATO system version according to the specification referenced in Appendix A-1, index [C]."@en ;
    # Source: ontology comment for atoSystemVersion

    sh:class geosparql:Concept ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "atoSystemVersion: The CommonCharacteristicsSubset must have at most one ATO system version value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:atoSystemVersion ;

    sh:severity sh:Violation .

era-sh:AtoSystemVersionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:atoSystemVersion ;
    era:rinfIndex "1.1.1.3.13.2" ;
    era:scope "local" ;

    rdfs:comment "Applicable when 'ATO Grade of Automation' is applicable."@en ;
    # Source: RAG dependency note

    sh:message "atoSystemVersion: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: ATO system version must be defined when 'ATO Grade of Automation' is applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:atoSystemVersion ?value } .
        FILTER EXISTS { $this era:atoGradeAutomation ?gradeValue . FILTER(?gradeValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AuthorizedCountry a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:authorizedCountry ;
    era:scope "local" ;

    rdfs:comment "Indicates the country(ies) in where a vehicle type has been authorized to operate."@en ;
    # Source: ontology comment for authorizedCountry

    sh:class geosparql:Concept ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "authorizedCountry: The VehicleType must have at most one authorized country value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:authorizedCountry ;

    sh:severity sh:Violation .

era-sh:AuthorizedCountryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:authorizedCountry ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires an authorized country."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to authorization

    sh:message "authorizedCountry: This error is due to the VehicleType {?vehicleLabel}, violating the rule: An authorized country must be defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:authorizedCountry ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AxleBearingConditionMonitoring a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:axleBearingConditionMonitoring ;
    era:rinfIndex "4.9.2" ;
    era:scope "local" ;

    rdfs:comment "Axle bearing condition monitoring."@en ;
    # Source: ontology comment for axleBearingConditionMonitoring

    sh:class geosparql:Concept ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "axleBearingConditionMonitoring: The VehicleType must have at most one axle bearing condition monitoring value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:axleBearingConditionMonitoring ;

    sh:severity sh:Violation .

era-sh:AxleBearingConditionMonitoringApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:axleBearingConditionMonitoring ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires axle bearing condition monitoring."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to axle bearing condition monitoring

    sh:message "axleBearingConditionMonitoring: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Axle bearing condition monitoring must be defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:axleBearingConditionMonitoring ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:BelongsTo a sh:PropertyShape ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:belongsTo ;
    era:scope "local" ;

    rdfs:comment "Indicates that an infrastructure element belongs to a certain subset that contains common characteristics."@en ;
    # Source: ontology comment for belongsTo

    sh:class era:CommonCharacteristicsSubset ;
    # Source: ontology range is http://data.europa.eu/949/CommonCharacteristicsSubset

    sh:maxCount 1 ;
    # Source: inferred: typically, an infrastructure element belongs to a single subset.

    sh:message "belongsTo: The InfrastructureElement must have at most one belongsTo reference that is an instance of CommonCharacteristicsSubset. This error may be due to having more than one value or having a value that is not an instance of CommonCharacteristicsSubset."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable subsets (IRI references)

    sh:path era:belongsTo ;

    sh:severity sh:Violation .

era-sh:BelongsToApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:belongsTo ;
    era:scope "local" ;

    rdfs:comment "Applicable when the InfrastructureElement is defined and requires a belonging subset."@en ;
    # Source: inferred from the definition of InfrastructureElement and its relationship to subsets

    sh:message "belongsTo: This error is due to the InfrastructureElement {?elementLabel}, violating the rule: A belonging subset must be defined for the infrastructure element."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?elementLabel WHERE {
        $this rdfs:label ?elementLabel .
        FILTER NOT EXISTS { $this era:belongsTo ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:BorderPointOf a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:borderPointOf ;
    era:scope "local" ;

    rdfs:comment "Relates the information that each country maintains on a border point, to the operational point that represents the general information on the border point."@en ;
    # Source: ontology comment for borderPointOf

    sh:class era:OperationalPoint ;
    # Source: ontology range is http://data.europa.eu/949/OperationalPoint

    sh:maxCount 1 ;
    # Source: inferred: typically, an operational point can only have one border point relationship.

    sh:message "borderPointOf: The OperationalPoint must have at most one border point of reference that is an instance of OperationalPoint. This error may be due to having more than one value or having a value that is not an instance of OperationalPoint."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable operational points (IRI references)

    sh:path era:borderPointOf ;

    sh:severity sh:Violation .

era-sh:BorderPointOfApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:borderPointOf ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OperationalPoint is defined as a border point."@en ;
    # Source: inferred from the RAG indicating the necessity of bilateral agreements for border points.

    sh:message "borderPointOf: This error is due to the OperationalPoint {?pointLabel}, violating the rule: A border point must be defined for the operational point."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?pointLabel WHERE {
        $this rdfs:label ?pointLabel .
        FILTER NOT EXISTS { $this era:borderPointOf ?value } .
        # Additional conditions can be added here based on specific requirements for border points
    }
    """ ;

    sh:severity sh:Violation .

era-sh:CantDeficiencyBasicSSP a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:cantDeficiencyBasicSSP ;
    era:scope "local" ;

    rdfs:comment "Essential information for railway undertakings with a worse (lower) tolerated cant deficiency than those for which the ETCS trackside provides SSP (Static Speed Profiles) in conjunction with parameter 'Other Cant Deficiency train categories for which the ETCS trackside is configured to provide SSP'."@en ;
    # Source: ontology comment for cantDeficiencyBasicSSP

    sh:class geosparql:Concept ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "cantDeficiencyBasicSSP: The CommonCharacteristicsSubset must have at most one cant deficiency basic SSP value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:cantDeficiencyBasicSSP ;

    sh:severity sh:Violation .

era-sh:CantDeficiencyBasicSSPApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:cantDeficiencyBasicSSP ;
    era:scope "local" ;

    rdfs:comment "Applicable when ETCS is present and a value for cant deficiency must be provided."@en ;
    # Source: RAG dependency note

    sh:message "cantDeficiencyBasicSSP: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A value for cant deficiency basic SSP must be defined when ETCS is present."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:cantDeficiencyBasicSSP ?value } .
        FILTER EXISTS { $this era:etcsLevelType ?etcsValue . FILTER(?etcsValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:CarrierLinearElement a sh:PropertyShape ;
    era:affectedClass era:Orientation ;
    era:affectedProperty era:carrierLinearElement ;
    era:scope "local" ;

    rdfs:comment "The carrier linear element in relation to which the orientation is based on."@en ;
    # Source: ontology comment for carrierLinearElement

    sh:class era:LinearElement ;
    # Source: ontology range is http://data.europa.eu/949/LinearElement

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "carrierLinearElement: The Orientation must have at most one carrier linear element value that is an instance of LinearElement. This error may be due to having more than one value or having a value that is not an instance of LinearElement."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable linear elements (IRI references)

    sh:path era:carrierLinearElement ;

    sh:severity sh:Violation .

era-sh:CarrierLinearElementApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Orientation ;
    era:affectedProperty era:carrierLinearElement ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Orientation is defined and requires a carrier linear element."@en ;
    # Source: inferred from the definition of Orientation and its relationship to carrier linear elements

    sh:message "carrierLinearElement: This error is due to the Orientation {?orientationLabel}, violating the rule: A carrier linear element must be defined for the orientation."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?orientationLabel WHERE {
        $this rdfs:label ?orientationLabel .
        FILTER NOT EXISTS { $this era:carrierLinearElement ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Category a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:category ;
    era:scope "local" ;

    rdfs:comment "(Deprecated) Vehicle category."@en ;
    # Source: ontology comment for category

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "category: The VehicleType must have at most one category value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:category ;

    sh:severity sh:Violation .

era-sh:CategoryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:category ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires a category."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to categories

    sh:message "category: This error is due to the VehicleType {?vehicleLabel}, violating the rule: A category must be defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:category ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Certificate a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:certificate ;
    era:scope "local" ;

    rdfs:comment "For this Vehicle Type, the type or design examination certificate described in the relevant verification module as issued by Notified Bodies, supporting the EC Declaration(s) of Verification for the subsystems in scope of the type's authorisation by an authorizing entity."@en ;
    # Source: ontology comment for certificate

    sh:class era:Certificate ;
    # Source: ontology range is http://data.europa.eu/949/Certificate

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "certificate: The VehicleType must have at most one certificate value that is an instance of Certificate. This error may be due to having more than one value or having a value that is not an instance of Certificate."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable certificates (IRI references)

    sh:path era:certificate ;

    sh:severity sh:Violation .

era-sh:CertificateApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:certificate ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires a certificate."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to certificates

    sh:message "certificate: This error is due to the VehicleType {?vehicleLabel}, violating the rule: A certificate must be defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:certificate ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:CompatibilityProcedureDocument a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:compatibilityProcedureDocument ; 
    era:rinfIndex "1.1.1.1.2.4.4" ; 
    era:scope "local" ; 

    rdfs:comment "Electronic document available in two EU languages from the IM stored by the Agency with precise procedures for the static and dynamic route compatibility checks."@en ; 
    # Source: ontology comment for compatibilityProcedureDocument

    sh:class era:Document ; 
    # Source: ontology range is http://data.europa.eu/949/Document

    sh:maxCount 1 ; 
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "compatibilityProcedureDocument: The CommonCharacteristicsSubset must have at most one compatibility procedure document value that is a Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ; 
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ; 
    # Inferred: links must refer to identifiable documents (IRI references)

    sh:path era:compatibilityProcedureDocument ; 

    sh:severity sh:Violation .

era-sh:CompatibilityProcedureDocumentApplicability a sh:SPARQLConstraint ; 
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:compatibilityProcedureDocument ; 
    era:rinfIndex "1.1.1.1.2.4.4" ; 
    era:scope "local" ; 

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a compatibility procedure document."@en ; 
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to compatibility procedures

    sh:message "compatibilityProcedureDocument: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A compatibility procedure document must be defined for the subset."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:compatibilityProcedureDocument ?value } .
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:ConditionalRegenerativeBrake a sh:PropertyShape ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:conditionalRegenerativeBrake ;
    era:rinfIndex "1.1.1.2.2.4" ;
    era:scope "local" ;

    rdfs:comment "Indication whether regenerative braking is permitted, not permitted, or permitted under specific conditions."@en ;
    # Source: ontology comment for conditionalRegenerativeBrake

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "conditionalRegenerativeBrake: The ContactLineSystem must have at most one conditional regenerative brake value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:conditionalRegenerativeBrake ;

    sh:severity sh:Violation .

era-sh:ConditionalRegenerativeBrakeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:conditionalRegenerativeBrake ;
    era:scope "local" ;

    rdfs:comment "Applicable when the ContactLineSystem is defined and requires a conditional regenerative brake."@en ;
    # Source: inferred from the definition of ContactLineSystem and its relationship to regenerative braking

    sh:message "conditionalRegenerativeBrake: This error is due to the ContactLineSystem {?systemLabel}, violating the rule: A conditional regenerative brake must be defined for the contact line system."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:conditionalRegenerativeBrake ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ConditionsAppliedRegenerativeBraking a sh:PropertyShape ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:conditionsAppliedRegenerativeBraking ;
    era:rinfIndex "1.1.1.2.2.4.1" ;
    era:scope "local" ;

    rdfs:comment "Name and/or reference of the document specifying the conditions applying in regards to regenerative braking."@en ;
    # Source: ontology comment for conditionsAppliedRegenerativeBraking

    sh:class era:Document ;
    # Source: ontology range is http://data.europa.eu/949/Document

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "conditionsAppliedRegenerativeBraking: The ContactLineSystem must have at most one conditions applied regenerative braking value that is a Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable documents (IRI references)

    sh:path era:conditionsAppliedRegenerativeBraking ;

    sh:severity sh:Violation .

era-sh:ConditionsAppliedRegenerativeBrakingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:conditionsAppliedRegenerativeBraking ;
    era:scope "local" ;

    rdfs:comment "Mandatory when parameter 1.1.1.2.2.4 announces Conditions."@en ;
    # Source: RAG dependency note

    sh:message "conditionsAppliedRegenerativeBraking: This error is due to the ContactLineSystem {?systemLabel}, violating the rule: Conditions applied regenerative braking must be defined when conditions are announced."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:conditionsAppliedRegenerativeBraking ?value } .
        FILTER EXISTS { $this era:permissionForRegenerativeBraking ?permValue . FILTER(?permValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ConditionsSwitchClassBSystems a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:conditionsSwitchClassBSystems ;
    era:rinfIndex "1.1.1.3.8.1" ;
    era:scope "local" ;

    rdfs:comment "Name and/or reference of the document specifying the Special technical conditions required to switch over between ERTMS/ETCS and Class B systems."@en ;
    # Source: ontology comment for conditionsSwitchClassBSystems

    sh:class era:Document ;
    # Source: ontology range is http://data.europa.eu/949/Document

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "conditionsSwitchClassBSystems: The CommonCharacteristicsSubset must have at most one conditions switch Class B systems value that is a Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable documents (IRI references)

    sh:path era:conditionsSwitchClassBSystems ;

    sh:severity sh:Violation .

era-sh:ConditionsSwitchClassBSystemsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:conditionsSwitchClassBSystems ;
    era:scope "local" ;

    rdfs:comment "Applicable when at least two different classes of protection systems exist."@en ;
    # Source: RAG dependency note

    sh:message "conditionsSwitchClassBSystems: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Conditions for switching Class B systems must be defined when at least two different classes of protection systems exist."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:conditionsSwitchClassBSystems ?value } .
        # Additional conditions can be added here based on specific requirements for protection systems
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ConditionsUseReflectivePlates a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:conditionsUseReflectivePlates ;
    era:rinfIndex "1.1.1.1.7.12.1" ;
    era:scope "local" ;

    rdfs:comment "Details of any conditions for using the reflective plates on freight corridors. Specific case for Portugal and Spain until 1.1.2025 and Belgium and France until 1.1.2026."@en ;
    # Source: ontology comment for conditionsUseReflectivePlates

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "conditionsUseReflectivePlates: The CommonCharacteristicsSubset must have at most one conditions use reflective plates value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:conditionsUseReflectivePlates ;

    sh:severity sh:Violation .

era-sh:ConditionsUseReflectivePlatesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:conditionsUseReflectivePlates ;
    era:rinfIndex "1.1.1.1.7.12.1" ;
    era:scope "local" ;

    rdfs:comment "A value is mandatory if the value of parameter 'Permit of use of reflective plates' is boolean 'Yes'."@en ;
    # Source: RAG dependency note

    sh:message "conditionsUseReflectivePlates: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A value for conditions use of reflective plates must be defined when the permit of use is 'Yes'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:conditionsUseReflectivePlates ?value } .
        FILTER EXISTS { $this era:permitOfUseReflectivePlates ?permitValue . FILTER(?permitValue = "Yes") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ConnectedTo a sh:PropertyShape ;
    era:affectedClass era:Track ;
    era:affectedProperty era:connectedTo ;
    era:scope "local" ;

    rdfs:comment "Represents a bidirectional connection between two Track instances."@en ;
    # Source: ontology comment for connectedTo

    sh:class era:Track ;
    # Source: ontology range is http://data.europa.eu/949/Track

    sh:maxCount 1 ;
    # Inferred: typically, a track can only have one connection relationship to another track.

    sh:message "connectedTo: The Track must have at most one connectedTo reference that is an instance of Track. This error may be due to having more than one value or having a value that is not an instance of Track."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable track instances (IRI references)

    sh:path era:connectedTo ;

    sh:severity sh:Violation .

era-sh:ConnectedToApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Track ;
    era:affectedProperty era:connectedTo ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Track is defined and requires a connection to another track."@en ;
    # Source: inferred from the definition of connectedTo and its relationship to track connectivity

    sh:message "connectedTo: This error is due to the Track {?trackLabel}, violating the rule: A connection must be defined for the track."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:connectedTo ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ContactLineSystem a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:contactLineSystem ;
    era:scope "local" ;

    rdfs:comment "System that is used to transmit electrical energy to road or rail vehicles."@en ;
    # Source: ontology comment for contactLineSystem

    sh:class era:ContactLineSystem ;
    # Source: ontology range is http://data.europa.eu/949/ContactLineSystem

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "contactLineSystem: The CommonCharacteristicsSubset must have at most one contact line system value that is an instance of ContactLineSystem. This error may be due to having more than one value or having a value that is not an instance of ContactLineSystem."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable contact line systems (IRI references)

    sh:path era:contactLineSystem ;

    sh:severity sh:Violation .

era-sh:ContactLineSystemApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:contactLineSystem ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a contact line system."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to contact line systems

    sh:message "contactLineSystem: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A contact line system must be defined for the subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:contactLineSystem ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ContactLineSystemType a sh:PropertyShape ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:contactLineSystemType ;
    era:scope "local" ;

    rdfs:comment "Indication of the type of the contact line system."@en ;
    # Source: ontology comment for contactLineSystemType

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "contactLineSystemType: The ContactLineSystem must have at most one contact line system type value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:contactLineSystemType ;

    sh:severity sh:Violation .

era-sh:ContactLineSystemTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:contactLineSystemType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the ContactLineSystem is defined and requires a contact line system type."@en ;
    # Source: inferred from the definition of ContactLineSystem and its relationship to contact line system types

    sh:message "contactLineSystemType: This error is due to the ContactLineSystem {?systemLabel}, violating the rule: A contact line system type must be defined for the contact line system."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:contactLineSystemType ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ContactStripMaterial a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:contactStripMaterial ;
    era:rinfIndex "4.10.10" ;
    era:scope "local" ;

    rdfs:comment "Indication of which contact strip materials are permitted to be used."@en ;
    # Source: ontology comment for contactStripMaterial

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "contactStripMaterial: The CommonCharacteristicsSubset must have at most one contact strip material value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:contactStripMaterial ;

    sh:severity sh:Violation .

era-sh:ContactStripMaterialApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:contactStripMaterial ;
    era:rinfIndex "4.10.10" ;
    era:scope "local" ;

    rdfs:comment "This parameter is applicable only if 'Overhead contact line (OCL)' is selected in 1.1.1.2.2.1.1."@en ;
    # Source: RAG explanation on applicability

    sh:message "contactStripMaterial: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A contact strip material must be defined when 'Overhead contact line (OCL)' is selected."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:contactStripMaterial ?value } .
        # Additional condition to check if OCL is selected can be added here
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Contains a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:contains ;
    era:scope "local" ;

    rdfs:comment "Indicates that a subset with common characteristics contains a certain infrastructure element."@en ;
    # Source: ontology comment for contains

    sh:class era:InfrastructureElement ;
    # Source: ontology range is http://data.europa.eu/949/InfrastructureElement

    sh:maxCount 1 ;
    # Source: inferred: typically, a subset can contain at most one instance of an infrastructure element.

    sh:message "contains: The CommonCharacteristicsSubset must have at most one contains reference that is an instance of InfrastructureElement. This error may be due to having more than one value or having a value that is not an instance of InfrastructureElement."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable infrastructure elements (IRI references)

    sh:path era:contains ;

    sh:severity sh:Violation .

era-sh:ContainsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:contains ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to an infrastructure element."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to contains

    sh:message "contains: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A contains reference must be defined for the subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:contains ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DataGSMRNetwork a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:dataGSMRNetwork ;
    era:rinfIndex "4.13.2.11" ;
    era:scope "local" ;

    rdfs:comment "Data SIM Card GSM-R Home Network."@en ;
    # Source: ontology comment for dataGSMRNetwork

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "dataGSMRNetwork: The VehicleType must have at most one data GSM-R network value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:dataGSMRNetwork ;

    sh:severity sh:Violation .

era-sh:DataGSMRNetworkApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:dataGSMRNetwork ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires a data GSM-R network."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to GSM-R networks

    sh:message "dataGSMRNetwork: This error is due to the VehicleType {?vehicleLabel}, violating the rule: A data GSM-R network must be defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:dataGSMRNetwork ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DataRadioCompatible a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:dataRadioCompatible ;
    era:rinfIndex "1.1.1.3.3.10" ;
    era:scope "local" ;

    rdfs:comment "Radio requirements used for demonstrating technical compatibility data."@en ;
    # Source: ontology comment for dataRadioCompatible

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "dataRadioCompatible: The VehicleType must have at most one data radio compatible value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:dataRadioCompatible ;

    sh:severity sh:Violation .

era-sh:DataRadioCompatibleApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:dataRadioCompatible ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires data radio compatibility."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to radio compatibility

    sh:message "dataRadioCompatible: This error is due to the VehicleType {?vehicleLabel}, violating the rule: A data radio compatibility must be defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:dataRadioCompatible ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DefinesSubset a sh:PropertyShape ;
    era:affectedClass era:InfrastructureManager ;
    era:affectedProperty era:definesSubset ;
    era:scope "local" ;

    rdfs:comment "(deprecated) not in use anymore. For the purposes of the register of infrastructure, each infrastructure manager may describe its railway network optionally via common characteristic subsets."@en ;
    # Source: ontology comment for definesSubset

    sh:class era:CommonCharacteristicsSubset ;
    # Source: ontology range is http://data.europa.eu/949/CommonCharacteristicsSubset

    sh:maxCount 1 ;
    # Source: inferred: typically, an infrastructure manager can define at most one subset.

    sh:message "definesSubset: The InfrastructureManager must have at most one defines subset value that is an instance of CommonCharacteristicsSubset. This error may be due to having more than one value or having a value that is not an instance of CommonCharacteristicsSubset."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable common characteristic subsets (IRI references)

    sh:path era:definesSubset ;

    sh:severity sh:Violation .

era-sh:DefinesSubsetApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:InfrastructureManager ;
    era:affectedProperty era:definesSubset ;
    era:scope "local" ;

    rdfs:comment "Applicable when the InfrastructureManager is defined and requires a reference to a common characteristics subset."@en ;
    # Source: inferred from the definition of InfrastructureManager and its relationship to common characteristics subsets

    sh:message "definesSubset: This error is due to the InfrastructureManager {?managerLabel}, violating the rule: A defines subset reference must be defined for the infrastructure manager."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?managerLabel WHERE {
        $this rdfs:label ?managerLabel .
        FILTER NOT EXISTS { $this era:definesSubset ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DigitalSchematicOverview a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:digitalSchematicOverview ;
    era:rinfIndex "1.2.0.0.0.7.2" ;
    era:scope "local" ;

    rdfs:comment "Diagrammatic representation of the operational point in Well Known Text polyline."@en ;
    # Source: ontology comment for digitalSchematicOverview

    sh:datatype geosparql:wktLiteral ;
    # Source: ontology range is http://www.opengis.net/ont/geosparql#wktLiteral

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "digitalSchematicOverview: The OperationalPoint must have at most one digital schematic overview value that is a WKT literal. This error may be due to having more than one value or having a value that is not a WKT literal."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:Literal ;
    # Inferred: links must refer to identifiable WKT literals

    sh:path era:digitalSchematicOverview ;

    sh:severity sh:Violation .

era-sh:DigitalSchematicOverviewApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:digitalSchematicOverview ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OperationalPoint is defined and requires a digital schematic overview."@en ;
    # Source: inferred from the definition of OperationalPoint and its relationship to digital schematic overviews

    sh:message "digitalSchematicOverview: This error is due to the OperationalPoint {?opLabel}, violating the rule: A digital schematic overview must be defined for the operational point."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?opLabel WHERE {
        $this rdfs:label ?opLabel .
        FILTER NOT EXISTS { $this era:digitalSchematicOverview ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Direction a sh:PropertyShape ;
    era:affectedClass era:Orientation ;
    era:affectedProperty era:direction ;
    era:scope "local" ;

    rdfs:comment "The direction of the orientation of a railway element, in relation to the carrier linear element."@en ;
    # Source: ontology comment for direction

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "direction: The Orientation must have at most one direction value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:direction ;

    sh:severity sh:Violation .

era-sh:DirectionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Orientation ;
    era:affectedProperty era:direction ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Orientation is defined and requires a direction."@en ;
    # Source: inferred from the definition of Orientation and its relationship to direction

    sh:message "direction: This error is due to the Orientation {?orientationLabel}, violating the rule: A direction must be defined for the orientation."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?orientationLabel WHERE {
        $this rdfs:label ?orientationLabel .
        FILTER NOT EXISTS { $this era:direction ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DocumentRestrictionPositionContactLineSeparation a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:documentRestrictionPositionContactLineSeparation ; 
    era:rinfIndex "1.1.1.2.5.5" ; 
    era:scope "local" ; 

    rdfs:comment "Name and/or reference of the document specifying the restriction(s) related to the position of Multiple Traction unit(s) to comply with contact line separation."@en ; 
    # Source: ontology comment for documentRestrictionPositionContactLineSeparation

    sh:class era:Document ; 
    # Source: ontology range is http://data.europa.eu/949/Document

    sh:maxCount 1 ; 
    # Inferred: typically, a document reference can only be provided once for a specific restriction.

    sh:message "documentRestrictionPositionContactLineSeparation: The CommonCharacteristicsSubset must have at most one document restriction position contact line separation value that is an instance of Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ; 
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ; 
    # Inferred: links must refer to identifiable document instances (IRI references)

    sh:path era:documentRestrictionPositionContactLineSeparation ; 

    sh:severity sh:Violation .

era-sh:DocumentRestrictionPositionContactLineSeparationApplicability a sh:SPARQLConstraint ; 
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:documentRestrictionPositionContactLineSeparation ; 
    era:scope "local" ; 

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a document restriction related to contact line separation."@en ; 
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to document restrictions

    sh:message "documentRestrictionPositionContactLineSeparation: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A document restriction related to contact line separation must be defined for the subset."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:documentRestrictionPositionContactLineSeparation ?value } .
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:DocumentRestrictionPowerConsumption a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:documentRestrictionPowerConsumption ;
    era:rinfIndex "1.1.1.2.5.4" ;
    era:scope "local" ;

    rdfs:comment "Name and/or reference of the document specifying the restriction(s) related to power consumption of specific electric traction unit(s)."@en ;
    # Source: ontology comment for documentRestrictionPowerConsumption

    sh:class era:Document ;
    # Source: ontology range is http://data.europa.eu/949/Document

    sh:maxCount 1 ;
    # Inferred: typically, a document reference can only be provided once for a specific restriction.

    sh:message "documentRestrictionPowerConsumption: The CommonCharacteristicsSubset must have at most one document restriction power consumption value that is an instance of Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable document instances (IRI references)

    sh:path era:documentRestrictionPowerConsumption ;

    sh:severity sh:Violation .

era-sh:DocumentRestrictionPowerConsumptionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:documentRestrictionPowerConsumption ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a document restriction related to power consumption."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to document restrictions

    sh:message "documentRestrictionPowerConsumption: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A document restriction related to power consumption must be defined for the subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:documentRestrictionPowerConsumption ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EddyCurrentBraking a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:eddyCurrentBraking ;
    era:rinfIndex "1.1.1.1.6.2" ;
    era:scope "local" ;

    rdfs:comment "Indication of limitations on the use of eddy current brakes."@en ;
    # Source: ontology comment for eddyCurrentBraking

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "eddyCurrentBraking: The CommonCharacteristicsSubset must have at most one eddy current braking value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:eddyCurrentBraking ;

    sh:severity sh:Violation .

era-sh:EddyCurrentBrakingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:eddyCurrentBraking ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to eddy current braking."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to eddy current braking

    sh:message "eddyCurrentBraking: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: An eddy current braking reference must be defined for the subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:eddyCurrentBraking ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EddyCurrentBrakingConditionsDocument a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:eddyCurrentBrakingConditionsDocument ;
    era:rinfIndex "1.1.1.1.6.4" ;
    era:scope "local" ;

    rdfs:comment "Electronic document available in two EU languages from the IM stored by the Agency with conditions for the use of eddy current brakes identified in 1.1.1.1.6.2."@en ;
    # Source: ontology comment for eddyCurrentBrakingConditionsDocument

    sh:class era:Document ;
    # Source: ontology range is http://data.europa.eu/949/Document

    sh:maxCount 1 ;
    # Source: inferred: typically, a document reference can only be provided once for a specific condition.

    sh:message "eddyCurrentBrakingConditionsDocument: The CommonCharacteristicsSubset must have at most one eddy current braking conditions document value that is an instance of Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable document instances (IRI references)

    sh:path era:eddyCurrentBrakingConditionsDocument ;

    sh:severity sh:Violation .

era-sh:EddyCurrentBrakingConditionsDocumentApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:eddyCurrentBrakingConditionsDocument ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to the document with conditions for the use of eddy current brakes."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to eddy current braking conditions

    sh:message "eddyCurrentBrakingConditionsDocument: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: An eddy current braking conditions document reference must be defined for the subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:eddyCurrentBrakingConditionsDocument ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ElementA a sh:PropertyShape ;
    era:affectedClass era:NetRelation ;
    era:affectedProperty era:elementA ;
    era:rinfIndex "1.1.1.0.1.2" ;
    era:scope "local" ;

    rdfs:comment "References the first topological element in an oriented relation. For example, it may indicate the object that corresponds to a section of line, an operational point or a track inside an operational point connected at start."@en ;
    # Source: ontology comment for elementA

    sh:class era:LinearElement ;
    # Source: ontology range is http://data.europa.eu/949/LinearElement

    sh:maxCount 1 ;
    # Inferred: typically, a net relation can reference at most one linear element as the first element.

    sh:message "elementA: The NetRelation must have at most one elementA reference that is an instance of LinearElement. This error may be due to having more than one value or having a value that is not an instance of LinearElement."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable linear elements (IRI references)

    sh:path era:elementA ;

    sh:severity sh:Violation .

era-sh:ElementAApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:NetRelation ;
    era:affectedProperty era:elementA ;
    era:scope "local" ;

    rdfs:comment "Applicable when the NetRelation is defined and requires a reference to the first topological element."@en ;
    # Source: inferred from the definition of NetRelation and its relationship to elementA

    sh:message "elementA: This error is due to the NetRelation {?relationLabel}, violating the rule: A reference to the first topological element must be defined for the net relation."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?relationLabel WHERE {
        $this rdfs:label ?relationLabel .
        FILTER NOT EXISTS { $this era:elementA ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ElementB a sh:PropertyShape ;
    era:affectedClass era:NetRelation ;
    era:affectedProperty era:elementB ;
    era:rinfIndex "1.1.1.0.1.2" ;
    era:scope "local" ;

    rdfs:comment "References the second topological linear element in an oriented relation. For example, it may indicate the object that corresponds to a section of line, an operational point or a track inside an operational point connected at end."@en ;
    # Source: ontology comment for elementB

    sh:class era:LinearElement ;
    # Source: ontology range is http://data.europa.eu/949/LinearElement

    sh:maxCount 1 ;
    # Inferred: typically, a net relation can reference at most one linear element as the second element.

    sh:message "elementB: The NetRelation must have at most one elementB reference that is an instance of LinearElement. This error may be due to having more than one value or having a value that is not an instance of LinearElement."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable linear elements (IRI references)

    sh:path era:elementB ;

    sh:severity sh:Violation .

era-sh:ElementBApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:NetRelation ;
    era:affectedProperty era:elementB ;
    era:scope "local" ;

    rdfs:comment "Applicable when the NetRelation is defined and requires a reference to the second topological element."@en ;
    # Source: inferred from the definition of NetRelation and its relationship to elementB

    sh:message "elementB: This error is due to the NetRelation {?relationLabel}, violating the rule: A reference to the second topological element must be defined for the net relation."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?relationLabel WHERE {
        $this rdfs:label ?relationLabel .
        FILTER NOT EXISTS { $this era:elementB ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EndCouplingType a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:endCouplingType ;
    era:rinfIndex "4.9.1" ;
    era:scope "local" ;

    rdfs:comment "Type of end coupling (indicating tensile and compressive forces)."@en ;
    # Source: ontology comment for endCouplingType

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "endCouplingType: The VehicleType must have at most one end coupling type value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:endCouplingType ;

    sh:severity sh:Violation .

era-sh:EndCouplingTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:endCouplingType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires an end coupling type."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to end coupling types

    sh:message "endCouplingType: This error is due to the VehicleType {?vehicleLabel}, violating the rule: An end coupling type must be defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:endCouplingType ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EndLocation a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:endLocation ;
    era:rinfIndex "1.1.1.1.8.4" ;
    era:scope "local" ;

    rdfs:comment "Geographical coordinates in decimal degrees and km of the line at the end of a tunnel."@en ;
    # Source: ontology comment for endLocation

    sh:datatype xsd:string ;
    # Inferred: the data presentation indicates a predefined character string format.

    sh:maxCount 1 ;
    # Source: inferred: typically, a tunnel can have at most one end location.

    sh:message "endLocation: The Tunnel must have at most one end location value that is a predefined character string representing geographical coordinates. This error may be due to having more than one value or having a value that does not conform to the expected format."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:pattern "Latitude=\\d{1,2}\\.\\d{7} Longitude=([-+]?\\d{1,3}\\.\\d{7}) Kilometer=([-+]?\\d{1,3}\\.\\d{3})" ;
    # Inferred: pattern based on the data presentation format provided in the RAG.

    sh:path era:endLocation ;

    sh:severity sh:Violation .

era-sh:EndLocationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:endLocation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Tunnel is defined and requires an end location."@en ;
    # Source: inferred from the definition of Tunnel and its relationship to end location

    sh:message "endLocation: This error is due to the Tunnel {?tunnelLabel}, violating the rule: An end location must be defined for the tunnel."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:endLocation ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EndsAt a sh:PropertyShape ;
    era:affectedClass era:NetLinearReference ;
    era:affectedProperty era:endsAt ;
    era:scope "local" ;

    rdfs:comment "Specifies the ending coordinate of a linear reference."@en ;
    # Source: ontology comment for endsAt

    sh:class era:TopologicalCoordinate ;
    # Source: ontology range is http://data.europa.eu/949/TopologicalCoordinate

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "endsAt: The NetLinearReference must have at most one endsAt value that is an instance of TopologicalCoordinate. This error may be due to having more than one value or having a value that is not an instance of TopologicalCoordinate."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable topological coordinates (IRI references)

    sh:path era:endsAt ;

    sh:severity sh:Violation .

era-sh:EndsAtApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:NetLinearReference ;
    era:affectedProperty era:endsAt ;
    era:scope "local" ;

    rdfs:comment "Applicable when the NetLinearReference is defined and requires an ending coordinate."@en ;
    # Source: inferred from the definition of NetLinearReference and its relationship to endsAt

    sh:message "endsAt: This error is due to the NetLinearReference {?referenceLabel}, violating the rule: An ending coordinate must be defined for the linear reference."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?referenceLabel WHERE {
        $this rdfs:label ?referenceLabel .
        FILTER NOT EXISTS { $this era:endsAt ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EnergySupplySystem a sh:PropertyShape ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:energySupplySystem ;
    era:rinfIndex "4.10.1" ;
    era:scope "local" ;

    rdfs:comment "Indication of the traction supply system (nominal voltage and frequency)."@en ;
    # Source: ontology comment for energySupplySystem

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "energySupplySystem: The ContactLineSystem must have at most one energy supply system value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:energySupplySystem ;

    sh:severity sh:Violation .

era-sh:EnergySupplySystemApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:energySupplySystem ;
    era:scope "local" ;

    rdfs:comment "Applicable when the ContactLineSystem is defined and requires an energy supply system."@en ;
    # Source: inferred from the definition of ContactLineSystem and its relationship to energy supply systems

    sh:message "energySupplySystem: This error is due to the ContactLineSystem {?systemLabel}, violating the rule: An energy supply system must be defined for the contact line system."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:energySupplySystem ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ErrorCorrectionsOnboard a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:errorCorrectionsOnboard ; 
    era:rinfIndex "1.1.1.3.1.2" ; 
    era:scope "local" ; 

    rdfs:comment "List of unacceptable errors impacting the IM network that are required to be solved in the on-board according to the TSI CCS point 7.2.10.3 specification maintenance point (ETCS, GSM-R and/or ATO)."@en ; 
    # Source: ontology comment for errorCorrectionsOnboard

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ; 
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ; 
    # Inferred: typically, a property can only have one value in this context.

    sh:message "errorCorrectionsOnboard: The CommonCharacteristicsSubset must have at most one error corrections onboard value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ; 
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ; 
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:errorCorrectionsOnboard ; 

    sh:severity sh:Violation .

era-sh:ErrorCorrectionsOnboardApplicability a sh:SPARQLConstraint ; 
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:errorCorrectionsOnboard ; 
    era:scope "local" ; 

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to error corrections onboard."@en ; 
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to error corrections

    sh:message "errorCorrectionsOnboard: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: An error corrections onboard reference must be defined for the subset."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:errorCorrectionsOnboard ?value } .
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:ETCS a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:etcs ;
    era:rinfIndex "1.1.1.3.2" ;
    era:scope "local" ;

    rdfs:comment "European Train Control System (ETCS) application level supported by the track side equipment."@en ;
    # Source: ontology comment for etcs

    sh:class era:ETCS ;
    # Source: ontology range is http://data.europa.eu/949/ETCS

    sh:maxCount 1 ;
    # Inferred: typically, a property can only have one value in this context.

    sh:message "etcs: The CommonCharacteristicsSubset must have at most one ETCS value that is an instance of ETCS. This error may be due to having more than one value or having a value that is not an instance of ETCS."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable ETCS instances (IRI references)

    sh:path era:etcs ;

    sh:severity sh:Violation .

era-sh:ETCSApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:etcs ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires an ETCS reference."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to ETCS

    sh:message "etcs: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: An ETCS reference must be defined for the subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:etcs ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EtcsBaseline a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:etcsBaseline ;
    era:rinfIndex "1.1.1.3.2.2" ;
    era:scope "local" ;

    rdfs:comment "ETCS baseline installed lineside."@en ;
    # Source: ontology comment for etcsBaseline

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "etcsBaseline: The VehicleType must have at most one ETCS baseline value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:etcsBaseline ;

    sh:severity sh:Violation .

era-sh:EtcsBaselineApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:etcsBaseline ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires an ETCS baseline."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to ETCS baseline

    sh:message "etcsBaseline: This error is due to the VehicleType {?vehicleLabel}, violating the rule: An ETCS baseline must be defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:etcsBaseline ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EtcsDegradedSituation a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:etcsDegradedSituation ;
    era:rinfIndex "1.1.1.3.10.1" ;
    era:scope "local" ;

    rdfs:comment "ERTMS / ETCS application level for degraded situation related to the track side equipment."@en ;
    # Source: ontology comment for etcsDegradedSituation

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "etcsDegradedSituation: The CommonCharacteristicsSubset must have at most one ETCS degraded situation value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:etcsDegradedSituation ;

    sh:severity sh:Violation .

era-sh:EtcsDegradedSituationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:etcsDegradedSituation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires an ETCS degraded situation reference."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to ETCS degraded situations

    sh:message "etcsDegradedSituation: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: An ETCS degraded situation reference must be defined for the subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:etcsDegradedSituation ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ETCSEquipmentOnBoardLevel a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:etcsEquipmentOnBoardLevel ;
    era:rinfIndex "4.13.1.1" ;
    era:scope "local" ;

    rdfs:comment "ETCS equipment on-board and its level."@en ;
    # Source: ontology comment for etcsEquipmentOnBoardLevel

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "etcsEquipmentOnBoardLevel: The VehicleType must have at most one ETCS equipment on-board level value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:etcsEquipmentOnBoardLevel ;

    sh:severity sh:Violation .

era-sh:ETCSEquipmentOnBoardLevelApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:etcsEquipmentOnBoardLevel ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires an ETCS equipment on-board level."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to ETCS equipment levels

    sh:message "etcsEquipmentOnBoardLevel: This error is due to the VehicleType {?vehicleLabel}, violating the rule: An ETCS equipment on-board level must be defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:etcsEquipmentOnBoardLevel ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EtcsInfill a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:etcsInfill ;
    era:rinfIndex "1.1.1.3.2.4" ;
    era:scope "local" ;

    rdfs:comment "Information about installed trackside equipment capable to transmit infill information by loop or GSM-R for level 1 installation."@en ;
    # Source: ontology comment for etcsInfill

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "etcsInfill: The VehicleType must have at most one ETCS infill value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:etcsInfill ;

    sh:severity sh:Violation .

era-sh:EtcsInfillApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:etcsInfill ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires an ETCS infill reference."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to ETCS infill

    sh:message "etcsInfill: This error is due to the VehicleType {?vehicleLabel}, violating the rule: An ETCS infill reference must be defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:etcsInfill ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EtcsLevelType a sh:PropertyShape ;
    era:affectedClass era:ETCS ;
    era:affectedProperty era:etcsLevelType ;
    era:rinfIndex "1.1.1.3.2.1" ;
    era:scope "local" ;

    rdfs:comment "ETCS application level related to the track side equipment."@en ;
    # Source: ontology comment for etcsLevelType

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "etcsLevelType: The ETCS must have at most one ETCS level type value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:etcsLevelType ;

    sh:severity sh:Violation .

era-sh:EtcsLevelTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ETCS ;
    era:affectedProperty era:etcsLevelType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the ETCS is defined and requires an ETCS level type."@en ;
    # Source: inferred from the definition of ETCS and its relationship to level types

    sh:message "etcsLevelType: This error is due to the ETCS {?etcsLabel}, violating the rule: An ETCS level type must be defined for the ETCS."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?etcsLabel WHERE {
        $this rdfs:label ?etcsLabel .
        FILTER NOT EXISTS { $this era:etcsLevelType ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EtcsMVersion a sh:PropertyShape ;
    era:affectedClass era:ETCS ;
    era:affectedProperty era:etcsMVersion ;
    era:rinfIndex "1.1.1.3.2.10" ;
    era:scope "local" ;

    rdfs:comment "ETCS M_version according to SRS 7.5.1.9."@en ;
    # Source: ontology comment for etcsMVersion

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "etcsMVersion: The ETCS must have at most one ETCS M_version value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:etcsMVersion ;

    sh:severity sh:Violation .

era-sh:EtcsMVersionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ETCS ;
    era:affectedProperty era:etcsMVersion ;
    era:scope "local" ;

    rdfs:comment "Applicable when the ETCS is defined and requires an ETCS M_version."@en ;
    # Source: inferred from the definition of ETCS and its relationship to M_version

    sh:message "etcsMVersion: This error is due to the ETCS {?etcsLabel}, violating the rule: An ETCS M_version must be defined for the ETCS."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?etcsLabel WHERE {
        $this rdfs:label ?etcsLabel .
        FILTER NOT EXISTS { $this era:etcsMVersion ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EtcsNationalPacket44Function a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:etcsNationalPacket44Function ;
    era:rinfIndex "1.1.1.3.2.5" ;
    era:scope "local" ;

    rdfs:comment "Indication whether data for national applications is transmitted between track and train."@en ;
    # Source: ontology comment for etcsNationalPacket44Function

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "etcsNationalPacket44Function: The CommonCharacteristicsSubset must have at most one ETCS national packet 44 function value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:etcsNationalPacket44Function ;

    sh:severity sh:Violation .

era-sh:EtcsNationalPacket44FunctionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:etcsNationalPacket44Function ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires an ETCS national packet 44 function reference."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to ETCS national packet 44 function

    sh:message "etcsNationalPacket44Function: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: An ETCS national packet 44 function must be defined for the subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:etcsNationalPacket44Function ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EtcsRestrictionsConditionsDoc a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:etcsRestrictionsConditionsDoc ;
    era:rinfIndex "1.1.1.3.2.6" ;
    era:scope "local" ;

    rdfs:comment "Indication of the document where restrictions or conditions due to partial compliance with the TSI CCS are described."@en ;
    # Source: ontology comment for etcsRestrictionsConditionsDoc

    sh:class era:Document ;
    # Source: ontology range is http://data.europa.eu/949/Document

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "etcsRestrictionsConditionsDoc: The CommonCharacteristicsSubset must have at most one ETCS restrictions conditions document value that is a Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable documents (IRI references)

    sh:path era:etcsRestrictionsConditionsDoc ;

    sh:severity sh:Violation .

era-sh:EtcsRestrictionsConditionsDocApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:etcsRestrictionsConditionsDoc ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to ETCS restrictions conditions document."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to ETCS restrictions conditions

    sh:message "etcsRestrictionsConditionsDoc: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: An ETCS restrictions conditions document must be defined for the subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:etcsRestrictionsConditionsDoc ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EtcsSystemCompatibility a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:etcsSystemCompatibility ;
    era:rinfIndex "1.1.1.3.2.9" ;
    era:scope "local" ;

    rdfs:comment "ETCS requirements used for demonstrating technical compatibility."@en ;
    # Source: ontology comment for etcsSystemCompatibility

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: typically, a property can only have one value in this context.

    sh:message "etcsSystemCompatibility: The VehicleType must have at most one ETCS system compatibility value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:etcsSystemCompatibility ;

    sh:severity sh:Violation .

era-sh:EtcsSystemCompatibilityApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:etcsSystemCompatibility ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires an ETCS system compatibility reference."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to ETCS system compatibility

    sh:message "etcsSystemCompatibility: This error is due to the VehicleType {?vehicleLabel}, violating the rule: An ETCS system compatibility reference must be defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:etcsSystemCompatibility ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EtcsTransmittedTrackConditions a sh:PropertyShape ;
    era:affectedClass era:ETCS ;
    era:affectedProperty era:etcsTransmittedTrackConditions ;
    era:rinfIndex "1.1.1.3.2.12" ;
    era:scope "local" ;

    rdfs:comment "Transmittable track conditions by the CCSSubsystem, according to the specification referenced in Appendix A-1, index [C]."@en ;
    # Source: ontology comment for etcsTransmittedTrackConditions

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "etcsTransmittedTrackConditions: The ETCS must have at most one transmitted track conditions value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:etcsTransmittedTrackConditions ;

    sh:severity sh:Violation .

era-sh:EtcsTransmittedTrackConditionsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ETCS ;
    era:affectedProperty era:etcsTransmittedTrackConditions ;
    era:scope "local" ;

    rdfs:comment "Applicable when the ETCS is defined and requires a reference to transmitted track conditions."@en ;
    # Source: inferred from the definition of ETCS and its relationship to transmitted track conditions

    sh:message "etcsTransmittedTrackConditions: This error is due to the ETCS {?etcsLabel}, violating the rule: Transmitted track conditions must be defined for the ETCS."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?etcsLabel WHERE {
        $this rdfs:label ?etcsLabel .
        FILTER NOT EXISTS { $this era:etcsTransmittedTrackConditions ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:FireSafetyCategory a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:fireSafetyCategory ;
    era:rinfIndex "4.4.1" ;
    era:scope "local" ;

    rdfs:comment "Fire safety category for tunnels."@en ;
    # Source: ontology comment for fireSafetyCategory

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "fireSafetyCategory: The VehicleType must have at most one fire safety category value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:fireSafetyCategory ;

    sh:severity sh:Violation .

era-sh:FireSafetyCategoryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:fireSafetyCategory ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires a fire safety category."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to fire safety categories

    sh:message "fireSafetyCategory: This error is due to the VehicleType {?vehicleLabel}, violating the rule: A fire safety category must be defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:fireSafetyCategory ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:FreightCorridor a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:freightCorridor ;
    era:rinfIndex "1.1.1.1.2.3" ;
    era:scope "local" ;

    rdfs:comment "Indication whether the line is designated to a Railway Freight Corridor."@en ;
    # Source: ontology comment for freightCorridor

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "freightCorridor: The RunningTrack must have at most one freight corridor value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:freightCorridor ;

    sh:severity sh:Violation .

era-sh:FreightCorridorApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:freightCorridor ;
    era:scope "local" ;

    rdfs:comment "Applicable when the RunningTrack is defined and requires a freight corridor reference."@en ;
    # Source: inferred from the definition of RunningTrack and its relationship to freight corridors

    sh:message "freightCorridor: This error is due to the RunningTrack {?trackLabel}, violating the rule: A freight corridor reference must be defined for the running track."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:freightCorridor ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:FrenchTrainDetectionSystemLimitation a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:frenchTrainDetectionSystemLimitation ;
    era:scope "local" ;

    rdfs:comment "Specific for route compatibility check on French network. The property is composed of two values: A boolean [Y/N] and a number N between 1 and 8."@en ;
    # Source: ontology comment for frenchTrainDetectionSystemLimitation

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "frenchTrainDetectionSystemLimitation: The TrainDetectionSystem must have at most one French train detection system limitation value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:frenchTrainDetectionSystemLimitation ;

    sh:severity sh:Violation .

era-sh:FrenchTrainDetectionSystemLimitationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:frenchTrainDetectionSystemLimitation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the TrainDetectionSystem is defined and requires a French train detection system limitation."@en ;
    # Source: inferred from the definition of TrainDetectionSystem and its relationship to French train detection system limitations

    sh:message "frenchTrainDetectionSystemLimitation: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: A French train detection system limitation must be defined for the train detection system."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:frenchTrainDetectionSystemLimitation ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:FrenchTrainDetectionSystemLimitationNumber a sh:PropertyShape ;
    era:affectedClass era:FrenchTrainDetectionSystemLimitation ;
    era:affectedProperty era:frenchTrainDetectionSystemLimitationNumber ;
    era:scope "local" ;

    rdfs:comment "Part of the section with train detection limitation that indicates the type of train detection limitation. Specific for route compatibility check on French network."@en ;
    # Source: ontology comment for frenchTrainDetectionSystemLimitationNumber

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "frenchTrainDetectionSystemLimitationNumber: The FrenchTrainDetectionSystemLimitation must have at most one limitation number value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:frenchTrainDetectionSystemLimitationNumber ;

    sh:severity sh:Violation .

era-sh:FrenchTrainDetectionSystemLimitationNumberApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:FrenchTrainDetectionSystemLimitation ;
    era:affectedProperty era:frenchTrainDetectionSystemLimitationNumber ;
    era:scope "local" ;

    rdfs:comment "Applicable when the FrenchTrainDetectionSystemLimitation is defined and requires a limitation number."@en ;
    # Source: inferred from the definition of FrenchTrainDetectionSystemLimitation and its relationship to limitation numbers

    sh:message "frenchTrainDetectionSystemLimitationNumber: This error is due to the FrenchTrainDetectionSystemLimitation {?limitationLabel}, violating the rule: A limitation number must be defined for the French train detection system limitation."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?limitationLabel WHERE {
        $this rdfs:label ?limitationLabel .
        FILTER NOT EXISTS { $this era:frenchTrainDetectionSystemLimitationNumber ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:FrequencyBandsForDetection a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:frequencyBandsForDetection ;
    era:rinfIndex "1.1.1.3.4.2" ;
    era:scope "local" ;

    rdfs:comment "Bands of the frequency management of the train detection systems as defined in the TSI CCS."@en ;
    # Source: ontology comment for frequencyBandsForDetection

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "frequencyBandsForDetection: The TrainDetectionSystem must have at most one frequency band for detection value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:frequencyBandsForDetection ;

    sh:severity sh:Violation .

era-sh:FrequencyBandsForDetectionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:frequencyBandsForDetection ;
    era:scope "local" ;

    rdfs:comment "Applicable when the TrainDetectionSystem is defined and requires frequency bands for detection."@en ;
    # Source: inferred from the definition of TrainDetectionSystem and its relationship to frequency bands

    sh:message "frequencyBandsForDetection: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: Frequency bands for detection must be defined for the train detection system."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:frequencyBandsForDetection ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GaugingProfile a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gaugingProfile ;
    era:rinfIndex "1.1.1.1.3.1.1" ;
    era:scope "local" ;

    rdfs:comment "Gauges as defined in European standard or other local gauges, including lower or upper part."@en ;
    # Source: ontology comment for gaugingProfile

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "gaugingProfile: The CommonCharacteristicsSubset must have at most one gauging profile value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:gaugingProfile ;

    sh:severity sh:Violation .

era-sh:GaugingProfileApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gaugingProfile ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a gauging profile reference."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to gauging profiles

    sh:message "gaugingProfile: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A gauging profile must be defined for the subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gaugingProfile ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GaugingTransversalDocument a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:gaugingTransversalDocument ; 
    era:rinfIndex "1.1.1.1.3.1.3" ; 
    era:scope "local" ; 

    rdfs:comment "Electronic document available from the IM stored by the Agency with the transversal section of the particular points requiring specific checks due to deviations from gauging referred to in parameter 'Gauging'."@en ; 
    # Source: ontology comment for gaugingTransversalDocument

    sh:class era:Document ; 
    # Source: ontology range is era:Document

    sh:maxCount 1 ; 
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "gaugingTransversalDocument: The CommonCharacteristicsSubset must have at most one gauging transversal document value that is a Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ; 
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ; 
    # Inferred: links must refer to identifiable documents (IRI references)

    sh:path era:gaugingTransversalDocument ; 

    sh:severity sh:Violation .

era-sh:GaugingTransversalDocumentApplicability a sh:SPARQLConstraint ; 
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:gaugingTransversalDocument ; 
    era:scope "local" ; 

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a gauging transversal document reference."@en ; 
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to gauging transversal documents

    sh:message "gaugingTransversalDocument: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A gauging transversal document must be defined for the subset."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gaugingTransversalDocument ?value } .
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:GsmRActiveMobiles a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gsmRActiveMobiles ;
    era:rinfIndex "1.1.1.3.3.2" ;
    era:scope "local" ;

    rdfs:comment "Number of simultaneous communication sessions on board for ETCS level 2 required for a smooth running of the train. This relates to the radio block centre (RBC) handling of communication sessions."@en ;
    # Source: ontology comment for gsmRActiveMobiles

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "gsmRActiveMobiles: The CommonCharacteristicsSubset must have at most one GSM-R active mobiles value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:gsmRActiveMobiles ;

    sh:severity sh:Violation .

era-sh:GsmRActiveMobilesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gsmRActiveMobiles ;
    era:scope "local" ;

    rdfs:comment "Applicable only when level 2 or 3 is selected for parameter 1.1.1.3.2.1."@en ;
    # Source: inferred from the applicability note in RAG

    sh:message "gsmRActiveMobiles: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: GSM-R active mobiles must be defined only when ETCS Level 2 or 3 is selected."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gsmRActiveMobiles ?value } .
        # Assuming there is a property indicating the ETCS level
        FILTER NOT EXISTS { $this era:etcsLevel ?level . FILTER(?level != "2" && ?level != "3") }
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GsmRAdditionalInfo a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gsmRAdditionalInfo ;
    era:rinfIndex "1.1.1.3.3.3.1" ;
    era:scope "local" ;

    rdfs:comment "Any additional information on network characteristics or corresponding document available from the IM and stored by the Agency, e.g.; interference level, leading to the recommendation of additional on-board protection."@en ;
    # Source: ontology comment for gsmRAdditionalInfo

    sh:class era:Document ;
    # Source: ontology range is era:Document

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "gsmRAdditionalInfo: The CommonCharacteristicsSubset must have at most one GSM-R additional information value that is a Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable documents (IRI references)

    sh:path era:gsmRAdditionalInfo ;

    sh:severity sh:Violation .

era-sh:GsmRAdditionalInfoApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gsmRAdditionalInfo ;
    era:scope "local" ;

    rdfs:comment "Applicable when GSM-R is installed for this parameter to be applicable."@en ;
    # Source: inferred from the dependency note in the ontology

    sh:message "gsmRAdditionalInfo: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Additional information on network characteristics must be defined when GSM-R is installed."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gsmRAdditionalInfo ?value } .
        # Assuming there is a property indicating GSM-R installation
        FILTER NOT EXISTS { $this era:gsmRInstalled ?installed . FILTER(?installed != "Y") }
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GsmROptionalFunctions a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gsmROptionalFunctions ;
    era:rinfIndex "1.1.1.3.3.3" ;
    era:scope "local" ;

    rdfs:comment "Use of optional GSM-R functions which might improve operation on the line. They are for information only and not for network access criteria."@en ;
    # Source: ontology comment for gsmROptionalFunctions

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "gsmROptionalFunctions: The CommonCharacteristicsSubset must have at most one GSM-R optional functions value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:gsmROptionalFunctions ;

    sh:severity sh:Violation .

era-sh:GsmROptionalFunctionsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gsmROptionalFunctions ;
    era:scope "local" ;

    rdfs:comment "Applicable when GSM-R (parameter 1.1.1.3.3.1) must be installed for this parameter to be applicable."@en ;
    # Source: inferred from the dependency note in the ontology

    sh:message "gsmROptionalFunctions: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: GSM-R optional functions must be defined when GSM-R is installed."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gsmROptionalFunctions ?value } .
        # Assuming there is a property indicating GSM-R installation
        FILTER NOT EXISTS { $this era:gsmRInstalled ?installed . FILTER(?installed != "Y") }
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GsmRRadioDataCommunication a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:gsmRRadioDataCommunication ;
    era:rinfIndex "4.13.2.7" ;
    era:scope "local" ;

    rdfs:comment "GSM-R Radio Data communication on board and its Baseline."@en ;
    # Source: ontology comment for gsmRRadioDataCommunication

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "gsmRRadioDataCommunication: The VehicleType must have at most one GSM-R radio data communication value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:gsmRRadioDataCommunication ;

    sh:severity sh:Violation .

era-sh:GsmRRadioDataCommunicationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:gsmRRadioDataCommunication ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires GSM-R radio data communication."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to GSM-R radio data communication

    sh:message "gsmRRadioDataCommunication: This error is due to the VehicleType {?vehicleLabel}, violating the rule: GSM-R radio data communication must be defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:gsmRRadioDataCommunication ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GsmRVersion a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gsmRVersion ;
    era:rinfIndex "1.1.1.3.3.1" ;
    era:scope "local" ;

    rdfs:comment "GSM-R version installed lineside."@en ;
    # Source: RAG definition for GSM-R version

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: inferred from the nature of the property and its definition, as it can have multiple values but only one should be published at a time

    sh:message "gsmRVersion: The CommonCharacteristicsSubset must have at most one GSM-R version value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:gsmRVersion ;

    sh:severity sh:Violation .

era-sh:GsmRVersionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gsmRVersion ;
    era:scope "local" ;

    rdfs:comment "Applicable when GSM-R is installed for this parameter to be applicable."@en ;
    # Source: inferred from the dependency note in the ontology

    sh:message "gsmRVersion: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: GSM-R version must be defined when GSM-R is installed."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gsmRVersion ?value } .
        # Assuming there is a property indicating GSM-R installation
        FILTER NOT EXISTS { $this era:gsmRInstalled ?installed . FILTER(?installed != "Y") }
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GsmrConstraintsOperateOnlyInCircuitSwitch a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gsmrConstraintsOperateOnlyInCircuitSwitch ;
    era:rinfIndex "1.2.1.1.2.12" ;
    era:scope "local" ;

    rdfs:comment "These constraints, where applicable, are meant to manage the limited number of circuit-switched radio connections that can be handled simultaneously by a Radio Block Center."@en ;
    # Source: ontology comment for gsmrConstraintsOperateOnlyInCircuitSwitch

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "gsmrConstraintsOperateOnlyInCircuitSwitch: The CommonCharacteristicsSubset must have at most one GSM-R constraints operate only in circuit switch value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:gsmrConstraintsOperateOnlyInCircuitSwitch ;

    sh:severity sh:Violation .

era-sh:GsmrConstraintsOperateOnlyInCircuitSwitchApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gsmrConstraintsOperateOnlyInCircuitSwitch ;
    era:scope "local" ;

    rdfs:comment "Applicable when GSM-R and ETCS L2 must be installed for this parameter to be applicable."@en ;
    # Source: dependency note in the ontology

    sh:message "gsmrConstraintsOperateOnlyInCircuitSwitch: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: GSM-R constraints operate only in circuit switch must be defined when GSM-R and ETCS L2 are installed."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gsmrConstraintsOperateOnlyInCircuitSwitch ?value } .
        # Assuming there is a property indicating GSM-R and ETCS L2 installation
        FILTER NOT EXISTS { $this era:gsmRInstalled ?installed . FILTER(?installed != "Y") }
        FILTER NOT EXISTS { $this era:etcsLevel ?level . FILTER(?level != "2") }
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GsmrNetworkCoverage a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gsmrNetworkCoverage ;
    era:rinfIndex "1.1.1.3.3.5" ;
    era:scope "local" ;

    rdfs:comment "Name of the own GSM-R network and list of GSM-R networks which are covered by a roaming agreement (for CS services)."@en ;
    # Source: ontology comment for gsmrNetworkCoverage

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "gsmrNetworkCoverage: The CommonCharacteristicsSubset must have at most one GSM-R network coverage value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:gsmrNetworkCoverage ;

    sh:severity sh:Violation .

era-sh:GsmrNetworkCoverageApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gsmrNetworkCoverage ;
    era:scope "local" ;

    rdfs:comment "Applicable when GSM-R is installed for this parameter to be applicable."@en ;
    # Source: inferred from the dependency note in the ontology

    sh:message "gsmrNetworkCoverage: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: GSM-R network coverage must be defined when GSM-R is installed."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gsmrNetworkCoverage ?value } .
        # Assuming there is a property indicating GSM-R installation
        FILTER NOT EXISTS { $this era:gsmRInstalled ?installed . FILTER(?installed != "Y") }
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasAbstraction a sh:PropertyShape ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:hasAbstraction ;
    era:scope "local" ;

    rdfs:comment "Property that links a physical entity with its topological abstraction representation."@en ;
    # Source: ontology comment for hasAbstraction

    sh:class era:Abstraction ; 
    # Inferred: assuming there is a class representing the abstraction linked to the infrastructure element

    sh:maxCount 1 ;
    # Inferred: typically, an infrastructure element would have at most one abstraction representation

    sh:message "hasAbstraction: The InfrastructureElement must have at most one abstraction value that is an Abstraction. This error may be due to having more than one value or having a value that is not an instance of Abstraction."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable abstractions (IRI references)

    sh:path era:hasAbstraction ;

    sh:severity sh:Violation .

era-sh:HasAbstractionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:hasAbstraction ;
    era:scope "local" ;

    rdfs:comment "Applicable when the InfrastructureElement is defined and requires an abstraction representation."@en ;
    # Source: inferred from the definition of InfrastructureElement and its relationship to abstractions

    sh:message "hasAbstraction: This error is due to the InfrastructureElement {?elementLabel}, violating the rule: An abstraction must be defined for the infrastructure element."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?elementLabel WHERE {
        $this rdfs:label ?elementLabel .
        FILTER NOT EXISTS { $this era:hasAbstraction ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasLRS a sh:PropertyShape ;
    era:affectedClass era:KilometricPost ;
    era:affectedProperty era:hasLRS ;
    era:scope "local" ;

    rdfs:comment "Specifies the linear referencing system to which the kilometre post is referenced. The linear referencing system is commonly the main railway line and it provides the framework for identifying the post's location along the railway line, using distance measurements rather than geographical coordinates."@en ;
    # Source: ontology comment for hasLRS

    sh:class era:LinearPositioningSystem ;
    # Source: ontology range is era:LinearPositioningSystem

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "hasLRS: The KilometricPost must have at most one linear referencing system value that is a LinearPositioningSystem. This error may be due to having more than one value or having a value that is not an instance of LinearPositioningSystem."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable linear positioning systems (IRI references)

    sh:path era:hasLRS ;

    sh:severity sh:Violation .

era-sh:HasLRSApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:KilometricPost ;
    era:affectedProperty era:hasLRS ;
    era:scope "local" ;

    rdfs:comment "Applicable when the KilometricPost is defined and requires a linear referencing system."@en ;
    # Source: inferred from the definition of KilometricPost and its relationship to linear referencing systems

    sh:message "hasLRS: This error is due to the KilometricPost {?postLabel}, violating the rule: A linear referencing system must be defined for the kilometre post."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?postLabel WHERE {
        $this rdfs:label ?postLabel .
        FILTER NOT EXISTS { $this era:hasLRS ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasLrsCoordinate a sh:PropertyShape ;
    era:affectedClass era:NetPointReference ;
    era:affectedProperty era:hasLrsCoordinate ;
    era:scope "local" ;

    rdfs:comment "Specifies the LRS coordinate associated with a topological coordinate."@en ;
    # Source: ontology comment for hasLrsCoordinate

    sh:class era:LinearPositioningSystemCoordinate ;
    # Source: ontology range is era:LinearPositioningSystemCoordinate

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "hasLrsCoordinate: The NetPointReference must have at most one LRS coordinate value that is a LinearPositioningSystemCoordinate. This error may be due to having more than one value or having a value that is not an instance of LinearPositioningSystemCoordinate."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable coordinates (IRI references)

    sh:path era:hasLrsCoordinate ;

    sh:severity sh:Violation .

era-sh:HasLrsCoordinateApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:NetPointReference ;
    era:affectedProperty era:hasLrsCoordinate ;
    era:scope "local" ;

    rdfs:comment "Applicable when the NetPointReference is defined and requires an LRS coordinate."@en ;
    # Source: inferred from the definition of NetPointReference and its relationship to LRS coordinates

    sh:message "hasLrsCoordinate: This error is due to the NetPointReference {?pointLabel}, violating the rule: An LRS coordinate must be defined for the net point reference."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?pointLabel WHERE {
        $this rdfs:label ?pointLabel .
        FILTER NOT EXISTS { $this era:hasLrsCoordinate ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasOrganisationRole a sh:PropertyShape ;
    era:affectedClass era:OrganisationRole ;
    era:affectedProperty era:hasOrganisationRole ;
    era:scope "local" ;

    rdfs:comment "Relates the Organisation role instance (the role played by an Organisation) with the specific role in the taxonomy of organisation roles."@en ;
    # Source: ontology comment for hasOrganisationRole

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "hasOrganisationRole: The OrganisationRole must have at most one organisation role value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:hasOrganisationRole ;

    sh:severity sh:Violation .

era-sh:HasOrganisationRoleApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OrganisationRole ;
    era:affectedProperty era:hasOrganisationRole ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OrganisationRole is defined and requires a specific role in the taxonomy."@en ;
    # Source: inferred from the definition of OrganisationRole and its relationship to organisation roles

    sh:message "hasOrganisationRole: This error is due to the OrganisationRole {?roleLabel}, violating the rule: An organisation role must be defined for the organisation."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?roleLabel WHERE {
        $this rdfs:label ?roleLabel .
        FILTER NOT EXISTS { $this era:hasOrganisationRole ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasOrientation a sh:PropertyShape ;
    era:affectedClass era:Signal ;
    era:affectedProperty era:hasOrientation ;
    era:scope "local" ;

    rdfs:comment "Indicates the orientation of the signal in relation to the carrier linear element. Possible values include Normal, Opposite, Both."@en ;
    # Source: ontology comment for hasOrientation

    sh:class era:Orientation ;
    # Source: ontology range is era:Orientation

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "hasOrientation: The Signal must have at most one orientation value that is an Orientation. This error may be due to having more than one value or having a value that is not an instance of Orientation."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable orientations (IRI references)

    sh:path era:hasOrientation ;

    sh:severity sh:Violation .

era-sh:HasOrientationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Signal ;
    era:affectedProperty era:hasOrientation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Signal is defined and requires an orientation."@en ;
    # Source: inferred from the definition of Signal and its relationship to orientation

    sh:message "hasOrientation: This error is due to the Signal {?signalLabel}, violating the rule: An orientation must be defined for the signal."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?signalLabel WHERE {
        $this rdfs:label ?signalLabel .
        FILTER NOT EXISTS { $this era:hasOrientation ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasPart a sh:PropertyShape ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:hasPart ;
    era:scope "local" ;

    rdfs:comment "Indicates that an infrastructure element is composed of one or more other (distinct) infrastructure elements. This property is used to represent part-whole relationships between infrastructure elements. An element cannot be a part of itself."@en ;
    # Source: ontology comment for hasPart

    sh:class era:InfrastructureElement ;
    # Source: ontology range is era:InfrastructureElement

    sh:minCount 1 ;
    # Inferred: at least one part is required to establish a part-whole relationship

    sh:maxCount 1 ;
    # Inferred: typically, an infrastructure element would have at most one direct part relationship

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable infrastructure elements (IRI references)

    sh:message "hasPart: The InfrastructureElement must have at least one hasPart reference that is an instance of InfrastructureElement. This error may be due to not having a value or having a value that is not an instance of InfrastructureElement."@en ;

    sh:path era:hasPart ;

    sh:severity sh:Violation .

era-sh:HasPartIrreflexivity a sh:SPARQLConstraint ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:hasPart ;
    era:scope "local" ;

    rdfs:comment "Enforces irreflexivity of hasPart: an element cannot be a part of itself."@en ;
    # Source: ontology declares owl:IrreflexiveProperty

    sh:message "hasPart: The InfrastructureElement {?thisLabel} cannot have itself as part."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?thisLabel WHERE {
        $this rdfs:label ?thisLabel .
        FILTER EXISTS { $this era:hasPart $this } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasSchematicOverviewOPDigitalForm a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:hasSchematicOverviewOPDigitalForm ;
    era:rinfIndex "1.2.0.0.0.7" ;
    era:scope "local" ;

    rdfs:comment "The existence of a schematic overview of the operational point in digital form."@en ;
    # Source: ontology comment for hasSchematicOverviewOPDigitalForm

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "hasSchematicOverviewOPDigitalForm: The OperationalPoint must have at most one schematic overview in digital form that is a boolean value. This error may be due to having more than one value or having a value that is not a boolean."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:path era:hasSchematicOverviewOPDigitalForm ;

    sh:severity sh:Violation .

era-sh:HasSchematicOverviewOPDigitalFormApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:hasSchematicOverviewOPDigitalForm ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OperationalPoint is defined and requires a schematic overview in digital form."@en ;
    # Source: inferred from the definition of OperationalPoint and its relationship to schematic overviews

    sh:message "hasSchematicOverviewOPDigitalForm: This error is due to the OperationalPoint {?opLabel}, violating the rule: A schematic overview in digital form must be defined for the operational point."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?opLabel WHERE {
        $this rdfs:label ?opLabel .
        FILTER NOT EXISTS { $this era:hasSchematicOverviewOPDigitalForm ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasSequence a sh:PropertyShape ;
    era:affectedClass era:NetLinearReference ;
    era:affectedProperty era:hasSequence ;
    era:scope "local" ;

    rdfs:comment "The ordered sequence of topological linear elements which create a linear net reference."@en ;
    # Source: ontology comment for hasSequence

    sh:class rdf:List ;
    # Source: ontology range is rdf:List

    sh:minCount 1 ;
    # Inferred: at least one sequence element is required to establish a valid sequence

    sh:message "hasSequence: The NetLinearReference must have at least one hasSequence reference that is a List. This error may be due to not having a value or having a value that is not a List."@en ;

    sh:path era:hasSequence ;

    sh:severity sh:Violation .

era-sh:HasSequenceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:NetLinearReference ;
    era:affectedProperty era:hasSequence ;
    era:scope "local" ;

    rdfs:comment "Applicable when the NetLinearReference is defined and requires a sequence of topological linear elements."@en ;
    # Source: inferred from the definition of NetLinearReference and its relationship to sequences

    sh:message "hasSequence: This error is due to the NetLinearReference {?referenceLabel}, violating the rule: A sequence must be defined for the net linear reference."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?referenceLabel WHERE {
        $this rdfs:label ?referenceLabel .
        FILTER NOT EXISTS { $this era:hasSequence ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasTopoCoordinate a sh:PropertyShape ;
    era:affectedClass era:NetPointReference ;
    era:affectedProperty era:hasTopoCoordinate ;
    era:scope "local" ;

    rdfs:comment "Specifies the topological coordinate of a network point reference."@en ;
    # Source: ontology comment for hasTopoCoordinate

    sh:class era:TopologicalCoordinate ;
    # Source: ontology range is era:TopologicalCoordinate

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "hasTopoCoordinate: The NetPointReference must have at most one topological coordinate value that is a TopologicalCoordinate. This error may be due to having more than one value or having a value that is not an instance of TopologicalCoordinate."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable topological coordinates (IRI references)

    sh:path era:hasTopoCoordinate ;

    sh:severity sh:Violation .

era-sh:HasTopoCoordinateApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:NetPointReference ;
    era:affectedProperty era:hasTopoCoordinate ;
    era:scope "local" ;

    rdfs:comment "Applicable when the NetPointReference is defined and requires a topological coordinate."@en ;
    # Source: inferred from the definition of NetPointReference and its relationship to topological coordinates

    sh:message "hasTopoCoordinate: This error is due to the NetPointReference {?pointLabel}, violating the rule: A topological coordinate must be defined for the net point reference."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?pointLabel WHERE {
        $this rdfs:label ?pointLabel .
        FILTER NOT EXISTS { $this era:hasTopoCoordinate ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HotAxleBoxDetectorDirection a sh:PropertyShape ;
    era:affectedClass era:HABD ;
    era:affectedProperty era:hotAxleBoxDetectorDirection ;
    era:rinfIndex "1.1.1.1.7.9" ;
    era:scope "local" ;

    rdfs:comment "Direction of measurement of trackside HABD, specific for the French, Italian, and Swedish networks."@en ;
    # Source: ontology comment for hotAxleBoxDetectorDirection

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "hotAxleBoxDetectorDirection: The HABD must have at most one direction value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:hotAxleBoxDetectorDirection ;

    sh:severity sh:Violation .

era-sh:HotAxleBoxDetectorDirectionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:HABD ;
    era:affectedProperty era:hotAxleBoxDetectorDirection ;
    era:scope "local" ;

    rdfs:comment "Applicable if trackside HABD is not TSI compliant, direction of measurement of trackside hot axle box detector."@en ;
    # Source: inferred from the dependency note in the ontology

    sh:message "hotAxleBoxDetectorDirection: This error is due to the HABD {?habdLabel}, violating the rule: The direction of measurement must be defined when the HABD is not TSI compliant."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?habdLabel WHERE {
        $this rdfs:label ?habdLabel .
        FILTER NOT EXISTS { $this era:hotAxleBoxDetectorDirection ?value } .
        # Assuming there is a property indicating TSI compliance
        FILTER EXISTS { $this era:tsiCompliant ?tsi . FILTER(?tsi = "N") }
    }
    """ ;

    sh:severity sh:Violation .

era-sh:InCountry a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:inCountry ;
    era:scope "local" ;

    rdfs:comment "Indicates the country in which an entity resides."@en ;
    # Source: ontology comment for inCountry

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "inCountry: The CommonCharacteristicsSubset must have at most one inCountry reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:inCountry ;

    sh:severity sh:Violation .

era-sh:InCountryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:inCountry ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a country designation."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to country designations

    sh:message "inCountry: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A country must be defined for the common characteristics subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:inCountry ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Includes a sh:PropertyShape ;
    era:affectedClass era:NetAreaReference ;
    era:affectedProperty era:includes ;
    era:scope "local" ;

    rdfs:comment "List of linear references included in a network area reference."@en ;
    # Source: ontology comment for includes

    sh:class rdf:List ;
    # Source: ontology range is rdf:List

    sh:minCount 1 ;
    # Inferred: at least one linear reference is required to establish a valid inclusion

    sh:message "includes: The NetAreaReference must have at least one includes reference that is a List. This error may be due to not having a value or having a value that is not a List."@en ;

    sh:path era:includes ;

    sh:severity sh:Violation .

era-sh:IncludesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:NetAreaReference ;
    era:affectedProperty era:includes ;
    era:scope "local" ;

    rdfs:comment "Applicable when the NetAreaReference is defined and requires a list of linear references."@en ;
    # Source: inferred from the definition of NetAreaReference and its relationship to included linear references

    sh:message "includes: This error is due to the NetAreaReference {?referenceLabel}, violating the rule: A list of linear references must be defined for the network area reference."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?referenceLabel WHERE {
        $this rdfs:label ?referenceLabel .
        FILTER NOT EXISTS { $this era:includes ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:InfrastructureManager a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:infrastructureManager ; 
    era:scope "local" ;

    rdfs:comment "Relates a subset with common characteristics with its Infrastructure Manager, represented by an instance of OrganisationRole."@en ;
    # Source: ontology comment for infrastructureManager

    sh:class era:OrganisationRole ; 
    # Source: ontology range is era:OrganisationRole

    sh:maxCount 1 ; 
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "infrastructureManager: The CommonCharacteristicsSubset must have at most one infrastructure manager reference that is an OrganisationRole. This error may be due to having more than one value or having a value that is not an instance of OrganisationRole."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ; 
    # Inferred: links must refer to identifiable organisation roles (IRI references)

    sh:path era:infrastructureManager ;

    sh:severity sh:Violation .

era-sh:InfrastructureManagerApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:infrastructureManager ; 
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires an Infrastructure Manager."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to infrastructure managers

    sh:message "infrastructureManager: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: An infrastructure manager must be defined for the common characteristics subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:infrastructureManager ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:InfrastructureMgr a sh:PropertyShape ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:infrastructureMgr ;
    era:scope "local" ;

    rdfs:comment "(deprecated) not in use. Relates any feature implemented in the European railway infrastructure with its infrastructure manager."@en ;
    # Source: ontology comment for infrastructureMgr

    sh:class era:InfrastructureManager ;
    # Source: ontology range is era:InfrastructureManager

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "infrastructureMgr: The InfrastructureElement must have at most one infrastructure manager reference that is an InfrastructureManager. This error may be due to having more than one value or having a value that is not an instance of InfrastructureManager."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable infrastructure managers (IRI references)

    sh:path era:infrastructureMgr ;

    sh:severity sh:Violation .

era-sh:InfrastructureMgrApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:infrastructureMgr ;
    era:scope "local" ;

    rdfs:comment "Applicable when the InfrastructureElement is defined and requires an infrastructure manager."@en ;
    # Source: inferred from the definition of InfrastructureElement and its relationship to infrastructure managers

    sh:message "infrastructureMgr: This error is due to the InfrastructureElement {?elementLabel}, violating the rule: An infrastructure manager must be defined for the infrastructure element."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?elementLabel WHERE {
        $this rdfs:label ?elementLabel .
        FILTER NOT EXISTS { $this era:infrastructureMgr ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:InstructionsSwitchRadioSystems a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:instructionsSwitchRadioSystems ; 
    era:scope "local" ;

    rdfs:comment "Name and/or reference of the document specifying the Special instructions to switch over between different radio systems."@en ;
    # Source: ontology comment for instructionsSwitchRadioSystems

    sh:class era:Document ; 
    # Source: ontology range is era:Document

    sh:maxCount 1 ; 
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "instructionsSwitchRadioSystems: The CommonCharacteristicsSubset must have at most one instructionsSwitchRadioSystems reference that is a Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ; 
    # Inferred: links must refer to identifiable documents (IRI references)

    sh:path era:instructionsSwitchRadioSystems ;

    sh:severity sh:Violation .

era-sh:InstructionsSwitchRadioSystemsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:instructionsSwitchRadioSystems ; 
    era:scope "local" ;

    rdfs:comment "Applicable when at least two different radio systems exist."@en ;
    # Source: RAG explanation on applicability

    sh:message "instructionsSwitchRadioSystems: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Instructions for switching radio systems must be defined when at least two different radio systems exist."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        # Assuming there is a property indicating the existence of multiple radio systems
        FILTER NOT EXISTS { $this era:instructionsSwitchRadioSystems ?value } .
        FILTER EXISTS { $this era:otherRadioSystems ?systems . FILTER(?systems >= 2) }
    }
    """ ;

    sh:severity sh:Violation .

era-sh:IsPartOf a sh:PropertyShape ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:isPartOf ;
    era:scope "local" ;

    rdfs:comment "Indicates that an infrastructure element is a part of another (distinct) infrastructure element. An element cannot be part of itself. This property is the inverse of era:hasPart and represents the partial or whole-part relationship between infrastructure elements."@en ;
    # Source: ontology comment for isPartOf

    sh:class era:InfrastructureElement ;
    # Source: ontology range is era:InfrastructureElement

    sh:minCount 1 ;
    # Inferred: at least one part relationship is required to establish a valid part-whole relationship

    sh:maxCount 1 ;
    # Inferred: typically, an infrastructure element would have at most one direct part relationship

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable infrastructure elements (IRI references)

    sh:message "isPartOf: The InfrastructureElement must have at least one isPartOf reference that is an instance of InfrastructureElement. This error may be due to not having a value or having a value that is not an instance of InfrastructureElement."@en ;

    sh:path era:isPartOf ;

    sh:severity sh:Violation .

era-sh:IsPartOfIrreflexivity a sh:SPARQLConstraint ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:isPartOf ;
    era:scope "local" ;

    rdfs:comment "Enforces irreflexivity of isPartOf: an element cannot be part of itself."@en ;
    # Source: ontology declares owl:IrreflexiveProperty

    sh:message "isPartOf: The InfrastructureElement {?thisLabel} cannot be part of itself."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?thisLabel WHERE {
        $this rdfs:label ?thisLabel .
        FILTER EXISTS { $this era:isPartOf $this } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:KmPost a sh:PropertyShape ;
    era:affectedClass era:LinearPositioningSystemCoordinate ;
    era:affectedProperty era:kmPost ;
    era:scope "local" ;

    rdfs:comment "The closest kilometric post on the line, used as a reference by the LRS coordinate."@en ;
    # Source: ontology comment for kmPost

    sh:class era:KilometricPost ;
    # Source: ontology range is era:KilometricPost

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "kmPost: The LinearPositioningSystemCoordinate must have at most one kmPost reference that is a KilometricPost. This error may be due to having more than one value or having a value that is not an instance of KilometricPost."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable kilometric posts (IRI references)

    sh:path era:kmPost ;

    sh:severity sh:Violation .

era-sh:KmPostApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LinearPositioningSystemCoordinate ;
    era:affectedProperty era:kmPost ;
    era:scope "local" ;

    rdfs:comment "Applicable when the LinearPositioningSystemCoordinate is defined and requires a reference to a kilometric post."@en ;
    # Source: inferred from the definition of LinearPositioningSystemCoordinate and its relationship to kmPost

    sh:message "kmPost: This error is due to the LinearPositioningSystemCoordinate {?coordinateLabel}, violating the rule: A kmPost must be defined for the linear positioning system coordinate."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?coordinateLabel WHERE {
        $this rdfs:label ?coordinateLabel .
        FILTER NOT EXISTS { $this era:kmPost ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LegacyRadioSystem a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:legacyRadioSystem ;
    era:scope "local" ;

    rdfs:comment "Indication of radio legacy systems installed."@en ;
    # Source: ontology comment for legacyRadioSystem

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "legacyRadioSystem: The CommonCharacteristicsSubset must have at most one legacy radio system reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:legacyRadioSystem ;

    sh:severity sh:Violation .

era-sh:LegacyRadioSystemApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:legacyRadioSystem ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a legacy radio system indication."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to legacy radio systems

    sh:message "legacyRadioSystem: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A legacy radio system must be defined for the common characteristics subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:legacyRadioSystem ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LineCategory a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:lineCategory ;
    era:scope "local" ;

    rdfs:comment "Classification of a line according to the TSI INF."@en ;
    # Source: ontology comment for lineCategory

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:minCount 1 ;
    # Inferred: at least one line category is required to classify the line

    sh:maxCount 1 ;
    # Inferred: typically, a line would have at most one direct category relationship

    sh:message "lineCategory: The RunningTrack must have at least one line category reference that is a Concept. This error may be due to not having a value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:lineCategory ;

    sh:severity sh:Violation .

era-sh:LineCategoryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:lineCategory ;
    era:scope "local" ;

    rdfs:comment "Applicable when the RunningTrack is defined and requires a line category."@en ;
    # Source: inferred from the definition of RunningTrack and its relationship to line categories

    sh:message "lineCategory: This error is due to the RunningTrack {?trackLabel}, violating the rule: A line category must be defined for the running track."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:lineCategory ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LineNationalId a sh:PropertyShape ;
    era:affectedClass era:LineReference ;
    era:affectedProperty era:lineNationalId ;
    era:rinfIndex "1.1.0.0.0.2" ;
    era:scope "local" ;

    rdfs:comment "Unique line identification or unique line number within Member State."@en ;
    # Source: ontology comment for lineNationalId

    sh:datatype xsd:string ;
    # Source: inferred from the nature of the property and the requirement for a character string

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "lineNationalId: The LineReference must have at most one line national ID that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:path era:lineNationalId ;

    sh:severity sh:Violation .

era-sh:LineNationalIdApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LineReference ;
    era:affectedProperty era:lineNationalId ;
    era:scope "local" ;

    rdfs:comment "Applicable when the LineReference is defined and requires a national line identification."@en ;
    # Source: inferred from the definition of LineReference and its relationship to line national identification

    sh:message "lineNationalId: This error is due to the LineReference {?lineLabel}, violating the rule: A national line ID must be defined for the line reference."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?lineLabel WHERE {
        $this rdfs:label ?lineLabel .
        FILTER NOT EXISTS { $this era:lineNationalId ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LineReference a sh:PropertyShape ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:lineReference ;
    era:scope "local" ;

    rdfs:comment "Indicates a relationship with a national railway line at a specific kilometer point."@en ;
    # Source: ontology comment for lineReference

    sh:class era:LineReference ;
    # Source: ontology range is era:LineReference

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "lineReference: The InfrastructureElement must have at most one line reference that is a LineReference. This error may be due to having more than one value or having a value that is not an instance of LineReference."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable line references (IRI references)

    sh:path era:lineReference ;

    sh:severity sh:Violation .

era-sh:LineReferenceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:lineReference ;
    era:scope "local" ;

    rdfs:comment "Applicable when the InfrastructureElement is defined and requires a reference to a national railway line."@en ;
    # Source: inferred from the definition of InfrastructureElement and its relationship to line references

    sh:message "lineReference: This error is due to the InfrastructureElement {?elementLabel}, violating the rule: A line reference must be defined for the infrastructure element."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?elementLabel WHERE {
        $this rdfs:label ?elementLabel .
        FILTER NOT EXISTS { $this era:lineReference ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LineReferenceTunnelEnd a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:lineReferenceTunnelEnd ;
    era:rinfIndex "1.1.1.1.8.4" ;
    era:scope "local" ;

    rdfs:comment "Geographical coordinates in decimal degrees and km of the line at the end of a tunnel."@en ;
    # Source: ontology comment for lineReferenceTunnelEnd

    sh:class era:NetPointReference ;
    # Source: ontology range is era:NetPointReference

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "lineReferenceTunnelEnd: The Tunnel must have at most one line reference tunnel end that is a NetPointReference. This error may be due to having more than one value or having a value that is not an instance of NetPointReference."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable net point references (IRI references)

    sh:path era:lineReferenceTunnelEnd ;

    sh:severity sh:Violation .

era-sh:LineReferenceTunnelEndApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:lineReferenceTunnelEnd ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Tunnel is defined and requires a reference to the line reference tunnel end."@en ;
    # Source: inferred from the definition of Tunnel and its relationship to line reference tunnel ends

    sh:message "lineReferenceTunnelEnd: This error is due to the Tunnel {?tunnelLabel}, violating the rule: A line reference tunnel end must be defined for the tunnel."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:lineReferenceTunnelEnd ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LineReferenceTunnelStart a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:lineReferenceTunnelStart ;
    era:rinfIndex "1.1.1.1.8.3" ;
    era:scope "local" ;

    rdfs:comment "Geographical coordinates in decimal degrees and km of the line at the beginning of a tunnel."@en ;
    # Source: ontology comment for lineReferenceTunnelStart

    sh:class era:NetPointReference ;
    # Source: ontology range is era:NetPointReference

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "lineReferenceTunnelStart: The Tunnel must have at most one line reference tunnel start that is a NetPointReference. This error may be due to having more than one value or having a value that is not an instance of NetPointReference."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable net point references (IRI references)

    sh:path era:lineReferenceTunnelStart ;

    sh:severity sh:Violation .

era-sh:LineReferenceTunnelStartApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:lineReferenceTunnelStart ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Tunnel is defined and requires a reference to the line reference tunnel start."@en ;
    # Source: inferred from the definition of Tunnel and its relationship to line reference tunnel starts

    sh:message "lineReferenceTunnelStart: This error is due to the Tunnel {?tunnelLabel}, violating the rule: A line reference tunnel start must be defined for the tunnel."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:lineReferenceTunnelStart ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LinesideDistanceIndication a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:linesideDistanceIndication ;
    era:scope "local" ;

    rdfs:comment "Indication of types of appearance of track lineside distance indications."@en ;
    # Source: ontology comment for linesideDistanceIndication

    sh:class era:LinesideDistanceIndication ;
    # Source: ontology range is era:LinesideDistanceIndication

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "linesideDistanceIndication: The CommonCharacteristicsSubset must have at most one lineside distance indication reference that is a LinesideDistanceIndication. This error may be due to having more than one value or having a value that is not an instance of LinesideDistanceIndication."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable lineside distance indications (IRI references)

    sh:path era:linesideDistanceIndication ;

    sh:severity sh:Violation .

era-sh:LinesideDistanceIndicationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:linesideDistanceIndication ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a lineside distance indication."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to lineside distance indications

    sh:message "linesideDistanceIndication: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A lineside distance indication must be defined for the common characteristics subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:linesideDistanceIndication ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LinesideDistanceIndicationAppearance a sh:PropertyShape ;
    era:affectedClass era:LinesideDistanceIndication ;
    era:affectedProperty era:linesideDistanceIndicationAppearance ;
    era:scope "local" ;

    rdfs:comment "Indication of types of appearance of track lineside distance indications."@en ;
    # Source: ontology comment for linesideDistanceIndicationAppearance

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "linesideDistanceIndicationAppearance: The LinesideDistanceIndication must have at most one lineside distance indication appearance reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:linesideDistanceIndicationAppearance ;

    sh:severity sh:Violation .

era-sh:LinesideDistanceIndicationAppearanceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LinesideDistanceIndication ;
    era:affectedProperty era:linesideDistanceIndicationAppearance ;
    era:scope "local" ;

    rdfs:comment "Applicable when the LinesideDistanceIndication is defined and requires a lineside distance indication appearance."@en ;
    # Source: inferred from the definition of LinesideDistanceIndication and its relationship to lineside distance indication appearance

    sh:message "linesideDistanceIndicationAppearance: This error is due to the LinesideDistanceIndication {?indicationLabel}, violating the rule: A lineside distance indication appearance must be defined for the lineside distance indication."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?indicationLabel WHERE {
        $this rdfs:label ?indicationLabel .
        FILTER NOT EXISTS { $this era:linesideDistanceIndicationAppearance ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LinesideDistanceIndicationPositioning a sh:PropertyShape ;
    era:affectedClass era:LinesideDistanceIndication ;
    era:affectedProperty era:linesideDistanceIndicationPositioning ;
    era:scope "local" ;

    rdfs:comment "Indication of the side along the track where the lineside indication is positioned (left or right, or both sides)."@en ;
    # Source: ontology comment for linesideDistanceIndicationPositioning

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "linesideDistanceIndicationPositioning: The LinesideDistanceIndication must have at most one lineside distance indication positioning reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:linesideDistanceIndicationPositioning ;

    sh:severity sh:Violation .

era-sh:LinesideDistanceIndicationPositioningApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LinesideDistanceIndication ;
    era:affectedProperty era:linesideDistanceIndicationPositioning ;
    era:scope "local" ;

    rdfs:comment "Applicable when the LinesideDistanceIndication is defined and requires a lineside distance indication positioning."@en ;
    # Source: inferred from the definition of LinesideDistanceIndication and its relationship to lineside distance indication positioning

    sh:message "linesideDistanceIndicationPositioning: This error is due to the LinesideDistanceIndication {?indicationLabel}, violating the rule: A lineside distance indication positioning must be defined for the lineside distance indication."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?indicationLabel WHERE {
        $this rdfs:label ?indicationLabel .
        FILTER NOT EXISTS { $this era:linesideDistanceIndicationPositioning ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LinkedToPrimaryLocation a sh:PropertyShape ;
    era:affectedClass era:SubsidiaryLocation ;
    era:affectedProperty era:linkedToPrimaryLocation ;
    era:scope "local" ;

    rdfs:comment "Relates a subsidiary location with a primary location."@en ;
    # Source: ontology comment for linkedToPrimaryLocation

    sh:class era:PrimaryLocation ;
    # Source: ontology range is era:PrimaryLocation

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "linkedToPrimaryLocation: The SubsidiaryLocation must have at most one linkedToPrimaryLocation reference that is a PrimaryLocation. This error may be due to having more than one value or having a value that is not an instance of PrimaryLocation."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable primary locations (IRI references)

    sh:path era:linkedToPrimaryLocation ;

    sh:severity sh:Violation .

era-sh:LinkedToPrimaryLocationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SubsidiaryLocation ;
    era:affectedProperty era:linkedToPrimaryLocation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the SubsidiaryLocation is defined and requires a reference to a primary location."@en ;
    # Source: inferred from the definition of SubsidiaryLocation and its relationship to primary locations

    sh:message "linkedToPrimaryLocation: This error is due to the SubsidiaryLocation {?subsidiaryLabel}, violating the rule: A linkedToPrimaryLocation must be defined for the subsidiary location."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsidiaryLabel WHERE {
        $this rdfs:label ?subsidiaryLabel .
        FILTER NOT EXISTS { $this era:linkedToPrimaryLocation ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LoadCapabilityLineCategory a sh:PropertyShape ;
    era:affectedClass era:LoadCapability ;
    era:affectedProperty era:loadCapabilityLineCategory ;
    era:scope "local" ;

    rdfs:comment "Part of the load capability of a track that corresponds to the line category of the load model."@en ;
    # Source: ontology comment for loadCapabilityLineCategory

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:minCount 1 ;
    # Inferred: at least one load capability line category is required to define the load capability

    sh:maxCount 1 ;
    # Inferred: typically, a load capability would have at most one direct category relationship

    sh:message "loadCapabilityLineCategory: The LoadCapability must have at least one load capability line category reference that is a Concept. This error may be due to not having a value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:loadCapabilityLineCategory ;

    sh:severity sh:Violation .

era-sh:LoadCapabilityLineCategoryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LoadCapability ;
    era:affectedProperty era:loadCapabilityLineCategory ;
    era:scope "local" ;

    rdfs:comment "Applicable when the LoadCapability is defined and requires a load capability line category."@en ;
    # Source: inferred from the definition of LoadCapability and its relationship to load capability line categories

    sh:message "loadCapabilityLineCategory: This error is due to the LoadCapability {?loadCapabilityLabel}, violating the rule: A load capability line category must be defined for the load capability."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?loadCapabilityLabel WHERE {
        $this rdfs:label ?loadCapabilityLabel .
        FILTER NOT EXISTS { $this era:loadCapabilityLineCategory ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LocalRulesOrRestrictionsDoc a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:localRulesOrRestrictionsDoc ; 
    era:scope "local" ;

    rdfs:comment "Documents regarding the rules or restrictions of a strictly local nature available by the IM."@en ;
    # Source: ontology comment for localRulesOrRestrictionsDoc

    sh:class era:Document ; 
    # Source: ontology range is era:Document

    sh:minCount 1 ; 
    # Inferred: at least one document is required if the applicable parameter is 'Yes'

    sh:maxCount 1 ; 
    # Inferred: typically, a local rules document would have at most one direct reference

    sh:message "localRulesOrRestrictionsDoc: The CommonCharacteristicsSubset must have at least one local rules or restrictions document reference that is a Document. This error may be due to not having a value or having a value that is not an instance of Document."@en ;

    sh:nodeKind sh:IRI ; 
    # Inferred: links must refer to identifiable documents (IRI references)

    sh:path era:localRulesOrRestrictionsDoc ;

    sh:severity sh:Violation .

era-sh:LocalRulesOrRestrictionsDocApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:localRulesOrRestrictionsDoc ; 
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a local rules or restrictions document."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to local rules documents

    sh:message "localRulesOrRestrictionsDoc: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A local rules or restrictions document must be defined for the common characteristics subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:localRulesOrRestrictionsDoc ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LrsMethod a sh:PropertyShape ;
    era:affectedClass era:LinearPositioningSystem ;
    era:affectedProperty era:lrsMethod ;
    era:scope "local" ;

    rdfs:comment "The preferred line referencing system."@en ;
    # Source: ontology comment for lrsMethod

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "lrsMethod: The LinearPositioningSystem must have at most one line referencing system that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:lrsMethod ;

    sh:severity sh:Violation .

era-sh:LrsMethodApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LinearPositioningSystem ;
    era:affectedProperty era:lrsMethod ;
    era:scope "local" ;

    rdfs:comment "Applicable when the LinearPositioningSystem is defined and requires a line referencing system."@en ;
    # Source: inferred from the definition of LinearPositioningSystem and its relationship to line referencing systems

    sh:message "lrsMethod: This error is due to the LinearPositioningSystem {?systemLabel}, violating the rule: A line referencing system must be defined for the linear positioning system."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:lrsMethod ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MNvcontact a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:mNvcontact ;
    era:scope "local" ;

    rdfs:comment "On-Board system reaction when T_NVCONTACT expires according to the specification referenced in Appendix A-1, index [C]."@en ;
    # Source: ontology comment for mNvcontact

    sh:class era:ETCSNationalValuesObjParameter ;
    # Source: ontology range is era:ETCSNationalValuesObjParameter

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "mNvcontact: The CommonCharacteristicsSubset must have at most one mNvcontact reference that is an ETCSNationalValuesObjParameter. This error may be due to having more than one value or having a value that is not an instance of ETCSNationalValuesObjParameter."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable ETCS national values (IRI references)

    sh:path era:mNvcontact ;

    sh:severity sh:Violation .

era-sh:MNvcontactApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:mNvcontact ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a mNvcontact."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to mNvcontact

    sh:message "mNvcontact: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A mNvcontact must be defined for the common characteristics subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:mNvcontact ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MagneticBraking a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:magneticBraking ;
    era:rinfIndex "1.1.1.1.6.3" ;
    era:scope "local" ;

    rdfs:comment "Indication of limitations on the use of magnetic brakes."@en ;
    # Source: ontology comment for magneticBraking

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "magneticBraking: The CommonCharacteristicsSubset must have at most one magnetic braking reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:magneticBraking ;

    sh:severity sh:Violation .

era-sh:MagneticBrakingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:magneticBraking ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a magnetic braking indication."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to magnetic braking

    sh:message "magneticBraking: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A magnetic braking indication must be defined for the common characteristics subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:magneticBraking ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MagneticBrakingConditionsDocument a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:magneticBrakingConditionsDocument ;
    era:rinfIndex "1.1.1.1.6.5" ;
    era:scope "local" ;

    rdfs:comment "Electronic document available in two EU languages from the IM stored by the Agency with conditions for the use of magnetic brakes identified in 1.1.1.1.6.3."@en ;
    # Source: ontology comment for magneticBrakingConditionsDocument

    sh:class era:Document ;
    # Source: ontology range is era:Document

    sh:minCount 1 ;
    # Inferred: at least one document is required if the applicable parameter is 'Yes'

    sh:maxCount 1 ;
    # Inferred: typically, a magnetic braking conditions document would have at most one direct reference

    sh:message "magneticBrakingConditionsDocument: The CommonCharacteristicsSubset must have at least one magnetic braking conditions document reference that is a Document. This error may be due to not having a value or having a value that is not an instance of Document."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable documents (IRI references)

    sh:path era:magneticBrakingConditionsDocument ;

    sh:severity sh:Violation .

era-sh:MagneticBrakingConditionsDocumentApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:magneticBrakingConditionsDocument ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a magnetic braking conditions document."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to magnetic braking conditions documents

    sh:message "magneticBrakingConditionsDocument: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A magnetic braking conditions document must be defined for the common characteristics subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:magneticBrakingConditionsDocument ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Manufacturer a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:manufacturer ;
    era:scope "local" ;

    rdfs:comment "Vehicle manufacturer company."@en ;
    # Source: ontology comment for manufacturer

    sh:class era:OrganisationRole ;
    # Source: ontology range is era:OrganisationRole

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "manufacturer: The VehicleType must have at most one manufacturer reference that is an OrganisationRole. This error may be due to having more than one value or having a value that is not an instance of OrganisationRole."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable organisation roles (IRI references)

    sh:path era:manufacturer ;

    sh:severity sh:Violation .

era-sh:ManufacturerApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:manufacturer ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires a manufacturer."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to manufacturers

    sh:message "manufacturer: This error is due to the VehicleType {?vehicleLabel}, violating the rule: A manufacturer must be defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:manufacturer ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ManufacturingCountry a sh:PropertyShape ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:manufacturingCountry ;
    era:scope "local" ;

    rdfs:comment "Indicates the country in which a vehicle or vehicle type is manufactured."@en ;
    # Source: ontology comment for manufacturingCountry

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "manufacturingCountry: The Vehicle must have at most one manufacturing country reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:manufacturingCountry ;

    sh:severity sh:Violation .

era-sh:ManufacturingCountryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:manufacturingCountry ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Vehicle is defined and requires a manufacturing country."@en ;
    # Source: inferred from the definition of Vehicle and its relationship to manufacturing country

    sh:message "manufacturingCountry: This error is due to the Vehicle {?vehicleLabel}, violating the rule: A manufacturing country must be defined for the vehicle."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:manufacturingCountry ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaxSandingOutput a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:maxSandingOutput ;
    era:rinfIndex "1.1.1.3.7.17" ;
    era:scope "local" ;

    rdfs:comment "Maximum amount of sand accepted on the track within value of sanding output for 30s, given in grams."@en ;
    # Source: ontology comment for maxSandingOutput

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "maxSandingOutput: The TrainDetectionSystem must have at most one max sanding output reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:maxSandingOutput ;

    sh:severity sh:Violation .

era-sh:MaxSandingOutputApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:maxSandingOutput ;
    era:scope "local" ;

    rdfs:comment "Applicable when the TrainDetectionSystem is defined and requires a maximum sanding output."@en ;
    # Source: inferred from the definition of TrainDetectionSystem and its relationship to max sanding output

    sh:message "maxSandingOutput: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: A maximum sanding output must be defined for the train detection system."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:maxSandingOutput ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinAxleLoadVehicleCategory a sh:PropertyShape ;
    era:affectedClass era:MinAxleLoadVehicleCategory ;
    era:affectedProperty era:minAxleLoadVehicleCategory ;
    era:rinfIndex "1.1.1.3.7.11.1" ;
    era:scope "local" ;

    rdfs:comment "Represents the category of vehicle which is amended by value of minimum permitted axle load [tons]."@en ;
    # Source: ontology comment for minAxleLoadVehicleCategory

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:minCount 1 ;
    # Inferred: at least one category is required to define the minimum axle load vehicle category

    sh:maxCount 1 ;
    # Inferred: typically, a vehicle category would have at most one direct category relationship

    sh:message "minAxleLoadVehicleCategory: The MinAxleLoadVehicleCategory must have at least one reference that is a Concept. This error may be due to not having a value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:minAxleLoadVehicleCategory ;

    sh:severity sh:Violation .

era-sh:MinAxleLoadVehicleCategoryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:MinAxleLoadVehicleCategory ;
    era:affectedProperty era:minAxleLoadVehicleCategory ;
    era:scope "local" ;

    rdfs:comment "Applicable when the MinAxleLoadVehicleCategory is defined and requires a reference to a minimum axle load vehicle category."@en ;
    # Source: inferred from the definition of MinAxleLoadVehicleCategory and its relationship to minimum axle load categories

    sh:message "minAxleLoadVehicleCategory: This error is due to the MinAxleLoadVehicleCategory {?categoryLabel}, violating the rule: A minimum axle load vehicle category must be defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?categoryLabel WHERE {
        $this rdfs:label ?categoryLabel .
        FILTER NOT EXISTS { $this era:minAxleLoadVehicleCategory ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinVehicleImpedance a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:minVehicleImpedance ;
    era:rinfIndex "1.1.1.3.4.2" ;
    era:scope "local" ;

    rdfs:comment "Impedance as defined in the specification referenced in Appendix A-1, index [D]."@en ;
    # Source: ontology comment for minVehicleImpedance

    sh:class era:MinVehicleImpedance ;
    # Source: ontology range is era:MinVehicleImpedance

    sh:minCount 1 ;
    # Inferred: at least one impedance value is required for the train detection system

    sh:maxCount 1 ;
    # Inferred: typically, a train detection system would have at most one direct impedance relationship

    sh:message "minVehicleImpedance: The TrainDetectionSystem must have at most one min vehicle impedance reference that is an instance of MinVehicleImpedance. This error may be due to having more than one value or having a value that is not an instance of MinVehicleImpedance."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable impedance instances (IRI references)

    sh:path era:minVehicleImpedance ;

    sh:severity sh:Violation .

era-sh:MinVehicleImpedanceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:minVehicleImpedance ;
    era:scope "local" ;

    rdfs:comment "Applicable only for track circuits."@en ;
    # Source: dependencyNote indicates mandatory only for track circuits

    sh:message "minVehicleImpedance: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: A min vehicle impedance must be defined for the train detection system when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:minVehicleImpedance ?value } .
        # Source: RAG indicates applicability is only for track circuits
        FILTER EXISTS { $this era:trackCircuit ?tcValue } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinVehicleImpedanceVoltages a sh:PropertyShape ;
    era:affectedClass era:MinVehicleImpedance ;
    era:affectedProperty era:minVehicleImpedanceVoltages ;
    era:scope "local" ;

    rdfs:comment "Minimum vehicle impedance voltages applicable for vehicles equipped for 1500V or 3000V DC."@en ;
    # Source: inferred from ontology definition of minVehicleImpedanceVoltages

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:minCount 1 ;
    # Inferred: at least one voltage value is required for the minimum vehicle impedance

    sh:maxCount 1 ;
    # Inferred: typically, a vehicle impedance would have at most one direct voltage relationship

    sh:message "minVehicleImpedanceVoltages: The MinVehicleImpedance must have at most one min vehicle impedance voltage reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable voltage concepts (IRI references)

    sh:path era:minVehicleImpedanceVoltages ;

    sh:severity sh:Violation .

era-sh:MinVehicleImpedanceVoltagesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:MinVehicleImpedance ;
    era:affectedProperty era:minVehicleImpedanceVoltages ;
    era:scope "local" ;

    rdfs:comment "Applicable when the MinVehicleImpedance is defined and requires a minimum vehicle impedance voltage."@en ;
    # Source: inferred from the definition of MinVehicleImpedance and its relationship to voltage requirements

    sh:message "minVehicleImpedanceVoltages: This error is due to the MinVehicleImpedance {?impedanceLabel}, violating the rule: A minimum vehicle impedance voltage must be defined for the minimum vehicle impedance."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?impedanceLabel WHERE {
        $this rdfs:label ?impedanceLabel .
        FILTER NOT EXISTS { $this era:minVehicleImpedanceVoltages ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinimumVerticalRadius a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:minimumVerticalRadius ;
    era:rinfIndex "1.2.2.0.3.3" ;
    era:scope "local" ;

    rdfs:comment "Radius of the smallest vertical curve expressed in metres."@en ;
    # Source: ontology comment for minimumVerticalRadius

    sh:class era:MinimumVerticalRadius ;
    # Source: ontology range is era:MinimumVerticalRadius

    sh:minCount 1 ;
    # Inferred: at least one value is required for the minimum vertical radius

    sh:maxCount 1 ;
    # Inferred: typically, a vertical radius would have at most one direct relationship

    sh:message "minimumVerticalRadius: The CommonCharacteristicsSubset must have at least one minimum vertical radius reference that is an instance of MinimumVerticalRadius. This error may be due to not having a value or having a value that is not an instance of MinimumVerticalRadius."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable minimum vertical radius instances (IRI references)

    sh:path era:minimumVerticalRadius ;

    sh:severity sh:Violation .

era-sh:MinimumVerticalRadiusApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:minimumVerticalRadius ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a minimum vertical radius."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to minimum vertical radius

    sh:message "minimumVerticalRadius: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A minimum vertical radius must be defined for the common characteristics subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:minimumVerticalRadius ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:NationalLine a sh:PropertyShape ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:nationalLine ;
    era:rinfIndex "1.1.0.0.0.2" ;
    era:scope "local" ;

    rdfs:comment "Indicates a relationship with a national railway line at a specific kilometer point. For a Section of Line: unique line identification or unique line number within Member State."@en ;
    # Source: ontology comment for nationalLine

    sh:class era:LinearPositioningSystem ;
    # Source: ontology range is era:LinearPositioningSystem

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "nationalLine: The SectionOfLine must have at most one national line reference that is a LinearPositioningSystem. This error may be due to having more than one value or having a value that is not an instance of LinearPositioningSystem."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable linear positioning systems (IRI references)

    sh:path era:nationalLine ;

    sh:severity sh:Violation .

era-sh:NationalLineApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:nationalLine ;
    era:scope "local" ;

    rdfs:comment "Applicable when the SectionOfLine is defined and requires a national line identification."@en ;
    # Source: inferred from the definition of SectionOfLine and its relationship to national line identification

    sh:message "nationalLine: This error is due to the SectionOfLine {?sectionLabel}, violating the rule: A national line identification must be defined for the section of line."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sectionLabel WHERE {
        $this rdfs:label ?sectionLabel .
        FILTER NOT EXISTS { $this era:nationalLine ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Navigability a sh:PropertyShape ;
    era:affectedClass era:NetRelation ;
    era:affectedProperty era:navigability ;
    era:rinfIndex "1.1.1.0.1.2" ;
    era:scope "local" ;

    rdfs:comment "Indicates the navigation possibilities between two related topological linear elements."@en ;
    # Source: ontology comment for navigability

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "navigability: The NetRelation must have at most one navigability reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:navigability ;

    sh:severity sh:Violation .

era-sh:NavigabilityApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:NetRelation ;
    era:affectedProperty era:navigability ;
    era:scope "local" ;

    rdfs:comment "Applicable when the NetRelation is defined and requires a navigability indication."@en ;
    # Source: inferred from the definition of NetRelation and its relationship to navigability

    sh:message "navigability: This error is due to the NetRelation {?relationLabel}, violating the rule: A navigability indication must be defined for the net relation."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?relationLabel WHERE {
        $this rdfs:label ?relationLabel .
        FILTER NOT EXISTS { $this era:navigability ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:NetReference a sh:PropertyShape ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:netReference ;
    era:scope "local" ;

    rdfs:comment "The reference that a physical infrastructure element has on the topology. It may be used to define the positioning or a representation as a point, line or area."@en ;
    # Source: ontology comment for netReference

    sh:class era:NetBasicReference ;
    # Source: ontology range is era:NetBasicReference

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "netReference: The InfrastructureElement must have at most one net reference that is a NetBasicReference. This error may be due to having more than one value or having a value that is not an instance of NetBasicReference."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable net references (IRI references)

    sh:path era:netReference ;

    sh:severity sh:Violation .

era-sh:NetReferenceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:netReference ;
    era:scope "local" ;

    rdfs:comment "Applicable when the InfrastructureElement is defined and requires a net reference."@en ;
    # Source: inferred from the definition of InfrastructureElement and its relationship to net references

    sh:message "netReference: This error is due to the InfrastructureElement {?elementLabel}, violating the rule: A net reference must be defined for the infrastructure element."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?elementLabel WHERE {
        $this rdfs:label ?elementLabel .
        FILTER NOT EXISTS { $this era:netReference ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:NotApplicable a sh:PropertyShape ;
    era:affectedClass era:Parameter ; 
    era:affectedProperty era:notApplicable ; 
    era:scope "local" ;

    rdfs:comment "Reference to a property that is not applicable."@en ; 
    # Source: ontology comment for notApplicable

    sh:in ( "Y" "N" "NYA" ) ; 
    # Source: RAG indicates allowed values for applicability are Y, N, NYA

    sh:maxCount 1 ; 
    # Source: inferred from the nature of the property and its definition, indicating it should not be repeated

    sh:message "notApplicable: The Parameter must have at most one not applicable reference that is either Y, N, or NYA. This error may be due to having more than one value or having a value that is not one of the allowed values."@en ; 
    # Source: inferred from the nature of the property and its definition

    sh:path era:notApplicable ; 

    sh:severity sh:Violation .

era-sh:NotApplicableApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Parameter ; 
    era:affectedProperty era:notApplicable ; 
    era:scope "local" ;

    rdfs:comment "Applicable when the Parameter is defined and requires a not applicable indication."@en ; 
    # Source: inferred from the definition of Parameter and its relationship to applicability

    sh:message "notApplicable: This error is due to the Parameter {?parameterLabel}, violating the rule: A not applicable indication must be defined for the parameter."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?parameterLabel WHERE {
        $this rdfs:label ?parameterLabel .
        FILTER NOT EXISTS { $this era:notApplicable ?value } .
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:OfParameter a sh:PropertyShape ;
    era:affectedClass era:ParameterApplicability ;
    era:affectedProperty era:ofParameter ;
    era:scope "local" ;

    rdfs:comment "Indicates the parameter (object or datatype property) for which an applicability is being defined."@en ;
    # Source: ontology comment for ofParameter

    sh:class era:Parameter ;
    # Source: inferred from the relationship to ParameterApplicability

    sh:maxCount 1 ;
    # Source: inferred from the nature of the property and its definition, indicating it should not be repeated

    sh:message "ofParameter: The ParameterApplicability must have at most one ofParameter reference that is a Parameter. This error may be due to having more than one value or having a value that is not an instance of Parameter."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable parameters (IRI references)

    sh:path era:ofParameter ;

    sh:severity sh:Violation .

era-sh:OfParameterApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ParameterApplicability ;
    era:affectedProperty era:ofParameter ;
    era:scope "local" ;

    rdfs:comment "Applicable when the ParameterApplicability is defined and requires a reference to a parameter."@en ;
    # Source: inferred from the definition of ParameterApplicability and its relationship to parameters

    sh:message "ofParameter: This error is due to the ParameterApplicability {?applicabilityLabel}, violating the rule: An ofParameter must be defined for the parameter applicability."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?applicabilityLabel WHERE {
        $this rdfs:label ?applicabilityLabel .
        FILTER NOT EXISTS { $this era:ofParameter ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OnElement a sh:PropertyShape ;
    era:affectedClass era:LinearElementSection ;
    era:affectedProperty era:onElement ;
    era:scope "local" ;

    rdfs:comment "Specifies the linear element a section belongs to."@en ;
    # Source: ontology comment for onElement

    sh:class era:LinearElement ;
    # Source: ontology range is era:LinearElement

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "onElement: The LinearElementSection must have at most one onElement reference that is a LinearElement. This error may be due to having more than one value or having a value that is not an instance of LinearElement."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable linear elements (IRI references)

    sh:path era:onElement ;

    sh:severity sh:Violation .

era-sh:OnElementApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LinearElementSection ;
    era:affectedProperty era:onElement ;
    era:scope "local" ;

    rdfs:comment "Applicable when the LinearElementSection is defined and requires a reference to a linear element."@en ;
    # Source: inferred from the definition of LinearElementSection and its relationship to linear elements

    sh:message "onElement: This error is due to the LinearElementSection {?sectionLabel}, violating the rule: An onElement must be defined for the linear element section."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sectionLabel WHERE {
        $this rdfs:label ?sectionLabel .
        FILTER NOT EXISTS { $this era:onElement ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OnLinearElement a sh:PropertyShape ;
    era:affectedClass era:TopologicalCoordinate ;
    era:affectedProperty era:onLinearElement ;
    era:scope "local" ;

    rdfs:comment "Specifies the linear element a coordinate is associated with."@en ;
    # Source: ontology comment for onLinearElement

    sh:class era:LinearElement ;
    # Source: ontology range is era:LinearElement

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "onLinearElement: The TopologicalCoordinate must have at most one onLinearElement reference that is a LinearElement. This error may be due to having more than one value or having a value that is not an instance of LinearElement."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable linear elements (IRI references)

    sh:path era:onLinearElement ;

    sh:severity sh:Violation .

era-sh:OnLinearElementApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TopologicalCoordinate ;
    era:affectedProperty era:onLinearElement ;
    era:scope "local" ;

    rdfs:comment "Applicable when the TopologicalCoordinate is defined and requires a reference to a linear element."@en ;
    # Source: inferred from the definition of TopologicalCoordinate and its relationship to linear elements

    sh:message "onLinearElement: This error is due to the TopologicalCoordinate {?coordinateLabel}, violating the rule: An onLinearElement must be defined for the topological coordinate."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?coordinateLabel WHERE {
        $this rdfs:label ?coordinateLabel .
        FILTER NOT EXISTS { $this era:onLinearElement ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OpEnd a sh:PropertyShape ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:opEnd ;
    era:rinfIndex "1.1.0.0.0.4" ;
    era:scope "local" ;

    rdfs:comment "Operational point at the end of section of line (kilometres increasing from start OP to the end OP)."@en ;
    # Source: ontology comment for opEnd

    sh:class era:OperationalPoint ;
    # Source: ontology range is era:OperationalPoint

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "opEnd: The SectionOfLine must have at most one operational point at the end reference that is an instance of OperationalPoint. This error may be due to having more than one value or having a value that is not an instance of OperationalPoint."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable operational points (IRI references)

    sh:path era:opEnd ;

    sh:severity sh:Violation .

era-sh:OpEndApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:opEnd ;
    era:scope "local" ;

    rdfs:comment "Applicable when the SectionOfLine is defined and requires an operational point at the end."@en ;
    # Source: inferred from the definition of SectionOfLine and its relationship to operational points

    sh:message "opEnd: This error is due to the SectionOfLine {?sectionLabel}, violating the rule: An operational point at the end must be defined for the section of line."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sectionLabel WHERE {
        $this rdfs:label ?sectionLabel .
        FILTER NOT EXISTS { $this era:opEnd ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OpInfoPerCountry a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:opInfoPerCountry ;
    era:scope "local" ;

    rdfs:comment "Relates an operational point that represents the general information of a border point to the information that each country maintains for the border point."@en ;
    # Source: ontology comment for opInfoPerCountry

    sh:class era:OperationalPoint ;
    # Source: ontology range is era:OperationalPoint

    sh:maxCount 1 ;
    # Inferred: typically, an operational point would have at most one reference to country-specific information

    sh:message "opInfoPerCountry: The OperationalPoint must have at most one opInfoPerCountry reference that is an OperationalPoint. This error may be due to having more than one value or having a value that is not an instance of OperationalPoint."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable operational points (IRI references)

    sh:path era:opInfoPerCountry ;

    sh:severity sh:Violation .

era-sh:OpInfoPerCountryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:opInfoPerCountry ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OperationalPoint is defined and requires country-specific operational point information."@en ;
    # Source: inferred from the definition of OperationalPoint and its relationship to country-specific information

    sh:message "opInfoPerCountry: This error is due to the OperationalPoint {?pointLabel}, violating the rule: An opInfoPerCountry must be defined for the operational point."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?pointLabel WHERE {
        $this rdfs:label ?pointLabel .
        FILTER NOT EXISTS { $this era:opInfoPerCountry ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OpName a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:opName ;
    era:rinfIndex "1.2.0.0.0.1" ;
    era:scope "local" ;

    rdfs:comment "Name normally related to the town or village or to traffic control purpose."@en ;
    # Source: ontology comment for opName

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "opName: The OperationalPoint must have at most one opName reference that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:opName ;

    sh:severity sh:Violation .

era-sh:OpNameApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:opName ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OperationalPoint is defined and requires a name."@en ;
    # Source: inferred from the definition of OperationalPoint and its relationship to the name

    sh:message "opName: This error is due to the OperationalPoint {?pointLabel}, violating the rule: An opName must be defined for the operational point."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?pointLabel WHERE {
        $this rdfs:label ?pointLabel .
        FILTER NOT EXISTS { $this era:opName ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OpStart a sh:PropertyShape ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:opStart ;
    era:rinfIndex "1.1.0.0.0.3" ;
    era:scope "local" ;

    rdfs:comment "Operational point at the start of section of line (kilometres increasing from start OP to the end OP)."@en ;
    # Source: ontology comment for opStart

    sh:class era:OperationalPoint ;
    # Source: ontology range is era:OperationalPoint

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "opStart: The SectionOfLine must have at most one operational point at the start reference that is an instance of OperationalPoint. This error may be due to having more than one value or having a value that is not an instance of OperationalPoint."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable operational points (IRI references)

    sh:path era:opStart ;

    sh:severity sh:Violation .

era-sh:OpStartApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:opStart ;
    era:scope "local" ;

    rdfs:comment "Applicable when the SectionOfLine is defined and requires an operational point at the start."@en ;
    # Source: inferred from the definition of SectionOfLine and its relationship to operational points

    sh:message "opStart: This error is due to the SectionOfLine {?sectionLabel}, violating the rule: An operational point at the start must be defined for the section of line."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sectionLabel WHERE {
        $this rdfs:label ?sectionLabel .
        FILTER NOT EXISTS { $this era:opStart ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OpType a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:opType ;
    era:rinfIndex "1.2.0.0.0.4" ;
    era:scope "local" ;

    rdfs:comment "Type of facility in relation to the dominating operational functions."@en ;
    # Source: ontology comment for opType

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "opType: The OperationalPoint must have at most one opType reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:opType ;

    sh:severity sh:Violation .

era-sh:OpTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:opType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OperationalPoint is defined and requires an operational point type indication."@en ;
    # Source: inferred from the definition of OperationalPoint and its relationship to operational point types

    sh:message "opType: This error is due to the OperationalPoint {?pointLabel}, violating the rule: An opType must be defined for the operational point."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?pointLabel WHERE {
        $this rdfs:label ?pointLabel .
        FILTER NOT EXISTS { $this era:opType ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OpTypeGaugeChangeover a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:opTypeGaugeChangeover ;
    era:rinfIndex "1.2.0.0.0.4.1" ;
    era:scope "local" ;

    rdfs:comment "Type of track gauge changeover facility."@en ;
    # Source: ontology comment for opTypeGaugeChangeover

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "opTypeGaugeChangeover: The OperationalPoint must have at most one opTypeGaugeChangeover reference that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:opTypeGaugeChangeover ;

    sh:severity sh:Violation .

era-sh:OpTypeGaugeChangeoverApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:opTypeGaugeChangeover ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OperationalPoint is defined and requires a type of track gauge changeover facility."@en ;
    # Source: inferred from the definition of OperationalPoint and its relationship to gauge changeover facilities

    sh:message "opTypeGaugeChangeover: This error is due to the OperationalPoint {?pointLabel}, violating the rule: An opTypeGaugeChangeover must be defined for the operational point."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?pointLabel WHERE {
        $this rdfs:label ?pointLabel .
        FILTER NOT EXISTS { $this era:opTypeGaugeChangeover ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OperatingLanguage a sh:PropertyShape ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:operatingLanguage ;
    era:scope "local" ;

    rdfs:comment "The language or languages used in daily operation by infrastructure manager and published in its Network Statement, for the communication of operational or safety related messages between the staff of the infrastructure manager and the railway undertaking."@en ;
    # Source: ontology comment for operatingLanguage

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "operatingLanguage: The InfrastructureElement must have at most one operating language reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:operatingLanguage ;

    sh:severity sh:Violation .

era-sh:OperatingLanguageApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:operatingLanguage ;
    era:scope "local" ;

    rdfs:comment "Applicable when the InfrastructureElement is defined and requires an operating language indication."@en ;
    # Source: inferred from the definition of InfrastructureElement and its relationship to operating languages

    sh:message "operatingLanguage: This error is due to the InfrastructureElement {?elementLabel}, violating the rule: An operating language must be defined for the infrastructure element."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?elementLabel WHERE {
        $this rdfs:label ?elementLabel .
        FILTER NOT EXISTS { $this era:operatingLanguage ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OperationalRegimeType a sh:PropertyShape ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:operationalRegimeType ;
    era:rinfIndex "1.1.0.0.1.3" ;
    era:scope "local" ;

    rdfs:comment "Defines the operational regime type for a section of line."@en ;
    # Source: inferred from the ontology definition of operationalRegimeType

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "operationalRegimeType: The SectionOfLine must have at most one operational regime type reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:operationalRegimeType ;

    sh:severity sh:Violation .

era-sh:OperationalRegimeTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:operationalRegimeType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the SectionOfLine is defined and requires an operational regime type indication."@en ;
    # Source: inferred from the definition of SectionOfLine and its relationship to operational regime types

    sh:message "operationalRegimeType: This error is due to the SectionOfLine {?sectionLabel}, violating the rule: An operational regime type must be defined for the section of line."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sectionLabel WHERE {
        $this rdfs:label ?sectionLabel .
        FILTER NOT EXISTS { $this era:operationalRegimeType ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OperationalRestriction a sh:PropertyShape ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:operationalRestriction ;
    era:scope "local" ;

    rdfs:comment "Indicates an operational restriction of vehicle or wagon. Parking brake type (if the vehicle is fitted with it)."@en ;
    # Source: ontology comment for operationalRestriction

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "operationalRestriction: The Vehicle must have at most one operational restriction reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:operationalRestriction ;

    sh:severity sh:Violation .

era-sh:OperationalRestrictionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:operationalRestriction ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Vehicle is defined and requires an operational restriction indication."@en ;
    # Source: inferred from the definition of Vehicle and its relationship to operational restrictions

    sh:message "operationalRestriction: This error is due to the Vehicle {?vehicleLabel}, violating the rule: An operational restriction must be defined for the vehicle."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:operationalRestriction ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Orientation a sh:PropertyShape ;
    era:affectedClass era:Orientation ;
    era:affectedProperty era:orientation ;
    era:scope "local" ;

    rdfs:comment "The orientation of the object in relation to the carrier linear element. Possible values: Normal, Opposite, Both."@en ;
    # Source: ontology comment for orientation

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "orientation: The Orientation must have at most one orientation reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:orientation ;

    sh:severity sh:Violation .

era-sh:OrientationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Orientation ;
    era:affectedProperty era:orientation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Orientation is defined and requires an orientation indication."@en ;
    # Source: inferred from the definition of Orientation and its relationship to orientation values

    sh:message "orientation: This error is due to the Orientation {?orientationLabel}, violating the rule: An orientation must be defined for the orientation."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?orientationLabel WHERE {
        $this rdfs:label ?orientationLabel .
        FILTER NOT EXISTS { $this era:orientation ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OsmClass a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:osmClass ; 
    era:scope "local" ;

    rdfs:comment "(deprecated) not in use anymore"@en ; 
    # Source: ontology comment for osmClass

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ; 
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ; 
    # Source: inferred from the nature of the property and its definition

    sh:message "osmClass: The CommonCharacteristicsSubset must have at most one osmClass reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ; 
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:osmClass ;

    sh:severity sh:Violation .

era-sh:OsmClassApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:osmClass ; 
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires an OpenStreetMap class indication."@en ; 
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to osmClass

    sh:message "osmClass: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: An osmClass must be defined for the common characteristics subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:osmClass ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OtherCantDeficiencyBasicSSP a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:otherCantDeficiencyBasicSSP ;
    era:scope "local" ;

    rdfs:comment "Essential information for drivers of trains with a worse (lower) tolerated cant deficiency than those for which the ETCS trackside provides SSP (Static Speed Profiles) in conjunction with parameter 'Cant Deficiency used for the basic SSP'."@en ;
    # Source: ontology comment for otherCantDeficiencyBasicSSP

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "otherCantDeficiencyBasicSSP: The CommonCharacteristicsSubset must have at most one otherCantDeficiencyBasicSSP reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:otherCantDeficiencyBasicSSP ;

    sh:severity sh:Violation .

era-sh:OtherCantDeficiencyBasicSSPApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:otherCantDeficiencyBasicSSP ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires an indication of other cant deficiency."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to other cant deficiency

    sh:message "otherCantDeficiencyBasicSSP: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: An otherCantDeficiencyBasicSSP must be defined for the common characteristics subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:otherCantDeficiencyBasicSSP ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OtherPantographHead a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:otherPantographHead ;
    era:rinfIndex "1.1.1.2.3.2" ;
    era:scope "local" ;

    rdfs:comment "Indication of pantograph heads which are allowed to be used."@en ;
    # Source: ontology comment for otherPantographHead

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: inferred from the nature of the property and its definition

    sh:message "otherPantographHead: The CommonCharacteristicsSubset must have at most one other pantograph head reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:otherPantographHead ;

    sh:severity sh:Violation .

era-sh:OtherPantographHeadApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:otherPantographHead ;
    era:scope "local" ;

    rdfs:comment "Applicable only if 'Overhead contact line (OCL)' is selected in 1.1.1.2.2.1.1."@en ;
    # Source: RAG explanation on applicability

    sh:message "otherPantographHead: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: An other pantograph head must be defined only if 'Overhead contact line (OCL)' is selected."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:otherPantographHead ?value } .
        # Assuming there is a property indicating the selection of OCL
        FILTER NOT EXISTS { ?oclSelection era:overheadContactLine ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OtherTrainProtection a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:otherTrainProtection ;
    era:rinfIndex "1.1.1.3.10.2" ;
    era:scope "local" ;

    rdfs:comment "Indication of existence of other systems than ETCS for degraded situation."@en ;
    # Source: ontology comment for otherTrainProtection

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "otherTrainProtection: The CommonCharacteristicsSubset must have at most one other train protection reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:otherTrainProtection ;

    sh:severity sh:Violation .

era-sh:OtherTrainProtectionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:otherTrainProtection ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires an indication of other train protection systems."@en ;
    # Source: inferred from the definition of otherTrainProtection and its relationship to degraded situations

    sh:message "otherTrainProtection: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: An other train protection must be defined for the common characteristics subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:otherTrainProtection ?value } .
        # Assuming there is a property indicating the selection of parameter 1.1.1.3.10.1
        FILTER NOT EXISTS { $this era:etcsLevel ?etcsValue . FILTER(?etcsValue = "none") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ParameterApplicability a sh:PropertyShape ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:parameterApplicability ;
    era:scope "local" ;

    rdfs:comment "Relates an infrastructure element or a common characteristics subset with the applicability of a certain parameter."@en ;
    # Source: ontology comment for parameterApplicability

    sh:class era:ParameterApplicability ;
    # Source: ontology range is era:ParameterApplicability

    sh:maxCount 1 ;
    # Inferred: expected at most one applicability reference for each infrastructure element or subset

    sh:message "parameterApplicability: The InfrastructureElement must have at most one parameter applicability reference that is a ParameterApplicability. This error may be due to having more than one value or having a value that is not an instance of ParameterApplicability."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable parameter applicability instances (IRI references)

    sh:path era:parameterApplicability ;

    sh:severity sh:Violation .

era-sh:ParameterApplicabilityApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:parameterApplicability ;
    era:scope "local" ;

    rdfs:comment "Applicable when the InfrastructureElement is defined and requires an indication of parameter applicability."@en ;
    # Source: inferred from the definition of InfrastructureElement and its relationship to parameter applicability

    sh:message "parameterApplicability: This error is due to the InfrastructureElement {?elementLabel}, violating the rule: A parameter applicability must be defined for the infrastructure element."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?elementLabel WHERE {
        $this rdfs:label ?elementLabel .
        FILTER NOT EXISTS { $this era:parameterApplicability ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ParameterValueType a sh:PropertyShape ;
    era:affectedClass era:ParameterApplicability ;
    era:affectedProperty era:parameterValueType ;
    era:scope "local" ;

    rdfs:comment "In the case of a change in the value of a parameter, there needs to be an information on the type of the new value. The 'Value type' will have different possible options: 'nominal', 'planned temporary restriction', 'permanent restriction', 'planned temporary closure'."@en ;
    # Source: ontology comment for parameterValueType

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "parameterValueType: The ParameterApplicability must have at most one parameter value type reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:parameterValueType ;

    sh:severity sh:Violation .

era-sh:ParameterValueTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ParameterApplicability ;
    era:affectedProperty era:parameterValueType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the ParameterApplicability is defined and requires a parameter value type indication."@en ;
    # Source: inferred from the definition of ParameterApplicability and its relationship to parameter value types

    sh:message "parameterValueType: This error is due to the ParameterApplicability {?applicabilityLabel}, violating the rule: A parameter value type must be defined for the parameter applicability."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?applicabilityLabel WHERE {
        $this rdfs:label ?applicabilityLabel .
        FILTER NOT EXISTS { $this era:parameterValueType ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ParkingBrakeType a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:parkingBrakeType ;
    era:scope "local" ;

    rdfs:comment "Parking brake type (if the vehicle is fitted with it)."@en ;
    # Source: ontology comment for parkingBrakeType

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: inferred from the nature of the property and its definition, indicating only one parking brake type should be specified

    sh:message "parkingBrakeType: The VehicleType must have at most one parking brake type reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:parkingBrakeType ;

    sh:severity sh:Violation .

era-sh:ParkingBrakeTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:parkingBrakeType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires a parking brake type indication."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to parking brake types

    sh:message "parkingBrakeType: This error is due to the VehicleType {?vehicleLabel}, violating the rule: A parking brake type must be defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:parkingBrakeType ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PassesThroughTunnel a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:passesThroughTunnel ;
    era:scope "local" ;

    rdfs:comment "References a tunnel that a track goes through."@en ;
    # Source: ontology comment for passesThroughTunnel

    sh:class era:Tunnel ;
    # Source: ontology range is era:Tunnel

    sh:maxCount 1 ;
    # Inferred: expected at most one tunnel reference for each running track

    sh:message "passesThroughTunnel: The RunningTrack must have at most one passesThroughTunnel reference that is a Tunnel. This error may be due to having more than one value or having a value that is not an instance of Tunnel."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable tunnels (IRI references)

    sh:path era:passesThroughTunnel ;

    sh:severity sh:Violation .

era-sh:PassesThroughTunnelApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:passesThroughTunnel ;
    era:scope "local" ;

    rdfs:comment "Applicable when the RunningTrack is defined and requires a tunnel indication."@en ;
    # Source: inferred from the definition of RunningTrack and its relationship to tunnels

    sh:message "passesThroughTunnel: This error is due to the RunningTrack {?trackLabel}, violating the rule: A passesThroughTunnel must be defined for the running track if it passes through a tunnel."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:passesThroughTunnel ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PlatformEdge a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:platformEdge ;
    era:scope "local" ;

    rdfs:comment "Reference to a related platform edge."@en ;
    # Source: ontology comment for platformEdge

    sh:class era:PlatformEdge ;
    # Source: ontology range is era:PlatformEdge

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "platformEdge: The RunningTrack must have at most one platform edge reference that is a PlatformEdge. This error may be due to having more than one value or having a value that is not an instance of PlatformEdge."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable platform edges (IRI references)

    sh:path era:platformEdge ;

    sh:severity sh:Violation .

era-sh:PlatformEdgeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:platformEdge ;
    era:scope "local" ;

    rdfs:comment "Applicable when the RunningTrack is defined and requires a platform edge indication."@en ;
    # Source: inferred from the definition of RunningTrack and its relationship to platform edges

    sh:message "platformEdge: This error is due to the RunningTrack {?trackLabel}, violating the rule: A platform edge must be defined for the running track if it has a platform edge."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:platformEdge ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PlatformHeight a sh:PropertyShape ;
    era:affectedClass era:PlatformEdge ;
    era:affectedProperty era:platformHeight ;
    era:scope "local" ;

    rdfs:comment "Distance between the upper surface of platform and running surface of the neighbouring track. It is the nominal value expressed in millimetres."@en ;
    # Source: ontology comment for platformHeight

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "platformHeight: The PlatformEdge must have at most one platform height reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:platformHeight ;

    sh:severity sh:Violation .

era-sh:PlatformHeightApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PlatformEdge ;
    era:affectedProperty era:platformHeight ;
    era:scope "local" ;

    rdfs:comment "Applicable when the PlatformEdge is defined and requires a platform height indication."@en ;
    # Source: inferred from the definition of PlatformEdge and its relationship to platform height

    sh:message "platformHeight: This error is due to the PlatformEdge {?edgeLabel}, violating the rule: A platform height must be defined for the platform edge."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?edgeLabel WHERE {
        $this rdfs:label ?edgeLabel .
        FILTER NOT EXISTS { $this era:platformHeight ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PreviousVehicleType a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:previousVehicleType ;
    era:scope "local" ;

    rdfs:comment "Denotes the previous VehicleType."@en ;
    # Source: ontology comment for previousVehicleType

    sh:class era:VehicleType ;
    # Source: ontology range is era:VehicleType

    sh:maxCount 1 ;
    # Source: inferred from the nature of the property and its definition, indicating only one previous vehicle type should be specified

    sh:message "previousVehicleType: The VehicleType must have at most one previous vehicle type reference that is a VehicleType. This error may be due to having more than one value or having a value that is not an instance of VehicleType."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable vehicle types (IRI references)

    sh:path era:previousVehicleType ;

    sh:severity sh:Violation .

era-sh:PreviousVehicleTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:previousVehicleType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires an indication of the previous vehicle type."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to previous vehicle types

    sh:message "previousVehicleType: This error is due to the VehicleType {?vehicleLabel}, violating the rule: A previous vehicle type must be defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:previousVehicleType ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PrimaryLocation a sh:PropertyShape ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:primaryLocation ;
    era:rinfIndex "1.2.0.0.0.3" ;
    era:scope "local" ;

    rdfs:comment "Primary location code developed for information exchange in accordance with the TSIs relating to the telematics applications subsystem."@en ;
    # Source: ontology comment for primaryLocation

    sh:class era:PrimaryLocation ;
    # Source: ontology range is era:PrimaryLocation

    sh:maxCount 1 ;
    # Inferred: expected at most one primary location reference for each infrastructure element

    sh:message "primaryLocation: The InfrastructureElement must have at most one primary location reference that is a PrimaryLocation. This error may be due to having more than one value or having a value that is not an instance of PrimaryLocation."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable primary locations (IRI references)

    sh:path era:primaryLocation ;

    sh:severity sh:Violation .

era-sh:PrimaryLocationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:primaryLocation ;
    era:scope "local" ;

    rdfs:comment "Applicable only in case when a primary location exists."@en ;
    # Source: ontology dependency note for primaryLocation

    sh:message "primaryLocation: This error is due to the InfrastructureElement {?elementLabel}, violating the rule: A primary location must be defined for the infrastructure element if a primary location exists."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?elementLabel WHERE {
        $this rdfs:label ?elementLabel .
        FILTER NOT EXISTS { $this era:primaryLocation ?value } .
        # Assuming there is a property indicating the existence of a primary location
        FILTER NOT EXISTS { ?primaryLocation era:exists ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ProfileNumberSemiTrailers a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:profileNumberSemiTrailers ;
    era:rinfIndex "1.1.1.1.3.5" ;
    era:scope "local" ;

    rdfs:comment "Coding for combined transport for semi-trailers (for all freight and mixed-traffic lines) in accordance with the specification referenced in Appendix A-1, index [B]."@en ;
    # Source: ontology comment for profileNumberSemiTrailers

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one profile number for semi-trailers

    sh:message "profileNumberSemiTrailers: The CommonCharacteristicsSubset must have at most one profile number for semi-trailers that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:profileNumberSemiTrailers ;

    sh:severity sh:Violation .

era-sh:ProfileNumberSemiTrailersApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:profileNumberSemiTrailers ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a profile number for semi-trailers."@en ;
    # Source: inferred from the definition of profileNumberSemiTrailers and its relationship to common characteristics

    sh:message "profileNumberSemiTrailers: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A profile number for semi-trailers must be defined for the common characteristics subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:profileNumberSemiTrailers ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ProfileNumberSwapBodies a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:profileNumberSwapBodies ;
    era:rinfIndex "1.1.1.1.3.4" ;
    era:scope "local" ;

    rdfs:comment "Coding for combined transport with swap bodies (for all freight and mixed-traffic lines) in accordance with the specification referenced in Appendix A-1, index [B]."@en ;
    # Source: ontology comment for profileNumberSwapBodies

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one profile number for swap bodies

    sh:message "profileNumberSwapBodies: The CommonCharacteristicsSubset must have at most one profile number for swap bodies that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:profileNumberSwapBodies ;

    sh:severity sh:Violation .

era-sh:ProfileNumberSwapBodiesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:profileNumberSwapBodies ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a profile number for swap bodies."@en ;
    # Source: inferred from the definition of profileNumberSwapBodies and its relationship to common characteristics

    sh:message "profileNumberSwapBodies: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A profile number for swap bodies must be defined for the common characteristics subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:profileNumberSwapBodies ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ProtectionLegacySystem a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:protectionLegacySystem ;
    era:scope "local" ;

    rdfs:comment "Indication of which class B system is installed."@en ;
    # Source: ontology comment for protectionLegacySystem

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one protection legacy system reference for each vehicle type

    sh:message "protectionLegacySystem: The VehicleType must have at most one protection legacy system reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:protectionLegacySystem ;

    sh:severity sh:Violation .

era-sh:ProtectionLegacySystemApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:protectionLegacySystem ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires an indication of the protection legacy system."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to protection legacy systems

    sh:message "protectionLegacySystem: This error is due to the VehicleType {?vehicleLabel}, violating the rule: A protection legacy system must be defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:protectionLegacySystem ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:QNvdriverAdhes a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:qNvdriverAdhes ;
    era:scope "local" ;

    rdfs:comment "Qualifier determining whether the driver is allowed to modify the adhesion factor used by the ETCS on-board to calculate the braking curves."@en ;
    # Source: ontology comment for qNvdriverAdhes

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "qNvdriverAdhes: The CommonCharacteristicsSubset must have at most one qNvdriverAdhes reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:qNvdriverAdhes ;

    sh:severity sh:Violation .

era-sh:QNvdriverAdhesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:qNvdriverAdhes ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a qNvdriverAdhes indication."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to qNvdriverAdhes

    sh:message "qNvdriverAdhes: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A qNvdriverAdhes must be defined for the common characteristics subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:qNvdriverAdhes ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:QNvemrrls a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:qNvemrrls ;
    era:scope "local" ;

    rdfs:comment "Qualifier defining whether the application of the emergency brake for reasons other than a trip can be revoked as soon as the conditions for it have disappeared or after the train has come to a complete standstill."@en ;
    # Source: ontology comment for qNvemrrls

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: inferred from the nature of the property and its definition, indicating only one qNvemrrls should be specified

    sh:message "qNvemrrls: The CommonCharacteristicsSubset must have at most one qNvemrrls reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:qNvemrrls ;

    sh:severity sh:Violation .

era-sh:QNvemrrlsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:qNvemrrls ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a qNvemrrls indication."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to qNvemrrls

    sh:message "qNvemrrls: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A qNvemrrls must be defined for the common characteristics subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:qNvemrrls ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:QuieterRoutesExemptedCountry a sh:PropertyShape ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:quieterRoutesExemptedCountry ;
    era:scope "local" ;

    rdfs:comment "Country where a vehicle may operate without noise restrictions."@en ;
    # Source: ontology comment for quieterRoutesExemptedCountry

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one exempted country reference for each vehicle

    sh:message "quieterRoutesExemptedCountry: The Vehicle must have at most one quieterRoutesExemptedCountry reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:quieterRoutesExemptedCountry ;

    sh:severity sh:Violation .

era-sh:QuieterRoutesExemptedCountryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:quieterRoutesExemptedCountry ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Vehicle is defined and requires an indication of the exempted country."@en ;
    # Source: inferred from the definition of Vehicle and its relationship to quieter routes

    sh:message "quieterRoutesExemptedCountry: This error is due to the Vehicle {?vehicleLabel}, violating the rule: A quieterRoutesExemptedCountry must be defined for the vehicle if it operates on quieter routes."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:quieterRoutesExemptedCountry ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RailInclination a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ; 
    era:affectedProperty era:railInclination ; 
    era:scope "local" ; 

    rdfs:comment "An angle defining the inclination of the head of a rail relative to the running surface."@en ; 
    # Source: ontology comment for railInclination

    sh:datatype xsd:integer ; 
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ; 
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "railInclination: The RunningTrack must have at most one rail inclination value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ; 

    sh:path era:railInclination ; 

    sh:severity sh:Violation .

era-sh:RailInclinationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ; 
    era:affectedProperty era:railInclination ; 
    era:scope "local" ; 

    rdfs:comment "Applicable when the RunningTrack is defined and requires a rail inclination indication."@en ; 
    # Source: inferred from the definition of RunningTrack and its relationship to rail inclination

    sh:message "railInclination: This error is due to the RunningTrack {?trackLabel}, violating the rule: A rail inclination must be defined for the running track."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:railInclination ?value } .
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:ReasonsEtcsRadioBlockCenterReject a sh:PropertyShape ;
    era:affectedClass era:RadioBlockCenter ;
    era:affectedProperty era:reasonsEtcsRadioBlockCenterReject ;
    era:scope "local" ;

    rdfs:comment "List of cases subject to system design choices made by the infrastructure manager according to the specification referenced in Appendix A-1, index [C]."@en ;
    # Source: ontology comment for reasonsEtcsRadioBlockCenterReject

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "reasonsEtcsRadioBlockCenterReject: The RadioBlockCenter must have at most one reason for ETCS Radio Block Center rejection that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:reasonsEtcsRadioBlockCenterReject ;

    sh:severity sh:Violation .

era-sh:ReasonsEtcsRadioBlockCenterRejectApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RadioBlockCenter ;
    era:affectedProperty era:reasonsEtcsRadioBlockCenterReject ;
    era:scope "local" ;

    rdfs:comment "Applicable when the RadioBlockCenter is defined and requires a reason for ETCS Radio Block Center rejection."@en ;
    # Source: inferred from the definition of RadioBlockCenter and its relationship to rejection reasons

    sh:message "reasonsEtcsRadioBlockCenterReject: This error is due to the RadioBlockCenter {?centerLabel}, violating the rule: A reason for ETCS Radio Block Center rejection must be defined for the radio block center."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?centerLabel WHERE {
        $this rdfs:label ?centerLabel .
        FILTER NOT EXISTS { $this era:reasonsEtcsRadioBlockCenterReject ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ReferenceBorderPoint a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:referenceBorderPoint ;
    era:scope "local" ;

    rdfs:comment "Relates an operational point that is a border point with an instance of the list of reference border points that are specified in the RINF Application Guide."@en ;
    # Source: ontology comment for referenceBorderPoint

    sh:class era:ReferenceBorderPoint ;
    # Source: ontology range is era:ReferenceBorderPoint

    sh:maxCount 1 ;
    # Inferred: expected at most one reference border point for each operational point

    sh:message "referenceBorderPoint: The OperationalPoint must have at most one reference border point that is a ReferenceBorderPoint. This error may be due to having more than one value or having a value that is not an instance of ReferenceBorderPoint."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable reference border points (IRI references)

    sh:path era:referenceBorderPoint ;

    sh:severity sh:Violation .

era-sh:ReferenceBorderPointApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:referenceBorderPoint ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OperationalPoint is defined and requires a reference border point."@en ;
    # Source: inferred from the definition of OperationalPoint and its relationship to reference border points

    sh:message "referenceBorderPoint: This error is due to the OperationalPoint {?pointLabel}, violating the rule: A reference border point must be defined for the operational point."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?pointLabel WHERE {
        $this rdfs:label ?pointLabel .
        FILTER NOT EXISTS { $this era:referenceBorderPoint ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Role a sh:PropertyShape ;
    era:affectedClass era:Body ;
    era:affectedProperty era:role ;
    era:scope "local" ;

    rdfs:comment "Indicates the relationship of a Body to the organisation roles that it can play."@en ;
    # Source: ontology comment for role

    sh:class era:OrganisationRole ;
    # Source: ontology range is era:OrganisationRole

    sh:maxCount 1 ;
    # Inferred: expected at most one role reference for each body

    sh:message "role: The Body must have at most one role reference that is an OrganisationRole. This error may be due to having more than one value or having a value that is not an instance of OrganisationRole."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable organisation roles (IRI references)

    sh:path era:role ;

    sh:severity sh:Violation .

era-sh:RoleApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Body ;
    era:affectedProperty era:role ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Body is defined and requires a role indication."@en ;
    # Source: inferred from the definition of Body and its relationship to roles

    sh:message "role: This error is due to the Body {?bodyLabel}, violating the rule: A role must be defined for the body."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?bodyLabel WHERE {
        $this rdfs:label ?bodyLabel .
        FILTER NOT EXISTS { $this era:role ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RoleOf a sh:PropertyShape ;
    era:affectedClass era:OrganisationRole ;
    era:affectedProperty era:roleOf ;
    era:scope "local" ;

    rdfs:comment "Indicates the corresponding Body that plays a certain organisation role."@en ;
    # Source: ontology comment for roleOf

    sh:class era:Body ;
    # Source: ontology range is era:Body

    sh:maxCount 1 ;
    # Inferred: expected at most one role reference for each organisation role

    sh:message "roleOf: The OrganisationRole must have at most one roleOf reference that is a Body. This error may be due to having more than one value or having a value that is not an instance of Body."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable bodies (IRI references)

    sh:path era:roleOf ;

    sh:severity sh:Violation .

era-sh:RoleOfApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OrganisationRole ;
    era:affectedProperty era:roleOf ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OrganisationRole is defined and requires a role indication."@en ;
    # Source: inferred from the definition of OrganisationRole and its relationship to roles

    sh:message "roleOf: This error is due to the OrganisationRole {?roleLabel}, violating the rule: A roleOf must be defined for the organisation role."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?roleLabel WHERE {
        $this rdfs:label ?roleLabel .
        FILTER NOT EXISTS { $this era:roleOf ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RollingStockFireCategory a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:rollingStockFireCategory ;
    era:scope "local" ;

    rdfs:comment "Categorisation on how a passenger train with a fire on board will continue to operate for a defined time period."@en ;
    # Source: ontology comment for rollingStockFireCategory

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one fire category for each tunnel

    sh:message "rollingStockFireCategory: The Tunnel must have at most one rolling stock fire category that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:rollingStockFireCategory ;

    sh:severity sh:Violation .

era-sh:RollingStockFireCategoryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:rollingStockFireCategory ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Tunnel is defined and requires a rolling stock fire category indication."@en ;
    # Source: inferred from the definition of Tunnel and its relationship to fire categories

    sh:message "rollingStockFireCategory: This error is due to the Tunnel {?tunnelLabel}, violating the rule: A rolling stock fire category must be defined for the tunnel."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:rollingStockFireCategory ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SafeConsistLengthInformationNecessary a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:safeConsistLengthInformationNecessary ;
    era:scope "local" ;

    rdfs:comment "Indication whether safe consist train length information from on-board is required to access the line for safety reasons and the required safety integrity level."@en ;
    # Source: ontology comment for safeConsistLengthInformationNecessary

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one safe consist length information reference for each common characteristics subset

    sh:message "safeConsistLengthInformationNecessary: The CommonCharacteristicsSubset must have at most one safe consist length information reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:safeConsistLengthInformationNecessary ;

    sh:severity sh:Violation .

era-sh:SafeConsistLengthInformationNecessaryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:safeConsistLengthInformationNecessary ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires an indication of safe consist length information."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to safe consist length information

    sh:message "safeConsistLengthInformationNecessary: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Safe consist length information must be defined for the common characteristics subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:safeConsistLengthInformationNecessary ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SchematicOverviewOP a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:schematicOverviewOP ;
    era:scope "local" ;

    rdfs:comment "Document providing the schematic overview of the operational point."@en ;
    # Source: ontology comment for schematicOverviewOP

    sh:datatype xsd:anyURI ;
    # Source: ontology range is xsd:anyURI

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "schematicOverviewOP: The OperationalPoint must have at most one schematic overview that is a valid URI. This error may be due to having more than one value or having a value that is not a valid URI."@en ;

    sh:path era:schematicOverviewOP ;

    sh:severity sh:Violation .

era-sh:SchematicOverviewOPApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:schematicOverviewOP ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OperationalPoint is defined and requires a schematic overview."@en ;
    # Source: inferred from the definition of OperationalPoint and its relationship to schematic overview

    sh:message "schematicOverviewOP: This error is due to the OperationalPoint {?pointLabel}, violating the rule: A schematic overview must be defined for the operational point."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?pointLabel WHERE {
        $this rdfs:label ?pointLabel .
        FILTER NOT EXISTS { $this era:schematicOverviewOP ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Side a sh:PropertyShape ;
    era:affectedClass era:Orientation ;
    era:affectedProperty era:side ;
    era:scope "local" ;

    rdfs:comment "Indication of the position of the signal in relation to the track in the running direction from the origin of the referencing system (e.g., direction of the main line)."@en ;
    # Source: ontology comment for side

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one side reference for each orientation

    sh:message "side: The Orientation must have at most one side reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:side ;

    sh:severity sh:Violation .

era-sh:SideApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Orientation ;
    era:affectedProperty era:side ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Orientation is defined and requires a side indication."@en ;
    # Source: inferred from the definition of Orientation and its relationship to side

    sh:message "side: This error is due to the Orientation {?orientationLabel}, violating the rule: A side must be defined for the orientation."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?orientationLabel WHERE {
        $this rdfs:label ?orientationLabel .
        FILTER NOT EXISTS { $this era:side ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Siding a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:siding ;
    era:scope "local" ;

    rdfs:comment "Reference to a related siding."@en ;
    # Source: ontology comment for siding

    sh:class era:Siding ;
    # Source: ontology range is era:Siding

    sh:minCount 1 ;
    # Inferred: at least one siding must be referenced for each operational point

    sh:message "siding: The OperationalPoint must have at least one siding reference that is a Siding. This error may be due to not having a value or having a value that is not an instance of Siding."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable siding instances (IRI references)

    sh:path era:siding ;

    sh:severity sh:Violation .

era-sh:SidingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:siding ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OperationalPoint is defined and requires a siding indication."@en ;
    # Source: inferred from the definition of OperationalPoint and its relationship to siding

    sh:message "siding: This error is due to the OperationalPoint {?pointLabel}, violating the rule: A siding must be defined for the operational point."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?pointLabel WHERE {
        $this rdfs:label ?pointLabel .
        FILTER NOT EXISTS { $this era:siding ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SignalOrientation a sh:PropertyShape ;
    era:affectedClass era:Signal ;
    era:affectedProperty era:signalOrientation ;
    era:scope "local" ;

    rdfs:comment "Relative position to the line of reference, given in km and indication if the signal refers to normal or opposite track direction."@en ;
    # Source: ontology comment for signalOrientation

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "signalOrientation: The Signal must have at most one signal orientation value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:signalOrientation ;

    sh:severity sh:Violation .

era-sh:SignalOrientationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Signal ;
    era:affectedProperty era:signalOrientation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Signal is defined and requires a signal orientation indication."@en ;
    # Source: inferred from the definition of Signal and its relationship to signal orientation

    sh:message "signalOrientation: This error is due to the Signal {?signalLabel}, violating the rule: A signal orientation must be defined for the signal."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?signalLabel WHERE {
        $this rdfs:label ?signalLabel .
        FILTER NOT EXISTS { $this era:signalOrientation ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SignalType a sh:PropertyShape ;
    era:affectedClass era:Signal ;
    era:affectedProperty era:signalType ;
    era:scope "local" ;

    rdfs:comment "Signalling information for Route Book compilation. This list shall include fixed signals that protect danger points."@en ;
    # Source: ontology comment for signalType

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "signalType: The Signal must have at most one signal type that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:signalType ;

    sh:severity sh:Violation .

era-sh:SignalTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Signal ;
    era:affectedProperty era:signalType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Signal is defined and requires a signal type indication."@en ;
    # Source: inferred from the definition of Signal and its relationship to signal types

    sh:message "signalType: This error is due to the Signal {?signalLabel}, violating the rule: A signal type must be defined for the signal."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?signalLabel WHERE {
        $this rdfs:label ?signalLabel .
        FILTER NOT EXISTS { $this era:signalType ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SnowIceHailConditions a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:snowIceHailConditions ;
    era:scope "local" ;

    rdfs:comment "Snow, ice and hail conditions."@en ;
    # Source: ontology comment for snowIceHailConditions

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one snow, ice, or hail condition for each vehicle type

    sh:message "snowIceHailConditions: The VehicleType must have at most one snow, ice, or hail condition that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:snowIceHailConditions ;

    sh:severity sh:Violation .

era-sh:SnowIceHailConditionsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:snowIceHailConditions ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires an indication of snow, ice, or hail conditions."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to snow, ice, and hail conditions

    sh:message "snowIceHailConditions: This error is due to the VehicleType {?vehicleLabel}, violating the rule: A snow, ice, or hail condition must be defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:snowIceHailConditions ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SolNature a sh:PropertyShape ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:solNature ;
    era:scope "local" ;

    rdfs:comment "Kind of section of line expressing size of presented data which depends on fact whether it connects OPs generated by division of a big node into several OPs or not."@en ;
    # Source: ontology comment for solNature

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one nature reference for each section of line

    sh:message "solNature: The SectionOfLine must have at most one solNature reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:solNature ;

    sh:severity sh:Violation .

era-sh:SolNatureApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:solNature ;
    era:scope "local" ;

    rdfs:comment "Applicable when the SectionOfLine is defined and requires a solNature indication."@en ;
    # Source: inferred from the definition of SectionOfLine and its relationship to solNature

    sh:message "solNature: This error is due to the SectionOfLine {?sectionLabel}, violating the rule: A solNature must be defined for the section of line."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sectionLabel WHERE {
        $this rdfs:label ?sectionLabel .
        FILTER NOT EXISTS { $this era:solNature ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SpecialAreaType a sh:PropertyShape ;
    era:affectedClass era:SpecialArea ;
    era:affectedProperty era:specialAreaType ;
    era:scope "local" ;

    rdfs:comment "Indicates the special area or location types such as safe areas and restricted area types."@en ;
    # Source: ontology comment for specialAreaType

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one special area type for each special area

    sh:message "specialAreaType: The SpecialArea must have at most one special area type that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:specialAreaType ;

    sh:severity sh:Violation .

era-sh:SpecialAreaTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SpecialArea ;
    era:affectedProperty era:specialAreaType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the SpecialArea is defined and requires a special area type indication."@en ;
    # Source: inferred from the definition of SpecialArea and its relationship to special area types

    sh:message "specialAreaType: This error is due to the SpecialArea {?areaLabel}, violating the rule: A special area type must be defined for the special area."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?areaLabel WHERE {
        $this rdfs:label ?areaLabel .
        FILTER NOT EXISTS { $this era:specialAreaType ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SpecialTunnelArea a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:specialTunnelArea ;
    era:scope "local" ;

    rdfs:comment "Relates a tunnel with a special area or location."@en ;
    # Source: ontology comment for specialTunnelArea

    sh:class era:SpecialTunnelArea ;
    # Source: ontology range is era:SpecialTunnelArea

    sh:maxCount 1 ;
    # Inferred: expected at most one special tunnel area for each tunnel

    sh:message "specialTunnelArea: The Tunnel must have at most one special tunnel area that is a SpecialTunnelArea. This error may be due to having more than one value or having a value that is not an instance of SpecialTunnelArea."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable special tunnel area instances (IRI references)

    sh:path era:specialTunnelArea ;

    sh:severity sh:Violation .

era-sh:SpecialTunnelAreaApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:specialTunnelArea ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Tunnel is defined and requires a special tunnel area indication."@en ;
    # Source: inferred from the definition of Tunnel and its relationship to special tunnel areas

    sh:message "specialTunnelArea: This error is due to the Tunnel {?tunnelLabel}, violating the rule: A special tunnel area must be defined for the tunnel."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:specialTunnelArea ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:StandardCombinedTransportContainers a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:standardCombinedTransportContainers ;
    era:scope "local" ;

    rdfs:comment "Coding for combined transport for containers (for all freight and mixed-traffic lines) in accordance with the specification referenced in Appendix A-1, index [B]."@en ;
    # Source: ontology comment for standardCombinedTransportContainers

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one standard combined transport container reference for each common characteristics subset

    sh:message "standardCombinedTransportContainers: The CommonCharacteristicsSubset must have at most one standard combined transport container reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:standardCombinedTransportContainers ;

    sh:severity sh:Violation .

era-sh:StandardCombinedTransportContainersApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:standardCombinedTransportContainers ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires an indication of standard combined transport containers."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to standard combined transport containers

    sh:message "standardCombinedTransportContainers: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A standard combined transport container must be defined for the common characteristics subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:standardCombinedTransportContainers ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:StandardCombinedTransportRollerUnits a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:standardCombinedTransportRollerUnits ;
    era:scope "local" ;

    rdfs:comment "Coding for combined transport for roller units (for all freight and mixed-traffic lines) in accordance with the specification referenced in Appendix A-1, index [B]."@en ;
    # Source: ontology comment for standardCombinedTransportRollerUnits

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one standard combined transport roller unit reference for each common characteristics subset

    sh:message "standardCombinedTransportRollerUnits: The CommonCharacteristicsSubset must have at most one standard combined transport roller unit reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:standardCombinedTransportRollerUnits ;

    sh:severity sh:Violation .

era-sh:StandardCombinedTransportRollerUnitsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:standardCombinedTransportRollerUnits ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires an indication of standard combined transport roller units."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to standard combined transport roller units

    sh:message "standardCombinedTransportRollerUnits: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A standard combined transport roller unit must be defined for the common characteristics subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:standardCombinedTransportRollerUnits ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:StartLocation a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:startLocation ;
    era:scope "local" ;

    rdfs:comment "Geographical coordinates in decimal degrees and km of the line at the beginning of a tunnel."@en ;
    # Source: ontology comment for startLocation

    sh:datatype xsd:string ;
    # Inferred: the data presentation indicates a predefined character string format

    sh:maxCount 1 ;
    # Inferred: expected at most one start location reference for each tunnel

    sh:message "startLocation: The Tunnel must have at most one start location value that is a valid geographical coordinate string. This error may be due to having more than one value or having a value that does not conform to the expected format."@en ;

    sh:pattern "^[+-]?\\d{1,2}\\.\\d{7} [+-]?\\d{1,3}\\.\\d{7} km [+-]?\\d{1,4}\\.\\d{3}$" ;
    # Inferred: pattern based on the data presentation format provided in the RAG

    sh:path era:startLocation ;

    sh:severity sh:Violation .

era-sh:StartLocationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:startLocation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Tunnel is defined and requires a start location indication."@en ;
    # Source: inferred from the definition of Tunnel and its relationship to start location

    sh:message "startLocation: This error is due to the Tunnel {?tunnelLabel}, violating the rule: A start location must be defined for the tunnel."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:startLocation ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:StartsAt a sh:PropertyShape ;
    era:affectedClass era:NetLinearReference ;
    era:affectedProperty era:startsAt ;
    era:scope "local" ;

    rdfs:comment "Specifies the starting coordinate of a linear reference."@en ;
    # Source: ontology comment for startsAt

    sh:class era:TopologicalCoordinate ;
    # Source: ontology range is era:TopologicalCoordinate

    sh:maxCount 1 ;
    # Inferred: expected at most one starting coordinate for each linear reference

    sh:message "startsAt: The NetLinearReference must have at most one starting coordinate that is a TopologicalCoordinate. This error may be due to having more than one value or having a value that is not an instance of TopologicalCoordinate."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable topological coordinate instances (IRI references)

    sh:path era:startsAt ;

    sh:severity sh:Violation .

era-sh:StartsAtApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:NetLinearReference ;
    era:affectedProperty era:startsAt ;
    era:scope "local" ;

    rdfs:comment "Applicable when the NetLinearReference is defined and requires a starting coordinate indication."@en ;
    # Source: inferred from the definition of NetLinearReference and its relationship to starting coordinates

    sh:message "startsAt: This error is due to the NetLinearReference {?referenceLabel}, violating the rule: A starting coordinate must be defined for the linear reference."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?referenceLabel WHERE {
        $this rdfs:label ?referenceLabel .
        FILTER NOT EXISTS { $this era:startsAt ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:State a sh:PropertyShape ;
    era:affectedClass era:Certificate ;
    era:affectedProperty era:state ;
    era:scope "local" ;

    rdfs:comment "Denoting the state of the certificate. Can be in one of the following: Amended, New, Suspended, Withdrawn."@en ;
    # Source: ontology comment for state

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "state: The Certificate must have at most one state value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:state ;

    sh:severity sh:Violation .

era-sh:StateApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Certificate ;
    era:affectedProperty era:state ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Certificate is defined and requires a state indication."@en ;
    # Source: inferred from the definition of Certificate and its relationship to state

    sh:message "state: This error is due to the Certificate {?certificateLabel}, violating the rule: A state must be defined for the certificate."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?certificateLabel WHERE {
        $this rdfs:label ?certificateLabel .
        FILTER NOT EXISTS { $this era:state ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SubCategory a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:subCategory ;
    era:scope "local" ;

    rdfs:comment "(Deprecated) Vehicle subcategory."@en ;
    # Source: ontology comment for subCategory

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: inferred from the nature of subCategory as a property that should have a single value

    sh:message "subCategory: The VehicleType must have at most one subCategory that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:subCategory ;

    sh:severity sh:Violation .

era-sh:SubCategoryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:subCategory ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires a subcategory indication."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to subCategory

    sh:message "subCategory: This error is due to the VehicleType {?vehicleLabel}, violating the rule: A subCategory must be defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:subCategory ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SubsetOf a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:subsetOf ;
    era:scope "local" ;

    rdfs:comment "Relates a subset with common characteristics with another subset with common characteristics."@en ;
    # Source: ontology comment for subsetOf

    sh:class era:CommonCharacteristicsSubset ;
    # Source: ontology range is era:CommonCharacteristicsSubset

    sh:maxCount 1 ;
    # Inferred: expected at most one subset reference for each common characteristics subset

    sh:message "subsetOf: The CommonCharacteristicsSubset must have at most one subset reference that is a CommonCharacteristicsSubset. This error may be due to having more than one value or having a value that is not an instance of CommonCharacteristicsSubset."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable common characteristics subset instances (IRI references)

    sh:path era:subsetOf ;

    sh:severity sh:Violation .

era-sh:SubsetOfApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:subsetOf ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a subset indication."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to subsets

    sh:message "subsetOf: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A subset must be defined for the common characteristics subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:subsetOf ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SubsidiaryLocationType a sh:PropertyShape ;
    era:affectedClass era:SubsidiaryLocation ;
    era:affectedProperty era:subsidiaryLocationType ;
    era:scope "local" ;

    rdfs:comment "Indicates the subsidiary location type that belongs to a taxonomy."@en ;
    # Source: ontology comment for subsidiaryLocationType

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one subsidiary location type for each subsidiary location

    sh:message "subsidiaryLocationType: The SubsidiaryLocation must have at most one subsidiary location type that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:subsidiaryLocationType ;

    sh:severity sh:Violation .

era-sh:SubsidiaryLocationTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SubsidiaryLocation ;
    era:affectedProperty era:subsidiaryLocationType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the SubsidiaryLocation is defined and requires a subsidiary location type indication."@en ;
    # Source: inferred from the definition of SubsidiaryLocation and its relationship to subsidiary location types

    sh:message "subsidiaryLocationType: This error is due to the SubsidiaryLocation {?locationLabel}, violating the rule: A subsidiary location type must be defined for the subsidiary location."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?locationLabel WHERE {
        $this rdfs:label ?locationLabel .
        FILTER NOT EXISTS { $this era:subsidiaryLocationType ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SupportedPlatformHeight a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:supportedPlatformHeight ;
    era:scope "local" ;

    rdfs:comment "Platform height for which the vehicle is designed."@en ;
    # Source: ontology comment for supportedPlatformHeight

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one supported platform height for each vehicle type

    sh:message "supportedPlatformHeight: The VehicleType must have at most one supported platform height that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:supportedPlatformHeight ;

    sh:severity sh:Violation .

era-sh:SupportedPlatformHeightApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:supportedPlatformHeight ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires a supported platform height indication."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to supported platform height

    sh:message "supportedPlatformHeight: This error is due to the VehicleType {?vehicleLabel}, violating the rule: A supported platform height must be defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:supportedPlatformHeight ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TdsFrenchTrainDetectionSystemLimitation a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tdsFrenchTrainDetectionSystemLimitation ;
    era:scope "local" ;

    rdfs:comment "Specific for route compatibility check on French network. Sections with train detection limitations."@en ;
    # Source: ontology comment for tdsFrenchTrainDetectionSystemLimitation

    sh:class era:FrenchTrainDetectionSystemLimitation ;
    # Source: ontology range is era:FrenchTrainDetectionSystemLimitation

    sh:maxCount 1 ;
    # Inferred: expected at most one limitation for each train detection system

    sh:message "tdsFrenchTrainDetectionSystemLimitation: The TrainDetectionSystem must have at most one train detection system limitation that is a FrenchTrainDetectionSystemLimitation. This error may be due to having more than one value or having a value that is not an instance of FrenchTrainDetectionSystemLimitation."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable French train detection system limitation instances (IRI references)

    sh:path era:tdsFrenchTrainDetectionSystemLimitation ;

    sh:severity sh:Violation .

era-sh:TdsFrenchTrainDetectionSystemLimitationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tdsFrenchTrainDetectionSystemLimitation ;
    era:scope "local" ;

    rdfs:comment "Applicable only on running tracks in France."@en ;
    # Source: dependencyNote in ontology

    sh:message "tdsFrenchTrainDetectionSystemLimitation: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: A train detection system limitation must be defined for the train detection system only if it is applicable on running tracks in France."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:tdsFrenchTrainDetectionSystemLimitation ?value } .
        # Additional condition to check if the system is applicable only on running tracks in France can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TdsMaximumMagneticField a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tdsMaximumMagneticField ;
    era:scope "local" ;

    rdfs:comment "The maximum magnetic field limits allowed for axle counters (in dB µA/m) for a defined frequency band. It should be provided in 3 directions."@en ;
    # Source: ontology comment for tdsMaximumMagneticField

    sh:class era:MaximumMagneticField ;
    # Source: ontology range is era:MaximumMagneticField

    sh:maxCount 1 ;
    # Inferred: expected at most one maximum magnetic field reference for each train detection system

    sh:message "tdsMaximumMagneticField: The TrainDetectionSystem must have at most one maximum magnetic field value that is a MaximumMagneticField. This error may be due to having more than one value or having a value that is not an instance of MaximumMagneticField."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable maximum magnetic field instances (IRI references)

    sh:path era:tdsMaximumMagneticField ;

    sh:severity sh:Violation .

era-sh:TdsMaximumMagneticFieldApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tdsMaximumMagneticField ;
    era:scope "local" ;

    rdfs:comment "Applicable for train detection systems based on axle counters."@en ;
    # Source: dependencyNote in ontology

    sh:message "tdsMaximumMagneticField: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: A maximum magnetic field must be defined for the train detection system if it is applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:tdsMaximumMagneticField ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TdsMinAxleLoadVehicleCategory a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tdsMinAxleLoadVehicleCategory ;
    era:rinfIndex "1.1.1.3.7.11.1" ;
    era:scope "local" ;

    rdfs:comment "Indication of load given in tons depending on the category of vehicle. Deprecated according to the amendment to the Regulation (EU) 2019/777."@en ;
    # Source: ontology comment for tdsMinAxleLoadVehicleCategory

    sh:class era:MinAxleLoadVehicleCategory ;
    # Source: ontology range is era:MinAxleLoadVehicleCategory

    sh:maxCount 1 ;
    # Inferred: expected at most one minimum axle load vehicle category reference for each train detection system

    sh:message "tdsMinAxleLoadVehicleCategory: The TrainDetectionSystem must have at most one minimum axle load vehicle category that is a MinAxleLoadVehicleCategory. This error may be due to having more than one value or having a value that is not an instance of MinAxleLoadVehicleCategory."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable minimum axle load vehicle category instances (IRI references)

    sh:path era:tdsMinAxleLoadVehicleCategory ;

    sh:severity sh:Violation .

era-sh:TdsMinAxleLoadVehicleCategoryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tdsMinAxleLoadVehicleCategory ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the selected option for parameter 1.1.1.3.7.1.1 is ‘track circuit’ or ‘wheel detector’."@en ;
    # Source: RAG explanation on applicability

    sh:message "tdsMinAxleLoadVehicleCategory: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: A minimum axle load vehicle category must be defined for the train detection system only if applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:tdsMinAxleLoadVehicleCategory ?value } .
        # Additional condition to check if the system is applicable based on the selected option can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TemperatureRange a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:temperatureRange ;
    era:scope "local" ;

    rdfs:comment "Temperature range for unrestricted access to the line according to European standard."@en ;
    # Source: ontology comment for temperatureRange

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one temperature range for each common characteristics subset

    sh:message "temperatureRange: The CommonCharacteristicsSubset must have at most one temperature range value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concept instances (IRI references)

    sh:path era:temperatureRange ;

    sh:severity sh:Violation .

era-sh:TemperatureRangeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:temperatureRange ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a temperature range indication."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to temperature range

    sh:message "temperatureRange: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A temperature range must be defined for the common characteristics subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:temperatureRange ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TenClassification a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:tenClassification ;
    era:scope "local" ;

    rdfs:comment "Indication of the part of the trans-European network the infrastructure element belongs to."@en ;
    # Source: ontology comment for tenClassification

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one TEN classification for each common characteristics subset

    sh:message "tenClassification: The CommonCharacteristicsSubset must have at most one ten classification that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concept instances (IRI references)

    sh:path era:tenClassification ;

    sh:severity sh:Violation .

era-sh:TenClassificationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:tenClassification ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a TEN classification indication."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to TEN classification

    sh:message "tenClassification: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A TEN classification must be defined for the common characteristics subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:tenClassification ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ThermalCapacityTSIReference a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:thermalCapacityTSIReference ;
    era:scope "local" ;

    rdfs:comment "Reference case of a TSI thermal capacity."@en ;
    # Source: ontology comment for thermalCapacityTSIReference

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "thermalCapacityTSIReference: The VehicleType must have at most one thermal capacity TSI reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:thermalCapacityTSIReference ;

    sh:severity sh:Violation .

era-sh:ThermalCapacityTSIReferenceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:thermalCapacityTSIReference ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires a thermal capacity TSI reference indication."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to thermal capacity TSI reference

    sh:message "thermalCapacityTSIReference: This error is due to the VehicleType {?vehicleLabel}, violating the rule: A thermal capacity TSI reference must be defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:thermalCapacityTSIReference ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Track a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:track ;
    era:scope "local" ;

    rdfs:comment "Reference to a related railway track."@en ;
    # Source: ontology comment for track

    sh:class era:RunningTrack ;
    # Source: ontology range is era:RunningTrack

    sh:maxCount 1 ;
    # Inferred: expected at most one track reference for each operational point

    sh:message "track: The OperationalPoint must have at most one track that is a RunningTrack. This error may be due to having more than one value or having a value that is not an instance of RunningTrack."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable running track instances (IRI references)

    sh:path era:track ;

    sh:severity sh:Violation .

era-sh:TrackApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:track ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OperationalPoint is defined and requires a track indication."@en ;
    # Source: inferred from the definition of OperationalPoint and its relationship to track

    sh:message "track: This error is due to the OperationalPoint {?pointLabel}, violating the rule: A track must be defined for the operational point."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?pointLabel WHERE {
        $this rdfs:label ?pointLabel .
        FILTER NOT EXISTS { $this era:track ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TrackDirection a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:trackDirection ;
    era:scope "local" ;

    rdfs:comment "The normal running direction is: N - same direction as in SoL, O - opposite direction to as in SoL, B - both directions."@en ;
    # Source: ontology comment for trackDirection

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one track direction for each running track

    sh:message "trackDirection: The RunningTrack must have at most one track direction that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concept instances (IRI references)

    sh:path era:trackDirection ;

    sh:severity sh:Violation .

era-sh:TrackDirectionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:trackDirection ;
    era:scope "local" ;

    rdfs:comment "Applicable when the RunningTrack is defined and requires a track direction indication."@en ;
    # Source: inferred from the definition of RunningTrack and its relationship to track direction

    sh:message "trackDirection: This error is due to the RunningTrack {?trackLabel}, violating the rule: A track direction must be defined for the running track."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:trackDirection ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TrackLoadCapability a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:trackLoadCapability ;
    era:rinfIndex "1.1.1.1.2.4" ;
    era:scope "local" ;

    rdfs:comment "A combination of the line category and speed at the weakest point of the track."@en ;
    # Source: ontology comment for trackLoadCapability

    sh:class era:LoadCapability ;
    # Source: ontology range is era:LoadCapability

    sh:maxCount 1 ;
    # Inferred: expected at most one load capability for each running track

    sh:message "trackLoadCapability: The RunningTrack must have at most one track load capability that is a LoadCapability. This error may be due to having more than one value or having a value that is not an instance of LoadCapability."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable load capability instances (IRI references)

    sh:path era:trackLoadCapability ;

    sh:severity sh:Violation .

era-sh:TrackLoadCapabilityApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:trackLoadCapability ;
    era:scope "local" ;

    rdfs:comment "Applicable when the RunningTrack is defined and requires a track load capability indication."@en ;
    # Source: inferred from the definition of RunningTrack and its relationship to track load capability

    sh:message "trackLoadCapability: This error is due to the RunningTrack {?trackLabel}, violating the rule: A track load capability must be defined for the running track."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:trackLoadCapability ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TrackPhaseInfo a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:trackPhaseInfo ;
    era:scope "local" ;

    rdfs:comment "Indication of required several information on phase separation."@en ;
    # Source: ontology comment for trackPhaseInfo

    sh:class era:PhaseInfo ;
    # Source: ontology range is era:PhaseInfo

    sh:maxCount 1 ;
    # Inferred: expected at most one phase info for each running track

    sh:message "trackPhaseInfo: The RunningTrack must have at most one track phase info that is a PhaseInfo. This error may be due to having more than one value or having a value that is not an instance of PhaseInfo."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable phase info instances (IRI references)

    sh:path era:trackPhaseInfo ;

    sh:severity sh:Violation .

era-sh:TrackPhaseInfoApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:trackPhaseInfo ;
    era:scope "local" ;

    rdfs:comment "Applicable when the phase separation parameter is defined and requires phase information."@en ;
    # Source: inferred from the dependencyNote in ontology

    sh:message "trackPhaseInfo: This error is due to the RunningTrack {?trackLabel}, violating the rule: Phase information must be defined for the running track only if the phase separation parameter is applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:trackPhaseInfo ?value } .
        # Additional condition to check if the phase separation parameter is applicable can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TrackRaisedPantographsDistanceAndSpeed a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:trackRaisedPantographsDistanceAndSpeed ;
    era:rinfIndex "1.1.1.2.3.3" ;
    era:scope "local" ;

    rdfs:comment "Indication of maximum number of raised pantographs per train allowed and minimum spacing centre line to centre line of adjacent pantograph heads, expressed in metres, at the given speed."@en ;
    # Source: ontology comment for trackRaisedPantographsDistanceAndSpeed

    sh:class era:RaisedPantographsDistanceAndSpeed ;
    # Source: ontology range is era:RaisedPantographsDistanceAndSpeed

    sh:maxCount 1 ;
    # Inferred: expected at most one value for the distance and speed parameter

    sh:message "trackRaisedPantographsDistanceAndSpeed: The RunningTrack must have at most one track raised pantographs distance and speed value that is a RaisedPantographsDistanceAndSpeed. This error may be due to having more than one value or having a value that is not an instance of RaisedPantographsDistanceAndSpeed."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable raised pantographs distance and speed instances (IRI references)

    sh:path era:trackRaisedPantographsDistanceAndSpeed ;

    sh:severity sh:Violation .

era-sh:TrackRaisedPantographsDistanceAndSpeedApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:trackRaisedPantographsDistanceAndSpeed ;
    era:scope "local" ;

    rdfs:comment "Applicable only if 'Overhead contact line (OCL)' is selected in 1.1.1.2.2.1.1."@en ;
    # Source: dependencyNote in ontology

    sh:message "trackRaisedPantographsDistanceAndSpeed: This error is due to the RunningTrack {?trackLabel}, violating the rule: The track raised pantographs distance and speed must be defined only if the overhead contact line is applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:trackRaisedPantographsDistanceAndSpeed ?value } .
        # Additional condition to check if the overhead contact line is selected can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TrackSystemSeparationInfo a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:trackSystemSeparationInfo ;
    era:rinfIndex "1.1.1.2.4.2.2" ;
    era:scope "local" ;

    rdfs:comment "Indication of required several information on system separation."@en ;
    # Source: ontology comment for trackSystemSeparationInfo

    sh:class era:SystemSeparationInfo ;
    # Source: ontology range is era:SystemSeparationInfo

    sh:maxCount 1 ;
    # Inferred: expected at most one system separation info for each common characteristics subset

    sh:message "trackSystemSeparationInfo: The CommonCharacteristicsSubset must have at most one system separation info that is a SystemSeparationInfo. This error may be due to having more than one value or having a value that is not an instance of SystemSeparationInfo."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable system separation info instances (IRI references)

    sh:path era:trackSystemSeparationInfo ;

    sh:severity sh:Violation .

era-sh:TrackSystemSeparationInfoApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:trackSystemSeparationInfo ;
    era:scope "local" ;

    rdfs:comment "Mandatory if there is an indication of existence of system separation."@en ;
    # Source: dependencyNote in ontology

    sh:message "trackSystemSeparationInfo: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: System separation info must be defined if the existence of system separation is indicated."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:trackSystemSeparationInfo ?value } .
        # Additional condition to check if system separation exists can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TracksideHABD a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:tracksideHabd ;
    era:scope "local" ;

    rdfs:comment "Link between the running track and the HABD, covering the RINF parameters 1.1.1.1.7.(5-9)."@en ;
    # Source: ontology comment for tracksideHabd

    sh:class era:HABD ;
    # Source: ontology range is era:HABD

    sh:maxCount 1 ;
    # Inferred: expected at most one HABD reference for each running track

    sh:message "tracksideHabd: The RunningTrack must have at most one trackside HABD that is a HABD. This error may be due to having more than one value or having a value that is not an instance of HABD."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable HABD instances (IRI references)

    sh:path era:tracksideHabd ;

    sh:severity sh:Violation .

era-sh:TracksideHABDApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:tracksideHabd ;
    era:scope "local" ;

    rdfs:comment "Applicable when the RunningTrack is defined and requires a trackside HABD indication."@en ;
    # Source: inferred from the definition of RunningTrack and its relationship to trackside HABD

    sh:message "tracksideHabd: This error is due to the RunningTrack {?trackLabel}, violating the rule: A trackside HABD must be defined for the running track."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:tracksideHabd ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TrainDetectionSystem a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:trainDetectionSystem ;
    era:scope "local" ;

    rdfs:comment "Technical characteristics of the train detection systems installed in the section of line."@en ;
    # Source: ontology comment for trainDetectionSystem

    sh:class era:TrainDetectionSystem ;
    # Source: ontology range is era:TrainDetectionSystem

    sh:maxCount 1 ;
    # Inferred: expected at most one train detection system for each common characteristics subset

    sh:message "trainDetectionSystem: The CommonCharacteristicsSubset must have at most one train detection system that is a TrainDetectionSystem. This error may be due to having more than one value or having a value that is not an instance of TrainDetectionSystem."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable train detection system instances (IRI references)

    sh:path era:trainDetectionSystem ;

    sh:severity sh:Violation .

era-sh:TrainDetectionSystemApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:trainDetectionSystem ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a train detection system indication."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to train detection systems

    sh:message "trainDetectionSystem: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A train detection system must be defined for the common characteristics subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:trainDetectionSystem ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TrainDetectionSystemSpecificCheck a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:trainDetectionSystemSpecificCheck ;
    era:scope "local" ;

    rdfs:comment "Reference to the technical specification of train detection system, in accordance with the specification referenced in Appendix A-1, index [D]."@en ;
    # Source: ontology comment for trainDetectionSystemSpecificCheck

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one specific check for each train detection system

    sh:message "trainDetectionSystemSpecificCheck: The TrainDetectionSystem must have at most one specific check that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concept instances (IRI references)

    sh:path era:trainDetectionSystemSpecificCheck ;

    sh:severity sh:Violation .

era-sh:TrainDetectionSystemSpecificCheckApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:trainDetectionSystemSpecificCheck ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the parameter 'Type of train detection system' is applicable."@en ;
    # Source: dependencyNote in ontology

    sh:message "trainDetectionSystemSpecificCheck: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: A specific check must be defined for the train detection system only if the type of train detection system is applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:trainDetectionSystemSpecificCheck ?value } .
        # Additional condition to check if the type of train detection system is applicable can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TrainDetectionSystemSpecificCheckDocument a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:trainDetectionSystemSpecificCheckDocument ;
    era:scope "local" ;

    rdfs:comment "Electronic document from the IM stored by the Agency with precise values in accordance with TSI CCS Article 13 for the specific check to be performed for train detection systems."@en ;
    # Source: ontology comment for trainDetectionSystemSpecificCheckDocument

    sh:class era:Document ;
    # Source: ontology range is era:Document

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "trainDetectionSystemSpecificCheckDocument: The TrainDetectionSystem must have at most one specific check document that is a Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable document instances (IRI references)

    sh:path era:trainDetectionSystemSpecificCheckDocument ;

    sh:severity sh:Violation .

era-sh:TrainDetectionSystemSpecificCheckDocumentApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:trainDetectionSystemSpecificCheckDocument ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the parameter 'Type of train detection system' is applicable."@en ;
    # Source: dependencyNote in ontology

    sh:message "trainDetectionSystemSpecificCheckDocument: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: A specific check document must be defined for the train detection system only if the type of train detection system is applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:trainDetectionSystemSpecificCheckDocument ?value } .
        # Additional condition to check if the type of train detection system is applicable can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TrainDetectionSystemType a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:trainDetectionSystemType ;
    era:scope "local" ;

    rdfs:comment "Indication of types of train detection systems installed."@en ;
    # Source: ontology comment for trainDetectionSystemType

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one type of train detection system for each train detection system

    sh:message "trainDetectionSystemType: The TrainDetectionSystem must have at most one type that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concept instances (IRI references)

    sh:path era:trainDetectionSystemType ;

    sh:severity sh:Violation .

era-sh:TrainDetectionSystemTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:trainDetectionSystemType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the TrainDetectionSystem is defined and requires a type indication."@en ;
    # Source: inferred from the definition of TrainDetectionSystem and its relationship to train detection system type

    sh:message "trainDetectionSystemType: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: A type of train detection system must be defined for the train detection system."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:trainDetectionSystemType ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TSICompliantCompositeBrakeBlocks a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantCompositeBrakeBlocks ;
    era:rinfIndex "1.1.1.3.7.21" ;
    era:scope "local" ;

    rdfs:comment "Indication whether rules are compliant with the TSI."@en ;
    # Source: RAG definition for TSI compliance of rules on the use of composite brake blocks

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one TSI compliance value for each train detection system

    sh:message "tsiCompliantCompositeBrakeBlocks: The TrainDetectionSystem must have at most one TSI compliance value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concept instances (IRI references)

    sh:path era:tsiCompliantCompositeBrakeBlocks ;

    sh:severity sh:Violation .

era-sh:TSICompliantCompositeBrakeBlocksApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantCompositeBrakeBlocks ;
    era:scope "local" ;

    rdfs:comment "Applicable only when for parameter 1.1.1.3.7.1.1 the selected option is ‘track circuit’."@en ;
    # Source: RAG explanation on applicability

    sh:message "tsiCompliantCompositeBrakeBlocks: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: TSI compliance must be defined only when the selected option for parameter 1.1.1.3.7.1.1 is 'track circuit'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:tsiCompliantCompositeBrakeBlocks ?value } .
        # Additional condition to check if the selected option for parameter 1.1.1.3.7.1.1 is 'track circuit' can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TSICompliantFerromagneticWheel a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantFerromagneticWheel ;
    era:rinfIndex "1.1.1.3.7.14" ;
    era:scope "local" ;

    rdfs:comment "Indication whether rules are compliant with the TSI for ferromagnetic characteristics of wheel material required."@en ;
    # Source: RAG definition for TSI compliance of ferromagnetic characteristics

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one TSI compliance value for each train detection system

    sh:message "tsiCompliantFerromagneticWheel: The TrainDetectionSystem must have at most one TSI compliance value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concept instances (IRI references)

    sh:path era:tsiCompliantFerromagneticWheel ;

    sh:severity sh:Violation .

era-sh:TSICompliantFerromagneticWheelApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantFerromagneticWheel ;
    era:scope "local" ;

    rdfs:comment "Applicable only when for parameter 1.1.1.3.7.1.1 the selected option is ‘wheel detector’."@en ;
    # Source: RAG explanation on applicability

    sh:message "tsiCompliantFerromagneticWheel: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: TSI compliance must be defined only when the selected option for parameter 1.1.1.3.7.1.1 is 'wheel detector'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:tsiCompliantFerromagneticWheel ?value } .
        # Additional condition to check if the selected option for parameter 1.1.1.3.7.1.1 is 'wheel detector' can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TSICompliantMaxDistConsecutiveAxles a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantMaxDistConsecutiveAxles ;
    era:rinfIndex "1.1.1.3.7.2.1" ;
    era:scope "local" ;

    rdfs:comment "Indication whether required distance between two consecutive axles is compliant with the TSI."@en ;
    # Source: ontology comment for tsiCompliantMaxDistConsecutiveAxles

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one TSI compliance value for each train detection system

    sh:message "tsiCompliantMaxDistConsecutiveAxles: The TrainDetectionSystem must have at most one TSI compliance value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concept instances (IRI references)

    sh:path era:tsiCompliantMaxDistConsecutiveAxles ;

    sh:severity sh:Violation .

era-sh:TSICompliantMaxDistConsecutiveAxlesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantMaxDistConsecutiveAxles ;
    era:scope "local" ;

    rdfs:comment "Applicable when the selected option for parameter 1.1.1.3.7.2.1 is ‘Not TSI compliant’."@en ;
    # Source: RAG explanation on applicability

    sh:message "tsiCompliantMaxDistConsecutiveAxles: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: TSI compliance must be defined only when the selected option for parameter 1.1.1.3.7.2.1 is 'Not TSI compliant'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:tsiCompliantMaxDistConsecutiveAxles ?value } .
        # Additional condition to check if the selected option for parameter 1.1.1.3.7.2.1 is 'Not TSI compliant' can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TSICompliantMaxImpedanceWheelset a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantMaxImpedanceWheelset ;
    era:rinfIndex "1.1.1.3.7.15.1" ;
    era:scope "local" ;

    rdfs:comment "TSI compliance of maximum permitted impedance between opposite wheels of a wheelset. Deprecated according to the amendment to the Regulation (EU) 2019/777."@en ;
    # Source: ontology comment for tsiCompliantMaxImpedanceWheelset

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one TSI compliance value for each train detection system

    sh:message "tsiCompliantMaxImpedanceWheelset: The TrainDetectionSystem must have at most one TSI compliance value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concept instances (IRI references)

    sh:path era:tsiCompliantMaxImpedanceWheelset ;

    sh:severity sh:Violation .

era-sh:TSICompliantMaxImpedanceWheelsetApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantMaxImpedanceWheelset ;
    era:scope "local" ;

    rdfs:comment "Applicable when the selected option for parameter related to impedance compliance is defined."@en ;
    # Source: inferred from the context of TSI compliance

    sh:message "tsiCompliantMaxImpedanceWheelset: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: TSI compliance must be defined only when the relevant impedance compliance parameter is applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:tsiCompliantMaxImpedanceWheelset ?value } .
        # Additional condition to check if the impedance compliance parameter is applicable can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TSICompliantMetalConstruction a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantMetalConstruction ;
    era:rinfIndex "1.1.1.3.7.13" ;
    era:scope "local" ;

    rdfs:comment "Indication whether rules are compliant with the TSI for vehicle metal construction."@en ;
    # Source: RAG definition for TSI compliance of vehicle metal construction

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one TSI compliance value for each train detection system

    sh:message "tsiCompliantMetalConstruction: The TrainDetectionSystem must have at most one TSI compliance value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concept instances (IRI references)

    sh:path era:tsiCompliantMetalConstruction ;

    sh:severity sh:Violation .

era-sh:TSICompliantMetalConstructionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantMetalConstruction ;
    era:scope "local" ;

    rdfs:comment "Applicable only when for parameter 1.1.1.3.7.1.1 the selected option is ‘loop’."@en ;
    # Source: RAG explanation on applicability

    sh:message "tsiCompliantMetalConstruction: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: TSI compliance must be defined only when the selected option for parameter 1.1.1.3.7.1.1 is 'loop'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:tsiCompliantMetalConstruction ?value } .
        # Additional condition to check if the selected option for parameter 1.1.1.3.7.1.1 is 'loop' can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TSICompliantMetalFreeSpace a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantMetalFreeSpace ;
    era:rinfIndex "1.1.1.3.7.12" ;
    era:scope "local" ;

    rdfs:comment "Indication whether rules are compliant with the TSI for metal-free space around wheels."@en ;
    # Source: RAG definition for TSI compliance of rules for metal-free space around wheels

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one TSI compliance value for each train detection system

    sh:message "tsiCompliantMetalFreeSpace: The TrainDetectionSystem must have at most one TSI compliance value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concept instances (IRI references)

    sh:path era:tsiCompliantMetalFreeSpace ;

    sh:severity sh:Violation .

era-sh:TSICompliantMetalFreeSpaceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantMetalFreeSpace ;
    era:scope "local" ;

    rdfs:comment "Applicable only when for parameter 1.1.1.3.7.1.1 the selected option is ‘wheel detector’."@en ;
    # Source: RAG explanation on applicability

    sh:message "tsiCompliantMetalFreeSpace: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: TSI compliance must be defined only when the selected option for parameter 1.1.1.3.7.1.1 is 'wheel detector'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:tsiCompliantMetalFreeSpace ?value } .
        # Additional condition to check if the selected option for parameter 1.1.1.3.7.1.1 is 'wheel detector' can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TSICompliantRSTShuntImpedance a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantRSTShuntImpedance ;
    era:rinfIndex "1.1.1.3.7.23" ;
    era:scope "local" ;

    rdfs:comment "Indication whether rules are compliant with the TSI for the combination of RST characteristics influencing shunting impedance."@en ;
    # Source: RAG definition for TSI compliance of rules on combination of RST characteristics influencing shunting impedance

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one TSI compliance value for each train detection system

    sh:message "tsiCompliantRSTShuntImpedance: The TrainDetectionSystem must have at most one TSI compliance value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concept instances (IRI references)

    sh:path era:tsiCompliantRSTShuntImpedance ;

    sh:severity sh:Violation .

era-sh:TSICompliantRSTShuntImpedanceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantRSTShuntImpedance ;
    era:scope "local" ;

    rdfs:comment "Applicable only when for parameter 1.1.1.3.7.1.1 the selected option is ‘track circuit’."@en ;
    # Source: RAG explanation on applicability

    sh:message "tsiCompliantRSTShuntImpedance: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: TSI compliance must be defined only when the selected option for parameter 1.1.1.3.7.1.1 is 'track circuit'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:tsiCompliantRSTShuntImpedance ?value } .
        # Additional condition to check if the selected option for parameter 1.1.1.3.7.1.1 is 'track circuit' can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TSICompliantSandCharacteristics a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantSandCharacteristics ;
    era:rinfIndex "1.1.1.3.7.19" ;
    era:scope "local" ;

    rdfs:comment "TSI Compliance of rules on sand characteristics. Deprecated according to the amendment to the Regulation (EU) 2019/777."@en ;
    # Source: ontology comment for tsiCompliantSandCharacteristics

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one TSI compliance value for each train detection system

    sh:message "tsiCompliantSandCharacteristics: The TrainDetectionSystem must have at most one TSI compliance value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concept instances (IRI references)

    sh:path era:tsiCompliantSandCharacteristics ;

    sh:severity sh:Violation .

era-sh:TSICompliantSandCharacteristicsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantSandCharacteristics ;
    era:scope "local" ;

    rdfs:comment "Applicable only if the nominal track gauge corresponds to 1520 mm. Any value indicating TSI compliance is wrong for other gauges."@en ;
    # Source: RAG explanation on applicability

    sh:message "tsiCompliantSandCharacteristics: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: TSI compliance must be defined only when the nominal track gauge is 1520 mm."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:tsiCompliantSandCharacteristics ?value } .
        # Additional condition to check if the nominal track gauge is 1520 mm can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TSICompliantSanding a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantSanding ;
    era:rinfIndex "1.1.1.3.7.16" ;
    era:scope "local" ;

    rdfs:comment "TSI compliance of sanding rules to allow compatibility with track circuits. Too much sand brings the risk of not detecting trains in tracks equipped with track circuits. Deprecated according to the amendment to the Regulation (EU) 2019/777."@en ;
    # Source: ontology comment for tsiCompliantSanding

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one TSI compliance value for each train detection system

    sh:message "tsiCompliantSanding: The TrainDetectionSystem must have at most one TSI compliance value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concept instances (IRI references)

    sh:path era:tsiCompliantSanding ;

    sh:severity sh:Violation .

era-sh:TSICompliantSandingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantSanding ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the selected option for parameter 1.1.1.3.7.1.1 is ‘track circuit’."@en ;
    # Source: RAG explanation on applicability

    sh:message "tsiCompliantSanding: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: TSI compliance must be defined only when the selected option for parameter 1.1.1.3.7.1.1 is 'track circuit'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:tsiCompliantSanding ?value } .
        # Additional condition to check if the selected option for parameter 1.1.1.3.7.1.1 is 'track circuit' can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TSICompliantShuntDevices a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantShuntDevices ;
    era:rinfIndex "1.1.1.3.7.22" ;
    era:scope "local" ;

    rdfs:comment "Indication whether rules are compliant with the TSI for shunt assisting devices."@en ;
    # Source: RAG definition for TSI compliance of rules on shunt assisting devices

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one TSI compliance value for each train detection system

    sh:message "tsiCompliantShuntDevices: The TrainDetectionSystem must have at most one TSI compliance value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concept instances (IRI references)

    sh:path era:tsiCompliantShuntDevices ;

    sh:severity sh:Violation .

era-sh:TSICompliantShuntDevicesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantShuntDevices ;
    era:scope "local" ;

    rdfs:comment "Applicable only when for parameter 1.1.1.3.7.1.1 the selected option is ‘track circuit’."@en ;
    # Source: RAG explanation on applicability

    sh:message "tsiCompliantShuntDevices: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: TSI compliance must be defined only when the selected option for parameter 1.1.1.3.7.1.1 is 'track circuit'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:tsiCompliantShuntDevices ?value } .
        # Additional condition to check if the selected option for parameter 1.1.1.3.7.1.1 is 'track circuit' can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TSIPantographHead a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:tsiPantographHead ;
    era:rinfIndex "1.1.1.2.3.1" ;
    era:scope "local" ;

    rdfs:comment "Indication of TSI compliant pantograph heads which are allowed to be used."@en ;
    # Source: ontology comment for tsiPantographHead

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one TSI compliant pantograph head for each common characteristics subset

    sh:message "tsiPantographHead: The CommonCharacteristicsSubset must have at most one TSI compliant pantograph head that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concept instances (IRI references)

    sh:path era:tsiPantographHead ;

    sh:severity sh:Violation .

era-sh:TSIPantographHeadApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:tsiPantographHead ;
    era:scope "local" ;

    rdfs:comment "Applicable only if 'Overhead contact line (OCL)' is selected for parameter 1.1.1.2.2.1.1."@en ;
    # Source: RAG explanation on applicability

    sh:message "tsiPantographHead: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: TSI compliance must be defined only when 'Overhead contact line (OCL)' is selected."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:tsiPantographHead ?value } .
        # Additional condition to check if 'Overhead contact line (OCL)' is selected can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TunnelDocRef a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:tunnelDocRef ;
    era:rinfIndex "1.1.1.1.8.8.2" ;
    era:scope "local" ;

    rdfs:comment "Electronic document available from the IM stored by the Agency with precise description of the clearance gauge and geometry of the tunnel."@en ;
    # Source: RAG definition for tunnel document reference

    sh:class era:Document ;
    # Source: ontology range is http://data.europa.eu/949/Document

    sh:maxCount 1 ;
    # Inferred: expected at most one document reference for each tunnel

    sh:message "tunnelDocRef: The Tunnel must have at most one tunnel document reference that is a Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable document instances (IRI references)

    sh:path era:tunnelDocRef ;

    sh:severity sh:Violation .

era-sh:TunnelDocRefApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:tunnelDocRef ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the document reference is required for the tunnel."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "tunnelDocRef: This error is due to the Tunnel {?tunnelLabel}, violating the rule: The tunnel document reference must be defined only when required."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:tunnelDocRef ?value } .
        # Additional condition to check if the document reference is required can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TypeVersionId a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:typeVersionId ;
    era:scope "local" ;

    rdfs:comment "Indicates the registrationVersion with Ids 10, 20 and 30 indicating registration according to Directive 2008/57/EC and implementing Decision 2011/665/EU, and 40 and 50 indicating registration according to regime described in Directive 2008/57/EC."@en ;
    # Source: ontology comment for typeVersionId

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one type version id for each vehicle type

    sh:message "typeVersionId: The VehicleType must have at most one type version id that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concept instances (IRI references)

    sh:path era:typeVersionId ;

    sh:severity sh:Violation .

era-sh:TypeVersionIdApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:typeVersionId ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is registered according to the relevant directives."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "typeVersionId: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The type version id must be defined only when the vehicle type is registered according to the relevant directives."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:typeVersionId ?value } .
        # Additional condition to check if the vehicle type is registered according to the relevant directives can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Uopid a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:uopid ;
    era:rinfIndex "1.2.0.0.0.2" ;
    era:scope "local" ;

    rdfs:comment "Code composed of country code and alphanumeric operational point code."@en ;
    # Source: ontology comment for uopid

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one unique OP ID for each operational point

    sh:pattern "^[A-Z]{2}[A-Za-z0-9_-]{1,10}$" ;
    # Inferred: pattern based on the explanation of data presentation in RAG

    sh:message "uopid: The OperationalPoint must have a unique OP ID that follows the format [AA+AAAAAAAAAA]. This error may be due to having more than one value or having a value that does not match the required pattern."@en ;

    sh:path era:uopid ;

    sh:severity sh:Violation .

era-sh:UopidApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:uopid ;
    era:scope "local" ;

    rdfs:comment "Applicable when the operational point is defined and requires a unique OP ID."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "uopid: This error is due to the OperationalPoint {?opLabel}, violating the rule: The unique OP ID must be defined only when the operational point is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?opLabel WHERE {
        $this rdfs:label ?opLabel .
        FILTER NOT EXISTS { $this era:uopid ?value } .
        # Additional condition to check if the operational point requires a unique OP ID can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Validity a sh:PropertyShape ;
    era:affectedClass era:Feature ;
    era:affectedProperty era:validity ;
    era:scope "local" ;

    rdfs:comment "Relates a feature with a temporal feature to indicate a validity period."@en ;
    # Source: ontology comment for validity

    sh:class era:TemporalFeature ;
    # Source: ontology range is era:TemporalFeature

    sh:maxCount 1 ;
    # Inferred: expected at most one validity reference for each feature

    sh:message "validity: The Feature must have at most one validity reference that is a TemporalFeature. This error may be due to having more than one value or having a value that is not an instance of TemporalFeature."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable temporal feature instances (IRI references)

    sh:path era:validity ;

    sh:severity sh:Violation .

era-sh:ValidityApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Feature ;
    era:affectedProperty era:validity ;
    era:scope "local" ;

    rdfs:comment "Applicable when the feature has a defined validity period."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "validity: This error is due to the Feature {?featureLabel}, violating the rule: The validity must be defined only when the feature has a defined validity period."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?featureLabel WHERE {
        $this rdfs:label ?featureLabel .
        FILTER NOT EXISTS { $this era:validity ?value } .
        # Additional condition to check if the feature has a defined validity period can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehicleCategory a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehicleCategory ;
    era:scope "local" ;

    rdfs:comment "Vehicle category."@en ;
    # Source: ontology comment for vehicleCategory

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one vehicle category for each vehicle type

    sh:message "vehicleCategory: The VehicleType must have at most one vehicle category that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concept instances (IRI references)

    sh:path era:vehicleCategory ;

    sh:severity sh:Violation .

era-sh:VehicleCategoryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehicleCategory ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is defined and requires a category."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "vehicleCategory: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The vehicle category must be defined only when the vehicle type is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:vehicleCategory ?value } .
        # Additional condition to check if the vehicle type requires a category can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehicleKeeper a sh:PropertyShape ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:vehicleKeeper ;
    era:scope "local" ;

    rdfs:comment "Indicates the organization that owns/operated a vehicle or wagon."@en ;
    # Source: ontology comment for vehicleKeeper

    sh:class era:OrganisationRole ;
    # Source: ontology range is http://data.europa.eu/949/OrganisationRole

    sh:maxCount 1 ;
    # Inferred: expected at most one vehicle keeper for each vehicle

    sh:message "vehicleKeeper: The Vehicle must have at most one vehicle keeper that is an OrganisationRole. This error may be due to having more than one value or having a value that is not an instance of OrganisationRole."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable organization role instances (IRI references)

    sh:path era:vehicleKeeper ;

    sh:severity sh:Violation .

era-sh:VehicleKeeperApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:vehicleKeeper ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle is defined and requires a keeper."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "vehicleKeeper: This error is due to the Vehicle {?vehicleLabel}, violating the rule: The vehicle keeper must be defined only when the vehicle is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:vehicleKeeper ?value } .
        # Additional condition to check if the vehicle requires a keeper can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehicleSubCategory a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehicleSubCategory ;
    era:scope "local" ;

    rdfs:comment "Vehicle subcategory."@en ;
    # Source: ontology comment for vehicleSubCategory

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one vehicle subcategory for each vehicle type

    sh:message "vehicleSubCategory: The VehicleType must have at most one vehicle subcategory that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concept instances (IRI references)

    sh:path era:vehicleSubCategory ;

    sh:severity sh:Violation .

era-sh:VehicleSubCategoryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehicleSubCategory ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is defined and requires a subcategory."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "vehicleSubCategory: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The vehicle subcategory must be defined only when the vehicle type is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:vehicleSubCategory ?value } .
        # Additional condition to check if the vehicle type requires a subcategory can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehicleType a sh:PropertyShape ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:vehicleType ;
    era:scope "local" ;

    rdfs:comment "Indicates the vehicle type of a specific vehicle or wagon."@en ;
    # Source: ontology comment for vehicleType

    sh:class era:VehicleType ;
    # Source: ontology range is era:VehicleType

    sh:maxCount 1 ;
    # Inferred: expected at most one vehicle type for each vehicle

    sh:message "vehicleType: The Vehicle must have at most one vehicle type that is a VehicleType. This error may be due to having more than one value or having a value that is not an instance of VehicleType."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable vehicle type instances (IRI references)

    sh:path era:vehicleType ;

    sh:severity sh:Violation .

era-sh:VehicleTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:vehicleType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle is defined and requires a type."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "vehicleType: This error is due to the Vehicle {?vehicleLabel}, violating the rule: The vehicle type must be defined only when the vehicle is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:vehicleType ?value } .
        # Additional condition to check if the vehicle requires a type can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehicleTypeMaximumSpeedAndCantDeficiency a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehicleTypeMaximumSpeedAndCantDeficiency ;
    era:rinfIndex "4.6.4" ;
    era:scope "local" ;

    rdfs:comment "Relates the vehicle type with its values of maximum speed and cant deficiency."@en ;
    # Source: ontology comment for vehicleTypeMaximumSpeedAndCantDeficiency

    sh:class era:MaximumSpeedAndCantDeficiency ;
    # Source: ontology range is era:MaximumSpeedAndCantDeficiency

    sh:maxCount 1 ;
    # Inferred: expected at most one maximum speed and cant deficiency value for each vehicle type

    sh:message "vehicleTypeMaximumSpeedAndCantDeficiency: The VehicleType must have at most one maximum speed and cant deficiency value that is a MaximumSpeedAndCantDeficiency. This error may be due to having more than one value or having a value that is not an instance of MaximumSpeedAndCantDeficiency."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable maximum speed and cant deficiency instances (IRI references)

    sh:path era:vehicleTypeMaximumSpeedAndCantDeficiency ;

    sh:severity sh:Violation .

era-sh:VehicleTypeMaximumSpeedAndCantDeficiencyApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehicleTypeMaximumSpeedAndCantDeficiency ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is defined and requires maximum speed and cant deficiency values."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "vehicleTypeMaximumSpeedAndCantDeficiency: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The maximum speed and cant deficiency must be defined only when the vehicle type is required to have them."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:vehicleTypeMaximumSpeedAndCantDeficiency ?value } .
        # Additional condition to check if the vehicle type requires maximum speed and cant deficiency can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehicleTypesCompatibleTrafficLoad a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:vehicleTypesCompatibleTrafficLoad ;
    era:rinfIndex "1.1.1.5.1" ;
    era:scope "local" ;

    rdfs:comment "The infrastructure managers shall provide through RINF the information to the RU regarding list of vehicle types compatible with the route for which they have already verified compatibility for parameter Traffic load and load carrying capacity of infrastructure and train detection systems, where such information is available."@en ;
    # Source: ontology comment for vehicleTypesCompatibleTrafficLoad

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: inferred from the ontology range

    sh:maxCount 1 ;
    # Inferred: expected at most one compatible traffic load for each common characteristics subset

    sh:message "vehicleTypesCompatibleTrafficLoad: The CommonCharacteristicsSubset must have at most one compatible traffic load that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concept instances (IRI references)

    sh:path era:vehicleTypesCompatibleTrafficLoad ;

    sh:severity sh:Violation .

era-sh:VehicleTypesCompatibleTrafficLoadApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:vehicleTypesCompatibleTrafficLoad ;
    era:scope "local" ;

    rdfs:comment "Applicable when the infrastructure manager has verified compatibility for parameter Traffic load and load carrying capacity."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "vehicleTypesCompatibleTrafficLoad: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The compatible traffic load must be defined only when the infrastructure manager has verified compatibility."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:vehicleTypesCompatibleTrafficLoad ?value } .
        # Additional condition to check if compatibility has been verified can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehiclesCompatibleTrafficLoad a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:vehiclesCompatibleTrafficLoad ;
    era:rinfIndex "1.1.1.5.2" ;
    era:scope "local" ;

    rdfs:comment "The infrastructure managers shall provide through RINF the information or a document to the RU regarding the list of vehicles compatible with the route for which they have already verified compatibility for parameter Traffic load and load carrying capacity of infrastructure and train detection systems, where such information is available."@en ;
    # Source: ontology comment for vehiclesCompatibleTrafficLoad

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: inferred from the ontology range

    sh:maxCount 1 ;
    # Inferred: expected at most one compatible traffic load for each common characteristics subset

    sh:message "vehiclesCompatibleTrafficLoad: The CommonCharacteristicsSubset must have at most one compatible traffic load that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concept instances (IRI references)

    sh:path era:vehiclesCompatibleTrafficLoad ;

    sh:severity sh:Violation .

era-sh:VehiclesCompatibleTrafficLoadApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:vehiclesCompatibleTrafficLoad ;
    era:scope "local" ;

    rdfs:comment "Applicable when the infrastructure manager has verified compatibility for parameter Traffic load and load carrying capacity."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "vehiclesCompatibleTrafficLoad: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The compatible traffic load must be defined only when the infrastructure manager has verified compatibility."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:vehiclesCompatibleTrafficLoad ?value } .
        # Additional condition to check if compatibility has been verified can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VoiceGSMRNetwork a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:voiceGSMRNetwork ;
    era:scope "local" ;

    rdfs:comment "Voice SIM Card GSM-R Home Network. The allowed values for this property belong to the SKOS Concept Scheme http://data.europa.eu/949/concepts/gsmr-networks/GSMRNetworks."@en ;
    # Source: ontology comment for voiceGSMRNetwork

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one voice GSM-R network for each vehicle type

    sh:message "voiceGSMRNetwork: The VehicleType must have at most one voice GSM-R network that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concept instances (IRI references)

    sh:path era:voiceGSMRNetwork ;

    sh:severity sh:Violation .

era-sh:VoiceGSMRNetworkApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:voiceGSMRNetwork ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is defined and requires a voice GSM-R network."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "voiceGSMRNetwork: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The voice GSM-R network must be defined only when the vehicle type is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:voiceGSMRNetwork ?value } .
        # Additional condition to check if the vehicle type requires a voice GSM-R network can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VoiceRadioCompatible a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:voiceRadioCompatible ;
    era:rinfIndex "1.1.1.3.3.9" ;
    era:scope "local" ;

    rdfs:comment "Radio requirements used for demonstrating technical compatibility voice."@en ;
    # Source: ontology comment for voiceRadioCompatible

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one voice radio compatibility value for each vehicle type

    sh:message "voiceRadioCompatible: The VehicleType must have at most one voice radio compatibility value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concept instances (IRI references)

    sh:path era:voiceRadioCompatible ;

    sh:severity sh:Violation .

era-sh:VoiceRadioCompatibleApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:voiceRadioCompatible ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is defined and requires voice radio compatibility."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "voiceRadioCompatible: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The voice radio compatibility must be defined only when the vehicle type is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:voiceRadioCompatible ?value } .
        # Additional condition to check if the vehicle type requires voice radio compatibility can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:WheelSetGauge a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:wheelSetGauge ;
    era:rinfIndex "4.1.3" ;
    era:scope "local" ;

    rdfs:comment "A single value expressed in millimetres that identifies the track gauge."@en ;
    # Source: ontology comment for wheelSetGauge

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one wheel set gauge for each common characteristics subset

    sh:message "wheelSetGauge: The CommonCharacteristicsSubset must have at most one wheel set gauge that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concept instances (IRI references)

    sh:path era:wheelSetGauge ;

    sh:severity sh:Violation .

era-sh:WheelSetGaugeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:wheelSetGauge ;
    era:scope "local" ;

    rdfs:comment "Applicable when the common characteristics subset is defined and requires a wheel set gauge."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "wheelSetGauge: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The wheel set gauge must be defined only when the common characteristics subset is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:wheelSetGauge ?value } .
        # Additional condition to check if the common characteristics subset requires a wheel set gauge can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:WheelSetGaugeChangeoverFacility a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:wheelSetGaugeChangeoverFacility ;
    era:rinfIndex "4.1.11" ;
    era:scope "local" ;

    rdfs:comment "Wheelset gauge changeover facility. The allowed values for this property belong to the SKOS Concept Scheme http://data.europa.eu/949/concepts/gauge-changeover-facilities/GaugeChangeoverFacilities."@en ;
    # Source: ontology comment for wheelSetGaugeChangeoverFacility

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one gauge changeover facility for each vehicle type

    sh:message "wheelSetGaugeChangeoverFacility: The VehicleType must have at most one wheelset gauge changeover facility that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concept instances (IRI references)

    sh:path era:wheelSetGaugeChangeoverFacility ;

    sh:severity sh:Violation .

era-sh:WheelSetGaugeChangeoverFacilityApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:wheelSetGaugeChangeoverFacility ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is defined and requires a wheelset gauge changeover facility."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "wheelSetGaugeChangeoverFacility: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The wheelset gauge changeover facility must be defined only when the vehicle type is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:wheelSetGaugeChangeoverFacility ?value } .
        # Additional condition to check if the vehicle type requires a gauge changeover facility can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AlternativeName a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:alternativeName ;
    era:scope "local" ;

    rdfs:comment "Alternative name of a vehicle type."@en ;
    # Source: ontology comment for alternativeName

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one alternative name for each vehicle type

    sh:message "alternativeName: The VehicleType must have at most one alternative name that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:alternativeName ;

    sh:severity sh:Violation .

era-sh:AlternativeNameApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:alternativeName ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is defined and requires an alternative name."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "alternativeName: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The alternative name must be defined only when the vehicle type is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:alternativeName ?value } .
        # Additional condition to check if the vehicle type requires an alternative name can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AltitudeRange a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:altitudeRange ;
    era:rinfIndex "4.3.2" ;
    era:scope "local" ;

    rdfs:comment "Altitude range."@en ;
    # Source: ontology comment for altitudeRange

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one altitude range for each vehicle type

    sh:message "altitudeRange: The VehicleType must have at most one altitude range that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:altitudeRange ;

    sh:severity sh:Violation .

era-sh:AltitudeRangeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:altitudeRange ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is defined and requires an altitude range."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "altitudeRange: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The altitude range must be defined only when the vehicle type is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:altitudeRange ?value } .
        # Additional condition to check if the vehicle type requires an altitude range can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AltitudeRangeDetail a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:altitudeRangeDetail ;
    era:rinfIndex "4.3.2.1" ;
    era:scope "local" ;

    rdfs:comment "Altitude range value for 'X' if value 'AX' is selected in altitude range."@en ;
    # Source: ontology comment for altitudeRangeDetail

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one altitude range detail for each vehicle type

    sh:message "altitudeRangeDetail: The VehicleType must have at most one altitude range detail that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:altitudeRangeDetail ;

    sh:severity sh:Violation .

era-sh:AltitudeRangeDetailApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:altitudeRangeDetail ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is defined and requires an altitude range detail."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "altitudeRangeDetail: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The altitude range detail must be defined only when the vehicle type is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:altitudeRangeDetail ?value } .
        # Additional condition to check if the vehicle type requires an altitude range detail can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AppliesInBothDirections a sh:PropertyShape ;
    era:affectedClass era:LinearElementSection ;
    era:affectedProperty era:appliesInBothDirections ;
    era:scope "local" ;

    rdfs:comment "Specifies if the section applies in both directions (optional)."@en ;
    # Source: ontology comment for appliesInBothDirections

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one appliesInBothDirections value for each LinearElementSection

    sh:message "appliesInBothDirections: The LinearElementSection must have at most one appliesInBothDirections value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:appliesInBothDirections ;

    sh:severity sh:Violation .

era-sh:AppliesInBothDirectionsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LinearElementSection ;
    era:affectedProperty era:appliesInBothDirections ;
    era:scope "local" ;

    rdfs:comment "Applicable when the LinearElementSection is defined and requires a directionality specification."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "appliesInBothDirections: This error is due to the LinearElementSection {?sectionLabel}, violating the rule: The appliesInBothDirections must be defined only when the section is required to have a directionality specification."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sectionLabel WHERE {
        $this rdfs:label ?sectionLabel .
        FILTER NOT EXISTS { $this era:appliesInBothDirections ?value } .
        # Additional condition to check if the section requires a directionality specification can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AreaBoardingAid a sh:PropertyShape ;
    era:affectedClass era:PlatformEdge ;
    era:affectedProperty era:areaBoardingAid ;
    era:rinfIndex "1.2.1.0.6.7" ;
    era:scope "local" ;

    rdfs:comment "Information of the train access level for which the boarding aid can be used. The value represents the vertical difference that is overcome by the platform boarding aid in millimetres."@en ;
    # Source: ontology comment for areaBoardingAid

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one area boarding aid value for each platform edge

    sh:message "areaBoardingAid: The PlatformEdge must have at most one area boarding aid value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:areaBoardingAid ;

    sh:severity sh:Violation .

era-sh:AreaBoardingAidApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PlatformEdge ;
    era:affectedProperty era:areaBoardingAid ;
    era:scope "local" ;

    rdfs:comment "Applicable when the platform edge is defined and requires an area boarding aid."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "areaBoardingAid: This error is due to the PlatformEdge {?platformLabel}, violating the rule: The area boarding aid must be defined only when the platform edge is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?platformLabel WHERE {
        $this rdfs:label ?platformLabel .
        FILTER NOT EXISTS { $this era:areaBoardingAid ?value } .
        # Additional condition to check if the platform edge requires an area boarding aid can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AssistanceStartingTrain a sh:PropertyShape ;
    era:affectedClass era:PlatformEdge ;
    era:affectedProperty era:assistanceStartingTrain ;
    era:rinfIndex "1.2.1.0.6.6" ;
    era:scope "local" ;

    rdfs:comment "Indication of existence of equipment or staff supporting the train crew in starting the train."@en ;
    # Source: ontology comment for assistanceStartingTrain

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one assistance starting train value for each platform edge

    sh:message "assistanceStartingTrain: The PlatformEdge must have at most one assistance starting train value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:assistanceStartingTrain ;

    sh:severity sh:Violation .

era-sh:AssistanceStartingTrainApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PlatformEdge ;
    era:affectedProperty era:assistanceStartingTrain ;
    era:scope "local" ;

    rdfs:comment "Applicable when the platform edge is defined and requires assistance for starting the train."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "assistanceStartingTrain: This error is due to the PlatformEdge {?platformLabel}, violating the rule: The assistance starting train must be defined only when the platform edge is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?platformLabel WHERE {
        $this rdfs:label ?platformLabel .
        FILTER NOT EXISTS { $this era:assistanceStartingTrain ?value } .
        # Additional condition to check if the platform edge requires assistance for starting the train can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AutomaticDroppingDeviceRequired a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:automaticDroppingDeviceRequired ; 
    era:rinfIndex "1.1.1.2.5.3" ; 
    era:scope "local" ;

    rdfs:comment "Indication of whether an automatic dropping device (ADD) is required on the vehicle."@en ; 
    # Source: ontology comment for automaticDroppingDeviceRequired

    sh:datatype xsd:boolean ; 
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ; 
    # Inferred: expected at most one value for whether an automatic dropping device is required

    sh:message "automaticDroppingDeviceRequired: The CommonCharacteristicsSubset must have at most one automatic dropping device required value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:automaticDroppingDeviceRequired ; 

    sh:severity sh:Violation .

era-sh:AutomaticDroppingDeviceRequiredApplicability a sh:SPARQLConstraint ; 
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:automaticDroppingDeviceRequired ; 
    era:scope "local" ;

    rdfs:comment "Applicable when the common characteristics subset is defined and requires an automatic dropping device."@en ; 
    # Source: inferred from the context of the RAG and ontology

    sh:message "automaticDroppingDeviceRequired: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The automatic dropping device required must be defined only when the common characteristics subset is required to have one."@en ;

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:automaticDroppingDeviceRequired ?value } .
        # Additional condition to check if the common characteristics subset requires an automatic dropping device can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AxleSpacing a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:axleSpacing ;
    era:rinfIndex "4.5.3.4" ;
    era:scope "local" ;

    rdfs:comment "Position of the axles along the unit. a: Distance between axles; b: Distance from end axle to the end of the nearest coupling plane; c: distance between two inside axles."@en ;
    # Source: ontology comment for axleSpacing

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one axle spacing value for each vehicle type

    sh:message "axleSpacing: The VehicleType must have at most one axle spacing value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:axleSpacing ;

    sh:severity sh:Violation .

era-sh:AxleSpacingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:axleSpacing ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is defined and requires axle spacing information."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "axleSpacing: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The axle spacing must be defined only when the vehicle type is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:axleSpacing ?value } .
        # Additional condition to check if the vehicle type requires axle spacing can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:BigMetalMass a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:bigMetalMass ;
    era:rinfIndex "1.1.1.3.2.18" ;
    era:scope "local" ;

    rdfs:comment "Indication of existence of metal mass in the vicinity of the location, susceptible of perturbating the reading of balises by the on-board system."@en ;
    # Source: ontology comment for bigMetalMass

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one big metal mass value for each common characteristics subset

    sh:message "bigMetalMass: The CommonCharacteristicsSubset must have at most one big metal mass value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:bigMetalMass ;

    sh:severity sh:Violation .

era-sh:BigMetalMassApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:bigMetalMass ;
    era:scope "local" ;

    rdfs:comment "Applicable when the common characteristics subset is defined and requires a big metal mass indication."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "bigMetalMass: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The big metal mass must be defined only when the common characteristics subset is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:bigMetalMass ?value } .
        # Additional condition to check if the common characteristics subset requires a big metal mass can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:BoardingAids a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:boardingAids ;
    era:rinfIndex "4.12.3.2" ;
    era:scope "local" ;

    rdfs:comment "Description of any integrated boarding aids (if provided)."@en ;
    # Source: ontology comment for boardingAids

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one boarding aids description for each vehicle type

    sh:message "boardingAids: The VehicleType must have at most one boarding aids description that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:boardingAids ;

    sh:severity sh:Violation .

era-sh:BoardingAidsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:boardingAids ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is defined and requires a description of boarding aids."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "boardingAids: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The boarding aids must be defined only when the vehicle type is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:boardingAids ?value } .
        # Additional condition to check if the vehicle type requires boarding aids can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:BorderPointId a sh:PropertyShape ;
    era:affectedClass era:ReferenceBorderPoint ;
    era:affectedProperty era:borderPointId ;
    era:scope "local" ;

    rdfs:comment "Border point identification in the list of reference border points in the RINF application guide."@en ;
    # Source: ontology comment for borderPointId

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one border point ID for each reference border point

    sh:message "borderPointId: The ReferenceBorderPoint must have at most one border point ID that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:borderPointId ;

    sh:severity sh:Violation .

era-sh:BorderPointIdApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ReferenceBorderPoint ;
    era:affectedProperty era:borderPointId ;
    era:scope "local" ;

    rdfs:comment "Applicable when the reference border point is defined and requires a border point ID."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "borderPointId: This error is due to the ReferenceBorderPoint {?borderPointLabel}, violating the rule: The border point ID must be defined only when the reference border point is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?borderPointLabel WHERE {
        $this rdfs:label ?borderPointLabel .
        FILTER NOT EXISTS { $this era:borderPointId ?value } .
        # Additional condition to check if the reference border point requires a border point ID can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:BrakeWeightPercentage a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:brakeWeightPercentage ;
    era:rinfIndex "4.7.6" ;
    era:scope "local" ;

    rdfs:comment "Brake weight percentage (lambda) or Braked mass."@en ;
    # Source: ontology comment for brakeWeightPercentage

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one brake weight percentage for each vehicle type

    sh:message "brakeWeightPercentage: The VehicleType must have at most one brake weight percentage that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:brakeWeightPercentage ;

    sh:severity sh:Violation .

era-sh:BrakeWeightPercentageApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:brakeWeightPercentage ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is defined and requires a brake weight percentage."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "brakeWeightPercentage: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The brake weight percentage must be defined only when the vehicle type is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:brakeWeightPercentage ?value } .
        # Additional condition to check if the vehicle type requires a brake weight percentage can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:CantDeficiencyApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:cantDefficiency ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is defined and requires cant deficiency information."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "cantDefficiency: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The cant deficiency must be defined only when the vehicle type is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:cantDefficiency ?value } .
        # Additional condition to check if the vehicle type requires cant deficiency can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:CantDeficiency a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:cantDeficiency ;
    era:rinfIndex "1.1.1.1.4.2" ;
    era:scope "local" ;

    rdfs:comment "Maximum cant deficiency expressed in millimetres defined as difference between the applied cant and a higher equilibrium cant the line has been designed for."@en ;
    # Source: ontology comment for cantDeficiency

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one cant deficiency value for each running track

    sh:message "cantDeficiency: The RunningTrack must have at most one cant deficiency value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:cantDeficiency ;

    sh:severity sh:Violation .

era-sh:CantDeficiencyApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:cantDeficiency ;
    era:scope "local" ;

    rdfs:comment "Applicable when the running track is defined and requires cant deficiency information."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "cantDeficiency: This error is due to the RunningTrack {?trackLabel}, violating the rule: The cant deficiency must be defined only when the running track is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:cantDeficiency ?value } .
        # Additional condition to check if the running track requires cant deficiency can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:CatenaryMaxRatedCurrent a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:catenaryMaxRatedCurrent ;
    era:rinfIndex "4.10.3" ;
    era:scope "local" ;

    rdfs:comment "Maximum rated current from the catenary (to be indicated for each electrical energy supply system the vehicle is equipped for), given in A."@en ;
    # Source: ontology comment for catenaryMaxRatedCurrent

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one catenary max rated current value for each vehicle type

    sh:message "catenaryMaxRatedCurrent: The VehicleType must have at most one catenary max rated current value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:catenaryMaxRatedCurrent ;

    sh:severity sh:Violation .

era-sh:CatenaryMaxRatedCurrentApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:catenaryMaxRatedCurrent ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is defined and requires catenary max rated current information."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "catenaryMaxRatedCurrent: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The catenary max rated current must be defined only when the vehicle type is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:catenaryMaxRatedCurrent ?value } .
        # Additional condition to check if the vehicle type requires catenary max rated current can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ComplianceInfTsi a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:complianceInfTsi ;
    era:rinfIndex "1.1.1.1.8.8.1" ;
    era:scope "local" ;

    rdfs:comment "Compliance of the tunnel with TSI INF at the maximum permitted speed."@en ;
    # Source: ontology comment for complianceInfTsi

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one compliance value for each tunnel

    sh:message "complianceInfTsi: The Tunnel must have at most one complianceInfTsi value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:complianceInfTsi ;

    sh:severity sh:Violation .

era-sh:ComplianceInfTsiApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:complianceInfTsi ;
    era:scope "local" ;

    rdfs:comment "Mandatory if the speed of the line is equal or greater than 200km/h."@en ;
    # Source: dependency note in ontology

    sh:message "complianceInfTsi: This error is due to the Tunnel {?tunnelLabel}, violating the rule: The complianceInfTsi must be defined only when the speed of the line is equal or greater than 200 km/h."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:complianceInfTsi ?value } .
        # Additional condition to check if the speed of the line is >= 200 km/h can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:CompositeBrakeBlockRetrofitted a sh:PropertyShape ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:compositeBrakeBlockRetrofitted ;
    era:rinfIndex "1.1.1.3.7.21" ;
    era:scope "local" ;

    rdfs:comment "Indicates if a vehicle's composite brake block is retrofitted."@en ;
    # Source: ontology comment for compositeBrakeBlockRetrofitted

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for whether the composite brake block is retrofitted

    sh:message "compositeBrakeBlockRetrofitted: The Vehicle must have at most one composite brake block retrofitted value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:compositeBrakeBlockRetrofitted ;

    sh:severity sh:Violation .

era-sh:CompositeBrakeBlockRetrofittedApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:compositeBrakeBlockRetrofitted ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle is defined and requires information on whether the composite brake block is retrofitted."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "compositeBrakeBlockRetrofitted: This error is due to the Vehicle {?vehicleLabel}, violating the rule: The composite brake block retrofitted must be defined only when the vehicle is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:compositeBrakeBlockRetrofitted ?value } .
        # Additional condition to check if the vehicle requires composite brake block retrofitting can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ConditionsChargingElectricEnergyStorage a sh:PropertyShape ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:conditionsChargingElectricEnergyStorage ;
    era:rinfIndex "1.2.1.0.7.2" ;
    era:scope "local" ;

    rdfs:comment "Conditions set by IMs according to a standardised document for charging electric energy storage for traction purposes at standstill."@en ;
    # Source: ontology comment for conditionsChargingElectricEnergyStorage

    sh:datatype xsd:anyURI ;
    # Source: ontology range is xsd:anyURI

    sh:maxCount 1 ;
    # Inferred: expected at most one conditions value for each contact line system

    sh:message "conditionsChargingElectricEnergyStorage: The ContactLineSystem must have at most one conditions for charging electric energy storage that is a URI. This error may be due to having more than one value or having a value that is not a URI."@en ;

    sh:path era:conditionsChargingElectricEnergyStorage ;

    sh:severity sh:Violation .

era-sh:ConditionsChargingElectricEnergyStorageApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:conditionsChargingElectricEnergyStorage ;
    era:scope "local" ;

    rdfs:comment "Applicable when the contact line system is defined and requires conditions for charging electric energy storage."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "conditionsChargingElectricEnergyStorage: This error is due to the ContactLineSystem {?systemLabel}, violating the rule: The conditions for charging electric energy storage must be defined only when the contact line system is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:conditionsChargingElectricEnergyStorage ?value } .
        # Additional condition to check if the contact line system requires conditions for charging electric energy storage can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ConditionsSwitchTrainProtectionSystems a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:conditionsSwitchTrainProtectionSystems ; 
    era:rinfIndex "1.1.1.3.8.1" ; 
    era:scope "local" ; 

    rdfs:comment "Conditions to switch over between different class B train protection, control and warning systems."@en ; 
    # Source: ontology comment for conditionsSwitchTrainProtectionSystems

    sh:datatype xsd:string ; 
    # Source: ontology range is xsd:string

    sh:maxCount 1 ; 
    # Inferred: expected at most one conditions value for each common characteristics subset

    sh:message "conditionsSwitchTrainProtectionSystems: The CommonCharacteristicsSubset must have at most one conditions value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ; 

    sh:path era:conditionsSwitchTrainProtectionSystems ; 

    sh:severity sh:Violation .

era-sh:ConditionsSwitchTrainProtectionSystemsApplicability a sh:SPARQLConstraint ; 
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:conditionsSwitchTrainProtectionSystems ; 
    era:scope "local" ; 

    rdfs:comment "Applicable when at least two different protection, control and warning systems exist."@en ; 
    # Source: dependency note in ontology

    sh:message "conditionsSwitchTrainProtectionSystems: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The conditions for switch train protection systems must be defined only when at least two different systems exist."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:conditionsSwitchTrainProtectionSystems ?value } .
        # Additional condition to check if at least two different systems exist can be added here if data is available
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:ConditionsTrainFormation a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:conditionsTrainFormation ;
    era:rinfIndex "4.1.4" ;
    era:scope "local" ;

    rdfs:comment "Conditions of use regarding train formation."@en ;
    # Source: ontology comment for conditionsTrainFormation

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one conditions description for each vehicle type

    sh:message "conditionsTrainFormation: The VehicleType must have at most one conditions description that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:conditionsTrainFormation ;

    sh:severity sh:Violation .

era-sh:ConditionsTrainFormationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:conditionsTrainFormation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is defined and requires conditions for train formation."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "conditionsTrainFormation: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The conditions for train formation must be defined only when the vehicle type is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:conditionsTrainFormation ?value } .
        # Additional condition to check if the vehicle type requires conditions for train formation can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ContactStripMaterialMetallicContent a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:contactStripMaterialMetallicContent ; 
    era:rinfIndex "1.1.1.2.3.4" ; 
    era:scope "local" ; 

    rdfs:comment "Indication of max. percentage of contact strip material Impregnated Carbon permitted to be used."@en ; 
    # Source: ontology comment for contactStripMaterialMetallicContent

    sh:datatype xsd:integer ; 
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ; 
    # Inferred: expected at most one metallic content value for each contact strip material

    sh:message "contactStripMaterialMetallicContent: The CommonCharacteristicsSubset must have at most one metallic content value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ; 

    sh:path era:contactStripMaterialMetallicContent ; 

    sh:severity sh:Violation .

era-sh:ContactStripMaterialMetallicContentApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:contactStripMaterialMetallicContent ; 
    era:scope "local" ; 

    rdfs:comment "Mandatory only if impregnated carbon ([NN] % of metallic content) is selected in permitted contact strip material."@en ; 
    # Source: dependency note in ontology

    sh:message "contactStripMaterialMetallicContent: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The metallic content must be defined only when impregnated carbon is selected as the contact strip material."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:contactStripMaterialMetallicContent ?value } .
        # Additional condition to check if impregnated carbon is selected can be added here if data is available
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:ContainerHandlingFlag a sh:PropertyShape ;
    era:affectedClass era:PrimaryLocation ;
    era:affectedProperty era:containerHandlingFlag ;
    era:scope "local" ;

    rdfs:comment "Indicates whether container handling is applicable at the primary location."@en ;
    # Source: ontology comment for containerHandlingFlag

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one container handling flag for each primary location

    sh:message "containerHandlingFlag: The PrimaryLocation must have at most one container handling flag that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:containerHandlingFlag ;

    sh:severity sh:Violation .

era-sh:ContainerHandlingFlagApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PrimaryLocation ;
    era:affectedProperty era:containerHandlingFlag ;
    era:scope "local" ;

    rdfs:comment "Applicable when the primary location is defined and requires information on container handling."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "containerHandlingFlag: This error is due to the PrimaryLocation {?locationLabel}, violating the rule: The container handling flag must be defined only when the primary location is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?locationLabel WHERE {
        $this rdfs:label ?locationLabel .
        FILTER NOT EXISTS { $this era:containerHandlingFlag ?value } .
        # Additional condition to check if the primary location requires container handling can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:CrossSectionArea a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:crossSectionArea ;
    era:rinfIndex "1.1.1.1.8.8" ;
    era:scope "local" ;

    rdfs:comment "Smallest cross section area in square metres of the tunnel."@en ;
    # Source: ontology comment for crossSectionArea

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one cross section area value for each tunnel

    sh:message "crossSectionArea: The Tunnel must have at most one cross section area value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:crossSectionArea ;

    sh:severity sh:Violation .

era-sh:CrossSectionAreaApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:crossSectionArea ;
    era:scope "local" ;

    rdfs:comment "Mandatory if the speed of the line is equal or greater than 200km/h."@en ;
    # Source: dependency note in ontology

    sh:message "crossSectionArea: This error is due to the Tunnel {?tunnelLabel}, violating the rule: The cross section area must be defined only when the speed of the line is equal or greater than 200 km/h."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:crossSectionArea ?value } .
        # Additional condition to check if the speed of the line is >= 200 km/h can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:CurrentLimitationRequired a sh:PropertyShape ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:currentLimitationRequired ;
    era:rinfIndex "1.1.1.2.5.1" ;
    era:scope "local" ;

    rdfs:comment "Indication of whether an on board current or power limitation function on vehicles is required."@en ;
    # Source: ontology comment for currentLimitationRequired

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one current limitation required value for each contact line system

    sh:message "currentLimitationRequired: The ContactLineSystem must have at most one current limitation required value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:currentLimitationRequired ;

    sh:severity sh:Violation .

era-sh:CurrentLimitationRequiredApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:currentLimitationRequired ;
    era:scope "local" ;

    rdfs:comment "Applicable when the contact line system is defined and requires information on current or power limitation."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "currentLimitationRequired: This error is due to the ContactLineSystem {?systemLabel}, violating the rule: The current limitation required must be defined only when the contact line system is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:currentLimitationRequired ?value } .
        # Additional condition to check if the contact line system requires current limitation can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:CurrentlyValid a sh:PropertyShape ;
    era:affectedClass era:Feature ;
    era:affectedProperty era:currentlyValid ;
    era:scope "local" ;

    rdfs:comment "Indicates whether the feature is currently valid."@en ;
    # Source: ontology comment for currentlyValid

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one currently valid value for each feature

    sh:message "currentlyValid: The Feature must have at most one currently valid value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:currentlyValid ;

    sh:severity sh:Violation .

era-sh:CurrentlyValidApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Feature ;
    era:affectedProperty era:currentlyValid ;
    era:scope "local" ;

    rdfs:comment "Applicable when the feature is defined and requires information on its current validity."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "currentlyValid: This error is due to the Feature {?featureLabel}, violating the rule: The currently valid status must be defined only when the feature is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?featureLabel WHERE {
        $this rdfs:label ?featureLabel .
        FILTER NOT EXISTS { $this era:currentlyValid ?value } .
        # Additional condition to check if the feature requires currently valid information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:dNvovtrp a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:dNvovtrp ;
    era:rinfIndex "1.1.1.3.2.16.5" ;
    era:scope "local" ;

    rdfs:comment "Maximum distance for overriding the train trip in metres, according to the specification referenced in Appendix A-1, index [C]."@en ;
    # Source: ontology comment for dNvovtrp

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for maximum distance

    sh:message "dNvovtrp: The CommonCharacteristicsSubset must have at most one dNvovtrp value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:dNvovtrp ;

    sh:severity sh:Violation .

era-sh:dNvovtrpApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:dNvovtrp ;
    era:scope "local" ;

    rdfs:comment "Mandatory when ETCS is present; a value must be provided."@en ;
    # Source: dependency note in ontology

    sh:message "dNvovtrp: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The dNvovtrp must be defined only when ETCS is present."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:dNvovtrp ?value } .
        # Additional condition to check if ETCS is present can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:dNvpotrp a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:dNvpotrp ;
    era:rinfIndex "1.1.1.3.2.16.7" ;
    era:scope "local" ;

    rdfs:comment "Maximum distance for reversing in Post Trip mode in metres, according to the specification referenced in Appendix A-1, index [C]."@en ;
    # Source: ontology comment for dNvpotrp

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for maximum distance

    sh:message "dNvpotrp: The CommonCharacteristicsSubset must have at most one dNvpotrp value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:dNvpotrp ;

    sh:severity sh:Violation .

era-sh:dNvpotrpApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:dNvpotrp ;
    era:scope "local" ;

    rdfs:comment "Mandatory when ETCS is present; a value must be provided."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "dNvpotrp: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The dNvpotrp must be defined only when ETCS is present."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:dNvpotrp ?value } .
        # Additional condition to check if ETCS is present can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:dNvroll a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:dNvroll ;
    era:rinfIndex "1.1.1.3.2.16.1" ;
    era:scope "local" ;

    rdfs:comment "Parameter used by the ETCS on-board to supervise the distance allowed to be travelled under the roll-away protection and the reverse movement protection, in metres."@en ;
    # Source: ontology comment for dNvroll

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for dNvroll

    sh:message "dNvroll: The CommonCharacteristicsSubset must have at most one dNvroll value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:dNvroll ;

    sh:severity sh:Violation .

era-sh:dNvrollApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:dNvroll ;
    era:scope "local" ;

    rdfs:comment "Mandatory when ETCS is present; a value must be provided."@en ;
    # Source: dependency note in ontology

    sh:message "dNvroll: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The dNvroll must be defined only when ETCS is present."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:dNvroll ?value } .
        # Additional condition to check if ETCS is present can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DangerousGoodsTankCode a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:dangerousGoodsTankCode ;
    era:rinfIndex "4.1.9" ;
    era:scope "local" ;

    rdfs:comment "Dangerous goods for which the vehicle is suitable (tank code)."@en ;
    # Source: ontology comment for dangerousGoodsTankCode

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one tank code for each vehicle type

    sh:message "dangerousGoodsTankCode: The VehicleType must have at most one dangerous goods tank code that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:dangerousGoodsTankCode ;

    sh:severity sh:Violation .

era-sh:DangerousGoodsTankCodeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:dangerousGoodsTankCode ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is defined and requires a dangerous goods tank code."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "dangerousGoodsTankCode: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The dangerous goods tank code must be defined only when the vehicle type is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:dangerousGoodsTankCode ?value } .
        # Additional condition to check if the vehicle type requires a dangerous goods tank code can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DemonstrationENE a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:demonstrationENE ; 
    era:rinfIndex "1.1.1.2.1.2" ; 
    era:scope "local" ; 

    rdfs:comment "Unique number for EI declarations following the same format requirements as specified for EC declarations in Annex VII of Commission Implementing Regulation (EU) 2019/250."@en ; 
    # Source: ontology comment for demonstrationENE

    sh:datatype xsd:string ; 
    # Source: ontology range is xsd:string

    sh:maxCount 1 ; 
    # Inferred: expected at most one EI declaration for each common characteristics subset

    sh:message "demonstrationENE: The CommonCharacteristicsSubset must have at most one demonstrationENE value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ; 

    sh:path era:demonstrationENE ; 

    sh:severity sh:Violation .

era-sh:DemonstrationENEApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:demonstrationENE ; 
    era:scope "local" ; 

    rdfs:comment "Applicable when the EI declaration was executed and issued for the energy subsystem on the specific track."@en ; 
    # Source: inferred from the context of the RAG and ontology

    sh:message "demonstrationENE: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The demonstrationENE must be defined only when the EI declaration was executed and issued."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:demonstrationENE ?value } .
        # Additional condition to check if the EI declaration was executed can be added here if data is available
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:DemonstrationINF a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:demonstrationINF ;
    era:rinfIndex "1.2.1.0.1.2" ;
    era:scope "local" ;

    rdfs:comment "Unique number for EI declarations following the same format requirements as specified in the ‘Document about practical arrangements for transmitting interoperability documents’."@en ;
    # Source: ontology comment for demonstrationINF

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one EI declaration for each common characteristics subset

    sh:message "demonstrationINF: The CommonCharacteristicsSubset must have at most one demonstrationINF value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:demonstrationINF ;

    sh:severity sh:Violation .

era-sh:DemonstrationINFApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:demonstrationINF ;
    era:scope "local" ;

    rdfs:comment "Applicable when the demonstration was executed and EI declaration was issued."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "demonstrationINF: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The demonstrationINF must be defined only when the EI declaration was executed and issued."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:demonstrationINF ?value } .
        # Additional condition to check if the EI declaration was executed can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DemonstrationSRT a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:demonstrationSRT ;
    era:rinfIndex "1.1.1.1.8.6" ;
    era:scope "local" ;

    rdfs:comment "Unique number for EI declarations following the same format requirements as specified in the ‘Document about practical arrangements for transmitting interoperability documents’."@en ;
    # Source: ontology comment for demonstrationSRT

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one EI declaration for each tunnel

    sh:message "demonstrationSRT: The Tunnel must have at most one demonstrationSRT value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:demonstrationSRT ;

    sh:severity sh:Violation .

era-sh:DemonstrationSRTApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:demonstrationSRT ;
    era:scope "local" ;

    rdfs:comment "Applicable when the demonstration was executed and EI declaration was issued for the tunnel."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "demonstrationSRT: This error is due to the Tunnel {?tunnelLabel}, violating the rule: The demonstrationSRT must be defined only when the EI declaration was executed and issued."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:demonstrationSRT ?value } .
        # Additional condition to check if the EI declaration was executed can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DesignMassExceptionalPayload a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:designMassExceptionalPayload ;
    era:rinfIndex "4.5.2.3" ;
    era:scope "local" ;

    rdfs:comment "Design mass under exceptional payload."@en ;
    # Source: ontology comment for designMassExceptionalPayload

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one design mass exceptional payload value for each vehicle type

    sh:message "designMassExceptionalPayload: The VehicleType must have at most one design mass exceptional payload value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:designMassExceptionalPayload ;

    sh:severity sh:Violation .

era-sh:DesignMassExceptionalPayloadApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:designMassExceptionalPayload ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the vehicle type is defined and requires information on design mass under exceptional payload."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "designMassExceptionalPayload: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The design mass exceptional payload must be defined only when the vehicle type is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:designMassExceptionalPayload ?value } .
        # Additional condition to check if the vehicle type requires design mass exceptional payload can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DesignMassNormalPayload a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:designMassNormalPayload ;
    era:rinfIndex "4.5.2.2" ;
    era:scope "local" ;

    rdfs:comment "Design mass under normal payload."@en ;
    # Source: ontology comment for designMassNormalPayload

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one design mass normal payload value for each vehicle type

    sh:message "designMassNormalPayload: The VehicleType must have at most one design mass normal payload value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:designMassNormalPayload ;

    sh:severity sh:Violation .

era-sh:DesignMassNormalPayloadApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:designMassNormalPayload ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the vehicle type is defined and requires information on design mass under normal payload."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "designMassNormalPayload: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The design mass normal payload must be defined only when the vehicle type is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:designMassNormalPayload ?value } .
        # Additional condition to check if the vehicle type requires design mass normal payload can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DesignMassWorkingOrder a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:designMassWorkingOrder ;
    era:rinfIndex "4.5.2.1" ;
    era:scope "local" ;

    rdfs:comment "Design mass in working order."@en ;
    # Source: ontology comment for designMassWorkingOrder

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one design mass working order value for each vehicle type

    sh:message "designMassWorkingOrder: The VehicleType must have at most one design mass working order value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:designMassWorkingOrder ;

    sh:severity sh:Violation .

era-sh:DesignMassWorkingOrderApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:designMassWorkingOrder ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the vehicle type is defined and requires information on design mass in working order."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "designMassWorkingOrder: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The design mass working order must be defined only when the vehicle type is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:designMassWorkingOrder ?value } .
        # Additional condition to check if the vehicle type requires design mass working order can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DieselThermalAllowed a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:dieselThermalAllowed ;
    era:rinfIndex "1.2.1.0.5.9" ;
    era:scope "local" ;

    rdfs:comment "Indication whether it is allowed to use diesel or other thermal traction in the tunnel."@en ;
    # Source: ontology comment for dieselThermalAllowed

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for diesel thermal allowed

    sh:message "dieselThermalAllowed: The Tunnel must have at most one dieselThermalAllowed value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:dieselThermalAllowed ;

    sh:severity sh:Violation .

era-sh:DieselThermalAllowedApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:dieselThermalAllowed ;
    era:scope "local" ;

    rdfs:comment "Applicable only for tunnels when the parameter 1.2.1.0.5.7 (none) was selected."@en ;
    # Source: RAG explanation on applicability

    sh:message "dieselThermalAllowed: This error is due to the Tunnel {?tunnelLabel}, violating the rule: The dieselThermalAllowed must be defined only when the respective national rules exist."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:dieselThermalAllowed ?value } .
        # Additional condition to check if the parameter 1.2.1.0.5.7 is 'none' can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DigitalSchematicOverview a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:digitalSchematicOverview ;
    era:rinfIndex "1.2.0.0.0.7.2" ;
    era:scope "local" ;

    rdfs:comment "Diagrammatic representation of the operational point in Well Known Text polyline."@en ;
    # Source: ontology comment for digitalSchematicOverview

    sh:datatype geosparql:wktLiteral ;
    # Source: ontology range is geosparql:wktLiteral

    sh:maxCount 1 ;
    # Inferred: expected at most one digital schematic overview for each operational point

    sh:message "digitalSchematicOverview: The OperationalPoint must have at most one digital schematic overview value that is a Well Known Text representation. This error may be due to having more than one value or having a value that is not a valid WKT."@en ;

    sh:path era:digitalSchematicOverview ;

    sh:severity sh:Violation .

era-sh:DigitalSchematicOverviewApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:digitalSchematicOverview ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the operational point is defined and requires a digital schematic overview."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "digitalSchematicOverview: This error is due to the OperationalPoint {?opLabel}, violating the rule: The digital schematic overview must be defined only when the operational point is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?opLabel WHERE {
        $this rdfs:label ?opLabel .
        FILTER NOT EXISTS { $this era:digitalSchematicOverview ?value } .
        # Additional condition to check if the operational point requires a digital schematic overview can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DistSignToPhaseEnd a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:distSignToPhaseEnd ;
    era:rinfIndex "1.1.1.2.4.3" ;
    era:scope "local" ;

    rdfs:comment "Distance between the signboard authorizing the driver to raise pantograph or close the circuit breaker after passing the phase separation and the end of the phase separation section."@en ;
    # Source: ontology comment for distSignToPhaseEnd

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for distSignToPhaseEnd

    sh:message "distSignToPhaseEnd: The CommonCharacteristicsSubset must have at most one distSignToPhaseEnd value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:distSignToPhaseEnd ;

    sh:severity sh:Violation .

era-sh:DistSignToPhaseEndApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:distSignToPhaseEnd ;
    era:scope "local" ;

    rdfs:comment "Applicable only if the value 'Overhead contact line (OCL)' is selected for parameter 'Type of contact line system'."@en ;
    # Source: dependency note in ontology

    sh:message "distSignToPhaseEnd: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The distSignToPhaseEnd must be defined only when the Type of contact line system is 'Overhead contact line (OCL)'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:distSignToPhaseEnd ?value } .
        # Additional condition to check if the Type of contact line system is 'Overhead contact line (OCL)' can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DocumentUrl a sh:PropertyShape ;
    era:affectedClass era:Document ;
    era:affectedProperty era:documentUrl ;
    era:scope "local" ;

    rdfs:comment "URL that is used to download a document, e.g. url for a reference document in RINF."@en ;
    # Source: ontology comment for documentUrl

    sh:datatype xsd:anyURI ;
    # Source: ontology range is xsd:anyURI

    sh:maxCount 1 ;
    # Inferred: expected at most one document URL for each document

    sh:message "documentUrl: The Document must have at most one document URL that is a valid URI. This error may be due to having more than one value or having a value that is not a valid URI."@en ;

    sh:path era:documentUrl ;

    sh:severity sh:Violation .

era-sh:DocumentUrlApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Document ;
    era:affectedProperty era:documentUrl ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the document is defined and requires a URL for downloading."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "documentUrl: This error is due to the Document {?docLabel}, violating the rule: The document URL must be defined only when the document is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?docLabel WHERE {
        $this rdfs:label ?docLabel .
        FILTER NOT EXISTS { $this era:documentUrl ?value } .
        # Additional condition to check if the document requires a URL can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DrivingCabs a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:drivingCabs ;
    era:rinfIndex "4.1.1" ;
    era:scope "local" ;

    rdfs:comment "Number of driving cabs. For wagons the number of driving cabs is to be set to zero (0)."@en ;
    # Source: ontology comment for drivingCabs

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one driving cab value for each vehicle type

    sh:message "drivingCabs: The VehicleType must have at most one drivingCabs value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:drivingCabs ;

    sh:severity sh:Violation .

era-sh:DrivingCabsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:drivingCabs ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the vehicle type is defined and requires information on the number of driving cabs."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "drivingCabs: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The drivingCabs must be defined only when the vehicle type is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:drivingCabs ?value } .
        # Additional condition to check if the vehicle type requires driving cabs can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EddyCurrentBrakePrevention a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:eddyCurrentBrakePrevention ;
    era:rinfIndex "4.7.4.1.2" ;
    era:scope "local" ;

    rdfs:comment "Possibility of preventing the use of the eddy current track brake (only if fitted with eddy current brake)."@en ;
    # Source: ontology comment for eddyCurrentBrakePrevention

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for eddyCurrentBrakePrevention

    sh:message "eddyCurrentBrakePrevention: The VehicleType must have at most one eddyCurrentBrakePrevention value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:eddyCurrentBrakePrevention ;

    sh:severity sh:Violation .

era-sh:EddyCurrentBrakePreventionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:eddyCurrentBrakePrevention ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the vehicle type is defined and fitted with an eddy current brake."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "eddyCurrentBrakePrevention: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The eddyCurrentBrakePrevention must be defined only when the vehicle type is fitted with an eddy current brake."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:eddyCurrentBrakePrevention ?value } .
        # Additional condition to check if the vehicle type is fitted with an eddy current brake can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EddyCurrentBrakingFitted a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:eddyCurrentBrakingFitted ;
    era:rinfIndex "4.7.4.1.1" ;
    era:scope "local" ;

    rdfs:comment "Eddy current track brake fitted. New property defined to distinguish it from eddyCurrentBraking which is a RINF SKOS property."@en ;
    # Source: ontology comment for eddyCurrentBrakingFitted

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for eddyCurrentBrakingFitted

    sh:message "eddyCurrentBrakingFitted: The VehicleType must have at most one eddyCurrentBrakingFitted value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:eddyCurrentBrakingFitted ;

    sh:severity sh:Violation .

era-sh:EddyCurrentBrakingFittedApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:eddyCurrentBrakingFitted ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the vehicle type is defined and fitted with an eddy current brake."@en ;
    # Source: inferred from the context of the RAG and ontology

    sh:message "eddyCurrentBrakingFitted: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The eddyCurrentBrakingFitted must be defined only when the vehicle type is fitted with an eddy current brake."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:eddyCurrentBrakingFitted ?value } .
        # Additional condition to check if the vehicle type is fitted with an eddy current brake can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EmergencyBrake a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:emergencyBrake ;
    era:rinfIndex "4.7.5" ;
    era:scope "local" ;

    rdfs:comment "Stopping distance and deceleration profile for each load condition per design maximum speed."@en ;
    # Source: ontology comment for emergencyBrake

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one emergency brake value for each vehicle type

    sh:message "emergencyBrake: The VehicleType must have at most one emergency brake value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:emergencyBrake ;

    sh:severity sh:Violation .

era-sh:EmergencyBrakeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:emergencyBrake ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the vehicle type is defined and requires information on emergency braking."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "emergencyBrake: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The emergency brake must be defined only when the vehicle type is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:emergencyBrake ?value } .
        # Additional condition to check if the vehicle type requires emergency brake information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EndOffsetFromOrigin a sh:PropertyShape ;
    era:affectedClass era:LinearElementSection ;
    era:affectedProperty era:endOffsetFromOrigin ;
    era:scope "local" ;

    rdfs:comment "End offset of the section from the origin."@en ;
    # Source: ontology comment for endOffsetFromOrigin

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one end offset value for each linear element section

    sh:message "endOffsetFromOrigin: The LinearElementSection must have at most one end offset from origin value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:endOffsetFromOrigin ;

    sh:severity sh:Violation .

era-sh:EndOffsetFromOriginApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LinearElementSection ;
    era:affectedProperty era:endOffsetFromOrigin ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the linear element section is defined and requires an end offset from the origin."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "endOffsetFromOrigin: This error is due to the LinearElementSection {?sectionLabel}, violating the rule: The end offset from origin must be defined only when the linear element section is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sectionLabel WHERE {
        $this rdfs:label ?sectionLabel .
        FILTER NOT EXISTS { $this era:endOffsetFromOrigin ?value } .
        # Additional condition to check if the linear element section requires an end offset can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EnergyMeterInstalled a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:energyMeterInstalled ;
    era:rinfIndex "4.10.12" ;
    era:scope "local" ;

    rdfs:comment "TSI conform energy meter for billing purposes installed on board."@en ;
    # Source: ontology comment for energyMeterInstalled

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one energy meter installed value for each vehicle type

    sh:message "energyMeterInstalled: The VehicleType must have at most one energy meter installed value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:energyMeterInstalled ;

    sh:severity sh:Violation .

era-sh:EnergyMeterInstalledApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:energyMeterInstalled ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the vehicle type is defined and requires information on the installation of an energy meter."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "energyMeterInstalled: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The energy meter installed must be defined only when the vehicle type is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:energyMeterInstalled ?value } .
        # Additional condition to check if the vehicle type requires an energy meter can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EnergySupplyMaxPower a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:energySupplyMaxPower ;
    era:rinfIndex "4.10.2" ;
    era:scope "local" ;

    rdfs:comment "Maximum power (to be indicated for each energy supply system the vehicle is equipped for), given in kW."@en ;
    # Source: ontology comment for energySupplyMaxPower

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one maximum power value for each vehicle type

    sh:message "energySupplyMaxPower: The VehicleType must have at most one energySupplyMaxPower value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:energySupplyMaxPower ;

    sh:severity sh:Violation .

era-sh:EnergySupplyMaxPowerApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:energySupplyMaxPower ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the vehicle type is defined and requires information on the maximum power for energy supply."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "energySupplyMaxPower: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The energySupplyMaxPower must be defined only when the vehicle type is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:energySupplyMaxPower ?value } .
        # Additional condition to check if the vehicle type requires energy supply max power can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EnergySupplySystemTSICompliant a sh:PropertyShape ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:energySupplySystemTSICompliant ;
    era:rinfIndex "1.1.1.2.2.1.2.1" ;
    era:scope "local" ;

    rdfs:comment "Indication if the traction supply system (nominal voltage and frequency) is fully compliant with TSI."@en ;
    # Source: ontology comment for energySupplySystemTSICompliant

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one compliance value for each contact line system

    sh:message "energySupplySystemTSICompliant: The ContactLineSystem must have at most one energySupplySystemTSICompliant value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:energySupplySystemTSICompliant ;

    sh:severity sh:Violation .

era-sh:EnergySupplySystemTSICompliantApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:energySupplySystemTSICompliant ;
    era:scope "local" ;

    rdfs:comment "Applicable when the traction supply system is electrified."@en ;
    # Source: inferred from the applicability explanation in RAG

    sh:message "energySupplySystemTSICompliant: This error is due to the ContactLineSystem {?clsLabel}, violating the rule: The energySupplySystemTSICompliant must be defined only when the traction supply system is not 'not electrified'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?clsLabel WHERE {
        $this rdfs:label ?clsLabel .
        FILTER NOT EXISTS { $this era:energySupplySystemTSICompliant ?value } .
        # Additional condition to check if the traction supply system is electrified can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ErrorCorrectionsOnboardExplanation a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:errorCorrectionsOnboardExplanation ; 
    era:rinfIndex "1.1.1.3.1.2" ; 
    era:scope "local" ;

    rdfs:comment "Explanation on why a mandatory onboard CR required to be solved in the on-board (ETCS, GSM-R and/or ATO) was accepted by the IM."@en ; 
    # Source: ontology comment for errorCorrectionsOnboardExplanation

    sh:datatype xsd:string ; 
    # Source: ontology range is xsd:string

    sh:maxCount 1 ; 
    # Inferred: expected at most one explanation for error corrections onboard

    sh:message "errorCorrectionsOnboardExplanation: The CommonCharacteristicsSubset must have at most one errorCorrectionsOnboardExplanation value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:errorCorrectionsOnboardExplanation ;

    sh:severity sh:Violation .

era-sh:ErrorCorrectionsOnboardExplanationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:errorCorrectionsOnboardExplanation ; 
    era:scope "local" ;

    rdfs:comment "Mandatory when ETCS is present and a value is provided on 1.1.1.3.2.1."@en ; 
    # Source: dependency note in ontology

    sh:message "errorCorrectionsOnboardExplanation: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The errorCorrectionsOnboardExplanation must be defined only when ETCS is present."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:errorCorrectionsOnboardExplanation ?value } .
        # Additional condition to check if ETCS is present can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ETCSDataCommApp a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:etcsDataCommApp ;
    era:rinfIndex "4.13.2.9" ;
    era:scope "local" ;

    rdfs:comment "Data communication application for ETCS implementation."@en ;
    # Source: ontology comment for etcsDataCommApp

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for ETCS data communication application

    sh:message "etcsDataCommApp: The VehicleType must have at most one etcsDataCommApp value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:etcsDataCommApp ;

    sh:severity sh:Violation .

era-sh:ETCSDataCommAppApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:etcsDataCommApp ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the vehicle type is defined and requires information on the ETCS data communication application."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "etcsDataCommApp: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The etcsDataCommApp must be defined only when the vehicle type is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:etcsDataCommApp ?value } .
        # Additional condition to check if the vehicle type requires etcsDataCommApp can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EtcsImplementsLevelCrossingProcedure a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:etcsImplementsLevelCrossingProcedure ;
    era:rinfIndex "1.1.1.3.2.13" ;
    era:scope "local" ;

    rdfs:comment "Indicates whether the ETCS trackside implements the level crossing procedure or an equivalent solution."@en ;
    # Source: ontology comment for etcsImplementsLevelCrossingProcedure

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for ETCS implementation of level crossing procedure

    sh:message "etcsImplementsLevelCrossingProcedure: The CommonCharacteristicsSubset must have at most one etcsImplementsLevelCrossingProcedure value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:etcsImplementsLevelCrossingProcedure ;

    sh:severity sh:Violation .

era-sh:EtcsImplementsLevelCrossingProcedureApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:etcsImplementsLevelCrossingProcedure ;
    era:scope "local" ;

    rdfs:comment "Mandatory when ETCS is present and a value is provided on 1.1.1.3.2.1."@en ;
    # Source: dependency note in ontology

    sh:message "etcsImplementsLevelCrossingProcedure: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The etcsImplementsLevelCrossingProcedure must be defined only when ETCS is present."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:etcsImplementsLevelCrossingProcedure ?value } .
        # Additional condition to check if ETCS is present can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EtcsInfillLineAccess a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:etcsInfillLineAccess ;
    era:rinfIndex "1.1.1.3.2.3" ;
    era:scope "local" ;

    rdfs:comment "Indication whether infill is required to access the line for safety reasons."@en ;
    # Source: ontology comment for etcsInfillLineAccess

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for etcsInfillLineAccess

    sh:message "etcsInfillLineAccess: The CommonCharacteristicsSubset must have at most one etcsInfillLineAccess value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:etcsInfillLineAccess ;

    sh:severity sh:Violation .

era-sh:EtcsInfillLineAccessApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:etcsInfillLineAccess ;
    era:scope "local" ;

    rdfs:comment "Only applicable when selected value for 1.1.1.3.2.1 is ‘1’."@en ;
    # Source: applicability explanation in RAG

    sh:message "etcsInfillLineAccess: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The etcsInfillLineAccess must be defined only when the selected value for 1.1.1.3.2.1 is '1'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:etcsInfillLineAccess ?value } .
        # Additional condition to check if the selected value for 1.1.1.3.2.1 is '1' can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ETCSNationalApplications a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:etcsNationalApplications ;
    era:rinfIndex "4.13.1.4" ;
    era:scope "local" ;

    rdfs:comment "ETCS national applications implemented (NID_XUSER of Packet 44)."@en ;
    # Source: ontology comment for etcsNationalApplications

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for ETCS national applications

    sh:message "etcsNationalApplications: The VehicleType must have at most one etcsNationalApplications value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:etcsNationalApplications ;

    sh:severity sh:Violation .

era-sh:ETCSNationalApplicationsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:etcsNationalApplications ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the vehicle type is defined and requires information on ETCS national applications."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "etcsNationalApplications: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The etcsNationalApplications must be defined only when the vehicle type is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:etcsNationalApplications ?value } .
        # Additional condition to check if the vehicle type requires etcsNationalApplications can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ETCSOnBoardImplementation a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:etcsOnBoardImplementation ;
    era:rinfIndex "4.13.1.7" ;
    era:scope "local" ;

    rdfs:comment "ETCS on-board implementation."@en ;
    # Source: ontology comment for etcsOnBoardImplementation

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for ETCS on-board implementation

    sh:message "etcsOnBoardImplementation: The VehicleType must have at most one etcsOnBoardImplementation value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:etcsOnBoardImplementation ;

    sh:severity sh:Violation .

era-sh:ETCSOnBoardImplementationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:etcsOnBoardImplementation ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the vehicle type is defined and requires information on ETCS on-board implementation."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "etcsOnBoardImplementation: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The etcsOnBoardImplementation must be defined only when the vehicle type is required to have one."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:etcsOnBoardImplementation ?value } .
        # Additional condition to check if the vehicle type requires etcsOnBoardImplementation can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EtcsTransmitsTrackConditions a sh:PropertyShape ;
    era:affectedClass era:ETCS ;
    era:affectedProperty era:etcsTransmitsTrackConditions ;
    era:scope "local" ;

    rdfs:comment "Indicates whether the ETCS trackside is engineered to transmit Track Conditions."@en ;
    # Source: ontology comment for etcsTransmitsTrackConditions

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for ETCS trackside transmission of track conditions

    sh:message "etcsTransmitsTrackConditions: The ETCS must have at most one etcsTransmitsTrackConditions value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:etcsTransmitsTrackConditions ;

    sh:severity sh:Violation .

era-sh:EtcsTransmitsTrackConditionsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ETCS ;
    era:affectedProperty era:etcsTransmitsTrackConditions ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the ETCS is defined and requires information on the transmission of track conditions."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "etcsTransmitsTrackConditions: This error is due to the ETCS {?etcsLabel}, violating the rule: The etcsTransmitsTrackConditions must be defined only when the ETCS is required to have this capability."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?etcsLabel WHERE {
        $this rdfs:label ?etcsLabel .
        FILTER NOT EXISTS { $this era:etcsTransmitsTrackConditions ?value } .
        # Additional condition to check if the ETCS requires track conditions transmission can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ExistanceOfIndustrialRisk a sh:PropertyShape ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:existanceOfIndustrialRisk ;
    era:rinfIndex "1.1.0.0.1.1" ;
    era:scope "local" ;

    rdfs:comment "Indication that the full section of line (or a part of it) is identified by the Infrastructure Managers (and communicated to the RU) where drivers are not supposed to get off the locomotive."@en ;
    # Source: ontology comment for existanceOfIndustrialRisk

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for existence of industrial risk for each section of line

    sh:message "existanceOfIndustrialRisk: The SectionOfLine must have at most one existanceOfIndustrialRisk value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:existanceOfIndustrialRisk ;

    sh:severity sh:Violation .

era-sh:ExistanceOfIndustrialRiskApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:existanceOfIndustrialRisk ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the section of line is defined and requires information on the existence of industrial risks."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "existanceOfIndustrialRisk: This error is due to the SectionOfLine {?sectionLabel}, violating the rule: The existanceOfIndustrialRisk must be defined only when the section of line is required to have this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sectionLabel WHERE {
        $this rdfs:label ?sectionLabel .
        FILTER NOT EXISTS { $this era:existanceOfIndustrialRisk ?value } .
        # Additional condition to check if the section of line requires information on industrial risks can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:FerromagneticWheelMaterial a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:ferromagneticWheelMaterial ;
    era:rinfIndex "4.14.2.12" ;
    era:scope "local" ;

    rdfs:comment "Indicates whether the wheel material is ferromagnetic."@en ;
    # Source: ontology comment for ferromagneticWheelMaterial

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one ferromagnetic wheel material value for each vehicle type

    sh:message "ferromagneticWheelMaterial: The VehicleType must have at most one ferromagneticWheelMaterial value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:ferromagneticWheelMaterial ;

    sh:severity sh:Violation .

era-sh:FerromagneticWheelMaterialApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:ferromagneticWheelMaterial ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the vehicle type is defined and requires information on the ferromagnetic characteristics of the wheel material."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "ferromagneticWheelMaterial: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The ferromagneticWheelMaterial must be defined only when the vehicle type is required to have this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:ferromagneticWheelMaterial ?value } .
        # Additional condition to check if the vehicle type requires ferromagnetic wheel material can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:FixedSeats a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:fixedSeats ;
    era:rinfIndex "4.12.1.1" ;
    era:scope "local" ;

    rdfs:comment "Number of fixed seats."@en ;
    # Source: ontology comment for fixedSeats

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for fixed seats

    sh:message "fixedSeats: The VehicleType must have at most one fixedSeats value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:fixedSeats ;

    sh:severity sh:Violation .

era-sh:FixedSeatsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:fixedSeats ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the vehicle type is defined and requires information on the number of fixed seats."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "fixedSeats: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The fixedSeats must be defined only when the vehicle type is required to have this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:fixedSeats ?value } .
        # Additional condition to check if the vehicle type requires fixedSeats can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:FlangeLubeForbidden a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:flangeLubeForbidden ; 
    era:rinfIndex "1.1.1.1.7.1" ; 
    era:scope "local" ;

    rdfs:comment "Indication whether the use of on-board device for flange lubrication is forbidden."@en ; 
    # Source: ontology comment for flangeLubeForbidden

    sh:datatype xsd:boolean ; 
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ; 
    # Inferred: expected at most one value for flange lubrication forbidden

    sh:message "flangeLubeForbidden: The CommonCharacteristicsSubset must have at most one flangeLubeForbidden value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:flangeLubeForbidden ;

    sh:severity sh:Violation .

era-sh:FlangeLubeForbiddenApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:flangeLubeForbidden ; 
    era:scope "local" ;

    rdfs:comment "Mandatory when the flange lubrication rules are applicable."@en ; 
    # Source: inferred from the context of the ontology and RAG

    sh:message "flangeLubeForbidden: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The flangeLubeForbidden must be defined only when the use of flange lubrication is applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:flangeLubeForbidden ?value } .
        # Additional condition to check if flange lubrication is applicable can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:FlangeLubeRules a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:flangeLubeRules ;
    era:rinfIndex "1.1.1.3.7.20" ;
    era:scope "local" ;

    rdfs:comment "Indication whether rules for activation or deactivation of flange lubrication exist."@en ;
    # Source: ontology comment for flangeLubeRules

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for flange lubrication rules

    sh:message "flangeLubeRules: The TrainDetectionSystem must have at most one flangeLubeRules value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:flangeLubeRules ;

    sh:severity sh:Violation .

era-sh:FlangeLubeRulesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:flangeLubeRules ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the selected option for parameter 1.1.1.3.7.1.1 is ‘track circuit’."@en ;
    # Source: applicability explanation in RAG

    sh:message "flangeLubeRules: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: The flangeLubeRules must be defined only when the selected option for parameter 1.1.1.3.7.1.1 is 'track circuit'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:flangeLubeRules ?value } .
        # Additional condition to check if the selected option for parameter 1.1.1.3.7.1.1 is 'track circuit' can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:FlangeLubricationFitted a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:flangeLubricationFitted ;
    era:rinfIndex "4.9.3.1" ;
    era:scope "local" ;

    rdfs:comment "Indicates if the vehicle type is fitted for flange lubrication."@en ;
    # Source: ontology comment for flangeLubricationFitted

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for flange lubrication fitted

    sh:message "flangeLubricationFitted: The VehicleType must have at most one flangeLubricationFitted value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:flangeLubricationFitted ;

    sh:severity sh:Violation .

era-sh:FlangeLubricationFittedApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:flangeLubricationFitted ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the vehicle type is defined and requires information on flange lubrication fitting."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "flangeLubricationFitted: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The flangeLubricationFitted must be defined only when the vehicle type is required to have this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:flangeLubricationFitted ?value } .
        # Additional condition to check if the vehicle type requires flange lubrication fitting can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:FreightFlag a sh:PropertyShape ;
    era:affectedClass era:PrimaryLocation ;
    era:affectedProperty era:freightFlag ;
    era:scope "local" ;

    rdfs:comment "Indicates whether the primary location is designated for freight operations."@en ;
    # Source: inferred from the context of the ontology and typical usage of freightFlag

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for freightFlag

    sh:message "freightFlag: The PrimaryLocation must have at most one freightFlag value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:freightFlag ;

    sh:severity sh:Violation .

era-sh:FreightFlagApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PrimaryLocation ;
    era:affectedProperty era:freightFlag ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the primary location is defined and requires information on freight operations."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "freightFlag: This error is due to the PrimaryLocation {?locationLabel}, violating the rule: The freightFlag must be defined only when the primary location is required to have this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?locationLabel WHERE {
        $this rdfs:label ?locationLabel .
        FILTER NOT EXISTS { $this era:freightFlag ?value } .
        # Additional condition to check if the primary location requires freightFlag can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:FrenchTrainDetectionSystemLimitationApplicable a sh:PropertyShape ;
    era:affectedClass era:FrenchTrainDetectionSystemLimitation ;
    era:affectedProperty era:frenchTrainDetectionSystemLimitationApplicable ;
    era:scope "local" ;

    rdfs:comment "Indicates if the limitation for the French train detection system is applicable."@en ;
    # Source: ontology comment for frenchTrainDetectionSystemLimitationApplicable

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for applicability

    sh:message "frenchTrainDetectionSystemLimitationApplicable: The FrenchTrainDetectionSystemLimitation must have at most one frenchTrainDetectionSystemLimitationApplicable value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:frenchTrainDetectionSystemLimitationApplicable ;

    sh:severity sh:Violation .

era-sh:FrenchTrainDetectionSystemLimitationApplicableApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:FrenchTrainDetectionSystemLimitation ;
    era:affectedProperty era:frenchTrainDetectionSystemLimitationApplicable ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the French train detection system limitation is defined and requires information on its applicability."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "frenchTrainDetectionSystemLimitationApplicable: This error is due to the FrenchTrainDetectionSystemLimitation {?limitationLabel}, violating the rule: The frenchTrainDetectionSystemLimitationApplicable must be defined only when the limitation is required to have this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?limitationLabel WHERE {
        $this rdfs:label ?limitationLabel .
        FILTER NOT EXISTS { $this era:frenchTrainDetectionSystemLimitationApplicable ?value } .
        # Additional condition to check if the limitation requires applicability information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GaugingCheckLocation a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gaugingCheckLocation ;
    era:rinfIndex "1.1.1.1.3.1.2" ;
    era:scope "local" ;

    rdfs:comment "Location of particular points requiring specific checks due to deviations from gauging."@en ;
    # Source: ontology comment for gaugingCheckLocation

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for gauging check location

    sh:message "gaugingCheckLocation: The CommonCharacteristicsSubset must have at most one gaugingCheckLocation value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:gaugingCheckLocation ;

    sh:severity sh:Violation .

era-sh:GaugingCheckLocationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gaugingCheckLocation ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the gauging check location is defined and requires information on specific checks."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "gaugingCheckLocation: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The gaugingCheckLocation must be defined only when the conditions for the application of the gauge are not fulfilled."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gaugingCheckLocation ?value } .
        # Additional condition to check if the conditions for the application of the gauge are not fulfilled can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GprsForETCS a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:gprsForETCS ; 
    era:rinfIndex "1.1.1.3.3.3.2" ; 
    era:scope "local" ;

    rdfs:comment "Indication if GPRS can be used for ETCS."@en ; 
    # Source: ontology comment for gprsForETCS

    sh:datatype xsd:boolean ; 
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ; 
    # Inferred: expected at most one value for GPRS for ETCS

    sh:message "gprsForETCS: The CommonCharacteristicsSubset must have at most one gprsForETCS value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:gprsForETCS ;

    sh:severity sh:Violation .

era-sh:GprsForETCSApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:gprsForETCS ; 
    era:scope "local" ;

    rdfs:comment "Applicable if the answer to 1.1.1.3.3.3.2 / GPRS for ETCS is Y."@en ; 
    # Source: applicability explanation in RAG

    sh:message "gprsForETCS: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The gprsForETCS must be defined only when the conditions for GPRS usage for ETCS are met."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gprsForETCS ?value } .
        # Additional condition to check if GPRS can be used for ETCS can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GprsImplementationArea a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gprsImplementationArea ;
    era:rinfIndex "1.1.1.3.3.3.3" ;
    era:scope "local" ;

    rdfs:comment "Indication of the area in which GPRS can be used for ETCS, expressed as a list of GPRS-enabled RBCs."@en ;
    # Source: ontology comment for gprsImplementationArea

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for GPRS implementation area

    sh:message "gprsImplementationArea: The CommonCharacteristicsSubset must have at most one gprsImplementationArea value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:gprsImplementationArea ;

    sh:severity sh:Violation .

era-sh:GprsImplementationAreaApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gprsImplementationArea ;
    era:scope "local" ;

    rdfs:comment "Applicable if the answer to 1.1.1.3.3.3.2 / GPRS for ETCS is Y."@en ;
    # Source: applicability explanation in RAG

    sh:message "gprsImplementationArea: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The gprsImplementationArea must be defined only when GPRS can be used for ETCS."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gprsImplementationArea ?value } .
        # Additional condition to check if GPRS can be used for ETCS can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Gradient a sh:PropertyShape ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:gradient ;
    era:rinfIndex "1.2.2.0.3.1" ;
    era:scope "local" ;

    rdfs:comment "Maximum value of the gradient for stabling tracks expressed in millimetres per metre."@en ;
    # Source: ontology comment for gradient

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one gradient value for each siding

    sh:message "gradient: The Siding must have at most one gradient value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:gradient ;

    sh:severity sh:Violation .

era-sh:GradientApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:gradient ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the siding is defined and requires information on the maximum gradient."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "gradient: This error is due to the Siding {?sidingLabel}, violating the rule: The gradient must be defined only when the siding is required to have this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sidingLabel WHERE {
        $this rdfs:label ?sidingLabel .
        FILTER NOT EXISTS { $this era:gradient ?value } .
        # Additional condition to check if the siding requires gradient information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GradientProfile a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gradientProfile ;
    era:rinfIndex "1.1.1.1.3.6" ;
    era:scope "local" ;

    rdfs:comment "Sequence of gradient values and locations of change in gradient."@en ;
    # Source: ontology comment for gradientProfile

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for gradient profile

    sh:message "gradientProfile: The CommonCharacteristicsSubset must have at most one gradientProfile value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:gradientProfile ;

    sh:severity sh:Violation .

era-sh:GradientProfileApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gradientProfile ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the gradient profile is defined and requires information on the sequence of gradient values."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "gradientProfile: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The gradientProfile must be defined only when the conditions for gradient profiling are met."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gradientProfile ?value } .
        # Additional condition to check if the gradient profile is applicable can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GsmRNoCoverage a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:gsmRNoCoverage ; 
    era:rinfIndex "1.1.1.3.3.8" ; 
    era:scope "local" ;

    rdfs:comment "Indication if there is no GSMR coverage."@en ; 
    # Source: ontology comment for gsmRNoCoverage

    sh:datatype xsd:boolean ; 
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ; 
    # Inferred: expected at most one value for GSMR no coverage

    sh:message "gsmRNoCoverage: The CommonCharacteristicsSubset must have at most one gsmRNoCoverage value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:gsmRNoCoverage ;

    sh:severity sh:Violation .

era-sh:GsmRNoCoverageApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:gsmRNoCoverage ; 
    era:scope "local" ;

    rdfs:comment "Mandatory when GSM-R is installed and applicable."@en ; 
    # Source: inferred from the context of the ontology and RAG

    sh:message "gsmRNoCoverage: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The gsmRNoCoverage must be defined only when GSM-R is installed."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gsmRNoCoverage ?value } .
        # Additional condition to check if GSM-R is installed can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GsmRSetsInDrivingCab a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:gsmRSetsInDrivingCab ;
    era:rinfIndex "4.13.2.2" ;
    era:scope "local" ;

    rdfs:comment "Number of GSM-R mobile sets in driving cab for data transmission."@en ;
    # Source: ontology comment for gsmRSetsInDrivingCab

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for GSM-R sets in driving cab

    sh:message "gsmRSetsInDrivingCab: The VehicleType must have at most one gsmRSetsInDrivingCab value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:gsmRSetsInDrivingCab ;

    sh:severity sh:Violation .

era-sh:GsmRSetsInDrivingCabApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:gsmRSetsInDrivingCab ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the vehicle type is defined and requires information on the number of GSM-R mobile sets in the driving cab."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "gsmRSetsInDrivingCab: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The gsmRSetsInDrivingCab must be defined only when the vehicle type requires this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:gsmRSetsInDrivingCab ?value } .
        # Additional condition to check if the vehicle type requires GSM-R sets information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GsmrForcedDeregistrationFunctionalNumber a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:gsmrForcedDeregistrationFunctionalNumber ; 
    era:rinfIndex "1.1.1.3.3.11" ; 
    era:scope "local" ;

    rdfs:comment "Indicates if the GSM-R network is configured to allow forced de-registration of a functional number by another driver."@en ; 
    # Source: ontology comment for gsmrForcedDeregistrationFunctionalNumber

    sh:datatype xsd:boolean ; 
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ; 
    # Inferred: expected at most one value for forced deregistration functional number

    sh:message "gsmrForcedDeregistrationFunctionalNumber: The CommonCharacteristicsSubset must have at most one gsmrForcedDeregistrationFunctionalNumber value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:gsmrForcedDeregistrationFunctionalNumber ;

    sh:severity sh:Violation .

era-sh:GsmrForcedDeregistrationFunctionalNumberApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:gsmrForcedDeregistrationFunctionalNumber ; 
    era:scope "local" ;

    rdfs:comment "Mandatory when GSM-R and ETCS L2 must be installed for this parameter to be applicable."@en ; 
    # Source: dependency note in ontology

    sh:message "gsmrForcedDeregistrationFunctionalNumber: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The gsmrForcedDeregistrationFunctionalNumber must be defined only when GSM-R and ETCS L2 are installed."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gsmrForcedDeregistrationFunctionalNumber ?value } .
        # Additional condition to check if GSM-R and ETCS L2 are installed can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HandoverPointFlag a sh:PropertyShape ;
    era:affectedClass era:PrimaryLocation ;
    era:affectedProperty era:handoverPointFlag ;
    era:scope "local" ;

    rdfs:comment "Indicates whether the primary location is designated as a handover point."@en ;
    # Source: inferred from the context of the ontology and typical usage of handoverPointFlag

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for handoverPointFlag

    sh:message "handoverPointFlag: The PrimaryLocation must have at most one handoverPointFlag value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:handoverPointFlag ;

    sh:severity sh:Violation .

era-sh:HandoverPointFlagApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PrimaryLocation ;
    era:affectedProperty era:handoverPointFlag ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the primary location is defined and requires information on handover points."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "handoverPointFlag: This error is due to the PrimaryLocation {?locationLabel}, violating the rule: The handoverPointFlag must be defined only when the primary location is required to have this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?locationLabel WHERE {
        $this rdfs:label ?locationLabel .
        FILTER NOT EXISTS { $this era:handoverPointFlag ?value } .
        # Additional condition to check if the primary location requires handoverPointFlag can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasAdditionalBrakingInformation a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasAdditionalBrakingInformation ;
    era:rinfIndex "1.1.1.3.11.2" ;
    era:scope "local" ;

    rdfs:comment "Availability by the IM of additional information as defined in point (2) of point 4.2.2.6.2 of Regulation (EU) 2023/1693 - TSI OPE."@en ;
    # Source: ontology comment for hasAdditionalBrakingInformation

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for additional braking information

    sh:message "hasAdditionalBrakingInformation: The CommonCharacteristicsSubset must have at most one hasAdditionalBrakingInformation value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasAdditionalBrakingInformation ;

    sh:severity sh:Violation .

era-sh:HasAdditionalBrakingInformationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasAdditionalBrakingInformation ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the additional braking information is defined and requires information on its availability."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "hasAdditionalBrakingInformation: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The hasAdditionalBrakingInformation must be defined only when the conditions for additional braking information are met."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:hasAdditionalBrakingInformation ?value } .
        # Additional condition to check if the additional braking information is required can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasAutomaticDroppingDevice a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasAutomaticDroppingDevice ;
    era:rinfIndex "4.10.11" ;
    era:scope "local" ;

    rdfs:comment "Automatic dropping device (ADD) fitted (to be indicated for each energy supply system the vehicle is equipped for)."@en ;
    # Source: ontology comment for hasAutomaticDroppingDevice

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for automatic dropping device

    sh:message "hasAutomaticDroppingDevice: The VehicleType must have at most one hasAutomaticDroppingDevice value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasAutomaticDroppingDevice ;

    sh:severity sh:Violation .

era-sh:HasAutomaticDroppingDeviceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasAutomaticDroppingDevice ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the vehicle type is defined and requires information on the presence of an automatic dropping device."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "hasAutomaticDroppingDevice: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The hasAutomaticDroppingDevice must be defined only when the conditions for the automatic dropping device are met."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:hasAutomaticDroppingDevice ?value } .
        # Additional condition to check if the vehicle type requires information on the automatic dropping device can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasBallast a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:hasBallast ;
    era:rinfIndex "1.1.1.1.4.4" ;
    era:scope "local" ;

    rdfs:comment "Specifies whether track construction is with sleepers embedded in ballast or not."@en ;
    # Source: ontology comment for hasBallast

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for hasBallast

    sh:message "hasBallast: The RunningTrack must have at most one hasBallast value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasBallast ;

    sh:severity sh:Violation .

era-sh:HasBallastApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:hasBallast ;
    era:scope "local" ;

    rdfs:comment "Mandatory for tracks with permitted speed greater than or equal to 250 km/h."@en ;
    # Source: dependency note in ontology

    sh:message "hasBallast: This error is due to the RunningTrack {?trackLabel}, violating the rule: The hasBallast must be defined only when the permitted speed is greater than or equal to 250 km/h."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:hasBallast ?value } .
        # Additional condition to check if the permitted speed is >= 250 km/h can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasBridge a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:hasBridge ;
    era:scope "local" ;

    rdfs:comment "Indicates whether or not a track is associated with a bridge."@en ;
    # Source: ontology comment for hasBridge

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for hasBridge

    sh:message "hasBridge: The RunningTrack must have at most one hasBridge value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasBridge ;

    sh:severity sh:Violation .

era-sh:HasBridgeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:hasBridge ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the running track is defined and requires information on the association with a bridge."@en ;
    # Source: inferred from the context of the ontology and typical usage of hasBridge

    sh:message "hasBridge: This error is due to the RunningTrack {?trackLabel}, violating the rule: The hasBridge must be defined only when the running track is required to have this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:hasBridge ?value } .
        # Additional condition to check if the running track requires bridge information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasCantDefficiencyCompensation a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasCantDefficiencyCompensation ;
    era:rinfIndex "4.6.2" ;
    era:scope "local" ;

    rdfs:comment "Vehicle equipped with a cant deficiency compensation system (tilting vehicle)."@en ;
    # Source: ontology comment for hasCantDefficiencyCompensation

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for cant deficiency compensation

    sh:message "hasCantDefficiencyCompensation: The VehicleType must have at most one hasCantDefficiencyCompensation value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasCantDefficiencyCompensation ;

    sh:severity sh:Violation .

era-sh:HasCantDefficiencyCompensationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasCantDefficiencyCompensation ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the vehicle type is defined and requires information on the presence of a cant deficiency compensation system."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "hasCantDefficiencyCompensation: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The hasCantDefficiencyCompensation must be defined only when the vehicle type requires this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:hasCantDefficiencyCompensation ?value } .
        # Additional condition to check if the vehicle type requires cant deficiency compensation information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasCurrentLimitation a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasCurrentLimitation ;
    era:rinfIndex "4.10.14" ;
    era:scope "local" ;

    rdfs:comment "Electric units equipped with power or current limitation function."@en ;
    # Source: ontology comment for hasCurrentLimitation

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for current limitation

    sh:message "hasCurrentLimitation: The VehicleType must have at most one hasCurrentLimitation value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasCurrentLimitation ;

    sh:severity sh:Violation .

era-sh:HasCurrentLimitationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasCurrentLimitation ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the vehicle type is defined and requires information on the current limitation function."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "hasCurrentLimitation: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The hasCurrentLimitation must be defined only when the vehicle type requires this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:hasCurrentLimitation ?value } .
        # Additional condition to check if the vehicle type requires current limitation information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasETCSRestrictionsConditions a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasETCSRestrictionsConditions ;
    era:rinfIndex "1.1.1.3.2.6" ;
    era:scope "local" ;

    rdfs:comment "Indication whether restrictions or conditions due to partial compliance with the TSI CCS exist."@en ;
    # Source: ontology comment for hasETCSRestrictionsConditions

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for ETCS restrictions conditions

    sh:message "hasETCSRestrictionsConditions: The CommonCharacteristicsSubset must have at most one hasETCSRestrictionsConditions value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasETCSRestrictionsConditions ;

    sh:severity sh:Violation .

era-sh:HasETCSRestrictionsConditionsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasETCSRestrictionsConditions ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the selected value for 1.1.1.3.2.1 is not ‘N’."@en ;
    # Source: inferred from the applicability explanation in RAG

    sh:message "hasETCSRestrictionsConditions: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The hasETCSRestrictionsConditions must be defined only when the selected value for 1.1.1.3.2.1 is not ‘N’."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:hasETCSRestrictionsConditions ?value } .
        # Additional condition to check if the selected value for 1.1.1.3.2.1 is not 'N' can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasElectricShoreSupply a sh:PropertyShape ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:hasElectricShoreSupply ;
    era:rinfIndex "1.2.2.0.4.6" ;
    era:scope "local" ;

    rdfs:comment "Indication whether an installation for electric shore supply exists (fixed installation for servicing trains)."@en ;
    # Source: ontology comment for hasElectricShoreSupply

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for electric shore supply

    sh:message "hasElectricShoreSupply: The Siding must have at most one hasElectricShoreSupply value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasElectricShoreSupply ;

    sh:severity sh:Violation .

era-sh:HasElectricShoreSupplyApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:hasElectricShoreSupply ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the siding is defined and requires information on the existence of electric shore supply."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "hasElectricShoreSupply: This error is due to the Siding {?sidingLabel}, violating the rule: The hasElectricShoreSupply must be defined only when the siding requires this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sidingLabel WHERE {
        $this rdfs:label ?sidingLabel .
        FILTER NOT EXISTS { $this era:hasElectricShoreSupply ?value } .
        # Additional condition to check if the siding requires electric shore supply information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasEmergencyPlan a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:hasEmergencyPlan ;
    era:rinfIndex "1.1.1.1.8.9" ;
    era:scope "local" ;

    rdfs:comment "Indication whether an emergency plan exists."@en ;
    # Source: ontology comment for hasEmergencyPlan

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for emergency plan existence

    sh:message "hasEmergencyPlan: The Tunnel must have at most one hasEmergencyPlan value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasEmergencyPlan ;

    sh:severity sh:Violation .

era-sh:HasEmergencyPlanApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:hasEmergencyPlan ;
    era:scope "local" ;

    rdfs:comment "Mandatory for tunnels longer than 1 km, as the emergency plan is required only for such tunnels."@en ;
    # Source: RAG applicability explanation

    sh:message "hasEmergencyPlan: This error is due to the Tunnel {?tunnelLabel}, violating the rule: The hasEmergencyPlan must be defined only when the tunnel length is greater than 1 km."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:hasEmergencyPlan ?value } .
        # Additional condition to check if the tunnel length is greater than 1 km can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasEtcsNationalPacket44 a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasEtcsNationalPacket44 ;
    era:rinfIndex "1.1.1.3.2.5" ;
    era:scope "local" ;

    rdfs:comment "Indication whether data for national packet 44 applications is transmitted between track and train."@en ;
    # Source: ontology comment for hasEtcsNationalPacket44

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for ETCS national packet 44 application

    sh:message "hasEtcsNationalPacket44: The CommonCharacteristicsSubset must have at most one hasEtcsNationalPacket44 value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasEtcsNationalPacket44 ;

    sh:severity sh:Violation .

era-sh:HasEtcsNationalPacket44Applicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasEtcsNationalPacket44 ;
    era:scope "local" ;

    rdfs:comment "Only applicable when selected value for 1.1.1.3.2.1 (ETCS present) is not ‘N’."@en ;
    # Source: dependency note in ontology

    sh:message "hasEtcsNationalPacket44: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The hasEtcsNationalPacket44 must be defined only when the selected value for 1.1.1.3.2.1 is not ‘N’."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:hasEtcsNationalPacket44 ?value } .
        # Additional condition to check if the selected value for 1.1.1.3.2.1 is not 'N' can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasEvacuationAndRescuePoints a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:hasEvacuationAndRescuePoints ;
    era:rinfIndex "1.1.1.1.8.13" ;
    era:scope "local" ;

    rdfs:comment "Indication of existence of evacuation and rescue points."@en ;
    # Source: ontology comment for hasEvacuationAndRescuePoints

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for evacuation and rescue points

    sh:message "hasEvacuationAndRescuePoints: The Tunnel must have at most one hasEvacuationAndRescuePoints value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasEvacuationAndRescuePoints ;

    sh:severity sh:Violation .

era-sh:HasEvacuationAndRescuePointsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:hasEvacuationAndRescuePoints ;
    era:scope "local" ;

    rdfs:comment "Mandatory for TSI-compliant tunnels longer than 0.5 km."@en ;
    # Source: dependency note in ontology

    sh:message "hasEvacuationAndRescuePoints: This error is due to the Tunnel {?tunnelLabel}, violating the rule: The hasEvacuationAndRescuePoints must be defined only for TSI-compliant tunnels longer than 0.5 km."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:hasEvacuationAndRescuePoints ?value } .
        # Additional condition to check if the tunnel length is greater than 0.5 km can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasExternalCleaning a sh:PropertyShape ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:hasExternalCleaning ;
    era:rinfIndex "1.2.2.0.4.2" ;
    era:scope "local" ;

    rdfs:comment "Indication whether exists an installation of external cleaning facility (fixed installation for servicing trains) as defined in TSI INF."@en ;
    # Source: ontology comment for hasExternalCleaning

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for external cleaning

    sh:message "hasExternalCleaning: The Siding must have at most one hasExternalCleaning value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasExternalCleaning ;

    sh:severity sh:Violation .

era-sh:HasExternalCleaningApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:hasExternalCleaning ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the siding is defined and requires information on the existence of external cleaning facilities."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "hasExternalCleaning: This error is due to the Siding {?sidingLabel}, violating the rule: The hasExternalCleaning must be defined only when the siding requires this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sidingLabel WHERE {
        $this rdfs:label ?sidingLabel .
        FILTER NOT EXISTS { $this era:hasExternalCleaning ?value } .
        # Additional condition to check if the siding requires external cleaning information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasHotAxleBoxDetector a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasHotAxleBoxDetector ;
    era:rinfIndex "1.1.1.1.7.4" ;
    era:scope "local" ;

    rdfs:comment "Indicates the existence of trackside hot axle box detector (HABD)."@en ;
    # Source: ontology comment for hasHotAxleBoxDetector

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for hasHotAxleBoxDetector

    sh:message "hasHotAxleBoxDetector: The CommonCharacteristicsSubset must have at most one hasHotAxleBoxDetector value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasHotAxleBoxDetector ;

    sh:severity sh:Violation .

era-sh:HasHotAxleBoxDetectorApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasHotAxleBoxDetector ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the selected value for parameter 1.1.1.2.4.1 is ‘Y’."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "hasHotAxleBoxDetector: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The hasHotAxleBoxDetector must be defined only when the selected value for parameter 1.1.1.2.4.1 is ‘Y’."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:hasHotAxleBoxDetector ?value } .
        # Additional condition to check if the selected value for parameter 1.1.1.2.4.1 is 'Y' can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasLevelCrossings a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasLevelCrossings ;
    era:scope "local" ;

    rdfs:comment "Indication whether level crossings (including pedestrian track crossing) exist on the section of line."@en ;
    # Source: ontology comment for hasLevelCrossings

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for hasLevelCrossings

    sh:message "hasLevelCrossings: The CommonCharacteristicsSubset must have at most one hasLevelCrossings value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasLevelCrossings ;

    sh:severity sh:Violation .

era-sh:HasLevelCrossingsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasLevelCrossings ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the section of line is defined and requires information on the existence of level crossings."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "hasLevelCrossings: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The hasLevelCrossings must be defined only when the section of line requires this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:hasLevelCrossings ?value } .
        # Additional condition to check if the section of line requires level crossing information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasLubricationDevicePrevention a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasLubricationDevicePrevention ;
    era:rinfIndex "4.9.3.2" ;
    era:scope "local" ;

    rdfs:comment "Possibility of preventing the use of the lubrication device (only if fitted with flange lubrication)."@en ;
    # Source: ontology comment for hasLubricationDevicePrevention

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for lubrication device prevention

    sh:message "hasLubricationDevicePrevention: The VehicleType must have at most one hasLubricationDevicePrevention value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasLubricationDevicePrevention ;

    sh:severity sh:Violation .

era-sh:HasLubricationDevicePreventionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasLubricationDevicePrevention ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the vehicle type is defined and requires information on the possibility of preventing the use of the lubrication device."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "hasLubricationDevicePrevention: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The hasLubricationDevicePrevention must be defined only when the vehicle type requires this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:hasLubricationDevicePrevention ?value } .
        # Additional condition to check if the vehicle type requires lubrication device prevention information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasParkingBrake a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasParkingBrake ;
    era:rinfIndex "4.7.3.4" ;
    era:scope "local" ;

    rdfs:comment "Indicates if a vehicle type has parking brake."@en ;
    # Source: ontology comment for hasParkingBrake

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for parking brake

    sh:message "hasParkingBrake: The VehicleType must have at most one hasParkingBrake value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasParkingBrake ;

    sh:severity sh:Violation .

era-sh:HasParkingBrakeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasParkingBrake ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the vehicle type is defined and requires information on the existence of a parking brake."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "hasParkingBrake: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The hasParkingBrake must be defined only when the vehicle type requires this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:hasParkingBrake ?value } .
        # Additional condition to check if the vehicle type requires parking brake information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasPhaseSeparation a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasPhaseSeparation ;
    era:rinfIndex "1.1.1.2.4.1.1" ;
    era:scope "local" ;

    rdfs:comment "Indication of existence of phase separation and required information."@en ;
    # Source: ontology comment for hasPhaseSeparation

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for phase separation

    sh:message "hasPhaseSeparation: The CommonCharacteristicsSubset must have at most one hasPhaseSeparation value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasPhaseSeparation ;

    sh:severity sh:Violation .

era-sh:HasPhaseSeparationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasPhaseSeparation ;
    era:scope "local" ;

    rdfs:comment "This parameter is applicable only if 'Overhead contact line (OCL)' is selected in 1.1.1.2.2.1.1."@en ;
    # Source: applicability explanation in RAG

    sh:message "hasPhaseSeparation: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The hasPhaseSeparation must be defined only when 'Overhead contact line (OCL)' is selected."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:hasPhaseSeparation ?value } .
        # Additional condition to check if 'Overhead contact line (OCL)' is selected can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasPlatformCurvature a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasPlatformCurvature ;
    era:rinfIndex "1.2.1.0.6.8" ;
    era:scope "local" ;

    rdfs:comment "Indication of the existence of the curvature of the platform."@en ;
    # Source: ontology comment for hasPlatformCurvature

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for platform curvature

    sh:message "hasPlatformCurvature: The CommonCharacteristicsSubset must have at most one hasPlatformCurvature value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasPlatformCurvature ;

    sh:severity sh:Violation .

era-sh:HasPlatformCurvatureApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasPlatformCurvature ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the platform is defined and requires information on the existence of platform curvature."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "hasPlatformCurvature: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The hasPlatformCurvature must be defined only when the platform requires this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:hasPlatformCurvature ?value } .
        # Additional condition to check if the platform requires curvature information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasRefuelling a sh:PropertyShape ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:hasRefuelling ;
    era:rinfIndex "1.2.2.0.4.4" ;
    era:scope "local" ;

    rdfs:comment "Indication whether exists an installation of refuelling (fixed installation for servicing trains) as defined in TSI INF."@en ;
    # Source: ontology comment for hasRefuelling

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for refuelling existence

    sh:message "hasRefuelling: The Siding must have at most one hasRefuelling value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasRefuelling ;

    sh:severity sh:Violation .

era-sh:HasRefuellingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:hasRefuelling ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the siding is defined and requires information on the existence of refuelling facilities."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "hasRefuelling: This error is due to the Siding {?sidingLabel}, violating the rule: The hasRefuelling must be defined only when the siding requires this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sidingLabel WHERE {
        $this rdfs:label ?sidingLabel .
        FILTER NOT EXISTS { $this era:hasRefuelling ?value } .
        # Additional condition to check if the siding requires refuelling information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasRegenerativeBrake a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasRegenerativeBrake ;
    era:rinfIndex "4.7.4.3.1" ;
    era:scope "local" ;

    rdfs:comment "Indication whether regenerative braking is permitted or not."@en ;
    # Source: ontology comment for hasRegenerativeBrake

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for regenerative brake permission

    sh:message "hasRegenerativeBrake: The VehicleType must have at most one hasRegenerativeBrake value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasRegenerativeBrake ;

    sh:severity sh:Violation .

era-sh:HasRegenerativeBrakeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasRegenerativeBrake ;
    era:scope "local" ;

    rdfs:comment "Applicable when the selected value for parameter 1.1.1.2.2.1.1 (ECS_SystemType) is not ‘not electrified’."@en ;
    # Source: applicability explanation in RAG

    sh:message "hasRegenerativeBrake: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The hasRegenerativeBrake must be defined only when the ECS_SystemType is not ‘not electrified’."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:hasRegenerativeBrake ?value } .
        # Additional condition to check if ECS_SystemType is not 'not electrified' can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasSandRestocking a sh:PropertyShape ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:hasSandRestocking ;
    era:rinfIndex "1.2.2.0.4.5" ;
    era:scope "local" ;

    rdfs:comment "Indication whether an installation for sand restocking exists (fixed installation for servicing trains)."@en ;
    # Source: ontology comment for hasSandRestocking

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for sand restocking

    sh:message "hasSandRestocking: The Siding must have at most one hasSandRestocking value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasSandRestocking ;

    sh:severity sh:Violation .

era-sh:HasSandRestockingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:hasSandRestocking ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the siding is defined and requires information on the existence of sand restocking facilities."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "hasSandRestocking: This error is due to the Siding {?sidingLabel}, violating the rule: The hasSandRestocking must be defined only when the siding requires this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sidingLabel WHERE {
        $this rdfs:label ?sidingLabel .
        FILTER NOT EXISTS { $this era:hasSandRestocking ?value } .
        # Additional condition to check if the siding requires sand restocking information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasSandingPrevention a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasSandingPrevention ;
    era:rinfIndex "4.14.2.12" ;
    era:scope "local" ;

    rdfs:comment "Possibility of preventing the use of sanding."@en ;
    # Source: ontology comment for hasSandingPrevention

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for sanding prevention

    sh:message "hasSandingPrevention: The VehicleType must have at most one hasSandingPrevention value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasSandingPrevention ;

    sh:severity sh:Violation .

era-sh:HasSandingPreventionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasSandingPrevention ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the vehicle type is defined and requires information on the possibility of preventing the use of sanding."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "hasSandingPrevention: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The hasSandingPrevention must be defined only when the vehicle type requires this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:hasSandingPrevention ?value } .
        # Additional condition to check if the vehicle type requires sanding prevention information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasSchematicOverviewOPDigitalForm a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:hasSchematicOverviewOPDigitalForm ;
    era:rinfIndex "1.2.0.0.0.7" ;
    era:scope "local" ;

    rdfs:comment "The existence of a schematic overview of the operational point in digital form."@en ;
    # Source: ontology comment for hasSchematicOverviewOPDigitalForm

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for hasSchematicOverviewOPDigitalForm

    sh:message "hasSchematicOverviewOPDigitalForm: The OperationalPoint must have at most one hasSchematicOverviewOPDigitalForm value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasSchematicOverviewOPDigitalForm ;

    sh:severity sh:Violation .

era-sh:HasSchematicOverviewOPDigitalFormApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:hasSchematicOverviewOPDigitalForm ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the operational point is defined and requires information on the existence of a schematic overview in digital form."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "hasSchematicOverviewOPDigitalForm: This error is due to the OperationalPoint {?opLabel}, violating the rule: The hasSchematicOverviewOPDigitalForm must be defined only when the operational point requires this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?opLabel WHERE {
        $this rdfs:label ?opLabel .
        FILTER NOT EXISTS { $this era:hasSchematicOverviewOPDigitalForm ?value } .
        # Additional condition to check if the operational point requires schematic overview information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasSevereWeatherConditions a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasSevereWeatherConditions ;
    era:rinfIndex "1.1.1.1.2.8" ;
    era:scope "local" ;

    rdfs:comment "Climatic conditions on the line are severe according to European standard."@en ;
    # Source: ontology comment for hasSevereWeatherConditions

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for hasSevereWeatherConditions

    sh:message "hasSevereWeatherConditions: The CommonCharacteristicsSubset must have at most one hasSevereWeatherConditions value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasSevereWeatherConditions ;

    sh:severity sh:Violation .

era-sh:HasSevereWeatherConditionsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasSevereWeatherConditions ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the section of line is defined and requires information on the existence of severe climatic conditions."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "hasSevereWeatherConditions: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The hasSevereWeatherConditions must be defined only when the section of line requires this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:hasSevereWeatherConditions ?value } .
        # Additional condition to check if the section of line requires severe weather conditions information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasShuntingRestrictions a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasShuntingRestrictions ;
    era:rinfIndex "4.8.3" ;
    era:scope "local" ;

    rdfs:comment "Indicates the presence of shunting restrictions."@en ;
    # Source: ontology comment for hasShuntingRestrictions

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for shunting restrictions

    sh:message "hasShuntingRestrictions: The VehicleType must have at most one hasShuntingRestrictions value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasShuntingRestrictions ;

    sh:severity sh:Violation .

era-sh:HasShuntingRestrictionsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasShuntingRestrictions ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the vehicle type is defined and requires information on the presence of shunting restrictions."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "hasShuntingRestrictions: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The hasShuntingRestrictions must be defined only when the vehicle type requires this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:hasShuntingRestrictions ?value } .
        # Additional condition to check if the vehicle type requires shunting restrictions information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasSystemSeparation a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasSystemSeparation ;
    era:rinfIndex "1.1.1.2.4.2.1" ;
    era:scope "local" ;

    rdfs:comment "Indication of existence of system separation."@en ;
    # Source: ontology comment for hasSystemSeparation

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for system separation

    sh:message "hasSystemSeparation: The CommonCharacteristicsSubset must have at most one hasSystemSeparation value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasSystemSeparation ;

    sh:severity sh:Violation .

era-sh:HasSystemSeparationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasSystemSeparation ;
    era:scope "local" ;

    rdfs:comment "This parameter is applicable only if 'Overhead contact line (OCL)' is selected in 1.1.1.2.2.1."@en ;
    # Source: applicability explanation in RAG

    sh:message "hasSystemSeparation: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The hasSystemSeparation must be defined only when 'Overhead contact line (OCL)' is selected."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:hasSystemSeparation ?value } .
        # Additional condition to check if 'Overhead contact line (OCL)' is selected can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasTSITrainDetection a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:hasTSITrainDetection ; 
    era:rinfIndex "1.1.1.3.4.1" ; 
    era:scope "local" ; 

    rdfs:comment "Indication if there is any train detection system installed and fully compliant with the TSI CCS."@en ; 
    # Source: ontology comment for hasTSITrainDetection

    sh:datatype xsd:boolean ; 
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ; 
    # Inferred: expected at most one value for TSI train detection compliance

    sh:message "hasTSITrainDetection: The CommonCharacteristicsSubset must have at most one hasTSITrainDetection value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ; 

    sh:path era:hasTSITrainDetection ; 

    sh:severity sh:Violation .

era-sh:HasTSITrainDetectionApplicability a sh:SPARQLConstraint ; 
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:hasTSITrainDetection ; 
    era:scope "local" ; 

    rdfs:comment "This parameter is applicable only if the train detection system is required as per TSI compliance."@en ; 
    # Source: inferred from the context of the ontology and RAG

    sh:message "hasTSITrainDetection: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The hasTSITrainDetection must be defined only when a train detection system is required."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:hasTSITrainDetection ?value } .
        # Additional condition to check if a train detection system is required can be added here if data is available
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:HasToiletDischarge a sh:PropertyShape ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:hasToiletDischarge ;
    era:rinfIndex "1.2.2.0.4.1" ;
    era:scope "local" ;

    rdfs:comment "Indication whether exists an installation for toilet discharge (fixed installation for servicing trains) as defined in INF TSIs."@en ;
    # Source: ontology comment for hasToiletDischarge

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for toilet discharge existence

    sh:message "hasToiletDischarge: The Siding must have at most one hasToiletDischarge value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasToiletDischarge ;

    sh:severity sh:Violation .

era-sh:HasToiletDischargeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:hasToiletDischarge ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the siding is defined and requires information on the existence of toilet discharge facilities."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "hasToiletDischarge: This error is due to the Siding {?sidingLabel}, violating the rule: The hasToiletDischarge must be defined only when the siding requires this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sidingLabel WHERE {
        $this rdfs:label ?sidingLabel .
        FILTER NOT EXISTS { $this era:hasToiletDischarge ?value } .
        # Additional condition to check if the siding requires toilet discharge information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasTrainIntegrityConfirmation a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasTrainIntegrityConfirmation ;
    era:rinfIndex "4.13.1.9" ;
    era:scope "local" ;

    rdfs:comment "Indication of on-board management system about completeness of the train information."@en ;
    # Source: ontology comment for hasTrainIntegrityConfirmation

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for train integrity confirmation

    sh:message "hasTrainIntegrityConfirmation: The VehicleType must have at most one hasTrainIntegrityConfirmation value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasTrainIntegrityConfirmation ;

    sh:severity sh:Violation .

era-sh:HasTrainIntegrityConfirmationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasTrainIntegrityConfirmation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the selected value for parameter 1.1.1.3.2.1 is ‘3’."@en ;
    # Source: applicability explanation in RAG

    sh:message "hasTrainIntegrityConfirmation: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The hasTrainIntegrityConfirmation must be defined only when the selected value for parameter 1.1.1.3.2.1 is ‘3’."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:hasTrainIntegrityConfirmation ?value } .
        # Additional condition to check if the selected value for parameter 1.1.1.3.2.1 is ‘3’ can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasWalkway a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:hasWalkway ;
    era:rinfIndex "1.1.1.1.8.12" ;
    era:scope "local" ;

    rdfs:comment "Indication of existence of walkways. Mandatory for TSI-compliant tunnels longer than 0.5 km."@en ;
    # Source: ontology comment for hasWalkway and dependency note

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for walkway existence

    sh:message "hasWalkway: The Tunnel must have at most one hasWalkway value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasWalkway ;

    sh:severity sh:Violation .

era-sh:HasWalkwayApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:hasWalkway ;
    era:scope "local" ;

    rdfs:comment "Mandatory for TSI-compliant tunnels longer than 0.5 km."@en ;
    # Source: dependency note in ontology

    sh:message "hasWalkway: This error is due to the Tunnel {?tunnelLabel}, violating the rule: The hasWalkway must be defined only for TSI-compliant tunnels longer than 0.5 km."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:hasWalkway ?value } .
        # Additional condition to check if the tunnel is longer than 0.5 km and TSI-compliant can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasWaterRestocking a sh:PropertyShape ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:hasWaterRestocking ;
    era:rinfIndex "1.2.2.0.4.3" ;
    era:scope "local" ;

    rdfs:comment "Indication whether exists an installation for water restocking (fixed installation for servicing trains) as defined in INF TSIs."@en ;
    # Source: ontology comment for hasWaterRestocking

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for water restocking

    sh:message "hasWaterRestocking: The Siding must have at most one hasWaterRestocking value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasWaterRestocking ;

    sh:severity sh:Violation .

era-sh:HasWaterRestockingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:hasWaterRestocking ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the siding is defined and requires information on the existence of water restocking facilities."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "hasWaterRestocking: This error is due to the Siding {?sidingLabel}, violating the rule: The hasWaterRestocking must be defined only when the siding requires this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sidingLabel WHERE {
        $this rdfs:label ?sidingLabel .
        FILTER NOT EXISTS { $this era:hasWaterRestocking ?value } .
        # Additional condition to check if the siding requires water restocking information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasWheelSlideProtectionSystem a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasWheelSlideProtectionSystem ;
    era:rinfIndex "4.7.8" ;
    era:scope "local" ;

    rdfs:comment "Indicates the presence of a wheel slide protection system."@en ;
    # Source: ontology comment for hasWheelSlideProtectionSystem

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for wheel slide protection system

    sh:message "hasWheelSlideProtectionSystem: The VehicleType must have at most one hasWheelSlideProtectionSystem value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hasWheelSlideProtectionSystem ;

    sh:severity sh:Violation .

era-sh:HasWheelSlideProtectionSystemApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasWheelSlideProtectionSystem ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the vehicle type is defined and requires information on the presence of a wheel slide protection system."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "hasWheelSlideProtectionSystem: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The hasWheelSlideProtectionSystem must be defined only when the vehicle type requires this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:hasWheelSlideProtectionSystem ?value } .
        # Additional condition to check if the vehicle type requires wheel slide protection system information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HighSpeedLoadModelCompliance a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:highSpeedLoadModelCompliance ;
    era:rinfIndex "1.1.1.1.2.4.2" ;
    era:scope "local" ;

    rdfs:comment "For sections of line with a maximum permitted speed of 200 km/h or more. Information regarding the procedure to be used to perform the dynamic compatibility check."@en ;
    # Source: ontology comment for highSpeedLoadModelCompliance

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for highSpeedLoadModelCompliance

    sh:message "highSpeedLoadModelCompliance: The RunningTrack must have at most one highSpeedLoadModelCompliance value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:highSpeedLoadModelCompliance ;

    sh:severity sh:Violation .

era-sh:HighSpeedLoadModelComplianceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:highSpeedLoadModelCompliance ;
    era:scope "local" ;

    rdfs:comment "Applicable if the maximum permitted speed of the running track is more than 200 km/h and the structures within the section of line are all compatible with the High Speed Load Model (HSLM); information regarding the procedure to be used to perform the dynamic compatibility check shall be provided as well."@en ;
    # Source: applicability explanation in RAG

    sh:message "highSpeedLoadModelCompliance: This error is due to the RunningTrack {?trackLabel}, violating the rule: The highSpeedLoadModelCompliance must be defined only when the maximum permitted speed is greater than 200 km/h."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:highSpeedLoadModelCompliance ?value } .
        # Additional condition to check if the maximum permitted speed is greater than 200 km/h can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HotAxleBoxDetectorGeneration a sh:PropertyShape ;
    era:affectedClass era:HABD ;
    era:affectedProperty era:hotAxleBoxDetectorGeneration ;
    era:rinfIndex "1.1.1.1.7.7" ;
    era:scope "local" ;

    rdfs:comment "Specific for the French, Italian and Swedish networks. Generation of trackside hot axle box detector."@en ;
    # Source: ontology comment for hotAxleBoxDetectorGeneration

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for hot axle box detector generation

    sh:message "hotAxleBoxDetectorGeneration: The HABD must have at most one hotAxleBoxDetectorGeneration value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:hotAxleBoxDetectorGeneration ;

    sh:severity sh:Violation .

era-sh:HotAxleBoxDetectorGenerationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:HABD ;
    era:affectedProperty era:hotAxleBoxDetectorGeneration ;
    era:scope "local" ;

    rdfs:comment "Applicable if the answer to parameter 1.1.1.1.7.5 is 'N'."@en ;
    # Source: applicability explanation in RAG

    sh:message "hotAxleBoxDetectorGeneration: This error is due to the HABD {?habdLabel}, violating the rule: The hotAxleBoxDetectorGeneration must be defined only when the answer to parameter 1.1.1.1.7.5 is 'N'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?habdLabel WHERE {
        $this rdfs:label ?habdLabel .
        FILTER NOT EXISTS { $this era:hotAxleBoxDetectorGeneration ?value } .
        # Additional condition to check if the answer to parameter 1.1.1.1.7.5 is 'N' can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HotAxleBoxDetectorIdentification a sh:PropertyShape ;
    era:affectedClass era:HABD ;
    era:affectedProperty era:hotAxleBoxDetectorIdentification ;
    era:rinfIndex "1.1.1.1.7.6" ;
    era:scope "local" ;

    rdfs:comment "Specific for the French, Italian and Swedish networks. Applicable if trackside HABD is not TSI compliant, identification of trackside hot axle box detector."@en ;
    # Source: ontology comment for hotAxleBoxDetectorIdentification

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for hot axle box detector identification

    sh:message "hotAxleBoxDetectorIdentification: The HABD must have at most one hotAxleBoxDetectorIdentification value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:hotAxleBoxDetectorIdentification ;

    sh:severity sh:Violation .

era-sh:HotAxleBoxDetectorIdentificationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:HABD ;
    era:affectedProperty era:hotAxleBoxDetectorIdentification ;
    era:scope "local" ;

    rdfs:comment "Applicable if the answer to parameter 1.1.1.1.7.5 is 'N'."@en ;
    # Source: applicability explanation in RAG

    sh:message "hotAxleBoxDetectorIdentification: This error is due to the HABD {?habdLabel}, violating the rule: The hotAxleBoxDetectorIdentification must be defined only when the answer to parameter 1.1.1.1.7.5 is 'N'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?habdLabel WHERE {
        $this rdfs:label ?habdLabel .
        FILTER NOT EXISTS { $this era:hotAxleBoxDetectorIdentification ?value } .
        # Additional condition to check if the answer to parameter 1.1.1.1.7.5 is 'N' can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HotAxleBoxDetectorLocation a sh:PropertyShape ;
    era:affectedClass era:HABD ;
    era:affectedProperty era:hotAxleBoxDetectorLocation ;
    era:rinfIndex "1.1.1.1.7.8" ;
    era:scope "local" ;

    rdfs:comment "Specific for the French, Italian and Swedish networks. Applicable if trackside HABD is not TSI compliant, localisation of trackside hot axle box detector."@en ;
    # Source: ontology comment for hotAxleBoxDetectorLocation

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for hot axle box detector location

    sh:message "hotAxleBoxDetectorLocation: The HABD must have at most one hotAxleBoxDetectorLocation value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:hotAxleBoxDetectorLocation ;

    sh:severity sh:Violation .

era-sh:HotAxleBoxDetectorLocationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:HABD ;
    era:affectedProperty era:hotAxleBoxDetectorLocation ;
    era:scope "local" ;

    rdfs:comment "Applicable if the answer to parameter 1.1.1.1.7.5 is 'N'."@en ;
    # Source: applicability explanation in RAG

    sh:message "hotAxleBoxDetectorLocation: This error is due to the HABD {?habdLabel}, violating the rule: The hotAxleBoxDetectorLocation must be defined only when the answer to parameter 1.1.1.1.7.5 is 'N'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?habdLabel WHERE {
        $this rdfs:label ?habdLabel .
        FILTER NOT EXISTS { $this era:hotAxleBoxDetectorLocation ?value } .
        # Additional condition to check if the answer to parameter 1.1.1.1.7.5 is 'N' can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HotAxleBoxDetectorTSICompliant a sh:PropertyShape ;
    era:affectedClass era:HABD ;
    era:affectedProperty era:hotAxleBoxDetectorTSICompliant ;
    era:rinfIndex "1.1.1.1.7.5" ;
    era:scope "local" ;

    rdfs:comment "Specific for the French, Italian and Swedish networks. Trackside HABD compliant to TSI means that the HABD Trackside is compliant with EN 15437-1:2009 referred in TSIs."@en ;
    # Source: ontology comment for hotAxleBoxDetectorTSICompliant

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for TSI compliance

    sh:message "hotAxleBoxDetectorTSICompliant: The HABD must have at most one hotAxleBoxDetectorTSICompliant value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:hotAxleBoxDetectorTSICompliant ;

    sh:severity sh:Violation .

era-sh:HotAxleBoxDetectorTSICompliantApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:HABD ;
    era:affectedProperty era:hotAxleBoxDetectorTSICompliant ;
    era:scope "local" ;

    rdfs:comment "Applicable if the trackside HABD is required to be compliant with TSI."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "hotAxleBoxDetectorTSICompliant: This error is due to the HABD {?habdLabel}, violating the rule: The hotAxleBoxDetectorTSICompliant must be defined only when TSI compliance is required."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?habdLabel WHERE {
        $this rdfs:label ?habdLabel .
        FILTER NOT EXISTS { $this era:hotAxleBoxDetectorTSICompliant ?value } .
        # Additional condition to check if TSI compliance is required can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:IMCode a sh:PropertyShape ;
    era:affectedClass era:OrganisationRole ;
    era:affectedProperty era:imCode ;
    era:rinfIndex "1.2.2.0.5.1" ;
    era:scope "local" ;

    rdfs:comment "Infrastructure Manager means any body or undertaking that is responsible in particular for establishing and maintaining railway infrastructure or a part thereof."@en ;
    # Source: ontology comment for imCode

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for IM code

    sh:pattern "^[A-Z]{4}$" ;
    # Inferred: based on RAG data presentation format [AAAA] indicating a four-letter code

    sh:message "imCode: The OrganisationRole must have at most one imCode value that is a string matching the pattern of four uppercase letters. This error may be due to having more than one value or having a value that does not conform to the expected format."@en ;

    sh:path era:imCode ;

    sh:severity sh:Violation .

era-sh:IMCodeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OrganisationRole ;
    era:affectedProperty era:imCode ;
    era:scope "local" ;

    rdfs:comment "Mandatory for each OrganisationRole that represents an Infrastructure Manager."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "imCode: This error is due to the OrganisationRole {?roleLabel}, violating the rule: The imCode must be defined for each Infrastructure Manager."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?roleLabel WHERE {
        $this rdfs:label ?roleLabel .
        FILTER NOT EXISTS { $this era:imCode ?value } .
        # Additional condition to check if the OrganisationRole represents an Infrastructure Manager can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:IsOnOriginOfElementA a sh:PropertyShape ;
    era:affectedClass era:NetRelation ;
    era:affectedProperty era:isOnOriginOfElementA ;
    era:rinfIndex "1.1.1.0.1.2" ;
    era:scope "local" ;

    rdfs:comment "Determines if the position of the topological element A in a relation is the origin of element A."@en ;
    # Source: ontology comment for isOnOriginOfElementA

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for the origin status

    sh:message "isOnOriginOfElementA: The NetRelation must have at most one isOnOriginOfElementA value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:isOnOriginOfElementA ;

    sh:severity sh:Violation .

era-sh:IsOnOriginOfElementAApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:NetRelation ;
    era:affectedProperty era:isOnOriginOfElementA ;
    era:scope "local" ;

    rdfs:comment "Applicable when the relation specifies the origin of element A."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "isOnOriginOfElementA: This error is due to the NetRelation {?relationLabel}, violating the rule: The isOnOriginOfElementA must be defined when the relation specifies the origin of element A."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?relationLabel WHERE {
        $this rdfs:label ?relationLabel .
        FILTER NOT EXISTS { $this era:isOnOriginOfElementA ?value } .
        # Additional condition to check if the relation specifies the origin of element A can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:IsOnOriginOfElementB a sh:PropertyShape ;
    era:affectedClass era:NetRelation ;
    era:affectedProperty era:isOnOriginOfElementB ;
    era:rinfIndex "1.1.1.0.1.2" ;
    era:scope "local" ;

    rdfs:comment "Determines if the position of the topological element B in a relation is the origin of element B."@en ;
    # Source: ontology comment for isOnOriginOfElementB

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for the origin status

    sh:message "isOnOriginOfElementB: The NetRelation must have at most one isOnOriginOfElementB value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:isOnOriginOfElementB ;

    sh:severity sh:Violation .

era-sh:IsOnOriginOfElementBApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:NetRelation ;
    era:affectedProperty era:isOnOriginOfElementB ;
    era:scope "local" ;

    rdfs:comment "Applicable when the relation specifies the origin of element B."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "isOnOriginOfElementB: This error is due to the NetRelation {?relationLabel}, violating the rule: The isOnOriginOfElementB must be defined when the relation specifies the origin of element B."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?relationLabel WHERE {
        $this rdfs:label ?relationLabel .
        FILTER NOT EXISTS { $this era:isOnOriginOfElementB ?value } .
        # Additional condition to check if the relation specifies the origin of element B can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:IsQuietRoute a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:isQuietRoute ;
    era:rinfIndex "1.1.1.1.7.11" ;
    era:scope "local" ;

    rdfs:comment "Belonging to a 'quieter route' in accordance with Article 5b of TSI NOI."@en ;
    # Source: ontology comment for isQuietRoute

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for isQuietRoute

    sh:message "isQuietRoute: The CommonCharacteristicsSubset must have at most one isQuietRoute value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:isQuietRoute ;

    sh:severity sh:Violation .

era-sh:IsQuietRouteApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:isQuietRoute ;
    era:scope "local" ;

    rdfs:comment "Mandatory when the CommonCharacteristicsSubset is defined and requires information on the existence of a quieter route."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "isQuietRoute: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The isQuietRoute must be defined only when the subset requires this information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:isQuietRoute ?value } .
        # Additional condition to check if the CommonCharacteristicsSubset requires isQuietRoute information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Kilometer a sh:PropertyShape ;
    era:affectedClass era:KilometricPost ;
    era:affectedProperty era:kilometer ;
    era:rinfIndex "1.2.0.0.0.6" ;
    era:scope "local" ;

    rdfs:comment "Kilometer number of the kilometric post related to line identification defining the location of an infrastructure element."@en ;
    # Source: ontology comment for kilometer

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for kilometer

    sh:message "kilometer: The KilometricPost must have at most one kilometer value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:kilometer ;

    sh:severity sh:Violation .

era-sh:KilometerApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:KilometricPost ;
    era:affectedProperty era:kilometer ;
    era:scope "local" ;

    rdfs:comment "Applicable when the kilometric post is defined and requires information on the kilometer value."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "kilometer: This error is due to the KilometricPost {?postLabel}, violating the rule: The kilometer must be defined for each KilometricPost."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?postLabel WHERE {
        $this rdfs:label ?postLabel .
        FILTER NOT EXISTS { $this era:kilometer ?value } .
        # Additional condition to check if the KilometricPost requires kilometer information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:KmPostName a sh:PropertyShape ;
    era:affectedClass era:KilometricPost ;
    era:affectedProperty era:kmPostName ;
    era:scope "local" ;

    rdfs:comment "Name of the kilometric post (optional)."@en ;
    # Source: ontology comment for kmPostName

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for kmPostName

    sh:message "kmPostName: The KilometricPost must have at most one kmPostName value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:kmPostName ;

    sh:severity sh:Violation .

era-sh:KmPostNameApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:KilometricPost ;
    era:affectedProperty era:kmPostName ;
    era:scope "local" ;

    rdfs:comment "Applicable when the KilometricPost is defined and requires information on the kmPostName."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "kmPostName: This error is due to the KilometricPost {?postLabel}, violating the rule: The kmPostName must be defined for each KilometricPost."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?postLabel WHERE {
        $this rdfs:label ?postLabel .
        FILTER NOT EXISTS { $this era:kmPostName ?value } .
        # Additional condition to check if the KilometricPost requires kmPostName information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LengthOfNetLinearElement a sh:PropertyShape ;
    era:affectedClass era:LinearElement ;
    era:affectedProperty era:lengthOfNetLinearElement ;
    era:scope "local" ;

    rdfs:comment "Length of a topological linear element."@en ;
    # Source: ontology comment for lengthOfNetLinearElement

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for length of net linear element

    sh:message "lengthOfNetLinearElement: The LinearElement must have at most one lengthOfNetLinearElement value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:lengthOfNetLinearElement ;

    sh:severity sh:Violation .

era-sh:LengthOfNetLinearElementApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LinearElement ;
    era:affectedProperty era:lengthOfNetLinearElement ;
    era:scope "local" ;

    rdfs:comment "Applicable when the LinearElement is defined and requires information on the length of the net linear element."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "lengthOfNetLinearElement: This error is due to the LinearElement {?elementLabel}, violating the rule: The lengthOfNetLinearElement must be defined for each LinearElement."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?elementLabel WHERE {
        $this rdfs:label ?elementLabel .
        FILTER NOT EXISTS { $this era:lengthOfNetLinearElement ?value } .
        # Additional condition to check if the LinearElement requires lengthOfNetLinearElement information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LengthOfPlatform a sh:PropertyShape ;
    era:affectedClass era:PlatformEdge ;
    era:affectedProperty era:lengthOfPlatform ;
    era:rinfIndex "1.2.1.0.6.4" ;
    era:scope "local" ;

    rdfs:comment "The maximum continuous length (expressed in metres) of that part of platform in front of which a train is intended to remain stationary in normal operating conditions for passengers to board and alight from the train, making appropriate allowance for stopping tolerances."@en ;
    # Source: ontology comment for lengthOfPlatform

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for length of platform

    sh:message "lengthOfPlatform: The PlatformEdge must have at most one lengthOfPlatform value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:lengthOfPlatform ;

    sh:severity sh:Violation .

era-sh:LengthOfPlatformApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PlatformEdge ;
    era:affectedProperty era:lengthOfPlatform ;
    era:scope "local" ;

    rdfs:comment "Applicable when the platform edge is defined and requires information on the usable length of the platform."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "lengthOfPlatform: This error is due to the PlatformEdge {?platformLabel}, violating the rule: The lengthOfPlatform must be defined for each PlatformEdge."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?platformLabel WHERE {
        $this rdfs:label ?platformLabel .
        FILTER NOT EXISTS { $this era:lengthOfPlatform ?value } .
        # Additional condition to check if the PlatformEdge requires lengthOfPlatform information can be added here if data is available
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LengthOfSectionOfLine a sh:PropertyShape ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:lengthOfSectionOfLine ;
    era:rinfIndex "1.1.0.0.0.5" ;
    era:scope "local" ;

    rdfs:comment "Length between operational points at start and end of section of line."@en ;
    # Source: ontology comment for lengthOfSectionOfLine

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for length of section of line

    sh:pattern "^[0-9]+(\\.[0-9]{1,3})?$" ;
    # Inferred: based on RAG data presentation format [NNNN.NNN] indicating a numeric value with up to three decimal places

    sh:message "lengthOfSectionOfLine: The SectionOfLine must have at most one lengthOfSectionOfLine value that is a double. This error may be due to having more than one value or having a value that does not conform to the expected format."@en ;

    sh:path era:lengthOfSectionOfLine ;

    sh:severity sh:Violation .

era-sh:LengthOfSectionOfLineApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:lengthOfSectionOfLine ;
    era:scope "local" ;

    rdfs:comment "Mandatory for each SectionOfLine that represents the length between operational points."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "lengthOfSectionOfLine: This error is due to the SectionOfLine {?sectionLabel}, violating the rule: The lengthOfSectionOfLine must be defined for each SectionOfLine."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sectionLabel WHERE {
        $this rdfs:label ?sectionLabel .
        FILTER NOT EXISTS { $this era:lengthOfSectionOfLine ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LengthOfSiding a sh:PropertyShape ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:lengthOfSiding ;
    era:rinfIndex "1.2.2.0.2.1" ;
    era:scope "local" ;

    rdfs:comment "Total length of the siding/stabling track expressed in metres where trains can be parked safely."@en ;
    # Source: ontology comment for lengthOfSiding

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for length of siding

    sh:message "lengthOfSiding: The Siding must have at most one lengthOfSiding value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:lengthOfSiding ;

    sh:severity sh:Violation .

era-sh:LengthOfSidingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:lengthOfSiding ;
    era:scope "local" ;

    rdfs:comment "Applicable when the siding is defined and requires information on the usable length of the siding."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "lengthOfSiding: This error is due to the Siding {?sidingLabel}, violating the rule: The lengthOfSiding must be defined for each Siding."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sidingLabel WHERE {
        $this rdfs:label ?sidingLabel .
        FILTER NOT EXISTS { $this era:lengthOfSiding ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LengthOfTunnel a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:lengthOfTunnel ;
    era:rinfIndex "1.1.1.1.8.7" ;
    era:scope "local" ;

    rdfs:comment "Length of a tunnel in metres from entrance portal to exit portal."@en ;
    # Source: ontology comment for lengthOfTunnel

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for length of tunnel

    sh:message "lengthOfTunnel: The Tunnel must have at most one lengthOfTunnel value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:lengthOfTunnel ;

    sh:severity sh:Violation .

era-sh:LengthOfTunnelApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:lengthOfTunnel ;
    era:scope "local" ;

    rdfs:comment "Applicable only for a tunnel with length of 100 metres or more."@en ;
    # Source: dependencyNote in ontology

    sh:message "lengthOfTunnel: This error is due to the Tunnel {?tunnelLabel}, violating the rule: The lengthOfTunnel must be defined only for tunnels that are 100 metres or longer."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:lengthOfTunnel ?value } .
        FILTER (xsd:double(?value) < 100) .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LengthOfVehicle a sh:PropertyShape ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:lengthOfVehicle ;
    era:rinfIndex "4.8.1" ;
    era:scope "local" ;

    rdfs:comment "Part of the combination of maximum speed and maximum cant deficiency for which the vehicle was assessed. Corresponds to the maximum length of the vehicle."@en ;
    # Source: ontology comment for lengthOfVehicle

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for length of vehicle

    sh:message "lengthOfVehicle: The Vehicle must have at most one lengthOfVehicle value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:lengthOfVehicle ;

    sh:severity sh:Violation .

era-sh:LengthOfVehicleApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:lengthOfVehicle ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Vehicle is defined and requires information on the length of the vehicle."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "lengthOfVehicle: This error is due to the Vehicle {?vehicleLabel}, violating the rule: The lengthOfVehicle must be defined for each Vehicle."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:lengthOfVehicle ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LetterMarking a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:letterMarking ;
    era:rinfIndex "4.1.7" ;
    era:scope "local" ;

    rdfs:comment "Letter marking associated with the vehicle type."@en ;
    # Source: ontology comment for letterMarking

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for letter marking

    sh:message "letterMarking: The VehicleType must have at most one letterMarking value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:letterMarking ;

    sh:severity sh:Violation .

era-sh:LetterMarkingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:letterMarking ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the letter marking."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "letterMarking: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The letterMarking must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:letterMarking ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LineId a sh:PropertyShape ;
    era:affectedClass era:LinearPositioningSystem ;
    era:affectedProperty era:lineId ;
    era:rinfIndex "1.1.0.0.0.2" ;
    era:scope "local" ;

    rdfs:comment "Unique line identification or unique line number within Member State."@en ;
    # Source: ontology comment for lineId

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for lineId

    sh:message "lineId: The LinearPositioningSystem must have at most one lineId value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:lineId ;

    sh:severity sh:Violation .

era-sh:LineIdApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LinearPositioningSystem ;
    era:affectedProperty era:lineId ;
    era:scope "local" ;

    rdfs:comment "Mandatory for each LinearPositioningSystem that requires a unique line identification."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "lineId: This error is due to the LinearPositioningSystem {?systemLabel}, violating the rule: The lineId must be defined for each LinearPositioningSystem."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:lineId ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LinesideDistanceIndicationFrequency a sh:PropertyShape ;
    era:affectedClass era:LinesideDistanceIndication ;
    era:affectedProperty era:linesideDistanceIndicationFrequency ;
    era:scope "local" ;

    rdfs:comment "Frequency of track lineside distance indications."@en ;
    # Source: ontology comment for linesideDistanceIndicationFrequency

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for frequency

    sh:message "linesideDistanceIndicationFrequency: The LinesideDistanceIndication must have at most one linesideDistanceIndicationFrequency value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:linesideDistanceIndicationFrequency ;

    sh:severity sh:Violation .

era-sh:LinesideDistanceIndicationFrequencyApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LinesideDistanceIndication ;
    era:affectedProperty era:linesideDistanceIndicationFrequency ;
    era:scope "local" ;

    rdfs:comment "Applicable when the LinesideDistanceIndication is defined and requires information on the frequency of distance indications."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "linesideDistanceIndicationFrequency: This error is due to the LinesideDistanceIndication {?indicationLabel}, violating the rule: The linesideDistanceIndicationFrequency must be defined for each LinesideDistanceIndication."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?indicationLabel WHERE {
        $this rdfs:label ?indicationLabel .
        FILTER NOT EXISTS { $this era:linesideDistanceIndicationFrequency ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LoadCapabilitySpeed a sh:PropertyShape ;
    era:affectedClass era:LoadCapability ;
    era:affectedProperty era:loadCapabilitySpeed ;
    era:scope "local" ;

    rdfs:comment "Part of the load capability of a track that corresponds to the speed of the load model."@en ;
    # Source: ontology comment for loadCapabilitySpeed

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for loadCapabilitySpeed

    sh:message "loadCapabilitySpeed: The LoadCapability must have at most one loadCapabilitySpeed value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:loadCapabilitySpeed ;

    sh:severity sh:Violation .

era-sh:LoadCapabilitySpeedApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LoadCapability ;
    era:affectedProperty era:loadCapabilitySpeed ;
    era:scope "local" ;

    rdfs:comment "Applicable when the LoadCapability is defined and requires information on the load capability speed."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "loadCapabilitySpeed: This error is due to the LoadCapability {?capabilityLabel}, violating the rule: The loadCapabilitySpeed must be defined for each LoadCapability."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?capabilityLabel WHERE {
        $this rdfs:label ?capabilityLabel .
        FILTER NOT EXISTS { $this era:loadCapabilitySpeed ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LoadingPlatformHeight a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:loadingPlatformHeight ;
    era:rinfIndex "4.8.7" ;
    era:scope "local" ;

    rdfs:comment "Height of loading platform (for flat wagons and combined transport), given in mm."@en ;
    # Source: ontology comment for loadingPlatformHeight

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for loading platform height

    sh:message "loadingPlatformHeight: The VehicleType must have at most one loadingPlatformHeight value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:loadingPlatformHeight ;

    sh:severity sh:Violation .

era-sh:LoadingPlatformHeightApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:loadingPlatformHeight ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the loading platform height."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "loadingPlatformHeight: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The loadingPlatformHeight must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:loadingPlatformHeight ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LocalRulesOrRestrictions a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:localRulesOrRestrictions ; 
    era:rinfIndex "1.1.1.4.1" ; 
    era:scope "local" ;

    rdfs:comment "Existence of rules and restrictions of a strictly local nature."@en ; 
    # Source: ontology comment for localRulesOrRestrictions

    sh:datatype xsd:boolean ; 
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ; 
    # Inferred: expected at most one value for local rules or restrictions

    sh:message "localRulesOrRestrictions: The CommonCharacteristicsSubset must have at most one localRulesOrRestrictions value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:localRulesOrRestrictions ;

    sh:severity sh:Violation .

era-sh:LocalRulesOrRestrictionsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:localRulesOrRestrictions ; 
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires information on local rules or restrictions."@en ; 
    # Source: inferred from the context of the ontology and RAG

    sh:message "localRulesOrRestrictions: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The localRulesOrRestrictions must be defined for each CommonCharacteristicsSubset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:localRulesOrRestrictions ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MNvderun a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:mNvderun ; 
    era:rinfIndex "1.1.1.3.2" ; 
    era:scope "local" ;

    rdfs:comment "Entry of Driver ID permitted while running according to the specification referenced in Appendix A-1, index [C]."@en ;
    # Source: ontology comment for mNvderun

    sh:datatype xsd:boolean ; 
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ; 
    # Inferred: expected at most one value for mNvderun

    sh:message "mNvderun: The CommonCharacteristicsSubset must have at most one mNvderun value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:mNvderun ;

    sh:severity sh:Violation .

era-sh:MNvderunApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:mNvderun ; 
    era:scope "local" ;

    rdfs:comment "Mandatory when ETCS is present and a value has to be provided."@en ;
    # Source: dependencyNote in ontology

    sh:message "mNvderun: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The mNvderun must be defined when ETCS is present."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:mNvderun ?value } .
        # Additional condition to check if ETCS is present can be added here
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MagneticBrakePrevention a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:magneticBrakePrevention ;
    era:rinfIndex "4.7.4.2.2" ;
    era:scope "local" ;

    rdfs:comment "Possibility of preventing the use of the magnetic track brake (only if fitted with magnetic brake)."@en ;
    # Source: ontology comment for magneticBrakePrevention

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for magnetic brake prevention

    sh:message "magneticBrakePrevention: The VehicleType must have at most one magneticBrakePrevention value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:magneticBrakePrevention ;

    sh:severity sh:Violation .

era-sh:MagneticBrakePreventionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:magneticBrakePrevention ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the possibility of preventing the use of magnetic brakes."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "magneticBrakePrevention: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The magneticBrakePrevention must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:magneticBrakePrevention ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MagneticBrakingFitted a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:magneticBrakingFitted ;
    era:rinfIndex "4.7.4.2.1" ;
    era:scope "local" ;

    rdfs:comment "Magnetic track brake fitted. New property defined to distinguish it from magneticBraking which is a RINF SKOS property."@en ;
    # Source: ontology comment for magneticBrakingFitted

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for magneticBrakingFitted

    sh:message "magneticBrakingFitted: The VehicleType must have at most one magneticBrakingFitted value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:magneticBrakingFitted ;

    sh:severity sh:Violation .

era-sh:MagneticBrakingFittedApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:magneticBrakingFitted ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on whether magnetic braking is fitted."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "magneticBrakingFitted: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The magneticBrakingFitted must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:magneticBrakingFitted ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MassPerWheel a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:massPerWheel ;
    era:rinfIndex "4.5.6" ;
    era:scope "local" ;

    rdfs:comment "Mass per wheel, given in kg."@en ;
    # Source: ontology comment for massPerWheel

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for mass per wheel

    sh:message "massPerWheel: The VehicleType must have at most one massPerWheel value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:massPerWheel ;

    sh:severity sh:Violation .

era-sh:MassPerWheelApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:massPerWheel ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the mass per wheel."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "massPerWheel: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The massPerWheel must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:massPerWheel ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaxCurrentStandstillPantograph a sh:PropertyShape ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:maxCurrentStandstillPantograph ;
    era:rinfIndex "1.1.1.2.2.3" ;
    era:scope "local" ;

    rdfs:comment "Indication of the maximum allowable train current at standstill expressed in amperes."@en ;
    # Source: ontology comment for maxCurrentStandstillPantograph

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for maxCurrentStandstillPantograph

    sh:message "maxCurrentStandstillPantograph: The ContactLineSystem must have at most one maxCurrentStandstillPantograph value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:maxCurrentStandstillPantograph ;

    sh:severity sh:Violation .

era-sh:MaxCurrentStandstillPantographApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:maxCurrentStandstillPantograph ;
    era:scope "local" ;

    rdfs:comment "Applicable only if 'Overhead contact line (OCL)' is selected for the contact line system."@en ;
    # Source: dependencyNote in ontology

    sh:message "maxCurrentStandstillPantograph: This error is due to the ContactLineSystem {?systemLabel}, violating the rule: The maxCurrentStandstillPantograph must be defined when the contact line system is 'Overhead contact line (OCL)'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:maxCurrentStandstillPantograph ?value } .
        # Additional condition to check if the contact line system is 'Overhead contact line (OCL)' can be added here
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaxDistConsecutiveAxles a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maxDistConsecutiveAxles ;
    era:rinfIndex "1.1.1.3.7.2.2" ;
    era:scope "local" ;

    rdfs:comment "Indication of maximum permitted distance between two consecutive axles in case of TSI non-compliance, given in millimetres."@en ;
    # Source: ontology comment for maxDistConsecutiveAxles

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for maxDistConsecutiveAxles

    sh:message "maxDistConsecutiveAxles: The VehicleType must have at most one maxDistConsecutiveAxles value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:maxDistConsecutiveAxles ;

    sh:severity sh:Violation .

era-sh:MaxDistConsecutiveAxlesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maxDistConsecutiveAxles ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the maximum permitted distance between two consecutive axles in case of TSI non-compliance."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "maxDistConsecutiveAxles: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The maxDistConsecutiveAxles must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:maxDistConsecutiveAxles ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaxDistEndTrainFirstAxle a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:maxDistEndTrainFirstAxle ;
    era:rinfIndex "1.1.1.3.7.5" ;
    era:scope "local" ;

    rdfs:comment "Indication of maximum distance between end of train and first axle, given in millimetres, applicable for both sides (front and rear) of a vehicle or train."@en ;
    # Source: ontology comment for maxDistEndTrainFirstAxle

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for maxDistEndTrainFirstAxle

    sh:message "maxDistEndTrainFirstAxle: The TrainDetectionSystem must have at most one maxDistEndTrainFirstAxle value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:maxDistEndTrainFirstAxle ;

    sh:severity sh:Violation .

era-sh:MaxDistEndTrainFirstAxleApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:maxDistEndTrainFirstAxle ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the selected option for parameter 1.1.1.3.7.1.1 is 'track circuits' or 'wheel detector'."@en ;
    # Source: RAG explanation on applicability

    sh:message "maxDistEndTrainFirstAxle: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: The maxDistEndTrainFirstAxle must be defined when the detection system is either 'track circuits' or 'wheel detector'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:maxDistEndTrainFirstAxle ?value } .
        # Additional condition to check if the detection type is 'track circuits' or 'wheel detector' can be added here
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaxFlangeHeight a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maxFlangeHeight ;
    era:rinfIndex "4.14.2.9" ;
    era:scope "local" ;

    rdfs:comment "Indication of maximum permitted height of the flange, given in millimetres."@en ;
    # Source: ontology comment for maxFlangeHeight

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for maxFlangeHeight

    sh:message "maxFlangeHeight: The VehicleType must have at most one maxFlangeHeight value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:maxFlangeHeight ;

    sh:severity sh:Violation .

era-sh:MaxFlangeHeightApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maxFlangeHeight ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the maximum permitted height of the flange."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "maxFlangeHeight: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The maxFlangeHeight must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:maxFlangeHeight ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaxImpedanceWheelset a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:maxImpedanceWheelset ;
    era:rinfIndex "4.14.2.16" ;
    era:scope "local" ;

    rdfs:comment "The value of maximum permitted impedance given in ohm in case of TSI non-compliance."@en ;
    # Source: ontology comment for maxImpedanceWheelset

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for maxImpedanceWheelset

    sh:message "maxImpedanceWheelset: The TrainDetectionSystem must have at most one maxImpedanceWheelset value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:maxImpedanceWheelset ;

    sh:severity sh:Violation .

era-sh:MaxImpedanceWheelsetApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:maxImpedanceWheelset ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the selected option for parameter 1.1.1.3.7.15.1 is 'Not TSI compliant'."@en ;
    # Source: RAG explanation on applicability

    sh:message "maxImpedanceWheelset: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: The maxImpedanceWheelset must be defined when the parameter indicates 'Not TSI compliant'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:maxImpedanceWheelset ?value } .
        # Additional condition to check if the parameter indicates 'Not TSI compliant' can be added here
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaxLengthVehicleNose a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maxLengthVehicleNose ;
    era:rinfIndex "4.14.2.4" ;
    era:scope "local" ;

    rdfs:comment "Maximum length of the vehicle nose."@en ;
    # Source: ontology comment for maxLengthVehicleNose

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for maxLengthVehicleNose

    sh:message "maxLengthVehicleNose: The VehicleType must have at most one maxLengthVehicleNose value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:maxLengthVehicleNose ;

    sh:severity sh:Violation .

era-sh:MaxLengthVehicleNoseApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maxLengthVehicleNose ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the maximum length of the vehicle nose."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "maxLengthVehicleNose: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The maxLengthVehicleNose must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:maxLengthVehicleNose ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaxTrainCurrent a sh:PropertyShape ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:maxTrainCurrent ;
    era:rinfIndex "1.1.1.2.2.2" ;
    era:scope "local" ;

    rdfs:comment "Indication of the maximum allowable train current expressed in amperes."@en ;
    # Source: ontology comment for maxTrainCurrent

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for maxTrainCurrent

    sh:message "maxTrainCurrent: The ContactLineSystem must have at most one maxTrainCurrent value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:maxTrainCurrent ;

    sh:severity sh:Violation .

era-sh:MaxTrainCurrentApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:maxTrainCurrent ;
    era:scope "local" ;

    rdfs:comment "Applicable when the system is electrified; if 'not electrified' is chosen in parameter 1.1.1.2.2.1.1, then this parameter is not applicable."@en ;
    # Source: dependencyNote in ontology

    sh:message "maxTrainCurrent: This error is due to the ContactLineSystem {?systemLabel}, violating the rule: The maxTrainCurrent must be defined when the system is electrified."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:maxTrainCurrent ?value } .
        # Additional condition to check if the system is electrified can be added here
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumAltitude a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:maximumAltitude ; 
    era:rinfIndex "1.1.1.1.2.7" ; 
    era:scope "local" ;

    rdfs:comment "Highest point of the section of line above sea level in reference to Normal Amsterdam's Peil (NAP)."@en ;
    # Source: ontology comment for maximumAltitude

    sh:datatype xsd:double ; 
    # Source: ontology range is xsd:double

    sh:maxCount 1 ; 
    # Inferred: expected at most one value for maximum altitude

    sh:message "maximumAltitude: The CommonCharacteristicsSubset must have at most one maximumAltitude value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:maximumAltitude ;

    sh:severity sh:Violation .

era-sh:MaximumAltitudeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:maximumAltitude ; 
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires information on the maximum altitude."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "maximumAltitude: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The maximumAltitude must be defined for each CommonCharacteristicsSubset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:maximumAltitude ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumAverageDeceleration a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumAverageDeceleration ;
    era:rinfIndex "4.7.1" ;
    era:scope "local" ;

    rdfs:comment "Maximum train deceleration given in m/s²."@en ;
    # Source: ontology comment for maximumAverageDeceleration

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for maximum average deceleration

    sh:message "maximumAverageDeceleration: The VehicleType must have at most one maximumAverageDeceleration value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:maximumAverageDeceleration ;

    sh:severity sh:Violation .

era-sh:MaximumAverageDecelerationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumAverageDeceleration ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the maximum average deceleration."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "maximumAverageDeceleration: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The maximumAverageDeceleration must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:maximumAverageDeceleration ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumBrakeThermalEnergyCapacity a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumBrakeThermalEnergyCapacity ;
    era:rinfIndex "4.7.2.1.6" ;
    era:scope "local" ;

    rdfs:comment "Maximum brake thermal energy capacity given in kJ."@en ;
    # Source: ontology comment for maximumBrakeThermalEnergyCapacity

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for maximum brake thermal energy capacity

    sh:message "maximumBrakeThermalEnergyCapacity: The VehicleType must have at most one maximumBrakeThermalEnergyCapacity value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:maximumBrakeThermalEnergyCapacity ;

    sh:severity sh:Violation .

era-sh:MaximumBrakeThermalEnergyCapacityApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumBrakeThermalEnergyCapacity ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the maximum brake thermal energy capacity."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "maximumBrakeThermalEnergyCapacity: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The maximumBrakeThermalEnergyCapacity must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:maximumBrakeThermalEnergyCapacity ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumBrakingDistance a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:maximumBrakingDistance ; 
    era:rinfIndex "1.1.1.3.11.1" ; 
    era:scope "local" ;

    rdfs:comment "The maximum value of the braking distance [in metres] of a train shall be given for the maximum line speed."@en ;
    # Source: ontology comment for maximumBrakingDistance

    sh:datatype xsd:integer ; 
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ; 
    # Inferred: expected at most one value for maximumBrakingDistance

    sh:message "maximumBrakingDistance: The CommonCharacteristicsSubset must have at most one maximumBrakingDistance value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:maximumBrakingDistance ;

    sh:severity sh:Violation .

era-sh:MaximumBrakingDistanceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:maximumBrakingDistance ; 
    era:scope "local" ;

    rdfs:comment "Applicable when the maximum braking distance is defined and requires information on the maximum braking distance for the maximum line speed."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "maximumBrakingDistance: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The maximumBrakingDistance must be defined for each CommonCharacteristicsSubset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:maximumBrakingDistance ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumContactWireHeight a sh:PropertyShape ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:maximumContactWireHeight ;
    era:rinfIndex "1.1.1.2.2.5" ;
    era:scope "local" ;

    rdfs:comment "Indication of the maximum contact wire height expressed in metres."@en ;
    # Source: ontology comment for maximumContactWireHeight

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for maximumContactWireHeight

    sh:message "maximumContactWireHeight: The ContactLineSystem must have at most one maximumContactWireHeight value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:maximumContactWireHeight ;

    sh:severity sh:Violation .

era-sh:MaximumContactWireHeightApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:maximumContactWireHeight ;
    era:scope "local" ;

    rdfs:comment "Applicable only if 'Overhead contact line (OCL)' is selected in parameter 1.1.1.2.2.1.1."@en ;
    # Source: RAG explanation on applicability

    sh:message "maximumContactWireHeight: This error is due to the ContactLineSystem {?systemLabel}, violating the rule: The maximumContactWireHeight must be defined when 'Overhead contact line (OCL)' is selected."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:maximumContactWireHeight ?value } .
        # Additional condition to check if 'Overhead contact line (OCL)' is selected can be added here
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumDesignSpeed a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumDesignSpeed ;
    era:rinfIndex "4.1.2.1" ;
    era:scope "local" ;

    rdfs:comment "Maximum design speed."@en ;
    # Source: ontology comment for maximumDesignSpeed

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for maximumDesignSpeed

    sh:message "maximumDesignSpeed: The VehicleType must have at most one maximumDesignSpeed value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:maximumDesignSpeed ;

    sh:severity sh:Violation .

era-sh:MaximumDesignSpeedApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumDesignSpeed ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the maximum design speed."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "maximumDesignSpeed: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The maximumDesignSpeed must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:maximumDesignSpeed ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumInterferenceCurrent a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:maximumInterferenceCurrent ;
    era:rinfIndex "1.1.1.3.4.2.1" ;
    era:scope "local" ;

    rdfs:comment "Maximum interference current limits allowed for track circuits for a defined frequency band."@en ;
    # Source: ontology comment for maximumInterferenceCurrent

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for maximumInterferenceCurrent

    sh:message "maximumInterferenceCurrent: The TrainDetectionSystem must have at most one maximumInterferenceCurrent value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:maximumInterferenceCurrent ;

    sh:severity sh:Violation .

era-sh:MaximumInterferenceCurrentApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:maximumInterferenceCurrent ;
    era:scope "local" ;

    rdfs:comment "Applicable when the TrainDetectionSystem is defined and requires information on the maximum interference current."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "maximumInterferenceCurrent: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: The maximumInterferenceCurrent must be defined for each TrainDetectionSystem."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:maximumInterferenceCurrent ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumInterferenceCurrentEvaluation a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:maximumInterferenceCurrentEvaluation ;
    era:rinfIndex "1.1.1.3.4.2.1" ;
    era:scope "local" ;

    rdfs:comment "Maximum interference current limits allowed for track circuits for a defined frequency band."@en ;
    # Source: ontology comment for maximumInterferenceCurrentEvaluation

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for maximumInterferenceCurrentEvaluation

    sh:message "maximumInterferenceCurrentEvaluation: The TrainDetectionSystem must have at most one maximumInterferenceCurrentEvaluation value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:maximumInterferenceCurrentEvaluation ;

    sh:severity sh:Violation .

era-sh:MaximumInterferenceCurrentEvaluationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:maximumInterferenceCurrentEvaluation ;
    era:scope "local" ;

    rdfs:comment "Applicable if the preferred frequency bands are not used; a description of the parameters for evaluation of compliance is mandatory."@en ;
    # Source: dependencyNote in ontology

    sh:message "maximumInterferenceCurrentEvaluation: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: The maximumInterferenceCurrentEvaluation must be defined when the preferred frequency bands are not used."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:maximumInterferenceCurrentEvaluation ?value } .
        # Additional condition to check if preferred frequency bands are not used can be added here
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumLocomotivesCoupled a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumLocomotivesCoupled ;
    era:rinfIndex "4.1.5" ;
    era:scope "local" ;

    rdfs:comment "Maximum number of trainsets or locomotives coupled together in multiple operation."@en ;
    # Source: ontology comment for maximumLocomotivesCoupled

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for maximumLocomotivesCoupled

    sh:message "maximumLocomotivesCoupled: The VehicleType must have at most one maximumLocomotivesCoupled value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:maximumLocomotivesCoupled ;

    sh:severity sh:Violation .

era-sh:MaximumLocomotivesCoupledApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumLocomotivesCoupled ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the maximum number of locomotives that can be coupled."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "maximumLocomotivesCoupled: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The maximumLocomotivesCoupled must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:maximumLocomotivesCoupled ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumMagneticFieldDirectionX a sh:PropertyShape ;
    era:affectedClass era:MaximumMagneticField ;
    era:affectedProperty era:maximumMagneticFieldDirectionX ;
    era:rinfIndex "4.6.4" ;
    era:scope "local" ;

    rdfs:comment "The maximum magnetic field limits allowed for axle counters (in dB µA/m) for a defined frequency band. Direction X."@en ;
    # Source: ontology comment for maximumMagneticFieldDirectionX

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for maximumMagneticFieldDirectionX

    sh:message "maximumMagneticFieldDirectionX: The MaximumMagneticField must have at most one maximumMagneticFieldDirectionX value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:maximumMagneticFieldDirectionX ;

    sh:severity sh:Violation .

era-sh:MaximumMagneticFieldDirectionXApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:MaximumMagneticField ;
    era:affectedProperty era:maximumMagneticFieldDirectionX ;
    era:scope "local" ;

    rdfs:comment "Applicable only for axle counters."@en ;
    # Source: scopeNote in ontology

    sh:message "maximumMagneticFieldDirectionX: This error is due to the MaximumMagneticField {?fieldLabel}, violating the rule: The maximumMagneticFieldDirectionX must be defined for each MaximumMagneticField."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?fieldLabel WHERE {
        $this rdfs:label ?fieldLabel .
        FILTER NOT EXISTS { $this era:maximumMagneticFieldDirectionX ?value } .
        # Additional condition to check if the system is an axle counter can be added here
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumMagneticFieldDirectionY a sh:PropertyShape ;
    era:affectedClass era:MaximumMagneticField ;
    era:affectedProperty era:maximumMagneticFieldDirectionY ;
    era:rinfIndex "4.6.4" ;
    era:scope "local" ;

    rdfs:comment "The maximum magnetic field limits allowed for axle counters (in dB µA/m) for a defined frequency band. Direction Y."@en ;
    # Source: ontology comment for maximumMagneticFieldDirectionY

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for maximumMagneticFieldDirectionY

    sh:message "maximumMagneticFieldDirectionY: The MaximumMagneticField must have at most one maximumMagneticFieldDirectionY value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:maximumMagneticFieldDirectionY ;

    sh:severity sh:Violation .

era-sh:MaximumMagneticFieldDirectionYApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:MaximumMagneticField ;
    era:affectedProperty era:maximumMagneticFieldDirectionY ;
    era:scope "local" ;

    rdfs:comment "Applicable only for axle counters."@en ;
    # Source: scopeNote in ontology

    sh:message "maximumMagneticFieldDirectionY: This error is due to the MaximumMagneticField {?fieldLabel}, violating the rule: The maximumMagneticFieldDirectionY must be defined for each MaximumMagneticField."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?fieldLabel WHERE {
        $this rdfs:label ?fieldLabel .
        FILTER NOT EXISTS { $this era:maximumMagneticFieldDirectionY ?value } .
        # Additional condition to check if the system is an axle counter can be added here
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumMagneticFieldDirectionZ a sh:PropertyShape ;
    era:affectedClass era:MaximumMagneticField ;
    era:affectedProperty era:maximumMagneticFieldDirectionZ ;
    era:rinfIndex "4.6.4" ;
    era:scope "local" ;

    rdfs:comment "The maximum magnetic field limits allowed for axle counters (in dB µA/m) for a defined frequency band. Direction Z."@en ;
    # Source: ontology comment for maximumMagneticFieldDirectionZ

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for maximumMagneticFieldDirectionZ

    sh:message "maximumMagneticFieldDirectionZ: The MaximumMagneticField must have at most one maximumMagneticFieldDirectionZ value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:maximumMagneticFieldDirectionZ ;

    sh:severity sh:Violation .

era-sh:MaximumMagneticFieldDirectionZApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:MaximumMagneticField ;
    era:affectedProperty era:maximumMagneticFieldDirectionZ ;
    era:scope "local" ;

    rdfs:comment "Applicable only for axle counters."@en ;
    # Source: scopeNote in ontology

    sh:message "maximumMagneticFieldDirectionZ: This error is due to the MaximumMagneticField {?fieldLabel}, violating the rule: The maximumMagneticFieldDirectionZ must be defined for each MaximumMagneticField."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?fieldLabel WHERE {
        $this rdfs:label ?fieldLabel .
        FILTER NOT EXISTS { $this era:maximumMagneticFieldDirectionZ ?value } .
        # Additional condition to check if the system is an axle counter can be added here
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumPermittedSpeed a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:maximumPermittedSpeed ; 
    era:rinfIndex "1.1.1.1.2.5" ; 
    era:scope "local" ;

    rdfs:comment "Nominal maximum operational speed on the line as a result of INF, ENE and CCS subsystem characteristics expressed in kilometres/hour."@en ;
    # Source: ontology comment for maximumPermittedSpeed

    sh:datatype xsd:integer ; 
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ; 
    # Inferred: expected at most one value for maximumPermittedSpeed

    sh:message "maximumPermittedSpeed: The CommonCharacteristicsSubset must have at most one maximumPermittedSpeed value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:maximumPermittedSpeed ;

    sh:severity sh:Violation .

era-sh:MaximumPermittedSpeedApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:maximumPermittedSpeed ; 
    era:scope "local" ;

    rdfs:comment "Applicable when the maximum permitted speed is defined and requires information on the nominal maximum operational speed."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "maximumPermittedSpeed: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The maximumPermittedSpeed must be defined for each CommonCharacteristicsSubset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:maximumPermittedSpeed ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumServiceBrake a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumServiceBrake ;
    era:rinfIndex "4.7.7" ;
    era:scope "local" ;

    rdfs:comment "At maximum service brake: Stopping distance, Maximum deceleration, for the load condition 'design mass under normal payload' at the design maximum speed."@en ;
    # Source: ontology comment for maximumServiceBrake

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for maximumServiceBrake

    sh:message "maximumServiceBrake: The VehicleType must have at most one maximumServiceBrake value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:maximumServiceBrake ;

    sh:severity sh:Violation .

era-sh:MaximumServiceBrakeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumServiceBrake ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the maximum service brake."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "maximumServiceBrake: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The maximumServiceBrake must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:maximumServiceBrake ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumSpeedAndCantDeficiency a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumSpeedAndCantDeficiency ;
    era:rinfIndex "4.6.4" ;
    era:scope "local" ;

    rdfs:comment "Combination of maximum speed and maximum cant deficiency for which the vehicle was assessed."@en ;
    # Source: ontology comment for maximumSpeedAndCantDeficiency

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for maximumSpeedAndCantDeficiency

    sh:message "maximumSpeedAndCantDeficiency: The VehicleType must have at most one maximumSpeedAndCantDeficiency value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:maximumSpeedAndCantDeficiency ;

    sh:severity sh:Violation .

era-sh:MaximumSpeedAndCantDeficiencyApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumSpeedAndCantDeficiency ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the maximum speed and cant deficiency."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "maximumSpeedAndCantDeficiency: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The maximumSpeedAndCantDeficiency must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:maximumSpeedAndCantDeficiency ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumSpeedEmpty a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumSpeedEmpty ;
    era:rinfIndex "4.1.2.2" ;
    era:scope "local" ;

    rdfs:comment "Maximum speed when empty."@en ;
    # Source: ontology comment for maximumSpeedEmpty

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for maximumSpeedEmpty

    sh:message "maximumSpeedEmpty: The VehicleType must have at most one maximumSpeedEmpty value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:maximumSpeedEmpty ;

    sh:severity sh:Violation .

era-sh:MaximumSpeedEmptyApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumSpeedEmpty ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the maximum speed when empty."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "maximumSpeedEmpty: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The maximumSpeedEmpty must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:maximumSpeedEmpty ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumTrainDeceleration a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:maximumTrainDeceleration ;
    era:rinfIndex "1.1.1.1.6.1" ;
    era:scope "local" ;

    rdfs:comment "Limit for longitudinal track resistance given as a maximum allowed train deceleration and expressed in metres per square second."@en ;
    # Source: ontology comment for maximumTrainDeceleration

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for maximumTrainDeceleration

    sh:message "maximumTrainDeceleration: The RunningTrack must have at most one maximumTrainDeceleration value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:maximumTrainDeceleration ;

    sh:severity sh:Violation .

era-sh:MaximumTrainDecelerationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:maximumTrainDeceleration ;
    era:scope "local" ;

    rdfs:comment "Applicable when the RunningTrack is defined and requires information on the maximum train deceleration."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "maximumTrainDeceleration: This error is due to the RunningTrack {?trackLabel}, violating the rule: The maximumTrainDeceleration must be defined for each RunningTrack."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:maximumTrainDeceleration ?value } .
        # Additional condition to check if the line is in scope of INF TSI can be added here
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MeasuredDistance a sh:PropertyShape ;
    era:affectedClass era:KilometricPost ;
    era:affectedProperty era:measuredDistance ;
    era:scope "local" ;

    rdfs:comment "Measured distance from the origin of the LRS."@en ;
    # Source: ontology comment for measuredDistance

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for measuredDistance

    sh:message "measuredDistance: The KilometricPost must have at most one measuredDistance value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:measuredDistance ;

    sh:severity sh:Violation .

era-sh:MeasuredDistanceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:KilometricPost ;
    era:affectedProperty era:measuredDistance ;
    era:scope "local" ;

    rdfs:comment "Applicable when the KilometricPost is defined and requires information on the measured distance from the origin of the LRS."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "measuredDistance: This error is due to the KilometricPost {?postLabel}, violating the rule: The measuredDistance must be defined for each KilometricPost."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?postLabel WHERE {
        $this rdfs:label ?postLabel .
        FILTER NOT EXISTS { $this era:measuredDistance ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MeetsRequirementVehicleAuthorisation a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:meetsRequirementVehicleAuthorisation ;
    era:rinfIndex "4.1.8" ;
    era:scope "local" ;

    rdfs:comment "Type meets the requirements necessary for validity of the vehicle authorisation granted by one Member State in other MSs."@en ;
    # Source: ontology comment for meetsRequirementVehicleAuthorisation

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for meetsRequirementVehicleAuthorisation

    sh:message "meetsRequirementVehicleAuthorisation: The VehicleType must have at most one meetsRequirementVehicleAuthorisation value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:meetsRequirementVehicleAuthorisation ;

    sh:severity sh:Violation .

era-sh:MeetsRequirementVehicleAuthorisationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:meetsRequirementVehicleAuthorisation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on whether it meets the requirements for vehicle authorisation."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "meetsRequirementVehicleAuthorisation: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The meetsRequirementVehicleAuthorisation must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:meetsRequirementVehicleAuthorisation ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinAxleLoad a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minAxleLoad ;
    era:rinfIndex "4.14.2.10" ;
    era:scope "local" ;

    rdfs:comment "Minimum permitted axle load, given in tons."@en ;
    # Source: ontology comment for minAxleLoad

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for minAxleLoad

    sh:message "minAxleLoad: The VehicleType must have at most one minAxleLoad value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:minAxleLoad ;

    sh:severity sh:Violation .

era-sh:MinAxleLoadApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minAxleLoad ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the minimum permitted axle load."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "minAxleLoad: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The minAxleLoad must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:minAxleLoad ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinDistConsecutiveAxles a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minDistConsecutiveAxles ;
    era:rinfIndex "4.14.2.2" ;
    era:scope "local" ;

    rdfs:comment "Indication of minimum permitted distance between two consecutive axles, given in millimetres."@en ;
    # Source: ontology comment for minDistConsecutiveAxles

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for minDistConsecutiveAxles

    sh:message "minDistConsecutiveAxles: The VehicleType must have at most one minDistConsecutiveAxles value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:minDistConsecutiveAxles ;

    sh:severity sh:Violation .

era-sh:MinDistConsecutiveAxlesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minDistConsecutiveAxles ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the minimum permitted distance between two consecutive axles."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "minDistConsecutiveAxles: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The minDistConsecutiveAxles must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:minDistConsecutiveAxles ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinDistFirstLastAxle a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minDistFirstLastAxle ;
    era:rinfIndex "4.14.2.3" ;
    era:scope "local" ;

    rdfs:comment "Indication of minimum permitted distance between first and last axles, given in millimetres."@en ;
    # Source: ontology comment for minDistFirstLastAxle

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for minDistFirstLastAxle

    sh:message "minDistFirstLastAxle: The VehicleType must have at most one minDistFirstLastAxle value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:minDistFirstLastAxle ;

    sh:severity sh:Violation .

era-sh:MinDistFirstLastAxleApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minDistFirstLastAxle ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the minimum permitted distance between first and last axles."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "minDistFirstLastAxle: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The minDistFirstLastAxle must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:minDistFirstLastAxle ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinFlangeHeight a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minFlangeHeight ;
    era:rinfIndex "1.1.1.3.7.9" ;
    era:scope "local" ;

    rdfs:comment "Indication of height of flange given in millimetres."@en ;
    # Source: ontology comment for minFlangeHeight

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for minFlangeHeight

    sh:message "minFlangeHeight: The VehicleType must have at most one minFlangeHeight value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:minFlangeHeight ;

    sh:severity sh:Violation .

era-sh:MinFlangeHeightApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minFlangeHeight ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the minimum permitted height of the flange."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "minFlangeHeight: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The minFlangeHeight must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:minFlangeHeight ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinFlangeThickness a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minFlangeThickness ;
    era:rinfIndex "4.14.2.7" ;
    era:scope "local" ;

    rdfs:comment "Indication of flange thickness given in millimetres."@en ;
    # Source: ontology comment for minFlangeThickness

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for minFlangeThickness

    sh:message "minFlangeThickness: The VehicleType must have at most one minFlangeThickness value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:minFlangeThickness ;

    sh:severity sh:Violation .

era-sh:MinFlangeThicknessApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minFlangeThickness ;
    era:scope "local" ;

    rdfs:comment "Applicable only when for parameter 1.1.1.3.7.1.1 the selected option is ‘wheel detector’."@en ;
    # Source: RAG explanation on applicability

    sh:message "minFlangeThickness: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The minFlangeThickness must be defined for each VehicleType when the selected option is 'wheel detector'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:minFlangeThickness ?value } .
        # Additional condition to check if the selected option for parameter 1.1.1.3.7.1.1 is 'wheel detector' can be added here
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinRimWidth a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minRimWidth ;
    era:rinfIndex "4.14.2.5" ;
    era:scope "local" ;

    rdfs:comment "Indication of minimum permitted width of the rim given in millimetres."@en ;
    # Source: ontology comment for minRimWidth

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for minRimWidth

    sh:message "minRimWidth: The VehicleType must have at most one minRimWidth value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:minRimWidth ;

    sh:severity sh:Violation .

era-sh:MinRimWidthApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minRimWidth ;
    era:scope "local" ;

    rdfs:comment "Applicable only when for parameter 1.1.1.3.7.1.1 the selected option is ‘wheel detector’."@en ;
    # Source: RAG explanation on applicability

    sh:message "minRimWidth: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The minRimWidth must be defined for each VehicleType when the selected option is 'wheel detector'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:minRimWidth ?value } .
        # Additional condition to check if the selected option for parameter 1.1.1.3.7.1.1 is 'wheel detector' can be added here
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinVehicleInputCapacitance a sh:PropertyShape ;
    era:affectedClass era:MinVehicleImpedance ;
    era:affectedProperty era:minVehicleInputCapacitance ;
    era:rinfIndex "4.6.4" ;
    era:scope "local" ;

    rdfs:comment "Indication of the minimum vehicle input capacitance for the selected DC voltage."@en ;
    # Source: ontology comment for minVehicleInputCapacitance

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for minVehicleInputCapacitance

    sh:message "minVehicleInputCapacitance: The MinVehicleImpedance must have at most one minVehicleInputCapacitance value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:minVehicleInputCapacitance ;

    sh:severity sh:Violation .

era-sh:MinVehicleInputCapacitanceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:MinVehicleImpedance ;
    era:affectedProperty era:minVehicleInputCapacitance ;
    era:scope "local" ;

    rdfs:comment "Applicable when the MinVehicleImpedance is defined and requires information on the minimum vehicle input capacitance."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "minVehicleInputCapacitance: This error is due to the MinVehicleImpedance {?impedanceLabel}, violating the rule: The minVehicleInputCapacitance must be defined for each MinVehicleImpedance."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?impedanceLabel WHERE {
        $this rdfs:label ?impedanceLabel .
        FILTER NOT EXISTS { $this era:minVehicleInputCapacitance ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinVehicleInputImpedance a sh:PropertyShape ;
    era:affectedClass era:MinVehicleImpedance ;
    era:affectedProperty era:minVehicleInputImpedance ;
    era:rinfIndex "4.6.4" ;
    era:scope "local" ;

    rdfs:comment "Indication of the minimum vehicle input impedance for the selected DC voltage."@en ;
    # Source: ontology comment for minVehicleInputImpedance

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for minVehicleInputImpedance

    sh:message "minVehicleInputImpedance: The MinVehicleImpedance must have at most one minVehicleInputImpedance value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:minVehicleInputImpedance ;

    sh:severity sh:Violation .

era-sh:MinVehicleInputImpedanceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:MinVehicleImpedance ;
    era:affectedProperty era:minVehicleInputImpedance ;
    era:scope "local" ;

    rdfs:comment "Applicable when the MinVehicleImpedance is defined and requires information on the minimum vehicle input impedance."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "minVehicleInputImpedance: This error is due to the MinVehicleImpedance {?impedanceLabel}, violating the rule: The minVehicleInputImpedance must be defined for each MinVehicleImpedance."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?impedanceLabel WHERE {
        $this rdfs:label ?impedanceLabel .
        FILTER NOT EXISTS { $this era:minVehicleInputImpedance ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinWheelDiameter a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minWheelDiameter ;
    era:rinfIndex "4.14.2.6" ;
    era:scope "local" ;

    rdfs:comment "Indication of minimum permitted wheel diameter, given in millimetres."@en ;
    # Source: ontology comment for minWheelDiameter

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for minWheelDiameter

    sh:message "minWheelDiameter: The VehicleType must have at most one minWheelDiameter value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:minWheelDiameter ;

    sh:severity sh:Violation .

era-sh:MinWheelDiameterApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minWheelDiameter ;
    era:scope "local" ;

    rdfs:comment "Applicable only when for parameter 1.1.1.3.7.1.1 the selected option is ‘wheel detector’."@en ;
    # Source: RAG explanation on applicability

    sh:message "minWheelDiameter: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The minWheelDiameter must be defined for each VehicleType when the selected option is 'wheel detector'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:minWheelDiameter ?value } .
        # Additional condition to check if the selected option for parameter 1.1.1.3.7.1.1 is 'wheel detector' can be added here
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinimumConcaveVerticalRadius a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minimumConcaveVerticalRadius ;
    era:rinfIndex "4.8.6" ;
    era:scope "local" ;

    rdfs:comment "Minimum vertical concave curve radius capability."@en ;
    # Source: ontology comment for minimumConcaveVerticalRadius

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for minimumConcaveVerticalRadius

    sh:message "minimumConcaveVerticalRadius: The VehicleType must have at most one minimumConcaveVerticalRadius value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:minimumConcaveVerticalRadius ;

    sh:severity sh:Violation .

era-sh:MinimumConcaveVerticalRadiusApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minimumConcaveVerticalRadius ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the minimum concave vertical radius."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "minimumConcaveVerticalRadius: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The minimumConcaveVerticalRadius must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:minimumConcaveVerticalRadius ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinimumContactWireHeight a sh:PropertyShape ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:minimumContactWireHeight ;
    era:rinfIndex "4.10.5" ;
    era:scope "local" ;

    rdfs:comment "Indication of the minimum contact wire height expressed in metres."@en ;
    # Source: ontology comment for minimumContactWireHeight

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for minimumContactWireHeight

    sh:message "minimumContactWireHeight: The ContactLineSystem must have at most one minimumContactWireHeight value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:minimumContactWireHeight ;

    sh:severity sh:Violation .

era-sh:MinimumContactWireHeightApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:minimumContactWireHeight ;
    era:scope "local" ;

    rdfs:comment "Applicable only if 'Overhead contact line (OCL)' is selected in parameter 1.1.1.2.2.1.1."@en ;
    # Source: RAG explanation on applicability

    sh:message "minimumContactWireHeight: This error is due to the ContactLineSystem {?contactLabel}, violating the rule: The minimumContactWireHeight must be defined for each ContactLineSystem when 'Overhead contact line (OCL)' is selected."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?contactLabel WHERE {
        $this rdfs:label ?contactLabel .
        FILTER NOT EXISTS { $this era:minimumContactWireHeight ?value } .
        # Additional condition to check if 'Overhead contact line (OCL)' is selected can be added here
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinimumConvexVerticalRadius a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minimumConvexVerticalRadius ;
    era:rinfIndex "4.8.5" ;
    era:scope "local" ;

    rdfs:comment "Minimum vertical convex curve radius capability."@en ;
    # Source: ontology comment for minimumConvexVerticalRadius

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for minimumConvexVerticalRadius

    sh:message "minimumConvexVerticalRadius: The VehicleType must have at most one minimumConvexVerticalRadius value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:minimumConvexVerticalRadius ;

    sh:severity sh:Violation .

era-sh:MinimumConvexVerticalRadiusApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minimumConvexVerticalRadius ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the minimum convex vertical radius."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "minimumConvexVerticalRadius: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The minimumConvexVerticalRadius must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:minimumConvexVerticalRadius ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinimumHorizontalRadius a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:minimumHorizontalRadius ;
    era:rinfIndex "4.8.4" ;
    era:scope "local" ;

    rdfs:comment "Radius of the smallest horizontal curve, expressed in metres."@en ;
    # Source: ontology comment for minimumHorizontalRadius

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for minimumHorizontalRadius

    sh:message "minimumHorizontalRadius: The RunningTrack must have at most one minimumHorizontalRadius value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:minimumHorizontalRadius ;

    sh:severity sh:Violation .

era-sh:MinimumHorizontalRadiusApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:minimumHorizontalRadius ;
    era:scope "local" ;

    rdfs:comment "Applicable when the RunningTrack is defined and requires information on the minimum horizontal radius."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "minimumHorizontalRadius: This error is due to the RunningTrack {?trackLabel}, violating the rule: The minimumHorizontalRadius must be defined for each RunningTrack."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:minimumHorizontalRadius ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinimumVerticalRadiusCrest a sh:PropertyShape ;
    era:affectedClass era:MinimumVerticalRadius ;
    era:affectedProperty era:minimumVerticalRadiusCrest ;
    era:rinfIndex "1.2.2.0.3.3" ;
    era:scope "local" ;

    rdfs:comment "Part of the minimum radius of vertical curve that indicates the crest."@en ;
    # Source: ontology comment for minimumVerticalRadiusCrest

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for minimumVerticalRadiusCrest

    sh:message "minimumVerticalRadiusCrest: The MinimumVerticalRadius must have at most one minimumVerticalRadiusCrest value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:minimumVerticalRadiusCrest ;

    sh:severity sh:Violation .

era-sh:MinimumVerticalRadiusCrestApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:MinimumVerticalRadius ;
    era:affectedProperty era:minimumVerticalRadiusCrest ;
    era:scope "local" ;

    rdfs:comment "Applicable when the MinimumVerticalRadius is defined and requires information on the minimum vertical radius crest."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "minimumVerticalRadiusCrest: This error is due to the MinimumVerticalRadius {?radiusLabel}, violating the rule: The minimumVerticalRadiusCrest must be defined for each MinimumVerticalRadius."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?radiusLabel WHERE {
        $this rdfs:label ?radiusLabel .
        FILTER NOT EXISTS { $this era:minimumVerticalRadiusCrest ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinimumVerticalRadiusHollow a sh:PropertyShape ;
    era:affectedClass era:MinimumVerticalRadius ;
    era:affectedProperty era:minimumVerticalRadiusHollow ;
    era:rinfIndex "1.2.2.0.3.3" ;
    era:scope "local" ;

    rdfs:comment "Part of the minimum radius of vertical curve that indicates the hollow."@en ;
    # Source: ontology comment for minimumVerticalRadiusHollow

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for minimumVerticalRadiusHollow

    sh:message "minimumVerticalRadiusHollow: The MinimumVerticalRadius must have at most one minimumVerticalRadiusHollow value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:minimumVerticalRadiusHollow ;

    sh:severity sh:Violation .

era-sh:MinimumVerticalRadiusHollowApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:MinimumVerticalRadius ;
    era:affectedProperty era:minimumVerticalRadiusHollow ;
    era:scope "local" ;

    rdfs:comment "Applicable when the MinimumVerticalRadius is defined and requires information on the minimum vertical radius hollow."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "minimumVerticalRadiusHollow: This error is due to the MinimumVerticalRadius {?radiusLabel}, violating the rule: The minimumVerticalRadiusHollow must be defined for each MinimumVerticalRadius."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?radiusLabel WHERE {
        $this rdfs:label ?radiusLabel .
        FILTER NOT EXISTS { $this era:minimumVerticalRadiusHollow ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinimumWheelDiameter a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minWheelDiameter ;
    era:rinfIndex "4.8.2" ;
    era:scope "local" ;

    rdfs:comment "Indication of minimum permitted wheel diameter, given in millimetres."@en ;
    # Source: ontology comment for minimumWheelDiameter

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for minimumWheelDiameter

    sh:message "minWheelDiameter: The VehicleType must have at most one minWheelDiameter value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:minWheelDiameter ;

    sh:severity sh:Violation .

era-sh:MinimumWheelDiameterApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minWheelDiameter ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the minimum wheel diameter is defined and must be specified."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "minWheelDiameter: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The minWheelDiameter must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:minWheelDiameter ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:NationalLoadCapability a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:nationalLoadCapability ;
    era:rinfIndex "1.1.1.1.2.4.1" ;
    era:scope "local" ;

    rdfs:comment "National classification for load capability."@en ;
    # Source: ontology comment for nationalLoadCapability

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for nationalLoadCapability

    sh:message "nationalLoadCapability: The CommonCharacteristicsSubset must have at most one nationalLoadCapability value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:nationalLoadCapability ;

    sh:severity sh:Violation .

era-sh:NationalLoadCapabilityApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:nationalLoadCapability ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires information on the national load capability."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "nationalLoadCapability: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The nationalLoadCapability must be defined for each CommonCharacteristicsSubset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:nationalLoadCapability ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:NationalRollingStockFireCategory a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:nationalRollingStockFireCategory ;
    era:rinfIndex "1.1.1.1.8.11" ;
    era:scope "local" ;

    rdfs:comment "Categorisation of how a passenger train with a fire on board will continue to operate for a defined time period - according to national rules if they exist."@en ;
    # Source: ontology comment for nationalRollingStockFireCategory

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for nationalRollingStockFireCategory

    sh:message "nationalRollingStockFireCategory: The Tunnel must have at most one nationalRollingStockFireCategory value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:nationalRollingStockFireCategory ;

    sh:severity sh:Violation .

era-sh:NationalRollingStockFireCategoryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:nationalRollingStockFireCategory ;
    era:scope "local" ;

    rdfs:comment "Applicable for tunnels when the value of parameter 1.1.1.1.8.10 is 'none' and national rules exist."@en ;
    # Source: RAG explanation on applicability

    sh:message "nationalRollingStockFireCategory: This error is due to the Tunnel {?tunnelLabel}, violating the rule: The nationalRollingStockFireCategory must be defined for each Tunnel when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:nationalRollingStockFireCategory ?value } .
        # Additional condition to check if the value of parameter 1.1.1.1.8.10 is 'none' can be added here
    }
    """ ;

    sh:severity sh:Violation .

era-sh:NationalValuesBrakeModel a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:nationalValuesBrakeModel ;
    era:rinfIndex "1.1.1.3.2.16.13" ;
    era:scope "local" ;

    rdfs:comment "Set of parameters for adapting the braking curves calculated by the ETCS on-board system to match accuracy, performance and safety margins imposed by the infrastructure manager."@en ;
    # Source: ontology comment for nationalValuesBrakeModel

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for nationalValuesBrakeModel

    sh:message "nationalValuesBrakeModel: The CommonCharacteristicsSubset must have at most one nationalValuesBrakeModel value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:nationalValuesBrakeModel ;

    sh:severity sh:Violation .

era-sh:NationalValuesBrakeModelApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:nationalValuesBrakeModel ;
    era:scope "local" ;

    rdfs:comment "Applicable when ETCS is present and a value must be provided for nationalValuesBrakeModel."@en ;
    # Source: inferred from the dependency note in the ontology

    sh:message "nationalValuesBrakeModel: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The nationalValuesBrakeModel must be defined for each CommonCharacteristicsSubset when ETCS is present."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:nationalValuesBrakeModel ?value } .
        # Additional condition to check if ETCS is present can be added here
    }
    """ ;

    sh:severity sh:Violation .

era-sh:NonCodedRestrictions a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:nonCodedRestrictions ;
    era:scope "local" ;

    rdfs:comment "Denotes a non-coded restrictions for this particular vehicleType, as a set of sentences."@en ;
    # Source: ontology comment for nonCodedRestrictions

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for nonCodedRestrictions

    sh:message "nonCodedRestrictions: The VehicleType must have at most one nonCodedRestrictions value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:nonCodedRestrictions ;

    sh:severity sh:Violation .

era-sh:NonCodedRestrictionsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:nonCodedRestrictions ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on non-coded restrictions."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "nonCodedRestrictions: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The nonCodedRestrictions must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:nonCodedRestrictions ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:NumberElementsRakeFreightWagons a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:numberElementsRakeFreightWagons ;
    era:rinfIndex "4.1.6" ;
    era:scope "local" ;

    rdfs:comment "Number of elements in the rake of freight wagons (only for subcategory 'rake of freight wagons')."@en ;
    # Source: ontology comment for numberElementsRakeFreightWagons

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for numberElementsRakeFreightWagons

    sh:message "numberElementsRakeFreightWagons: The VehicleType must have at most one numberElementsRakeFreightWagons value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:numberElementsRakeFreightWagons ;

    sh:severity sh:Violation .

era-sh:NumberElementsRakeFreightWagonsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:numberElementsRakeFreightWagons ;
    era:scope "local" ;

    rdfs:comment "Applicable only for the subcategory 'rake of freight wagons'."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "numberElementsRakeFreightWagons: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The numberElementsRakeFreightWagons must be defined for each VehicleType that is a rake of freight wagons."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:numberElementsRakeFreightWagons ?value } .
        # Additional condition to check if the VehicleType is a rake of freight wagons can be added here
    }
    """ ;

    sh:severity sh:Violation .

era-sh:NumberOfPantographsInContactWithOCL a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:numberOfPantographsInContactWithOCL ;
    era:rinfIndex "4.10.7" ;
    era:scope "local" ;

    rdfs:comment "Number of pantographs in contact with the overhead contact line (OCL) (to be indicated for each energy supply system the vehicle is equipped for)."@en ;
    # Source: ontology comment for numberOfPantographsInContactWithOCL

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for numberOfPantographsInContactWithOCL

    sh:message "numberOfPantographsInContactWithOCL: The VehicleType must have at most one numberOfPantographsInContactWithOCL value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:numberOfPantographsInContactWithOCL ;

    sh:severity sh:Violation .

era-sh:NumberOfPantographsInContactWithOCLApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:numberOfPantographsInContactWithOCL ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the number of pantographs in contact with OCL."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "numberOfPantographsInContactWithOCL: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The numberOfPantographsInContactWithOCL must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:numberOfPantographsInContactWithOCL ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:NumberOfToilets a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:numberOfToilets ;
    era:rinfIndex "4.12.1.2" ;
    era:scope "local" ;

    rdfs:comment "Number of toilets."@en ;
    # Source: ontology comment for numberOfToilets

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for numberOfToilets

    sh:message "numberOfToilets: The VehicleType must have at most one numberOfToilets value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:numberOfToilets ;

    sh:severity sh:Violation .

era-sh:NumberOfToiletsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:numberOfToilets ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the number of toilets."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "numberOfToilets: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The numberOfToilets must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:numberOfToilets ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OclType a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:oclType ;
    era:rinfIndex "4.10.9" ;
    era:scope "local" ;

    rdfs:comment "OCL type."@en ;
    # Source: ontology comment for oclType

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for oclType

    sh:message "oclType: The VehicleType must have at most one oclType value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:oclType ;

    sh:severity sh:Violation .

era-sh:OclTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:oclType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the OCL type."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "oclType: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The oclType must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:oclType ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OffsetFromKilometricPost a sh:PropertyShape ;
    era:affectedClass era:LinearPositioningSystemCoordinate ;
    era:affectedProperty era:offsetFromKilometricPost ;
    era:scope "local" ;

    rdfs:comment "Relative distance from a reference kilometric post."@en ;
    # Source: ontology comment for offsetFromKilometricPost

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for offsetFromKilometricPost

    sh:message "offsetFromKilometricPost: The LinearPositioningSystemCoordinate must have at most one offsetFromKilometricPost value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:offsetFromKilometricPost ;

    sh:severity sh:Violation .

era-sh:OffsetFromKilometricPostApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LinearPositioningSystemCoordinate ;
    era:affectedProperty era:offsetFromKilometricPost ;
    era:scope "local" ;

    rdfs:comment "Applicable when the LinearPositioningSystemCoordinate is defined and requires information on the offset from the kilometric post."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "offsetFromKilometricPost: This error is due to the LinearPositioningSystemCoordinate {?coordinateLabel}, violating the rule: The offsetFromKilometricPost must be defined for each LinearPositioningSystemCoordinate."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?coordinateLabel WHERE {
        $this rdfs:label ?coordinateLabel .
        FILTER NOT EXISTS { $this era:offsetFromKilometricPost ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OffsetFromOrigin a sh:PropertyShape ;
    era:affectedClass era:TopologicalCoordinate ;
    era:affectedProperty era:offsetFromOrigin ;
    era:scope "local" ;

    rdfs:comment "Offset from the origin in a topological coordinate."@en ;
    # Source: ontology comment for offsetFromOrigin

    sh:datatype xsd:positiveInteger ;
    # Source: ontology range is xsd:positiveInteger

    sh:maxCount 1 ;
    # Inferred: expected at most one value for offsetFromOrigin

    sh:message "offsetFromOrigin: The TopologicalCoordinate must have at most one offsetFromOrigin value that is a positive integer. This error may be due to having more than one value or having a value that is not a positive integer."@en ;

    sh:path era:offsetFromOrigin ;

    sh:severity sh:Violation .

era-sh:OffsetFromOriginApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TopologicalCoordinate ;
    era:affectedProperty era:offsetFromOrigin ;
    era:scope "local" ;

    rdfs:comment "Applicable when the TopologicalCoordinate is defined and requires information on the offset from the origin."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "offsetFromOrigin: This error is due to the TopologicalCoordinate {?coordinateLabel}, violating the rule: The offsetFromOrigin must be defined for each TopologicalCoordinate."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?coordinateLabel WHERE {
        $this rdfs:label ?coordinateLabel .
        FILTER NOT EXISTS { $this era:offsetFromOrigin ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OpName a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:opName ;
    era:rinfIndex "1.2.0.0.0.1" ;
    era:scope "local" ;

    rdfs:comment "Name normally related to the town or village or to traffic control purpose."@en ;
    # Source: ontology comment for opName

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for opName

    sh:message "opName: The OperationalPoint must have at most one opName value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:opName ;

    sh:severity sh:Violation .

era-sh:OpNameApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:opName ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OperationalPoint is defined and requires a name."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "opName: This error is due to the OperationalPoint {?opLabel}, violating the rule: The opName must be defined for each OperationalPoint."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?opLabel WHERE {
        $this rdfs:label ?opLabel .
        FILTER NOT EXISTS { $this era:opName ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OpTypeGaugeChangeover a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:opTypeGaugeChangeover ;
    era:rinfIndex "1.2.0.0.0.4.1" ;
    era:scope "local" ;

    rdfs:comment "Type of track gauge changeover facility."@en ;
    # Source: ontology comment for opTypeGaugeChangeover

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for opTypeGaugeChangeover

    sh:message "opTypeGaugeChangeover: The OperationalPoint must have at most one opTypeGaugeChangeover value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:opTypeGaugeChangeover ;

    sh:severity sh:Violation .

era-sh:OpTypeGaugeChangeoverApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:opTypeGaugeChangeover ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OperationalPoint is defined and requires information on the type of track gauge changeover facility."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "opTypeGaugeChangeover: This error is due to the OperationalPoint {?opLabel}, violating the rule: The opTypeGaugeChangeover must be defined for each OperationalPoint when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?opLabel WHERE {
        $this rdfs:label ?opLabel .
        FILTER NOT EXISTS { $this era:opTypeGaugeChangeover ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OrganisationCode a sh:PropertyShape ;
    era:affectedClass era:OrganisationRole ;
    era:affectedProperty era:organisationCode ;
    era:rinfIndex "1.2.1.0.0.1" ; # Example index from RAG
    era:scope "local" ;

    rdfs:comment "Four alpha-numeric code allocated by ERA to a body. It represents the Infrastructure Manager (IM) code in RINF."@en ;
    # Source: ontology comment for organisationCode

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for organisationCode

    sh:pattern "^[A-Z0-9]{4}$" ;
    # Inferred: based on the data presentation format [AAAA] from RAG

    sh:message "organisationCode: The OrganisationRole must have at most one organisationCode value that is a string matching the pattern of four alphanumeric characters. This error may be due to having more than one value or having a value that does not conform to the expected format."@en ;

    sh:path era:organisationCode ;

    sh:severity sh:Violation .

era-sh:OrganisationCodeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OrganisationRole ;
    era:affectedProperty era:organisationCode ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OrganisationRole is defined and requires an organisation code."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "organisationCode: This error is due to the OrganisationRole {?roleLabel}, violating the rule: The organisationCode must be defined for each OrganisationRole."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?roleLabel WHERE {
        $this rdfs:label ?roleLabel .
        FILTER NOT EXISTS { $this era:organisationCode ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ParameterValue a sh:PropertyShape ;
    era:affectedClass era:ParameterApplicability ;
    era:affectedProperty era:parameterValue ;
    era:scope "local" ;

    rdfs:comment "This property is used to link an applicability to the precise value of the parameter."@en ;
    # Source: ontology comment for parameterValue

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (Literal)

    sh:maxCount 1 ;
    # Inferred: expected at most one value for parameterValue

    sh:message "parameterValue: The ParameterApplicability must have at most one parameterValue that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:parameterValue ;

    sh:severity sh:Violation .

era-sh:ParameterValueApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ParameterApplicability ;
    era:affectedProperty era:parameterValue ;
    era:scope "local" ;

    rdfs:comment "Applicable when the ParameterApplicability is defined and requires a parameter value."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "parameterValue: This error is due to the ParameterApplicability {?applicabilityLabel}, violating the rule: The parameterValue must be defined for each ParameterApplicability."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?applicabilityLabel WHERE {
        $this rdfs:label ?applicabilityLabel .
        FILTER NOT EXISTS { $this era:parameterValue ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ParkingBrake a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:parkingBrake ;
    era:rinfIndex "4.7.3" ;
    era:scope "local" ;

    rdfs:comment "Indicates whether all vehicles of this type must be equipped with a parking brake."@en ;
    # Source: ontology comment for parkingBrake

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for parkingBrake

    sh:message "parkingBrake: The VehicleType must have at most one parkingBrake value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:parkingBrake ;

    sh:severity sh:Violation .

era-sh:ParkingBrakeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:parkingBrake ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the parking brake."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "parkingBrake: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The parkingBrake must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:parkingBrake ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ParkingBrakeMandatory a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:parkingBrakeMandatory ;
    era:rinfIndex "4.7.3.1" ;
    era:scope "local" ;

    rdfs:comment "All vehicles of this type must be equipped with a parking brake (parking brake mandatory for vehicles of this type)."@en ;
    # Source: ontology comment for parkingBrakeMandatory

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for parkingBrakeMandatory

    sh:message "parkingBrakeMandatory: The VehicleType must have at most one parkingBrakeMandatory value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:parkingBrakeMandatory ;

    sh:severity sh:Violation .

era-sh:ParkingBrakeMandatoryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:parkingBrakeMandatory ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the parking brake mandatory status."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "parkingBrakeMandatory: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The parkingBrakeMandatory must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:parkingBrakeMandatory ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ParkingBrakeMaximumGradient a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:parkingBrakeMaximumGradient ;
    era:rinfIndex "4.7.3.3" ;
    era:scope "local" ;

    rdfs:comment "Maximum gradient on which the unit is kept immobilised by the parking brake alone (if the vehicle is fitted with it)."@en ;
    # Source: ontology comment for parkingBrakeMaximumGradient

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for parkingBrakeMaximumGradient

    sh:message "parkingBrakeMaximumGradient: The VehicleType must have at most one parkingBrakeMaximumGradient value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:parkingBrakeMaximumGradient ;

    sh:severity sh:Violation .

era-sh:ParkingBrakeMaximumGradientApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:parkingBrakeMaximumGradient ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the maximum gradient for the parking brake."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "parkingBrakeMaximumGradient: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The parkingBrakeMaximumGradient must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:parkingBrakeMaximumGradient ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PassByNoiseLevel a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:passByNoiseLevel ;
    era:rinfIndex "4.11.1" ;
    era:scope "local" ;

    rdfs:comment "Pass-by noise level given in dB(A)."@en ;
    # Source: ontology comment for passByNoiseLevel

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for passByNoiseLevel

    sh:message "passByNoiseLevel: The VehicleType must have at most one passByNoiseLevel value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:passByNoiseLevel ;

    sh:severity sh:Violation .

era-sh:PassByNoiseLevelApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:passByNoiseLevel ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the pass-by noise level."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "passByNoiseLevel: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The passByNoiseLevel must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:passByNoiseLevel ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PermissiblePayload a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:permissiblePayload ;
    era:rinfIndex "4.5.1" ;
    era:scope "local" ;

    rdfs:comment "Permissible payload for different line categories."@en ;
    # Source: ontology comment for permissiblePayload

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for permissiblePayload

    sh:message "permissiblePayload: The VehicleType must have at most one permissiblePayload value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:permissiblePayload ;

    sh:severity sh:Violation .

era-sh:PermissiblePayloadApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:permissiblePayload ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the permissible payload."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "permissiblePayload: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The permissiblePayload must be defined for each VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:permissiblePayload ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PermissionChargingElectricEnergyTractionStandstill a sh:PropertyShape ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:permissionChargingElectricEnergyTractionStandstill ;
    era:rinfIndex "1.2.1.0.7.1" ;
    era:scope "local" ;

    rdfs:comment "Point at which IM authorises charging of electric energy storage for traction purposes at standstill."@en ;
    # Source: ontology comment for permissionChargingElectricEnergyTractionStandstill

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for permissionChargingElectricEnergyTractionStandstill

    sh:message "permissionChargingElectricEnergyTractionStandstill: The ContactLineSystem must have at most one permissionChargingElectricEnergyTractionStandstill value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:permissionChargingElectricEnergyTractionStandstill ;

    sh:severity sh:Violation .

era-sh:PermissionChargingElectricEnergyTractionStandstillApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:permissionChargingElectricEnergyTractionStandstill ;
    era:scope "local" ;

    rdfs:comment "Applicable when the ContactLineSystem is defined and requires information on the permission for charging electric energy storage."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "permissionChargingElectricEnergyTractionStandstill: This error is due to the ContactLineSystem {?contactLineLabel}, violating the rule: The permissionChargingElectricEnergyTractionStandstill must be defined for each ContactLineSystem."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?contactLineLabel WHERE {
        $this rdfs:label ?contactLineLabel .
        FILTER NOT EXISTS { $this era:permissionChargingElectricEnergyTractionStandstill ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PermitUseReflectivePlates a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:permitUseReflectivePlates ; 
    era:rinfIndex "1.1.1.1.7.12" ; 
    era:scope "local" ; 

    rdfs:comment "Sections where it is permitted to use the reflective plates on rail freight corridors, with a view to prioritise the current bottlenecks."@en ; 
    # Source: ontology comment for permitUseReflectivePlates

    sh:datatype xsd:boolean ; 
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ; 
    # Inferred: expected at most one value for permitUseReflectivePlates

    sh:message "permitUseReflectivePlates: The CommonCharacteristicsSubset must have at most one permitUseReflectivePlates value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ; 

    sh:path era:permitUseReflectivePlates ; 

    sh:severity sh:Violation .

era-sh:PermitUseReflectivePlatesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:permitUseReflectivePlates ; 
    era:scope "local" ; 

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires information on the permit use of reflective plates."@en ; 
    # Source: inferred from the context of the ontology and RAG

    sh:message "permitUseReflectivePlates: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The permitUseReflectivePlates must be defined for each CommonCharacteristicsSubset."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:permitUseReflectivePlates ?value } .
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:PermittedContactForce a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:permittedContactForce ; 
    era:rinfIndex "1.1.1.2.5.2" ; 
    era:scope "local" ; 

    rdfs:comment "Indication of contact force allowed expressed in newtons."@en ; 
    # Source: ontology comment for permittedContactForce

    sh:datatype xsd:string ; 
    # Source: ontology range is xsd:string

    sh:maxCount 1 ; 
    # Inferred: expected at most one value for permittedContactForce

    sh:pattern "^(\\d+|\\d+\\s*\\+\\s*\\d+)$" ; 
    # Inferred: based on the data presentation format which allows a value of static force and maximum force or a formula

    sh:message "permittedContactForce: The CommonCharacteristicsSubset must have at most one permittedContactForce value that is a string matching the expected format. This error may be due to having more than one value or having a value that does not conform to the expected format."@en ; 

    sh:path era:permittedContactForce ; 

    sh:severity sh:Violation .

era-sh:PermittedContactForceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:permittedContactForce ; 
    era:scope "local" ; 

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires information on the permitted contact force."@en ; 
    # Source: inferred from the context of the ontology and RAG

    sh:message "permittedContactForce: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The permittedContactForce must be defined for each CommonCharacteristicsSubset when applicable."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:permittedContactForce ?value } .
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:PhaseInfoChangeSupplySystem a sh:PropertyShape ;
    era:affectedClass era:PhaseInfo ;
    era:affectedProperty era:phaseInfoChangeSupplySystem ;
    era:scope "local" ;

    rdfs:comment "Part of the phase info of a track that corresponds to the single selection of Y=yes or N=no to show if the energy supply system changes."@en ;
    # Source: ontology comment for phaseInfoChangeSupplySystem

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for phaseInfoChangeSupplySystem

    sh:message "phaseInfoChangeSupplySystem: The PhaseInfo must have at most one phaseInfoChangeSupplySystem value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:phaseInfoChangeSupplySystem ;

    sh:severity sh:Violation .

era-sh:PhaseInfoChangeSupplySystemApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PhaseInfo ;
    era:affectedProperty era:phaseInfoChangeSupplySystem ;
    era:scope "local" ;

    rdfs:comment "Applicable when the PhaseInfo is defined and requires information on the phase info change supply system."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "phaseInfoChangeSupplySystem: This error is due to the PhaseInfo {?phaseInfoLabel}, violating the rule: The phaseInfoChangeSupplySystem must be defined for each PhaseInfo."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?phaseInfoLabel WHERE {
        $this rdfs:label ?phaseInfoLabel .
        FILTER NOT EXISTS { $this era:phaseInfoChangeSupplySystem ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PhaseInfoDistanceType a sh:PropertyShape ;
    era:affectedClass era:PhaseInfo ;
    era:affectedProperty era:phaseInfoDistanceType ;
    era:scope "local" ;

    rdfs:comment "Part of the phase info of a track that corresponds to the single selection of 'MIN=minimum' or 'MAX=maximum' to show whether the length is a minimum distance between the inner contact strips of the pantographs or a maximum distance between the outer contact strips of the pantographs. Multiple strings for this parameter are accepted."@en ;
    # Source: ontology comment for phaseInfoDistanceType

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (Literal)

    sh:maxCount 1 ;
    # Inferred: expected at most one value for phaseInfoDistanceType

    sh:message "phaseInfoDistanceType: The PhaseInfo must have at most one phaseInfoDistanceType value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:phaseInfoDistanceType ;

    sh:severity sh:Violation .

era-sh:PhaseInfoDistanceTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PhaseInfo ;
    era:affectedProperty era:phaseInfoDistanceType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the PhaseInfo is defined and requires information on the phase info distance type."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "phaseInfoDistanceType: This error is due to the PhaseInfo {?phaseInfoLabel}, violating the rule: The phaseInfoDistanceType must be defined for each PhaseInfo."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?phaseInfoLabel WHERE {
        $this rdfs:label ?phaseInfoLabel .
        FILTER NOT EXISTS { $this era:phaseInfoDistanceType ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PhaseInfoKm a sh:PropertyShape ;
    era:affectedClass era:PhaseInfo ;
    era:affectedProperty era:phaseInfoKm ;
    era:scope "local" ;

    rdfs:comment "Part of the phase info of a track that indicates the location from the start of the line where the new value is valid."@en ;
    # Source: ontology comment for phaseInfoKm

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for phaseInfoKm

    sh:message "phaseInfoKm: The PhaseInfo must have at most one phaseInfoKm value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:phaseInfoKm ;

    sh:severity sh:Violation .

era-sh:PhaseInfoKmApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PhaseInfo ;
    era:affectedProperty era:phaseInfoKm ;
    era:scope "local" ;

    rdfs:comment "Applicable when the PhaseInfo is defined and requires information on the phase info km."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "phaseInfoKm: This error is due to the PhaseInfo {?phaseInfoLabel}, violating the rule: The phaseInfoKm must be defined for each PhaseInfo."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?phaseInfoLabel WHERE {
        $this rdfs:label ?phaseInfoLabel .
        FILTER NOT EXISTS { $this era:phaseInfoKm ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PhaseInfoLength a sh:PropertyShape ;
    era:affectedClass era:PhaseInfo ;
    era:affectedProperty era:phaseInfoLength ;
    era:scope "local" ;

    rdfs:comment "Part of the phase info of a track that corresponds to the length of the phase separation in metres."@en ;
    # Source: ontology comment for phaseInfoLength

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for phaseInfoLength

    sh:message "phaseInfoLength: The PhaseInfo must have at most one phaseInfoLength value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:phaseInfoLength ;

    sh:severity sh:Violation .

era-sh:PhaseInfoLengthApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PhaseInfo ;
    era:affectedProperty era:phaseInfoLength ;
    era:scope "local" ;

    rdfs:comment "Applicable when the PhaseInfo is defined and requires information on the phase info length."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "phaseInfoLength: This error is due to the PhaseInfo {?phaseInfoLabel}, violating the rule: The phaseInfoLength must be defined for each PhaseInfo."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?phaseInfoLabel WHERE {
        $this rdfs:label ?phaseInfoLabel .
        FILTER NOT EXISTS { $this era:phaseInfoLength ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PhaseInfoPantographLowered a sh:PropertyShape ;
    era:affectedClass era:PhaseInfo ;
    era:affectedProperty era:phaseInfoPantographLowered ;
    era:scope "local" ;

    rdfs:comment "Part of the phase info of a track that shows whether a pantograph has to be lowered."@en ;
    # Source: ontology comment for phaseInfoPantographLowered

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for phaseInfoPantographLowered

    sh:message "phaseInfoPantographLowered: The PhaseInfo must have at most one phaseInfoPantographLowered value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:phaseInfoPantographLowered ;

    sh:severity sh:Violation .

era-sh:PhaseInfoPantographLoweredApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PhaseInfo ;
    era:affectedProperty era:phaseInfoPantographLowered ;
    era:scope "local" ;

    rdfs:comment "Applicable when the PhaseInfo is defined and requires information on whether the pantograph has to be lowered."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "phaseInfoPantographLowered: This error is due to the PhaseInfo {?phaseInfoLabel}, violating the rule: The phaseInfoPantographLowered must be defined for each PhaseInfo."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?phaseInfoLabel WHERE {
        $this rdfs:label ?phaseInfoLabel .
        FILTER NOT EXISTS { $this era:phaseInfoPantographLowered ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PhaseInfoSwitchOffBreaker a sh:PropertyShape ;
    era:affectedClass era:PhaseInfo ;
    era:affectedProperty era:phaseInfoSwitchOffBreaker ;
    era:scope "local" ;

    rdfs:comment "Part of the phase info of a track that shows whether the breaker has to be switched off."@en ;
    # Source: ontology comment for phaseInfoSwitchOffBreaker

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for phaseInfoSwitchOffBreaker

    sh:message "phaseInfoSwitchOffBreaker: The PhaseInfo must have at most one phaseInfoSwitchOffBreaker value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:phaseInfoSwitchOffBreaker ;

    sh:severity sh:Violation .

era-sh:PhaseInfoSwitchOffBreakerApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PhaseInfo ;
    era:affectedProperty era:phaseInfoSwitchOffBreaker ;
    era:scope "local" ;

    rdfs:comment "Applicable when the PhaseInfo is defined and requires information on whether the breaker has to be switched off."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "phaseInfoSwitchOffBreaker: This error is due to the PhaseInfo {?phaseInfoLabel}, violating the rule: The phaseInfoSwitchOffBreaker must be defined for each PhaseInfo."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?phaseInfoLabel WHERE {
        $this rdfs:label ?phaseInfoLabel .
        FILTER NOT EXISTS { $this era:phaseInfoSwitchOffBreaker ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PlatformId a sh:PropertyShape ;
    era:affectedClass era:PlatformEdge ;
    era:affectedProperty era:platformId ;
    era:rinfIndex "1.2.1.0.6.2" ;
    era:scope "local" ;

    rdfs:comment "Unique platform identification or unique platform number within an Operational Point."@en ;
    # Source: ontology comment for platformId

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for platformId

    sh:message "platformId: The PlatformEdge must have at most one platformId value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:platformId ;

    sh:severity sh:Violation .

era-sh:PlatformIdApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PlatformEdge ;
    era:affectedProperty era:platformId ;
    era:scope "local" ;

    rdfs:comment "Applicable when the PlatformEdge is defined and requires information on the unique platform identification."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "platformId: This error is due to the PlatformEdge {?platformLabel}, violating the rule: The platformId must be defined for each PlatformEdge."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?platformLabel WHERE {
        $this rdfs:label ?platformLabel .
        FILTER NOT EXISTS { $this era:platformId ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PortableBoardingAids a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:portableBoardingAids ;
    era:rinfIndex "4.12.3.3" ;
    era:scope "local" ;

    rdfs:comment "Description of any portable boarding aids if considered in the design of the vehicle for meeting the PRM TSI requirements."@en ;
    # Source: ontology comment for portableBoardingAids

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for portableBoardingAids

    sh:message "portableBoardingAids: The VehicleType must have at most one portableBoardingAids value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:portableBoardingAids ;

    sh:severity sh:Violation .

era-sh:PortableBoardingAidsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:portableBoardingAids ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on portable boarding aids."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "portableBoardingAids: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The portableBoardingAids must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:portableBoardingAids ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PreventRegenerativeBrakeUse a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:preventRegenerativeBrakeUse ;
    era:rinfIndex "4.7.4.3.2" ;
    era:scope "local" ;

    rdfs:comment "Possibility of preventing the use of the regenerative brake (only if fitted with regenerative brake)."@en ;
    # Source: ontology comment for preventRegenerativeBrakeUse

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for preventRegenerativeBrakeUse

    sh:message "preventRegenerativeBrakeUse: The VehicleType must have at most one preventRegenerativeBrakeUse value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:preventRegenerativeBrakeUse ;

    sh:severity sh:Violation .

era-sh:PreventRegenerativeBrakeUseApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:preventRegenerativeBrakeUse ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the possibility of preventing regenerative brake use."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "preventRegenerativeBrakeUse: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The preventRegenerativeBrakeUse must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:preventRegenerativeBrakeUse ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PrimaryLocationCode a sh:PropertyShape ;
    era:affectedClass era:PrimaryLocation ;
    era:affectedProperty era:primaryLocationCode ;
    era:rinfIndex "1.2.0.0.0.3" ;
    era:scope "local" ;

    rdfs:comment "Primary location code developed for information exchange in accordance with the TSIs relating to the telematics applications subsystem."@en ;
    # Source: ontology comment for primaryLocationCode

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for primaryLocationCode

    sh:pattern "^[A-Z]{2}\\d{5}$" ;
    # Inferred: based on the data presentation format which requires two letters for the Country Code and five numbers for the Location Code

    sh:message "primaryLocationCode: The PrimaryLocation must have at most one primaryLocationCode value that is a string matching the expected format. This error may be due to having more than one value or having a value that does not conform to the expected format."@en ;

    sh:path era:primaryLocationCode ;

    sh:severity sh:Violation .

era-sh:PrimaryLocationCodeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PrimaryLocation ;
    era:affectedProperty era:primaryLocationCode ;
    era:scope "local" ;

    rdfs:comment "Applicable when the PrimaryLocation is defined and requires information on the primary location code."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "primaryLocationCode: This error is due to the PrimaryLocation {?locationLabel}, violating the rule: The primaryLocationCode must be defined for each PrimaryLocation when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?locationLabel WHERE {
        $this rdfs:label ?locationLabel .
        FILTER NOT EXISTS { $this era:primaryLocationCode ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PrimaryLocationName a sh:PropertyShape ;
    era:affectedClass era:PrimaryLocation ;
    era:affectedProperty era:primaryLocationName ;
    era:scope "local" ;

    rdfs:comment "The primary location name is a string that identifies the primary location within the rail network."@en ;
    # Source: inferred from the ontology definition of primaryLocationName

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for primaryLocationName

    sh:message "primaryLocationName: The PrimaryLocation must have at most one primaryLocationName value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:primaryLocationName ;

    sh:severity sh:Violation .

era-sh:PrimaryLocationNameApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PrimaryLocation ;
    era:affectedProperty era:primaryLocationName ;
    era:scope "local" ;

    rdfs:comment "Applicable when the PrimaryLocation is defined and requires information on the primary location name."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "primaryLocationName: This error is due to the PrimaryLocation {?locationLabel}, violating the rule: The primaryLocationName must be defined for each PrimaryLocation when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?locationLabel WHERE {
        $this rdfs:label ?locationLabel .
        FILTER NOT EXISTS { $this era:primaryLocationName ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PrioritySeats a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:prioritySeats ;
    era:scope "local" ;

    rdfs:comment "Number of priority seats."@en ;
    # Source: ontology comment for prioritySeats

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (Literal)

    sh:maxCount 1 ;
    # Inferred: expected at most one value for prioritySeats

    sh:message "prioritySeats: The VehicleType must have at most one prioritySeats value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:prioritySeats ;

    sh:severity sh:Violation .

era-sh:PrioritySeatsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:prioritySeats ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the number of priority seats."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "prioritySeats: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The prioritySeats must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:prioritySeats ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PrmAccessibleToilets a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:prmAccessibleToilets ;
    era:scope "local" ;

    rdfs:comment "Number of PRM accessible toilets."@en ;
    # Source: ontology comment for prmAccessibleToilets

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for prmAccessibleToilets

    sh:message "prmAccessibleToilets: The VehicleType must have at most one prmAccessibleToilets value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:prmAccessibleToilets ;

    sh:severity sh:Violation .

era-sh:PrmAccessibleToiletsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:prmAccessibleToilets ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the number of PRM accessible toilets."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "prmAccessibleToilets: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The prmAccessibleToilets must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:prmAccessibleToilets ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PublicNetworkRoaming a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:publicNetworkRoaming ;
    era:scope "local" ;

    rdfs:comment "Existence of roaming to a public network. In case of Y, provide the name of the public network(s) under parameter 'Details on GSM-R roaming to public networks'."@en ;
    # Source: ontology comment for publicNetworkRoaming

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for publicNetworkRoaming

    sh:message "publicNetworkRoaming: The CommonCharacteristicsSubset must have at most one publicNetworkRoaming value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:publicNetworkRoaming ;

    sh:severity sh:Violation .

era-sh:PublicNetworkRoamingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:publicNetworkRoaming ;
    era:scope "local" ;

    rdfs:comment "Applicable if the answer to parameter 1.1.1.3.3.6 / Existence of roaming to public networks is ‘Y’."@en ;
    # Source: RAG applicability note

    sh:message "publicNetworkRoaming: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The publicNetworkRoaming must be defined for each CommonCharacteristicsSubset when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:publicNetworkRoaming ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PublicNetworkRoamingDetails a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:publicNetworkRoamingDetails ;
    era:scope "local" ;

    rdfs:comment "If roaming to public networks is configured, please indicate to which networks, for which users and in which areas."@en ;
    # Source: ontology comment for publicNetworkRoamingDetails

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for publicNetworkRoamingDetails

    sh:message "publicNetworkRoamingDetails: The CommonCharacteristicsSubset must have at most one publicNetworkRoamingDetails value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:publicNetworkRoamingDetails ;

    sh:severity sh:Violation .

era-sh:PublicNetworkRoamingDetailsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:publicNetworkRoamingDetails ;
    era:scope "local" ;

    rdfs:comment "Applicable if the answer to parameter 1.1.1.3.3.6 / Existence of roaming to public networks is ‘Y’."@en ;
    # Source: RAG applicability note

    sh:message "publicNetworkRoamingDetails: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The publicNetworkRoamingDetails must be defined for each CommonCharacteristicsSubset when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:publicNetworkRoamingDetails ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:QNvsbtsmperm a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:qNvsbtsmperm ;
    era:scope "local" ;

    rdfs:comment "Permission to use service brake in target speed monitoring."@en ;
    # Source: ontology comment for qNvsbtsmperm

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for qNvsbtsmperm

    sh:message "qNvsbtsmperm: The CommonCharacteristicsSubset must have at most one qNvsbtsmperm value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:qNvsbtsmperm ;

    sh:severity sh:Violation .

era-sh:QNvsbtsmpermApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:qNvsbtsmperm ;
    era:scope "local" ;

    rdfs:comment "Applicable when ETCS is present and a value has to be provided for qNvsbtsmperm."@en ;
    # Source: inferred from the dependency note in the ontology

    sh:message "qNvsbtsmperm: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The qNvsbtsmperm must be defined for each CommonCharacteristicsSubset when ETCS is present."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:qNvsbtsmperm ?value } .
        # Assuming ETCS presence is indicated by another property, e.g., era:etcsPresent
        FILTER EXISTS { $this era:etcsPresent ?etcsValue } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:QuasiStaticGuidingForce a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:quasiStaticGuidingForce ;
    era:rinfIndex "4.5.4" ;
    era:scope "local" ;

    rdfs:comment "Quasi-static guiding force (if exceeds the limit defined in TSI or not defined in the TSI), given in kN."@en ;
    # Source: ontology comment for quasiStaticGuidingForce

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for quasiStaticGuidingForce

    sh:message "quasiStaticGuidingForce: The VehicleType must have at most one quasiStaticGuidingForce value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:quasiStaticGuidingForce ;

    sh:severity sh:Violation .

era-sh:QuasiStaticGuidingForceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:quasiStaticGuidingForce ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the quasi-static guiding force."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "quasiStaticGuidingForce: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The quasiStaticGuidingForce must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:quasiStaticGuidingForce ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RadioNetworkId a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:radioNetworkId ; 
    era:rinfIndex "1.1.1.3.3" ; 
    era:scope "local" ; 

    rdfs:comment "Unique identification of the GSM-R network the calling mobile station has to register with, as defined in the specification referenced in Appendix A-1, index [C]."@en ; 
    # Source: ontology comment for radioNetworkId

    sh:datatype xsd:string ; 
    # Source: ontology range is xsd:string

    sh:maxCount 1 ; 
    # Inferred: expected at most one value for radioNetworkId

    sh:pattern "^[0-9]{6}$" ; 
    # Inferred: based on the format note indicating a six-digit number

    sh:message "radioNetworkId: The CommonCharacteristicsSubset must have at most one radioNetworkId value that is a string matching the expected format. This error may be due to having more than one value or having a value that does not conform to the expected format."@en ; 

    sh:path era:radioNetworkId ; 

    sh:severity sh:Violation .

era-sh:RadioNetworkIdApplicability a sh:SPARQLConstraint ; 
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:radioNetworkId ; 
    era:scope "local" ; 

    rdfs:comment "Applicable when GSM-R must be installed for this parameter to be applicable."@en ; 
    # Source: dependency note in the ontology

    sh:message "radioNetworkId: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The radioNetworkId must be defined for each CommonCharacteristicsSubset when GSM-R is installed."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:radioNetworkId ?value } .
        # Assuming the presence of GSM-R is indicated by another property, e.g., era:gsmRInstalled
        FILTER EXISTS { $this era:gsmRInstalled ?gsmRValue } .
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:RadioSwitchOverSpecialConditions a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:radioSwitchOverSpecialConditions ;
    era:rinfIndex "4.13.2.4" ;
    era:scope "local" ;

    rdfs:comment "Special conditions implemented on-board to switch over between different radio systems. Given as combination of systems installed on board ('System XX'_'System YY')."@en ;
    # Source: ontology comment for radioSwitchOverSpecialConditions

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for radioSwitchOverSpecialConditions

    sh:message "radioSwitchOverSpecialConditions: The VehicleType must have at most one radioSwitchOverSpecialConditions value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:radioSwitchOverSpecialConditions ;

    sh:severity sh:Violation .

era-sh:RadioSwitchOverSpecialConditionsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:radioSwitchOverSpecialConditions ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the special conditions for radio switch over."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "radioSwitchOverSpecialConditions: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The radioSwitchOverSpecialConditions must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:radioSwitchOverSpecialConditions ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RailSystemType a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:railSystemType ;
    era:scope "local" ;

    rdfs:comment "(deprecated) Type of railway system. Can be 'High-speed' or 'Conventional'. Deprecated due to the lack of information."@en ;
    # Source: ontology comment for railSystemType

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for railSystemType

    sh:in ( "High-speed" "Conventional" ) ;
    # Source: inferred from the comment indicating these are the valid values

    sh:message "railSystemType: The CommonCharacteristicsSubset must have at most one railSystemType value that is a string matching the expected values. This error may be due to having more than one value or having a value that is not one of the allowed types."@en ;

    sh:path era:railSystemType ;

    sh:severity sh:Violation .

era-sh:RailSystemTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:railSystemType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires information on the type of railway system."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "railSystemType: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The railSystemType must be defined for each CommonCharacteristicsSubset when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:railSystemType ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RaisedPantographsDistance a sh:PropertyShape ;
    era:affectedClass era:RaisedPantographsDistanceAndSpeed ;
    era:affectedProperty era:raisedPantographsDistance ;
    era:scope "local" ;

    rdfs:comment "Part of the raised pantographs distance and speed of a track that corresponds to the minimum distance between pantographs, in metres."@en ;
    # Source: ontology comment for raisedPantographsDistance

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for raisedPantographsDistance

    sh:message "raisedPantographsDistance: The RaisedPantographsDistanceAndSpeed must have at most one raisedPantographsDistance value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:raisedPantographsDistance ;

    sh:severity sh:Violation .

era-sh:RaisedPantographsDistanceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RaisedPantographsDistanceAndSpeed ;
    era:affectedProperty era:raisedPantographsDistance ;
    era:scope "local" ;

    rdfs:comment "Applicable when the RaisedPantographsDistanceAndSpeed is defined and requires information on the raised pantographs distance."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "raisedPantographsDistance: This error is due to the RaisedPantographsDistanceAndSpeed {?trackLabel}, violating the rule: The raisedPantographsDistance must be defined for each RaisedPantographsDistanceAndSpeed when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:raisedPantographsDistance ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RaisedPantographsNumber a sh:PropertyShape ;
    era:affectedClass era:RaisedPantographsDistanceAndSpeed ;
    era:affectedProperty era:raisedPantographsNumber ;
    era:scope "local" ;

    rdfs:comment "Part of the raised pantographs distance and speed of a track that corresponds to the number of pantographs."@en ;
    # Source: ontology comment for raisedPantographsNumber

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for raisedPantographsNumber

    sh:message "raisedPantographsNumber: The RaisedPantographsDistanceAndSpeed must have at most one raisedPantographsNumber value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:raisedPantographsNumber ;

    sh:severity sh:Violation .

era-sh:RaisedPantographsNumberApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RaisedPantographsDistanceAndSpeed ;
    era:affectedProperty era:raisedPantographsNumber ;
    era:scope "local" ;

    rdfs:comment "Applicable when the RaisedPantographsDistanceAndSpeed is defined and requires information on the number of raised pantographs."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "raisedPantographsNumber: This error is due to the RaisedPantographsDistanceAndSpeed {?trackLabel}, violating the rule: The raisedPantographsNumber must be defined for each RaisedPantographsDistanceAndSpeed when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:raisedPantographsNumber ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RaisedPantographsSpeed a sh:PropertyShape ;
    era:affectedClass era:RaisedPantographsDistanceAndSpeed ;
    era:affectedProperty era:raisedPantographsSpeed ;
    era:scope "local" ;

    rdfs:comment "Indication of maximum number of raised pantographs per train allowed and minimum spacing centre line to centre line of adjacent pantograph heads, expressed in metres, at the given speed."@en ;
    # Source: ontology comment for raisedPantographsSpeed

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for raisedPantographsSpeed

    sh:message "raisedPantographsSpeed: The RaisedPantographsDistanceAndSpeed must have at most one raisedPantographsSpeed value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:raisedPantographsSpeed ;

    sh:severity sh:Violation .

era-sh:RaisedPantographsSpeedApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RaisedPantographsDistanceAndSpeed ;
    era:affectedProperty era:raisedPantographsSpeed ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Overhead contact line (OCL) is selected in the relevant parameter."@en ;
    # Source: RAG applicability note

    sh:message "raisedPantographsSpeed: This error is due to the RaisedPantographsDistanceAndSpeed {?trackLabel}, violating the rule: The raisedPantographsSpeed must be defined for each RaisedPantographsDistanceAndSpeed when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:raisedPantographsSpeed ?value } .
        # Assuming the presence of OCL is indicated by another property, e.g., era:overheadContactLine
        FILTER EXISTS { $this era:overheadContactLine ?oclValue } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RbcID a sh:PropertyShape ;
    era:affectedClass era:RadioBlockCenter ;
    era:affectedProperty era:rbcID ;
    era:rinfIndex "1.1.1.3.2.17" ;
    era:scope "local" ;

    rdfs:comment "Unique RBC identification (NID_C+NID_RBC) as defined in the specification referenced in Appendix A-1, index [C] (TSI CCS)."@en ;
    # Source: ontology comment for rbcID

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for rbcID

    sh:pattern "^[0-9]{4}$" ;
    # Inferred: based on the format note indicating a four-digit number

    sh:message "rbcID: The RadioBlockCenter must have at most one rbcID value that is a string matching the expected format. This error may be due to having more than one value or having a value that does not conform to the expected format."@en ;

    sh:path era:rbcID ;

    sh:severity sh:Violation .

era-sh:RbcIDApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RadioBlockCenter ;
    era:affectedProperty era:rbcID ;
    era:scope "local" ;

    rdfs:comment "Applicable when the RadioBlockCenter is defined and requires a unique identification."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "rbcID: This error is due to the RadioBlockCenter {?rbcLabel}, violating the rule: The rbcID must be defined for each RadioBlockCenter when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?rbcLabel WHERE {
        $this rdfs:label ?rbcLabel .
        FILTER NOT EXISTS { $this era:rbcID ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RbcPhone a sh:PropertyShape ;
    era:affectedClass era:RadioBlockCenter ;
    era:affectedProperty era:rbcPhone ;
    era:rinfIndex "1.1.1.3.2.17" ;
    era:scope "local" ;

    rdfs:comment "Unique RBC calling number (NID_RADIO) as defined in the specification referenced in Appendix A-1, index [C]."@en ;
    # Source: ontology comment for rbcPhone

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for rbcPhone

    sh:pattern "^[0-9]{4}$" ;
    # Inferred: based on the format note indicating a four-digit number

    sh:message "rbcPhone: The RadioBlockCenter must have at most one rbcPhone value that is a string matching the expected format. This error may be due to having more than one value or having a value that does not conform to the expected format."@en ;

    sh:path era:rbcPhone ;

    sh:severity sh:Violation .

era-sh:RbcPhoneApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RadioBlockCenter ;
    era:affectedProperty era:rbcPhone ;
    era:scope "local" ;

    rdfs:comment "Applicable when the RadioBlockCenter is defined and requires a unique RBC calling number."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "rbcPhone: This error is due to the RadioBlockCenter {?rbcLabel}, violating the rule: The rbcPhone must be defined for each RadioBlockCenter when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?rbcLabel WHERE {
        $this rdfs:label ?rbcLabel .
        FILTER NOT EXISTS { $this era:rbcPhone ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RedLightsRequired a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:redLightsRequired ; 
    era:rinfIndex "1.1.1.1.7.10" ; 
    era:scope "local" ; 

    rdfs:comment "Sections where two steady red lights are required in accordance with TSI OPE."@en ; 
    # Source: ontology comment for redLightsRequired

    sh:datatype xsd:boolean ; 
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ; 
    # Inferred: expected at most one value for redLightsRequired

    sh:message "redLightsRequired: The CommonCharacteristicsSubset must have at most one redLightsRequired value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ; 

    sh:path era:redLightsRequired ; 

    sh:severity sh:Violation .

era-sh:RedLightsRequiredApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:redLightsRequired ; 
    era:scope "local" ; 

    rdfs:comment "Applicable when the section of line requires two steady red lights as per national rules."@en ; 
    # Source: inferred from the RAG and context of the ontology

    sh:message "redLightsRequired: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The redLightsRequired must be defined for each CommonCharacteristicsSubset when applicable."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:redLightsRequired ?value } .
        # Assuming the presence of national rules is indicated by another property, e.g., era:nationalRulesApplicable
        FILTER EXISTS { $this era:nationalRulesApplicable ?rulesValue } .
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:ReferencePassByNoiseLevel a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:referencePassByNoiseLevel ;
    era:rinfIndex "4.11.2" ;
    era:scope "local" ;

    rdfs:comment "Pass-by noise level was measured under reference conditions."@en ;
    # Source: ontology comment for referencePassByNoiseLevel

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for referencePassByNoiseLevel

    sh:message "referencePassByNoiseLevel: The VehicleType must have at most one referencePassByNoiseLevel value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:referencePassByNoiseLevel ;

    sh:severity sh:Violation .

era-sh:ReferencePassByNoiseLevelApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:referencePassByNoiseLevel ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the reference pass-by noise level."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "referencePassByNoiseLevel: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The referencePassByNoiseLevel must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:referencePassByNoiseLevel ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RelativeDistanceDangerPoint a sh:PropertyShape ;
    era:affectedClass era:Signal ;
    era:affectedProperty era:relativeDistanceDangerPoint ;
    era:rinfIndex "1.1.1.3.14.4" ;
    era:scope "local" ;

    rdfs:comment "Distance in meters to the danger point."@en ;
    # Source: ontology comment for relativeDistanceDangerPoint

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for relativeDistanceDangerPoint

    sh:message "relativeDistanceDangerPoint: The Signal must have at most one relativeDistanceDangerPoint value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:relativeDistanceDangerPoint ;

    sh:severity sh:Violation .

era-sh:RelativeDistanceDangerPointApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Signal ;
    era:affectedProperty era:relativeDistanceDangerPoint ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Signal is defined and requires information on the relative distance to the danger point."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "relativeDistanceDangerPoint: This error is due to the Signal {?signalLabel}, violating the rule: The relativeDistanceDangerPoint must be defined for each Signal when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?signalLabel WHERE {
        $this rdfs:label ?signalLabel .
        FILTER NOT EXISTS { $this era:relativeDistanceDangerPoint ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RequiredSandingOverride a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:requiredSandingOverride ;
    era:scope "local" ;

    rdfs:comment "Indication whether possibility to activate/deactivate sanding devices by driver, according to instructions from the Infrastructure Manager, is required or not."@en ;
    # Source: ontology comment for requiredSandingOverride

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for requiredSandingOverride

    sh:message "requiredSandingOverride: The TrainDetectionSystem must have at most one requiredSandingOverride value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:requiredSandingOverride ;

    sh:severity sh:Violation .

era-sh:RequiredSandingOverrideApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:requiredSandingOverride ;
    era:scope "local" ;

    rdfs:comment "Applicable when the TrainDetectionSystem is defined and requires information on the sanding override by driver."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "requiredSandingOverride: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: The requiredSandingOverride must be defined for each TrainDetectionSystem when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:requiredSandingOverride ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SchematicOverviewOP a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:schematicOverviewOP ;
    era:rinfIndex "1.2.0.0.0.7.1" ;
    era:scope "local" ;

    rdfs:comment "Document providing the schematic overview of the operational point."@en ;
    # Source: ontology comment for schematicOverviewOP

    sh:datatype xsd:anyURI ;
    # Source: ontology range is xsd:anyURI

    sh:maxCount 1 ;
    # Inferred: expected at most one value for schematicOverviewOP

    sh:message "schematicOverviewOP: The OperationalPoint must have at most one schematicOverviewOP value that is a valid URI. This error may be due to having more than one value or having a value that is not a valid URI."@en ;

    sh:path era:schematicOverviewOP ;

    sh:severity sh:Violation .

era-sh:SchematicOverviewOPApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:schematicOverviewOP ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OperationalPoint is defined and requires a schematic overview."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "schematicOverviewOP: This error is due to the OperationalPoint {?opLabel}, violating the rule: The schematicOverviewOP must be defined for each OperationalPoint when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?opLabel WHERE {
        $this rdfs:label ?opLabel .
        FILTER NOT EXISTS { $this era:schematicOverviewOP ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ShortestDistanceBetweenPantographsInContactWithOCL a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:shortestDistanceBetweenPantographsInContactWithOCL ;
    era:scope "local" ;

    rdfs:comment "Shortest distance between two pantographs in contact with the OCL, to be indicated for each energy supply system the vehicle is equipped for; to be indicated for single and, if applicable, multiple operation."@en ;
    # Source: ontology comment for shortestDistanceBetweenPantographsInContactWithOCL

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for shortestDistanceBetweenPantographsInContactWithOCL

    sh:message "shortestDistanceBetweenPantographsInContactWithOCL: The VehicleType must have at most one shortestDistanceBetweenPantographsInContactWithOCL value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:shortestDistanceBetweenPantographsInContactWithOCL ;

    sh:severity sh:Violation .

era-sh:ShortestDistanceBetweenPantographsInContactWithOCLApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:shortestDistanceBetweenPantographsInContactWithOCL ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the shortest distance between pantographs in contact with OCL."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "shortestDistanceBetweenPantographsInContactWithOCL: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The shortestDistanceBetweenPantographsInContactWithOCL must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:shortestDistanceBetweenPantographsInContactWithOCL ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SidingId a sh:PropertyShape ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:sidingId ;
    era:rinfIndex "1.2.2.0.0.2" ;
    era:scope "local" ;

    rdfs:comment "Unique siding identification or unique siding number within an Operational Point."@en ;
    # Source: ontology comment for sidingId

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for sidingId

    sh:message "sidingId: The Siding must have at most one sidingId value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:sidingId ;

    sh:severity sh:Violation .

era-sh:SidingIdApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:sidingId ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Siding is defined and requires a unique identification."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "sidingId: This error is due to the Siding {?sidingLabel}, violating the rule: The sidingId must be defined for each Siding when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sidingLabel WHERE {
        $this rdfs:label ?sidingLabel .
        FILTER NOT EXISTS { $this era:sidingId ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SignalId a sh:PropertyShape ;
    era:affectedClass era:Signal ;
    era:affectedProperty era:signalId ;
    era:scope "local" ;

    rdfs:comment "Identifier of signal."@en ;
    # Source: ontology comment for signalId

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for signalId

    sh:message "signalId: The Signal must have at most one signalId value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:signalId ;

    sh:severity sh:Violation .

era-sh:SignalIdApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Signal ;
    era:affectedProperty era:signalId ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Signal is defined and requires a unique identifier."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "signalId: This error is due to the Signal {?signalLabel}, violating the rule: The signalId must be defined for each Signal when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?signalLabel WHERE {
        $this rdfs:label ?signalLabel .
        FILTER NOT EXISTS { $this era:signalId ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SleepingPlaces a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:sleepingPlaces ;
    era:rinfIndex "4.12.1.3" ;
    era:scope "local" ;

    rdfs:comment "Number of sleeping places."@en ;
    # Source: ontology comment for sleepingPlaces

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for sleepingPlaces

    sh:message "sleepingPlaces: The VehicleType must have at most one sleepingPlaces value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:sleepingPlaces ;

    sh:severity sh:Violation .

era-sh:SleepingPlacesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:sleepingPlaces ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the number of sleeping places."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "sleepingPlaces: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The sleepingPlaces must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:sleepingPlaces ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SpecificInformation a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:specificInformation ; 
    era:rinfIndex "1.1.1.1.3.5.1" ; 
    era:scope "local" ; 

    rdfs:comment "Any relevant information from the IM relating to the line layout."@en ; 
    # Source: ontology comment for specificInformation

    sh:datatype xsd:string ; 
    # Source: ontology range is xsd:string

    sh:maxCount 1 ; 
    # Inferred: expected at most one value for specificInformation

    sh:message "specificInformation: The CommonCharacteristicsSubset must have at most one specificInformation value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ; 

    sh:path era:specificInformation ; 

    sh:severity sh:Violation .

era-sh:SpecificInformationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:specificInformation ; 
    era:scope "local" ; 

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires specific information related to the line layout."@en ; 
    # Source: inferred from the context of the ontology and RAG

    sh:message "specificInformation: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The specificInformation must be defined for each CommonCharacteristicsSubset when applicable."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:specificInformation ?value } .
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:StartOffsetFromOrigin a sh:PropertyShape ;
    era:affectedClass era:LinearElementSection ;
    era:affectedProperty era:startOffsetFromOrigin ;
    era:scope "local" ;

    rdfs:comment "Start offset of the section from the origin."@en ;
    # Source: ontology comment for startOffsetFromOrigin

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for startOffsetFromOrigin

    sh:message "startOffsetFromOrigin: The LinearElementSection must have at most one startOffsetFromOrigin value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:startOffsetFromOrigin ;

    sh:severity sh:Violation .

era-sh:StartOffsetFromOriginApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LinearElementSection ;
    era:affectedProperty era:startOffsetFromOrigin ;
    era:scope "local" ;

    rdfs:comment "Applicable when the LinearElementSection is defined and requires a start offset from the origin."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "startOffsetFromOrigin: This error is due to the LinearElementSection {?sectionLabel}, violating the rule: The startOffsetFromOrigin must be defined for each LinearElementSection when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sectionLabel WHERE {
        $this rdfs:label ?sectionLabel .
        FILTER NOT EXISTS { $this era:startOffsetFromOrigin ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:StartingNoiseLevel a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:startingNoiseLevel ;
    era:rinfIndex "4.11.4" ;
    era:scope "local" ;

    rdfs:comment "Starting noise level given in dB(A)."@en ;
    # Source: ontology comment for startingNoiseLevel

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for startingNoiseLevel

    sh:message "startingNoiseLevel: The VehicleType must have at most one startingNoiseLevel value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:startingNoiseLevel ;

    sh:severity sh:Violation .

era-sh:StartingNoiseLevelApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:startingNoiseLevel ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the starting noise level."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "startingNoiseLevel: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The startingNoiseLevel must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:startingNoiseLevel ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:StaticAxleLoadExceptionalPayload a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:staticAxleLoadExceptionalPayload ;
    era:rinfIndex "4.5.3.3" ;
    era:scope "local" ;

    rdfs:comment "Static axle load under exceptional payload."@en ;
    # Source: ontology comment for staticAxleLoadExceptionalPayload

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for staticAxleLoadExceptionalPayload

    sh:message "staticAxleLoadExceptionalPayload: The VehicleType must have at most one staticAxleLoadExceptionalPayload value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:staticAxleLoadExceptionalPayload ;

    sh:severity sh:Violation .

era-sh:StaticAxleLoadExceptionalPayloadApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:staticAxleLoadExceptionalPayload ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the static axle load under exceptional payload."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "staticAxleLoadExceptionalPayload: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The staticAxleLoadExceptionalPayload must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:staticAxleLoadExceptionalPayload ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:StaticAxleLoadNormalPayload a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:staticAxleLoadNormalPayload ;
    era:rinfIndex "4.5.3.2" ;
    era:scope "local" ;

    rdfs:comment "Static axle load under normal payload."@en ;
    # Source: ontology comment for staticAxleLoadNormalPayload

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for staticAxleLoadNormalPayload

    sh:message "staticAxleLoadNormalPayload: The VehicleType must have at most one staticAxleLoadNormalPayload value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:staticAxleLoadNormalPayload ;

    sh:severity sh:Violation .

era-sh:StaticAxleLoadNormalPayloadApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:staticAxleLoadNormalPayload ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the static axle load under normal payload."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "staticAxleLoadNormalPayload: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The staticAxleLoadNormalPayload must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:staticAxleLoadNormalPayload ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:StaticAxleLoadWorkingOrder a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:staticAxleLoadWorkingOrder ;
    era:rinfIndex "4.5.3.1" ;
    era:scope "local" ;

    rdfs:comment "Static axle load in working order."@en ;
    # Source: ontology comment for staticAxleLoadWorkingOrder

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for staticAxleLoadWorkingOrder

    sh:message "staticAxleLoadWorkingOrder: The VehicleType must have at most one staticAxleLoadWorkingOrder value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:staticAxleLoadWorkingOrder ;

    sh:severity sh:Violation .

era-sh:StaticAxleLoadWorkingOrderApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:staticAxleLoadWorkingOrder ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the static axle load in working order."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "staticAxleLoadWorkingOrder: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The staticAxleLoadWorkingOrder must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:staticAxleLoadWorkingOrder ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:StationaryNoiseLevel a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:stationaryNoiseLevel ;
    era:rinfIndex "4.11.3" ;
    era:scope "local" ;

    rdfs:comment "Stationary noise level given in dB(A)."@en ;
    # Source: ontology comment for stationaryNoiseLevel

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for stationaryNoiseLevel

    sh:message "stationaryNoiseLevel: The VehicleType must have at most one stationaryNoiseLevel value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:stationaryNoiseLevel ;

    sh:severity sh:Violation .

era-sh:StationaryNoiseLevelApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:stationaryNoiseLevel ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the stationary noise level."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "stationaryNoiseLevel: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The stationaryNoiseLevel must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:stationaryNoiseLevel ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:StructuralCategory a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:structuralCategory ;
    era:rinfIndex "4.1.10" ;
    era:scope "local" ;

    rdfs:comment "Structural category."@en ;
    # Source: ontology comment for structuralCategory

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for structuralCategory

    sh:message "structuralCategory: The VehicleType must have at most one structuralCategory value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:structuralCategory ;

    sh:severity sh:Violation .

era-sh:StructuralCategoryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:structuralCategory ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the structural category."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "structuralCategory: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The structuralCategory must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:structuralCategory ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:StructureCheckLocation a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:structureCheckLocation ; 
    era:rinfIndex "1.1.1.1.2.4.3" ; 
    era:scope "local" ; 

    rdfs:comment "Localisation of structures requiring specific checks."@en ; 
    # Source: ontology comment for structureCheckLocation

    sh:datatype xsd:double ; 
    # Source: ontology range is xsd:double

    sh:maxCount 1 ; 
    # Inferred: expected at most one value for structureCheckLocation

    sh:message "structureCheckLocation: The CommonCharacteristicsSubset must have at most one structureCheckLocation value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ; 

    sh:path era:structureCheckLocation ; 

    sh:severity sh:Violation .

era-sh:StructureCheckLocationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:structureCheckLocation ; 
    era:scope "local" ; 

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a location for structures needing specific checks."@en ; 
    # Source: inferred from the context of the ontology and RAG

    sh:message "structureCheckLocation: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The structureCheckLocation must be defined for each CommonCharacteristicsSubset when applicable."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:structureCheckLocation ?value } .
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:SubsetName a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:subsetName ;
    era:scope "local" ;

    rdfs:comment "A subset of elements with common characteristics could be identified with a unique name/id."@en ;
    # Source: ontology comment for subsetName

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for subsetName

    sh:message "subsetName: The CommonCharacteristicsSubset must have at most one subsetName value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:subsetName ;

    sh:severity sh:Violation .

era-sh:SubsetNameApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:subsetName ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a unique name/id."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "subsetName: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The subsetName must be defined for each CommonCharacteristicsSubset when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:subsetName ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SubsidiaryLocationCode a sh:PropertyShape ;
    era:affectedClass era:SubsidiaryLocation ;
    era:affectedProperty era:subsidiaryLocationCode ;
    era:scope "local" ;

    rdfs:comment "The numeric code for the subsidiary location."@en ;
    # Source: ontology comment for subsidiaryLocationCode

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for subsidiaryLocationCode

    sh:message "subsidiaryLocationCode: The SubsidiaryLocation must have at most one subsidiaryLocationCode value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:subsidiaryLocationCode ;

    sh:severity sh:Violation .

era-sh:SubsidiaryLocationCodeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SubsidiaryLocation ;
    era:affectedProperty era:subsidiaryLocationCode ;
    era:scope "local" ;

    rdfs:comment "Applicable when the SubsidiaryLocation is defined and requires a subsidiary location code."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "subsidiaryLocationCode: This error is due to the SubsidiaryLocation {?locationLabel}, violating the rule: The subsidiaryLocationCode must be defined for each SubsidiaryLocation when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?locationLabel WHERE {
        $this rdfs:label ?locationLabel .
        FILTER NOT EXISTS { $this era:subsidiaryLocationCode ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SubsidiaryLocationName a sh:PropertyShape ;
    era:affectedClass era:SubsidiaryLocation ;
    era:affectedProperty era:subsidiaryLocationName ;
    era:scope "local" ;

    rdfs:comment "The common name given to the subsidiary location."@en ;
    # Source: ontology comment for subsidiaryLocationName

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for subsidiaryLocationName

    sh:message "subsidiaryLocationName: The SubsidiaryLocation must have at most one subsidiaryLocationName value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:subsidiaryLocationName ;

    sh:severity sh:Violation .

era-sh:SubsidiaryLocationNameApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SubsidiaryLocation ;
    era:affectedProperty era:subsidiaryLocationName ;
    era:scope "local" ;

    rdfs:comment "Applicable when the SubsidiaryLocation is defined and requires a common name."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "subsidiaryLocationName: This error is due to the SubsidiaryLocation {?locationLabel}, violating the rule: The subsidiaryLocationName must be defined for each SubsidiaryLocation when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?locationLabel WHERE {
        $this rdfs:label ?locationLabel .
        FILTER NOT EXISTS { $this era:subsidiaryLocationName ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SwitchProtectControlWarning a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:switchProtectControlWarning ;
    era:rinfIndex "1.1.1.3.8.1" ;
    era:scope "local" ;

    rdfs:comment "Indication whether a switch over between different systems whilst running exists."@en ;
    # Source: ontology comment for switchProtectControlWarning

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for switchProtectControlWarning

    sh:message "switchProtectControlWarning: The CommonCharacteristicsSubset must have at most one switchProtectControlWarning value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:switchProtectControlWarning ;

    sh:severity sh:Violation .

era-sh:SwitchProtectControlWarningApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:switchProtectControlWarning ;
    era:scope "local" ;

    rdfs:comment "Applicable when at least two different protection, control and warning systems exist."@en ;
    # Source: inferred from the dependency note in the ontology

    sh:message "switchProtectControlWarning: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The switchProtectControlWarning must be defined for each CommonCharacteristicsSubset when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:switchProtectControlWarning ?value } .
        # Check if at least two different systems exist
        FILTER EXISTS { 
            ?system1 era:protectionControlWarningSystem ?value1 .
            ?system2 era:protectionControlWarningSystem ?value2 .
            FILTER(?system1 != ?system2)
        }
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SwitchRadioSystem a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:switchRadioSystem ;
    era:rinfIndex "1.1.1.3.8.2" ;
    era:scope "local" ;

    rdfs:comment "Indication whether a switch over between different radio systems and no communication system whilst running exists."@en ;
    # Source: ontology comment for switchRadioSystem

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for switchRadioSystem

    sh:message "switchRadioSystem: The CommonCharacteristicsSubset must have at most one switchRadioSystem value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:switchRadioSystem ;

    sh:severity sh:Violation .

era-sh:SwitchRadioSystemApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:switchRadioSystem ;
    era:scope "local" ;

    rdfs:comment "Applicable when at least two different radio systems exist."@en ;
    # Source: dependency note in the ontology

    sh:message "switchRadioSystem: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The switchRadioSystem must be defined for each CommonCharacteristicsSubset when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:switchRadioSystem ?value } .
        # Check if at least two different radio systems exist
        FILTER EXISTS { 
            ?system1 era:radioSystem ?value1 .
            ?system2 era:radioSystem ?value2 .
            FILTER(?system1 != ?system2)
        }
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SystemSeparationInfoChangeSupplySystem a sh:PropertyShape ;
    era:affectedClass era:SystemSeparationInfo ;
    era:affectedProperty era:systemSeparationInfoChangeSupplySystem ;
    era:scope "local" ;

    rdfs:comment "Part of the system separation info of a track that shows whether the supply system has to be changed. The system separation info is the indication of required several information on system separation."@en ;
    # Source: ontology comment for systemSeparationInfoChangeSupplySystem

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for systemSeparationInfoChangeSupplySystem

    sh:message "systemSeparationInfoChangeSupplySystem: The SystemSeparationInfo must have at most one systemSeparationInfoChangeSupplySystem value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:systemSeparationInfoChangeSupplySystem ;

    sh:severity sh:Violation .

era-sh:SystemSeparationInfoChangeSupplySystemApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SystemSeparationInfo ;
    era:affectedProperty era:systemSeparationInfoChangeSupplySystem ;
    era:scope "local" ;

    rdfs:comment "Applicable when the SystemSeparationInfo is defined and requires information on whether the supply system has to be changed."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "systemSeparationInfoChangeSupplySystem: This error is due to the SystemSeparationInfo {?infoLabel}, violating the rule: The systemSeparationInfoChangeSupplySystem must be defined for each SystemSeparationInfo when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?infoLabel WHERE {
        $this rdfs:label ?infoLabel .
        FILTER NOT EXISTS { $this era:systemSeparationInfoChangeSupplySystem ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SystemSeparationInfoKm a sh:PropertyShape ;
    era:affectedClass era:SystemSeparationInfo ;
    era:affectedProperty era:systemSeparationInfoKm ;
    era:scope "local" ;

    rdfs:comment "Part of the system separation info of a track. Indicates the location from the start of the line where the new value is valid. The system separation info is the indication of required several information on system separation."@en ;
    # Source: ontology comment for systemSeparationInfoKm

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for systemSeparationInfoKm

    sh:message "systemSeparationInfoKm: The SystemSeparationInfo must have at most one systemSeparationInfoKm value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:systemSeparationInfoKm ;

    sh:severity sh:Violation .

era-sh:SystemSeparationInfoKmApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SystemSeparationInfo ;
    era:affectedProperty era:systemSeparationInfoKm ;
    era:scope "local" ;

    rdfs:comment "Applicable when the SystemSeparationInfo is defined and requires information on the location from the start of the line where the new value is valid."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "systemSeparationInfoKm: This error is due to the SystemSeparationInfo {?infoLabel}, violating the rule: The systemSeparationInfoKm must be defined for each SystemSeparationInfo when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?infoLabel WHERE {
        $this rdfs:label ?infoLabel .
        FILTER NOT EXISTS { $this era:systemSeparationInfoKm ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SystemSeparationInfoLength a sh:PropertyShape ;
    era:affectedClass era:SystemSeparationInfo ;
    era:affectedProperty era:systemSeparationInfoLength ;
    era:scope "local" ;

    rdfs:comment "Part of the system separation info of a track that shows the length of the system separation in metres."@en ;
    # Source: ontology comment for systemSeparationInfoLength

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for systemSeparationInfoLength

    sh:message "systemSeparationInfoLength: The SystemSeparationInfo must have at most one systemSeparationInfoLength value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:systemSeparationInfoLength ;

    sh:severity sh:Violation .

era-sh:SystemSeparationInfoLengthApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SystemSeparationInfo ;
    era:affectedProperty era:systemSeparationInfoLength ;
    era:scope "local" ;

    rdfs:comment "Applicable when the SystemSeparationInfo is defined and requires information on the length of the system separation."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "systemSeparationInfoLength: This error is due to the SystemSeparationInfo {?infoLabel}, violating the rule: The systemSeparationInfoLength must be defined for each SystemSeparationInfo when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?infoLabel WHERE {
        $this rdfs:label ?infoLabel .
        FILTER NOT EXISTS { $this era:systemSeparationInfoLength ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SystemSeparationInfoPantographLowered a sh:PropertyShape ;
    era:affectedClass era:SystemSeparationInfo ;
    era:affectedProperty era:systemSeparationInfoPantographLowered ;
    era:scope "local" ;

    rdfs:comment "Part of the system separation info of a track that shows whether the pantograph has to be lowered. The system separation info is the indication of required several information on system separation."@en ;
    # Source: ontology comment for systemSeparationInfoPantographLowered

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for systemSeparationInfoPantographLowered

    sh:message "systemSeparationInfoPantographLowered: The SystemSeparationInfo must have at most one systemSeparationInfoPantographLowered value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:systemSeparationInfoPantographLowered ;

    sh:severity sh:Violation .

era-sh:SystemSeparationInfoPantographLoweredApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SystemSeparationInfo ;
    era:affectedProperty era:systemSeparationInfoPantographLowered ;
    era:scope "local" ;

    rdfs:comment "Applicable when the SystemSeparationInfo is defined and requires information on whether the pantograph has to be lowered."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "systemSeparationInfoPantographLowered: This error is due to the SystemSeparationInfo {?infoLabel}, violating the rule: The systemSeparationInfoPantographLowered must be defined for each SystemSeparationInfo when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?infoLabel WHERE {
        $this rdfs:label ?infoLabel .
        FILTER NOT EXISTS { $this era:systemSeparationInfoPantographLowered ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SystemSeparationInfoSwitchOffBreaker a sh:PropertyShape ;
    era:affectedClass era:SystemSeparationInfo ;
    era:affectedProperty era:systemSeparationInfoSwitchOffBreaker ;
    era:scope "local" ;

    rdfs:comment "Part of the system separation info of a track that shows whether the breaker has to be switched off. The system separation info is the indication of required several information on system separation."@en ;
    # Source: ontology comment for systemSeparationInfoSwitchOffBreaker

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for systemSeparationInfoSwitchOffBreaker

    sh:message "systemSeparationInfoSwitchOffBreaker: The SystemSeparationInfo must have at most one systemSeparationInfoSwitchOffBreaker value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:systemSeparationInfoSwitchOffBreaker ;

    sh:severity sh:Violation .

era-sh:SystemSeparationInfoSwitchOffBreakerApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SystemSeparationInfo ;
    era:affectedProperty era:systemSeparationInfoSwitchOffBreaker ;
    era:scope "local" ;

    rdfs:comment "Applicable when the SystemSeparationInfo is defined and requires information on whether the breaker has to be switched off."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "systemSeparationInfoSwitchOffBreaker: This error is due to the SystemSeparationInfo {?infoLabel}, violating the rule: The systemSeparationInfoSwitchOffBreaker must be defined for each SystemSeparationInfo when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?infoLabel WHERE {
        $this rdfs:label ?infoLabel .
        FILTER NOT EXISTS { $this era:systemSeparationInfoSwitchOffBreaker ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TNvcontact a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:tNvcontact ;
    era:scope "local" ;

    rdfs:comment "Maximum time without a safe message from Radio Block Center before train reacts in seconds."@en ;
    # Source: ontology comment for tNvcontact

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for tNvcontact

    sh:message "tNvcontact: The CommonCharacteristicsSubset must have at most one tNvcontact value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:tNvcontact ;

    sh:severity sh:Violation .

era-sh:TNvcontactApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:tNvcontact ;
    era:scope "local" ;

    rdfs:comment "Applicable when ETCS is present and a value must be provided."@en ;
    # Source: dependency note in the ontology

    sh:message "tNvcontact: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The tNvcontact must be defined for each CommonCharacteristicsSubset when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:tNvcontact ?value } .
        # Check if ETCS is present
        FILTER EXISTS { 
            $this era:etcsNationalValuesDataParameter ?etcsValue .
        }
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TNvovtrp a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:tNvovtrp ;
    era:scope "local" ;

    rdfs:comment "Maximum time for overriding the train trip in seconds. According to the specification referenced in Appendix A-1, index [C]."@en ;
    # Source: ontology comment for tNvovtrp

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for tNvovtrp

    sh:message "tNvovtrp: The CommonCharacteristicsSubset must have at most one tNvovtrp value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:tNvovtrp ;

    sh:severity sh:Violation .

era-sh:TNvovtrpApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:tNvovtrp ;
    era:scope "local" ;

    rdfs:comment "Applicable when ETCS is present and a value has to be provided."@en ;
    # Source: dependency note in the ontology

    sh:message "tNvovtrp: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The tNvovtrp must be defined for each CommonCharacteristicsSubset when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:tNvovtrp ?value } .
        # Check if ETCS is present
        FILTER EXISTS { 
            $this era:etcsNationalValuesDataParameter ?etcsValue .
        }
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TAFTAPCode a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:tafTAPCode ;
    era:scope "local" ;

    rdfs:comment "Primary location code developed for information exchange in accordance with the TSIs relating to the telematics applications subsystem."@en ;
    # Source: ontology comment for tafTAPCode

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for tafTAPCode

    sh:message "tafTAPCode: The OperationalPoint must have at most one tafTAPCode value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:tafTAPCode ;

    sh:severity sh:Violation .

era-sh:TAFTAPCodeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:tafTAPCode ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OperationalPoint is defined and requires a TAF TAP code."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "tafTAPCode: This error is due to the OperationalPoint {?opLabel}, violating the rule: The tafTAPCode must be defined for each OperationalPoint when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?opLabel WHERE {
        $this rdfs:label ?opLabel .
        FILTER NOT EXISTS { $this era:tafTAPCode ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TenGISId a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:tenGISId ; 
    era:rinfIndex "1.1.1.1.2.1.2" ; 
    era:scope "local" ; 

    rdfs:comment "Indication of the GIS identity (GIS ID) of the section of TEN-T database to which the track belongs."@en ; 
    # Source: ontology comment for tenGISId

    sh:datatype xsd:string ; 
    # Source: ontology range is xsd:string

    sh:maxCount 1 ; 
    # Inferred: expected at most one value for tenGISId

    sh:message "tenGISId: The CommonCharacteristicsSubset must have at most one tenGISId value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ; 

    sh:path era:tenGISId ; 

    sh:severity sh:Violation .

era-sh:TenGISIdApplicability a sh:SPARQLConstraint ; 
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:tenGISId ; 
    era:scope "local" ; 

    rdfs:comment "Applicable if the answer to parameter 1.1.1.1.2.1 is not off TEN."@en ; 
    # Source: RAG indicates applicability based on TEN classification

    sh:message "tenGISId: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The tenGISId must be defined for each CommonCharacteristicsSubset when applicable."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:tenGISId ?value } .
        # Check if the classification indicates it is part of TEN
        FILTER EXISTS { 
            $this era:tenClassification ?classification .
            FILTER(?classification != "Off-TEN")
        }
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:ThermalCapacityDistance a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:thermalCapacityDistance ;
    era:rinfIndex "4.7.2.1.4" ;
    era:scope "local" ;

    rdfs:comment "Thermal capacity distance. If no reference case is indicated."@en ;
    # Source: ontology comment for thermalCapacityDistance

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for thermalCapacityDistance

    sh:message "thermalCapacityDistance: The VehicleType must have at most one thermalCapacityDistance value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:thermalCapacityDistance ;

    sh:severity sh:Violation .

era-sh:ThermalCapacityDistanceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:thermalCapacityDistance ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on thermal capacity distance."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "thermalCapacityDistance: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The thermalCapacityDistance must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:thermalCapacityDistance ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ThermalCapacityGradient a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:thermalCapacityGradient ;
    era:rinfIndex "4.7.2.1.3" ;
    era:scope "local" ;

    rdfs:comment "Thermal capacity gradient. If no reference case is indicated."@en ;
    # Source: ontology comment for thermalCapacityGradient

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for thermalCapacityGradient

    sh:message "thermalCapacityGradient: The VehicleType must have at most one thermalCapacityGradient value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:thermalCapacityGradient ;

    sh:severity sh:Violation .

era-sh:ThermalCapacityGradientApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:thermalCapacityGradient ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on thermal capacity gradient."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "thermalCapacityGradient: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The thermalCapacityGradient must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:thermalCapacityGradient ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ThermalCapacitySpeed a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:thermalCapacitySpeed ;
    era:rinfIndex "4.7.2.1.2" ;
    era:scope "local" ;

    rdfs:comment "Thermal capacity speed. If no reference case is indicated."@en ;
    # Source: ontology comment for thermalCapacitySpeed

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double

    sh:maxCount 1 ;
    # Inferred: expected at most one value for thermalCapacitySpeed

    sh:message "thermalCapacitySpeed: The VehicleType must have at most one thermalCapacitySpeed value that is a double. This error may be due to having more than one value or having a value that is not a double."@en ;

    sh:path era:thermalCapacitySpeed ;

    sh:severity sh:Violation .

era-sh:ThermalCapacitySpeedApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:thermalCapacitySpeed ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on thermal capacity speed."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "thermalCapacitySpeed: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The thermalCapacitySpeed must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:thermalCapacitySpeed ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ThermalCapacityTime a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:thermalCapacityTime ;
    era:rinfIndex "4.7.2.1.5" ;
    era:scope "local" ;

    rdfs:comment "Thermal capacity time. If no reference case is indicated."@en ;
    # Source: ontology comment for thermalCapacityTime

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for thermalCapacityTime

    sh:message "thermalCapacityTime: The VehicleType must have at most one thermalCapacityTime value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:thermalCapacityTime ;

    sh:severity sh:Violation .

era-sh:ThermalCapacityTimeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:thermalCapacityTime ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on thermal capacity time."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "thermalCapacityTime: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The thermalCapacityTime must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:thermalCapacityTime ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TotalVehicleMass a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:totalVehicleMass ;
    era:scope "local" ;

    rdfs:comment "Total vehicle mass (for each vehicle of the unit), given in kg."@en ;
    # Source: ontology comment for totalVehicleMass

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for totalVehicleMass

    sh:message "totalVehicleMass: The VehicleType must have at most one totalVehicleMass value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:totalVehicleMass ;

    sh:severity sh:Violation .

era-sh:TotalVehicleMassApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:totalVehicleMass ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on total vehicle mass."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "totalVehicleMass: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The totalVehicleMass must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:totalVehicleMass ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TrackId a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:trackId ;
    era:scope "local" ;

    rdfs:comment "Unique track identification or unique track number within operational point or section of line."@en ;
    # Source: ontology comment for trackId

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for trackId

    sh:message "trackId: The RunningTrack must have at most one trackId value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:trackId ;

    sh:severity sh:Violation .

era-sh:TrackIdApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:trackId ;
    era:scope "local" ;

    rdfs:comment "Applicable when the RunningTrack is defined and requires a unique track identification."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "trackId: This error is due to the RunningTrack {?trackLabel}, violating the rule: The trackId must be defined for each RunningTrack when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:trackId ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TrainControlSwitchOverSpecialConditions a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:trainControlSwitchOverSpecialConditions ;
    era:scope "local" ;

    rdfs:comment "Special conditions implemented on-board to switch over between different train protection control and warning systems. Given as combination of systems installed on board ('System XX'_'System YY')."@en ;
    # Source: ontology comment for trainControlSwitchOverSpecialConditions

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for trainControlSwitchOverSpecialConditions

    sh:message "trainControlSwitchOverSpecialConditions: The VehicleType must have at most one trainControlSwitchOverSpecialConditions value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:trainControlSwitchOverSpecialConditions ;

    sh:severity sh:Violation .

era-sh:TrainControlSwitchOverSpecialConditionsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:trainControlSwitchOverSpecialConditions ;
    era:scope "local" ;

    rdfs:comment "Applicable when at least two different protection, control and warning systems exist."@en ;
    # Source: RAG indicates applicability based on the existence of multiple systems

    sh:message "trainControlSwitchOverSpecialConditions: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The trainControlSwitchOverSpecialConditions must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:trainControlSwitchOverSpecialConditions ?value } .
        # Check if at least two different systems exist
        FILTER EXISTS { 
            $this era:otherTrainProtectionControlWarningSystems ?systemValue .
        }
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TrainIntegrityOnBoardRequired a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:trainIntegrityOnBoardRequired ; 
    era:rinfIndex "1.1.1.3.2.8" ; 
    era:scope "local" ; 

    rdfs:comment "Indication whether train confirmation from on-board is required to access the line for safety reasons."@en ; 
    # Source: ontology comment for trainIntegrityOnBoardRequired

    sh:datatype xsd:string ; 
    # Source: ontology range is xsd:string

    sh:maxCount 1 ; 
    # Inferred: expected at most one value for trainIntegrityOnBoardRequired

    sh:message "trainIntegrityOnBoardRequired: The CommonCharacteristicsSubset must have at most one trainIntegrityOnBoardRequired value that is a string (Y/N/NYA). This error may be due to having more than one value or having a value that is not Y/N/NYA."@en ; 

    sh:path era:trainIntegrityOnBoardRequired ; 

    sh:severity sh:Violation .

era-sh:TrainIntegrityOnBoardRequiredApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:trainIntegrityOnBoardRequired ; 
    era:scope "local" ; 

    rdfs:comment "Applicable only when selected value for 1.1.1.3.2.1 is ‘3’."@en ; 
    # Source: RAG indicates applicability based on the value of another parameter

    sh:message "trainIntegrityOnBoardRequired: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The trainIntegrityOnBoardRequired must be defined for each CommonCharacteristicsSubset when applicable."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:trainIntegrityOnBoardRequired ?value } .
        # Check if the selected value for 1.1.1.3.2.1 is '3'
        FILTER EXISTS { 
            $this era:someOtherParameter ?paramValue .
            FILTER(?paramValue = "3")
        }
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:TransportableOnFerry a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:transportableOnFerry ;
    era:scope "local" ;

    rdfs:comment "Indicates the suitability for transport on ferries."@en ;
    # Source: ontology comment for transportableOnFerry

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for transportableOnFerry

    sh:message "transportableOnFerry: The VehicleType must have at most one transportableOnFerry value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:transportableOnFerry ;

    sh:severity sh:Violation .

era-sh:TransportableOnFerryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:transportableOnFerry ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on suitability for transport on ferries."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "transportableOnFerry: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The transportableOnFerry must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:transportableOnFerry ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TSIMagneticFields a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:tsiMagneticFields ; 
    era:rinfIndex "1.1.1.3.9.1" ; 
    era:scope "local" ; 

    rdfs:comment "Indication whether rules exist and are compliant with the TSI."@en ; 
    # Source: ontology comment for tsiMagneticFields

    sh:datatype xsd:boolean ; 
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ; 
    # Inferred: expected at most one value for tsiMagneticFields

    sh:message "tsiMagneticFields: The CommonCharacteristicsSubset must have at most one tsiMagneticFields value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ; 

    sh:path era:tsiMagneticFields ; 

    sh:severity sh:Violation .

era-sh:TSIMagneticFieldsApplicability a sh:SPARQLConstraint ; 
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:tsiMagneticFields ; 
    era:scope "local" ; 

    rdfs:comment "Applicable (‘Y’) only when for parameter 1.1.1.3.7.1 the selected option is ‘wheel detector’."@en ; 
    # Source: RAG indicates applicability based on the selected option for another parameter

    sh:message "tsiMagneticFields: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The tsiMagneticFields must be defined for each CommonCharacteristicsSubset when applicable."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:tsiMagneticFields ?value } .
        # Check if the selected option for parameter 1.1.1.3.7.1 is 'wheel detector'
        FILTER EXISTS { 
            $this era:someOtherParameter ?paramValue .
            FILTER(?paramValue = "wheel detector")
        }
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:TSISwitchCrossing a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:tsiSwitchCrossing ;
    era:rinfIndex "1.1.1.1.5.1" ;
    era:scope "local" ;

    rdfs:comment "Switches and crossings are maintained to in service limit dimension as specified in TSI."@en ;
    # Source: ontology comment for tsiSwitchCrossing

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for tsiSwitchCrossing

    sh:message "tsiSwitchCrossing: The CommonCharacteristicsSubset must have at most one tsiSwitchCrossing value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:tsiSwitchCrossing ;

    sh:severity sh:Violation .

era-sh:TSISwitchCrossingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:tsiSwitchCrossing ;
    era:scope "local" ;

    rdfs:comment "Applicable if for existing track at least one parameter has less strict value than specified in the TSI, then 'N' shall be selected."@en ;
    # Source: RAG indicates applicability based on the compliance with TSI

    sh:message "tsiSwitchCrossing: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The tsiSwitchCrossing must be defined for each CommonCharacteristicsSubset when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:tsiSwitchCrossing ?value } .
        # Check if at least one parameter has less strict value than specified in the TSI
        FILTER EXISTS { 
            $this era:someOtherParameter ?paramValue .
            FILTER(?paramValue = "N")
        }
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TSITractionHarmonics a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:tsiTractionHarmonics ;
    era:rinfIndex "1.1.1.3.9.2" ;
    era:scope "local" ;

    rdfs:comment "Indication whether rules exist and are compliant with the TSI."@en ;
    # Source: ontology comment for tsiTractionHarmonics

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for tsiTractionHarmonics

    sh:message "tsiTractionHarmonics: The CommonCharacteristicsSubset must have at most one tsiTractionHarmonics value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:tsiTractionHarmonics ;

    sh:severity sh:Violation .

era-sh:TSITractionHarmonicsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:tsiTractionHarmonics ;
    era:scope "local" ;

    rdfs:comment "Applicable (‘Y’) only when for parameter 1.1.1.3.7.1 the selected option is ‘wheel detector’ or ‘track circuit’."@en ;
    # Source: RAG indicates applicability based on the selected option for another parameter

    sh:message "tsiTractionHarmonics: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The tsiTractionHarmonics must be defined for each CommonCharacteristicsSubset when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:tsiTractionHarmonics ?value } .
        # Check if the selected option for parameter 1.1.1.3.7.1 is 'wheel detector' or 'track circuit'
        FILTER EXISTS { 
            $this era:someOtherParameter ?paramValue .
            FILTER(?paramValue IN ("wheel detector", "track circuit"))
        }
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TunnelIdentification a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:tunnelIdentification ;
    era:rinfIndex "1.2.2.0.5.2" ;
    era:scope "local" ;

    rdfs:comment "Unique tunnel identification or unique number within Member State."@en ;
    # Source: RAG definition for tunnel identification

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for tunnelIdentification

    sh:message "tunnelIdentification: The Tunnel must have at most one tunnelIdentification value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:tunnelIdentification ;

    sh:severity sh:Violation .

era-sh:TunnelIdentificationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:tunnelIdentification ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Tunnel is defined and requires a unique identification."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "tunnelIdentification: This error is due to the Tunnel {?tunnelLabel}, violating the rule: The tunnelIdentification must be defined for each Tunnel when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:tunnelIdentification ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TypeVersionNumber a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:typeVersionNumber ;
    era:scope "local" ;

    rdfs:comment "Serial number that identifies a vehicle type."@en ;
    # Source: ontology comment for typeVersionNumber

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for typeVersionNumber

    sh:message "typeVersionNumber: The VehicleType must have at most one typeVersionNumber value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:typeVersionNumber ;

    sh:severity sh:Violation .

era-sh:TypeVersionNumberApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:typeVersionNumber ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires a type version number."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "typeVersionNumber: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The typeVersionNumber must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:typeVersionNumber ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Umax2 a sh:PropertyShape ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:umax2 ;
    era:scope "local" ;

    rdfs:comment "Highest non-permanent voltage (Umax2) for France on lines not compliant with values defined in the EN50163:2004+A1:2007+A2:2020+A3:2022."@en ;
    # Source: ontology comment for umax2

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for umax2

    sh:message "umax2: The ContactLineSystem must have at most one umax2 value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:umax2 ;

    sh:severity sh:Violation .

era-sh:Umax2Applicability a sh:SPARQLConstraint ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:umax2 ;
    era:scope "local" ;

    rdfs:comment "Applicable when the ContactLineSystem is defined and requires information on the highest non-permanent voltage."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "umax2: This error is due to the ContactLineSystem {?contactLineLabel}, violating the rule: The umax2 must be defined for each ContactLineSystem when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?contactLineLabel WHERE {
        $this rdfs:label ?contactLineLabel .
        FILTER NOT EXISTS { $this era:umax2 ?value } .
        # Check if the parameter 'not electrified' is chosen
        FILTER NOT EXISTS { 
            $this era:someOtherParameter ?paramValue .
            FILTER(?paramValue = "not electrified")
        }
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Uopid a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:uopid ;
    era:scope "local" ;

    rdfs:comment "Code composed of country code and alphanumeric operational point code."@en ;
    # Source: ontology comment for uopid

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for uopid

    sh:message "uopid: The OperationalPoint must have at most one uopid value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:uopid ;

    sh:severity sh:Violation .

era-sh:UopidApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:uopid ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OperationalPoint is defined and requires a unique OP ID."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "uopid: This error is due to the OperationalPoint {?opLabel}, violating the rule: The uopid must be defined for each OperationalPoint when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?opLabel WHERE {
        $this rdfs:label ?opLabel .
        FILTER NOT EXISTS { $this era:uopid ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:UsesGroup555 a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:usesGroup555 ;
    era:scope "local" ;

    rdfs:comment "Indication if group 555 is used."@en ;
    # Source: ontology comment for usesGroup555

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean

    sh:maxCount 1 ;
    # Inferred: expected at most one value for usesGroup555

    sh:message "usesGroup555: The CommonCharacteristicsSubset must have at most one usesGroup555 value that is a boolean. This error may be due to having more than one value or having a value that is not a boolean."@en ;

    sh:path era:usesGroup555 ;

    sh:severity sh:Violation .

era-sh:UsesGroup555Applicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:usesGroup555 ;
    era:scope "local" ;

    rdfs:comment "Applicable when GSM-R and ETCS L2 must be installed for this parameter to be applicable."@en ;
    # Source: RAG indicates applicability based on the installation of specific systems

    sh:message "usesGroup555: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The usesGroup555 must be defined for each CommonCharacteristicsSubset when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:usesGroup555 ?value } .
        # Check if GSM-R and ETCS L2 are installed
        FILTER EXISTS { 
            $this era:someOtherParameter ?paramValue .
            FILTER(?paramValue IN ("GSM-R", "ETCS L2"))
        }
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VNallowovtrp a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:vNvallowovtrp ;
    era:rinfIndex "1.1.1.3.2.16.3" ; 
    era:scope "local" ;

    rdfs:comment "Speed limit allowing the driver to select the override function in km/h according to the specification referenced in Appendix A-1, index [C]."@en ;
    # Source: ontology comment for vNvallowovtrp

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for vNvallowovtrp

    sh:message "vNvallowovtrp: The CommonCharacteristicsSubset must have at most one vNvallowovtrp value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:vNvallowovtrp ;

    sh:severity sh:Violation .

era-sh:VNallowovtrpApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:vNvallowovtrp ;
    era:scope "local" ;

    rdfs:comment "Applicable when ETCS is present; a value has to be provided when ETCS is present."@en ;
    # Source: dependency note in ontology

    sh:message "vNvallowovtrp: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The vNvallowovtrp must be defined for each CommonCharacteristicsSubset when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:vNvallowovtrp ?value } .
        # Check if ETCS is present
        FILTER EXISTS { 
            $this era:someOtherParameter ?paramValue .
            FILTER(?paramValue = "ETCS")
        }
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VNvsupovtrp a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:vNvsupovtrp ;
    era:rinfIndex "1.1.1.3.2.16.4" ;
    era:scope "local" ;

    rdfs:comment "Override speed limit to be supervised when the 'override' function is active in km/h."@en ;
    # Source: ontology comment for vNvsupovtrp

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for vNvsupovtrp

    sh:message "vNvsupovtrp: The CommonCharacteristicsSubset must have at most one vNvsupovtrp value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:vNvsupovtrp ;

    sh:severity sh:Violation .

era-sh:VNvsupovtrpApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:vNvsupovtrp ;
    era:scope "local" ;

    rdfs:comment "Applicable when ETCS is present; a value has to be provided when ETCS is present."@en ;
    # Source: dependency note in ontology

    sh:message "vNvsupovtrp: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The vNvsupovtrp must be defined for each CommonCharacteristicsSubset when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:vNvsupovtrp ?value } .
        # Check if ETCS is present
        FILTER EXISTS { 
            $this era:someOtherParameter ?paramValue .
            FILTER(?paramValue = "ETCS")
        }
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ValidityEndDate a sh:PropertyShape ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:validityEndDate ;
    era:scope "local" ;

    rdfs:comment "The validity end date for the infrastructure element, indicating when the current data is no longer applicable."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:datatype xsd:date ;
    # Source: ontology range is xsd:date

    sh:maxCount 1 ;
    # Inferred: expected at most one validity end date

    sh:message "validityEndDate: The InfrastructureElement must have at most one validityEndDate value that is a date. This error may be due to having more than one value or having a value that is not a date."@en ;

    sh:path era:validityEndDate ;

    sh:severity sh:Violation .

era-sh:ValidityEndDateApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:validityEndDate ;
    era:scope "local" ;

    rdfs:comment "Applicable when the InfrastructureElement is defined and requires a validity end date."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "validityEndDate: This error is due to the InfrastructureElement {?elementLabel}, violating the rule: The validityEndDate must be defined for each InfrastructureElement when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?elementLabel WHERE {
        $this rdfs:label ?elementLabel .
        FILTER NOT EXISTS { $this era:validityEndDate ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ValidityStartDate a sh:PropertyShape ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:validityStartDate ;
    era:scope "local" ;

    rdfs:comment "The validity start date for the infrastructure element, indicating when the current data becomes applicable."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:datatype xsd:date ;
    # Source: ontology range is xsd:date

    sh:maxCount 1 ;
    # Inferred: expected at most one validity start date

    sh:message "validityStartDate: The InfrastructureElement must have at most one validityStartDate value that is a date. This error may be due to having more than one value or having a value that is not a date."@en ;

    sh:path era:validityStartDate ;

    sh:severity sh:Violation .

era-sh:ValidityStartDateApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:validityStartDate ;
    era:scope "local" ;

    rdfs:comment "Applicable when the InfrastructureElement is defined and requires a validity start date."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "validityStartDate: This error is due to the InfrastructureElement {?elementLabel}, violating the rule: The validityStartDate must be defined for each InfrastructureElement when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?elementLabel WHERE {
        $this rdfs:label ?elementLabel .
        FILTER NOT EXISTS { $this era:validityStartDate ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehicleContactForce a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehicleContactForce ;
    era:rinfIndex "4.10.15" ;
    era:scope "local" ;

    rdfs:comment "Mean contact force."@en ;
    # Source: ontology comment for vehicleContactForce

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for vehicleContactForce

    sh:message "vehicleContactForce: The VehicleType must have at most one vehicleContactForce value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:vehicleContactForce ;

    sh:severity sh:Violation .

era-sh:VehicleContactForceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehicleContactForce ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the mean contact force."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "vehicleContactForce: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The vehicleContactForce must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:vehicleContactForce ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehicleKinematicGaugeOther a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehicleKinematicGaugeOther ;
    era:rinfIndex "4.2.2" ;
    era:scope "local" ;

    rdfs:comment "Vehicle kinematic gauge that corresponds to other gauges assessed using the kinematic method."@en ;
    # Source: ontology comment for vehicleKinematicGaugeOther

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for vehicleKinematicGaugeOther

    sh:message "vehicleKinematicGaugeOther: The VehicleType must have at most one vehicleKinematicGaugeOther value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:vehicleKinematicGaugeOther ;

    sh:severity sh:Violation .

era-sh:VehicleKinematicGaugeOtherApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehicleKinematicGaugeOther ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the kinematic gauge."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "vehicleKinematicGaugeOther: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The vehicleKinematicGaugeOther must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:vehicleKinematicGaugeOther ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehicleMaxSandingOutput a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehicleMaxSandingOutput ;
    era:rinfIndex "4.14.2.13" ;
    era:scope "local" ;

    rdfs:comment "Vehicle Maximum sanding output, given in grams per second."@en ;
    # Source: ontology comment for vehicleMaxSandingOutput

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for vehicleMaxSandingOutput

    sh:message "vehicleMaxSandingOutput: The VehicleType must have at most one vehicleMaxSandingOutput value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:vehicleMaxSandingOutput ;

    sh:severity sh:Violation .

era-sh:VehicleMaxSandingOutputApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehicleMaxSandingOutput ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the maximum sanding output."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "vehicleMaxSandingOutput: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The vehicleMaxSandingOutput must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:vehicleMaxSandingOutput ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehicleNumber a sh:PropertyShape ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:vehicleNumber ;
    era:scope "local" ;

    rdfs:comment "Identification number of a vehicle or wagon."@en ;
    # Source: ontology comment for vehicleNumber

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for vehicleNumber

    sh:message "vehicleNumber: The Vehicle must have at most one vehicleNumber value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:vehicleNumber ;

    sh:severity sh:Violation .

era-sh:VehicleNumberApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:vehicleNumber ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Vehicle is defined and requires a unique identification number."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "vehicleNumber: This error is due to the Vehicle {?vehicleLabel}, violating the rule: The vehicleNumber must be defined for each Vehicle when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:vehicleNumber ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehiclePantographHead a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehiclePantographHead ;
    era:rinfIndex "4.10.6" ;
    era:scope "local" ;

    rdfs:comment "Pantograph head geometry (to be indicated for each energy supply system the vehicle is equipped for)."@en ;
    # Source: ontology comment for vehiclePantographHead

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for vehiclePantographHead

    sh:message "vehiclePantographHead: The VehicleType must have at most one vehiclePantographHead value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:vehiclePantographHead ;

    sh:severity sh:Violation .

era-sh:VehiclePantographHeadApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehiclePantographHead ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the pantograph head geometry."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "vehiclePantographHead: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The vehiclePantographHead must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:vehiclePantographHead ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehicleSeries a sh:PropertyShape ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:vehicleSeries ;
    era:scope "local" ;

    rdfs:comment "Manufacturing series of a vehicle."@en ;
    # Source: ontology comment for vehicleSeries

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for vehicleSeries

    sh:message "vehicleSeries: The Vehicle must have at most one vehicleSeries value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:vehicleSeries ;

    sh:severity sh:Violation .

era-sh:VehicleSeriesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:vehicleSeries ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Vehicle is defined and requires information on the manufacturing series."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "vehicleSeries: This error is due to the Vehicle {?vehicleLabel}, violating the rule: The vehicleSeries must be defined for each Vehicle when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:vehicleSeries ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehicleTypeMaximumCantDeficiency a sh:PropertyShape ;
    era:affectedClass era:MaximumSpeedAndCantDeficiency ;
    era:affectedProperty era:vehicleTypeMaximumCantDeficiency ;
    era:rinfIndex "4.6.4" ;
    era:scope "local" ;

    rdfs:comment "Maximum cant deficiency expressed in millimetres defined as difference between the applied cant and a higher equilibrium cant the line has been designed for."@en ;
    # Source: ontology comment for vehicleTypeMaximumCantDeficiency

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for vehicleTypeMaximumCantDeficiency

    sh:message "vehicleTypeMaximumCantDeficiency: The MaximumSpeedAndCantDeficiency must have at most one vehicleTypeMaximumCantDeficiency value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:vehicleTypeMaximumCantDeficiency ;

    sh:severity sh:Violation .

era-sh:VehicleTypeMaximumCantDeficiencyApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:MaximumSpeedAndCantDeficiency ;
    era:affectedProperty era:vehicleTypeMaximumCantDeficiency ;
    era:scope "local" ;

    rdfs:comment "Applicable when the MaximumSpeedAndCantDeficiency is defined and requires information on the maximum cant deficiency."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "vehicleTypeMaximumCantDeficiency: This error is due to the MaximumSpeedAndCantDeficiency {?elementLabel}, violating the rule: The vehicleTypeMaximumCantDeficiency must be defined for each MaximumSpeedAndCantDeficiency when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?elementLabel WHERE {
        $this rdfs:label ?elementLabel .
        FILTER NOT EXISTS { $this era:vehicleTypeMaximumCantDeficiency ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehicleTypeMaximumSpeed a sh:PropertyShape ;
    era:affectedClass era:MaximumSpeedAndCantDeficiency ;
    era:affectedProperty era:vehicleTypeMaximumSpeed ;
    era:rinfIndex "4.6.4" ;
    era:scope "local" ;

    rdfs:comment "Part of the combination of maximum speed and maximum cant deficiency for which the vehicle was assessed. Corresponds to the maximum speed."@en ;
    # Source: ontology comment for vehicleTypeMaximumSpeed

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for vehicleTypeMaximumSpeed

    sh:message "vehicleTypeMaximumSpeed: The MaximumSpeedAndCantDeficiency must have at most one vehicleTypeMaximumSpeed value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:vehicleTypeMaximumSpeed ;

    sh:severity sh:Violation .

era-sh:VehicleTypeMaximumSpeedApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:MaximumSpeedAndCantDeficiency ;
    era:affectedProperty era:vehicleTypeMaximumSpeed ;
    era:scope "local" ;

    rdfs:comment "Applicable when the MaximumSpeedAndCantDeficiency is defined and requires information on the maximum speed."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "vehicleTypeMaximumSpeed: This error is due to the MaximumSpeedAndCantDeficiency {?elementLabel}, violating the rule: The vehicleTypeMaximumSpeed must be defined for each MaximumSpeedAndCantDeficiency when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?elementLabel WHERE {
        $this rdfs:label ?elementLabel .
        FILTER NOT EXISTS { $this era:vehicleTypeMaximumSpeed ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehiclesComposingFixedFormation a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehiclesComposingFixedFormation ;
    era:rinfIndex "4.1.12" ;
    era:scope "local" ;

    rdfs:comment "Number of vehicles composing the fixed formation (for fixed formation only). Note: the value is mandatory for all vehicle categories. If the vehicle is composed of only one car, the indicated value shall be '1'."@en ;
    # Source: ontology comment for vehiclesComposingFixedFormation

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for vehiclesComposingFixedFormation

    sh:message "vehiclesComposingFixedFormation: The VehicleType must have at most one vehiclesComposingFixedFormation value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:vehiclesComposingFixedFormation ;

    sh:severity sh:Violation .

era-sh:VehiclesComposingFixedFormationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehiclesComposingFixedFormation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the number of vehicles composing the fixed formation."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "vehiclesComposingFixedFormation: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The vehiclesComposingFixedFormation must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:vehiclesComposingFixedFormation ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VerificationCCS a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:verificationCCS ;
    era:rinfIndex "1.1.1.3.1.1" ;
    era:scope "local" ;

    rdfs:comment "Unique number for EC declarations in accordance with Commission Implementing Regulation (EU) 2019/250."@en ;
    # Source: ontology comment for verificationCCS

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for verificationCCS

    sh:pattern "^[A-Z]{2}/[0-9]{13}/[0-9]{4}/[0-9]{6}$" ;
    # Source: RAG specifies the format as CC/RRRRRRRRRRRRRR/YYYY/NNNNNN

    sh:message "verificationCCS: The CommonCharacteristicsSubset must have at most one verificationCCS value that matches the format CC/RRRRRRRRRRRRRR/YYYY/NNNNNN. This error may be due to having more than one value or having a value that does not match the required format."@en ;

    sh:path era:verificationCCS ;

    sh:severity sh:Violation .

era-sh:VerificationCCSApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:verificationCCS ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a verification number."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "verificationCCS: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The verificationCCS must be defined for each CommonCharacteristicsSubset when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:verificationCCS ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VerificationENE a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:verificationENE ; 
    era:rinfIndex "1.1.1.2.1.1" ; 
    era:scope "local" ;

    rdfs:comment "Unique number for EC declarations in accordance with Commission Implementing Regulation (EU) 2019/250."@en ; 
    # Source: ontology comment for verificationENE

    sh:datatype xsd:string ; 
    # Source: ontology range is xsd:string

    sh:maxCount 1 ; 
    # Inferred: expected at most one value for verificationENE

    sh:pattern "^[A-Z]{2}/[0-9]{13}/[0-9]{4}/[0-9]{6}$" ; 
    # Source: RAG specifies the format as CC/RRRRRRRRRRRRRR/YYYY/NNNNNN

    sh:message "verificationENE: The CommonCharacteristicsSubset must have at most one verificationENE value that matches the format CC/RRRRRRRRRRRRRR/YYYY/NNNNNN. This error may be due to having more than one value or having a value that does not match the required format."@en ;

    sh:path era:verificationENE ;

    sh:severity sh:Violation .

era-sh:VerificationENEApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:verificationENE ; 
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a verification number."@en ; 
    # Source: inferred from the context of the ontology and RAG

    sh:message "verificationENE: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The verificationENE must be defined for each CommonCharacteristicsSubset when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:verificationENE ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VerificationINF a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:verificationINF ;
    era:rinfIndex "1.1.1.1.1.1" ;
    era:scope "local" ;

    rdfs:comment "Unique number for EC declarations in accordance with Commission Implementing Regulation (EU) 2019/250."@en ;
    # Source: ontology comment for verificationINF

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for verificationINF

    sh:pattern "^[A-Z]{2}/[0-9]{13}/[0-9]{4}/[0-9]{6}$" ;
    # Source: RAG specifies the format as CC/RRRRRRRRRRRRRR/YYYY/NNNNNN

    sh:message "verificationINF: The CommonCharacteristicsSubset must have at most one verificationINF value that matches the format CC/RRRRRRRRRRRRRR/YYYY/NNNNNN. This error may be due to having more than one value or having a value that does not match the required format."@en ;

    sh:path era:verificationINF ;

    sh:severity sh:Violation .

era-sh:VerificationINFApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:verificationINF ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a verification number."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "verificationINF: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: The verificationINF must be defined for each CommonCharacteristicsSubset when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:verificationINF ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VerificationSRT a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:verificationSRT ;
    era:rinfIndex "1.1.1.1.8.5" ;
    era:scope "local" ;

    rdfs:comment "Unique number for EC declarations in accordance with Commission Implementing Regulation (EU) 2019/250."@en ;
    # Source: ontology comment for verificationSRT

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for verificationSRT

    sh:pattern "^[A-Z]{2}/[0-9]{13}/[0-9]{4}/[0-9]{6}$" ;
    # Source: RAG specifies the format as CC/RRRRRRRRRRRRRR/YYYY/NNNNNN

    sh:message "verificationSRT: The Tunnel must have at most one verificationSRT value that matches the format CC/RRRRRRRRRRRRRR/YYYY/NNNNNN. This error may be due to having more than one value or having a value that does not match the required format."@en ;

    sh:path era:verificationSRT ;

    sh:severity sh:Violation .

era-sh:VerificationSRTApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:verificationSRT ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Tunnel is defined and requires a verification number."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "verificationSRT: This error is due to the Tunnel {?tunnelLabel}, violating the rule: The verificationSRT must be defined for each Tunnel when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:verificationSRT ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VoiceOperationalCommImpl a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:voiceOperationalCommImpl ;
    era:scope "local" ;

    rdfs:comment "Voice and operational communication implementation."@en ;
    # Source: ontology comment for voiceOperationalCommImpl

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for voiceOperationalCommImpl

    sh:message "voiceOperationalCommImpl: The VehicleType must have at most one voiceOperationalCommImpl value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:voiceOperationalCommImpl ;

    sh:severity sh:Violation .

era-sh:VoiceOperationalCommImplApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:voiceOperationalCommImpl ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on voice and operational communication implementation."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "voiceOperationalCommImpl: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The voiceOperationalCommImpl must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:voiceOperationalCommImpl ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:WheelSetGaugeTransformationMethod a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:wheelSetGaugeTransformationMethod ;
    era:rinfIndex "4.1.3.b" ;
    era:scope "local" ;

    rdfs:comment "Wheel gauge transformation method. Defined if more than one wheel gauges have been selected."@en ;
    # Source: ontology comment for wheelSetGaugeTransformationMethod

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for wheelSetGaugeTransformationMethod

    sh:message "wheelSetGaugeTransformationMethod: The VehicleType must have at most one wheelSetGaugeTransformationMethod value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:wheelSetGaugeTransformationMethod ;

    sh:severity sh:Violation .

era-sh:WheelSetGaugeTransformationMethodApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:wheelSetGaugeTransformationMethod ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the wheel gauge transformation method."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "wheelSetGaugeTransformationMethod: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The wheelSetGaugeTransformationMethod must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:wheelSetGaugeTransformationMethod ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:WheelchairSleepingPlaces a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:wheelchairSleepingPlaces ;
    era:scope "local" ;

    rdfs:comment "Number of wheelchair accessible sleeping places."@en ;
    # Source: ontology comment for wheelchairSleepingPlaces

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: expected at most one value for wheelchairSleepingPlaces

    sh:message "wheelchairSleepingPlaces: The VehicleType must have at most one wheelchairSleepingPlaces value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:wheelchairSleepingPlaces ;

    sh:severity sh:Violation .

era-sh:WheelchairSleepingPlacesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:wheelchairSleepingPlaces ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the number of wheelchair accessible sleeping places."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "wheelchairSleepingPlaces: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The wheelchairSleepingPlaces must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:wheelchairSleepingPlaces ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:WheelchairSpaces a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:wheelchairSpaces ;
    era:rinfIndex "4.12.2.2" ;
    era:scope "local" ;

    rdfs:comment "Number of wheelchair spaces."@en ;
    # Source: ontology comment for wheelchairSpaces

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer

    sh:maxCount 1 ;
    # Inferred: expected at most one value for wheelchairSpaces

    sh:message "wheelchairSpaces: The VehicleType must have at most one wheelchairSpaces value that is an integer. This error may be due to having more than one value or having a value that is not an integer."@en ;

    sh:path era:wheelchairSpaces ;

    sh:severity sh:Violation .

era-sh:WheelchairSpacesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:wheelchairSpaces ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires information on the number of wheelchair spaces."@en ;
    # Source: inferred from the context of the ontology and RAG

    sh:message "wheelchairSpaces: This error is due to the VehicleType {?vehicleLabel}, violating the rule: The wheelchairSpaces must be defined for each VehicleType when applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:wheelchairSpaces ?value } .
    }
    """ ;

    sh:severity sh:Violation .