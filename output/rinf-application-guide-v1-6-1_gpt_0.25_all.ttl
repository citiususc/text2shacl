@prefix era: <http://data.europa.eu/949/> .
@prefix era-sh: <http://data.europa.eu/949/shapes/> .
@prefix geosparql: <http://www.opengis.net/ont/geosparql#> .
@prefix owl: <http://www.w3.org/2002/07/owl#> .
@prefix rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#> .
@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .
@prefix sh: <http://www.w3.org/ns/shacl#> .
@prefix time: <http://www.w3.org/2006/time#> .
@prefix wgs1: <http://www.w3.org/2003/01/geo/wgs84_pos#> .
@prefix xsd: <http://www.w3.org/2001/XMLSchema#> .
@prefix org: <http://www.w3.org/ns/org#> .

era:TrackShape a sh:NodeShape ;
               sh:property era-sh:AccelerationLevelCrossing,
               era-sh:ConnectedTo,
               era-sh:SwitchProtectControlWarning,
               era-sh:SwitchRadioSystem ;
               sh:targetClass era:Track .

era:CommonCharacteristicsSubsetShape a sh:NodeShape ;
               sh:property era-sh:AdditionalBrakingInformationDocument,
               era-sh:AtoGradeAutomation,
               era-sh:AtoSystemVersion,
               era-sh:CantDeficiencyBasicSSP,
               era-sh:CompatibilityProcedureDocument,
               era-sh:ConditionsSwitchClassBSystems,
               era-sh:ConditionsUseReflectivePlates,
               era-sh:ContactLineSystem,
               era-sh:ContactStripMaterial,
               era-sh:Contains,
               era-sh:DocumentRestrictionPositionContactLineSeparation,
               era-sh:DocumentRestrictionPowerConsumption,
               era-sh:EddyCurrentBraking,
               era-sh:EddyCurrentBrakingConditionsDocument,
               era-sh:ErrorCorrectionsOnboard,
               era-sh:ETCS,
               era-sh:EtcsDegradedSituation,
               era-sh:EtcsNationalPacket44Function,
               era-sh:EtcsRestrictionsConditionsDoc,
               era-sh:FreightCorridor,
               era-sh:GaugingProfile,
               era-sh:GaugingTransversalDocument,
               era-sh:GsmRActiveMobiles,
               era-sh:GsmRAdditionalInfo,
               era-sh:GsmROptionalFunctions,
               era-sh:GsmRVersion,
               era-sh:GsmrConstraintsOperateOnlyInCircuitSwitch,
               era-sh:GsmrNetworkCoverage,
               era-sh:InCountry,
               era-sh:InfrastructureManager,
               era-sh:InstructionsSwitchRadioSystems,
               era-sh:LegacyRadioSystem,
               era-sh:LinesideDistanceIndication,
               era-sh:LocalRulesOrRestrictionsDoc,
               era-sh:MNvcontact,
               era-sh:MagneticBraking,
               era-sh:MagneticBrakingConditionsDocument,
               era-sh:MinimumVerticalRadius,
               era-sh:OsmClass,
               era-sh:OtherCantDeficiencyBasicSSP,
               era-sh:OtherPantographHead,
               era-sh:OtherTrainProtection,
               era-sh:PlatformHeight,
               era-sh:ProfileNumberSemiTrailers,
               era-sh:ProfileNumberSwapBodies,
               era-sh:ProtectionLegacySystem,
               era-sh:QNvdriverAdhes,
               era-sh:QNvemrrls,
               era-sh:SafeConsistLengthInformationNecessary,
               era-sh:StandardCombinedTransportContainers,
               era-sh:StandardCombinedTransportRollerUnits,
               era-sh:SubsetOf,
               era-sh:TemperatureRange,
               era-sh:TenClassification,
               era-sh:TrackPhaseInfo,
               era-sh:TrackRaisedPantographsDistanceAndSpeed,
               era-sh:TrackSystemSeparationInfo,
               era-sh:TrainDetectionSystem,
               era-sh:TSIPantographHead,
               era-sh:VehicleTypesCompatibleTrafficLoad,
               era-sh:VehiclesCompatibleTrafficLoad,
               era-sh:AssistanceStartingTrain,
               era-sh:AutomaticDroppingDeviceRequired,
               era-sh:BigMetalMass,
               era-sh:ConditionsSwitchTrainProtectionSystems,
               era-sh:ContactStripMaterialMetallicContent,
               era-sh:DNvovtrp,
               era-sh:dNvpotrp,
               era-sh:DNvroll,
               era-sh:DemonstrationENE,
               era-sh:DemonstrationINF,
               era-sh:DistSignToPhaseEnd,
               era-sh:ErrorCorrectionsOnboardExplanation,
               era-sh:EtcsImplementsLevelCrossingProcedure,
               era-sh:EtcsInfillLineAccess,
               era-sh:FlangeLubeForbidden,
               era-sh:GaugingCheckLocation,
               era-sh:GprsForETCS,
               era-sh:GprsImplementationArea,
               era-sh:GradientProfile,
               era-sh:GsmRNoCoverage,
               era-sh:GsmrForcedDeregistrationFunctionalNumber,
               era-sh:HasAdditionalBrakingInformation,
               era-sh:HasETCSRestrictionsConditions,
               era-sh:HasEtcsNationalPacket44,
               era-sh:HasHotAxleBoxDetector,
               era-sh:HasPhaseSeparation,
               era-sh:HasPlatformCurvature,
               era-sh:HasSevereWeatherConditions,
               era-sh:HasSystemSeparation,
               era-sh:HasTSITrainDetection,
               era-sh:IsQuietRoute,
               era-sh:LocalRulesOrRestrictions,
               era-sh:MNvderun,
               era-sh:MaximumAltitude,
               era-sh:MaximumBrakingDistance,
               era-sh:MaximumPermittedSpeed,
               era-sh:NationalLoadCapability,
               era-sh:NationalRollingStockFireCategory,
               era-sh:NationalValuesBrakeModel,
               era-sh:PermitUseReflectivePlates,
               era-sh:PermittedContactForce,
               era-sh:PublicNetworkRoaming,
               era-sh:PublicNetworkRoamingDetails,
               era-sh:QNvsbtsmperm,
               era-sh:RadioNetworkId,
               era-sh:RailSystemType,
               era-sh:RedLightsRequired,
               era-sh:SpecificInformation,
               era-sh:StructureCheckLocation,
               era-sh:SubsetName,
               era-sh:SwitchProtectControlWarning,
               era-sh:SwitchRadioSystem,
               era-sh:TNvcontact,
               era-sh:TNvovtrp,
               era-sh:TenGISId,
               era-sh:TrainIntegrityOnBoardRequired,
               era-sh:TsiMagneticFields,
               era-sh:TSISwitchCrossing,
               era-sh:TsiTractionHarmonics,
               era-sh:UsesGroup555,
               era-sh:VNallowovtrp,
               era-sh:VNvsupovtrp,
               era-sh:VerificationCCS,
               era-sh:VerificationENE,
               era-sh:VerificationINF ;
               sh:targetClass era:CommonCharacteristicsSubset .

era:NonLinearElementShape a sh:NodeShape ;
               sh:property era-sh:Aggregates ;
               sh:targetClass era:NonLinearElement .

era:SubsidiaryLocationShape a sh:NodeShape ;
               sh:property era-sh:AllocationCompany,
               era-sh:LinkedToPrimaryLocation,
               era-sh:SubsidiaryLocationType,
               era-sh:SubsidiaryLocationCode,
               era-sh:SubsidiaryLocationName ;
               sh:targetClass era:SubsidiaryLocation .

era:NetPointReferenceShape a sh:NodeShape ;
               sh:property era-sh:AppliesToDirection,
               era-sh:HasLrsCoordinate,
               era-sh:HasTopoCoordinate ;
               sh:targetClass era:NetPointReference .

era:RunningTrackShape a sh:NodeShape ;
               sh:property era-sh:AtoCommunicationSystem,
               era-sh:LineCategory,
               era-sh:PassesThroughTunnel,
               era-sh:PlatformEdge,
               era-sh:RailInclination,
               era-sh:TrackDirection,
               era-sh:TrackLoadCapability,
               era-sh:TracksideHABD,
               era-sh:CantDeficiency,
               era-sh:HasBallast,
               era-sh:HasBridge,
               era-sh:HasLevelCrossings,
               era-sh:HighSpeedLoadModelCompliance,
               era-sh:MaximumTrainDeceleration,
               era-sh:MinimumHorizontalRadius,
               era-sh:NationalLoadCapability,
               era-sh:NationalValuesBrakeModel,
               era-sh:PublicNetworkRoaming,
               era-sh:PublicNetworkRoamingDetails,
               era-sh:QNvsbtsmperm,
               era-sh:RadioNetworkId,
               era-sh:RedLightsRequired,
               era-sh:SpecificInformation,
               era-sh:StructureCheckLocation,
               era-sh:TNvcontact,
               era-sh:TNvovtrp,
               era-sh:TenGISId,
               era-sh:TrackId,
               era-sh:TrainIntegrityOnBoardRequired,
               era-sh:TsiMagneticFields,
               era-sh:TSISwitchCrossing,
               era-sh:TsiTractionHarmonics,
               era-sh:UsesGroup555,
               era-sh:VNallowovtrp,
               era-sh:VNvsupovtrp ;
               sh:targetClass era:RunningTrack .

era:VehicleTypeShape a sh:NodeShape ;
               sh:property era-sh:AuthorizedCountry,
               era-sh:AxleBearingConditionMonitoring,
               era-sh:Category,
               era-sh:Certificate,
               era-sh:DataGSMRNetwork,
               era-sh:DataRadioCompatible,
               era-sh:EndCouplingType,
               era-sh:ETCSEquipmentOnBoardLevel,
               era-sh:EtcsInfill,
               era-sh:EtcsSystemCompatibility,
               era-sh:FireSafetyCategory,
               era-sh:GsmRRadioDataCommunication,
               era-sh:Manufacturer,
               era-sh:ParkingBrakeType,
               era-sh:PreviousVehicleType,
               era-sh:SnowIceHailConditions,
               era-sh:SubCategory,
               era-sh:SupportedPlatformHeight,
               era-sh:ThermalCapacityTSIReference,
               era-sh:TypeVersionId,
               era-sh:VehicleCategory,
               era-sh:VehicleSubCategory,
               era-sh:VehicleTypeMaximumSpeedAndCantDeficiency,
               era-sh:VoiceGSMRNetwork,
               era-sh:VoiceRadioCompatible,
               era-sh:WheelSetGauge,
               era-sh:WheelSetGaugeChangeoverFacility,
               era-sh:AlternativeName,
               era-sh:AltitudeRange,
               era-sh:AltitudeRangeDetail,
               era-sh:AxleSpacing,
               era-sh:BoardingAids,
               era-sh:BrakeWeightPercentage,
               era-sh:CantDeficiency,
               era-sh:CatenaryMaxRatedCurrent,
               era-sh:ConditionsTrainFormation,
               era-sh:DangerousGoodsTankCode,
               era-sh:DesignMassExceptionalPayload,
               era-sh:DesignMassNormalPayload,
               era-sh:DesignMassWorkingOrder,
               era-sh:DrivingCabs,
               era-sh:EddyCurrentBrakePrevention,
               era-sh:EddyCurrentBrakingFitted,
               era-sh:EmergencyBrake,
               era-sh:EnergyMeterInstalled,
               era-sh:EnergySupplyMaxPower,
               era-sh:ETCSDataCommApp,
               era-sh:ETCSNationalApplications,
               era-sh:ETCSOnBoardImplementation,
               era-sh:FerromagneticWheelMaterial,
               era-sh:FixedSeats,
               era-sh:FlangeLubricationFitted,
               era-sh:GsmRSetsInDrivingCab,
               era-sh:HasAutomaticDroppingDevice,
               era-sh:HasCantDefficiencyCompensation,
               era-sh:HasCurrentLimitation,
               era-sh:HasLubricationDevicePrevention,
               era-sh:HasParkingBrake,
               era-sh:HasRegenerativeBrake,
               era-sh:HasSandingPrevention,
               era-sh:HasShuntingRestrictions,
               era-sh:HasTrainIntegrityConfirmation,
               era-sh:HasWheelSlideProtectionSystem,
               era-sh:LetterMarking,
               era-sh:LoadingPlatformHeight,
               era-sh:MagneticBrakePrevention,
               era-sh:MagneticBrakingFitted,
               era-sh:MassPerWheel,
               era-sh:MaxDistConsecutiveAxles,
               era-sh:MaxFlangeHeight,
               era-sh:MaxLengthVehicleNose,
               era-sh:MaximumAverageDeceleration,
               era-sh:MaximumBrakeThermalEnergyCapacity,
               era-sh:MaximumDesignSpeed,
               era-sh:MaximumLocomotivesCoupled,
               era-sh:MaximumServiceBrake,
               era-sh:MaximumSpeedAndCantDeficiency,
               era-sh:MaximumSpeedEmpty,
               era-sh:MeetsRequirementVehicleAuthorisation,
               era-sh:MinAxleLoad,
               era-sh:MinDistConsecutiveAxles,
               era-sh:MinDistFirstLastAxle,
               era-sh:MinFlangeHeight,
               era-sh:MinFlangeThickness,
               era-sh:MinRimWidth,
               era-sh:MinWheelDiameter,
               era-sh:MinimumConcaveVerticalRadius,
               era-sh:MinimumConvexVerticalRadius,
               era-sh:MinimumWheelDiameter,
               era-sh:NonCodedRestrictions,
               era-sh:NumberElementsRakeFreightWagons,
               era-sh:NumberOfPantographsInContactWithOCL,
               era-sh:NumberOfToilets,
               era-sh:OclType,
               era-sh:ParkingBrake,
               era-sh:ParkingBrakeMandatory,
               era-sh:ParkingBrakeMaximumGradient,
               era-sh:PassByNoiseLevel,
               era-sh:PermissiblePayload,
               era-sh:PortableBoardingAids,
               era-sh:PreventRegenerativeBrakeUse,
               era-sh:PrioritySeats,
               era-sh:PrmAccessibleToilets,
               era-sh:QuasiStaticGuidingForce,
               era-sh:RadioSwitchOverSpecialConditions,
               era-sh:ReferencePassByNoiseLevel,
               era-sh:ShortestDistanceBetweenPantographsInContactWithOCL,
               era-sh:SleepingPlaces,
               era-sh:StartingNoiseLevel,
               era-sh:StaticAxleLoadExceptionalPayload,
               era-sh:StaticAxleLoadNormalPayload,
               era-sh:StaticAxleLoadWorkingOrder,
               era-sh:StationaryNoiseLevel,
               era-sh:StructuralCategory,
               era-sh:ThermalCapacityDistance,
               era-sh:ThermalCapacityGradient,
               era-sh:ThermalCapacitySpeed,
               era-sh:ThermalCapacityTime,
               era-sh:TotalVehicleMass,
               era-sh:TrainControlSwitchOverSpecialConditions,
               era-sh:TransportableOnFerry,
               era-sh:TypeVersionNumber,
               era-sh:VehicleContactForce,
               era-sh:VehicleKinematicGaugeOther,
               era-sh:VehicleMaxSandingOutput,
               era-sh:VehiclePantographHead,
               era-sh:VehiclesComposingFixedFormation,
               era-sh:VoiceOperationalCommImpl,
               era-sh:WheelSetGaugeTransformationMethod,
               era-sh:WheelchairSleepingPlaces,
               era-sh:WheelchairSpaces ;
               sh:targetClass era:VehicleType .

era:InfrastructureElementShape a sh:NodeShape ;
               sh:property era-sh:BelongsTo,
               era-sh:HasAbstraction,
               era-sh:HasPart,
               era-sh:InfrastructureMgr,
               era-sh:IsPartOf,
               era-sh:LineReference,
               era-sh:NetReference,
               era-sh:OperatingLanguage,
               era-sh:ParameterApplicability,
               era-sh:PrimaryLocation,
               era-sh:ValidityEndDate,
               era-sh:ValidityStartDate ;
               sh:targetClass era:InfrastructureElement .

era:OperationalPointShape a sh:NodeShape ;
               sh:property era-sh:BorderPointOf,
               era-sh:DigitalSchematicOverview,
               era-sh:HasSchematicOverviewOPDigitalForm,
               era-sh:OpInfoPerCountry,
               era-sh:OpName,
               era-sh:OpType,
               era-sh:OpTypeGaugeChangeover,
               era-sh:ReferenceBorderPoint,
               era-sh:SchematicOverviewOP,
               era-sh:Siding,
               era-sh:Track,
               era-sh:Uopid,
               era-sh:TAFTAPCode ;
               sh:targetClass era:OperationalPoint .

era:OrientationShape a sh:NodeShape ;
               sh:property era-sh:CarrierLinearElement,
               era-sh:Direction,
               era-sh:Orientation,
               era-sh:Side ;
               sh:targetClass era:Orientation .

era:ContactLineSystemShape a sh:NodeShape ;
               sh:property era-sh:ConditionalRegenerativeBrake,
               era-sh:ConditionsAppliedRegenerativeBraking,
               era-sh:ContactLineSystemType,
               era-sh:EnergySupplySystem,
               era-sh:ConditionsChargingElectricEnergyStorage,
               era-sh:CurrentLimitationRequired,
               era-sh:EnergySupplySystemTSICompliant,
               era-sh:MaxCurrentStandstillPantograph,
               era-sh:MaxTrainCurrent,
               era-sh:MaximumContactWireHeight,
               era-sh:MinimumContactWireHeight,
               era-sh:PermissionChargingElectricEnergyTractionStandstill,
               era-sh:Umax2 ;
               sh:targetClass era:ContactLineSystem .

era:InfrastructureManagerShape a sh:NodeShape ;
               sh:property era-sh:DefinesSubset ;
               sh:targetClass era:InfrastructureManager .

era:NetRelationShape a sh:NodeShape ;
               sh:property era-sh:ElementA,
               era-sh:ElementB,
               era-sh:Navigability,
               era-sh:IsOnOriginOfElementA,
               era-sh:IsOnOriginOfElementB ;
               sh:targetClass era:NetRelation .

era:TunnelShape a sh:NodeShape ;
               sh:property era-sh:EndLocation,
               era-sh:LineReferenceTunnelEnd,
               era-sh:LineReferenceTunnelStart,
               era-sh:RollingStockFireCategory,
               era-sh:SpecialTunnelArea,
               era-sh:StartLocation,
               era-sh:TunnelDocRef,
               era-sh:ComplianceInfTsi,
               era-sh:CrossSectionArea,
               era-sh:DemonstrationSRT,
               era-sh:DieselThermalAllowed,
               era-sh:HasEmergencyPlan,
               era-sh:HasEvacuationAndRescuePoints,
               era-sh:HasWalkway,
               era-sh:LengthOfTunnel,
               era-sh:NationalRollingStockFireCategory,
               era-sh:TunnelIdentification,
               era-sh:VerificationSRT ;
               sh:targetClass era:Tunnel .

era:NetLinearReferenceShape a sh:NodeShape ;
               sh:property era-sh:EndsAt,
               era-sh:HasSequence,
               era-sh:StartsAt ;
               sh:targetClass era:NetLinearReference .

era:ETCSShape a sh:NodeShape ;
               sh:property era-sh:EtcsBaseline,
               era-sh:EtcsLevelType,
               era-sh:EtcsMVersion,
               era-sh:EtcsTransmittedTrackConditions,
               era-sh:EtcsTransmitsTrackConditions ;
               sh:targetClass era:ETCS .

era:TrainDetectionSystemShape a sh:NodeShape ;
               sh:property era-sh:FrenchTrainDetectionSystemLimitation,
               era-sh:FrequencyBandsForDetection,
               era-sh:MaxSandingOutput,
               era-sh:MinVehicleImpedance,
               era-sh:TdsFrenchTrainDetectionSystemLimitation,
               era-sh:TdsMaximumMagneticField,
               era-sh:TdsMinAxleLoadVehicleCategory,
               era-sh:TrainDetectionSystemSpecificCheck,
               era-sh:TrainDetectionSystemSpecificCheckDocument,
               era-sh:TrainDetectionSystemType,
               era-sh:TSICompliantCompositeBrakeBlocks,
               era-sh:TSICompliantFerromagneticWheel,
               era-sh:TSICompliantMaxDistConsecutiveAxles,
               era-sh:TSICompliantMaxImpedanceWheelset,
               era-sh:TSICompliantMetalConstruction,
               era-sh:TSICompliantMetalFreeSpace,
               era-sh:TSICompliantRSTShuntImpedance,
               era-sh:TSICompliantSandCharacteristics,
               era-sh:TSICompliantSanding,
               era-sh:TSICompliantShuntDevices,
               era-sh:FlangeLubeRules,
               era-sh:MaxDistEndTrainFirstAxle,
               era-sh:MaxImpedanceWheelset,
               era-sh:MaximumInterferenceCurrent,
               era-sh:MaximumInterferenceCurrentEvaluation,
               era-sh:RequiredSandingOverride ;
               sh:targetClass era:TrainDetectionSystem .

era:FrenchTrainDetectionSystemLimitationShape a sh:NodeShape ;
               sh:property era-sh:FrenchTrainDetectionSystemLimitationNumber,
               era-sh:FrenchTrainDetectionSystemLimitationApplicable ;
               sh:targetClass era:FrenchTrainDetectionSystemLimitation .

era:KilometricPostShape a sh:NodeShape ;
               sh:property era-sh:HasLRS,
               era-sh:Kilometer,
               era-sh:KmPostName,
               era-sh:MeasuredDistance ;
               sh:targetClass era:KilometricPost .

era:OrganisationRoleShape a sh:NodeShape ;
               sh:property era-sh:HasOrganisationRole,
               era-sh:RoleOf,
               era-sh:IMCode,
               era-sh:OrganisationCode ;
               sh:targetClass era:OrganisationRole .

era:SignalShape a sh:NodeShape ;
               sh:property era-sh:HasOrientation,
               era-sh:SignalOrientation,
               era-sh:SignalType,
               era-sh:RelativeDistanceDangerPoint,
               era-sh:SignalId ;
               sh:targetClass era:Signal .

era:HABDShape a sh:NodeShape ;
               sh:property era-sh:HotAxleBoxDetectorDirection,
               era-sh:HotAxleBoxDetectorGeneration,
               era-sh:HotAxleBoxDetectorIdentification,
               era-sh:HotAxleBoxDetectorLocation,
               era-sh:HotAxleBoxDetectorTSICompliant ;
               sh:targetClass era:HABD .

era:NetAreaReferenceShape a sh:NodeShape ;
               sh:property era-sh:Includes ;
               sh:targetClass era:NetAreaReference .

era:LinearPositioningSystemCoordinateShape a sh:NodeShape ;
               sh:property era-sh:KmPost,
               era-sh:OffsetFromKilometricPost ;
               sh:targetClass era:LinearPositioningSystemCoordinate .

era:LineReferenceShape a sh:NodeShape ;
               sh:property era-sh:LineNationalId ;
               sh:targetClass era:LineReference .

era:LinesideDistanceIndicationShape a sh:NodeShape ;
               sh:property era-sh:LinesideDistanceIndicationAppearance,
               era-sh:LinesideDistanceIndicationPositioning,
               era-sh:LinesideDistanceIndicationFrequency ;
               sh:targetClass era:LinesideDistanceIndication .

era:LoadCapabilityShape a sh:NodeShape ;
               sh:property era-sh:LoadCapabilityLineCategory,
               era-sh:LoadCapabilitySpeed ;
               sh:targetClass era:LoadCapability .

era:LinearPositioningSystemShape a sh:NodeShape ;
               sh:property era-sh:LrsMethod,
               era-sh:LineId ;
               sh:targetClass era:LinearPositioningSystem .

era:VehicleShape a sh:NodeShape ;
               sh:property era-sh:ManufacturingCountry,
               era-sh:OperationalRestriction,
               era-sh:QuieterRoutesExemptedCountry,
               era-sh:VehicleKeeper,
               era-sh:VehicleType,
               era-sh:CompositeBrakeBlockRetrofitted,
               era-sh:LengthOfVehicle,
               era-sh:VehicleNumber,
               era-sh:VehicleSeries ;
               sh:targetClass era:Vehicle .

era:MinAxleLoadVehicleCategoryShape a sh:NodeShape ;
               sh:property era-sh:MinAxleLoadVehicleCategory ;
               sh:targetClass era:MinAxleLoadVehicleCategory .

era:MinVehicleImpedanceShape a sh:NodeShape ;
               sh:property era-sh:MinVehicleImpedanceVoltages,
               era-sh:MinVehicleInputCapacitance,
               era-sh:MinVehicleInputImpedance ;
               sh:targetClass era:MinVehicleImpedance .

era:SectionOfLineShape a sh:NodeShape ;
               sh:property era-sh:NationalLine,
               era-sh:OpEnd,
               era-sh:OpStart,
               era-sh:OperationalRegimeType,
               era-sh:SolNature,
               era-sh:ExistanceOfIndustrialRisk,
               era-sh:LengthOfSectionOfLine ;
               sh:targetClass era:SectionOfLine .

era:ParameterShape a sh:NodeShape ;
               sh:property era-sh:NotApplicable ;
               sh:targetClass era:Parameter .

era:SomeClassShape a sh:NodeShape ;
               sh:property era-sh:NotYetAvailable ;
               sh:targetClass era:SomeClass .

era:ParameterApplicabilityShape a sh:NodeShape ;
               sh:property era-sh:OfParameter,
               era-sh:ParameterValueType,
               era-sh:ParameterValue ;
               sh:targetClass era:ParameterApplicability .

era:LinearElementSectionShape a sh:NodeShape ;
               sh:property era-sh:OnElement,
               era-sh:AppliesInBothDirections,
               era-sh:EndOffsetFromOrigin,
               era-sh:StartOffsetFromOrigin ;
               sh:targetClass era:LinearElementSection .

era:TopologicalCoordinateShape a sh:NodeShape ;
               sh:property era-sh:OnLinearElement,
               era-sh:OffsetFromOrigin ;
               sh:targetClass era:TopologicalCoordinate .

era:RadioBlockCenterShape a sh:NodeShape ;
               sh:property era-sh:ReasonsEtcsRadioBlockCenterReject,
               era-sh:RbcID,
               era-sh:RbcPhone ;
               sh:targetClass era:RadioBlockCenter .

era:BodyShape a sh:NodeShape ;
               sh:property era-sh:Role ;
               sh:targetClass era:Body .

era:SpecialAreaShape a sh:NodeShape ;
               sh:property era-sh:SpecialAreaType ;
               sh:targetClass era:SpecialArea .

era:CertificateShape a sh:NodeShape ;
               sh:property era-sh:State ;
               sh:targetClass era:Certificate .

era:FeatureShape a sh:NodeShape ;
               sh:property era-sh:Validity,
               era-sh:CurrentlyValid ;
               sh:targetClass era:Feature .

era:PlatformEdgeShape a sh:NodeShape ;
               sh:property era-sh:AreaBoardingAid,
               era-sh:LengthOfPlatform,
               era-sh:PlatformId ;
               sh:targetClass era:PlatformEdge .

era:ReferenceBorderPointShape a sh:NodeShape ;
               sh:property era-sh:BorderPointId ;
               sh:targetClass era:ReferenceBorderPoint .

era:PrimaryLocationShape a sh:NodeShape ;
               sh:property era-sh:ContainerHandlingFlag,
               era-sh:FreightFlag,
               era-sh:HandoverPointFlag,
               era-sh:PrimaryLocationCode,
               era-sh:PrimaryLocationName ;
               sh:targetClass era:PrimaryLocation .

era:DocumentShape a sh:NodeShape ;
               sh:property era-sh:DocumentUrl ;
               sh:targetClass era:Document .

era:SidingShape a sh:NodeShape ;
               sh:property era-sh:Gradient,
               era-sh:HasElectricShoreSupply,
               era-sh:HasExternalCleaning,
               era-sh:HasRefuelling,
               era-sh:HasSandRestocking,
               era-sh:HasToiletDischarge,
               era-sh:HasWaterRestocking,
               era-sh:LengthOfSiding,
               era-sh:SidingId ;
               sh:targetClass era:Siding .

era:LinearElementShape a sh:NodeShape ;
               sh:property era-sh:LengthOfNetLinearElement ;
               sh:targetClass era:LinearElement .

era:MaximumMagneticFieldShape a sh:NodeShape ;
               sh:property era-sh:MaximumMagneticFieldDirectionX,
               era-sh:MaximumMagneticFieldDirectionY,
               era-sh:MaximumMagneticFieldDirectionZ ;
               sh:targetClass era:MaximumMagneticField .

era:MinimumVerticalRadiusShape a sh:NodeShape ;
               sh:property era-sh:MinimumVerticalRadiusCrest,
               era-sh:MinimumVerticalRadiusHollow ;
               sh:targetClass era:MinimumVerticalRadius .

era:PhaseInfoShape a sh:NodeShape ;
               sh:property era-sh:PhaseInfoChangeSupplySystem,
               era-sh:PhaseInfoDistanceType,
               era-sh:PhaseInfoKm,
               era-sh:PhaseInfoLength,
               era-sh:PhaseInfoPantographLowered,
               era-sh:PhaseInfoSwitchOffBreaker ;
               sh:targetClass era:PhaseInfo .

era:RaisedPantographsDistanceAndSpeedShape a sh:NodeShape ;
               sh:property era-sh:RaisedPantographsDistance,
               era-sh:RaisedPantographsNumber,
               era-sh:RaisedPantographsSpeed ;
               sh:targetClass era:RaisedPantographsDistanceAndSpeed .

era:SystemSeparationInfoShape a sh:NodeShape ;
               sh:property era-sh:SystemSeparationInfoChangeSupplySystem,
               era-sh:SystemSeparationInfoKm,
               era-sh:SystemSeparationInfoLength,
               era-sh:SystemSeparationInfoPantographLowered,
               era-sh:SystemSeparationInfoSwitchOffBreaker ;
               sh:targetClass era:SystemSeparationInfo .

era:MaximumSpeedAndCantDeficiencyShape a sh:NodeShape ;
               sh:property era-sh:VehicleTypeMaximumCantDeficiency,
               era-sh:VehicleTypeMaximumSpeed ;
               sh:targetClass era:MaximumSpeedAndCantDeficiency .













era-sh:AccelerationLevelCrossing a sh:PropertyShape ;
    era:affectedClass era:Track ;
    era:affectedProperty era:accelerationLevelCrossing ;
    era:rinfIndex "1.1.1.1.7.3" ;
    era:scope "local" ;
    rdfs:comment "Existence of limit for acceleration of train if stopping or recovering speed close to a level crossing expressed in a specific reference acceleration curve."@en ;
    # Source: ontology comment for accelerationLevelCrossing

    sh:class era:Document ;
    # Source: ontology range is http://data.europa.eu/949/Document

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "accelerationLevelCrossing (1.1.1.1.7.3): The track or subset with common characteristics must have at most one acceleration level crossing value that is a Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ;
    # Source: RAG indicates that the value must be a document reference

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to other identifiable documents (IRI references)

    sh:path era:accelerationLevelCrossing ;

    sh:severity sh:Violation .

era-sh:AccelerationLevelCrossingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Track ;
    era:affectedProperty era:accelerationLevelCrossing ;
    era:rinfIndex "1.1.1.1.7.3" ;
    era:scope "local" ;
    rdfs:comment "Applicable only when selected value of parameter 1.1.1.1.7.2 is ‘Y’."@en ;
    # Source: RAG applicability explanation

    sh:message "accelerationLevelCrossing (1.1.1.1.7.3): This error is due to the track or subset with common characteristics {?trackLabel}, violating the rule: Applicable only when selected value of parameter 1.1.1.1.7.2 is ‘Y’."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:accelerationLevelCrossing ?value } .
        FILTER EXISTS { $this era:hasLevelCrossings ?levelCrossingValue . FILTER(?levelCrossingValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AdditionalBrakingInformationDocument a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:additionalBrakingInformationDocument ; 
    era:scope "local" ; 

    rdfs:comment "Electronic document available in two EU languages from the IM stored by the Agency providing additional information as defined in point (2) of point 4.2.2.6.2 of TSI OPE."@en ; 
    # Source: ontology comment for additionalBrakingInformationDocument

    sh:class era:Document ; 
    # Source: ontology range is http://data.europa.eu/949/Document

    sh:maxCount 1 ; 
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "additionalBrakingInformationDocument (1.1.1.3.11.3): The CommonCharacteristicsSubset must have at most one additional braking information document that is a Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ; 
    # Source: RAG indicates that the value must be a document reference

    sh:nodeKind sh:IRI ; 
    # Inferred: links must refer to other identifiable documents (IRI references)

    sh:path era:additionalBrakingInformationDocument ; 

    sh:severity sh:Violation .

era-sh:AdditionalBrakingInformationDocumentApplicability a sh:SPARQLConstraint ; 
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:additionalBrakingInformationDocument ; 
    era:scope "local" ; 

    rdfs:comment "Applicable only when for parameter 1.1.1.3.11.2 True is selected."@en ; 
    # Source: RAG applicability explanation

    sh:message "additionalBrakingInformationDocument (1.1.1.3.11.3): This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when for parameter 1.1.1.3.11.2 True is selected."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:additionalBrakingInformationDocument ?value } .
        FILTER EXISTS { $this era:parameter1_1_1_3_11_2 ?paramValue . FILTER(?paramValue = "True") } .
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:Aggregates a sh:PropertyShape ;
    era:affectedClass era:NonLinearElement ;
    era:affectedProperty era:aggregates ;
    era:scope "local" ;

    rdfs:comment "Specifies the linear elements aggregated by a non-linear element."@en ;
    # Source: ontology comment for aggregates

    sh:class era:LinearElement ;
    # Source: ontology range is http://data.europa.eu/949/LinearElement

    sh:maxCount 1 ;
    # Inferred: expected to aggregate at least one linear element, not explicitly stated but inferred from the nature of aggregation

    sh:message "aggregates: The NonLinearElement must have at least one aggregate that is a LinearElement. This error may be due to not having a value or having a value that is not an instance of LinearElement."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable linear elements (IRI references)

    sh:path era:aggregates ;

    sh:severity sh:Violation .

era-sh:AggregatesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:NonLinearElement ;
    era:affectedProperty era:aggregates ;
    era:scope "local" ;

    rdfs:comment "Applicable when the NonLinearElement is intended to aggregate linear elements."@en ;
    # Source: inferred from the definition of NonLinearElement as aggregation points

    sh:message "aggregates: This error is due to the NonLinearElement {?elementLabel}, violating the rule: Applicable only when the NonLinearElement is intended to aggregate linear elements."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?elementLabel WHERE {
        $this rdfs:label ?elementLabel .
        FILTER NOT EXISTS { $this era:aggregates ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AllocationCompany a sh:PropertyShape ;
    era:affectedClass era:SubsidiaryLocation ;
    era:affectedProperty era:allocationCompany ;
    era:scope "local" ;

    rdfs:comment "The organisation in charge to allocate the code for the subsidiary location."@en ;
    # Source: ontology comment for allocationCompany

    sh:class era:OrganisationRole ;
    # Source: ontology range is http://data.europa.eu/949/OrganisationRole

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "allocationCompany: The SubsidiaryLocation must have at most one allocation company that is an OrganisationRole. This error may be due to having more than one value or having a value that is not an instance of OrganisationRole."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable organisation roles (IRI references)

    sh:path era:allocationCompany ;

    sh:severity sh:Violation .

era-sh:AllocationCompanyApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SubsidiaryLocation ;
    era:affectedProperty era:allocationCompany ;
    era:scope "local" ;

    rdfs:comment "Applicable when the subsidiary location is defined and requires an allocation company."@en ;
    # Source: inferred from the definition of SubsidiaryLocation and its relationship to allocation

    sh:message "allocationCompany: This error is due to the SubsidiaryLocation {?locationLabel}, violating the rule: Applicable when the subsidiary location is defined and requires an allocation company."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?locationLabel WHERE {
        $this rdfs:label ?locationLabel .
        FILTER NOT EXISTS { $this era:allocationCompany ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AppliesToDirection a sh:PropertyShape ;
    era:affectedClass era:NetPointReference ;
    era:affectedProperty era:appliesToDirection ;
    era:scope "local" ;

    rdfs:comment "The direction relative to the origin of the linear element towards the point reference applies."@en ;
    # Source: ontology comment for appliesToDirection

    sh:class geosparql:Concept ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "appliesToDirection: The NetPointReference must have at most one appliesToDirection value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:appliesToDirection ;

    sh:severity sh:Violation .

era-sh:AppliesToDirectionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:NetPointReference ;
    era:affectedProperty era:appliesToDirection ;
    era:scope "local" ;

    rdfs:comment "Applicable when the NetPointReference is defined and requires a direction."@en ;
    # Source: inferred from the definition of NetPointReference and its relationship to direction

    sh:message "appliesToDirection: This error is due to the NetPointReference {?referenceLabel}, violating the rule: Applicable when the NetPointReference is defined and requires a direction."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?referenceLabel WHERE {
        $this rdfs:label ?referenceLabel .
        FILTER NOT EXISTS { $this era:appliesToDirection ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AtoCommunicationSystem a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:atoCommunicationSystem ;
    era:rinfIndex "1.1.1.3.13.3" ;
    era:scope "local" ;

    rdfs:comment "Supported ATO communication systems from trackside."@en ;
    # Source: ontology comment for atoCommunicationSystem

    sh:class geosparql:Concept ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "atoCommunicationSystem: The RunningTrack must have at most one ATO communication system that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:atoCommunicationSystem ;

    sh:severity sh:Violation .

era-sh:AtoCommunicationSystemApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:atoCommunicationSystem ;
    era:scope "local" ;

    rdfs:comment "Applicable when the ATO Grade of Automation is applicable."@en ;
    # Source: ontology dependency note for atoCommunicationSystem

    sh:message "atoCommunicationSystem: This error is due to the RunningTrack {?trackLabel}, violating the rule: Applicable when 'ATO Grade of Automation' is applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:atoCommunicationSystem ?value } .
        FILTER EXISTS { $this era:atoGradeAutomation ?gradeValue . FILTER(?gradeValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AtoGradeAutomation a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:atoGradeAutomation ;
    era:rinfIndex "1.1.1.3.13.1" ;
    era:scope "local" ;

    rdfs:comment "ATO grade of automation installed lineside."@en ;
    # Source: ontology comment for atoGradeAutomation

    sh:class geosparql:Concept ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "atoGradeAutomation: The CommonCharacteristicsSubset must have at most one ATO grade of automation that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:atoGradeAutomation ;

    sh:severity sh:Violation .

era-sh:AtoGradeAutomationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:atoGradeAutomation ;
    era:scope "local" ;

    rdfs:comment "Applicable only when ETCS Baseline > 4 MR1 and ATO is implemented."@en ;
    # Source: ontology dependency note for atoGradeAutomation

    sh:message "atoGradeAutomation: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when ETCS Baseline > 4 MR1 and ATO is implemented."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:atoGradeAutomation ?value } .
        FILTER EXISTS { $this era:etcsBaseline ?baselineValue . FILTER(?baselineValue > 4) } .
        FILTER EXISTS { $this era:atoImplementation ?atoValue . FILTER(?atoValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AtoSystemVersion a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:atoSystemVersion ; 
    era:scope "local" ; 

    rdfs:comment "ATO system version according to the specification referenced in Appendix A-1, index [C]."@en ; 
    # Source: ontology comment for atoSystemVersion

    sh:class geosparql:Concept ; 
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ; 
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "atoSystemVersion: The CommonCharacteristicsSubset must have at most one ATO system version that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ; 
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ; 
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:atoSystemVersion ; 

    sh:severity sh:Violation .

era-sh:AtoSystemVersionApplicability a sh:SPARQLConstraint ; 
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:atoSystemVersion ; 
    era:scope "local" ; 

    rdfs:comment "Applicable when 'ATO Grade of Automation' is applicable."@en ; 
    # Source: ontology dependency note for atoSystemVersion

    sh:message "atoSystemVersion: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when 'ATO Grade of Automation' is applicable."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:atoSystemVersion ?value } .
        FILTER EXISTS { $this era:atoGradeAutomation ?gradeValue . FILTER(?gradeValue = "Y") } .
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:AuthorizedCountry a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:authorizedCountry ;
    era:scope "local" ;

    rdfs:comment "Indicates the country(ies) in where a vehicle type has been authorized to operate."@en ;
    # Source: ontology comment for authorizedCountry

    sh:class geosparql:Concept ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected to have at most one authorized country, as it is a characteristic of the vehicle type

    sh:message "authorizedCountry: The VehicleType must have at most one authorized country that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:authorizedCountry ;

    sh:severity sh:Violation .

era-sh:AuthorizedCountryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:authorizedCountry ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires an authorized country."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to authorization

    sh:message "authorizedCountry: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the VehicleType is defined and requires an authorized country."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:authorizedCountry ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AxleBearingConditionMonitoring a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:axleBearingConditionMonitoring ;
    era:rinfIndex "4.9.2" ;
    era:scope "local" ;

    rdfs:comment "Axle bearing condition monitoring."@en ;
    # Source: ontology comment for axleBearingConditionMonitoring

    sh:class geosparql:Concept ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "axleBearingConditionMonitoring: The VehicleType must have at most one axle bearing condition monitoring value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:axleBearingConditionMonitoring ;

    sh:severity sh:Violation .

era-sh:AxleBearingConditionMonitoringApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:axleBearingConditionMonitoring ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires axle bearing condition monitoring."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to axle bearing monitoring

    sh:message "axleBearingConditionMonitoring: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the VehicleType is defined and requires axle bearing condition monitoring."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:axleBearingConditionMonitoring ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:BelongsTo a sh:PropertyShape ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:belongsTo ;
    era:scope "local" ;

    rdfs:comment "Indicates that an infrastructure element belongs to a certain subset that contains common characteristics."@en ;
    # Source: ontology comment for belongsTo

    sh:class era:CommonCharacteristicsSubset ;
    # Source: ontology range is http://data.europa.eu/949/CommonCharacteristicsSubset

    sh:maxCount 1 ;
    # Source: inferred: expected to belong to at most one subset, as it represents a specific relationship

    sh:message "belongsTo: The InfrastructureElement must have at most one belongsTo reference that is a CommonCharacteristicsSubset. This error may be due to having more than one value or having a value that is not an instance of CommonCharacteristicsSubset."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable common characteristics subsets (IRI references)

    sh:path era:belongsTo ;

    sh:severity sh:Violation .

era-sh:BelongsToApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:belongsTo ;
    era:scope "local" ;

    rdfs:comment "Applicable when the InfrastructureElement is defined and requires a common characteristics subset."@en ;
    # Source: inferred from the definition of InfrastructureElement and its relationship to common characteristics

    sh:message "belongsTo: This error is due to the InfrastructureElement {?elementLabel}, violating the rule: Applicable when the InfrastructureElement is defined and requires a common characteristics subset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?elementLabel WHERE {
        $this rdfs:label ?elementLabel .
        FILTER NOT EXISTS { $this era:belongsTo ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:BorderPointOf a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:borderPointOf ;
    era:scope "local" ;

    rdfs:comment "Relates the information that each country maintains on a border point, to the operational point that represents the general information on the border point."@en ;
    # Source: ontology comment for borderPointOf

    sh:class era:OperationalPoint ;
    # Source: ontology range is http://data.europa.eu/949/OperationalPoint

    sh:maxCount 1 ;
    # Inferred: expected to relate to at most one border point, as it represents a specific relationship

    sh:message "borderPointOf: The OperationalPoint must have at most one border point reference that is an OperationalPoint. This error may be due to having more than one value or having a value that is not an instance of OperationalPoint."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable operational points (IRI references)

    sh:path era:borderPointOf ;

    sh:severity sh:Violation .

era-sh:BorderPointOfApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:borderPointOf ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OperationalPoint is defined as a border point."@en ;
    # Source: inferred from the definition of OperationalPoint and its relationship to border points

    sh:message "borderPointOf: This error is due to the OperationalPoint {?pointLabel}, violating the rule: Applicable when the OperationalPoint is defined as a border point."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?pointLabel WHERE {
        $this rdfs:label ?pointLabel .
        FILTER NOT EXISTS { $this era:borderPointOf ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:CantDeficiencyBasicSSP a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:cantDeficiencyBasicSSP ;
    era:scope "local" ;

    rdfs:comment "Essential information for railway undertakings with a worse (lower) tolerated cant deficiency than those for which the ETCS trackside provides SSP (Static Speed Profiles)."@en ;
    # Source: ontology comment for cantDeficiencyBasicSSP

    sh:class geosparql:Concept ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "cantDeficiencyBasicSSP: The CommonCharacteristicsSubset must have at most one cant deficiency basic SSP value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:cantDeficiencyBasicSSP ;

    sh:severity sh:Violation .

era-sh:CantDeficiencyBasicSSPApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:cantDeficiencyBasicSSP ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a cant deficiency basic SSP."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to cant deficiency

    sh:message "cantDeficiencyBasicSSP: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a cant deficiency basic SSP."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:cantDeficiencyBasicSSP ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:CarrierLinearElement a sh:PropertyShape ;
    era:affectedClass era:Orientation ;
    era:affectedProperty era:carrierLinearElement ;
    era:scope "local" ;

    rdfs:comment "The carrier linear element in relation to which the orientation is based on."@en ;
    # Source: ontology comment for carrierLinearElement

    sh:class era:LinearElement ;
    # Source: ontology range is http://data.europa.eu/949/LinearElement

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "carrierLinearElement: The Orientation must have at most one carrier linear element that is a LinearElement. This error may be due to having more than one value or having a value that is not an instance of LinearElement."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable linear elements (IRI references)

    sh:path era:carrierLinearElement ;

    sh:severity sh:Violation .

era-sh:CarrierLinearElementApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Orientation ;
    era:affectedProperty era:carrierLinearElement ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Orientation is defined and requires a carrier linear element."@en ;
    # Source: inferred from the definition of Orientation and its relationship to carrier linear elements

    sh:message "carrierLinearElement: This error is due to the Orientation {?orientationLabel}, violating the rule: Applicable when the Orientation is defined and requires a carrier linear element."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?orientationLabel WHERE {
        $this rdfs:label ?orientationLabel .
        FILTER NOT EXISTS { $this era:carrierLinearElement ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Category a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:category ;
    era:scope "local" ;

    rdfs:comment "Indicates the category of the vehicle type."@en ;
    # Source: inferred from the ontology definition of category

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "category: The VehicleType must have at most one category value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:category ;

    sh:severity sh:Violation .

era-sh:CategoryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:category ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires a category."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to category

    sh:message "category: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the VehicleType is defined and requires a category."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:category ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Certificate a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:certificate ;
    era:scope "local" ;

    rdfs:comment "For this Vehicle Type, the type or design examination certificate described in the relevant verification module as issued by Notified Bodies, supporting the EC Declaration(s) of Verification for the subsystems in scope of the type's authorisation by an authorizing entity."@en ;
    # Source: ontology comment for certificate

    sh:class era:Certificate ;
    # Source: ontology range is http://data.europa.eu/949/Certificate

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "certificate: The VehicleType must have at most one certificate that is a Certificate. This error may be due to having more than one value or having a value that is not an instance of Certificate."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable certificates (IRI references)

    sh:path era:certificate ;

    sh:severity sh:Violation .

era-sh:CertificateApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:certificate ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires a certificate."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to certificates

    sh:message "certificate: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the VehicleType is defined and requires a certificate."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:certificate ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:CompatibilityProcedureDocument a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:compatibilityProcedureDocument ; 
    era:rinfIndex "1.1.1.1.2.4.4" ; 
    era:scope "local" ; 

    rdfs:comment "Electronic document available in two EU languages from the IM stored by the Agency with precise procedures for the static and dynamic route compatibility checks."@en ; 
    # Source: ontology comment for compatibilityProcedureDocument

    sh:class era:Document ; 
    # Source: ontology range is http://data.europa.eu/949/Document

    sh:maxCount 1 ; 
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "compatibilityProcedureDocument: The CommonCharacteristicsSubset must have at most one compatibility procedure document that is a Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ; 
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ; 
    # Inferred: links must refer to identifiable documents (IRI references)

    sh:path era:compatibilityProcedureDocument ; 

    sh:severity sh:Violation .

era-sh:CompatibilityProcedureDocumentApplicability a sh:SPARQLConstraint ; 
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:compatibilityProcedureDocument ; 
    era:scope "local" ; 

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a compatibility procedure document."@en ; 
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to compatibility procedures

    sh:message "compatibilityProcedureDocument: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a compatibility procedure document."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:compatibilityProcedureDocument ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:ConditionalRegenerativeBrake a sh:PropertyShape ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:conditionalRegenerativeBrake ;
    era:rinfIndex "1.1.1.2.2.4" ;
    era:scope "local" ;

    rdfs:comment "Indication whether regenerative braking is permitted, not permitted, or permitted under specific conditions."@en ;
    # Source: ontology comment for conditionalRegenerativeBrake

    sh:in ( "Y" "N" "NYA" ) ;
    # Source: RAG states applicable values are Y/N/NYA

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "conditionalRegenerativeBrake: The ContactLineSystem must have at most one conditional regenerative brake value that is either 'Y', 'N', or 'NYA'. This error may be due to having more than one value or having a value that is not one of the allowed options."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:conditionalRegenerativeBrake ;

    sh:severity sh:Violation .

era-sh:ConditionalRegenerativeBrakeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:conditionalRegenerativeBrake ;
    era:scope "local" ;

    rdfs:comment "Applicable when the 'not electrified' option is not chosen in parameter 1.1.1.2.2.1.1."@en ;
    # Source: RAG states that this parameter is not applicable when 'not electrified' is chosen

    sh:message "conditionalRegenerativeBrake: This error is due to the ContactLineSystem {?systemLabel}, violating the rule: Applicable when 'not electrified' is not selected."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:conditionalRegenerativeBrake ?value } .
        FILTER EXISTS { $this era:notElectrified ?notElectrifiedValue . FILTER(?notElectrifiedValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ConditionsAppliedRegenerativeBraking a sh:PropertyShape ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:conditionsAppliedRegenerativeBraking ;
    era:rinfIndex "1.1.1.2.2.4.1" ;
    era:scope "local" ;

    rdfs:comment "Name and/or reference of the document specifying the conditions applying in regards to regenerative braking."@en ;
    # Source: ontology comment for conditionsAppliedRegenerativeBraking

    sh:class era:Document ;
    # Source: ontology range is http://data.europa.eu/949/Document

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "conditionsAppliedRegenerativeBraking: The ContactLineSystem must have at most one conditions applied regenerative braking value that is a Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable documents (IRI references)

    sh:path era:conditionsAppliedRegenerativeBraking ;

    sh:severity sh:Violation .

era-sh:ConditionsAppliedRegenerativeBrakingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:conditionsAppliedRegenerativeBraking ;
    era:scope "local" ;

    rdfs:comment "Mandatory when parameter 1.1.1.2.2.4 announces Conditions."@en ;
    # Source: dependencyNote in ontology

    sh:message "conditionsAppliedRegenerativeBraking: This error is due to the ContactLineSystem {?systemLabel}, violating the rule: Mandatory when conditions are announced."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:conditionsAppliedRegenerativeBraking ?value } .
        # Additional condition to check if the parameter announcing conditions is set
        FILTER EXISTS { $this era:permissionForRegenerativeBraking ?permValue . FILTER(?permValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ConditionsSwitchClassBSystems a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:conditionsSwitchClassBSystems ;
    era:rinfIndex "1.1.1.3.8.3" ;
    era:scope "local" ;

    rdfs:comment "Special technical conditions required to switch over between ERTMS/ETCS and Class B systems."@en ;
    # Source: ontology comment for conditionsSwitchClassBSystems

    sh:class era:Document ;
    # Source: ontology range is http://data.europa.eu/949/Document

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "conditionsSwitchClassBSystems: The CommonCharacteristicsSubset must have at most one conditions switch class B systems value that is a Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable documents (IRI references)

    sh:path era:conditionsSwitchClassBSystems ;

    sh:severity sh:Violation .

era-sh:ConditionsSwitchClassBSystemsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:conditionsSwitchClassBSystems ;
    era:scope "local" ;

    rdfs:comment "Applicable when at least two different classes of protection systems exist."@en ;
    # Source: dependencyNote in ontology

    sh:message "conditionsSwitchClassBSystems: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when at least two different classes of protection systems exist."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:conditionsSwitchClassBSystems ?value } .
        # Additional condition to check if at least two different classes of protection systems exist
        FILTER EXISTS { $this era:hasProtectionSystem ?system1 } .
        FILTER EXISTS { $this era:hasProtectionSystem ?system2 . FILTER(?system1 != ?system2) } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ConditionsUseReflectivePlates a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:conditionsUseReflectivePlates ;
    era:rinfIndex "1.1.1.1.7.12.1" ;
    era:scope "local" ;

    rdfs:comment "Details of any conditions for using the reflective plates on freight corridors. Specific case for Portugal and Spain until 1.1.2025 and Belgium and France until 1.1.2026."@en ;
    # Source: ontology comment for conditionsUseReflectivePlates

    sh:class rdfs:Concept ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "conditionsUseReflectivePlates: The CommonCharacteristicsSubset must have at most one conditions use reflective plates value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:conditionsUseReflectivePlates ;

    sh:severity sh:Violation .

era-sh:ConditionsUseReflectivePlatesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:conditionsUseReflectivePlates ;
    era:scope "local" ;

    rdfs:comment "Applicable when the conditions for using reflective plates are defined."@en ;
    # Source: inferred from the definition of conditionsUseReflectivePlates and its relationship to common characteristics

    sh:message "conditionsUseReflectivePlates: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the conditions for using reflective plates are defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:conditionsUseReflectivePlates ?value } .
        # Additional condition to check if the parameter announcing conditions is set
        FILTER EXISTS { $this era:permitOfUseReflectivePlates ?permValue . FILTER(?permValue = "Yes") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ConnectedTo a sh:PropertyShape ;
    era:affectedClass era:Track ;
    era:affectedProperty era:connectedTo ;
    era:scope "local" ;

    rdfs:comment "Represents a bidirectional connection between two Track instances."@en ;
    # Source: ontology comment for connectedTo

    sh:class era:Track ;
    # Source: ontology range is http://data.europa.eu/949/Track

    sh:maxCount 1 ;
    # Inferred: expected to relate to at most one connected track, as it represents a specific relationship

    sh:message "connectedTo: The Track must have at most one connectedTo reference that is a Track. This error may be due to having more than one value or having a value that is not an instance of Track."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable track instances (IRI references)

    sh:path era:connectedTo ;

    sh:severity sh:Violation .

era-sh:ConnectedToApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Track ;
    era:affectedProperty era:connectedTo ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Track is defined and requires a connection to another Track."@en ;
    # Source: inferred from the definition of Track and its relationship to connections

    sh:message "connectedTo: This error is due to the Track {?trackLabel}, violating the rule: Applicable when the Track is defined and requires a connection to another Track."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:connectedTo ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ContactLineSystem a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:contactLineSystem ;
    era:scope "local" ;

    rdfs:comment "System that is used to transmit electrical energy to road or rail vehicles."@en ;
    # Source: ontology comment for contactLineSystem

    sh:class era:ContactLineSystem ;
    # Source: ontology range is http://data.europa.eu/949/ContactLineSystem

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "contactLineSystem: The CommonCharacteristicsSubset must have at most one contact line system value that is an instance of ContactLineSystem. This error may be due to having more than one value or having a value that is not an instance of ContactLineSystem."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable contact line system instances (IRI references)

    sh:path era:contactLineSystem ;

    sh:severity sh:Violation .

era-sh:ContactLineSystemApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:contactLineSystem ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a contact line system."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to contact line systems

    sh:message "contactLineSystem: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a contact line system."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:contactLineSystem ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ContactLineSystemType a sh:PropertyShape ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:contactLineSystemType ;
    era:scope "local" ;

    rdfs:comment "Indication of the type of the contact line system."@en ;
    # Source: ontology comment for contactLineSystemType

    sh:in ( "Overhead contact line (OCL)" "Third Rail" "Fourth Rail" "Not electrified" ) ;
    # Source: RAG states applicable values are predefined options

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "contactLineSystemType: The ContactLineSystem must have at most one contact line system type value that is one of the predefined options. This error may be due to having more than one value or having a value that is not one of the allowed options."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:contactLineSystemType ;

    sh:severity sh:Violation .

era-sh:ContactLineSystemTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:contactLineSystemType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the ContactLineSystem is defined and requires a contact line system type."@en ;
    # Source: inferred from the definition of ContactLineSystem and its relationship to contact line system types

    sh:message "contactLineSystemType: This error is due to the ContactLineSystem {?systemLabel}, violating the rule: Applicable when the ContactLineSystem is defined and requires a contact line system type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:contactLineSystemType ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ContactStripMaterial a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:contactStripMaterial ;
    era:scope "local" ;

    rdfs:comment "Indication of which contact strip materials are permitted to be used."@en ;
    # Source: ontology comment for contactStripMaterial

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "contactStripMaterial: The CommonCharacteristicsSubset must have at most one contact strip material value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:contactStripMaterial ;

    sh:severity sh:Violation .

era-sh:ContactStripMaterialApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:contactStripMaterial ;
    era:scope "local" ;

    rdfs:comment "Applicable only if 'Overhead contact line (OCL)' is selected in parameter 1.1.1.2.2.1.1."@en ;
    # Source: RAG states that this parameter is applicable only under specific conditions

    sh:message "contactStripMaterial: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only if 'Overhead contact line (OCL)' is selected."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:contactStripMaterial ?value } .
        # Check if 'Overhead contact line (OCL)' is selected
        FILTER NOT EXISTS { $this era:overheadContactLine ?oclValue . FILTER(?oclValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Contains a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:contains ;
    era:scope "local" ;

    rdfs:comment "Indicates that a subset with common characteristics contains a certain infrastructure element."@en ;
    # Source: ontology comment for contains

    sh:class era:InfrastructureElement ;
    # Source: ontology range is http://data.europa.eu/949/InfrastructureElement

    sh:maxCount 1 ;
    # Inferred: expected to relate to at most one infrastructure element, as it represents a specific relationship

    sh:message "contains: The CommonCharacteristicsSubset must have at most one contains reference that is an instance of InfrastructureElement. This error may be due to having more than one value or having a value that is not an instance of InfrastructureElement."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable infrastructure elements (IRI references)

    sh:path era:contains ;

    sh:severity sh:Violation .

era-sh:ContainsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:contains ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to an InfrastructureElement."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to contains

    sh:message "contains: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to an InfrastructureElement."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:contains ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DataGSMRNetwork a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:dataGSMRNetwork ;
    era:scope "local" ;

    rdfs:comment "Data SIM Card GSM-R Home Network."@en ;
    # Source: ontology comment for dataGSMRNetwork

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "dataGSMRNetwork: The VehicleType must have at most one data GSM-R network value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:dataGSMRNetwork ;

    sh:severity sh:Violation .

era-sh:DataGSMRNetworkApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:dataGSMRNetwork ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires a data GSM-R network."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to GSM-R networks

    sh:message "dataGSMRNetwork: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the VehicleType is defined and requires a data GSM-R network."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:dataGSMRNetwork ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DataRadioCompatible a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:dataRadioCompatible ;
    era:rinfIndex "1.1.1.3.3.10" ;
    era:scope "local" ;

    rdfs:comment "Radio requirements used for demonstrating technical compatibility data."@en ;
    # Source: ontology comment for dataRadioCompatible

    sh:in ( "Y" "N" "NYA" ) ;
    # Source: RAG states applicable values are Y/N/NYA

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "dataRadioCompatible: The VehicleType must have at most one data radio compatibility value that is either 'Y', 'N', or 'NYA'. This error may be due to having more than one value or having a value that is not one of the allowed options."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:dataRadioCompatible ;

    sh:severity sh:Violation .

era-sh:DataRadioCompatibleApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:dataRadioCompatible ;
    era:scope "local" ;

    rdfs:comment "Applicable when GSM-R and ETCS L2 must be installed for this parameter to be applicable."@en ;
    # Source: dependencyNote in ontology

    sh:message "dataRadioCompatible: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when GSM-R and ETCS L2 must be installed."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:dataRadioCompatible ?value } .
        # Check for the presence of GSM-R and ETCS L2 installations
        FILTER NOT EXISTS { $this era:hasGSMR "Y" } .
        FILTER NOT EXISTS { $this era:hasETCSL2 "Y" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DefinesSubset a sh:PropertyShape ;
    era:affectedClass era:InfrastructureManager ;
    era:affectedProperty era:definesSubset ;
    era:scope "local" ;

    rdfs:comment "(deprecated) not in use anymore. For the purposes of the register of infrastructure, each infrastructure manager may describe its railway network optionally via common characteristic subsets."@en ;
    # Source: ontology comment for definesSubset

    sh:class era:CommonCharacteristicsSubset ;
    # Source: ontology range is http://data.europa.eu/949/CommonCharacteristicsSubset

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "definesSubset: The InfrastructureManager must have at most one definesSubset reference that is an instance of CommonCharacteristicsSubset. This error may be due to having more than one value or having a value that is not an instance of CommonCharacteristicsSubset."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable common characteristic subsets (IRI references)

    sh:path era:definesSubset ;

    sh:severity sh:Violation .

era-sh:DefinesSubsetApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:InfrastructureManager ;
    era:affectedProperty era:definesSubset ;
    era:scope "local" ;

    rdfs:comment "Applicable when the InfrastructureManager is defined and requires a reference to a CommonCharacteristicsSubset."@en ;
    # Source: inferred from the definition of InfrastructureManager and its relationship to common characteristics

    sh:message "definesSubset: This error is due to the InfrastructureManager {?managerLabel}, violating the rule: Applicable when the InfrastructureManager is defined and requires a reference to a CommonCharacteristicsSubset."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?managerLabel WHERE {
        $this rdfs:label ?managerLabel .
        FILTER NOT EXISTS { $this era:definesSubset ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DigitalSchematicOverview a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:digitalSchematicOverview ;
    era:rinfIndex "1.2.0.0.0.7.2" ;
    era:scope "local" ;

    rdfs:comment "Diagrammatic representation of the operational point in Well Known Text polyline."@en ;
    # Source: ontology comment for digitalSchematicOverview

    sh:datatype geosparql:wktLiteral ;
    # Source: ontology range is http://www.opengis.net/ont/geosparql#wktLiteral

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "digitalSchematicOverview: The OperationalPoint must have at most one digital schematic overview value that is a WKT literal. This error may be due to having more than one value or having a value that is not a WKT literal."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable WKT representations (IRI references)

    sh:path era:digitalSchematicOverview ;

    sh:severity sh:Violation .

era-sh:DigitalSchematicOverviewApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:digitalSchematicOverview ;
    era:scope "local" ;

    rdfs:comment "Applicable when the operational point requires a digital schematic overview."@en ;
    # Source: inferred from the definition of digitalSchematicOverview and its relationship to operational points

    sh:message "digitalSchematicOverview: This error is due to the OperationalPoint {?opLabel}, violating the rule: Applicable when the operational point requires a digital schematic overview."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?opLabel WHERE {
        $this rdfs:label ?opLabel .
        FILTER NOT EXISTS { $this era:digitalSchematicOverview ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Direction a sh:PropertyShape ;
    era:affectedClass era:Orientation ;
    era:affectedProperty era:direction ;
    era:scope "local" ;

    rdfs:comment "The direction of the orientation of a railway element, in relation to the carrier linear element."@en ;
    # Source: ontology comment for direction

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "direction: The Orientation must have at most one direction value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:direction ;

    sh:severity sh:Violation .

era-sh:DirectionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Orientation ;
    era:affectedProperty era:direction ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Orientation is defined and requires a direction."@en ;
    # Source: inferred from the definition of Orientation and its relationship to direction

    sh:message "direction: This error is due to the Orientation {?orientationLabel}, violating the rule: Applicable when the Orientation is defined and requires a direction."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?orientationLabel WHERE {
        $this rdfs:label ?orientationLabel .
        FILTER NOT EXISTS { $this era:direction ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DocumentRestrictionPositionContactLineSeparation a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:documentRestrictionPositionContactLineSeparation ; 
    era:scope "local" ;

    rdfs:comment "Name and/or reference of the document specifying the restriction(s) related to the position of Multiple Traction unit(s) to comply with contact line separation."@en ; 
    # Source: ontology comment for documentRestrictionPositionContactLineSeparation

    sh:class era:Document ; 
    # Source: ontology range is http://data.europa.eu/949/Document

    sh:maxCount 1 ; 
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "documentRestrictionPositionContactLineSeparation: The CommonCharacteristicsSubset must have at most one document restriction position contact line separation value that is a Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ; 
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ; 
    # Inferred: links must refer to identifiable document instances (IRI references)

    sh:path era:documentRestrictionPositionContactLineSeparation ;

    sh:severity sh:Violation .

era-sh:DocumentRestrictionPositionContactLineSeparationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:documentRestrictionPositionContactLineSeparation ; 
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a document restriction position contact line separation."@en ; 
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to document restrictions

    sh:message "documentRestrictionPositionContactLineSeparation: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a document restriction position contact line separation."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:documentRestrictionPositionContactLineSeparation ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DocumentRestrictionPowerConsumption a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:documentRestrictionPowerConsumption ;
    era:scope "local" ;

    rdfs:comment "Name and/or reference of the document specifying the restriction(s) related to power consumption of specific electric traction unit(s)."@en ;
    # Source: ontology comment for documentRestrictionPowerConsumption

    sh:class era:Document ;
    # Source: ontology range is http://data.europa.eu/949/Document

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "documentRestrictionPowerConsumption: The CommonCharacteristicsSubset must have at most one document restriction power consumption value that is a Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable document instances (IRI references)

    sh:path era:documentRestrictionPowerConsumption ;

    sh:severity sh:Violation .

era-sh:DocumentRestrictionPowerConsumptionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:documentRestrictionPowerConsumption ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a document restriction related to power consumption."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to document restrictions

    sh:message "documentRestrictionPowerConsumption: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a document restriction related to power consumption."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:documentRestrictionPowerConsumption ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EddyCurrentBraking a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:eddyCurrentBraking ;
    era:scope "local" ;

    rdfs:comment "Indication of limitations on the use of eddy current brakes."@en ;
    # Source: ontology comment for eddyCurrentBraking

    sh:in ( "allowed" "allowed under conditions" "allowed only for emergency brake" "allowed under conditions only for emergency brake" "not allowed" ) ;
    # Source: RAG states applicable values are predefined options

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "eddyCurrentBraking: The CommonCharacteristicsSubset must have at most one eddy current braking value that is one of the predefined options. This error may be due to having more than one value or having a value that is not one of the allowed options."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:eddyCurrentBraking ;

    sh:severity sh:Violation .

era-sh:EddyCurrentBrakingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:eddyCurrentBraking ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to eddy current braking limitations."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to eddy current braking

    sh:message "eddyCurrentBraking: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to eddy current braking limitations."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:eddyCurrentBraking ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EddyCurrentBrakingConditionsDocument a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:eddyCurrentBrakingConditionsDocument ;
    era:scope "local" ;

    rdfs:comment "Electronic document available in two EU languages from the IM stored by the Agency with conditions for the use of eddy current brakes identified in 1.1.1.1.6.2."@en ;
    # Source: ontology comment for eddyCurrentBrakingConditionsDocument

    sh:class era:Document ;
    # Source: ontology range is http://data.europa.eu/949/Document

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "eddyCurrentBrakingConditionsDocument: The CommonCharacteristicsSubset must have at most one eddy current braking conditions document value that is a Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable document instances (IRI references)

    sh:path era:eddyCurrentBrakingConditionsDocument ;

    sh:severity sh:Violation .

era-sh:EddyCurrentBrakingConditionsDocumentApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:eddyCurrentBrakingConditionsDocument ;
    era:scope "local" ;

    rdfs:comment "Applicable if the answer to the use of eddy current brakes is 'allowed under conditions' or 'allowed under conditions only for emergency brake'."@en ;
    # Source: RAG states that this parameter is applicable only under specific conditions

    sh:message "eddyCurrentBrakingConditionsDocument: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable if the answer to the use of eddy current brakes is 'allowed under conditions' or 'allowed under conditions only for emergency brake'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:eddyCurrentBrakingConditionsDocument ?value } .
        # Check if the use of eddy current brakes is allowed under conditions
        FILTER NOT EXISTS { $this era:eddyCurrentBraking ?ecbValue . FILTER(?ecbValue IN ("allowed under conditions", "allowed only for emergency brake")) } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ElementA a sh:PropertyShape ;
    era:affectedClass era:NetRelation ;
    era:affectedProperty era:elementA ;
    era:scope "local" ;

    rdfs:comment "References the first topological element in an oriented relation. For example, it may indicate the object that corresponds to a section of line, an operational point or a track inside an operational point connected at start."@en ;
    # Source: ontology comment for elementA

    sh:class era:LinearElement ;
    # Source: ontology range is http://data.europa.eu/949/LinearElement

    sh:maxCount 1 ;
    # Inferred: expected to relate to at most one linear element, as it represents a specific relationship

    sh:message "elementA: The NetRelation must have at most one elementA reference that is an instance of LinearElement. This error may be due to having more than one value or having a value that is not an instance of LinearElement."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable linear elements (IRI references)

    sh:path era:elementA ;

    sh:severity sh:Violation .

era-sh:ElementAApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:NetRelation ;
    era:affectedProperty era:elementA ;
    era:scope "local" ;

    rdfs:comment "Applicable when the NetRelation is defined and requires a reference to a LinearElement."@en ;
    # Source: inferred from the definition of NetRelation and its relationship to elementA

    sh:message "elementA: This error is due to the NetRelation {?relationLabel}, violating the rule: Applicable when the NetRelation is defined and requires a reference to a LinearElement."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?relationLabel WHERE {
        $this rdfs:label ?relationLabel .
        FILTER NOT EXISTS { $this era:elementA ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ElementB a sh:PropertyShape ;
    era:affectedClass era:NetRelation ;
    era:affectedProperty era:elementB ;
    era:scope "local" ;

    rdfs:comment "References the second topological linear element in an oriented relation. For example, it may indicate the object that corresponds to a section of line, an operational point or a track inside an operational point connected at end."@en ;
    # Source: ontology comment for elementB

    sh:class era:LinearElement ;
    # Source: ontology range is http://data.europa.eu/949/LinearElement

    sh:maxCount 1 ;
    # Inferred: expected to relate to at most one linear element, as it represents a specific relationship

    sh:message "elementB: The NetRelation must have at most one elementB reference that is an instance of LinearElement. This error may be due to having more than one value or having a value that is not an instance of LinearElement."@en ;

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable linear elements (IRI references)

    sh:path era:elementB ;

    sh:severity sh:Violation .

era-sh:ElementBApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:NetRelation ;
    era:affectedProperty era:elementB ;
    era:scope "local" ;

    rdfs:comment "Applicable when the NetRelation is defined and requires a reference to a LinearElement."@en ;
    # Source: inferred from the definition of NetRelation and its relationship to elementB

    sh:message "elementB: This error is due to the NetRelation {?relationLabel}, violating the rule: Applicable when the NetRelation is defined and requires a reference to a LinearElement."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?relationLabel WHERE {
        $this rdfs:label ?relationLabel .
        FILTER NOT EXISTS { $this era:elementB ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EndCouplingType a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:endCouplingType ;
    era:rinfIndex "4.9.1" ;
    era:scope "local" ;

    rdfs:comment "Type of end coupling (indicating tensile and compressive forces)."@en ;
    # Source: ontology comment for endCouplingType

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "endCouplingType: The VehicleType must have at most one end coupling type value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:endCouplingType ;

    sh:severity sh:Violation .

era-sh:EndCouplingTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:endCouplingType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires an end coupling type."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to end coupling type

    sh:message "endCouplingType: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the VehicleType is defined and requires an end coupling type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:endCouplingType ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EndLocation a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:endLocation ;
    era:rinfIndex "1.1.1.1.8.4" ;
    era:scope "local" ;

    rdfs:comment "Geographical coordinates in decimal degrees and km of the line at the end of a tunnel."@en ;
    # Source: ontology comment for endLocation

    sh:datatype xsd:string ;
    # Inferred: based on the data presentation format specified in the RAG

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:pattern "Latitude=([-+]?\\d{1,2}\\.\\d{7}) Longitude=([-+]?\\d{1,3}\\.\\d{7}) Kilometer=([-+]?\\d{1,3}\\.\\d{3})" ;
    # Source: RAG specifies the format for geographical coordinates and km

    sh:message "endLocation: The Tunnel must have at most one end location value that follows the specified format for geographical coordinates and km. This error may be due to having more than one value or having a value that does not conform to the required format."@en ;

    sh:path era:endLocation ;

    sh:severity sh:Violation .

era-sh:EndLocationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:endLocation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Tunnel is defined and requires an end location."@en ;
    # Source: inferred from the definition of Tunnel and its relationship to end location

    sh:message "endLocation: This error is due to the Tunnel {?tunnelLabel}, violating the rule: Applicable when the Tunnel is defined and requires an end location."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:endLocation ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EndsAt a sh:PropertyShape ;
    era:affectedClass era:NetLinearReference ;
    era:affectedProperty era:endsAt ;
    era:scope "local" ;

    rdfs:comment "Specifies the ending coordinate of a linear reference."@en ;
    # Source: ontology comment for endsAt

    sh:class era:TopologicalCoordinate ;
    # Source: ontology range is http://data.europa.eu/949/TopologicalCoordinate

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "endsAt: The NetLinearReference must have at most one ending coordinate value that is an instance of TopologicalCoordinate. This error may be due to having more than one value or having a value that is not an instance of TopologicalCoordinate."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable topological coordinates (IRI references)

    sh:path era:endsAt ;

    sh:severity sh:Violation .

era-sh:EndsAtApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:NetLinearReference ;
    era:affectedProperty era:endsAt ;
    era:scope "local" ;

    rdfs:comment "Applicable when the NetLinearReference is defined and requires an ending coordinate."@en ;
    # Source: inferred from the definition of NetLinearReference and its relationship to endsAt

    sh:message "endsAt: This error is due to the NetLinearReference {?referenceLabel}, violating the rule: Applicable when the NetLinearReference is defined and requires an ending coordinate."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?referenceLabel WHERE {
        $this rdfs:label ?referenceLabel .
        FILTER NOT EXISTS { $this era:endsAt ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EnergySupplySystem a sh:PropertyShape ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:energySupplySystem ;
    era:rinfIndex "4.10.1" ;
    era:scope "local" ;

    rdfs:comment "Indication of the traction supply system (nominal voltage and frequency)."@en ;
    # Source: ontology comment for energySupplySystem

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "energySupplySystem: The ContactLineSystem must have at most one energy supply system value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:energySupplySystem ;

    sh:severity sh:Violation .

era-sh:EnergySupplySystemApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:energySupplySystem ;
    era:scope "local" ;

    rdfs:comment "Applicable when the ContactLineSystem is defined and requires an energy supply system."@en ;
    # Source: inferred from the definition of ContactLineSystem and its relationship to energy supply system

    sh:message "energySupplySystem: This error is due to the ContactLineSystem {?contactLineLabel}, violating the rule: Applicable when the ContactLineSystem is defined and requires an energy supply system."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?contactLineLabel WHERE {
        $this rdfs:label ?contactLineLabel .
        FILTER NOT EXISTS { $this era:energySupplySystem ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ErrorCorrectionsOnboard a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:errorCorrectionsOnboard ;
    era:scope "local" ;

    rdfs:comment "List of unacceptable errors impacting the IM network that are required to be solved in the on-board according to the TSI CCS point 7.2.10.3 specification maintenance point (ETCS, GSM-R and/or ATO)."@en ;
    # Source: ontology comment for errorCorrectionsOnboard

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "errorCorrectionsOnboard: The CommonCharacteristicsSubset must have at most one error corrections onboard value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:errorCorrectionsOnboard ;

    sh:severity sh:Violation .

era-sh:ErrorCorrectionsOnboardApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:errorCorrectionsOnboard ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to error corrections onboard."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to error corrections

    sh:message "errorCorrectionsOnboard: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to error corrections onboard."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:errorCorrectionsOnboard ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ETCS a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:etcs ;
    era:scope "local" ;

    rdfs:comment "European Train Control System (ETCS) application level supported by the track side equipment."@en ;
    # Source: ontology comment for etcs

    sh:class era:ETCS ;
    # Source: ontology range is http://data.europa.eu/949/ETCS

    sh:maxCount 1 ;
    # Source: inferred from the nature of the property and its definition, as it is expected to represent a specific ETCS instance

    sh:message "etcs: The CommonCharacteristicsSubset must have at most one ETCS value that is an instance of ETCS. This error may be due to having more than one value or having a value that is not an instance of ETCS."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable ETCS instances (IRI references)

    sh:path era:etcs ;

    sh:severity sh:Violation .

era-sh:ETCSApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:etcs ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to the ETCS application level."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to ETCS

    sh:message "etcs: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to the ETCS application level."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:etcs ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EtcsBaseline a sh:PropertyShape ;
    era:affectedClass era:ETCS ;
    era:affectedProperty era:etcsBaseline ;
    era:scope "local" ;

    rdfs:comment "ETCS baseline installed lineside."@en ;
    # Source: ontology comment for etcsBaseline

    sh:in ( "prebaseline 2" "baseline 2" "baseline 3" "Maintenance release 1" "baseline 3 release 2" ) ;
    # Source: RAG specifies the predefined list of acceptable values

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "etcsBaseline: The ETCS must have at most one ETCS baseline value that is one of the predefined options. This error may be due to having more than one value or having a value that is not one of the allowed options."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:etcsBaseline ;

    sh:severity sh:Violation .

era-sh:EtcsBaselineApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ETCS ;
    era:affectedProperty era:etcsBaseline ;
    era:scope "local" ;

    rdfs:comment "Applicable when the ETCS is defined and requires a reference to the ETCS baseline."@en ;
    # Source: inferred from the definition of ETCS and its relationship to the baseline

    sh:message "etcsBaseline: This error is due to the ETCS {?etcsLabel}, violating the rule: Applicable when the ETCS is defined and requires a reference to the ETCS baseline."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?etcsLabel WHERE {
        $this rdfs:label ?etcsLabel .
        FILTER NOT EXISTS { $this era:etcsBaseline ?value } .
        # Check if the ETCS baseline is applicable based on the value of 1.1.1.3.2.1
        FILTER NOT EXISTS { $this era:etcsLevel ?levelValue . FILTER(?levelValue = "N") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EtcsDegradedSituation a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:etcsDegradedSituation ;
    era:scope "local" ;

    rdfs:comment "ERTMS / ETCS application level for degraded situation related to the track side equipment."@en ;
    # Source: ontology comment for etcsDegradedSituation

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "etcsDegradedSituation: The CommonCharacteristicsSubset must have at most one ETCS degraded situation value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:etcsDegradedSituation ;

    sh:severity sh:Violation .

era-sh:EtcsDegradedSituationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:etcsDegradedSituation ;
    era:scope "local" ;

    rdfs:comment "Applicable when ETCS is installed and the situation is defined."@en ;
    # Source: RAG specifies applicability conditions based on ETCS installation

    sh:message "etcsDegradedSituation: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when ETCS is installed and the situation is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:etcsDegradedSituation ?value } .
        # Check if ETCS is installed
        FILTER NOT EXISTS { $this era:etcs ?etcsValue . FILTER(?etcsValue = "N") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ETCSEquipmentOnBoardLevel a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:etcsEquipmentOnBoardLevel ;
    era:scope "local" ;

    rdfs:comment "ETCS equipment on-board and its level."@en ;
    # Source: ontology comment for etcsEquipmentOnBoardLevel

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "etcsEquipmentOnBoardLevel: The VehicleType must have at most one ETCS equipment on-board level value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:etcsEquipmentOnBoardLevel ;

    sh:severity sh:Violation .

era-sh:ETCSEquipmentOnBoardLevelApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:etcsEquipmentOnBoardLevel ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires a reference to the ETCS equipment on-board level."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to etcsEquipmentOnBoardLevel

    sh:message "etcsEquipmentOnBoardLevel: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the VehicleType is defined and requires a reference to the ETCS equipment on-board level."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:etcsEquipmentOnBoardLevel ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EtcsInfill a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:etcsInfill ;
    era:scope "local" ;

    rdfs:comment "Information about installed trackside equipment capable to transmit infill information by loop or GSM-R for level 1 installations."@en ;
    # Source: ontology comment for etcsInfill

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "etcsInfill: The VehicleType must have at most one ETCS infill value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:etcsInfill ;

    sh:severity sh:Violation .

era-sh:EtcsInfillApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:etcsInfill ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires a reference to ETCS infill."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to etcsInfill

    sh:message "etcsInfill: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the VehicleType is defined and requires a reference to ETCS infill."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:etcsInfill ?value } .
        # Check if the applicable conditions are met based on the value of 1.1.1.3.2.1
        FILTER NOT EXISTS { $this era:etcsLevel ?levelValue . FILTER(?levelValue = "N") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EtcsLevelType a sh:PropertyShape ;
    era:affectedClass era:ETCS ;
    era:affectedProperty era:etcsLevelType ;
    era:rinfIndex "1.1.1.3.2.1" ;
    era:scope "local" ;

    rdfs:comment "ERTMS / ETCS application level related to the track side equipment."@en ;
    # Source: ontology comment for etcsLevelType

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "etcsLevelType: The ETCS must have at most one ETCS level type value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:etcsLevelType ;

    sh:severity sh:Violation .

era-sh:EtcsLevelTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ETCS ;
    era:affectedProperty era:etcsLevelType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the ETCS is defined and requires an ETCS level type."@en ;
    # Source: inferred from the definition of ETCS and its relationship to etcsLevelType

    sh:message "etcsLevelType: This error is due to the ETCS {?etcsLabel}, violating the rule: Applicable when the ETCS is defined and requires an ETCS level type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?etcsLabel WHERE {
        $this rdfs:label ?etcsLabel .
        FILTER NOT EXISTS { $this era:etcsLevelType ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EtcsMVersion a sh:PropertyShape ;
    era:affectedClass era:ETCS ;
    era:affectedProperty era:etcsMVersion ;
    era:rinfIndex "1.1.1.3.2.10" ;
    era:scope "local" ;

    rdfs:comment "ETCS M_version according to SRS 7.5.1.9."@en ;
    # Source: ontology comment for etcsMVersion

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:in ( "1.0" "1.1" "2.0" "2.1" ) ;
    # Source: RAG specifies the predefined list of acceptable values for ETCS M_version

    sh:message "etcsMVersion: The ETCS must have at most one ETCS M_version value that is one of the predefined options. This error may be due to having more than one value or having a value that is not one of the allowed options."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:etcsMVersion ;

    sh:severity sh:Violation .

era-sh:EtcsMVersionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ETCS ;
    era:affectedProperty era:etcsMVersion ;
    era:scope "local" ;

    rdfs:comment "Applicable only when selected value for 1.1.1.3.2.1 is ‘1’. Not ‘N’."@en ;
    # Source: RAG specifies applicability conditions based on the value of 1.1.1.3.2.1

    sh:message "etcsMVersion: This error is due to the ETCS {?etcsLabel}, violating the rule: Applicable only when the selected value for 1.1.1.3.2.1 is '1'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?etcsLabel WHERE {
        $this rdfs:label ?etcsLabel .
        FILTER NOT EXISTS { $this era:etcsMVersion ?value } .
        # Check if the applicable conditions are met based on the value of 1.1.1.3.2.1
        FILTER NOT EXISTS { $this era:etcsLevelType ?levelValue . FILTER(?levelValue = "N") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EtcsNationalPacket44Function a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:etcsNationalPacket44Function ;
    era:scope "local" ;

    rdfs:comment "Indication whether data for national applications is transmitted between track and train."@en ;
    # Source: ontology comment for etcsNationalPacket44Function

    sh:in ( "Y" "N" "NYA" ) ;
    # Source: RAG specifies the predefined list of acceptable values

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "etcsNationalPacket44Function: The CommonCharacteristicsSubset must have at most one ETCS national packet 44 function value that is one of the predefined options (Y/N/NYA). This error may be due to having more than one value or having a value that is not one of the allowed options."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:etcsNationalPacket44Function ;

    sh:severity sh:Violation .

era-sh:EtcsNationalPacket44FunctionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:etcsNationalPacket44Function ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to the ETCS national packet 44 function."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to etcsNationalPacket44Function

    sh:message "etcsNationalPacket44Function: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to the ETCS national packet 44 function."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:etcsNationalPacket44Function ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EtcsRestrictionsConditionsDoc a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:etcsRestrictionsConditionsDoc ;
    era:rinfIndex "1.1.1.3.2.6" ;
    era:scope "local" ;

    rdfs:comment "Indication of the document where restrictions or conditions due to partial compliance with the TSI CCS are described."@en ;
    # Source: ontology comment for etcsRestrictionsConditionsDoc

    sh:class era:Document ;
    # Source: ontology range is http://data.europa.eu/949/Document

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "etcsRestrictionsConditionsDoc: The CommonCharacteristicsSubset must have at most one ETCS restrictions conditions document value that is a Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable documents (IRI references)

    sh:path era:etcsRestrictionsConditionsDoc ;

    sh:severity sh:Violation .

era-sh:EtcsRestrictionsConditionsDocApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:etcsRestrictionsConditionsDoc ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to the ETCS restrictions conditions document."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to etcsRestrictionsConditionsDoc

    sh:message "etcsRestrictionsConditionsDoc: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to the ETCS restrictions conditions document."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:etcsRestrictionsConditionsDoc ?value } .
        # Check if the applicable conditions are met based on the presence of ETCS
        FILTER NOT EXISTS { $this era:etcs ?etcsValue . FILTER(?etcsValue = "N") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EtcsSystemCompatibility a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:etcsSystemCompatibility ;
    era:scope "local" ;

    rdfs:comment "ETCS requirements used for demonstrating technical compatibility."@en ;
    # Source: ontology comment for etcsSystemCompatibility

    sh:in ( "Not Defined" "ESC-EU-0" "ESC-SE-01-HiL2" "ESC-SE-02-BoL2" "ESC-SE-03-L3" "ESC-SE-04-HiL2B3" "ESC-SE-05-BoL2B3" "ESC-NL-01" "ESC-NL-02" "ESC-NL-03" "ESC-NL-04" "ESC-NL-05" "ESC-NL-06" "ESC-NL-07" "ESC-NL-08" "ESC-NL-09" "ESC-NL-10" "ESC-NL-11" "ESC-NL-12" "ESC-NL-13" "ESC-NL-14" "ESC-NL-15" "ESC-NL-16" "ESC-NL-17" "ESC-NL-18" "ESC-NL-19" "ESC-NL-20" "ESC-NL-21" "ESC-NL-22" "ESC-NL-23" "ESC-FR-01-LB" "ESC-FR-02-LB" "ESC-FR-03-LB" "ESC-FR-04-LB" "ESC-FR-05-LB" "ESC-FR-06-LB" "ESC-FR-07-SF" "ESC-FR-08-SF" "ESC-FR-09-SF" "ESC-FR-10-SF" "ESC-FR-11-SF" "ESC-FR-12-SF" "ESC-FR-13-SF" "ESC-FR-14-SF" "ESC-FR-15-SF" "ESC-FR-16-SF" "ESC-FR-17-SF" "ESC-FR-18-SF" "ESC-FR-19-SF" "ESC-FR-20-SF" "ESC-FR-21-SF" "ESC-FR-22-LB" "ESC-FR-23-LB" "ESC-FR-24-AA" "ESC-FR-25-AD" "ESC-FR-26-AE" "ESC-FR-27-LGVEE" "ESC-FR-28-LGVEE" "ESC-FR-29-LGVEE" "ESC-FR-30-LGVEE" "ESC-FR-31-LGVEE" "ESC-FR-32-LGVEE" "ESC-FR-33-SEA" "ESC-FR-34-SEA" "ESC-FR-35-BPL" "ESC-FR-36-BPL" "ESC-BE-02-L2FS" "ESC-BE-03-L1LS" "ESC-BE-01-L1FS" "ESC-BE-04-LGV3_4" "ESC-IT-01-RFI-1.0_L2_AVp_RMNA_01" "ESC-IT-02-RFI-1.0_L2_AVp_MIBO_01" "ESC-IT-03-RFI-1.0_L2_AVp_BOFI_01" "ESC-IT-04-RFI-1.0_L2_AVp_TOMI_01" "ESC-IT-05-RFI-1.0_L2_AVp_TRBR_01" "ESC-IT-06-RFI-1.0_L2_AVp_DD_01" "ESC-IT-08-RFI-2.0_L1_Cs_DONO_01" "ESC-IT-07-RFI-2.0_L1_Cs_ISDO_01" "ESC-IT-09-RFI-2.0_L1_Cs_CHIASSO_01" "ESC-IT-10-RFI-2.1_L2_Cs_NOPD_01" "ESC-IT-11-RFI-2.0_L1_Cs_PTLU_01" "ESC-IT-12-RFI-2.0_L1_Cs_ISDO_CH_01" "ESC-IT-13-RFI-2.0_L1_Cs_PTLU_CH_01" "ESC-IT-14-RFI_2.1_L1_Cs_VENTIMIGLIA_01" "ESC-IT-15-RFI_2.1_L1_Cs_VENTIMIGLIA_FR_01" "ESC-IT-16-RFI_2.1_L1_Cs_VIVO_01" "ESC-IT-17-RFI_2.1_L2_Cs_MIMOCH_01" "ESC-IT-18-RFI_2.1_L2_Cs_NOPD_02" "ESC-IT-19-RFI_1.0_L2_AVp_AGGR01_01" "ESC-IT-20-RFI_1.0_L2_AVp_AGGR02_01" "ESC-IT-21-RFI_2.0_L1_Cs_AGGR03_01" "ESC-IT-23-RFI_2.1_L1_Cs_AGGR05_01" "ESC-PL-01-L1" "ESC-PL-02-L1LS" "ESC-PL-03-L2" "ESC-PL-04-L2" "ESC-PL-05-L2" "ESC-PL-06-L2" "ESC-NO-01" "ESC-NO-02" "ESC-DK-01-East" "ESC-DK-02-West" "ESC-AT-01" "ESC-CZ-01" "ESC-CZ-02" "ESC-RO-01" "ESC-DE-01-B2_L2" "ESC-DE-02-B3_L2" "ESC-CH-01-L1LS" "ESC-CH-02-L2" "ESC-CH-03-L1LSL2" "ESC-LU-01-RFN" "ESC-LU-02-MSM" ) ;
    # Source: RAG specifies the predefined list of acceptable values for ETCS system compatibility

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "etcsSystemCompatibility: The VehicleType must have at most one ETCS system compatibility value that is one of the predefined options. This error may be due to having more than one value or having a value that is not one of the allowed options."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:etcsSystemCompatibility ;

    sh:severity sh:Violation .

era-sh:EtcsSystemCompatibilityApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:etcsSystemCompatibility ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires a reference to ETCS system compatibility."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to etcsSystemCompatibility

    sh:message "etcsSystemCompatibility: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the VehicleType is defined and requires a reference to ETCS system compatibility."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:etcsSystemCompatibility ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EtcsTransmittedTrackConditions a sh:PropertyShape ;
    era:affectedClass era:ETCS ;
    era:affectedProperty era:etcsTransmittedTrackConditions ;
    era:scope "local" ;

    rdfs:comment "Transmittable track conditions by the CCSSubsystem, according to the specification referenced in Appendix A-1, index [C]."@en ;
    # Source: ontology comment for etcsTransmittedTrackConditions

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "etcsTransmittedTrackConditions: The ETCS must have at most one transmitted track conditions value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:etcsTransmittedTrackConditions ;

    sh:severity sh:Violation .

era-sh:EtcsTransmittedTrackConditionsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ETCS ;
    era:affectedProperty era:etcsTransmittedTrackConditions ;
    era:scope "local" ;

    rdfs:comment "Applicable when ETCS is present and requires a reference to the transmitted track conditions."@en ;
    # Source: inferred from the dependency note indicating mandatory conditions

    sh:message "etcsTransmittedTrackConditions: This error is due to the ETCS {?etcsLabel}, violating the rule: Applicable when ETCS is present and requires a reference to the transmitted track conditions."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?etcsLabel WHERE {
        $this rdfs:label ?etcsLabel .
        FILTER NOT EXISTS { $this era:etcsTransmittedTrackConditions ?value } .
        # Check if ETCS is present
        FILTER NOT EXISTS { $this era:etcsLevelType ?levelValue . FILTER(?levelValue = "N") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:FireSafetyCategory a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:fireSafetyCategory ;
    era:scope "local" ;

    rdfs:comment "Fire safety category for tunnels."@en ;
    # Source: ontology comment for fireSafetyCategory

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "fireSafetyCategory: The VehicleType must have at most one fire safety category value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:fireSafetyCategory ;

    sh:severity sh:Violation .

era-sh:FireSafetyCategoryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:fireSafetyCategory ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires a reference to the fire safety category."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to fireSafetyCategory

    sh:message "fireSafetyCategory: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the VehicleType is defined and requires a reference to the fire safety category."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:fireSafetyCategory ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:FreightCorridor a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:freightCorridor ;
    era:scope "local" ;

    rdfs:comment "Indication whether the line is designated to a Railway Freight Corridor."@en ;
    # Source: ontology comment for freightCorridor

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "freightCorridor: The CommonCharacteristicsSubset must have at most one freight corridor value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:freightCorridor ;

    sh:severity sh:Violation .

era-sh:FreightCorridorApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:freightCorridor ;
    era:scope "local" ;

    rdfs:comment "Applicable when the line is part of a Railway Freight Corridor (RFC)."@en ;
    # Source: RAG specifies applicability conditions based on RFC membership

    sh:message "freightCorridor: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the line is part of a Railway Freight Corridor."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:freightCorridor ?value } .
        # Check if the applicable conditions are met based on the value of the line's RFC status
        FILTER NOT EXISTS { $this era:partOfRFC ?rfcValue . FILTER(?rfcValue = "N") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:FrenchTrainDetectionSystemLimitation a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:frenchTrainDetectionSystemLimitation ;
    era:scope "local" ;

    rdfs:comment "Specific for route compatibility check on French network. The property is composed of two values: A boolean [Y/N] and a number N between 1 and 8."@en ;
    # Source: ontology comment for frenchTrainDetectionSystemLimitation

    sh:in ( "Y" "N" "NYA" ) ;
    # Source: RAG specifies the predefined list of acceptable values for the boolean part

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "frenchTrainDetectionSystemLimitation: The TrainDetectionSystem must have at most one French train detection system limitation value that is one of the predefined options (Y/N/NYA). This error may be due to having more than one value or having a value that is not one of the allowed options."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:frenchTrainDetectionSystemLimitation ;

    sh:severity sh:Violation .

era-sh:FrenchTrainDetectionSystemLimitationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:frenchTrainDetectionSystemLimitation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the TrainDetectionSystem is defined and requires a reference to the French train detection system limitation."@en ;
    # Source: inferred from the definition of TrainDetectionSystem and its relationship to frenchTrainDetectionSystemLimitation

    sh:message "frenchTrainDetectionSystemLimitation: This error is due to the TrainDetectionSystem {?tdsLabel}, violating the rule: Applicable when the TrainDetectionSystem is defined and requires a reference to the French train detection system limitation."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tdsLabel WHERE {
        $this rdfs:label ?tdsLabel .
        FILTER NOT EXISTS { $this era:frenchTrainDetectionSystemLimitation ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:FrenchTrainDetectionSystemLimitationNumber a sh:PropertyShape ;
    era:affectedClass era:FrenchTrainDetectionSystemLimitation ;
    era:affectedProperty era:frenchTrainDetectionSystemLimitationNumber ;
    era:scope "local" ;

    rdfs:comment "Part of the section with train detection limitation that indicates the type of train detection limitation specific for route compatibility check on French network."@en ;
    # Source: ontology comment for frenchTrainDetectionSystemLimitationNumber

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "frenchTrainDetectionSystemLimitationNumber: The FrenchTrainDetectionSystemLimitation must have at most one limitation number value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:frenchTrainDetectionSystemLimitationNumber ;

    sh:severity sh:Violation .

era-sh:FrenchTrainDetectionSystemLimitationNumberApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:FrenchTrainDetectionSystemLimitation ;
    era:affectedProperty era:frenchTrainDetectionSystemLimitationNumber ;
    era:scope "local" ;

    rdfs:comment "Applicable when the FrenchTrainDetectionSystemLimitation is defined and requires a reference to the limitation number."@en ;
    # Source: inferred from the definition of FrenchTrainDetectionSystemLimitation and its relationship to frenchTrainDetectionSystemLimitationNumber

    sh:message "frenchTrainDetectionSystemLimitationNumber: This error is due to the FrenchTrainDetectionSystemLimitation {?limitationLabel}, violating the rule: Applicable when the FrenchTrainDetectionSystemLimitation is defined and requires a reference to the limitation number."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?limitationLabel WHERE {
        $this rdfs:label ?limitationLabel .
        FILTER NOT EXISTS { $this era:frenchTrainDetectionSystemLimitationNumber ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:FrequencyBandsForDetection a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:frequencyBandsForDetection ;
    era:rinfIndex "1.1.1.3.4.2" ;
    era:scope "local" ;

    rdfs:comment "Bands of the frequency management of the train detection systems as defined in the TSI CCS."@en ;
    # Source: ontology comment for frequencyBandsForDetection

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:in ( "Axle Counters" "Track circuits" ) ;
    # Source: RAG specifies the predefined list of acceptable values for frequency bands

    sh:message "frequencyBandsForDetection: The TrainDetectionSystem must have at most one frequency band for detection value that is one of the predefined options. This error may be due to having more than one value or having a value that is not one of the allowed options."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:frequencyBandsForDetection ;

    sh:severity sh:Violation .

era-sh:FrequencyBandsForDetectionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:frequencyBandsForDetection ;
    era:scope "local" ;

    rdfs:comment "Applicable when the TrainDetectionSystem is defined and requires a reference to frequency bands for detection."@en ;
    # Source: inferred from the definition of TrainDetectionSystem and its relationship to frequencyBandsForDetection

    sh:message "frequencyBandsForDetection: This error is due to the TrainDetectionSystem {?tdsLabel}, violating the rule: Applicable when the TrainDetectionSystem is defined and requires a reference to frequency bands for detection."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tdsLabel WHERE {
        $this rdfs:label ?tdsLabel .
        FILTER NOT EXISTS { $this era:frequencyBandsForDetection ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GaugingProfile a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gaugingProfile ;
    era:rinfIndex "1.1.1.1.3.1.1" ;
    era:scope "local" ;

    rdfs:comment "Gauges as defined in European standard or other local gauges, including lower or upper part."@en ;
    # Source: ontology comment for gaugingProfile

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:in ( "GA" "GB" "GC" "G1" "DE3" "G2" "GB1" "GB2" "BE1" "BE2" "BE3" "FR-3.3" "PTb" "PTb+" "PTc" "FIN1" "SEa" "SEc" "DE1" "DE2" "Z-GCD" "UK1" "UK1[D]" "W6" "FS" "S" "GHE16" "GEA16" "GEB16" "GEC16" "IRL1" "IRL2" "IRL3" "GI1" "GI2" "GI3" "GEE10" "GED10" "AFM 423" "NL1" "NL2" "FR-3.4.1" "FR-3.4.2" "AFG" "AFM425" "AFM427" "M30" "M80" "Tram-train 2.40" "Tram-train 2.65" "Métrique BA" "Métrique SGV" "Métrique Cerd." "GB:GČD" "GCZ3" ) ;
    # Source: RAG specifies the predefined list of acceptable values for gaugingProfile

    sh:message "gaugingProfile: The CommonCharacteristicsSubset must have at most one gauging profile value that is one of the predefined options. This error may be due to having more than one value or having a value that is not one of the allowed options."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:gaugingProfile ;

    sh:severity sh:Violation .

era-sh:GaugingProfileApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gaugingProfile ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to the gauging profile."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to gaugingProfile

    sh:message "gaugingProfile: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to the gauging profile."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gaugingProfile ?value } .
        # Additional conditions can be added here if there are specific parameters to check
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GaugingTransversalDocument a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gaugingTransversalDocument ;
    era:scope "local" ;

    rdfs:comment "Electronic document available from the IM stored by the Agency with the transversal section of the particular points requiring specific checks due to deviations from gauging."@en ;
    # Source: ontology comment for gaugingTransversalDocument

    sh:class era:Document ;
    # Source: ontology range is http://data.europa.eu/949/Document

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "gaugingTransversalDocument: The CommonCharacteristicsSubset must have at most one gauging transversal document value that is a Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable documents (IRI references)

    sh:path era:gaugingTransversalDocument ;

    sh:severity sh:Violation .

era-sh:GaugingTransversalDocumentApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gaugingTransversalDocument ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to the gauging transversal document."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to gaugingTransversalDocument

    sh:message "gaugingTransversalDocument: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to the gauging transversal document."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gaugingTransversalDocument ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GsmRActiveMobiles a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gsmRActiveMobiles ;
    era:rinfIndex "1.1.1.3.3.2" ;
    era:scope "local" ;

    rdfs:comment "Number of active GSM-R mobiles (EDOR) or simultaneous communication session on board for ETCS level 2 needed to perform radio block centre handovers without having an operational disruption."@en ;
    # Source: ontology comment for gsmRActiveMobiles

    sh:in ( "0" "1" "2" ) ;
    # Source: RAG specifies the predefined list of acceptable values for GSM-R active mobiles

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "gsmRActiveMobiles: The CommonCharacteristicsSubset must have at most one GSM-R active mobiles value that is one of the predefined options (0, 1, 2). This error may be due to having more than one value or having a value that is not one of the allowed options."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:gsmRActiveMobiles ;

    sh:severity sh:Violation .

era-sh:GsmRActiveMobilesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gsmRActiveMobiles ;
    era:scope "local" ;

    rdfs:comment "Applicable only when level 2 or 3 is selected for parameter 1.1.1.3.2.1."@en ;
    # Source: RAG specifies applicability conditions based on ETCS level selection

    sh:message "gsmRActiveMobiles: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when level 2 or 3 is selected for parameter 1.1.1.3.2.1."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gsmRActiveMobiles ?value } .
        # Check if the applicable conditions are met based on the ETCS level
        FILTER NOT EXISTS { $this era:etcsLevelType ?levelValue . FILTER(?levelValue = "N") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GsmRAdditionalInfo a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gsmRAdditionalInfo ;
    era:scope "local" ;

    rdfs:comment "Any additional information on network characteristics or corresponding document available from the IM and stored by the Agency, e.g.; interference level, leading to the recommendation of additional on-board protection."@en ;
    # Source: ontology comment for gsmRAdditionalInfo

    sh:class era:Document ;
    # Source: ontology range is http://data.europa.eu/949/Document

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "gsmRAdditionalInfo: The CommonCharacteristicsSubset must have at most one GSM-R additional information value that is a Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable documents (IRI references)

    sh:path era:gsmRAdditionalInfo ;

    sh:severity sh:Violation .

era-sh:GsmRAdditionalInfoApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gsmRAdditionalInfo ;
    era:scope "local" ;

    rdfs:comment "Applicable when GSM-R is installed for this parameter to be applicable."@en ;
    # Source: dependency note indicates requirement for GSM-R installation

    sh:message "gsmRAdditionalInfo: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when GSM-R is installed for this parameter to be applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gsmRAdditionalInfo ?value } .
        # Check if GSM-R is installed
        FILTER NOT EXISTS { $this era:gsmRInstalled ?installedValue . FILTER(?installedValue = "N") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GsmROptionalFunctions a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gsmROptionalFunctions ;
    era:scope "local" ;

    rdfs:comment "Use of optional GSM-R functions which might improve operation on the line. They are for information only and not for network access criteria."@en ;
    # Source: ontology comment for gsmROptionalFunctions

    sh:in ( "Network selection manual" "Network selection via balise" "Network selection automatic" "Public emergency (112) available" "Broadcast calls (VBS) used" "Text message service used (SMS)" "Restriction of display of called/calling user" "Automatically forward of incoming call if no reply" "Automatically forward of incoming call if not reachable" "Use of chargeable Network Services" "General data applications" "ETCS RBC or other devices alerted when initiating a REC" "Display at the controller terminal of the location of the mobile initiating a REC" "Use of enhanced Railway Emergency Call (eREC)" "GSM-R shunting used" "Data recorded in case of Shunting Emergency Call" "Extended frequency bands used" "Other" ) ;
    # Source: RAG specifies the predefined list of acceptable values for optional GSM-R functions

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "gsmROptionalFunctions: The CommonCharacteristicsSubset must have at most one optional GSM-R functions value that is one of the predefined options. This error may be due to having more than one value or having a value that is not one of the allowed options."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:gsmROptionalFunctions ;

    sh:severity sh:Violation .

era-sh:GsmROptionalFunctionsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gsmROptionalFunctions ;
    era:scope "local" ;

    rdfs:comment "Applicable when GSM-R (parameter 1.1.1.3.3.1) must be installed for this parameter to be applicable."@en ;
    # Source: dependency note indicates requirement for GSM-R installation

    sh:message "gsmROptionalFunctions: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when GSM-R is installed for this parameter to be applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gsmROptionalFunctions ?value } .
        # Check if GSM-R is installed
        FILTER NOT EXISTS { $this era:gsmRInstalled ?installedValue . FILTER(?installedValue = "N") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GsmRRadioDataCommunication a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:gsmRRadioDataCommunication ;
    era:scope "local" ;

    rdfs:comment "GSM-R Radio Data communication on board and its Baseline."@en ;
    # Source: ontology comment for gsmRRadioDataCommunication

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "gsmRRadioDataCommunication: The VehicleType must have at most one GSM-R radio data communication value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:gsmRRadioDataCommunication ;

    sh:severity sh:Violation .

era-sh:GsmRRadioDataCommunicationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:gsmRRadioDataCommunication ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires a reference to GSM-R Radio Data communication."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to gsmRRadioDataCommunication

    sh:message "gsmRRadioDataCommunication: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the VehicleType is defined and requires a reference to GSM-R Radio Data communication."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:gsmRRadioDataCommunication ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GsmRVersion a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gsmRVersion ;
    era:scope "local" ;

    rdfs:comment "GSM-R version installed lineside."@en ;
    # Source: ontology comment for gsmRVersion

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:in ( "none" "previous version to Baseline 0" "Baseline 0" "Baseline 0 r3" "Baseline 0 r4" "Baseline 1" ) ;
    # Source: RAG specifies the predefined list of acceptable values for GSM-R versions

    sh:message "gsmRVersion: The CommonCharacteristicsSubset must have at most one GSM-R version value that is one of the predefined options. This error may be due to having more than one value or having a value that is not one of the allowed options."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:gsmRVersion ;

    sh:severity sh:Violation .

era-sh:GsmRVersionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gsmRVersion ;
    era:scope "local" ;

    rdfs:comment "Applicable when GSM-R must be installed for this parameter to be applicable."@en ;
    # Source: dependency note indicates requirement for GSM-R installation

    sh:message "gsmRVersion: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when GSM-R is installed for this parameter to be applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gsmRVersion ?value } .
        # Check if GSM-R is installed
        FILTER NOT EXISTS { $this era:gsmRInstalled ?installedValue . FILTER(?installedValue = "N") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GsmrConstraintsOperateOnlyInCircuitSwitch a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gsmrConstraintsOperateOnlyInCircuitSwitch ;
    era:rinfIndex "1.2.1.1.2.12" ;
    era:scope "local" ;

    rdfs:comment "These constraints, where applicable, are meant to manage the limited number of circuit-switched radio connections that can be handled simultaneously by a Radio Block Center."@en ;
    # Source: ontology comment for gsmrConstraintsOperateOnlyInCircuitSwitch

    sh:in ( "Y" "N" "NYA" ) ;
    # Source: RAG specifies the predefined list of acceptable values for the applicability of the parameter

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "gsmrConstraintsOperateOnlyInCircuitSwitch: The CommonCharacteristicsSubset must have at most one GSM-R constraints operate only in circuit switch value that is one of the predefined options (Y/N/NYA). This error may be due to having more than one value or having a value that is not one of the allowed options."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:gsmrConstraintsOperateOnlyInCircuitSwitch ;

    sh:severity sh:Violation .

era-sh:GsmrConstraintsOperateOnlyInCircuitSwitchApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gsmrConstraintsOperateOnlyInCircuitSwitch ;
    era:scope "local" ;

    rdfs:comment "Applicable when GSM-R and ETCS L2 must be installed for this parameter to be applicable."@en ;
    # Source: dependency note indicates requirement for GSM-R and ETCS L2 installation

    sh:message "gsmrConstraintsOperateOnlyInCircuitSwitch: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when GSM-R and ETCS L2 are installed."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gsmrConstraintsOperateOnlyInCircuitSwitch ?value } .
        # Check if GSM-R and ETCS L2 are installed
        FILTER NOT EXISTS { $this era:gsmRInstalled ?installedValue . FILTER(?installedValue = "N") } .
        FILTER NOT EXISTS { $this era:etcsLevelType ?levelValue . FILTER(?levelValue != "2" && ?levelValue != "3") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GsmrNetworkCoverage a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gsmrNetworkCoverage ;
    era:rinfIndex "1.1.1.3.3.5" ;
    era:scope "local" ;

    rdfs:comment "Name of the own GSM-R network and list of GSM-R networks which are covered by a roaming agreement (for CS services)."@en ;
    # Source: ontology comment for gsmrNetworkCoverage

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "gsmrNetworkCoverage: The CommonCharacteristicsSubset must have at most one GSM-R network coverage value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:gsmrNetworkCoverage ;

    sh:severity sh:Violation .

era-sh:GsmrNetworkCoverageApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gsmrNetworkCoverage ;
    era:scope "local" ;

    rdfs:comment "Applicable when GSM-R must be installed for this parameter to be applicable."@en ;
    # Source: dependency note indicates requirement for GSM-R installation

    sh:message "gsmrNetworkCoverage: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when GSM-R is installed for this parameter to be applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gsmrNetworkCoverage ?value } .
        # Check if GSM-R is installed
        FILTER NOT EXISTS { $this era:gsmRInstalled ?installedValue . FILTER(?installedValue = "N") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasAbstraction a sh:PropertyShape ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:hasAbstraction ;
    era:scope "local" ;

    rdfs:comment "Property that links a physical entity with its topological abstraction representation."@en ;
    # Source: ontology comment for hasAbstraction

    sh:class era:InfrastructureElement ;
    # Source: ontology range is era:InfrastructureElement (rdfs:range)

    sh:maxCount 1 ;
    # Inferred: expected at most one abstraction for a physical entity

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable infrastructure elements (IRI references)

    sh:message "hasAbstraction: The InfrastructureElement must have at most one abstraction value that is an instance of InfrastructureElement. This error may be due to having more than one value or having a value that is not an instance of InfrastructureElement."@en ;

    sh:path era:hasAbstraction ;

    sh:severity sh:Violation .

era-sh:HasAbstractionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:hasAbstraction ;
    era:scope "local" ;

    rdfs:comment "Applicable when the InfrastructureElement is defined and requires a reference to its abstraction."@en ;
    # Source: inferred from the definition of InfrastructureElement and its relationship to hasAbstraction

    sh:message "hasAbstraction: This error is due to the InfrastructureElement {?elementLabel}, violating the rule: Applicable when the InfrastructureElement is defined and requires a reference to its abstraction."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?elementLabel WHERE {
        $this rdfs:label ?elementLabel .
        FILTER NOT EXISTS { $this era:hasAbstraction ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasLRS a sh:PropertyShape ;
    era:affectedClass era:KilometricPost ;
    era:affectedProperty era:hasLRS ;
    era:scope "local" ;

    rdfs:comment "Specifies the linear referencing system to which the kilometre post is referenced. The linear referencing system is commonly the main railway line and it provides the framework for identifying the post's location along the railway line, using distance measurements rather than geographical coordinates."@en ;
    # Source: ontology comment for hasLRS

    sh:class era:LinearPositioningSystem ;
    # Source: ontology range is era:LinearPositioningSystem (rdfs:range)

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "hasLRS: The KilometricPost must have at most one linear referencing system value that is an instance of LinearPositioningSystem. This error may be due to having more than one value or having a value that is not an instance of LinearPositioningSystem."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable linear positioning systems (IRI references)

    sh:path era:hasLRS ;

    sh:severity sh:Violation .

era-sh:HasLRSApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:KilometricPost ;
    era:affectedProperty era:hasLRS ;
    era:scope "local" ;

    rdfs:comment "Applicable when the KilometricPost is defined and requires a reference to its linear referencing system."@en ;
    # Source: inferred from the definition of KilometricPost and its relationship to hasLRS

    sh:message "hasLRS: This error is due to the KilometricPost {?postLabel}, violating the rule: Applicable when the KilometricPost is defined and requires a reference to its linear referencing system."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?postLabel WHERE {
        $this rdfs:label ?postLabel .
        FILTER NOT EXISTS { $this era:hasLRS ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasLrsCoordinate a sh:PropertyShape ;
    era:affectedClass era:NetPointReference ;
    era:affectedProperty era:hasLrsCoordinate ;
    era:scope "local" ;

    rdfs:comment "Specifies the LRS coordinate associated with a topological coordinate."@en ;
    # Source: ontology comment for hasLrsCoordinate

    sh:class era:LinearPositioningSystemCoordinate ;
    # Source: ontology range is http://data.europa.eu/949/LinearPositioningSystemCoordinate

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "hasLrsCoordinate: The NetPointReference must have at most one LRS coordinate value that is an instance of LinearPositioningSystemCoordinate. This error may be due to having more than one value or having a value that is not an instance of LinearPositioningSystemCoordinate."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable coordinates (IRI references)

    sh:path era:hasLrsCoordinate ;

    sh:severity sh:Violation .

era-sh:HasLrsCoordinateApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:NetPointReference ;
    era:affectedProperty era:hasLrsCoordinate ;
    era:scope "local" ;

    rdfs:comment "Applicable when the NetPointReference is defined and requires a reference to its LRS coordinate."@en ;
    # Source: inferred from the definition of NetPointReference and its relationship to hasLrsCoordinate

    sh:message "hasLrsCoordinate: This error is due to the NetPointReference {?pointLabel}, violating the rule: Applicable when the NetPointReference is defined and requires a reference to its LRS coordinate."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?pointLabel WHERE {
        $this rdfs:label ?pointLabel .
        FILTER NOT EXISTS { $this era:hasLrsCoordinate ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasOrganisationRole a sh:PropertyShape ;
    era:affectedClass era:OrganisationRole ;
    era:affectedProperty era:hasOrganisationRole ;
    era:scope "local" ;

    rdfs:comment "Relates the Organisation role instance (the role played by an Organisation) with the specific role in the taxonomy of organisation roles."@en ;
    # Source: ontology comment for hasOrganisationRole

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "hasOrganisationRole: The OrganisationRole must have at most one organisation role value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable concepts (IRI references)

    sh:path era:hasOrganisationRole ;

    sh:severity sh:Violation .

era-sh:HasOrganisationRoleApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OrganisationRole ;
    era:affectedProperty era:hasOrganisationRole ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OrganisationRole is defined and requires a reference to its organisation role."@en ;
    # Source: inferred from the definition of OrganisationRole and its relationship to hasOrganisationRole

    sh:message "hasOrganisationRole: This error is due to the OrganisationRole {?roleLabel}, violating the rule: Applicable when the OrganisationRole is defined and requires a reference to its organisation role."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?roleLabel WHERE {
        $this rdfs:label ?roleLabel .
        FILTER NOT EXISTS { $this era:hasOrganisationRole ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasOrientation a sh:PropertyShape ;
    era:affectedClass era:Signal ;
    era:affectedProperty era:hasOrientation ;
    era:scope "local" ;

    rdfs:comment "Indicates the orientation of the signal in relation to the carrier linear element."@en ;
    # Source: ontology comment for hasOrientation

    sh:class era:Orientation ;
    # Source: ontology range is http://data.europa.eu/949/Orientation

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "hasOrientation: The Signal must have at most one orientation value that is an instance of Orientation. This error may be due to having more than one value or having a value that is not an instance of Orientation."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable orientation instances (IRI references)

    sh:path era:hasOrientation ;

    sh:severity sh:Violation .

era-sh:HasOrientationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Signal ;
    era:affectedProperty era:hasOrientation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Signal is defined and requires a reference to its orientation."@en ;
    # Source: inferred from the definition of Signal and its relationship to hasOrientation

    sh:message "hasOrientation: This error is due to the Signal {?signalLabel}, violating the rule: Applicable when the Signal is defined and requires a reference to its orientation."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?signalLabel WHERE {
        $this rdfs:label ?signalLabel .
        FILTER NOT EXISTS { $this era:hasOrientation ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasPart a sh:PropertyShape ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:hasPart ;
    era:scope "local" ;

    rdfs:comment "Indicates that an infrastructure element is composed of one or more other (distinct) infrastructure elements. This property is used to represent part-whole relationships between infrastructure elements. An element cannot be a part of itself."@en ;
    # Source: ontology comment for hasPart

    sh:class era:InfrastructureElement ;
    # Source: ontology range is era:InfrastructureElement (rdfs:range)

    sh:minCount 1 ;
    # Inferred: at least one part is expected to model part-whole structure

    sh:maxCount 1 ;
    # Inferred: expected at most one part for a specific relationship

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable infrastructure elements (IRI references)

    sh:message "hasPart: The InfrastructureElement must have at least one hasPart reference that is an instance of InfrastructureElement. This error may be due to not having a value or having a value that is not an instance of InfrastructureElement."@en ;

    sh:path era:hasPart ;

    sh:severity sh:Violation .

era-sh:HasPartIrreflexivity a sh:SPARQLConstraint ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:hasPart ;
    era:scope "local" ;

    rdfs:comment "Enforces irreflexivity of hasPart: an element cannot be a part of itself."@en ;
    # Source: ontology declares owl:IrreflexiveProperty

    sh:message "hasPart: The InfrastructureElement {?thisLabel} cannot have itself as part."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?thisLabel WHERE {
        $this rdfs:label ?thisLabel .
        FILTER EXISTS { $this era:hasPart $this } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasSchematicOverviewOPDigitalForm a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:hasSchematicOverviewOPDigitalForm ;
    era:scope "local" ;

    rdfs:comment "The existence of a schematic overview of the operational point in digital form."@en ;
    # Source: ontology comment for hasSchematicOverviewOPDigitalForm

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:in ( "Y" "N" "NYA" ) ;
    # Source: RAG specifies the predefined list of acceptable values for the applicability of the parameter

    sh:message "hasSchematicOverviewOPDigitalForm: The OperationalPoint must have at most one schematic overview in digital form value that is a boolean (Y/N/NYA). This error may be due to having more than one value or having a value that is not one of the allowed options."@en ;

    sh:path era:hasSchematicOverviewOPDigitalForm ;

    sh:severity sh:Violation .

era-sh:HasSchematicOverviewOPDigitalFormApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:hasSchematicOverviewOPDigitalForm ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OperationalPoint is defined and requires a reference to its schematic overview in digital form."@en ;
    # Source: inferred from the definition of OperationalPoint and its relationship to hasSchematicOverviewOPDigitalForm

    sh:message "hasSchematicOverviewOPDigitalForm: This error is due to the OperationalPoint {?opLabel}, violating the rule: Applicable when the OperationalPoint is defined and requires a reference to its schematic overview in digital form."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?opLabel WHERE {
        $this rdfs:label ?opLabel .
        FILTER NOT EXISTS { $this era:hasSchematicOverviewOPDigitalForm ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasSequence a sh:PropertyShape ;
    era:affectedClass era:NetLinearReference ;
    era:affectedProperty era:hasSequence ;
    era:scope "local" ;

    rdfs:comment "The ordered sequence of topological linear elements which create a linear net reference."@en ;
    # Source: ontology comment for hasSequence

    sh:class rdf:List ;
    # Source: ontology range is rdf:List (http://www.w3.org/1999/02/22-rdf-syntax-ns#List)

    sh:minCount 1 ;
    # Inferred: at least one sequence element is expected to create a valid ordered sequence

    sh:maxCount 1 ;
    # Inferred: expected at most one sequence for a specific linear reference

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable list instances (IRI references)

    sh:message "hasSequence: The NetLinearReference must have at least one hasSequence reference that is an instance of rdf:List. This error may be due to not having a value or having a value that is not an instance of rdf:List."@en ;

    sh:path era:hasSequence ;

    sh:severity sh:Violation .

era-sh:HasSequenceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:NetLinearReference ;
    era:affectedProperty era:hasSequence ;
    era:scope "local" ;

    rdfs:comment "Applicable when the NetLinearReference is defined and requires a reference to its ordered sequence of topological linear elements."@en ;
    # Source: inferred from the definition of NetLinearReference and its relationship to hasSequence

    sh:message "hasSequence: This error is due to the NetLinearReference {?referenceLabel}, violating the rule: Applicable when the NetLinearReference is defined and requires a reference to its ordered sequence."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?referenceLabel WHERE {
        $this rdfs:label ?referenceLabel .
        FILTER NOT EXISTS { $this era:hasSequence ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasTopoCoordinate a sh:PropertyShape ;
    era:affectedClass era:NetPointReference ;
    era:affectedProperty era:hasTopoCoordinate ;
    era:scope "local" ;

    rdfs:comment "Specifies the topological coordinate of a network point reference."@en ;
    # Source: ontology comment for hasTopoCoordinate

    sh:class era:TopologicalCoordinate ;
    # Source: ontology range is era:TopologicalCoordinate

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "hasTopoCoordinate: The NetPointReference must have at most one topological coordinate value that is an instance of TopologicalCoordinate. This error may be due to having more than one value or having a value that is not an instance of TopologicalCoordinate."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable topological coordinates (IRI references)

    sh:path era:hasTopoCoordinate ;

    sh:severity sh:Violation .

era-sh:HasTopoCoordinateApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:NetPointReference ;
    era:affectedProperty era:hasTopoCoordinate ;
    era:scope "local" ;

    rdfs:comment "Applicable when the NetPointReference is defined and requires a reference to its topological coordinate."@en ;
    # Source: inferred from the definition of NetPointReference and its relationship to hasTopoCoordinate

    sh:message "hasTopoCoordinate: This error is due to the NetPointReference {?pointLabel}, violating the rule: Applicable when the NetPointReference is defined and requires a reference to its topological coordinate."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?pointLabel WHERE {
        $this rdfs:label ?pointLabel .
        FILTER NOT EXISTS { $this era:hasTopoCoordinate ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HotAxleBoxDetectorDirection a sh:PropertyShape ;
    era:affectedClass era:HABD ;
    era:affectedProperty era:hotAxleBoxDetectorDirection ;
    era:rinfIndex "1.1.1.1.7.9" ;
    era:scope "local" ;

    rdfs:comment "Direction of measurement of trackside HABD, specific for the French, Italian, and Swedish networks."@en ;
    # Source: ontology comment for hotAxleBoxDetectorDirection

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:in ( "N" "O" "B" ) ;
    # Source: RAG specifies the predefined list of acceptable values for the direction of measurement

    sh:message "hotAxleBoxDetectorDirection: The HABD must have at most one direction value that is a Concept from the predefined list (N/O/B). This error may be due to having more than one value or having a value that is not one of the allowed options."@en ;

    sh:path era:hotAxleBoxDetectorDirection ;

    sh:severity sh:Violation .

era-sh:HotAxleBoxDetectorDirectionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:HABD ;
    era:affectedProperty era:hotAxleBoxDetectorDirection ;
    era:scope "local" ;

    rdfs:comment "Applicable if trackside HABD is not TSI compliant, direction of measurement of trackside hot axle box detector."@en ;
    # Source: dependency note indicates requirement for TSI compliance

    sh:message "hotAxleBoxDetectorDirection: This error is due to the HABD {?habdLabel}, violating the rule: Applicable only if the trackside HABD is not TSI compliant."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?habdLabel WHERE {
        $this rdfs:label ?habdLabel .
        FILTER NOT EXISTS { $this era:hotAxleBoxDetectorDirection ?value } .
        # Check if HABD is TSI compliant
        FILTER EXISTS { $this era:tsiCompliant ?tsiValue . FILTER(?tsiValue = "N") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:InCountry a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:inCountry ;
    era:scope "local" ;

    rdfs:comment "Indicates the country in which an entity resides."@en ;
    # Source: ontology comment for inCountry

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Inferred: expected at most one country reference for a specific entity

    sh:message "inCountry: The CommonCharacteristicsSubset must have at most one inCountry reference that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:inCountry ;

    sh:severity sh:Violation .

era-sh:InCountryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:inCountry ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its country."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to inCountry

    sh:message "inCountry: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its country."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:inCountry ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Includes a sh:PropertyShape ;
    era:affectedClass era:NetAreaReference ;
    era:affectedProperty era:includes ;
    era:scope "local" ;

    rdfs:comment "List of linear references included in a network area reference."@en ;
    # Source: ontology comment for includes

    sh:class rdf:List ;
    # Source: ontology range is rdf:List (http://www.w3.org/1999/02/22-rdf-syntax-ns#List)

    sh:minCount 1 ;
    # Inferred: at least one linear reference is expected to be included in the list

    sh:maxCount 1 ;
    # Inferred: expected at most one list for a specific network area reference

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable list instances (IRI references)

    sh:message "includes: The NetAreaReference must have at least one includes reference that is an instance of rdf:List. This error may be due to not having a value or having a value that is not an instance of rdf:List."@en ;

    sh:path era:includes ;

    sh:severity sh:Violation .

era-sh:IncludesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:NetAreaReference ;
    era:affectedProperty era:includes ;
    era:scope "local" ;

    rdfs:comment "Applicable when the NetAreaReference is defined and requires a reference to its list of linear references."@en ;
    # Source: inferred from the definition of NetAreaReference and its relationship to includes

    sh:message "includes: This error is due to the NetAreaReference {?areaLabel}, violating the rule: Applicable when the NetAreaReference is defined and requires a reference to its list of linear references."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?areaLabel WHERE {
        $this rdfs:label ?areaLabel .
        FILTER NOT EXISTS { $this era:includes ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:InfrastructureManager a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:infrastructureManager ;
    era:scope "local" ;

    rdfs:comment "Relates a subset with common characteristics with its Infrastructure Manager, represented by an instance of OrganisationRole."@en ;
    # Source: ontology comment for infrastructureManager

    sh:class era:OrganisationRole ;
    # Source: ontology range is http://data.europa.eu/949/OrganisationRole

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "infrastructureManager: The CommonCharacteristicsSubset must have at most one infrastructure manager value that is an instance of OrganisationRole. This error may be due to having more than one value or having a value that is not an instance of OrganisationRole."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable organisation roles (IRI references)

    sh:path era:infrastructureManager ;

    sh:severity sh:Violation .

era-sh:InfrastructureManagerApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:infrastructureManager ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its Infrastructure Manager."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to infrastructureManager

    sh:message "infrastructureManager: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its Infrastructure Manager."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:infrastructureManager ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:InfrastructureMgr a sh:PropertyShape ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:infrastructureMgr ;
    era:scope "local" ;

    rdfs:comment "(deprecated) not in use. Relates any feature implemented in the European railway infrastructure with its infrastructure manager."@en ;
    # Source: ontology comment for infrastructureMgr

    sh:class era:InfrastructureManager ;
    # Source: ontology range is era:InfrastructureManager

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "infrastructureMgr: The InfrastructureElement must have at most one infrastructure manager value that is an instance of InfrastructureManager. This error may be due to having more than one value or having a value that is not an instance of InfrastructureManager."@en ;
    # Source: inferred from the nature of the property and its definition

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable infrastructure managers (IRI references)

    sh:path era:infrastructureMgr ;

    sh:severity sh:Violation .

era-sh:InstructionsSwitchRadioSystems a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:instructionsSwitchRadioSystems ; 
    era:scope "local" ;

    rdfs:comment "Name and/or reference of the document specifying the Special instructions to switch over between different radio systems."@en ;
    # Source: ontology comment for instructionsSwitchRadioSystems

    sh:class era:Document ; 
    # Source: ontology range is http://data.europa.eu/949/Document

    sh:maxCount 1 ; 
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "instructionsSwitchRadioSystems: The CommonCharacteristicsSubset must have at most one instructionsSwitchRadioSystems reference that is an instance of Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ;

    sh:path era:instructionsSwitchRadioSystems ;

    sh:severity sh:Violation .

era-sh:InstructionsSwitchRadioSystemsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:instructionsSwitchRadioSystems ; 
    era:scope "local" ;

    rdfs:comment "Applicable when at least two different radio systems exist."@en ;
    # Source: dependency note indicates requirement for multiple radio systems

    sh:message "instructionsSwitchRadioSystems: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when at least two different radio systems exist."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:instructionsSwitchRadioSystems ?value } .
        # Check if at least two different radio systems exist
        FILTER EXISTS { $this era:hasRadioSystem ?system1 } .
        FILTER EXISTS { $this era:hasRadioSystem ?system2 . FILTER(?system1 != ?system2) } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:IsPartOf a sh:PropertyShape ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:isPartOf ;
    era:scope "local" ;

    rdfs:comment "Indicates that an infrastructure element is a part of another (distinct) infrastructure element. An element cannot be part of itself. This property is the inverse of era:hasPart."@en ;
    # Source: ontology comment for isPartOf

    sh:class era:InfrastructureElement ;
    # Source: ontology range is era:InfrastructureElement (rdfs:range)

    sh:minCount 1 ;
    # Inferred: at least one part is expected to model part-whole structure

    sh:maxCount 1 ;
    # Inferred: expected at most one part for a specific relationship

    sh:nodeKind sh:IRI ;
    # Inferred: links must refer to identifiable infrastructure elements (IRI references)

    sh:message "isPartOf: The InfrastructureElement must have at least one isPartOf reference that is an instance of InfrastructureElement. This error may be due to not having a value or having a value that is not an instance of InfrastructureElement."@en ;

    sh:path era:isPartOf ;

    sh:severity sh:Violation .

era-sh:IsPartOfIrreflexivity a sh:SPARQLConstraint ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:isPartOf ;
    era:scope "local" ;

    rdfs:comment "Enforces irreflexivity of isPartOf: an element cannot be part of itself."@en ;
    # Source: ontology declares owl:IrreflexiveProperty

    sh:message "isPartOf: The InfrastructureElement {?thisLabel} cannot be part of itself."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?thisLabel WHERE {
        $this rdfs:label ?thisLabel .
        FILTER EXISTS { $this era:isPartOf $this } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:KmPost a sh:PropertyShape ;
    era:affectedClass era:LinearPositioningSystemCoordinate ;
    era:affectedProperty era:kmPost ;
    era:scope "local" ;

    rdfs:comment "The closest kilometric post on the line, used as a reference by the LRS coordinate."@en ;
    # Source: ontology comment for kmPost

    sh:class era:KilometricPost ;
    # Source: ontology range is era:KilometricPost

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "kmPost: The LinearPositioningSystemCoordinate must have at most one kmPost reference that is an instance of KilometricPost. This error may be due to having more than one value or having a value that is not an instance of KilometricPost."@en ;

    sh:path era:kmPost ;

    sh:severity sh:Violation .

era-sh:KmPostApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LinearPositioningSystemCoordinate ;
    era:affectedProperty era:kmPost ;
    era:scope "local" ;

    rdfs:comment "Applicable when the LinearPositioningSystemCoordinate is defined and requires a reference to its closest kilometric post."@en ;
    # Source: inferred from the definition of LinearPositioningSystemCoordinate and its relationship to kmPost

    sh:message "kmPost: This error is due to the LinearPositioningSystemCoordinate {?coordinateLabel}, violating the rule: Applicable when the LinearPositioningSystemCoordinate is defined and requires a reference to its closest kilometric post."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?coordinateLabel WHERE {
        $this rdfs:label ?coordinateLabel .
        FILTER NOT EXISTS { $this era:kmPost ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LegacyRadioSystem a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:legacyRadioSystem ;
    era:scope "local" ;

    rdfs:comment "Indication of radio legacy systems installed."@en ;
    # Source: ontology comment for legacyRadioSystem

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:in ( "Y" "N" "NYA" ) ;
    # Source: RAG specifies the predefined list of acceptable values for legacy radio system indication

    sh:message "legacyRadioSystem: The CommonCharacteristicsSubset must have at most one legacy radio system value that is a Concept from the predefined list (Y/N/NYA). This error may be due to having more than one value or having a value that is not one of the allowed options."@en ;

    sh:path era:legacyRadioSystem ;

    sh:severity sh:Violation .

era-sh:LegacyRadioSystemApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:legacyRadioSystem ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its legacy radio system."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to legacyRadioSystem

    sh:message "legacyRadioSystem: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its legacy radio system."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:legacyRadioSystem ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LineCategory a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:lineCategory ;
    era:scope "local" ;

    rdfs:comment "Classification of a line according to the TSI INF."@en ;
    # Source: ontology comment for lineCategory

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:minCount 1 ;
    # Inferred: at least one category is expected to classify the line

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:in ( "P1" "P2" "P3" "P4" "P5" "P6" "P1520" "P1600" "F1" "F2" "F3" "F4" "F1520" "F1600" ) ;
    # Source: RAG specifies the predefined list of acceptable traffic codes for line categories

    sh:message "lineCategory: The RunningTrack must have at most one line category value that is a Concept from the predefined list of traffic codes. This error may be due to having more than one value or having a value that is not one of the allowed options."@en ;

    sh:path era:lineCategory ;

    sh:severity sh:Violation .

era-sh:LineCategoryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:lineCategory ;
    era:scope "local" ;

    rdfs:comment "Applicable when the RunningTrack is defined and requires a reference to its line category."@en ;
    # Source: inferred from the definition of RunningTrack and its relationship to lineCategory

    sh:message "lineCategory: This error is due to the RunningTrack {?trackLabel}, violating the rule: Applicable when the RunningTrack is defined and requires a reference to its line category."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:lineCategory ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LineNationalId a sh:PropertyShape ;
    era:affectedClass era:LineReference ;
    era:affectedProperty era:lineNationalId ;
    era:rinfIndex "1.1.0.0.0.2" ;
    era:scope "local" ;

    rdfs:comment "Unique line identification or unique line number within Member State."@en ;
    # Source: ontology comment for lineNationalId

    sh:datatype xsd:string ;
    # Source: RAG specifies data presentation as CharacterString, which corresponds to xsd:string

    sh:maxCount 1 ;
    # Source: inferred from the nature of unique identification, only one value should be present

    sh:message "lineNationalId: The LineReference must have at most one line national ID that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:lineNationalId ;

    sh:severity sh:Violation .

era-sh:LineNationalIdApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LineReference ;
    era:affectedProperty era:lineNationalId ;
    era:scope "local" ;

    rdfs:comment "Applicable when the LineReference is defined and requires a unique national line identification."@en ;
    # Source: inferred from the definition of LineReference and its relationship to lineNationalId

    sh:message "lineNationalId: This error is due to the LineReference {?lineLabel}, violating the rule: Applicable when the LineReference is defined and requires a unique national line identification."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?lineLabel WHERE {
        $this rdfs:label ?lineLabel .
        FILTER NOT EXISTS { $this era:lineNationalId ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LineReference a sh:PropertyShape ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:lineReference ;
    era:scope "local" ;

    rdfs:comment "Indicates a relationship with a national railway line at a specific kilometer point."@en ;
    # Source: ontology comment for lineReference

    sh:class era:LineReference ;
    # Source: ontology range is era:LineReference

    sh:maxCount 1 ;
    # Inferred: expected at most one line reference for a specific infrastructure element

    sh:message "lineReference: The InfrastructureElement must have at most one line reference that is an instance of LineReference. This error may be due to having more than one value or having a value that is not an instance of LineReference."@en ;

    sh:path era:lineReference ;

    sh:severity sh:Violation .

era-sh:LineReferenceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:lineReference ;
    era:scope "local" ;

    rdfs:comment "Applicable when the InfrastructureElement is defined and requires a reference to its national railway line."@en ;
    # Source: inferred from the definition of InfrastructureElement and its relationship to lineReference

    sh:message "lineReference: This error is due to the InfrastructureElement {?elementLabel}, violating the rule: Applicable when the InfrastructureElement is defined and requires a reference to its national railway line."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?elementLabel WHERE {
        $this rdfs:label ?elementLabel .
        FILTER NOT EXISTS { $this era:lineReference ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LineReferenceTunnelEnd a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:lineReferenceTunnelEnd ;
    era:rinfIndex "1.1.1.1.8.4" ;
    era:scope "local" ;

    rdfs:comment "Geographical coordinates in decimal degrees and km of the line at the end of a tunnel."@en ;
    # Source: ontology comment for lineReferenceTunnelEnd

    sh:class era:NetPointReference ;
    # Source: ontology range is http://data.europa.eu/949/NetPointReference

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "lineReferenceTunnelEnd: The Tunnel must have at most one lineReferenceTunnelEnd that is an instance of NetPointReference. This error may be due to having more than one value or having a value that is not an instance of NetPointReference."@en ;

    sh:path era:lineReferenceTunnelEnd ;

    sh:severity sh:Violation .

era-sh:LineReferenceTunnelEndApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:lineReferenceTunnelEnd ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Tunnel is defined and requires a reference to its line reference at the tunnel end."@en ;
    # Source: inferred from the definition of Tunnel and its relationship to lineReferenceTunnelEnd

    sh:message "lineReferenceTunnelEnd: This error is due to the Tunnel {?tunnelLabel}, violating the rule: Applicable when the Tunnel is defined and requires a reference to its line reference at the tunnel end."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:lineReferenceTunnelEnd ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LineReferenceTunnelStart a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:lineReferenceTunnelStart ;
    era:rinfIndex "1.1.1.1.8.3" ;
    era:scope "local" ;

    rdfs:comment "Geographical coordinates in decimal degrees and km of the line at the beginning of a tunnel."@en ;
    # Source: ontology comment for lineReferenceTunnelStart

    sh:class era:NetPointReference ;
    # Source: ontology range is http://data.europa.eu/949/NetPointReference

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "lineReferenceTunnelStart: The Tunnel must have at most one lineReferenceTunnelStart that is an instance of NetPointReference. This error may be due to having more than one value or having a value that is not an instance of NetPointReference."@en ;

    sh:path era:lineReferenceTunnelStart ;

    sh:severity sh:Violation .

era-sh:LineReferenceTunnelStartApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:lineReferenceTunnelStart ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Tunnel is defined and requires a reference to its line reference at the tunnel start."@en ;
    # Source: inferred from the definition of Tunnel and its relationship to lineReferenceTunnelStart

    sh:message "lineReferenceTunnelStart: This error is due to the Tunnel {?tunnelLabel}, violating the rule: Applicable when the Tunnel is defined and requires a reference to its line reference at the tunnel start."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:lineReferenceTunnelStart ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LinesideDistanceIndication a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:linesideDistanceIndication ;
    era:scope "local" ;

    rdfs:comment "Indication of types of appearance of track lineside distance indications."@en ;
    # Source: ontology comment for linesideDistanceIndication

    sh:class era:LinesideDistanceIndication ;
    # Source: ontology range is era:LinesideDistanceIndication

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "linesideDistanceIndication: The CommonCharacteristicsSubset must have at most one linesideDistanceIndication reference that is an instance of LinesideDistanceIndication. This error may be due to having more than one value or having a value that is not an instance of LinesideDistanceIndication."@en ;

    sh:path era:linesideDistanceIndication ;

    sh:severity sh:Violation .

era-sh:LinesideDistanceIndicationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:linesideDistanceIndication ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its lineside distance indication."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to linesideDistanceIndication

    sh:message "linesideDistanceIndication: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its lineside distance indication."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:linesideDistanceIndication ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LinesideDistanceIndicationAppearance a sh:PropertyShape ;
    era:affectedClass era:LinesideDistanceIndication ;
    era:affectedProperty era:linesideDistanceIndicationAppearance ;
    era:scope "local" ;

    rdfs:comment "Indication of types of appearance of track lineside distance indications."@en ;
    # Source: ontology comment for linesideDistanceIndicationAppearance

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "linesideDistanceIndicationAppearance: The LinesideDistanceIndication must have at most one linesideDistanceIndicationAppearance reference that is an instance of Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:linesideDistanceIndicationAppearance ;

    sh:severity sh:Violation .

era-sh:LinesideDistanceIndicationAppearanceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LinesideDistanceIndication ;
    era:affectedProperty era:linesideDistanceIndicationAppearance ;
    era:scope "local" ;

    rdfs:comment "Applicable when the LinesideDistanceIndication is defined and requires a reference to its lineside distance indication appearance."@en ;
    # Source: inferred from the definition of LinesideDistanceIndication and its relationship to linesideDistanceIndicationAppearance

    sh:message "linesideDistanceIndicationAppearance: This error is due to the LinesideDistanceIndication {?indicationLabel}, violating the rule: Applicable when the LinesideDistanceIndication is defined and requires a reference to its lineside distance indication appearance."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?indicationLabel WHERE {
        $this rdfs:label ?indicationLabel .
        FILTER NOT EXISTS { $this era:linesideDistanceIndicationAppearance ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LinesideDistanceIndicationPositioning a sh:PropertyShape ;
    era:affectedClass era:LinesideDistanceIndication ;
    era:affectedProperty era:linesideDistanceIndicationPositioning ;
    era:scope "local" ;

    rdfs:comment "Indication of the side along the track where the lineside indication is positioned (left or right, or both sides)."@en ;
    # Source: ontology comment for linesideDistanceIndicationPositioning

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:in ( "Left" "Right" "Left and Right" ) ;
    # Source: formatNote specifies the selectable values for lineside distance indication positioning

    sh:message "linesideDistanceIndicationPositioning: The LinesideDistanceIndication must have at most one linesideDistanceIndicationPositioning value that is a Concept from the predefined list (Left/Right/Left and Right). This error may be due to having more than one value or having a value that is not one of the allowed options."@en ;

    sh:path era:linesideDistanceIndicationPositioning ;

    sh:severity sh:Violation .

era-sh:LinesideDistanceIndicationPositioningApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LinesideDistanceIndication ;
    era:affectedProperty era:linesideDistanceIndicationPositioning ;
    era:scope "local" ;

    rdfs:comment "Applicable when the LinesideDistanceIndication is defined and requires a reference to its lineside distance indication positioning."@en ;
    # Source: inferred from the definition of LinesideDistanceIndication and its relationship to linesideDistanceIndicationPositioning

    sh:message "linesideDistanceIndicationPositioning: This error is due to the LinesideDistanceIndication {?indicationLabel}, violating the rule: Applicable when the LinesideDistanceIndication is defined and requires a reference to its lineside distance indication positioning."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?indicationLabel WHERE {
        $this rdfs:label ?indicationLabel .
        FILTER NOT EXISTS { $this era:linesideDistanceIndicationPositioning ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LinkedToPrimaryLocation a sh:PropertyShape ;
    era:affectedClass era:SubsidiaryLocation ;
    era:affectedProperty era:linkedToPrimaryLocation ;
    era:scope "local" ;

    rdfs:comment "Relates a subsidiary location with a primary location."@en ;
    # Source: ontology comment for linkedToPrimaryLocation

    sh:class era:PrimaryLocation ;
    # Source: ontology range is era:PrimaryLocation

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "linkedToPrimaryLocation: The SubsidiaryLocation must have at most one linkedToPrimaryLocation reference that is an instance of PrimaryLocation. This error may be due to having more than one value or having a value that is not an instance of PrimaryLocation."@en ;

    sh:path era:linkedToPrimaryLocation ;

    sh:severity sh:Violation .

era-sh:LinkedToPrimaryLocationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SubsidiaryLocation ;
    era:affectedProperty era:linkedToPrimaryLocation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the SubsidiaryLocation is defined and requires a reference to its linked primary location."@en ;
    # Source: inferred from the definition of SubsidiaryLocation and its relationship to linkedToPrimaryLocation

    sh:message "linkedToPrimaryLocation: This error is due to the SubsidiaryLocation {?subsidiaryLabel}, violating the rule: Applicable when the SubsidiaryLocation is defined and requires a reference to its linked primary location."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsidiaryLabel WHERE {
        $this rdfs:label ?subsidiaryLabel .
        FILTER NOT EXISTS { $this era:linkedToPrimaryLocation ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LoadCapabilityLineCategory a sh:PropertyShape ;
    era:affectedClass era:LoadCapability ;
    era:affectedProperty era:loadCapabilityLineCategory ;
    era:scope "local" ;

    rdfs:comment "Part of the load capability of a track that corresponds to the line category of the load model."@en ;
    # Source: ontology comment for loadCapabilityLineCategory

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:in ( "A" "B1" "B2" "C2" "C3" "C4" "D2" "D3" "D4" "D4xL" "E4" "E5" "RA1" "RA2" "RA3" "RA4" "RA5" "RA6" "RA7" "RA8" "RA9" "RA10" ) ;
    # Source: RAG specifies the predefined list of acceptable values for load capability line categories

    sh:message "loadCapabilityLineCategory: The LoadCapability must have at most one load capability line category value that is a Concept from the predefined list. This error may be due to having more than one value or having a value that is not one of the allowed options."@en ;

    sh:path era:loadCapabilityLineCategory ;

    sh:severity sh:Violation .

era-sh:LoadCapabilityLineCategoryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LoadCapability ;
    era:affectedProperty era:loadCapabilityLineCategory ;
    era:scope "local" ;

    rdfs:comment "Applicable when the LoadCapability is defined and requires a reference to its load capability line category."@en ;
    # Source: inferred from the definition of LoadCapability and its relationship to loadCapabilityLineCategory

    sh:message "loadCapabilityLineCategory: This error is due to the LoadCapability {?capabilityLabel}, violating the rule: Applicable when the LoadCapability is defined and requires a reference to its load capability line category."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?capabilityLabel WHERE {
        $this rdfs:label ?capabilityLabel .
        FILTER NOT EXISTS { $this era:loadCapabilityLineCategory ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LocalRulesOrRestrictionsDoc a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:localRulesOrRestrictionsDoc ; 
    era:scope "local" ; 

    rdfs:comment "Documents regarding the rules or restrictions of a strictly local nature available by the IM."@en ; 
    # Source: ontology comment for localRulesOrRestrictionsDoc

    sh:class era:Document ; 
    # Source: ontology range is era:Document

    sh:minCount 1 ; 
    # Inferred: at least one document is expected if local rules or restrictions exist

    sh:maxCount 1 ; 
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "localRulesOrRestrictionsDoc: The CommonCharacteristicsSubset must have at most one local rules or restrictions document that is an instance of Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ; 

    sh:path era:localRulesOrRestrictionsDoc ; 

    sh:severity sh:Violation .

era-sh:LocalRulesOrRestrictionsDocApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:localRulesOrRestrictionsDoc ; 
    era:scope "local" ; 

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its local rules or restrictions document."@en ; 
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to localRulesOrRestrictionsDoc

    sh:message "localRulesOrRestrictionsDoc: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its local rules or restrictions document."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:localRulesOrRestrictionsDoc ?value } .
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:LrsMethod a sh:PropertyShape ;
    era:affectedClass era:LinearPositioningSystem ;
    era:affectedProperty era:lrsMethod ;
    era:scope "local" ;

    rdfs:comment "The preferred line referencing system."@en ;
    # Source: ontology comment for lrsMethod

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: inferred from the nature of the preferred method, only one value should be present

    sh:message "lrsMethod: The LinearPositioningSystem must have at most one lrsMethod that is an instance of Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:lrsMethod ;

    sh:severity sh:Violation .

era-sh:LrsMethodApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LinearPositioningSystem ;
    era:affectedProperty era:lrsMethod ;
    era:scope "local" ;

    rdfs:comment "Applicable when the LinearPositioningSystem is defined and requires a reference to its preferred line referencing system."@en ;
    # Source: inferred from the definition of LinearPositioningSystem and its relationship to lrsMethod

    sh:message "lrsMethod: This error is due to the LinearPositioningSystem {?systemLabel}, violating the rule: Applicable when the LinearPositioningSystem is defined and requires a reference to its preferred line referencing system."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:lrsMethod ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MNvcontact a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:mNvcontact ; 
    era:scope "local" ; 

    rdfs:comment "On-Board system reaction when T_NVCONTACT expires according to the specification referenced in Appendix A-1, index [C]."@en ; 
    # Source: ontology comment for mNvcontact

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ; 
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ; 
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "mNvcontact: The CommonCharacteristicsSubset must have at most one mNvcontact reference that is an instance of Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ; 

    sh:path era:mNvcontact ; 

    sh:severity sh:Violation .

era-sh:MNvcontactApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:mNvcontact ; 
    era:scope "local" ; 

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its mNvcontact."@en ; 
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to mNvcontact

    sh:message "mNvcontact: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its mNvcontact."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:mNvcontact ?value } .
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:MagneticBraking a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:magneticBraking ;
    era:rinfIndex "1.1.1.1.6.3" ;
    era:scope "local" ;

    rdfs:comment "Indication of limitations on the use of magnetic brakes."@en ;
    # Source: ontology comment for magneticBraking

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:in ( "allowed" "allowed under conditions" "allowed under conditions only for emergency brake" "allowed only for emergency brake" "not allowed" ) ;
    # Source: RAG specifies the predefined list of acceptable values for magnetic braking

    sh:message "magneticBraking: The CommonCharacteristicsSubset must have at most one magneticBraking value that is a Concept from the predefined list (allowed, allowed under conditions, allowed under conditions only for emergency brake, allowed only for emergency brake, not allowed). This error may be due to having more than one value or having a value that is not one of the allowed options."@en ;

    sh:path era:magneticBraking ;

    sh:severity sh:Violation .

era-sh:MagneticBrakingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:magneticBraking ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its magnetic braking limitations."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to magneticBraking

    sh:message "magneticBraking: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its magnetic braking limitations."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:magneticBraking ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MagneticBrakingConditionsDocument a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:magneticBrakingConditionsDocument ;
    era:rinfIndex "1.1.1.1.6.5" ;
    era:scope "local" ;

    rdfs:comment "Electronic document available in two EU languages from the IM stored by the Agency with conditions for the use of magnetic brakes identified in 1.1.1.1.6.3."@en ;
    # Source: ontology comment for magneticBrakingConditionsDocument

    sh:class era:Document ;
    # Source: ontology range is era:Document

    sh:minCount 1 ;
    # Inferred: at least one document is expected when conditions for magnetic brakes exist

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "magneticBrakingConditionsDocument: The CommonCharacteristicsSubset must have at most one magneticBrakingConditionsDocument that is an instance of Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ;

    sh:path era:magneticBrakingConditionsDocument ;

    sh:severity sh:Violation .

era-sh:MagneticBrakingConditionsDocumentApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:magneticBrakingConditionsDocument ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its magnetic braking conditions document."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to magneticBrakingConditionsDocument

    sh:message "magneticBrakingConditionsDocument: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its magnetic braking conditions document."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:magneticBrakingConditionsDocument ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Manufacturer a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:manufacturer ;
    era:scope "local" ;

    rdfs:comment "Vehicle manufacturer company."@en ;
    # Source: ontology comment for manufacturer

    sh:class era:OrganisationRole ;
    # Source: ontology range is era:OrganisationRole

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "manufacturer: The VehicleType must have at most one manufacturer reference that is an instance of OrganisationRole. This error may be due to having more than one value or having a value that is not an instance of OrganisationRole."@en ;

    sh:path era:manufacturer ;

    sh:severity sh:Violation .

era-sh:ManufacturerApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:manufacturer ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires a reference to its manufacturer."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to manufacturer

    sh:message "manufacturer: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the VehicleType is defined and requires a reference to its manufacturer."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:manufacturer ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ManufacturingCountry a sh:PropertyShape ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:manufacturingCountry ;
    era:scope "local" ;

    rdfs:comment "Indicates the country in which a vehicle or vehicle type is manufactured."@en ;
    # Source: ontology comment for manufacturingCountry

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "manufacturingCountry: The Vehicle must have at most one manufacturingCountry reference that is an instance of Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:manufacturingCountry ;

    sh:severity sh:Violation .

era-sh:ManufacturingCountryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:manufacturingCountry ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Vehicle is defined and requires a reference to its manufacturing country."@en ;
    # Source: inferred from the definition of Vehicle and its relationship to manufacturingCountry

    sh:message "manufacturingCountry: This error is due to the Vehicle {?vehicleLabel}, violating the rule: Applicable when the Vehicle is defined and requires a reference to its manufacturing country."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:manufacturingCountry ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaxSandingOutput a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:maxSandingOutput ;
    era:rinfIndex "1.1.1.3.7.17" ;
    era:scope "local" ;

    rdfs:comment "Maximum amount of sand accepted on the track within value of sanding output for 30s, given in grams."@en ;
    # Source: ontology comment for maxSandingOutput

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "maxSandingOutput: The TrainDetectionSystem must have at most one maxSandingOutput value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:maxSandingOutput ;

    sh:severity sh:Violation .

era-sh:MaxSandingOutputApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:maxSandingOutput ;
    era:scope "local" ;

    rdfs:comment "Applicable when the TrainDetectionSystem is defined and requires a reference to its maximum sanding output."@en ;
    # Source: inferred from the definition of TrainDetectionSystem and its relationship to maxSandingOutput

    sh:message "maxSandingOutput: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: Applicable only when the selected option for parameter 1.1.1.3.7.1.1 is ‘track circuit’ and when selection for parameter 1.1.1.3.7.18 is 'yes'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:maxSandingOutput ?value } .
        # Additional filters based on applicability conditions
        FILTER EXISTS { $this era:trackCircuit ?tcValue . FILTER(?tcValue = "track circuit") } .
        FILTER EXISTS { $this era:sandingDriverOverride ?overrideValue . FILTER(?overrideValue = "yes") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinAxleLoadVehicleCategory a sh:PropertyShape ;
    era:affectedClass era:MinAxleLoadVehicleCategory ;
    era:affectedProperty era:minAxleLoadVehicleCategory ;
    era:scope "local" ;

    rdfs:comment "Represents the category of vehicle which is amended by value of minimum permitted axle load [tons]."@en ;
    # Source: ontology comment for minAxleLoadVehicleCategory

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "minAxleLoadVehicleCategory: The MinAxleLoadVehicleCategory must have at most one minAxleLoadVehicleCategory reference that is an instance of Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:minAxleLoadVehicleCategory ;

    sh:severity sh:Violation .

era-sh:MinAxleLoadVehicleCategoryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:MinAxleLoadVehicleCategory ;
    era:affectedProperty era:minAxleLoadVehicleCategory ;
    era:scope "local" ;

    rdfs:comment "Applicable when the MinAxleLoadVehicleCategory is defined and requires a reference to its minimum axle load vehicle category."@en ;
    # Source: inferred from the definition of MinAxleLoadVehicleCategory and its relationship to minAxleLoadVehicleCategory

    sh:message "minAxleLoadVehicleCategory: This error is due to the MinAxleLoadVehicleCategory {?categoryLabel}, violating the rule: Applicable when the MinAxleLoadVehicleCategory is defined and requires a reference to its minimum axle load vehicle category."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?categoryLabel WHERE {
        $this rdfs:label ?categoryLabel .
        FILTER NOT EXISTS { $this era:minAxleLoadVehicleCategory ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinVehicleImpedance a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:minVehicleImpedance ;
    era:scope "local" ;

    rdfs:comment "Impedance as defined in the specification referenced in Appendix A-1, index [D]."@en ;
    # Source: ontology comment for minVehicleImpedance

    sh:class era:MinVehicleImpedance ;
    # Source: ontology range is era:MinVehicleImpedance

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "minVehicleImpedance: The TrainDetectionSystem must have at most one minVehicleImpedance value that is an instance of MinVehicleImpedance. This error may be due to having more than one value or having a value that is not an instance of MinVehicleImpedance."@en ;

    sh:path era:minVehicleImpedance ;

    sh:severity sh:Violation .

era-sh:MinVehicleImpedanceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:minVehicleImpedance ;
    era:scope "local" ;

    rdfs:comment "Applicable only for track circuits."@en ;
    # Source: dependencyNote in ontology states it is mandatory only for track circuits

    sh:message "minVehicleImpedance: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: Applicable only for track circuits."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:minVehicleImpedance ?value } .
        # Additional filters based on applicability conditions
        FILTER EXISTS { $this era:trackCircuit ?tcValue . FILTER(?tcValue = "track circuit") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinVehicleImpedanceVoltages a sh:PropertyShape ;
    era:affectedClass era:MinVehicleImpedance ;
    era:affectedProperty era:minVehicleImpedanceVoltages ;
    era:scope "local" ;

    rdfs:comment "Minimum Vehicle Impedance (Voltage applicable) as defined in the specification referenced in Appendix A-1, index [D]."@en ;
    # Source: ontology comment for minVehicleImpedanceVoltages

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "minVehicleImpedanceVoltages: The MinVehicleImpedance must have at most one minVehicleImpedanceVoltages value that is an instance of Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:minVehicleImpedanceVoltages ;

    sh:severity sh:Violation .

era-sh:MinVehicleImpedanceVoltagesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:MinVehicleImpedance ;
    era:affectedProperty era:minVehicleImpedanceVoltages ;
    era:scope "local" ;

    rdfs:comment "Applicable when the MinVehicleImpedance is defined and requires a reference to its minimum vehicle impedance voltages."@en ;
    # Source: inferred from the definition of MinVehicleImpedance and its relationship to minVehicleImpedanceVoltages

    sh:message "minVehicleImpedanceVoltages: This error is due to the MinVehicleImpedance {?impedanceLabel}, violating the rule: Applicable when the MinVehicleImpedance is defined and requires a reference to its minimum vehicle impedance voltages."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?impedanceLabel WHERE {
        $this rdfs:label ?impedanceLabel .
        FILTER NOT EXISTS { $this era:minVehicleImpedanceVoltages ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinimumVerticalRadius a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:minimumVerticalRadius ;
    era:rinfIndex "1.2.2.0.3.3" ;
    era:scope "local" ;

    rdfs:comment "Radius of the smallest vertical curve expressed in metres."@en ;
    # Source: ontology comment for minimumVerticalRadius

    sh:class era:MinimumVerticalRadius ;
    # Source: ontology range is era:MinimumVerticalRadius

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "minimumVerticalRadius: The CommonCharacteristicsSubset must have at most one minimumVerticalRadius value that is an instance of MinimumVerticalRadius. This error may be due to having more than one value or having a value that is not an instance of MinimumVerticalRadius."@en ;

    sh:path era:minimumVerticalRadius ;

    sh:severity sh:Violation .

era-sh:MinimumVerticalRadiusApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:minimumVerticalRadius ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its minimum vertical radius."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to minimumVerticalRadius

    sh:message "minimumVerticalRadius: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its minimum vertical radius."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:minimumVerticalRadius ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:NationalLine a sh:PropertyShape ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:nationalLine ;
    era:rinfIndex "1.1.0.0.0.2" ;
    era:scope "local" ;

    rdfs:comment "Indicates a relationship with a national railway line at a specific kilometer point. For a Section of Line: unique line identification or unique line number within Member State."@en ;
    # Source: ontology comment for nationalLine

    sh:class era:LinearPositioningSystem ;
    # Source: ontology range is era:LinearPositioningSystem

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "nationalLine: The SectionOfLine must have at most one nationalLine reference that is an instance of LinearPositioningSystem. This error may be due to having more than one value or having a value that is not an instance of LinearPositioningSystem."@en ;

    sh:path era:nationalLine ;

    sh:severity sh:Violation .

era-sh:NationalLineApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:nationalLine ;
    era:scope "local" ;

    rdfs:comment "Applicable when the SectionOfLine is defined and requires a reference to its national line identification."@en ;
    # Source: inferred from the definition of SectionOfLine and its relationship to nationalLine

    sh:message "nationalLine: This error is due to the SectionOfLine {?sectionLabel}, violating the rule: Applicable when the SectionOfLine is defined and requires a reference to its national line identification."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sectionLabel WHERE {
        $this rdfs:label ?sectionLabel .
        FILTER NOT EXISTS { $this era:nationalLine ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Navigability a sh:PropertyShape ;
    era:affectedClass era:NetRelation ;
    era:affectedProperty era:navigability ;
    era:rinfIndex "1.1.1.0.1.2" ;
    era:scope "local" ;

    rdfs:comment "Indicates the navigation possibilities between two related topological linear elements."@en ;
    # Source: ontology comment for navigability

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is http://www.w3.org/2004/02/skos/core#Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:in ( "AB" "BA" "Both" "None" ) ;
    # Source: RAG specifies the predefined list of acceptable values for navigability

    sh:message "navigability: The NetRelation must have at most one navigability value that is a Concept from the predefined list (AB, BA, Both, None). This error may be due to having more than one value or having a value that is not one of the allowed options."@en ;

    sh:path era:navigability ;

    sh:severity sh:Violation .

era-sh:NavigabilityApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:NetRelation ;
    era:affectedProperty era:navigability ;
    era:scope "local" ;

    rdfs:comment "Applicable when the NetRelation is defined and requires a reference to its navigability."@en ;
    # Source: inferred from the definition of NetRelation and its relationship to navigability

    sh:message "navigability: This error is due to the NetRelation {?relationLabel}, violating the rule: Applicable when the NetRelation is defined and requires a reference to its navigability."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?relationLabel WHERE {
        $this rdfs:label ?relationLabel .
        FILTER NOT EXISTS { $this era:navigability ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:NetReference a sh:PropertyShape ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:netReference ;
    era:scope "local" ;

    rdfs:comment "The reference that a physical infrastructure element has on the topology. It may be used to define the positioning or a representation as a point, line or area."@en ;
    # Source: ontology comment for netReference

    sh:class era:NetBasicReference ;
    # Source: ontology range is era:NetBasicReference

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "netReference: The InfrastructureElement must have at most one netReference that is an instance of NetBasicReference. This error may be due to having more than one value or having a value that is not an instance of NetBasicReference."@en ;

    sh:path era:netReference ;

    sh:severity sh:Violation .

era-sh:NetReferenceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:netReference ;
    era:scope "local" ;

    rdfs:comment "Applicable when the InfrastructureElement is defined and requires a reference to its net reference."@en ;
    # Source: inferred from the definition of InfrastructureElement and its relationship to netReference

    sh:message "netReference: This error is due to the InfrastructureElement {?elementLabel}, violating the rule: Applicable when the InfrastructureElement is defined and requires a reference to its net reference."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?elementLabel WHERE {
        $this rdfs:label ?elementLabel .
        FILTER NOT EXISTS { $this era:netReference ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:NotApplicable a sh:PropertyShape ;
    era:affectedClass era:Parameter ; 
    era:affectedProperty era:notApplicable ; 
    era:scope "local" ;

    rdfs:comment "Reference to a property that is not applicable."@en ; 
    # Source: ontology comment for notApplicable

    sh:in ( "Y" "N" "NYA" ) ; 
    # Source: RAG indicates allowed values for applicability

    sh:maxCount 1 ; 
    # Source: inferred from the nature of applicability, expecting a single value

    sh:message "notApplicable: The Parameter must have a notApplicable value that is one of the allowed options (Y, N, NYA). This error may be due to having more than one value or having a value that is not one of the allowed options."@en ;

    sh:path era:notApplicable ;

    sh:severity sh:Violation .

era-sh:NotApplicableApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Parameter ; 
    era:affectedProperty era:notApplicable ; 
    era:scope "local" ;

    rdfs:comment "Applicable when the Parameter is defined and requires a reference to its applicability status."@en ; 
    # Source: inferred from the definition of Parameter and its relationship to notApplicable

    sh:message "notApplicable: This error is due to the Parameter {?parameterLabel}, violating the rule: Applicable when the Parameter is defined and requires a reference to its applicability status."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?parameterLabel WHERE {
        $this rdfs:label ?parameterLabel .
        FILTER NOT EXISTS { $this era:notApplicable ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:NotYetAvailable a sh:PropertyShape ;
    era:affectedClass era:SomeClass ;  # Replace with the actual class that uses this property
    era:affectedProperty era:notYetAvailable ;
    era:scope "local" ;

    rdfs:comment "Reference to a property that is not provided."@en ;
    # Source: ontology comment for notYetAvailable

    sh:message "notYetAvailable: The property must indicate that the value is not yet available. This error may be due to having more than one value or having a value that is not correctly indicating unavailability."@en ;

    sh:maxCount 1 ;
    # Inferred: expecting a single value indicating unavailability

    sh:path era:notYetAvailable ;

    sh:severity sh:Violation . 

era-sh:NotYetAvailableApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SomeClass ;  # Replace with the actual class that uses this property
    era:affectedProperty era:notYetAvailable ;
    era:scope "local" ;

    rdfs:comment "Applicable when the property is defined and requires a reference to its availability status."@en ;
    # Source: inferred from the definition of the property and its usage

    sh:message "notYetAvailable: This error is due to the instance {?instanceLabel}, violating the rule: Applicable when the property is defined and requires a reference to its availability status."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?instanceLabel WHERE {
        $this rdfs:label ?instanceLabel .
        FILTER NOT EXISTS { $this era:notYetAvailable ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OfParameter a sh:PropertyShape ;
    era:affectedClass era:ParameterApplicability ;
    era:affectedProperty era:ofParameter ;
    era:scope "local" ;

    rdfs:comment "Indicates the parameter (object or datatype property) for which an applicability is being defined."@en ;
    # Source: ontology comment for ofParameter

    sh:class <http://data.europa.eu/949/Parameter> ;
    # Inferred: range should be defined based on the context of applicability

    sh:maxCount 1 ;
    # Source: inferred from the nature of applicability, expecting a single value

    sh:message "ofParameter: The ParameterApplicability must have at most one ofParameter value that is a Parameter. This error may be due to having more than one value or having a value that is not an instance of Parameter."@en ;

    sh:path era:ofParameter ;

    sh:severity sh:Violation .

era-sh:OfParameterApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ParameterApplicability ;
    era:affectedProperty era:ofParameter ;
    era:scope "local" ;

    rdfs:comment "Applicable when the ParameterApplicability is defined and requires a reference to its corresponding parameter."@en ;
    # Source: inferred from the definition of ParameterApplicability and its relationship to ofParameter

    sh:message "ofParameter: This error is due to the ParameterApplicability {?applicabilityLabel}, violating the rule: A parameter is mandatory to be provided when it corresponds to a core parameter or when the corresponding item exists on the network."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?applicabilityLabel WHERE {
        $this rdfs:label ?applicabilityLabel .
        FILTER NOT EXISTS { $this era:ofParameter ?value } .
        # Additional filters based on applicability conditions
        # Assuming a core parameter check or network existence check is needed
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OnElement a sh:PropertyShape ;
    era:affectedClass era:LinearElementSection ;
    era:affectedProperty era:onElement ;
    era:scope "local" ;

    rdfs:comment "Specifies the linear element a section belongs to."@en ;
    # Source: ontology comment for onElement

    sh:class era:LinearElement ;
    # Source: ontology range is era:LinearElement

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "onElement: The LinearElementSection must have at most one onElement reference that is an instance of LinearElement. This error may be due to having more than one value or having a value that is not an instance of LinearElement."@en ;

    sh:path era:onElement ;

    sh:severity sh:Violation .

era-sh:OnElementApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LinearElementSection ;
    era:affectedProperty era:onElement ;
    era:scope "local" ;

    rdfs:comment "Applicable when the LinearElementSection is defined and requires a reference to its linear element."@en ;
    # Source: inferred from the definition of LinearElementSection and its relationship to onElement

    sh:message "onElement: This error is due to the LinearElementSection {?sectionLabel}, violating the rule: Applicable when the LinearElementSection is defined and requires a reference to its linear element."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sectionLabel WHERE {
        $this rdfs:label ?sectionLabel .
        FILTER NOT EXISTS { $this era:onElement ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OnLinearElement a sh:PropertyShape ;
    era:affectedClass era:TopologicalCoordinate ;
    era:affectedProperty era:onLinearElement ;
    era:scope "local" ;

    rdfs:comment "Specifies the linear element a coordinate is associated with."@en ;
    # Source: ontology comment for onLinearElement

    sh:class era:LinearElement ;
    # Source: ontology range is era:LinearElement

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "onLinearElement: The TopologicalCoordinate must have at most one onLinearElement reference that is an instance of LinearElement. This error may be due to having more than one value or having a value that is not an instance of LinearElement."@en ;

    sh:path era:onLinearElement ;

    sh:severity sh:Violation .

era-sh:OnLinearElementApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TopologicalCoordinate ;
    era:affectedProperty era:onLinearElement ;
    era:scope "local" ;

    rdfs:comment "Applicable when the TopologicalCoordinate is defined and requires a reference to its associated linear element."@en ;
    # Source: inferred from the definition of TopologicalCoordinate and its relationship to onLinearElement

    sh:message "onLinearElement: This error is due to the TopologicalCoordinate {?coordinateLabel}, violating the rule: Applicable when the TopologicalCoordinate is defined and requires a reference to its associated linear element."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?coordinateLabel WHERE {
        $this rdfs:label ?coordinateLabel .
        FILTER NOT EXISTS { $this era:onLinearElement ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OpEnd a sh:PropertyShape ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:opEnd ;
    era:rinfIndex "1.1.0.0.0.4" ;
    era:scope "local" ;

    rdfs:comment "Operational point at the end of section of line (kilometres increasing from start OP to the end OP)."@en ;
    # Source: ontology comment for opEnd

    sh:class era:OperationalPoint ;
    # Source: ontology range is era:OperationalPoint

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "opEnd: The SectionOfLine must have at most one opEnd reference that is an instance of OperationalPoint. This error may be due to having more than one value or having a value that is not an instance of OperationalPoint."@en ;

    sh:path era:opEnd ;

    sh:severity sh:Violation .

era-sh:OpEndApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:opEnd ;
    era:scope "local" ;

    rdfs:comment "Applicable when the SectionOfLine is defined and requires a reference to its operational point at the end."@en ;
    # Source: inferred from the definition of SectionOfLine and its relationship to opEnd

    sh:message "opEnd: This error is due to the SectionOfLine {?sectionLabel}, violating the rule: Applicable when the SectionOfLine is defined and requires a reference to its operational point at the end."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sectionLabel WHERE {
        $this rdfs:label ?sectionLabel .
        FILTER NOT EXISTS { $this era:opEnd ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OpInfoPerCountry a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:opInfoPerCountry ;
    era:scope "local" ;

    rdfs:comment "Relates an operational point that represents the general information of a border point to the information that each country maintains for the border point."@en ;
    # Source: ontology comment for opInfoPerCountry

    sh:class era:OperationalPoint ;
    # Source: ontology range is era:OperationalPoint

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "opInfoPerCountry: The OperationalPoint must have at most one opInfoPerCountry reference that is an instance of OperationalPoint. This error may be due to having more than one value or having a value that is not an instance of OperationalPoint."@en ;

    sh:path era:opInfoPerCountry ;

    sh:severity sh:Violation .

era-sh:OpInfoPerCountryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:opInfoPerCountry ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OperationalPoint is defined and requires a reference to its operational point information per country."@en ;
    # Source: inferred from the definition of OperationalPoint and its relationship to opInfoPerCountry

    sh:message "opInfoPerCountry: This error is due to the OperationalPoint {?pointLabel}, violating the rule: Applicable when the OperationalPoint is defined and requires a reference to its operational point information per country."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?pointLabel WHERE {
        $this rdfs:label ?pointLabel .
        FILTER NOT EXISTS { $this era:opInfoPerCountry ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OpName a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:opName ;
    era:rinfIndex "1.2.0.0.0.1" ;
    era:scope "local" ;

    rdfs:comment "Name normally related to the town or village or to traffic control purpose."@en ;
    # Source: ontology comment for opName

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "opName: The OperationalPoint must have at most one opName value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:opName ;

    sh:severity sh:Violation .

era-sh:OpNameApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:opName ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OperationalPoint is defined and requires a name."@en ;
    # Source: inferred from the definition of OperationalPoint and its relationship to opName

    sh:message "opName: This error is due to the OperationalPoint {?pointLabel}, violating the rule: Applicable when the OperationalPoint is defined and requires a name."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?pointLabel WHERE {
        $this rdfs:label ?pointLabel .
        FILTER NOT EXISTS { $this era:opName ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OpStart a sh:PropertyShape ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:opStart ;
    era:rinfIndex "1.1.0.0.0.3" ;
    era:scope "local" ;

    rdfs:comment "Operational point at the start of section of line (kilometres increasing from start OP to the end OP)."@en ;
    # Source: ontology comment for opStart

    sh:class era:OperationalPoint ;
    # Source: ontology range is era:OperationalPoint

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "opStart: The SectionOfLine must have at most one opStart reference that is an instance of OperationalPoint. This error may be due to having more than one value or having a value that is not an instance of OperationalPoint."@en ;

    sh:path era:opStart ;

    sh:severity sh:Violation .

era-sh:OpStartApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:opStart ;
    era:scope "local" ;

    rdfs:comment "Applicable when the SectionOfLine is defined and requires a reference to its operational point at the start."@en ;
    # Source: inferred from the definition of SectionOfLine and its relationship to opStart

    sh:message "opStart: This error is due to the SectionOfLine {?sectionLabel}, violating the rule: Applicable when the SectionOfLine is defined and requires a reference to its operational point at the start."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sectionLabel WHERE {
        $this rdfs:label ?sectionLabel .
        FILTER NOT EXISTS { $this era:opStart ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OpType a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:opType ;
    era:rinfIndex "1.2.0.0.0.4" ;
    era:scope "local" ;

    rdfs:comment "Type of facility in relation to the dominating operational functions."@en ;
    # Source: ontology comment for opType

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "opType: The OperationalPoint must have at most one opType value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:opType ;

    sh:severity sh:Violation .

era-sh:OpTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:opType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OperationalPoint is defined and requires a type."@en ;
    # Source: inferred from the definition of OperationalPoint and its relationship to opType

    sh:message "opType: This error is due to the OperationalPoint {?pointLabel}, violating the rule: Applicable when the OperationalPoint is defined and requires a type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?pointLabel WHERE {
        $this rdfs:label ?pointLabel .
        FILTER NOT EXISTS { $this era:opType ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OpTypeGaugeChangeover a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:opTypeGaugeChangeover ;
    era:rinfIndex "1.2.0.0.0.4.1" ;
    era:scope "local" ;

    rdfs:comment "Type of track gauge changeover facility."@en ;
    # Source: ontology comment for opTypeGaugeChangeover

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "opTypeGaugeChangeover: The OperationalPoint must have at most one opTypeGaugeChangeover value that is a string. This error may be due to having more than one value or having a value that is not a string."@en ;

    sh:path era:opTypeGaugeChangeover ;

    sh:severity sh:Violation .

era-sh:OpTypeGaugeChangeoverApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:opTypeGaugeChangeover ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OperationalPoint is defined and requires a reference to its gauge changeover type."@en ;
    # Source: inferred from the definition of OperationalPoint and its relationship to opTypeGaugeChangeover

    sh:message "opTypeGaugeChangeover: This error is due to the OperationalPoint {?pointLabel}, violating the rule: Applicable when the OperationalPoint is defined and requires a reference to its gauge changeover type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?pointLabel WHERE {
        $this rdfs:label ?pointLabel .
        FILTER NOT EXISTS { $this era:opTypeGaugeChangeover ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OperatingLanguage a sh:PropertyShape ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:operatingLanguage ;
    era:scope "local" ;

    rdfs:comment "The language or languages used in daily operation by infrastructure manager and published in its Network Statement, for the communication of operational or safety related messages between the staff of the infrastructure manager and the railway undertaking."@en ;
    # Source: ontology comment for operatingLanguage

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "operatingLanguage: The InfrastructureElement must have at most one operatingLanguage value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:operatingLanguage ;

    sh:severity sh:Violation .

era-sh:OperatingLanguageApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:operatingLanguage ;
    era:scope "local" ;

    rdfs:comment "Applicable when the InfrastructureElement is defined and requires a reference to its operating language."@en ;
    # Source: inferred from the definition of InfrastructureElement and its relationship to operatingLanguage

    sh:message "operatingLanguage: This error is due to the InfrastructureElement {?elementLabel}, violating the rule: Applicable when the InfrastructureElement is defined and requires a reference to its operating language."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?elementLabel WHERE {
        $this rdfs:label ?elementLabel .
        FILTER NOT EXISTS { $this era:operatingLanguage ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OperationalRegimeType a sh:PropertyShape ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:operationalRegimeType ;
    era:scope "local" ;

    rdfs:comment "Defines the operational regime type applicable to a section of line."@en ;
    # Source: inferred from the ontology definition of operationalRegimeType

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "operationalRegimeType: The SectionOfLine must have at most one operationalRegimeType value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:operationalRegimeType ;

    sh:severity sh:Violation .

era-sh:OperationalRegimeTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:operationalRegimeType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the SectionOfLine is defined and requires a reference to its operational regime type."@en ;
    # Source: inferred from the definition of SectionOfLine and its relationship to operationalRegimeType

    sh:message "operationalRegimeType: This error is due to the SectionOfLine {?sectionLabel}, violating the rule: Applicable when the SectionOfLine is defined and requires a reference to its operational regime type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sectionLabel WHERE {
        $this rdfs:label ?sectionLabel .
        FILTER NOT EXISTS { $this era:operationalRegimeType ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OperationalRestriction a sh:PropertyShape ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:operationalRestriction ;
    era:scope "local" ;

    rdfs:comment "Indicates an operational restriction of vehicle or wagon. Parking brake type (if the vehicle is fitted with it)."@en ;
    # Source: ontology comment for operationalRestriction

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "operationalRestriction: The Vehicle must have at most one operationalRestriction value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:operationalRestriction ;

    sh:severity sh:Violation .

era-sh:OperationalRestrictionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:operationalRestriction ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Vehicle is defined and requires a reference to its operational restriction."@en ;
    # Source: inferred from the definition of Vehicle and its relationship to operationalRestriction

    sh:message "operationalRestriction: This error is due to the Vehicle {?vehicleLabel}, violating the rule: Applicable when the Vehicle is defined and requires a reference to its operational restriction."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:operationalRestriction ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Orientation a sh:PropertyShape ;
    era:affectedClass era:Orientation ;
    era:affectedProperty era:orientation ;
    era:scope "local" ;

    rdfs:comment "The orientation of the object in relation to the carrier linear element. Possible values: Normal, Opposite, Both."@en ;
    # Source: ontology comment for orientation

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "orientation: The Orientation must have at most one orientation value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:orientation ;

    sh:severity sh:Violation .

era-sh:OrientationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Orientation ;
    era:affectedProperty era:orientation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Orientation is defined and requires a reference to its orientation."@en ;
    # Source: inferred from the definition of Orientation and its relationship to orientation

    sh:message "orientation: This error is due to the Orientation {?orientationLabel}, violating the rule: Applicable when the Orientation is defined and requires a reference to its orientation."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?orientationLabel WHERE {
        $this rdfs:label ?orientationLabel .
        FILTER NOT EXISTS { $this era:orientation ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OsmClass a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:osmClass ; 
    era:scope "local" ;

    rdfs:comment "(deprecated) not in use anymore"@en ; 
    # Source: ontology comment for osmClass

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ; 
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ; 
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "osmClass: The CommonCharacteristicsSubset must have at most one osmClass value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ; 

    sh:path era:osmClass ; 

    sh:severity sh:Violation .

era-sh:OsmClassApplicability a sh:SPARQLConstraint ; 
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:osmClass ; 
    era:scope "local" ; 

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its OpenStreetMap class."@en ; 
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to osmClass

    sh:message "osmClass: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its OpenStreetMap class."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:osmClass ?value } .
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:OtherCantDeficiencyBasicSSP a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:otherCantDeficiencyBasicSSP ; 
    era:scope "local" ;

    rdfs:comment "Essential information for drivers of trains with a worse (lower) tolerated cant deficiency than those for which the ETCS trackside provides SSP in conjunction with parameter 'Cant Deficiency used for the basic SSP'."@en ;
    # Source: ontology comment for otherCantDeficiencyBasicSSP

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "otherCantDeficiencyBasicSSP: The CommonCharacteristicsSubset must have at most one otherCantDeficiencyBasicSSP value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:otherCantDeficiencyBasicSSP ;

    sh:severity sh:Violation .

era-sh:OtherCantDeficiencyBasicSSPApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:otherCantDeficiencyBasicSSP ; 
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its other cant deficiency basic SSP."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to otherCantDeficiencyBasicSSP

    sh:message "otherCantDeficiencyBasicSSP: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its other cant deficiency basic SSP."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:otherCantDeficiencyBasicSSP ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OtherPantographHead a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:otherPantographHead ;
    era:rinfIndex "1.1.1.2.3.2" ;
    era:scope "local" ;

    rdfs:comment "Indication of pantograph heads which are allowed to be used."@en ;
    # Source: ontology comment for otherPantographHead

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "otherPantographHead: The CommonCharacteristicsSubset must have at most one otherPantographHead value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:otherPantographHead ;

    sh:severity sh:Violation .

era-sh:OtherPantographHeadApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:otherPantographHead ;
    era:scope "local" ;

    rdfs:comment "Applicable only if 'Overhead contact line (OCL)' is selected in 1.1.1.2.2.1.1."@en ;
    # Source: RAG explanation on applicability

    sh:message "otherPantographHead: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only if 'Overhead contact line (OCL)' is selected."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:otherPantographHead ?value } .
        # Additional condition to check if OCL is selected
        FILTER NOT EXISTS { ?oclSelection era:overheadContactLine ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OtherTrainProtection a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:otherTrainProtection ;
    era:rinfIndex "1.1.1.3.10.2" ;
    era:scope "local" ;

    rdfs:comment "Indication of existence of other system than ETCS for degraded situation."@en ;
    # Source: ontology comment for otherTrainProtection

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "otherTrainProtection: The CommonCharacteristicsSubset must have at most one otherTrainProtection value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:otherTrainProtection ;

    sh:severity sh:Violation .

era-sh:OtherTrainProtectionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:otherTrainProtection ;
    era:scope "local" ;

    rdfs:comment "Applicable only when for parameter 1.1.1.3.10.1 'none' was selected."@en ;
    # Source: dependencyNote in ontology and RAG explanation on applicability

    sh:message "otherTrainProtection: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when for parameter 1.1.1.3.10.1 'none' was selected."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:otherTrainProtection ?value } .
        # Additional condition to check if parameter 1.1.1.3.10.1 is 'none'
        FILTER NOT EXISTS { ?param1_1_1_3_10_1 era:etcsLevelType "none" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ParameterApplicability a sh:PropertyShape ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:parameterApplicability ;
    era:scope "local" ;

    rdfs:comment "Relates an infrastructure element or a common characteristics subset with the applicability of a certain parameter."@en ;
    # Source: ontology comment for parameterApplicability

    sh:class era:ParameterApplicability ;
    # Source: ontology range is era:ParameterApplicability

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "parameterApplicability: The InfrastructureElement must have at most one parameterApplicability value that is an instance of ParameterApplicability. This error may be due to having more than one value or having a value that is not an instance of ParameterApplicability."@en ;

    sh:path era:parameterApplicability ;

    sh:severity sh:Violation .

era-sh:ParameterApplicabilityApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:parameterApplicability ;
    era:scope "local" ;

    rdfs:comment "Applicable when the InfrastructureElement is defined and requires a reference to its parameter applicability."@en ;
    # Source: inferred from the definition of InfrastructureElement and its relationship to parameterApplicability

    sh:message "parameterApplicability: This error is due to the InfrastructureElement {?elementLabel}, violating the rule: Applicable when the InfrastructureElement is defined and requires a reference to its parameter applicability."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?elementLabel WHERE {
        $this rdfs:label ?elementLabel .
        FILTER NOT EXISTS { $this era:parameterApplicability ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ParameterValueType a sh:PropertyShape ;
    era:affectedClass era:ParameterApplicability ;
    era:affectedProperty era:parameterValueType ;
    era:scope "local" ;

    rdfs:comment "In the case of a change in the value of a parameter, there needs to be an information on the type of the new value. The 'Value type' will have different possible options: 'nominal', 'planned temporary restriction', 'permanent restriction', 'planned temporary closure'."@en ;
    # Source: ontology comment for parameterValueType

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "parameterValueType: The ParameterApplicability must have at most one parameterValueType value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:parameterValueType ;

    sh:severity sh:Violation .

era-sh:ParameterValueTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ParameterApplicability ;
    era:affectedProperty era:parameterValueType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the ParameterApplicability is defined and requires a reference to its parameter value type."@en ;
    # Source: inferred from the definition of ParameterApplicability and its relationship to parameterValueType

    sh:message "parameterValueType: This error is due to the ParameterApplicability {?applicabilityLabel}, violating the rule: Applicable when the ParameterApplicability is defined and requires a reference to its parameter value type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?applicabilityLabel WHERE {
        $this rdfs:label ?applicabilityLabel .
        FILTER NOT EXISTS { $this era:parameterValueType ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ParkingBrakeType a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:parkingBrakeType ;
    era:scope "local" ;

    rdfs:comment "Parking brake type (if the vehicle is fitted with it)."@en ;
    # Source: ontology comment for parkingBrakeType

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "parkingBrakeType: The VehicleType must have at most one parkingBrakeType value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:parkingBrakeType ;

    sh:severity sh:Violation .

era-sh:ParkingBrakeTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:parkingBrakeType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires a reference to its parking brake type."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to parkingBrakeType

    sh:message "parkingBrakeType: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the VehicleType is defined and requires a reference to its parking brake type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:parkingBrakeType ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PassesThroughTunnel a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:passesThroughTunnel ;
    era:scope "local" ;

    rdfs:comment "References a tunnel that a track goes through."@en ;
    # Source: ontology comment for passesThroughTunnel

    sh:class era:Tunnel ;
    # Source: ontology range is era:Tunnel

    sh:maxCount 1 ;
    # Inferred: expected at most one tunnel reference per running track

    sh:message "passesThroughTunnel: The RunningTrack must have at most one passesThroughTunnel value that is a Tunnel. This error may be due to having more than one value or having a value that is not an instance of Tunnel."@en ;

    sh:path era:passesThroughTunnel ;

    sh:severity sh:Violation .

era-sh:PassesThroughTunnelApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:passesThroughTunnel ;
    era:scope "local" ;

    rdfs:comment "Applicable when the RunningTrack is defined and requires a reference to the tunnel it passes through."@en ;
    # Source: inferred from the definition of RunningTrack and its relationship to passesThroughTunnel

    sh:message "passesThroughTunnel: This error is due to the RunningTrack {?trackLabel}, violating the rule: Applicable when the RunningTrack is defined and requires a reference to the tunnel it passes through."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:passesThroughTunnel ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PlatformEdge a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:platformEdge ;
    era:scope "local" ;

    rdfs:comment "Reference to a related platform edge."@en ;
    # Source: ontology comment for platformEdge

    sh:class era:PlatformEdge ;
    # Source: ontology range is era:PlatformEdge

    sh:maxCount 1 ;
    # Inferred: expected at most one platform edge reference per running track

    sh:message "platformEdge: The RunningTrack must have at most one platformEdge value that is a PlatformEdge. This error may be due to having more than one value or having a value that is not an instance of PlatformEdge."@en ;

    sh:path era:platformEdge ;

    sh:severity sh:Violation .

era-sh:PlatformEdgeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:platformEdge ;
    era:scope "local" ;

    rdfs:comment "Applicable when the RunningTrack is defined and requires a reference to its platform edge."@en ;
    # Source: inferred from the definition of RunningTrack and its relationship to platformEdge

    sh:message "platformEdge: This error is due to the RunningTrack {?trackLabel}, violating the rule: Applicable when the RunningTrack is defined and requires a reference to its platform edge."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:platformEdge ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PlatformHeight a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:platformHeight ;
    era:scope "local" ;

    rdfs:comment "Distance between the upper surface of platform and running surface of the neighbouring track. It is the nominal value expressed in millimetres."@en ;
    # Source: ontology comment for platformHeight

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "platformHeight: The CommonCharacteristicsSubset must have at most one platformHeight value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:platformHeight ;

    sh:severity sh:Violation .

era-sh:PlatformHeightApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:platformHeight ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its platform height."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to platformHeight

    sh:message "platformHeight: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its platform height."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:platformHeight ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PreviousVehicleType a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:previousVehicleType ;
    era:scope "local" ;

    rdfs:comment "Denotes the previous VehicleType."@en ;
    # Source: ontology comment for previousVehicleType

    sh:class era:VehicleType ;
    # Source: ontology range is era:VehicleType

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "previousVehicleType: The VehicleType must have at most one previousVehicleType value that is an instance of VehicleType. This error may be due to having more than one value or having a value that is not an instance of VehicleType."@en ;

    sh:path era:previousVehicleType ;

    sh:severity sh:Violation .

era-sh:PreviousVehicleTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:previousVehicleType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires a reference to its previous vehicle type."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to previousVehicleType

    sh:message "previousVehicleType: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the VehicleType is defined and requires a reference to its previous vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:previousVehicleType ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PrimaryLocation a sh:PropertyShape ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:primaryLocation ;
    era:rinfIndex "1.2.0.0.0.3" ;
    era:scope "local" ;

    rdfs:comment "Primary location code developed for information exchange in accordance with the TSIs relating to the telematics applications subsystem."@en ;
    # Source: ontology comment for primaryLocation

    sh:class era:PrimaryLocation ;
    # Source: ontology range is era:PrimaryLocation

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "primaryLocation: The InfrastructureElement must have at most one primaryLocation value that is a PrimaryLocation. This error may be due to having more than one value or having a value that is not an instance of PrimaryLocation."@en ;

    sh:path era:primaryLocation ;

    sh:severity sh:Violation .

era-sh:PrimaryLocationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:primaryLocation ;
    era:scope "local" ;

    rdfs:comment "Applicable only when a primary location exists for the InfrastructureElement."@en ;
    # Source: dependencyNote in ontology and RAG explanation on applicability

    sh:message "primaryLocation: This error is due to the InfrastructureElement {?elementLabel}, violating the rule: Applicable only when a primary location exists."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?elementLabel WHERE {
        $this rdfs:label ?elementLabel .
        FILTER NOT EXISTS { $this era:primaryLocation ?value } .
        # Additional condition to check if primary location exists
        FILTER NOT EXISTS { ?primaryLocation era:existsFor $this } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ProfileNumberSemiTrailers a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:profileNumberSemiTrailers ;
    era:rinfIndex "1.1.1.1.3.5" ;
    era:scope "local" ;

    rdfs:comment "Coding for combined transport for semi-trailers (for all freight and mixed-traffic lines) in accordance with the specification referenced in Appendix A-1, index [B]."@en ;
    # Source: ontology comment for profileNumberSemiTrailers

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "profileNumberSemiTrailers: The CommonCharacteristicsSubset must have at most one profileNumberSemiTrailers value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:profileNumberSemiTrailers ;

    sh:severity sh:Violation .

era-sh:ProfileNumberSemiTrailersApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:profileNumberSemiTrailers ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its profile number for semi-trailers."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to profileNumberSemiTrailers

    sh:message "profileNumberSemiTrailers: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its profile number for semi-trailers."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:profileNumberSemiTrailers ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ProfileNumberSwapBodies a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:profileNumberSwapBodies ;
    era:rinfIndex "1.1.1.1.3.4" ;
    era:scope "local" ;

    rdfs:comment "Coding for combined transport with swap bodies (for all freight and mixed-traffic lines) in accordance with the specification referenced in Appendix A-1, index [B]."@en ;
    # Source: ontology comment for profileNumberSwapBodies

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "profileNumberSwapBodies: The CommonCharacteristicsSubset must have at most one profileNumberSwapBodies value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:profileNumberSwapBodies ;

    sh:severity sh:Violation .

era-sh:ProfileNumberSwapBodiesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:profileNumberSwapBodies ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its profile number for swap bodies."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to profileNumberSwapBodies

    sh:message "profileNumberSwapBodies: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its profile number for swap bodies."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:profileNumberSwapBodies ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ProtectionLegacySystem a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:protectionLegacySystem ;
    era:scope "local" ;

    rdfs:comment "Indication of which class B system is installed."@en ;
    # Source: ontology comment for protectionLegacySystem

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "protectionLegacySystem: The CommonCharacteristicsSubset must have at most one protectionLegacySystem value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:protectionLegacySystem ;

    sh:severity sh:Violation .

era-sh:ProtectionLegacySystemApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:protectionLegacySystem ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its protection legacy system."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to protectionLegacySystem

    sh:message "protectionLegacySystem: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its protection legacy system."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:protectionLegacySystem ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:QNvdriverAdhes a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:qNvdriverAdhes ;
    era:scope "local" ;

    rdfs:comment "Qualifier determining whether the driver is allowed to modify the adhesion factor used by the ETCS on-board to calculate the braking curves."@en ;
    # Source: ontology comment for qNvdriverAdhes

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "qNvdriverAdhes: The CommonCharacteristicsSubset must have at most one qNvdriverAdhes value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:qNvdriverAdhes ;

    sh:severity sh:Violation .

era-sh:QNvdriverAdhesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:qNvdriverAdhes ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its qNvdriverAdhes."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to qNvdriverAdhes

    sh:message "qNvdriverAdhes: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its qNvdriverAdhes."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:qNvdriverAdhes ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:QNvemrrls a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:qNvemrrls ;
    era:scope "local" ;

    rdfs:comment "Qualifier defining whether the application of the emergency brake for reasons other than a trip can be revoked as soon as the conditions for it have disappeared or after the train has come to a complete standstill."@en ;
    # Source: ontology comment for qNvemrrls

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "qNvemrrls: The CommonCharacteristicsSubset must have at most one qNvemrrls value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:qNvemrrls ;

    sh:severity sh:Violation .

era-sh:QNvemrrlsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:qNvemrrls ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its qNvemrrls."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to qNvemrrls

    sh:message "qNvemrrls: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its qNvemrrls."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:qNvemrrls ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:QuieterRoutesExemptedCountry a sh:PropertyShape ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:quieterRoutesExemptedCountry ;
    era:scope "local" ;

    rdfs:comment "Country where a vehicle may operate without noise restrictions."@en ;
    # Source: ontology comment for quieterRoutesExemptedCountry

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "quieterRoutesExemptedCountry: The Vehicle must have at most one quieterRoutesExemptedCountry value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:quieterRoutesExemptedCountry ;

    sh:severity sh:Violation .

era-sh:QuieterRoutesExemptedCountryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:quieterRoutesExemptedCountry ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Vehicle is defined and requires a reference to the country where it may operate without noise restrictions."@en ;
    # Source: inferred from the definition of Vehicle and its relationship to quieterRoutesExemptedCountry

    sh:message "quieterRoutesExemptedCountry: This error is due to the Vehicle {?vehicleLabel}, violating the rule: Applicable when the Vehicle is defined and requires a reference to the country where it may operate without noise restrictions."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:quieterRoutesExemptedCountry ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RailInclination a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:railInclination ;
    era:scope "local" ;

    rdfs:comment "An angle defining the inclination of the head of a rail relative to the running surface."@en ;
    # Source: ontology comment for railInclination

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "railInclination: The RunningTrack must have at most one railInclination value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:railInclination ;

    sh:severity sh:Violation .

era-sh:RailInclinationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:railInclination ;
    era:scope "local" ;

    rdfs:comment "Applicable when the RunningTrack is defined and requires a reference to its rail inclination."@en ;
    # Source: inferred from the definition of RunningTrack and its relationship to railInclination

    sh:message "railInclination: This error is due to the RunningTrack {?trackLabel}, violating the rule: Applicable when the RunningTrack is defined and requires a reference to its rail inclination."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:railInclination ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ReasonsEtcsRadioBlockCenterReject a sh:PropertyShape ;
    era:affectedClass era:RadioBlockCenter ;
    era:affectedProperty era:reasonsEtcsRadioBlockCenterReject ;
    era:scope "local" ;

    rdfs:comment "List of cases subject to system design choices made by the infrastructure manager according to the specification referenced in Appendix A-1, index [C]."@en ;
    # Source: ontology comment for reasonsEtcsRadioBlockCenterReject

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "reasonsEtcsRadioBlockCenterReject: The RadioBlockCenter must have at most one reasonsEtcsRadioBlockCenterReject value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:reasonsEtcsRadioBlockCenterReject ;

    sh:severity sh:Violation .

era-sh:ReasonsEtcsRadioBlockCenterRejectApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RadioBlockCenter ;
    era:affectedProperty era:reasonsEtcsRadioBlockCenterReject ;
    era:scope "local" ;

    rdfs:comment "Applicable when the RadioBlockCenter is defined and requires a reference to its rejection reasons."@en ;
    # Source: inferred from the definition of RadioBlockCenter and its relationship to reasonsEtcsRadioBlockCenterReject

    sh:message "reasonsEtcsRadioBlockCenterReject: This error is due to the RadioBlockCenter {?centerLabel}, violating the rule: Applicable when the RadioBlockCenter is defined and requires a reference to its rejection reasons."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?centerLabel WHERE {
        $this rdfs:label ?centerLabel .
        FILTER NOT EXISTS { $this era:reasonsEtcsRadioBlockCenterReject ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ReferenceBorderPoint a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:referenceBorderPoint ;
    era:scope "local" ;

    rdfs:comment "Relates an operational point that is a border point with an instance of the list of reference border points that are specified in the RINF Application Guide."@en ;
    # Source: ontology comment for referenceBorderPoint

    sh:class era:ReferenceBorderPoint ;
    # Source: ontology range is era:ReferenceBorderPoint

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "referenceBorderPoint: The OperationalPoint must have at most one referenceBorderPoint value that is an instance of ReferenceBorderPoint. This error may be due to having more than one value or having a value that is not an instance of ReferenceBorderPoint."@en ;

    sh:path era:referenceBorderPoint ;

    sh:severity sh:Violation .

era-sh:ReferenceBorderPointApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:referenceBorderPoint ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OperationalPoint is defined and requires a reference to its corresponding border point."@en ;
    # Source: inferred from the definition of OperationalPoint and its relationship to referenceBorderPoint

    sh:message "referenceBorderPoint: This error is due to the OperationalPoint {?pointLabel}, violating the rule: Applicable when the OperationalPoint is defined and requires a reference to its corresponding border point."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?pointLabel WHERE {
        $this rdfs:label ?pointLabel .
        FILTER NOT EXISTS { $this era:referenceBorderPoint ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Role a sh:PropertyShape ;
    era:affectedClass era:Body ;
    era:affectedProperty era:role ;
    era:scope "local" ;

    rdfs:comment "Indicates the relationship of a Body to the organisation roles that it can play."@en ;
    # Source: ontology comment for role

    sh:class era:OrganisationRole ;
    # Source: ontology range is era:OrganisationRole

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "role: The Body must have at most one role value that is an OrganisationRole. This error may be due to having more than one value or having a value that is not an instance of OrganisationRole."@en ;

    sh:path era:role ;

    sh:severity sh:Violation .

era-sh:RoleApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Body ;
    era:affectedProperty era:role ;
    era:scope "local" ;

    rdfs:comment "Applicable when a Body is defined and requires a reference to its role."@en ;
    # Source: inferred from the definition of Body and its relationship to role

    sh:message "role: This error is due to the Body {?bodyLabel}, violating the rule: Applicable when the Body is defined and requires a reference to its role."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?bodyLabel WHERE {
        $this rdfs:label ?bodyLabel .
        FILTER NOT EXISTS { $this era:role ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RoleOf a sh:PropertyShape ;
    era:affectedClass era:OrganisationRole ;
    era:affectedProperty era:roleOf ;
    era:scope "local" ;

    rdfs:comment "Indicates the corresponding Body that plays a certain organisation role."@en ;
    # Source: ontology comment for roleOf

    sh:class era:Body ;
    # Source: ontology range is era:Body

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "roleOf: The OrganisationRole must have at most one roleOf value that is a Body. This error may be due to having more than one value or having a value that is not an instance of Body."@en ;

    sh:path era:roleOf ;

    sh:severity sh:Violation .

era-sh:RoleOfApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OrganisationRole ;
    era:affectedProperty era:roleOf ;
    era:scope "local" ;

    rdfs:comment "Applicable when an OrganisationRole is defined and requires a reference to its corresponding Body."@en ;
    # Source: inferred from the definition of OrganisationRole and its relationship to roleOf

    sh:message "roleOf: This error is due to the OrganisationRole {?roleLabel}, violating the rule: Applicable when the OrganisationRole is defined and requires a reference to its corresponding Body."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?roleLabel WHERE {
        $this rdfs:label ?roleLabel .
        FILTER NOT EXISTS { $this era:roleOf ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RollingStockFireCategory a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:rollingStockFireCategory ;
    era:scope "local" ;

    rdfs:comment "Categorisation how a passenger train with a fire on board will continue to operate for a defined time period."@en ;
    # Source: ontology comment for rollingStockFireCategory

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "rollingStockFireCategory: The Tunnel must have at most one rollingStockFireCategory value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:rollingStockFireCategory ;

    sh:severity sh:Violation .

era-sh:RollingStockFireCategoryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:rollingStockFireCategory ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Tunnel is defined and requires a reference to its rolling stock fire category."@en ;
    # Source: inferred from the definition of Tunnel and its relationship to rollingStockFireCategory

    sh:message "rollingStockFireCategory: This error is due to the Tunnel {?tunnelLabel}, violating the rule: Applicable when the Tunnel is defined and requires a reference to its rolling stock fire category."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:rollingStockFireCategory ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SafeConsistLengthInformationNecessary a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:safeConsistLengthInformationNecessary ;
    era:scope "local" ;

    rdfs:comment "Indication whether safe consist train length information from on-board is required to access the line for safety reasons and the required safety integrity level."@en ;
    # Source: ontology comment for safeConsistLengthInformationNecessary

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "safeConsistLengthInformationNecessary: The CommonCharacteristicsSubset must have at most one safeConsistLengthInformationNecessary value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:safeConsistLengthInformationNecessary ;

    sh:severity sh:Violation .

era-sh:SafeConsistLengthInformationNecessaryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:safeConsistLengthInformationNecessary ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its safe consist length information."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to safeConsistLengthInformationNecessary

    sh:message "safeConsistLengthInformationNecessary: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its safe consist length information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:safeConsistLengthInformationNecessary ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SchematicOverviewOP a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:schematicOverviewOP ;
    era:scope "local" ;

    rdfs:comment "Document providing the schematic overview of the operational point."@en ;
    # Source: ontology comment for schematicOverviewOP

    sh:datatype xsd:anyURI ;
    # Source: ontology range is xsd:anyURI

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "schematicOverviewOP: The OperationalPoint must have at most one schematicOverviewOP value that is a valid URI. This error may be due to having more than one value or having a value that is not a valid URI."@en ;

    sh:path era:schematicOverviewOP ;

    sh:severity sh:Violation .

era-sh:SchematicOverviewOPApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:schematicOverviewOP ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OperationalPoint is defined and requires a reference to its schematic overview."@en ;
    # Source: inferred from the definition of OperationalPoint and its relationship to schematicOverviewOP

    sh:message "schematicOverviewOP: This error is due to the OperationalPoint {?pointLabel}, violating the rule: Applicable when the OperationalPoint is defined and requires a reference to its schematic overview."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?pointLabel WHERE {
        $this rdfs:label ?pointLabel .
        FILTER NOT EXISTS { $this era:schematicOverviewOP ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Side a sh:PropertyShape ;
    era:affectedClass era:Orientation ;
    era:affectedProperty era:side ;
    era:scope "local" ;

    rdfs:comment "Indication of the position of the signal in relation to the track in the running direction from the origin of the referencing system (e.g., direction of the main line)."@en ;
    # Source: ontology comment for side

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "side: The Orientation must have at most one side value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:side ;

    sh:severity sh:Violation .

era-sh:SideApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Orientation ;
    era:affectedProperty era:side ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Orientation is defined and requires a reference to its side."@en ;
    # Source: inferred from the definition of Orientation and its relationship to side

    sh:message "side: This error is due to the Orientation {?orientationLabel}, violating the rule: Applicable when the Orientation is defined and requires a reference to its side."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?orientationLabel WHERE {
        $this rdfs:label ?orientationLabel .
        FILTER NOT EXISTS { $this era:side ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Siding a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:siding ;
    era:scope "local" ;

    rdfs:comment "Reference to a related siding."@en ;
    # Source: ontology comment for siding

    sh:class era:Siding ;
    # Source: ontology range is era:Siding

    sh:maxCount 1 ;
    # Inferred: siding should be a unique reference within an OperationalPoint context

    sh:message "siding: The OperationalPoint must have at most one siding value that is an instance of Siding. This error may be due to having more than one value or having a value that is not an instance of Siding."@en ;

    sh:path era:siding ;

    sh:severity sh:Violation .

era-sh:SidingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:siding ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OperationalPoint is defined and requires a reference to its siding."@en ;
    # Source: inferred from the definition of OperationalPoint and its relationship to siding

    sh:message "siding: This error is due to the OperationalPoint {?pointLabel}, violating the rule: Applicable when the OperationalPoint is defined and requires a reference to its siding."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?pointLabel WHERE {
        $this rdfs:label ?pointLabel .
        FILTER NOT EXISTS { $this era:siding ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SignalOrientation a sh:PropertyShape ;
    era:affectedClass era:Signal ;
    era:affectedProperty era:signalOrientation ;
    era:scope "local" ;

    rdfs:comment "Relative position to the line of reference, given in km and indication if the signal refers to normal or opposite track direction."@en ;
    # Source: ontology comment for signalOrientation

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "signalOrientation: The Signal must have at most one signalOrientation value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:signalOrientation ;

    sh:severity sh:Violation .

era-sh:SignalOrientationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Signal ;
    era:affectedProperty era:signalOrientation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Signal is defined and requires a reference to its orientation."@en ;
    # Source: inferred from the definition of Signal and its relationship to signalOrientation

    sh:message "signalOrientation: This error is due to the Signal {?signalLabel}, violating the rule: Applicable when the Signal is defined and requires a reference to its orientation."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?signalLabel WHERE {
        $this rdfs:label ?signalLabel .
        FILTER NOT EXISTS { $this era:signalOrientation ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SignalType a sh:PropertyShape ;
    era:affectedClass era:Signal ;
    era:affectedProperty era:signalType ;
    era:scope "local" ;

    rdfs:comment "Signalling information for Route Book compilation. This list shall include fixed signals that protect danger points."@en ;
    # Source: ontology comment for signalType

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "signalType: The Signal must have at most one signalType value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:signalType ;

    sh:severity sh:Violation .

era-sh:SignalTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Signal ;
    era:affectedProperty era:signalType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Signal is defined and requires a reference to its type."@en ;
    # Source: inferred from the definition of Signal and its relationship to signalType

    sh:message "signalType: This error is due to the Signal {?signalLabel}, violating the rule: Applicable when the Signal is defined and requires a reference to its type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?signalLabel WHERE {
        $this rdfs:label ?signalLabel .
        FILTER NOT EXISTS { $this era:signalType ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SnowIceHailConditions a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:snowIceHailConditions ;
    era:scope "local" ;

    rdfs:comment "Snow, ice and hail conditions."@en ;
    # Source: ontology comment for snowIceHailConditions

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "snowIceHailConditions: The VehicleType must have at most one snowIceHailConditions value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:snowIceHailConditions ;

    sh:severity sh:Violation .

era-sh:SnowIceHailConditionsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:snowIceHailConditions ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires a reference to its snow, ice and hail conditions."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to snowIceHailConditions

    sh:message "snowIceHailConditions: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the VehicleType is defined and requires a reference to its snow, ice and hail conditions."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:snowIceHailConditions ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SolNature a sh:PropertyShape ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:solNature ;
    era:scope "local" ;

    rdfs:comment "Kind of Section of Line expressing size of presented data which depends on fact whether it connects OPs generated by division of a big node into several OPs or not."@en ;
    # Source: ontology comment for solNature

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "solNature: The SectionOfLine must have at most one solNature value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:solNature ;

    sh:severity sh:Violation .

era-sh:SolNatureApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:solNature ;
    era:scope "local" ;

    rdfs:comment "Applicable when the SectionOfLine is defined and requires a reference to its nature."@en ;
    # Source: inferred from the definition of SectionOfLine and its relationship to solNature

    sh:message "solNature: This error is due to the SectionOfLine {?sectionLabel}, violating the rule: Applicable when the SectionOfLine is defined and requires a reference to its nature."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sectionLabel WHERE {
        $this rdfs:label ?sectionLabel .
        FILTER NOT EXISTS { $this era:solNature ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SpecialAreaType a sh:PropertyShape ;
    era:affectedClass era:SpecialArea ;
    era:affectedProperty era:specialAreaType ;
    era:scope "local" ;

    rdfs:comment "Indicates the special area or location types such as safe areas and restricted area types."@en ;
    # Source: ontology comment for specialAreaType

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "specialAreaType: The SpecialArea must have at most one specialAreaType value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:specialAreaType ;

    sh:severity sh:Violation .

era-sh:SpecialAreaTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SpecialArea ;
    era:affectedProperty era:specialAreaType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the SpecialArea is defined and requires a reference to its special area type."@en ;
    # Source: inferred from the definition of SpecialArea and its relationship to specialAreaType

    sh:message "specialAreaType: This error is due to the SpecialArea {?areaLabel}, violating the rule: Applicable when the SpecialArea is defined and requires a reference to its special area type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?areaLabel WHERE {
        $this rdfs:label ?areaLabel .
        FILTER NOT EXISTS { $this era:specialAreaType ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SpecialTunnelArea a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:specialTunnelArea ;
    era:scope "local" ;

    rdfs:comment "Relates a tunnel with a special area or location."@en ;
    # Source: ontology comment for specialTunnelArea

    sh:class era:SpecialTunnelArea ;
    # Source: ontology range is era:SpecialTunnelArea

    sh:maxCount 1 ;
    # Inferred: a tunnel should have at most one special tunnel area associated with it

    sh:message "specialTunnelArea: The Tunnel must have at most one specialTunnelArea value that is an instance of SpecialTunnelArea. This error may be due to having more than one value or having a value that is not an instance of SpecialTunnelArea."@en ;

    sh:path era:specialTunnelArea ;

    sh:severity sh:Violation .

era-sh:SpecialTunnelAreaApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:specialTunnelArea ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Tunnel is defined and requires a reference to its special tunnel area."@en ;
    # Source: inferred from the definition of Tunnel and its relationship to specialTunnelArea

    sh:message "specialTunnelArea: This error is due to the Tunnel {?tunnelLabel}, violating the rule: Applicable when the Tunnel is defined and requires a reference to its special tunnel area."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:specialTunnelArea ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:StandardCombinedTransportContainers a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:standardCombinedTransportContainers ;
    era:scope "local" ;

    rdfs:comment "Coding for combined transport for containers (for all freight and mixed-traffic lines) in accordance with the specification referenced in Appendix A-1, index [B]."@en ;
    # Source: ontology comment for standardCombinedTransportContainers

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "standardCombinedTransportContainers: The CommonCharacteristicsSubset must have at most one standardCombinedTransportContainers value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:standardCombinedTransportContainers ;

    sh:severity sh:Violation .

era-sh:StandardCombinedTransportContainersApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:standardCombinedTransportContainers ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its standard combined transport containers."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to standardCombinedTransportContainers

    sh:message "standardCombinedTransportContainers: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its standard combined transport containers."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:standardCombinedTransportContainers ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:StandardCombinedTransportRollerUnits a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:standardCombinedTransportRollerUnits ;
    era:scope "local" ;

    rdfs:comment "Coding for combined transport for roller units (for all freight and mixed-traffic lines) in accordance with the specification referenced in Appendix A-1, index [B]."@en ;
    # Source: ontology comment for standardCombinedTransportRollerUnits

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Inferred: only one standard combined transport roller units value is expected based on the nature of the property

    sh:message "standardCombinedTransportRollerUnits: The CommonCharacteristicsSubset must have at most one standardCombinedTransportRollerUnits value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:standardCombinedTransportRollerUnits ;

    sh:severity sh:Violation .

era-sh:StandardCombinedTransportRollerUnitsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:standardCombinedTransportRollerUnits ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its standard combined transport roller units."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to standardCombinedTransportRollerUnits

    sh:message "standardCombinedTransportRollerUnits: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its standard combined transport roller units."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:standardCombinedTransportRollerUnits ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:StartLocation a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:startLocation ;
    era:rinfIndex "1.1.1.1.8.3" ;
    era:scope "local" ;

    rdfs:comment "Geographical coordinates in decimal degrees and km of the line at the beginning of a tunnel."@en ;
    # Source: ontology comment for startLocation

    sh:datatype xsd:string ;
    # Inferred: data presentation indicates a predefined character string format

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:pattern "^[+-]?\\d{1,3}\\.\\d{7} [+-]?\\d{1,3}\\.\\d{7} km [+-]?\\d{1,3}\\.\\d{3}$" ;
    # Inferred: pattern based on the data presentation format provided in RAG

    sh:message "startLocation: The Tunnel must have at most one startLocation value that follows the format: Latitude (NN.NNNNNNN) + Longitude (±NN.NNNNNNN) + km (±NNNN.NNN). This error may be due to having more than one value or having a value that does not conform to the expected format."@en ;

    sh:path era:startLocation ;

    sh:severity sh:Violation .

era-sh:StartLocationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:startLocation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Tunnel is defined and requires a reference to its start location."@en ;
    # Source: inferred from the definition of Tunnel and its relationship to startLocation

    sh:message "startLocation: This error is due to the Tunnel {?tunnelLabel}, violating the rule: Applicable when the Tunnel is defined and requires a reference to its start location."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:startLocation ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:StartsAt a sh:PropertyShape ;
    era:affectedClass era:NetLinearReference ;
    era:affectedProperty era:startsAt ;
    era:scope "local" ;

    rdfs:comment "Specifies the starting coordinate of a linear reference."@en ;
    # Source: ontology comment for startsAt

    sh:class era:TopologicalCoordinate ;
    # Source: ontology range is era:TopologicalCoordinate

    sh:maxCount 1 ;
    # Inferred: a linear reference should have at most one starting coordinate

    sh:message "startsAt: The NetLinearReference must have at most one startsAt value that is an instance of TopologicalCoordinate. This error may be due to having more than one value or having a value that is not an instance of TopologicalCoordinate."@en ;

    sh:path era:startsAt ;

    sh:severity sh:Violation .

era-sh:StartsAtApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:NetLinearReference ;
    era:affectedProperty era:startsAt ;
    era:scope "local" ;

    rdfs:comment "Applicable when the NetLinearReference is defined and requires a reference to its starting coordinate."@en ;
    # Source: inferred from the definition of NetLinearReference and its relationship to startsAt

    sh:message "startsAt: This error is due to the NetLinearReference {?referenceLabel}, violating the rule: Applicable when the NetLinearReference is defined and requires a reference to its starting coordinate."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?referenceLabel WHERE {
        $this rdfs:label ?referenceLabel .
        FILTER NOT EXISTS { $this era:startsAt ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:State a sh:PropertyShape ;
    era:affectedClass era:Certificate ;
    era:affectedProperty era:state ;
    era:scope "local" ;

    rdfs:comment "Denoting the state of the certificate. Can be in one of the following: Amended, New, Suspended, Withdrawn."@en ;
    # Source: ontology comment for state

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "state: The Certificate must have at most one state value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:state ;

    sh:severity sh:Violation .

era-sh:StateApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Certificate ;
    era:affectedProperty era:state ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Certificate is defined and requires a reference to its state."@en ;
    # Source: inferred from the definition of Certificate and its relationship to state

    sh:message "state: This error is due to the Certificate {?certificateLabel}, violating the rule: Applicable when the Certificate is defined and requires a reference to its state."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?certificateLabel WHERE {
        $this rdfs:label ?certificateLabel .
        FILTER NOT EXISTS { $this era:state ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SubCategory a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:subCategory ;
    era:scope "local" ;

    rdfs:comment "(Deprecated) Vehicle subcategory."@en ;
    # Source: ontology comment for subCategory

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "subCategory: The VehicleType must have at most one subCategory value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:subCategory ;

    sh:severity sh:Violation .

era-sh:SubCategoryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:subCategory ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires a reference to its subcategory."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to subCategory

    sh:message "subCategory: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the VehicleType is defined and requires a reference to its subcategory."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:subCategory ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SubsetOf a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:subsetOf ;
    era:scope "local" ;

    rdfs:comment "Relates a subset with common characteristics with another subset with common characteristics."@en ;
    # Source: ontology comment for subsetOf

    sh:class era:CommonCharacteristicsSubset ;
    # Source: ontology range is CommonCharacteristicsSubset

    sh:maxCount 1 ;
    # Inferred: a subset should have at most one subsetOf relationship based on the nature of the property

    sh:message "subsetOf: The CommonCharacteristicsSubset must have at most one subsetOf value that is an instance of CommonCharacteristicsSubset. This error may be due to having more than one value or having a value that is not an instance of CommonCharacteristicsSubset."@en ;

    sh:path era:subsetOf ;

    sh:severity sh:Violation .

era-sh:SubsetOfIrreflexivity a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:subsetOf ;
    era:scope "local" ;

    rdfs:comment "Enforces irreflexivity of subsetOf: a subset cannot be a subset of itself."@en ;
    # Source: ontology declares owl:IrreflexiveProperty

    sh:message "subsetOf: The CommonCharacteristicsSubset {?thisLabel} cannot be a subset of itself."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?thisLabel WHERE {
        $this rdfs:label ?thisLabel .
        FILTER EXISTS { $this era:subsetOf $this } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SubsidiaryLocationType a sh:PropertyShape ;
    era:affectedClass era:SubsidiaryLocation ;
    era:affectedProperty era:subsidiaryLocationType ;
    era:scope "local" ;

    rdfs:comment "Indicates the subsidiary location type that belongs to a taxonomy."@en ;
    # Source: ontology comment for subsidiaryLocationType

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "subsidiaryLocationType: The SubsidiaryLocation must have at most one subsidiaryLocationType value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:subsidiaryLocationType ;

    sh:severity sh:Violation .

era-sh:SubsidiaryLocationTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SubsidiaryLocation ;
    era:affectedProperty era:subsidiaryLocationType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the SubsidiaryLocation is defined and requires a reference to its subsidiary location type."@en ;
    # Source: inferred from the definition of SubsidiaryLocation and its relationship to subsidiaryLocationType

    sh:message "subsidiaryLocationType: This error is due to the SubsidiaryLocation {?locationLabel}, violating the rule: Applicable when the SubsidiaryLocation is defined and requires a reference to its subsidiary location type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?locationLabel WHERE {
        $this rdfs:label ?locationLabel .
        FILTER NOT EXISTS { $this era:subsidiaryLocationType ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SupportedPlatformHeight a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:supportedPlatformHeight ;
    era:scope "local" ;

    rdfs:comment "Platform height for which the vehicle is designed."@en ;
    # Source: ontology comment for supportedPlatformHeight

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "supportedPlatformHeight: The VehicleType must have at most one supportedPlatformHeight value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:supportedPlatformHeight ;

    sh:severity sh:Violation .

era-sh:SupportedPlatformHeightApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:supportedPlatformHeight ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires a reference to its supported platform height."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to supportedPlatformHeight

    sh:message "supportedPlatformHeight: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the VehicleType is defined and requires a reference to its supported platform height."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:supportedPlatformHeight ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TdsFrenchTrainDetectionSystemLimitation a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tdsFrenchTrainDetectionSystemLimitation ;
    era:scope "local" ;

    rdfs:comment "Specific for route compatibility check on French network."@en ;
    # Source: ontology comment for tdsFrenchTrainDetectionSystemLimitation

    sh:class era:FrenchTrainDetectionSystemLimitation ;
    # Source: ontology range is era:FrenchTrainDetectionSystemLimitation

    sh:maxCount 1 ;
    # Inferred: a train detection system should have at most one limitation associated with it

    sh:message "tdsFrenchTrainDetectionSystemLimitation: The TrainDetectionSystem must have at most one tdsFrenchTrainDetectionSystemLimitation value that is an instance of FrenchTrainDetectionSystemLimitation. This error may be due to having more than one value or having a value that is not an instance of FrenchTrainDetectionSystemLimitation."@en ;

    sh:path era:tdsFrenchTrainDetectionSystemLimitation ;

    sh:severity sh:Violation .

era-sh:TdsFrenchTrainDetectionSystemLimitationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tdsFrenchTrainDetectionSystemLimitation ;
    era:scope "local" ;

    rdfs:comment "Applicable only on running tracks in France."@en ;
    # Source: dependencyNote in ontology

    sh:message "tdsFrenchTrainDetectionSystemLimitation: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: Applicable only on running tracks in France."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:tdsFrenchTrainDetectionSystemLimitation ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TdsMaximumMagneticField a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tdsMaximumMagneticField ;
    era:rinfIndex "1.1.1.3.4.2.3" ;
    era:scope "local" ;

    rdfs:comment "The maximum magnetic field limits allowed for axle counters (in dB µA/m) for a defined frequency band. It should be provided in 3 directions."@en ;
    # Source: ontology comment for tdsMaximumMagneticField

    sh:class era:MaximumMagneticField ;
    # Source: ontology range is MaximumMagneticField

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "tdsMaximumMagneticField: The TrainDetectionSystem must have at most one tdsMaximumMagneticField value that is an instance of MaximumMagneticField. This error may be due to having more than one value or having a value that is not an instance of MaximumMagneticField."@en ;

    sh:path era:tdsMaximumMagneticField ;

    sh:severity sh:Violation .

era-sh:TdsMaximumMagneticFieldApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tdsMaximumMagneticField ;
    era:scope "local" ;

    rdfs:comment "Applicable for train detection systems based on axle counters."@en ;
    # Source: dependencyNote in ontology

    sh:message "tdsMaximumMagneticField: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: Applicable for train detection systems based on axle counters."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:tdsMaximumMagneticField ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TdsMinAxleLoadVehicleCategory a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tdsMinAxleLoadVehicleCategory ;
    era:rinfIndex "1.1.1.3.7.11.1" ;
    era:scope "local" ;

    rdfs:comment "Indication of load given in tons depending on the category of vehicle. Deprecated according to the amendment to the Regulation (EU) 2019/777."@en ;
    # Source: ontology comment for tdsMinAxleLoadVehicleCategory

    sh:class era:MinAxleLoadVehicleCategory ;
    # Source: ontology range is MinAxleLoadVehicleCategory

    sh:maxCount 1 ;
    # Inferred: only one minimum axle load vehicle category value is expected based on the nature of the property

    sh:message "tdsMinAxleLoadVehicleCategory: The TrainDetectionSystem must have at most one tdsMinAxleLoadVehicleCategory value that is an instance of MinAxleLoadVehicleCategory. This error may be due to having more than one value or having a value that is not an instance of MinAxleLoadVehicleCategory."@en ;

    sh:path era:tdsMinAxleLoadVehicleCategory ;

    sh:severity sh:Violation .

era-sh:TdsMinAxleLoadVehicleCategoryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tdsMinAxleLoadVehicleCategory ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the selected option for parameter 1.1.1.3.7.1.1 is ‘track circuit’ or ‘wheel detector’."@en ;
    # Source: RAG explanation on applicability

    sh:message "tdsMinAxleLoadVehicleCategory: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: Applicable only when the selected option for parameter 1.1.1.3.7.1.1 is ‘track circuit’ or ‘wheel detector’."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:tdsMinAxleLoadVehicleCategory ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?param1 era:trackCircuit ?value . }
        UNION
        FILTER NOT EXISTS { ?param1 era:wheelDetector ?value . }
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TemperatureRange a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:temperatureRange ;
    era:rinfIndex "1.1.1.1.2.6" ;
    era:scope "local" ;

    rdfs:comment "Temperature range for unrestricted access to the line according to European standard."@en ;
    # Source: ontology comment for temperatureRange

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:in (era:T1 era:T2 era:T3 era:Tx) ;
    # Source: RAG specifies predefined list of temperature ranges

    sh:message "temperatureRange: The CommonCharacteristicsSubset must have at most one temperatureRange value that is a Concept from the predefined list (T1, T2, T3, Tx). This error may be due to having more than one value or having a value that is not in the allowed range."@en ;

    sh:path era:temperatureRange ;

    sh:severity sh:Violation .

era-sh:TemperatureRangeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:temperatureRange ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its temperature range."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to temperatureRange

    sh:message "temperatureRange: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its temperature range."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:temperatureRange ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TenClassification a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:tenClassification ;
    era:scope "local" ;

    rdfs:comment "Indication of the part of the trans-European network the infrastructure element belongs to."@en ;
    # Source: ontology comment for tenClassification

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Inferred: a classification should have at most one value based on the nature of the property

    sh:message "tenClassification: The CommonCharacteristicsSubset must have at most one tenClassification value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:tenClassification ;

    sh:severity sh:Violation .

era-sh:TenClassificationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:tenClassification ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its TEN classification."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to tenClassification

    sh:message "tenClassification: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its TEN classification."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:tenClassification ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ThermalCapacityTSIReference a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:thermalCapacityTSIReference ;
    era:scope "local" ;

    rdfs:comment "Reference case of a TSI thermal capacity."@en ;
    # Source: ontology comment for thermalCapacityTSIReference

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "thermalCapacityTSIReference: The VehicleType must have at most one thermalCapacityTSIReference value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:thermalCapacityTSIReference ;

    sh:severity sh:Violation .

era-sh:ThermalCapacityTSIReferenceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:thermalCapacityTSIReference ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and requires a reference to its thermal capacity TSI."@en ;
    # Source: inferred from the definition of VehicleType and its relationship to thermalCapacityTSIReference

    sh:message "thermalCapacityTSIReference: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the VehicleType is defined and requires a reference to its thermal capacity TSI."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:thermalCapacityTSIReference ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Track a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:track ;
    era:scope "local" ;

    rdfs:comment "Reference to a related railway track."@en ;
    # Source: ontology comment for track

    sh:class era:RunningTrack ;
    # Source: ontology range is RunningTrack

    sh:maxCount 1 ;
    # Inferred: a reference to a track should have at most one value based on the nature of the property

    sh:message "track: The OperationalPoint must have at most one track value that is an instance of RunningTrack. This error may be due to having more than one value or having a value that is not an instance of RunningTrack."@en ;

    sh:path era:track ;

    sh:severity sh:Violation .

era-sh:TrackApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:track ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OperationalPoint is defined and requires a reference to its associated track."@en ;
    # Source: inferred from the definition of OperationalPoint and its relationship to track

    sh:message "track: This error is due to the OperationalPoint {?pointLabel}, violating the rule: Applicable when the OperationalPoint is defined and requires a reference to its associated track."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?pointLabel WHERE {
        $this rdfs:label ?pointLabel .
        FILTER NOT EXISTS { $this era:track ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TrackDirection a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:trackDirection ;
    era:scope "local" ;

    rdfs:comment "The normal running direction is: N - same direction as in SoL, O - opposite direction to as in SoL, B - both directions."@en ;
    # Source: ontology comment for trackDirection

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:in (era:N era:O era:B) ;
    # Source: RAG specifies predefined list of values for track direction

    sh:message "trackDirection: The RunningTrack must have at most one trackDirection value that is a Concept from the predefined list (N, O, B). This error may be due to having more than one value or having a value that is not in the allowed range."@en ;

    sh:path era:trackDirection ;

    sh:severity sh:Violation .

era-sh:TrackDirectionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:trackDirection ;
    era:scope "local" ;

    rdfs:comment "Applicable when the RunningTrack is defined and requires a reference to its normal running direction."@en ;
    # Source: inferred from the definition of RunningTrack and its relationship to trackDirection

    sh:message "trackDirection: This error is due to the RunningTrack {?trackLabel}, violating the rule: Applicable when the RunningTrack is defined and requires a reference to its normal running direction."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:trackDirection ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TrackLoadCapability a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:trackLoadCapability ;
    era:rinfIndex "1.1.1.1.2.4" ;
    era:scope "local" ;

    rdfs:comment "A combination of the line category and speed at the weakest point of the track."@en ;
    # Source: ontology comment for trackLoadCapability

    sh:class era:LoadCapability ;
    # Source: ontology range is LoadCapability

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "trackLoadCapability: The RunningTrack must have at most one trackLoadCapability value that is an instance of LoadCapability. This error may be due to having more than one value or having a value that is not an instance of LoadCapability."@en ;

    sh:path era:trackLoadCapability ;

    sh:severity sh:Violation .

era-sh:TrackLoadCapabilityApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:trackLoadCapability ;
    era:scope "local" ;

    rdfs:comment "Applicable when the RunningTrack is defined and requires a reference to its load capability."@en ;
    # Source: inferred from the definition of RunningTrack and its relationship to trackLoadCapability

    sh:message "trackLoadCapability: This error is due to the RunningTrack {?trackLabel}, violating the rule: Applicable when the RunningTrack is defined and requires a reference to its load capability."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:trackLoadCapability ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TrackPhaseInfo a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:trackPhaseInfo ;
    era:scope "local" ;

    rdfs:comment "Indication of required several information on phase separation."@en ;
    # Source: ontology comment for trackPhaseInfo

    sh:class era:PhaseInfo ;
    # Source: ontology range is PhaseInfo

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "trackPhaseInfo: The CommonCharacteristicsSubset must have at most one trackPhaseInfo value that is an instance of PhaseInfo. This error may be due to having more than one value or having a value that is not an instance of PhaseInfo."@en ;

    sh:path era:trackPhaseInfo ;

    sh:severity sh:Violation .

era-sh:TrackPhaseInfoApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:trackPhaseInfo ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Phase separation parameter is defined and requires a reference to its phase separation information."@en ;
    # Source: inferred from the dependencyNote in ontology

    sh:message "trackPhaseInfo: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the Phase separation parameter is defined and requires a reference to its phase separation information."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:trackPhaseInfo ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { $this era:phaseSeparation "Y" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TrackRaisedPantographsDistanceAndSpeed a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:trackRaisedPantographsDistanceAndSpeed ;
    era:rinfIndex "1.1.1.2.3.3" ;
    era:scope "local" ;

    rdfs:comment "Indication of maximum number of raised pantographs per train allowed and minimum spacing centre line to centre line of adjacent pantograph heads, expressed in metres, at the given speed."@en ;
    # Source: ontology comment for trackRaisedPantographsDistanceAndSpeed

    sh:class era:RaisedPantographsDistanceAndSpeed ;
    # Source: ontology range is RaisedPantographsDistanceAndSpeed

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "trackRaisedPantographsDistanceAndSpeed: The CommonCharacteristicsSubset must have at most one trackRaisedPantographsDistanceAndSpeed value that is an instance of RaisedPantographsDistanceAndSpeed. This error may be due to having more than one value or having a value that is not an instance of RaisedPantographsDistanceAndSpeed."@en ;

    sh:path era:trackRaisedPantographsDistanceAndSpeed ;

    sh:severity sh:Violation .

era-sh:TrackRaisedPantographsDistanceAndSpeedApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:trackRaisedPantographsDistanceAndSpeed ;
    era:scope "local" ;

    rdfs:comment "Applicable only if 'Overhead contact line (OCL)' is selected in the relevant parameter."@en ;
    # Source: dependencyNote in ontology

    sh:message "trackRaisedPantographsDistanceAndSpeed: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only if 'Overhead contact line (OCL)' is selected."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:trackRaisedPantographsDistanceAndSpeed ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?param1 era:overheadContactLine "Y" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TrackSystemSeparationInfo a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:trackSystemSeparationInfo ;
    era:rinfIndex "1.1.1.2.4.2.2" ;
    era:scope "local" ;

    rdfs:comment "Indication of required several information on system separation."@en ;
    # Source: ontology comment for trackSystemSeparationInfo

    sh:class era:SystemSeparationInfo ;
    # Source: ontology range is SystemSeparationInfo

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "trackSystemSeparationInfo: The CommonCharacteristicsSubset must have at most one trackSystemSeparationInfo value that is an instance of SystemSeparationInfo. This error may be due to having more than one value or having a value that is not an instance of SystemSeparationInfo."@en ;

    sh:path era:trackSystemSeparationInfo ;

    sh:severity sh:Violation .

era-sh:TrackSystemSeparationInfoApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:trackSystemSeparationInfo ;
    era:scope "local" ;

    rdfs:comment "Mandatory if there is an indication of existence of system separation."@en ;
    # Source: dependencyNote in ontology

    sh:message "trackSystemSeparationInfo: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Mandatory if there is an indication of existence of system separation."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:trackSystemSeparationInfo ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?param1 era:systemSeparation "Y" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TracksideHABD a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:tracksideHabd ;
    era:scope "local" ;

    rdfs:comment "Link between the running track and the HABD, covering the RINF parameters 1.1.1.1.7.(5-9)."@en ;
    # Source: ontology comment for tracksideHabd

    sh:class era:HABD ;
    # Source: ontology range is HABD

    sh:maxCount 1 ;
    # Inferred: only one HABD reference is expected based on the nature of the property

    sh:message "tracksideHabd: The RunningTrack must have at most one tracksideHabd value that is an instance of HABD. This error may be due to having more than one value or having a value that is not an instance of HABD."@en ;

    sh:path era:tracksideHabd ;

    sh:severity sh:Violation .

era-sh:TracksideHABDApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:tracksideHabd ;
    era:scope "local" ;

    rdfs:comment "Applicable when the RunningTrack is defined and requires a reference to its associated trackside HABD."@en ;
    # Source: inferred from the definition of RunningTrack and its relationship to tracksideHabd

    sh:message "tracksideHabd: This error is due to the RunningTrack {?trackLabel}, violating the rule: Applicable when the RunningTrack is defined and requires a reference to its associated trackside HABD."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:tracksideHabd ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TrainDetectionSystem a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:trainDetectionSystem ;
    era:scope "local" ;

    rdfs:comment "Technical characteristics of the train detection systems installed in the section of line."@en ;
    # Source: ontology comment for trainDetectionSystem

    sh:class era:TrainDetectionSystem ;
    # Source: ontology range is TrainDetectionSystem

    sh:maxCount 1 ;
    # Inferred: only one train detection system reference is expected based on the nature of the property

    sh:message "trainDetectionSystem: The CommonCharacteristicsSubset must have at most one trainDetectionSystem value that is an instance of TrainDetectionSystem. This error may be due to having more than one value or having a value that is not an instance of TrainDetectionSystem."@en ;

    sh:path era:trainDetectionSystem ;

    sh:severity sh:Violation .

era-sh:TrainDetectionSystemApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:trainDetectionSystem ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its train detection system."@en ;
    # Source: inferred from the definition of CommonCharacteristicsSubset and its relationship to trainDetectionSystem

    sh:message "trainDetectionSystem: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable when the CommonCharacteristicsSubset is defined and requires a reference to its train detection system."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:trainDetectionSystem ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TrainDetectionSystemSpecificCheck a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:trainDetectionSystemSpecificCheck ;
    era:scope "local" ;

    rdfs:comment "Reference to the technical specification of train detection system, in accordance with the specification referenced in Appendix A-1, index [D]."@en ;
    # Source: ontology comment for trainDetectionSystemSpecificCheck

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Inferred: only one specific check reference is expected based on the nature of the property

    sh:message "trainDetectionSystemSpecificCheck: The TrainDetectionSystem must have at most one trainDetectionSystemSpecificCheck value that is an instance of Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:trainDetectionSystemSpecificCheck ;

    sh:severity sh:Violation .

era-sh:TrainDetectionSystemSpecificCheckApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:trainDetectionSystemSpecificCheck ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the parameter 'Type of train detection system' is applicable."@en ;
    # Source: dependencyNote in ontology

    sh:message "trainDetectionSystemSpecificCheck: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: Applicable only when the parameter 'Type of train detection system' is applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:trainDetectionSystemSpecificCheck ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?param1 era:trainDetectionSystem "Y" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TrainDetectionSystemSpecificCheckDocument a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:trainDetectionSystemSpecificCheckDocument ;
    era:scope "local" ;

    rdfs:comment "Electronic document from the IM stored by the Agency with precise values in accordance with TSI CCS Article 13 for the specific check to be performed for train detection systems."@en ;
    # Source: ontology comment for trainDetectionSystemSpecificCheckDocument

    sh:class era:Document ;
    # Source: ontology range is Document

    sh:maxCount 1 ;
    # Source: ontology declares owl:FunctionalProperty, indicating only one value allowed

    sh:message "trainDetectionSystemSpecificCheckDocument: The TrainDetectionSystem must have at most one trainDetectionSystemSpecificCheckDocument value that is an instance of Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ;

    sh:path era:trainDetectionSystemSpecificCheckDocument ;

    sh:severity sh:Violation .

era-sh:TrainDetectionSystemSpecificCheckDocumentApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:trainDetectionSystemSpecificCheckDocument ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the parameter 'Type of train detection system' is applicable."@en ;
    # Source: dependencyNote in ontology

    sh:message "trainDetectionSystemSpecificCheckDocument: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: Applicable only when the parameter 'Type of train detection system' is applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:trainDetectionSystemSpecificCheckDocument ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?param1 era:trainDetectionSystem "Y" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TrainDetectionSystemType a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:trainDetectionSystemType ;
    era:scope "local" ;

    rdfs:comment "Indication of types of train detection systems installed."@en ;
    # Source: ontology comment for trainDetectionSystemType

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:in (era:CPO_MultipleRequired era:CCD_TSITrainDetection) ;
    # Source: RAG specifies predefined list of values for train detection system types

    sh:message "trainDetectionSystemType: The TrainDetectionSystem must have at most one trainDetectionSystemType value that is a Concept from the predefined list (CPO_MultipleRequired, CCD_TSITrainDetection). This error may be due to having more than one value or having a value that is not in the allowed range."@en ;

    sh:path era:trainDetectionSystemType ;

    sh:severity sh:Violation .

era-sh:TrainDetectionSystemTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:trainDetectionSystemType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the TrainDetectionSystem is defined and requires a reference to its type."@en ;
    # Source: inferred from the definition of TrainDetectionSystem and its relationship to trainDetectionSystemType

    sh:message "trainDetectionSystemType: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: Applicable when the TrainDetectionSystem is defined and requires a reference to its type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:trainDetectionSystemType ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TSICompliantCompositeBrakeBlocks a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantCompositeBrakeBlocks ;
    era:rinfIndex "1.1.1.3.7.21" ;
    era:scope "local" ;

    rdfs:comment "Indication whether rules are compliant with the TSI."@en ;
    # Source: RAG definition for TSI compliance of rules on composite brake blocks

    sh:in (era:TSI_compliant era:Not_TSI_compliant) ;
    # Source: RAG specifies predefined list of values for TSI compliance

    sh:maxCount 1 ;
    # Source: inferred from the nature of the property, only one compliance status expected

    sh:message "tsiCompliantCompositeBrakeBlocks: The TrainDetectionSystem must have at most one tsiCompliantCompositeBrakeBlocks value that is either TSI compliant or Not TSI compliant. This error may be due to having more than one value or having a value that is not in the allowed range."@en ;

    sh:path era:tsiCompliantCompositeBrakeBlocks ;

    sh:severity sh:Violation .

era-sh:TSICompliantCompositeBrakeBlocksApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantCompositeBrakeBlocks ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the parameter 1.1.1.3.7.1.1 is defined and the selected option is 'track circuit'."@en ;
    # Source: RAG explanation on applicability

    sh:message "tsiCompliantCompositeBrakeBlocks: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: Applicable only when the parameter 1.1.1.3.7.1.1 is defined and the selected option is 'track circuit'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:tsiCompliantCompositeBrakeBlocks ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?param1 era:trainDetectionType "track circuit" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TSICompliantFerromagneticWheel a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantFerromagneticWheel ;
    era:rinfIndex "1.1.1.3.7.14" ;
    era:scope "local" ;

    rdfs:comment "Indication whether rules are compliant with the TSI regarding the ferromagnetic characteristics of wheel material."@en ;
    # Source: ontology comment for tsiCompliantFerromagneticWheel

    sh:in (era:TSI_compliant era:Not_TSI_compliant) ;
    # Source: RAG specifies predefined list of values for TSI compliance

    sh:maxCount 1 ;
    # Source: inferred from the nature of the property, only one compliance status expected

    sh:message "tsiCompliantFerromagneticWheel: The TrainDetectionSystem must have at most one tsiCompliantFerromagneticWheel value that is either TSI compliant or Not TSI compliant. This error may be due to having more than one value or having a value that is not in the allowed range."@en ;

    sh:path era:tsiCompliantFerromagneticWheel ;

    sh:severity sh:Violation .

era-sh:TSICompliantFerromagneticWheelApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantFerromagneticWheel ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the parameter 1.1.1.3.7.1.1 is defined and the selected option is 'wheel detector'."@en ;
    # Source: RAG explanation on applicability

    sh:message "tsiCompliantFerromagneticWheel: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: Applicable only when the parameter 1.1.1.3.7.1.1 is defined and the selected option is 'wheel detector'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:tsiCompliantFerromagneticWheel ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?param1 era:trainDetectionType "wheel detector" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TSICompliantMaxDistConsecutiveAxles a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantMaxDistConsecutiveAxles ;
    era:rinfIndex "1.1.1.3.7.2.1" ;
    era:scope "local" ;

    rdfs:comment "Indication whether required distance between two consecutive axles is compliant with the TSI. Deprecated according to the amendment to the Regulation (EU) 2019/777."@en ;
    # Source: ontology comment for tsiCompliantMaxDistConsecutiveAxles

    sh:in (era:TSI_compliant era:Not_TSI_compliant) ;
    # Source: RAG specifies predefined list of values for TSI compliance

    sh:maxCount 1 ;
    # Inferred: only one compliance status expected based on the nature of the property

    sh:message "tsiCompliantMaxDistConsecutiveAxles: The TrainDetectionSystem must have at most one tsiCompliantMaxDistConsecutiveAxles value that is either TSI compliant or Not TSI compliant. This error may be due to having more than one value or having a value that is not in the allowed range."@en ;

    sh:path era:tsiCompliantMaxDistConsecutiveAxles ;

    sh:severity sh:Violation .

era-sh:TSICompliantMaxDistConsecutiveAxlesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantMaxDistConsecutiveAxles ;
    era:scope "local" ;

    rdfs:comment "Applicable when the parameter 1.1.1.3.7.2.1 is defined and the selected option is 'Not TSI compliant'."@en ;
    # Source: RAG explanation on applicability

    sh:message "tsiCompliantMaxDistConsecutiveAxles: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: Applicable when the parameter 1.1.1.3.7.2.1 is defined and the selected option is 'Not TSI compliant'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:tsiCompliantMaxDistConsecutiveAxles ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?param1 era:tsiCompliance "Not TSI compliant" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TSICompliantMaxImpedanceWheelset a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantMaxImpedanceWheelset ;
    era:rinfIndex "1.1.1.3.7.15.1" ;
    era:scope "local" ;

    rdfs:comment "TSI compliance of maximum permitted impedance between opposite wheels of a wheelset. Deprecated according to the amendment to the Regulation (EU) 2019/777."@en ;
    # Source: ontology comment for tsiCompliantMaxImpedanceWheelset

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Inferred: only one compliance status expected based on the nature of the property

    sh:in (era:TSI_compliant era:Not_TSI_compliant) ;
    # Source: RAG specifies predefined list of values for TSI compliance

    sh:message "tsiCompliantMaxImpedanceWheelset: The TrainDetectionSystem must have at most one tsiCompliantMaxImpedanceWheelset value that is either TSI compliant or Not TSI compliant. This error may be due to having more than one value or having a value that is not in the allowed range."@en ;

    sh:path era:tsiCompliantMaxImpedanceWheelset ;

    sh:severity sh:Violation .

era-sh:TSICompliantMaxImpedanceWheelsetApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantMaxImpedanceWheelset ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the parameter regarding impedance compliance is defined."@en ;
    # Source: inferred from the context of TSI compliance

    sh:message "tsiCompliantMaxImpedanceWheelset: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: Applicable only when the parameter regarding impedance compliance is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:tsiCompliantMaxImpedanceWheelset ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?param1 era:impedanceCompliance "Y" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TSICompliantMetalConstruction a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantMetalConstruction ;
    era:rinfIndex "1.1.1.3.7.13" ;
    era:scope "local" ;

    rdfs:comment "Indication whether rules are compliant with the TSI regarding vehicle metal construction."@en ;
    # Source: ontology comment for tsiCompliantMetalConstruction

    sh:in (era:TSI_compliant era:Not_TSI_compliant) ;
    # Source: RAG specifies predefined list of values for TSI compliance

    sh:maxCount 1 ;
    # Inferred: only one compliance status expected based on the nature of the property

    sh:message "tsiCompliantMetalConstruction: The TrainDetectionSystem must have at most one tsiCompliantMetalConstruction value that is either TSI compliant or Not TSI compliant. This error may be due to having more than one value or having a value that is not in the allowed range."@en ;

    sh:path era:tsiCompliantMetalConstruction ;

    sh:severity sh:Violation .

era-sh:TSICompliantMetalConstructionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantMetalConstruction ;
    era:scope "local" ;

    rdfs:comment "Applicable only when for parameter 1.1.1.3.7.1.1 the selected option is 'loop'."@en ;
    # Source: RAG explanation on applicability

    sh:message "tsiCompliantMetalConstruction: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: Applicable only when for parameter 1.1.1.3.7.1.1 the selected option is 'loop'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:tsiCompliantMetalConstruction ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?param1 era:trainDetectionType "loop" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TSICompliantMetalFreeSpace a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantMetalFreeSpace ;
    era:rinfIndex "1.1.1.3.7.12" ;
    era:scope "local" ;

    rdfs:comment "Indication whether rules are compliant with the TSI regarding metal-free space around wheels."@en ;
    # Source: ontology comment for tsiCompliantMetalFreeSpace

    sh:in (era:TSI_compliant era:Not_TSI_compliant) ;
    # Source: RAG specifies predefined list of values for TSI compliance

    sh:maxCount 1 ;
    # Inferred: only one compliance status expected based on the nature of the property

    sh:message "tsiCompliantMetalFreeSpace: The TrainDetectionSystem must have at most one tsiCompliantMetalFreeSpace value that is either TSI compliant or Not TSI compliant. This error may be due to having more than one value or having a value that is not in the allowed range."@en ;

    sh:path era:tsiCompliantMetalFreeSpace ;

    sh:severity sh:Violation .

era-sh:TSICompliantMetalFreeSpaceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantMetalFreeSpace ;
    era:scope "local" ;

    rdfs:comment "Applicable only when for parameter 1.1.1.3.7.1.1 the selected option is 'wheel detector'."@en ;
    # Source: RAG explanation on applicability

    sh:message "tsiCompliantMetalFreeSpace: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: Applicable only when for parameter 1.1.1.3.7.1.1 the selected option is 'wheel detector'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:tsiCompliantMetalFreeSpace ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?param1 era:trainDetectionType "wheel detector" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TSICompliantRSTShuntImpedance a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantRSTShuntImpedance ;
    era:rinfIndex "1.1.1.3.7.23" ;
    era:scope "local" ;

    rdfs:comment "Indication whether rules are compliant with the TSI regarding the combination of RST characteristics influencing shunting impedance."@en ;
    # Source: ontology comment for tsiCompliantRSTShuntImpedance

    sh:in (era:TSI_compliant era:Not_TSI_compliant) ;
    # Source: RAG specifies predefined list of values for TSI compliance

    sh:maxCount 1 ;
    # Inferred: only one compliance status expected based on the nature of the property

    sh:message "tsiCompliantRSTShuntImpedance: The TrainDetectionSystem must have at most one tsiCompliantRSTShuntImpedance value that is either TSI compliant or Not TSI compliant. This error may be due to having more than one value or having a value that is not in the allowed range."@en ;

    sh:path era:tsiCompliantRSTShuntImpedance ;

    sh:severity sh:Violation .

era-sh:TSICompliantRSTShuntImpedanceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantRSTShuntImpedance ;
    era:scope "local" ;

    rdfs:comment "Applicable only when for parameter 1.1.1.3.7.1.1 the selected option is 'track circuit'."@en ;
    # Source: RAG explanation on applicability

    sh:message "tsiCompliantRSTShuntImpedance: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: Applicable only when for parameter 1.1.1.3.7.1.1 the selected option is 'track circuit'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:tsiCompliantRSTShuntImpedance ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?param1 era:trainDetectionType "track circuit" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TSICompliantSandCharacteristics a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantSandCharacteristics ;
    era:rinfIndex "1.1.1.3.7.19" ;
    era:scope "local" ;

    rdfs:comment "TSI Compliance of rules on sand characteristics. Deprecated according to the amendment to the Regulation (EU) 2019/777."@en ;
    # Source: ontology comment for tsiCompliantSandCharacteristics

    sh:in (era:TSI_compliant era:Not_TSI_compliant) ;
    # Source: RAG specifies predefined list of values for TSI compliance

    sh:maxCount 1 ;
    # Inferred: only one compliance status expected based on the nature of the property

    sh:message "tsiCompliantSandCharacteristics: The TrainDetectionSystem must have at most one tsiCompliantSandCharacteristics value that is either TSI compliant or Not TSI compliant. This error may be due to having more than one value or having a value that is not in the allowed range."@en ;

    sh:path era:tsiCompliantSandCharacteristics ;

    sh:severity sh:Violation .

era-sh:TSICompliantSandCharacteristicsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantSandCharacteristics ;
    era:scope "local" ;

    rdfs:comment "Applicable only if the nominal track gauge corresponds to 1520 mm."@en ;
    # Source: RAG specifies applicability condition for TSI compliance of sand characteristics

    sh:message "tsiCompliantSandCharacteristics: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: Applicable only if the nominal track gauge is 1520 mm."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:tsiCompliantSandCharacteristics ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?gauge era:nominalTrackGauge ?gaugeValue . FILTER(?gaugeValue != 1520) } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TSICompliantSanding a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantSanding ;
    era:rinfIndex "1.1.1.3.7.16" ;
    era:scope "local" ;

    rdfs:comment "TSI compliance of sanding rules to allow compatibility with track circuits. Too much sand brings the risk of not detecting trains in tracks equipped with track circuits. Deprecated according to the amendment to the Regulation (EU) 2019/777."@en ;
    # Source: ontology comment for tsiCompliantSanding

    sh:in (era:TSI_compliant era:Not_TSI_compliant) ;
    # Source: RAG specifies predefined list of values for TSI compliance

    sh:maxCount 1 ;
    # Inferred: only one compliance status expected based on the nature of the property

    sh:message "tsiCompliantSanding: The TrainDetectionSystem must have at most one tsiCompliantSanding value that is either TSI compliant or Not TSI compliant. This error may be due to having more than one value or having a value that is not in the allowed range."@en ;

    sh:path era:tsiCompliantSanding ;

    sh:severity sh:Violation .

era-sh:TSICompliantSandingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantSanding ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the parameter regarding sanding compliance is defined."@en ;
    # Source: inferred from the context of TSI compliance

    sh:message "tsiCompliantSanding: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: Applicable only when the parameter regarding sanding compliance is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:tsiCompliantSanding ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?param1 era:sandingCompliance "Y" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TSICompliantShuntDevices a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantShuntDevices ;
    era:rinfIndex "1.1.1.3.7.22" ;
    era:scope "local" ;

    rdfs:comment "Indication whether rules are compliant with the TSI regarding shunt assisting devices."@en ;
    # Source: ontology comment for tsiCompliantShuntDevices

    sh:in (era:TSI_compliant era:Not_TSI_compliant) ;
    # Source: RAG specifies predefined list of values for TSI compliance

    sh:maxCount 1 ;
    # Inferred: only one compliance status expected based on the nature of the property

    sh:message "tsiCompliantShuntDevices: The TrainDetectionSystem must have at most one tsiCompliantShuntDevices value that is either TSI compliant or Not TSI compliant. This error may be due to having more than one value or having a value that is not in the allowed range."@en ;

    sh:path era:tsiCompliantShuntDevices ;

    sh:severity sh:Violation .

era-sh:TSICompliantShuntDevicesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:tsiCompliantShuntDevices ;
    era:scope "local" ;

    rdfs:comment "Applicable only when for parameter 1.1.1.3.7.1.1 the selected option is 'track circuit'."@en ;
    # Source: RAG explanation on applicability

    sh:message "tsiCompliantShuntDevices: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: Applicable only when for parameter 1.1.1.3.7.1.1 the selected option is 'track circuit'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:tsiCompliantShuntDevices ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?param1 era:trainDetectionType "track circuit" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TSIPantographHead a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:tsiPantographHead ;
    era:rinfIndex "1.1.1.2.3.1" ;
    era:scope "local" ;

    rdfs:comment "Indication of TSI compliant pantograph heads which are allowed to be used."@en ;
    # Source: ontology comment for tsiPantographHead

    sh:in (era:EPA_TSIHeads era:EPA_OtherHeads) ;
    # Source: RAG specifies predefined list of values for TSI compliant pantograph heads

    sh:maxCount 1 ;
    # Inferred: only one pantograph head type expected based on the nature of the property

    sh:message "tsiPantographHead: The CommonCharacteristicsSubset must have at most one tsiPantographHead value that is either EPA_TSIHeads or EPA_OtherHeads. This error may be due to having more than one value or having a value that is not in the allowed range."@en ;

    sh:path era:tsiPantographHead ;

    sh:severity sh:Violation .

era-sh:TSIPantographHeadApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:tsiPantographHead ;
    era:scope "local" ;

    rdfs:comment "Applicable only if 'Overhead contact line (OCL)' is selected for parameter 1.1.1.2.2.1.1."@en ;
    # Source: RAG explanation on applicability

    sh:message "tsiPantographHead: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only if 'Overhead contact line (OCL)' is selected."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:tsiPantographHead ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?param1 era:overheadContactLine "N" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TunnelDocRef a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:tunnelDocRef ;
    era:rinfIndex "1.1.1.1.8.8.2" ;
    era:scope "local" ;

    rdfs:comment "Electronic document available from the IM stored by the Agency with precise description of the clearance gauge and geometry of the tunnel."@en ;
    # Source: ontology comment for tunnelDocRef

    sh:class era:Document ;
    # Source: ontology range is era:Document (rdfs:range)

    sh:maxCount 1 ;
    # Inferred: only one document reference expected based on the nature of the property

    sh:message "tunnelDocRef: The Tunnel must have at most one tunnelDocRef value that is a Document. This error may be due to having more than one value or having a value that is not an instance of Document."@en ;

    sh:path era:tunnelDocRef ;

    sh:severity sh:Violation .

era-sh:TunnelDocRefApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:tunnelDocRef ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the document reference is provided in characterstring format."@en ;
    # Source: RAG explanation on applicability

    sh:message "tunnelDocRef: This error is due to the Tunnel {?tunnelLabel}, violating the rule: Applicable only when the document reference is provided."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:tunnelDocRef ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { $this era:tunnelDocRef ?docRef . FILTER(!isLiteral(?docRef)) } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TypeVersionId a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:typeVersionId ;
    era:scope "local" ;

    rdfs:comment "Indicates the registrationVersion with Ids 10, 20 and 30 indicating registration according to Directive 2008/57/EC and implementing Decision 2011/665/EU, and 40 and 50 indicating registration according to regime described in Directive 2008/57/EC."@en ;
    # Source: ontology comment for typeVersionId

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Inferred: only one version ID expected based on the nature of the property

    sh:message "typeVersionId: The VehicleType must have at most one typeVersionId value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:typeVersionId ;

    sh:severity sh:Violation .

era-sh:TypeVersionIdApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:typeVersionId ;
    era:scope "local" ;

    rdfs:comment "Applicable when the typeVersionId corresponds to the defined SKOS Concept Scheme."@en ;
    # Source: inferred from the ontology and comment

    sh:message "typeVersionId: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the typeVersionId corresponds to the defined SKOS Concept Scheme."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:typeVersionId ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?value a <http://www.w3.org/2004/02/skos/core#Concept> } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Uopid a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:uopid ;
    era:rinfIndex "1.2.0.0.0.2" ;
    era:scope "local" ;

    rdfs:comment "Code composed of country code and alphanumeric operational point code."@en ;
    # Source: ontology comment for uopid

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string

    sh:maxCount 1 ;
    # Inferred: only one unique OP ID expected based on the nature of the property

    sh:pattern "^[A-Z]{2}[A-Za-z0-9_-]{1,10}$" ;
    # Inferred: pattern based on RAG explanation for valid OP ID format

    sh:message "uopid: The OperationalPoint must have at most one uopid value that matches the format 'AA+AAAAAAAAAA'. This error may be due to having more than one value or having a value that does not conform to the required format."@en ;

    sh:path era:uopid ;

    sh:severity sh:Violation .

era-sh:UopidApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:uopid ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the OP ID is defined according to the specified format."@en ;
    # Source: inferred from the context of uopid definition

    sh:message "uopid: This error is due to the OperationalPoint {?opLabel}, violating the rule: Applicable only when the OP ID is defined according to the specified format."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?opLabel WHERE {
        $this rdfs:label ?opLabel .
        FILTER NOT EXISTS { $this era:uopid ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?value a xsd:string . FILTER(!regex(?value, "^[A-Z]{2}[A-Za-z0-9_-]{1,10}$")) } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Validity a sh:PropertyShape ;
    era:affectedClass era:Feature ;
    era:affectedProperty era:validity ;
    era:scope "local" ;

    rdfs:comment "Relates a feature with a temporal feature to indicate a validity period."@en ;
    # Source: ontology comment for validity

    sh:class era:TemporalFeature ;
    # Source: ontology range is era:TemporalFeature (rdfs:range)

    sh:maxCount 1 ;
    # Inferred: only one validity reference expected based on the nature of the property

    sh:message "validity: The Feature must have at most one validity value that is a TemporalFeature. This error may be due to having more than one value or having a value that is not an instance of TemporalFeature."@en ;

    sh:path era:validity ;

    sh:severity sh:Violation .

era-sh:ValidityApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Feature ;
    era:affectedProperty era:validity ;
    era:scope "local" ;

    rdfs:comment "Applicable for Operational Point, Section of Line, Track, Siding, Tunnel, and Platform."@en ;
    # Source: RAG specifies applicable objects for validity

    sh:message "validity: This error is due to the Feature {?featureLabel}, violating the rule: Applicable only for Operational Point, Section of Line, Track, Siding, Tunnel, and Platform."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?featureLabel WHERE {
        $this rdfs:label ?featureLabel .
        FILTER NOT EXISTS { $this era:validity ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { 
            { $this a era:OperationalPoint } UNION
            { $this a era:SectionOfLine } UNION
            { $this a era:Track } UNION
            { $this a era:Siding } UNION
            { $this a era:Tunnel } UNION
            { $this a era:Platform }
        } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehicleCategory a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehicleCategory ;
    era:scope "local" ;

    rdfs:comment "Vehicle category."@en ;
    # Source: ontology comment for vehicleCategory

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Inferred: only one category expected based on the nature of the property

    sh:message "vehicleCategory: The VehicleType must have at most one vehicleCategory value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:vehicleCategory ;

    sh:severity sh:Violation .

era-sh:VehicleCategoryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehicleCategory ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle category corresponds to the defined SKOS Concept Scheme."@en ;
    # Source: inferred from the ontology and comment

    sh:message "vehicleCategory: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the vehicle category corresponds to the defined SKOS Concept Scheme."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:vehicleCategory ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?value a <http://www.w3.org/2004/02/skos/core#Concept> } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehicleKeeper a sh:PropertyShape ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:vehicleKeeper ;
    era:scope "local" ;

    rdfs:comment "Indicates the organization that owns/operated a vehicle or wagon."@en ;
    # Source: ontology comment for vehicleKeeper

    sh:class era:OrganisationRole ;
    # Source: ontology range is era:OrganisationRole (rdfs:range)

    sh:maxCount 1 ;
    # Inferred: only one vehicle keeper expected based on the nature of ownership

    sh:message "vehicleKeeper: The Vehicle must have at most one vehicleKeeper value that is an OrganisationRole. This error may be due to having more than one value or having a value that is not an instance of OrganisationRole."@en ;

    sh:path era:vehicleKeeper ;

    sh:severity sh:Violation .

era-sh:VehicleKeeperApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:vehicleKeeper ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle is associated with an organization that operates or owns it."@en ;
    # Source: inferred from the context of vehicle ownership

    sh:message "vehicleKeeper: This error is due to the Vehicle {?vehicleLabel}, violating the rule: Applicable only when the vehicle is associated with an organization."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:vehicleKeeper ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?org era:operates ?this } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehicleSubCategory a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehicleSubCategory ;
    era:scope "local" ;

    rdfs:comment "Vehicle subcategory."@en ;
    # Source: ontology comment for vehicleSubCategory

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Inferred: only one subcategory expected based on the nature of the property

    sh:message "vehicleSubCategory: The VehicleType must have at most one vehicleSubCategory value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:vehicleSubCategory ;

    sh:severity sh:Violation .

era-sh:VehicleSubCategoryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehicleSubCategory ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle subcategory corresponds to the defined SKOS Concept Scheme."@en ;
    # Source: inferred from the ontology and comment

    sh:message "vehicleSubCategory: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the vehicle subcategory corresponds to the defined SKOS Concept Scheme."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:vehicleSubCategory ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?value a <http://www.w3.org/2004/02/skos/core#Concept> } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehicleType a sh:PropertyShape ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:vehicleType ;
    era:scope "local" ;

    rdfs:comment "Indicates the vehicle type of a specific vehicle or wagon."@en ;
    # Source: ontology comment for vehicleType

    sh:class era:VehicleType ;
    # Source: ontology range is era:VehicleType (rdfs:range)

    sh:maxCount 1 ;
    # Inferred: only one vehicle type expected based on the nature of the property

    sh:message "vehicleType: The Vehicle must have at most one vehicleType value that is a VehicleType. This error may be due to having more than one value or having a value that is not an instance of VehicleType."@en ;

    sh:path era:vehicleType ;

    sh:severity sh:Violation .

era-sh:VehicleTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:vehicleType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type corresponds to the defined VehicleType."@en ;
    # Source: inferred from the ontology and comment

    sh:message "vehicleType: This error is due to the Vehicle {?vehicleLabel}, violating the rule: Applicable when the vehicle type corresponds to the defined VehicleType."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:vehicleType ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?value a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehicleTypeMaximumSpeedAndCantDeficiency a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehicleTypeMaximumSpeedAndCantDeficiency ;
    era:rinfIndex "4.6.4" ;
    era:scope "local" ;

    rdfs:comment "Relates the vehicle type with its values of maximum speed and cant deficiency."@en ;
    # Source: ontology comment for vehicleTypeMaximumSpeedAndCantDeficiency

    sh:class era:MaximumSpeedAndCantDeficiency ;
    # Source: ontology range is era:MaximumSpeedAndCantDeficiency (rdfs:range)

    sh:maxCount 1 ;
    # Inferred: only one maximum speed and cant deficiency expected based on the nature of the property

    sh:message "vehicleTypeMaximumSpeedAndCantDeficiency: The VehicleType must have at most one vehicleTypeMaximumSpeedAndCantDeficiency value that is an instance of MaximumSpeedAndCantDeficiency. This error may be due to having more than one value or having a value that is not an instance of MaximumSpeedAndCantDeficiency."@en ;

    sh:path era:vehicleTypeMaximumSpeedAndCantDeficiency ;

    sh:severity sh:Violation .

era-sh:VehicleTypeMaximumSpeedAndCantDeficiencyApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehicleTypeMaximumSpeedAndCantDeficiency ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is defined with maximum speed and cant deficiency characteristics."@en ;
    # Source: inferred from the context of vehicle type characteristics

    sh:message "vehicleTypeMaximumSpeedAndCantDeficiency: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the vehicle type is defined with maximum speed and cant deficiency characteristics."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:vehicleTypeMaximumSpeedAndCantDeficiency ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehicleTypesCompatibleTrafficLoad a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:vehicleTypesCompatibleTrafficLoad ;
    era:rinfIndex "1.1.1.5.1" ;
    era:scope "local" ;

    rdfs:comment "The infrastructure managers shall provide through RINF the information to the RU regarding the list of vehicle types compatible with the route for which they have already verified compatibility for parameter Traffic load and load carrying capacity of infrastructure and train detection systems."@en ;
    # Source: ontology comment for vehicleTypesCompatibleTrafficLoad

    sh:in (xsd:string) ;
    # Inferred: the property is expected to have values that are strings representing compatible vehicle types

    sh:maxCount 1 ;
    # Inferred: only one compatible traffic load value expected based on the nature of the property

    sh:message "vehicleTypesCompatibleTrafficLoad: The CommonCharacteristicsSubset must have at most one vehicleTypesCompatibleTrafficLoad value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:vehicleTypesCompatibleTrafficLoad ;

    sh:severity sh:Violation .

era-sh:VehicleTypesCompatibleTrafficLoadApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:vehicleTypesCompatibleTrafficLoad ;
    era:scope "local" ;

    rdfs:comment "Applicable when the infrastructure manager has verified compatibility for the parameter Traffic load and load carrying capacity."@en ;
    # Source: inferred from the context of vehicleTypesCompatibleTrafficLoad

    sh:message "vehicleTypesCompatibleTrafficLoad: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when compatibility for Traffic load and load carrying capacity has been verified."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:vehicleTypesCompatibleTrafficLoad ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?this a era:CommonCharacteristicsSubset } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehiclesCompatibleTrafficLoad a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:vehiclesCompatibleTrafficLoad ;
    era:rinfIndex "1.1.1.5.2" ;
    era:scope "local" ;

    rdfs:comment "The infrastructure managers shall provide through RINF the information or a document to the RU regarding the list of vehicles compatible with the route for which they have already verified compatibility for parameter Traffic load and load carrying capacity of infrastructure and train detection systems."@en ;
    # Source: ontology comment for vehiclesCompatibleTrafficLoad

    sh:in (xsd:string) ;
    # Inferred: the property is expected to have values that are strings representing compatible vehicle types

    sh:maxCount 1 ;
    # Inferred: only one compatible traffic load value expected based on the nature of the property

    sh:message "vehiclesCompatibleTrafficLoad: The CommonCharacteristicsSubset must have at most one vehiclesCompatibleTrafficLoad value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:vehiclesCompatibleTrafficLoad ;

    sh:severity sh:Violation .

era-sh:VehiclesCompatibleTrafficLoadApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:vehiclesCompatibleTrafficLoad ;
    era:scope "local" ;

    rdfs:comment "Applicable when the infrastructure manager has verified compatibility for the parameter Traffic load and load carrying capacity."@en ;
    # Source: inferred from the context of vehiclesCompatibleTrafficLoad

    sh:message "vehiclesCompatibleTrafficLoad: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when compatibility for Traffic load and load carrying capacity has been verified."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:vehiclesCompatibleTrafficLoad ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?this a era:CommonCharacteristicsSubset } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VoiceGSMRNetwork a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:voiceGSMRNetwork ;
    era:scope "local" ;

    rdfs:comment "Voice SIM Card GSM-R Home Network. The allowed values for this property belong to the SKOS Concept Scheme http://data.europa.eu/949/concepts/gsmr-networks/GSMRNetworks."@en ;
    # Source: ontology comment for voiceGSMRNetwork

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Inferred: only one voice GSM-R network expected based on the nature of the property

    sh:message "voiceGSMRNetwork: The VehicleType must have at most one voiceGSMRNetwork value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:voiceGSMRNetwork ;

    sh:severity sh:Violation .

era-sh:VoiceGSMRNetworkApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:voiceGSMRNetwork ;
    era:scope "local" ;

    rdfs:comment "Applicable when the voiceGSMRNetwork corresponds to the defined SKOS Concept Scheme."@en ;
    # Source: inferred from the ontology and comment

    sh:message "voiceGSMRNetwork: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the voiceGSMRNetwork corresponds to the defined SKOS Concept Scheme."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:voiceGSMRNetwork ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?value a <http://www.w3.org/2004/02/skos/core#Concept> } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VoiceRadioCompatible a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:voiceRadioCompatible ;
    era:rinfIndex "4.13.2.5" ;
    era:scope "local" ;

    rdfs:comment "Radio requirements used for demonstrating technical compatibility voice."@en ;
    # Source: ontology comment for voiceRadioCompatible

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Inferred: only one voice radio compatibility expected based on the nature of the property

    sh:message "voiceRadioCompatible: The VehicleType must have at most one voiceRadioCompatible value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:voiceRadioCompatible ;

    sh:severity sh:Violation .

era-sh:VoiceRadioCompatibleApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:voiceRadioCompatible ;
    era:scope "local" ;

    rdfs:comment "Applicable when the voice radio compatibility corresponds to the defined SKOS Concept Scheme."@en ;
    # Source: inferred from the ontology and comment

    sh:message "voiceRadioCompatible: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the voice radio compatibility corresponds to the defined SKOS Concept Scheme."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:voiceRadioCompatible ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?value a <http://www.w3.org/2004/02/skos/core#Concept> } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:WheelSetGauge a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:wheelSetGauge ;
    era:rinfIndex "4.1.3" ;
    era:scope "local" ;

    rdfs:comment "A single value expressed in millimetres that identifies the track gauge."@en ;
    # Source: ontology comment for wheelSetGauge

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Inferred: only one gauge value expected based on the nature of the property

    sh:message "wheelSetGauge: The VehicleType must have at most one wheelSetGauge value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:wheelSetGauge ;

    sh:severity sh:Violation .

era-sh:WheelSetGaugeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:wheelSetGauge ;
    era:scope "local" ;

    rdfs:comment "Applicable when the wheelSetGauge corresponds to the defined SKOS Concept Scheme."@en ;
    # Source: inferred from the ontology and comment

    sh:message "wheelSetGauge: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the wheelSetGauge corresponds to the defined SKOS Concept Scheme."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:wheelSetGauge ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?value a <http://www.w3.org/2004/02/skos/core#Concept> } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:WheelSetGaugeChangeoverFacility a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:wheelSetGaugeChangeoverFacility ;
    era:rinfIndex "4.1.11" ;
    era:scope "local" ;

    rdfs:comment "Wheelset gauge changeover facility. The allowed values for this property belong to the SKOS Concept Scheme http://data.europa.eu/949/concepts/gauge-changeover-facilities/GaugeChangeoverFacilities."@en ;
    # Source: ontology comment for wheelSetGaugeChangeoverFacility

    sh:class <http://www.w3.org/2004/02/skos/core#Concept> ;
    # Source: ontology range is skos:Concept

    sh:maxCount 1 ;
    # Inferred: only one gauge changeover facility expected based on the nature of the property

    sh:message "wheelSetGaugeChangeoverFacility: The VehicleType must have at most one wheelSetGaugeChangeoverFacility value that is a Concept. This error may be due to having more than one value or having a value that is not an instance of Concept."@en ;

    sh:path era:wheelSetGaugeChangeoverFacility ;

    sh:severity sh:Violation .

era-sh:WheelSetGaugeChangeoverFacilityApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:wheelSetGaugeChangeoverFacility ;
    era:scope "local" ;

    rdfs:comment "Applicable when the wheelSetGaugeChangeoverFacility corresponds to the defined SKOS Concept Scheme."@en ;
    # Source: inferred from the ontology and comment

    sh:message "wheelSetGaugeChangeoverFacility: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the wheelSetGaugeChangeoverFacility corresponds to the defined SKOS Concept Scheme."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:wheelSetGaugeChangeoverFacility ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?value a <http://www.w3.org/2004/02/skos/core#Concept> } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AlternativeName a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:alternativeName ;
    era:scope "local" ;

    rdfs:comment "Alternative name of a vehicle type."@en ;
    # Source: ontology comment for alternativeName

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one alternative name expected based on the nature of the property

    sh:message "alternativeName: The VehicleType must have at most one alternativeName value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:alternativeName ;

    sh:severity sh:Violation .

era-sh:AlternativeNameApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:alternativeName ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type has an alternative name defined."@en ;
    # Source: inferred from the context of vehicle type characteristics

    sh:message "alternativeName: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the vehicle type has an alternative name defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:alternativeName ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AltitudeRange a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:altitudeRange ;
    era:rinfIndex "4.3.2" ;
    era:scope "local" ;

    rdfs:comment "Altitude range."@en ;
    # Source: ontology comment for altitudeRange

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one altitude range expected based on the nature of the property

    sh:message "altitudeRange: The VehicleType must have at most one altitudeRange value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:altitudeRange ;

    sh:severity sh:Violation .

era-sh:AltitudeRangeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:altitudeRange ;
    era:scope "local" ;

    rdfs:comment "Applicable when the altitude range corresponds to the defined characteristics of the vehicle type."@en ;
    # Source: inferred from the context of altitude range in vehicle characteristics

    sh:message "altitudeRange: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the altitude range corresponds to the defined characteristics of the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:altitudeRange ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AltitudeRangeDetail a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:altitudeRangeDetail ;
    era:rinfIndex "4.3.2.1" ;
    era:scope "local" ;

    rdfs:comment "Altitude range value for 'X' if value 'AX' is selected in altitude range."@en ;
    # Source: ontology comment for altitudeRangeDetail

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one altitude range detail expected based on the nature of the property

    sh:message "altitudeRangeDetail: The VehicleType must have at most one altitudeRangeDetail value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:altitudeRangeDetail ;

    sh:severity sh:Violation .

era-sh:AltitudeRangeDetailApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:altitudeRangeDetail ;
    era:scope "local" ;

    rdfs:comment "Applicable when the altitude range detail corresponds to the defined characteristics of the vehicle type."@en ;
    # Source: inferred from the context of altitude range detail in vehicle characteristics

    sh:message "altitudeRangeDetail: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the altitude range detail corresponds to the defined characteristics of the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:altitudeRangeDetail ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AppliesInBothDirections a sh:PropertyShape ;
    era:affectedClass era:LinearElementSection ;
    era:affectedProperty era:appliesInBothDirections ;
    era:scope "local" ;

    rdfs:comment "Specifies if the section applies in both directions (optional)."@en ;
    # Source: ontology comment for appliesInBothDirections

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one appliesInBothDirections value expected based on the nature of the property

    sh:message "appliesInBothDirections: The LinearElementSection must have at most one appliesInBothDirections value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:appliesInBothDirections ;

    sh:severity sh:Violation .

era-sh:AppliesInBothDirectionsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LinearElementSection ;
    era:affectedProperty era:appliesInBothDirections ;
    era:scope "local" ;

    rdfs:comment "Applicable when the LinearElementSection is defined with directional characteristics."@en ;
    # Source: inferred from the context of directional properties in linear elements

    sh:message "appliesInBothDirections: This error is due to the LinearElementSection {?sectionLabel}, violating the rule: Applicable only when the section is defined with directional characteristics."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sectionLabel WHERE {
        $this rdfs:label ?sectionLabel .
        FILTER NOT EXISTS { $this era:appliesInBothDirections ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?this a era:LinearElementSection } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AreaBoardingAid a sh:PropertyShape ;
    era:affectedClass era:PlatformEdge ;
    era:affectedProperty era:areaBoardingAid ;
    era:rinfIndex "1.2.1.0.6.7" ;
    era:scope "local" ;

    rdfs:comment "Information of the train access level for which the boarding aid can be used."@en ;
    # Source: ontology comment for areaBoardingAid

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one area boarding aid value expected based on the nature of the property

    sh:message "areaBoardingAid: The PlatformEdge must have at most one areaBoardingAid value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:areaBoardingAid ;

    sh:severity sh:Violation .

era-sh:AreaBoardingAidApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PlatformEdge ;
    era:affectedProperty era:areaBoardingAid ;
    era:scope "local" ;

    rdfs:comment "Applicable when the platform is equipped with a boarding aid."@en ;
    # Source: inferred from the context of areaBoardingAid

    sh:message "areaBoardingAid: This error is due to the PlatformEdge {?platformLabel}, violating the rule: Applicable only when the platform is equipped with a boarding aid."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?platformLabel WHERE {
        $this rdfs:label ?platformLabel .
        FILTER NOT EXISTS { $this era:areaBoardingAid ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { $this era:applicable "Y" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:AssistanceStartingTrain a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:assistanceStartingTrain ; 
    era:rinfIndex "1.2.1.0.6.6" ; 
    era:scope "local" ; 

    rdfs:comment "Indication of existence of equipment or staff supporting the train crew in starting the train."@en ; 
    # Source: ontology comment for assistanceStartingTrain

    sh:datatype xsd:boolean ; 
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ; 
    # Inferred: only one assistance starting train value expected based on the nature of the property

    sh:message "assistanceStartingTrain: The CommonCharacteristicsSubset must have at most one assistanceStartingTrain value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ; 

    sh:path era:assistanceStartingTrain ; 

    sh:severity sh:Violation .

era-sh:AssistanceStartingTrainApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:assistanceStartingTrain ; 
    era:scope "local" ; 

    rdfs:comment "Applicable when there is equipment or staff supporting the train crew in starting the train."@en ; 
    # Source: inferred from the context of assistanceStartingTrain

    sh:message "assistanceStartingTrain: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when there is equipment or staff supporting the train crew in starting the train."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:assistanceStartingTrain ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?this a era:CommonCharacteristicsSubset } .
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:AutomaticDroppingDeviceRequired a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:automaticDroppingDeviceRequired ; 
    era:rinfIndex "1.1.1.2.5.3" ; 
    era:scope "local" ; 

    rdfs:comment "Indication of whether an automatic dropping device (ADD) is required on the vehicle."@en ; 
    # Source: ontology comment for automaticDroppingDeviceRequired

    sh:datatype xsd:boolean ; 
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ; 
    # Inferred: only one automatic dropping device required value expected based on the nature of the property

    sh:message "automaticDroppingDeviceRequired: The CommonCharacteristicsSubset must have at most one automaticDroppingDeviceRequired value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ; 

    sh:path era:automaticDroppingDeviceRequired ; 

    sh:severity sh:Violation .

era-sh:AutomaticDroppingDeviceRequiredApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:automaticDroppingDeviceRequired ; 
    era:scope "local" ; 

    rdfs:comment "Applicable when the value 'Overhead contact line (OCL)' is selected for parameter 'Type of contact line system'."@en ; 
    # Source: RAG explanation on applicability

    sh:message "automaticDroppingDeviceRequired: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only if 'Overhead contact line (OCL)' is selected for 'Type of contact line system'."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:automaticDroppingDeviceRequired ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?this era:typeOfContactLineSystem "Overhead contact line (OCL)" } .
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:AxleSpacing a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:axleSpacing ;
    era:rinfIndex "4.5.3.4" ;
    era:scope "local" ;

    rdfs:comment "Position of the axles along the unit. a: Distance between axles; b: Distance from end axle to the end of the nearest coupling plane; c: distance between two inside axles."@en ;
    # Source: ontology comment for axleSpacing

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one axle spacing value expected based on the nature of the property

    sh:message "axleSpacing: The VehicleType must have at most one axleSpacing value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:axleSpacing ;

    sh:severity sh:Violation .

era-sh:AxleSpacingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:axleSpacing ;
    era:scope "local" ;

    rdfs:comment "Applicable when the axle spacing corresponds to the defined characteristics of the vehicle type."@en ;
    # Source: inferred from the context of axle spacing in vehicle characteristics

    sh:message "axleSpacing: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the axle spacing corresponds to the defined characteristics of the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:axleSpacing ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:BigMetalMass a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:bigMetalMass ;
    era:rinfIndex "1.1.1.3.2.18" ;
    era:scope "local" ;

    rdfs:comment "Indication of existence of metal mass in the vicinity of the location, susceptible of perturbating the reading of balises by the on-board system."@en ;
    # Source: ontology comment for bigMetalMass

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "bigMetalMass: The CommonCharacteristicsSubset must have at most one bigMetalMass value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:bigMetalMass ;

    sh:severity sh:Violation .

era-sh:BigMetalMassApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:bigMetalMass ;
    era:scope "local" ;

    rdfs:comment "Applicable when ETCS is present and a value is provided for bigMetalMass."@en ;
    # Source: inferred from the dependency note in the ontology

    sh:message "bigMetalMass: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when ETCS is present and a value is provided."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:bigMetalMass ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { $this era:etcsLevelType ?etcsValue } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:BoardingAids a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:boardingAids ;
    era:rinfIndex "4.12.3.2" ;
    era:scope "local" ;

    rdfs:comment "Description of any integrated boarding aids (if provided)."@en ;
    # Source: ontology comment for boardingAids

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one boarding aids description expected based on the nature of the property

    sh:message "boardingAids: The VehicleType must have at most one boardingAids value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:boardingAids ;

    sh:severity sh:Violation .

era-sh:BoardingAidsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:boardingAids ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type has integrated boarding aids defined."@en ;
    # Source: inferred from the context of boarding aids in vehicle characteristics

    sh:message "boardingAids: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the vehicle type has integrated boarding aids defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:boardingAids ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:BorderPointId a sh:PropertyShape ;
    era:affectedClass era:ReferenceBorderPoint ;
    era:affectedProperty era:borderPointId ;
    era:scope "local" ;

    rdfs:comment "Border point identification in the list of reference border points in the RINF application guide."@en ;
    # Source: ontology comment for borderPointId

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one border point ID expected based on the nature of the property

    sh:message "borderPointId: The ReferenceBorderPoint must have at most one borderPointId value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:borderPointId ;

    sh:severity sh:Violation .

era-sh:BorderPointIdApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ReferenceBorderPoint ;
    era:affectedProperty era:borderPointId ;
    era:scope "local" ;

    rdfs:comment "Applicable when the reference border point has a border point ID defined."@en ;
    # Source: inferred from the context of border point identification

    sh:message "borderPointId: This error is due to the ReferenceBorderPoint {?borderLabel}, violating the rule: Applicable only when the reference border point has a border point ID defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?borderLabel WHERE {
        $this rdfs:label ?borderLabel .
        FILTER NOT EXISTS { $this era:borderPointId ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:BrakeWeightPercentage a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:brakeWeightPercentage ;
    era:rinfIndex "4.7.6" ;
    era:scope "local" ;

    rdfs:comment "Brake weight percentage (lambda) or Braked mass."@en ;
    # Source: ontology comment for brakeWeightPercentage

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one brake weight percentage expected based on the nature of the property

    sh:message "brakeWeightPercentage: The VehicleType must have at most one brakeWeightPercentage value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:brakeWeightPercentage ;

    sh:severity sh:Violation .

era-sh:BrakeWeightPercentageApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:brakeWeightPercentage ;
    era:scope "local" ;

    rdfs:comment "Applicable when the brake weight percentage corresponds to the defined characteristics of the vehicle type."@en ;
    # Source: inferred from the context of brake weight percentage in vehicle characteristics

    sh:message "brakeWeightPercentage: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the brake weight percentage corresponds to the defined characteristics of the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:brakeWeightPercentage ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .


era-sh:CantDeficiencyApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:cantDefficiency ;
    era:scope "local" ;

    rdfs:comment "Applicable when the cant deficiency corresponds to the defined characteristics of the vehicle type."@en ;
    # Source: inferred from the context of cant deficiency in vehicle characteristics

    sh:message "cantDefficiency: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable when the cant deficiency corresponds to the defined characteristics of the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:cantDefficiency ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:CantDeficiency a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:cantDeficiency ;
    era:rinfIndex "1.1.1.1.4.2" ;
    era:scope "local" ;

    rdfs:comment "Maximum cant deficiency expressed in millimetres defined as difference between the applied cant and a higher equilibrium cant the line has been designed for."@en ;
    # Source: ontology comment for cantDeficiency

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one cant deficiency value expected based on the nature of the property

    sh:message "cantDeficiency: The RunningTrack must have at most one cantDeficiency value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:cantDeficiency ;

    sh:severity sh:Violation .

era-sh:CantDeficiencyApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:cantDeficiency ;
    era:scope "local" ;

    rdfs:comment "Applicable when the cant deficiency corresponds to the defined characteristics of the running track."@en ;
    # Source: inferred from the context of cant deficiency in track characteristics

    sh:message "cantDeficiency: This error is due to the RunningTrack {?trackLabel}, violating the rule: Applicable when the cant deficiency corresponds to the defined characteristics of the running track."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:cantDeficiency ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?this a era:RunningTrack } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:CatenaryMaxRatedCurrent a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:catenaryMaxRatedCurrent ;
    era:rinfIndex "4.10.3" ;
    era:scope "local" ;

    rdfs:comment "Maximum rated current from the catenary (to be indicated for each electrical energy supply system the vehicle is equipped for), given in A."@en ;
    # Source: ontology comment for catenaryMaxRatedCurrent

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one maximum rated current value expected based on the nature of the property

    sh:message "catenaryMaxRatedCurrent: The VehicleType must have at most one catenaryMaxRatedCurrent value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:catenaryMaxRatedCurrent ;

    sh:severity sh:Violation .

era-sh:CatenaryMaxRatedCurrentApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:catenaryMaxRatedCurrent ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is equipped for an electrical energy supply system."@en ;
    # Source: inferred from the context of catenaryMaxRatedCurrent

    sh:message "catenaryMaxRatedCurrent: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the vehicle type is equipped for an electrical energy supply system."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:catenaryMaxRatedCurrent ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ComplianceInfTsi a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:complianceInfTsi ;
    era:rinfIndex "1.1.1.1.8.8.1" ;
    era:scope "local" ;

    rdfs:comment "Compliance of the tunnel with TSI INF at the maximum permitted speed."@en ;
    # Source: ontology comment for complianceInfTsi

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one complianceInfTsi value expected based on the nature of the property

    sh:message "complianceInfTsi: The Tunnel must have at most one complianceInfTsi value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:complianceInfTsi ;

    sh:severity sh:Violation .

era-sh:ComplianceInfTsiApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:complianceInfTsi ;
    era:scope "local" ;

    rdfs:comment "Mandatory if the speed of the line is equal or greater than 200km/h."@en ;
    # Source: dependency note in the ontology

    sh:message "complianceInfTsi: This error is due to the Tunnel {?tunnelLabel}, violating the rule: Applicable only if the speed of the line is equal or greater than 200 km/h."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:complianceInfTsi ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?this era:maximumPermittedSpeed ?speed . FILTER(xsd:integer(?speed) < 200) } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:CompositeBrakeBlockRetrofitted a sh:PropertyShape ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:compositeBrakeBlockRetrofitted ;
    era:scope "local" ;

    rdfs:comment "Indicates if a vehicle's composite brake block is retrofitted."@en ;
    # Source: ontology comment for compositeBrakeBlockRetrofitted

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one retrofitted value expected based on the nature of the property

    sh:message "compositeBrakeBlockRetrofitted: The Vehicle must have at most one compositeBrakeBlockRetrofitted value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:compositeBrakeBlockRetrofitted ;

    sh:severity sh:Violation .

era-sh:CompositeBrakeBlockRetrofittedApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:compositeBrakeBlockRetrofitted ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is equipped with composite brake blocks."@en ;
    # Source: inferred from the context of composite brake blocks in vehicle characteristics

    sh:message "compositeBrakeBlockRetrofitted: This error is due to the Vehicle {?vehicleLabel}, violating the rule: Applicable only when the vehicle type is equipped with composite brake blocks."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:compositeBrakeBlockRetrofitted ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?this a era:Vehicle . FILTER NOT EXISTS { ?this era:compositeBrakeBlock ?blockValue . FILTER(?blockValue = "composite") } } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ConditionsChargingElectricEnergyStorage a sh:PropertyShape ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:conditionsChargingElectricEnergyStorage ;
    era:rinfIndex "1.2.1.0.7.2" ;
    era:scope "local" ;

    rdfs:comment "Conditions set by IMs according to a standardised document."@en ;
    # Source: ontology comment for conditionsChargingElectricEnergyStorage

    sh:datatype xsd:anyURI ;
    # Source: ontology range is xsd:anyURI (http://www.w3.org/2001/XMLSchema#anyURI)

    sh:maxCount 1 ;
    # Inferred: only one conditions value expected based on the nature of the property

    sh:message "conditionsChargingElectricEnergyStorage: The ContactLineSystem must have at most one conditionsChargingElectricEnergyStorage value that is a valid URI. This error may be due to having more than one value or having a value that is not a valid URI."@en ;

    sh:path era:conditionsChargingElectricEnergyStorage ;

    sh:severity sh:Violation .

era-sh:ConditionsChargingElectricEnergyStorageApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:conditionsChargingElectricEnergyStorage ;
    era:scope "local" ;

    rdfs:comment "Applicable when the conditions for charging electric energy storage are defined according to the standardised document."@en ;
    # Source: inferred from the context of conditions for charging electric energy storage

    sh:message "conditionsChargingElectricEnergyStorage: This error is due to the ContactLineSystem {?contactLineLabel}, violating the rule: Applicable only when conditions for charging electric energy storage are defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?contactLineLabel WHERE {
        $this rdfs:label ?contactLineLabel .
        FILTER NOT EXISTS { $this era:conditionsChargingElectricEnergyStorage ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ConditionsSwitchTrainProtectionSystems a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:conditionsSwitchTrainProtectionSystems ; 
    era:rinfIndex "1.1.1.3.8.1" ; 
    era:scope "local" ; 

    rdfs:comment "Conditions to switch over between different class B train protection, control and warning systems."@en ; 
    # Source: ontology comment for conditionsSwitchTrainProtectionSystems

    sh:datatype xsd:string ; 
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ; 
    # Inferred: only one conditions value expected based on the nature of the property

    sh:message "conditionsSwitchTrainProtectionSystems: The CommonCharacteristicsSubset must have at most one conditionsSwitchTrainProtectionSystems value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ; 

    sh:path era:conditionsSwitchTrainProtectionSystems ; 

    sh:severity sh:Violation .

era-sh:ConditionsSwitchTrainProtectionSystemsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:conditionsSwitchTrainProtectionSystems ; 
    era:scope "local" ; 

    rdfs:comment "Applicable when at least two different protection, control and warning systems exist."@en ; 
    # Source: dependency note in the ontology

    sh:message "conditionsSwitchTrainProtectionSystems: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when at least two different protection, control and warning systems exist."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:conditionsSwitchTrainProtectionSystems ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { $this era:protectionControlWarningSystems ?systemCount . FILTER (COUNT(?systemCount) < 2) } .
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:ConditionsTrainFormation a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:conditionsTrainFormation ;
    era:rinfIndex "4.1.4" ;
    era:scope "local" ;

    rdfs:comment "Conditions of use regarding train formation."@en ;
    # Source: ontology comment for conditionsTrainFormation

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one conditions description expected based on the nature of the property

    sh:message "conditionsTrainFormation: The VehicleType must have at most one conditionsTrainFormation value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:conditionsTrainFormation ;

    sh:severity sh:Violation .

era-sh:ConditionsTrainFormationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:conditionsTrainFormation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type has specific conditions defined for train formation."@en ;
    # Source: inferred from the context of conditions for train formation in vehicle characteristics

    sh:message "conditionsTrainFormation: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when specific conditions for train formation are defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:conditionsTrainFormation ?value } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ContactStripMaterialMetallicContent a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:contactStripMaterialMetallicContent ;
    era:rinfIndex "1.1.1.2.3.4" ;
    era:scope "local" ;

    rdfs:comment "Indication of max. percentage of contact strip material Impregnated Carbon permitted to be used."@en ;
    # Source: ontology comment for contactStripMaterialMetallicContent

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one metallic content value expected based on the nature of the property

    sh:message "contactStripMaterialMetallicContent: The CommonCharacteristicsSubset must have at most one contactStripMaterialMetallicContent value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:contactStripMaterialMetallicContent ;

    sh:severity sh:Violation .

era-sh:ContactStripMaterialMetallicContentApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:contactStripMaterialMetallicContent ;
    era:scope "local" ;

    rdfs:comment "Mandatory only if impregnated carbon ([NN] % of metallic content) is selected in 1.1.1.2.3.4."@en ;
    # Source: dependency note in the ontology

    sh:message "contactStripMaterialMetallicContent: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only if impregnated carbon is selected as the contact strip material."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:contactStripMaterialMetallicContent ?value } .
        FILTER EXISTS { $this era:contactStripMaterial "impregnated carbon" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ContainerHandlingFlag a sh:PropertyShape ;
    era:affectedClass era:PrimaryLocation ;
    era:affectedProperty era:containerHandlingFlag ;
    era:scope "local" ;

    rdfs:comment "Indicates whether container handling is applicable at the primary location."@en ;
    # Source: ontology comment for containerHandlingFlag

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one container handling flag expected based on the nature of the property

    sh:message "containerHandlingFlag: The PrimaryLocation must have at most one containerHandlingFlag value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:containerHandlingFlag ;

    sh:severity sh:Violation .

era-sh:ContainerHandlingFlagApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PrimaryLocation ;
    era:affectedProperty era:containerHandlingFlag ;
    era:scope "local" ;

    rdfs:comment "Applicable when the primary location is designated for container handling."@en ;
    # Source: inferred from the context of container handling in primary location characteristics

    sh:message "containerHandlingFlag: This error is due to the PrimaryLocation {?locationLabel}, violating the rule: Applicable only when the primary location is designated for container handling."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?locationLabel WHERE {
        $this rdfs:label ?locationLabel .
        FILTER NOT EXISTS { $this era:containerHandlingFlag ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?this a era:PrimaryLocation } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:CrossSectionArea a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:crossSectionArea ;
    era:rinfIndex "1.1.1.1.8.8" ;
    era:scope "local" ;

    rdfs:comment "Smallest cross section area in square metres of the tunnel."@en ;
    # Source: ontology comment for crossSectionArea

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one cross section area value expected based on the nature of the property

    sh:message "crossSectionArea: The Tunnel must have at most one crossSectionArea value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:crossSectionArea ;

    sh:severity sh:Violation .

era-sh:CrossSectionAreaApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:crossSectionArea ;
    era:scope "local" ;

    rdfs:comment "Mandatory if the speed of the line is equal or greater than 200km/h."@en ;
    # Source: dependency note in the ontology

    sh:message "crossSectionArea: This error is due to the Tunnel {?tunnelLabel}, violating the rule: Applicable only if the speed of the line is equal or greater than 200 km/h."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:crossSectionArea ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { ?this era:maximumPermittedSpeed ?speed . FILTER(xsd:integer(?speed) < 200) } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:CurrentLimitationRequired a sh:PropertyShape ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:currentLimitationRequired ;
    era:rinfIndex "1.1.1.2.5.1" ;
    era:scope "local" ;

    rdfs:comment "Indication of whether an on board current or power limitation function on vehicles is required."@en ;
    # Source: ontology comment for currentLimitationRequired

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one current limitation required value expected based on the nature of the property

    sh:message "currentLimitationRequired: The ContactLineSystem must have at most one currentLimitationRequired value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:currentLimitationRequired ;

    sh:severity sh:Violation .

era-sh:CurrentLimitationRequiredApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:currentLimitationRequired ;
    era:scope "local" ;

    rdfs:comment "Applicable when the contact line system is electrified."@en ;
    # Source: dependency note in the ontology

    sh:message "currentLimitationRequired: This error is due to the ContactLineSystem {?contactLineLabel}, violating the rule: Applicable only when the contact line system is electrified."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?contactLineLabel WHERE {
        $this rdfs:label ?contactLineLabel .
        FILTER NOT EXISTS { $this era:currentLimitationRequired ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:typeOfContactLineSystem ?type . FILTER(?type != "not electrified") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:CurrentlyValid a sh:PropertyShape ;
    era:affectedClass era:Feature ;
    era:affectedProperty era:currentlyValid ;
    era:scope "local" ;

    rdfs:comment "Indicates whether the feature is currently valid."@en ;
    # Source: ontology comment for currentlyValid

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one currently valid value expected based on the nature of the property

    sh:message "currentlyValid: The Feature must have at most one currentlyValid value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:currentlyValid ;

    sh:severity sh:Violation .

era-sh:CurrentlyValidApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Feature ;
    era:affectedProperty era:currentlyValid ;
    era:scope "local" ;

    rdfs:comment "Applicable when the feature has a defined validity period."@en ;
    # Source: inferred from the RAG context regarding validity dates

    sh:message "currentlyValid: This error is due to the Feature {?featureLabel}, violating the rule: Applicable only when the feature has a defined validity period."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?featureLabel WHERE {
        $this rdfs:label ?featureLabel .
        FILTER NOT EXISTS { $this era:currentlyValid ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { $this era:validityPeriod ?period } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DNvovtrp a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:dNvovtrp ;
    era:rinfIndex "1.1.1.3.2.16.5" ;
    era:scope "local" ;

    rdfs:comment "Maximum distance for overriding the train trip in metres, according to the specification referenced in Appendix A-1, index [C]."@en ;
    # Source: ontology comment for dNvovtrp

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one dNvovtrp value expected based on the nature of the property

    sh:message "dNvovtrp: The CommonCharacteristicsSubset must have at most one dNvovtrp value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:dNvovtrp ;

    sh:severity sh:Violation .

era-sh:DNvovtrpApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:dNvovtrp ;
    era:scope "local" ;

    rdfs:comment "A value has to be provided when ETCS is present."@en ;
    # Source: dependency note in the ontology

    sh:message "dNvovtrp: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A value must be provided when ETCS is present."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:dNvovtrp ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:etcsPresent ?etcsValue . FILTER(?etcsValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:dNvpotrp a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:dNvpotrp ;
    era:rinfIndex "1.1.1.3.2.16.7" ;
    era:scope "local" ;

    rdfs:comment "Maximum distance for reversing in Post Trip mode in metres, according to the specification referenced in Appendix A-1, index [C]."@en ;
    # Source: ontology comment for dNvpotrp

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one dNvpotrp value expected based on the nature of the property

    sh:message "dNvpotrp: The CommonCharacteristicsSubset must have at most one dNvpotrp value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:dNvpotrp ;

    sh:severity sh:Violation .

era-sh:dNvpotrpApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:dNvpotrp ;
    era:scope "local" ;

    rdfs:comment "A value must be provided when ETCS is present."@en ;
    # Source: inferred from the context of ETCS requirements

    sh:message "dNvpotrp: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A value must be provided when ETCS is present."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:dNvpotrp ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:etcsPresent ?etcsValue . FILTER(?etcsValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DNvroll a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:dNvroll ;
    era:rinfIndex "1.1.1.3.2.16.1" ;
    era:scope "local" ;

    rdfs:comment "Parameter used by the ETCS on-board to supervise the distance allowed to be travelled under the roll-away protection and the reverse movement protection, in metres."@en ;
    # Source: ontology comment for dNvroll

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one dNvroll value expected based on the nature of the property

    sh:message "dNvroll: The CommonCharacteristicsSubset must have at most one dNvroll value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:dNvroll ;

    sh:severity sh:Violation .

era-sh:DNvrollApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:dNvroll ;
    era:scope "local" ;

    rdfs:comment "A value has to be provided when ETCS is present."@en ;
    # Source: dependency note in the ontology

    sh:message "dNvroll: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: A value must be provided when ETCS is present."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:dNvroll ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:etcsPresent ?etcsValue . FILTER(?etcsValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DangerousGoodsTankCode a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:dangerousGoodsTankCode ;
    era:rinfIndex "4.1.9" ;
    era:scope "local" ;

    rdfs:comment "Dangerous goods for which the vehicle is suitable (tank code)."@en ;
    # Source: ontology comment for dangerousGoodsTankCode

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one tank code value expected based on the nature of the property

    sh:message "dangerousGoodsTankCode: The VehicleType must have at most one dangerousGoodsTankCode value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:dangerousGoodsTankCode ;

    sh:severity sh:Violation .

era-sh:DangerousGoodsTankCodeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:dangerousGoodsTankCode ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is authorized to transport dangerous goods."@en ;
    # Source: inferred from the context of dangerous goods transport requirements

    sh:message "dangerousGoodsTankCode: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the vehicle type is authorized to transport dangerous goods."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:dangerousGoodsTankCode ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { $this era:authorizedToTransportDangerousGoods ?authValue . FILTER(?authValue != "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DemonstrationENE a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:demonstrationENE ; 
    era:rinfIndex "1.1.1.2.1.2" ; 
    era:scope "local" ;

    rdfs:comment "Unique number for EI declarations following the same format requirements as specified for EC declarations."@en ; 
    # Source: ontology comment for demonstrationENE

    sh:datatype xsd:string ; 
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ; 
    # Inferred: only one declaration expected based on the nature of the property

    sh:message "demonstrationENE: The CommonCharacteristicsSubset must have at most one demonstrationENE value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:demonstrationENE ; 

    sh:severity sh:Violation .

era-sh:DemonstrationENEApplicability a sh:SPARQLConstraint ; 
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:demonstrationENE ; 
    era:scope "local" ;

    rdfs:comment "Applicable when the demonstration was executed and EI declaration was issued."@en ; 
    # Source: inferred from the applicability explanation in RAG

    sh:message "demonstrationENE: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when the demonstration was executed and EI declaration was issued."@en ;

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:demonstrationENE ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { $this era:eiDeclarationIssued ?eiValue . FILTER(?eiValue != "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DemonstrationINF a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:demonstrationINF ;
    era:rinfIndex "1.1.1.1.1.2" ;
    era:scope "local" ;

    rdfs:comment "Unique number for EI declarations following the same format requirements as specified in the ‘Document about practical arrangements for transmitting interoperability documents’."@en ;
    # Source: ontology comment for demonstrationINF

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one declaration expected based on the nature of the property

    sh:message "demonstrationINF: The CommonCharacteristicsSubset must have at most one demonstrationINF value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:demonstrationINF ;

    sh:severity sh:Violation .

era-sh:DemonstrationINFApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:demonstrationINF ;
    era:scope "local" ;

    rdfs:comment "Applicable when the demonstration was executed and EI declaration was issued."@en ;
    # Source: inferred from the applicability explanation in RAG

    sh:message "demonstrationINF: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when the demonstration was executed and EI declaration was issued."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:demonstrationINF ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { $this era:eiDeclarationIssued ?eiValue . FILTER(?eiValue != "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DemonstrationSRT a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:demonstrationSRT ;
    era:rinfIndex "1.1.1.1.8.6" ;
    era:scope "local" ;

    rdfs:comment "Unique number for EI declarations following the same format requirements as specified in the ‘Document about practical arrangements for transmitting interoperability documents’."@en ;
    # Source: ontology comment for demonstrationSRT

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one demonstrationSRT value expected based on the nature of the property

    sh:message "demonstrationSRT: The Tunnel must have at most one demonstrationSRT value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:demonstrationSRT ;

    sh:severity sh:Violation .

era-sh:DemonstrationSRTApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:demonstrationSRT ;
    era:scope "local" ;

    rdfs:comment "Applicable when the demonstration was executed and EI declaration was issued."@en ;
    # Source: inferred from the applicability explanation in RAG

    sh:message "demonstrationSRT: This error is due to the Tunnel {?tunnelLabel}, violating the rule: Applicable only when the demonstration was executed and EI declaration was issued."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:demonstrationSRT ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { $this era:eiDeclarationIssued ?eiValue . FILTER(?eiValue != "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DesignMassExceptionalPayload a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:designMassExceptionalPayload ;
    era:rinfIndex "4.5.2.3" ;
    era:scope "local" ;

    rdfs:comment "Design mass under exceptional payload."@en ;
    # Source: ontology comment for designMassExceptionalPayload

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one design mass value expected based on the nature of the property

    sh:message "designMassExceptionalPayload: The VehicleType must have at most one designMassExceptionalPayload value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:designMassExceptionalPayload ;

    sh:severity sh:Violation .

era-sh:DesignMassExceptionalPayloadApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:designMassExceptionalPayload ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is designed to carry exceptional payloads."@en ;
    # Source: inferred from the context of exceptional payload requirements

    sh:message "designMassExceptionalPayload: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the vehicle type is designed to carry exceptional payloads."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:designMassExceptionalPayload ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { $this era:designedForExceptionalPayload ?payloadValue . FILTER(?payloadValue != "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DesignMassNormalPayload a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:designMassNormalPayload ;
    era:rinfIndex "4.5.2.2" ;
    era:scope "local" ;

    rdfs:comment "Design mass under normal payload."@en ;
    # Source: ontology comment for designMassNormalPayload

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one design mass value expected based on the nature of the property

    sh:message "designMassNormalPayload: The VehicleType must have at most one designMassNormalPayload value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:designMassNormalPayload ;

    sh:severity sh:Violation .

era-sh:DesignMassNormalPayloadApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:designMassNormalPayload ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is designed to operate under normal payload conditions."@en ;
    # Source: inferred from the context of normal payload requirements

    sh:message "designMassNormalPayload: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the vehicle type is designed to operate under normal payload conditions."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:designMassNormalPayload ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { $this era:designedForNormalPayload ?payloadValue . FILTER(?payloadValue != "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DesignMassWorkingOrder a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:designMassWorkingOrder ;
    era:rinfIndex "4.5.2.1" ;
    era:scope "local" ;

    rdfs:comment "Design mass in working order."@en ;
    # Source: ontology comment for designMassWorkingOrder

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one design mass value expected based on the nature of the property

    sh:message "designMassWorkingOrder: The VehicleType must have at most one designMassWorkingOrder value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:designMassWorkingOrder ;

    sh:severity sh:Violation .

era-sh:DesignMassWorkingOrderApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:designMassWorkingOrder ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type has a defined working order mass."@en ;
    # Source: inferred from the context of vehicle type specifications

    sh:message "designMassWorkingOrder: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the vehicle type has a defined working order mass."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:designMassWorkingOrder ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { $this era:workingOrderMassDefined ?definedValue . FILTER(?definedValue != "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DieselThermalAllowed a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:dieselThermalAllowed ;
    era:rinfIndex "1.2.1.0.5.9" ;
    era:scope "local" ;

    rdfs:comment "Indication whether it is allowed to use diesel or other thermal traction in the tunnel."@en ;
    # Source: ontology comment for dieselThermalAllowed

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one dieselThermalAllowed value expected based on the nature of the property

    sh:message "dieselThermalAllowed: The Tunnel must have at most one dieselThermalAllowed value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:dieselThermalAllowed ;

    sh:severity sh:Violation .

era-sh:DieselThermalAllowedApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:dieselThermalAllowed ;
    era:scope "local" ;

    rdfs:comment "Applicable only for tunnels when the parameter 1.2.1.0.5.7 (Fire category of rolling stock required) has the option ‘none’ selected."@en ;
    # Source: applicability explanation in RAG

    sh:message "dieselThermalAllowed: This error is due to the Tunnel {?tunnelLabel}, violating the rule: Applicable only when the fire category of rolling stock required is ‘none’."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:dieselThermalAllowed ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:fireCategoryRequired ?fireValue . FILTER(?fireValue = "none") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DigitalSchematicOverview a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:digitalSchematicOverview ;
    era:rinfIndex "1.2.0.0.0.7.2" ;
    era:scope "local" ;

    rdfs:comment "Diagrammatic representation of the operational point in Well Known Text polyline."@en ;
    # Source: ontology comment for digitalSchematicOverview

    sh:datatype geosparql:wktLiteral ;
    # Source: ontology range is geosparql:wktLiteral (http://www.opengis.net/ont/geosparql#wktLiteral)

    sh:maxCount 1 ;
    # Inferred: only one digital schematic overview value expected based on the nature of the property

    sh:message "digitalSchematicOverview: The OperationalPoint must have at most one digitalSchematicOverview value that is a WKT literal. This error may be due to having more than one value or having a value that is not a valid WKT literal."@en ;

    sh:path era:digitalSchematicOverview ;

    sh:severity sh:Violation .

era-sh:DigitalSchematicOverviewApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:digitalSchematicOverview ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the operational point has a defined schematic overview."@en ;
    # Source: inferred from the context of operational point requirements

    sh:message "digitalSchematicOverview: This error is due to the OperationalPoint {?opLabel}, violating the rule: Applicable only when the operational point has a defined schematic overview."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?opLabel WHERE {
        $this rdfs:label ?opLabel .
        FILTER NOT EXISTS { $this era:digitalSchematicOverview ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:schematicOverviewDefined ?definedValue . FILTER(?definedValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DistSignToPhaseEnd a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:distSignToPhaseEnd ;
    era:rinfIndex "1.1.1.2.4.3" ;
    era:scope "local" ;

    rdfs:comment "Distance between the signboard authorizing the driver to raise pantograph or close the circuit breaker after passing the phase separation and the end of the phase separation section."@en ;
    # Source: ontology comment for distSignToPhaseEnd

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one distSignToPhaseEnd value expected based on the nature of the property

    sh:message "distSignToPhaseEnd: The CommonCharacteristicsSubset must have at most one distSignToPhaseEnd value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:distSignToPhaseEnd ;

    sh:severity sh:Violation .

era-sh:DistSignToPhaseEndApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:distSignToPhaseEnd ;
    era:scope "local" ;

    rdfs:comment "Applicable only if the value 'Overhead contact line (OCL)' is selected for parameter 'Type of contact line system'."@en ;
    # Source: dependency note in the ontology

    sh:message "distSignToPhaseEnd: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only if the value 'Overhead contact line (OCL)' is selected for parameter 'Type of contact line system'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:distSignToPhaseEnd ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:typeOfContactLineSystem ?typeValue . FILTER(?typeValue = "OCL") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DocumentUrl a sh:PropertyShape ;
    era:affectedClass era:Document ;
    era:affectedProperty era:documentUrl ;
    era:scope "local" ;

    rdfs:comment "URL that is used to download a document, e.g. url for a reference document in RINF."@en ;
    # Source: ontology comment for documentUrl

    sh:datatype xsd:anyURI ;
    # Source: ontology range is xsd:anyURI (http://www.w3.org/2001/XMLSchema#anyURI)

    sh:maxCount 1 ;
    # Inferred: only one document URL expected based on the nature of the property

    sh:message "documentUrl: The Document must have at most one documentUrl value that is a valid URI. This error may be due to having more than one value or having a value that is not a valid URI."@en ;

    sh:path era:documentUrl ;

    sh:severity sh:Violation .

era-sh:DocumentUrlApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Document ;
    era:affectedProperty era:documentUrl ;
    era:scope "local" ;

    rdfs:comment "Applicable when the document is available for download and meets the specified criteria."@en ;
    # Source: inferred from the context of document availability

    sh:message "documentUrl: This error is due to the Document {?docLabel}, violating the rule: Applicable only when the document is available for download."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?docLabel WHERE {
        $this rdfs:label ?docLabel .
        FILTER NOT EXISTS { $this era:documentUrl ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { $this era:isAvailableForDownload ?availabilityValue . FILTER(?availabilityValue != "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:DrivingCabs a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:drivingCabs ;
    era:rinfIndex "4.1.1" ;
    era:scope "local" ;

    rdfs:comment "Number of driving cabs. For wagons the number of driving cabs is to be set to zero (0)."@en ;
    # Source: ontology comment for drivingCabs

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one driving cab value expected based on the nature of the property

    sh:message "drivingCabs: The VehicleType must have at most one drivingCabs value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:drivingCabs ;

    sh:severity sh:Violation .

era-sh:DrivingCabsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:drivingCabs ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is not a wagon."@en ;
    # Source: inferred from the context of driving cabs requirements

    sh:message "drivingCabs: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the vehicle type is not a wagon."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:drivingCabs ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this a era:VehicleType . FILTER(?vehicleLabel != "wagon") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EddyCurrentBrakePrevention a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:eddyCurrentBrakePrevention ;
    era:rinfIndex "4.7.4.1.2" ;
    era:scope "local" ;

    rdfs:comment "Possibility of preventing the use of the eddy current track brake (only if fitted with eddy current brake)."@en ;
    # Source: ontology comment for eddyCurrentBrakePrevention

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one eddyCurrentBrakePrevention value expected based on the nature of the property

    sh:message "eddyCurrentBrakePrevention: The VehicleType must have at most one eddyCurrentBrakePrevention value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:eddyCurrentBrakePrevention ;

    sh:severity sh:Violation .

era-sh:EddyCurrentBrakePreventionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:eddyCurrentBrakePrevention ;
    era:scope "local" ;

    rdfs:comment "Applicable only if the vehicle type is fitted with an eddy current brake."@en ;
    # Source: inferred from the context of eddy current brake requirements

    sh:message "eddyCurrentBrakePrevention: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only if the vehicle type is fitted with an eddy current brake."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:eddyCurrentBrakePrevention ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:fittedWithEddyCurrentBrake ?fittedValue . FILTER(?fittedValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EddyCurrentBrakingFitted a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:eddyCurrentBrakingFitted ;
    era:rinfIndex "4.7.4.1.1" ;
    era:scope "local" ;

    rdfs:comment "Eddy current track brake fitted. New property defined to distinguish it from eddyCurrentBraking which is a RINF SKOS property."@en ;
    # Source: ontology comment for eddyCurrentBrakingFitted

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one eddyCurrentBrakingFitted value expected based on the nature of the property

    sh:message "eddyCurrentBrakingFitted: The VehicleType must have at most one eddyCurrentBrakingFitted value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:eddyCurrentBrakingFitted ;

    sh:severity sh:Violation .

era-sh:EddyCurrentBrakingFittedApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:eddyCurrentBrakingFitted ;
    era:scope "local" ;

    rdfs:comment "Applicable only if the vehicle type is fitted with an eddy current brake."@en ;
    # Source: inferred from the context of eddy current brake requirements

    sh:message "eddyCurrentBrakingFitted: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only if the vehicle type is fitted with an eddy current brake."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:eddyCurrentBrakingFitted ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:fittedWithEddyCurrentBrake ?fittedValue . FILTER(?fittedValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EmergencyBrake a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:emergencyBrake ;
    era:rinfIndex "4.7.5" ;
    era:scope "local" ;

    rdfs:comment "Stopping distance and deceleration profile for each load condition per design maximum speed."@en ;
    # Source: ontology comment for emergencyBrake

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one emergency brake value expected based on the nature of the property

    sh:message "emergencyBrake: The VehicleType must have at most one emergencyBrake value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:emergencyBrake ;

    sh:severity sh:Violation .

era-sh:EmergencyBrakeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:emergencyBrake ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type has defined emergency braking characteristics."@en ;
    # Source: inferred from the context of emergency braking requirements

    sh:message "emergencyBrake: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the vehicle type has defined emergency braking characteristics."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:emergencyBrake ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:emergencyBrakingDefined ?definedValue . FILTER(?definedValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EndOffsetFromOrigin a sh:PropertyShape ;
    era:affectedClass era:LinearElementSection ;
    era:affectedProperty era:endOffsetFromOrigin ;
    era:scope "local" ;

    rdfs:comment "End offset of the section from the origin."@en ;
    # Source: ontology comment for endOffsetFromOrigin

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one end offset value expected based on the nature of the property

    sh:message "endOffsetFromOrigin: The LinearElementSection must have at most one endOffsetFromOrigin value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:endOffsetFromOrigin ;

    sh:severity sh:Violation .

era-sh:EndOffsetFromOriginApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LinearElementSection ;
    era:affectedProperty era:endOffsetFromOrigin ;
    era:scope "local" ;

    rdfs:comment "Applicable when the LinearElementSection has defined offsets from the origin."@en ;
    # Source: inferred from the context of linear element section requirements

    sh:message "endOffsetFromOrigin: This error is due to the LinearElementSection {?sectionLabel}, violating the rule: Applicable only when the section has defined offsets from the origin."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sectionLabel WHERE {
        $this rdfs:label ?sectionLabel .
        FILTER NOT EXISTS { $this era:endOffsetFromOrigin ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:hasDefinedOffsets ?offsetValue . FILTER(?offsetValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EnergyMeterInstalled a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:energyMeterInstalled ;
    era:rinfIndex "4.10.12" ;
    era:scope "local" ;

    rdfs:comment "TSI conform energy meter for billing purposes installed on board."@en ;
    # Source: ontology comment for energyMeterInstalled

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one energy meter installed value expected based on the nature of the property

    sh:message "energyMeterInstalled: The VehicleType must have at most one energyMeterInstalled value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:energyMeterInstalled ;

    sh:severity sh:Violation .

era-sh:EnergyMeterInstalledApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:energyMeterInstalled ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type has a defined energy supply system."@en ;
    # Source: inferred from the context of energy supply system requirements

    sh:message "energyMeterInstalled: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the vehicle type has a defined energy supply system."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:energyMeterInstalled ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:energySupplySystemDefined ?definedValue . FILTER(?definedValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EnergySupplyMaxPower a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:energySupplyMaxPower ;
    era:rinfIndex "4.10.2" ;
    era:scope "local" ;

    rdfs:comment "Maximum power (to be indicated for each energy supply system the vehicle is equipped for), given in kW."@en ;
    # Source: ontology comment for energySupplyMaxPower

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one energySupplyMaxPower value expected based on the nature of the property

    sh:message "energySupplyMaxPower: The VehicleType must have at most one energySupplyMaxPower value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:energySupplyMaxPower ;

    sh:severity sh:Violation .

era-sh:EnergySupplyMaxPowerApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:energySupplyMaxPower ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type has a defined energy supply system."@en ;
    # Source: inferred from the context of energy supply system requirements

    sh:message "energySupplyMaxPower: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the vehicle type has a defined energy supply system."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:energySupplyMaxPower ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:energySupplySystemDefined ?definedValue . FILTER(?definedValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EnergySupplySystemTSICompliant a sh:PropertyShape ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:energySupplySystemTSICompliant ;
    era:rinfIndex "1.1.1.2.2.1.2.1" ;
    era:scope "local" ;

    rdfs:comment "Indication if the traction supply system (nominal voltage and frequency) is fully compliant with TSI."@en ;
    # Source: ontology comment for energySupplySystemTSICompliant

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one energySupplySystemTSICompliant value expected based on the nature of the property

    sh:message "energySupplySystemTSICompliant: The ContactLineSystem must have at most one energySupplySystemTSICompliant value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:energySupplySystemTSICompliant ;

    sh:severity sh:Violation .

era-sh:EnergySupplySystemTSICompliantApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:energySupplySystemTSICompliant ;
    era:scope "local" ;

    rdfs:comment "Applicable when the traction supply system is electrified."@en ;
    # Source: inferred from the applicability explanation in RAG

    sh:message "energySupplySystemTSICompliant: This error is due to the ContactLineSystem {?clsLabel}, violating the rule: Applicable only when the traction supply system is electrified."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?clsLabel WHERE {
        $this rdfs:label ?clsLabel .
        FILTER NOT EXISTS { $this era:energySupplySystemTSICompliant ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:systemType ?typeValue . FILTER(?typeValue = "electrified") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ErrorCorrectionsOnboardExplanation a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:errorCorrectionsOnboardExplanation ;
    era:rinfIndex "1.1.1.3.1.2" ;
    era:scope "local" ;

    rdfs:comment "Explanation on why a mandatory onboard CR required to be solved in the on-board (ETCS, GSM-R and/or ATO) was accepted by the IM."@en ;
    # Source: ontology comment for errorCorrectionsOnboardExplanation

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one errorCorrectionsOnboardExplanation value expected based on the nature of the property

    sh:message "errorCorrectionsOnboardExplanation: The CommonCharacteristicsSubset must have at most one errorCorrectionsOnboardExplanation value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:errorCorrectionsOnboardExplanation ;

    sh:severity sh:Violation .

era-sh:ErrorCorrectionsOnboardExplanationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:errorCorrectionsOnboardExplanation ;
    era:scope "local" ;

    rdfs:comment "Applicable when ETCS is present and a value is provided for the corresponding parameter."@en ;
    # Source: dependency note in the ontology

    sh:message "errorCorrectionsOnboardExplanation: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when ETCS is present and a value is provided for the corresponding parameter."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:errorCorrectionsOnboardExplanation ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:etcsPresent ?etcsValue . FILTER(?etcsValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ETCSDataCommApp a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:etcsDataCommApp ;
    era:rinfIndex "4.13.2.9" ;
    era:scope "local" ;

    rdfs:comment "Data communication application for ETCS implementation."@en ;
    # Source: ontology comment for etcsDataCommApp

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one ETCS data communication application value expected based on the nature of the property

    sh:message "etcsDataCommApp: The VehicleType must have at most one etcsDataCommApp value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:etcsDataCommApp ;

    sh:severity sh:Violation .

era-sh:ETCSDataCommAppApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:etcsDataCommApp ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is equipped with ETCS."@en ;
    # Source: inferred from the context of ETCS requirements

    sh:message "etcsDataCommApp: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the vehicle type is equipped with ETCS."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:etcsDataCommApp ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:equippedWithETCS ?etcsValue . FILTER(?etcsValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EtcsImplementsLevelCrossingProcedure a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:etcsImplementsLevelCrossingProcedure ;
    era:rinfIndex "1.1.1.3.2.13" ;
    era:scope "local" ;

    rdfs:comment "Indicates whether the ETCS trackside implements level crossing procedure or an equivalent solution."@en ;
    # Source: ontology comment for etcsImplementsLevelCrossingProcedure

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "etcsImplementsLevelCrossingProcedure: The CommonCharacteristicsSubset must have at most one etcsImplementsLevelCrossingProcedure value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:etcsImplementsLevelCrossingProcedure ;

    sh:severity sh:Violation .

era-sh:EtcsImplementsLevelCrossingProcedureApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:etcsImplementsLevelCrossingProcedure ;
    era:scope "local" ;

    rdfs:comment "Applicable and mandatory when ETCS is present (a value is provided on 1.1.1.3.2.1)."@en ;
    # Source: dependency note in the ontology

    sh:message "etcsImplementsLevelCrossingProcedure: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when ETCS is present."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:etcsImplementsLevelCrossingProcedure ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:etcsPresent ?etcsValue . FILTER(?etcsValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EtcsInfillLineAccess a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:etcsInfillLineAccess ;
    era:rinfIndex "1.1.1.3.2.3" ;
    era:scope "local" ;

    rdfs:comment "Indication whether infill is required to access the line for safety reasons."@en ;
    # Source: ontology comment for etcsInfillLineAccess

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one infill line access value expected based on the nature of the property

    sh:message "etcsInfillLineAccess: The CommonCharacteristicsSubset must have at most one etcsInfillLineAccess value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:etcsInfillLineAccess ;

    sh:severity sh:Violation .

era-sh:EtcsInfillLineAccessApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:etcsInfillLineAccess ;
    era:scope "local" ;

    rdfs:comment "Applicable only when selected value for 1.1.1.3.2.1 is ‘1’."@en ;
    # Source: applicability explanation in RAG

    sh:message "etcsInfillLineAccess: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when the selected value for 1.1.1.3.2.1 is ‘1’."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:etcsInfillLineAccess ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:etcsLevel ?levelValue . FILTER(?levelValue = "1") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ETCSNationalApplications a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:etcsNationalApplications ;
    era:rinfIndex "4.13.1.4" ;
    era:scope "local" ;

    rdfs:comment "ETCS national applications implemented (NID_XUSER of Packet 44)."@en ;
    # Source: ontology comment for etcsNationalApplications

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "etcsNationalApplications: The VehicleType must have at most one etcsNationalApplications value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:etcsNationalApplications ;

    sh:severity sh:Violation .

era-sh:ETCSNationalApplicationsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:etcsNationalApplications ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the vehicle type is equipped with ETCS."@en ;
    # Source: inferred from the context of ETCS requirements

    sh:message "etcsNationalApplications: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the vehicle type is equipped with ETCS."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:etcsNationalApplications ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:equippedWithETCS ?etcsValue . FILTER(?etcsValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ETCSOnBoardImplementation a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:etcsOnBoardImplementation ;
    era:rinfIndex "4.13.1.7" ;
    era:scope "local" ;

    rdfs:comment "ETCS on-board implementation."@en ;
    # Source: ontology comment for etcsOnBoardImplementation

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one ETCS on-board implementation value expected based on the nature of the property

    sh:message "etcsOnBoardImplementation: The VehicleType must have at most one etcsOnBoardImplementation value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:etcsOnBoardImplementation ;

    sh:severity sh:Violation .

era-sh:ETCSOnBoardImplementationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:etcsOnBoardImplementation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is equipped with ETCS."@en ;
    # Source: inferred from the context of ETCS requirements

    sh:message "etcsOnBoardImplementation: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the vehicle type is equipped with ETCS."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:etcsOnBoardImplementation ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:equippedWithETCS ?etcsValue . FILTER(?etcsValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:EtcsTransmitsTrackConditions a sh:PropertyShape ;
    era:affectedClass era:ETCS ;
    era:affectedProperty era:etcsTransmitsTrackConditions ;
    era:rinfIndex "1.1.1.3.2.12" ;
    era:scope "local" ;

    rdfs:comment "Indicates whether the ETCS trackside is engineered to transmit Track Conditions."@en ;
    # Source: ontology comment for etcsTransmitsTrackConditions

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "etcsTransmitsTrackConditions: The ETCS must have at most one etcsTransmitsTrackConditions value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:etcsTransmitsTrackConditions ;

    sh:severity sh:Violation .

era-sh:EtcsTransmitsTrackConditionsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ETCS ;
    era:affectedProperty era:etcsTransmitsTrackConditions ;
    era:scope "local" ;

    rdfs:comment "Applicable when ETCS is present and a value is provided for the corresponding parameter."@en ;
    # Source: dependency note in the ontology

    sh:message "etcsTransmitsTrackConditions: This error is due to the ETCS {?etcsLabel}, violating the rule: Applicable only when ETCS is present."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?etcsLabel WHERE {
        $this rdfs:label ?etcsLabel .
        FILTER NOT EXISTS { $this era:etcsTransmitsTrackConditions ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:etcsPresent ?etcsValue . FILTER(?etcsValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ExistanceOfIndustrialRisk a sh:PropertyShape ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:existanceOfIndustrialRisk ;
    era:rinfIndex "1.1.0.0.1.1" ;
    era:scope "local" ;

    rdfs:comment "Indication that the full section of line (or a part of it) is identified by the Infrastructure Managers (and communicated to the RU) where drivers are not supposed to get off the locomotive."@en ;
    # Source: ontology comment for existanceOfIndustrialRisk

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one existanceOfIndustrialRisk value expected based on the nature of the property

    sh:message "existanceOfIndustrialRisk: The SectionOfLine must have at most one existanceOfIndustrialRisk value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:existanceOfIndustrialRisk ;

    sh:severity sh:Violation .

era-sh:ExistanceOfIndustrialRiskApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:existanceOfIndustrialRisk ;
    era:scope "local" ;

    rdfs:comment "Applicable when the section of line has been identified with industrial risks."@en ;
    # Source: inferred from the context of industrial risk identification

    sh:message "existanceOfIndustrialRisk: This error is due to the SectionOfLine {?sectionLabel}, violating the rule: Applicable only when the section of line has been identified with industrial risks."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sectionLabel WHERE {
        $this rdfs:label ?sectionLabel .
        FILTER NOT EXISTS { $this era:existanceOfIndustrialRisk ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:identifiedWithIndustrialRisks ?riskValue . FILTER(?riskValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:FerromagneticWheelMaterial a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:ferromagneticWheelMaterial ;
    era:rinfIndex "4.14.2.12" ;
    era:scope "local" ;

    rdfs:comment "Wheel material is ferromagnetic."@en ;
    # Source: ontology comment for ferromagneticWheelMaterial

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one ferromagneticWheelMaterial value expected based on the nature of the property

    sh:message "ferromagneticWheelMaterial: The VehicleType must have at most one ferromagneticWheelMaterial value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:ferromagneticWheelMaterial ;

    sh:severity sh:Violation .

era-sh:FerromagneticWheelMaterialApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:ferromagneticWheelMaterial ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type has a defined wheel detector."@en ;
    # Source: inferred from the applicability explanation in RAG

    sh:message "ferromagneticWheelMaterial: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the vehicle type has a defined wheel detector."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:ferromagneticWheelMaterial ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:wheelDetectorType ?detectorValue . FILTER(?detectorValue = "wheel detector") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:FixedSeats a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:fixedSeats ;
    era:rinfIndex "4.12.1.1" ;
    era:scope "local" ;

    rdfs:comment "Number of fixed seats."@en ;
    # Source: ontology comment for fixedSeats

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one fixedSeats value expected based on the nature of the property

    sh:message "fixedSeats: The VehicleType must have at most one fixedSeats value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:fixedSeats ;

    sh:severity sh:Violation .

era-sh:FixedSeatsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:fixedSeats ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is defined and has a fixed seating arrangement."@en ;
    # Source: inferred from the context of vehicle type characteristics

    sh:message "fixedSeats: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the vehicle type is defined and has a fixed seating arrangement."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:fixedSeats ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:vehicleTypeDefined ?definedValue . FILTER(?definedValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:FlangeLubeForbidden a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:flangeLubeForbidden ;
    era:rinfIndex "1.1.1.1.7.1" ;
    era:scope "local" ;

    rdfs:comment "Indication whether the use of on-board device for flange lubrication is forbidden."@en ;
    # Source: ontology comment for flangeLubeForbidden

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one flangeLubeForbidden value expected based on the nature of the property

    sh:message "flangeLubeForbidden: The CommonCharacteristicsSubset must have at most one flangeLubeForbidden value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:flangeLubeForbidden ;

    sh:severity sh:Violation .

era-sh:FlangeLubeForbiddenApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:flangeLubeForbidden ;
    era:scope "local" ;

    rdfs:comment "Applicable when the flange lubrication rules are defined."@en ;
    # Source: inferred from the context of flange lubrication rules

    sh:message "flangeLubeForbidden: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when flange lubrication rules are defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:flangeLubeForbidden ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:flangeLubeRulesDefined ?rulesValue . FILTER(?rulesValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:FlangeLubeRules a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:flangeLubeRules ;
    era:rinfIndex "1.1.1.3.7.20" ;
    era:scope "local" ;

    rdfs:comment "Indication whether rules for activation or deactivation of flange lubrication exist."@en ;
    # Source: ontology comment for flangeLubeRules

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "flangeLubeRules: The TrainDetectionSystem must have at most one flangeLubeRules value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:flangeLubeRules ;

    sh:severity sh:Violation .

era-sh:FlangeLubeRulesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:flangeLubeRules ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the selected option for parameter 1.1.1.3.7.1.1 is 'track circuit'."@en ;
    # Source: applicability explanation in RAG

    sh:message "flangeLubeRules: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: Applicable only when the selected option for parameter 1.1.1.3.7.1.1 is 'track circuit'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:flangeLubeRules ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:trackCircuitOption ?optionValue . FILTER(?optionValue = "track circuit") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:FlangeLubricationFitted a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:flangeLubricationFitted ;
    era:rinfIndex "4.9.3.1" ;
    era:scope "local" ;

    rdfs:comment "Indicates if the vehicle type is fitted for flange lubrication."@en ;
    # Source: ontology comment for flangeLubricationFitted

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "flangeLubricationFitted: The VehicleType must have at most one flangeLubricationFitted value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:flangeLubricationFitted ;

    sh:severity sh:Violation .

era-sh:FlangeLubricationFittedApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:flangeLubricationFitted ;
    era:scope "local" ;

    rdfs:comment "Applicable when flange lubrication rules are defined."@en ;
    # Source: inferred from the context of flange lubrication rules

    sh:message "flangeLubricationFitted: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when flange lubrication rules are defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:flangeLubricationFitted ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:flangeLubeRulesDefined ?rulesValue . FILTER(?rulesValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:FreightFlag a sh:PropertyShape ;
    era:affectedClass era:PrimaryLocation ;
    era:affectedProperty era:freightFlag ;
    era:scope "local" ;

    rdfs:comment "Indicates whether the primary location is designated for freight operations."@en ;
    # Source: inferred from the context of freight operations

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one freight flag value expected based on the nature of the property

    sh:message "freightFlag: The PrimaryLocation must have at most one freightFlag value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:freightFlag ;

    sh:severity sh:Violation .

era-sh:FreightFlagApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PrimaryLocation ;
    era:affectedProperty era:freightFlag ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the primary location is designated for freight operations."@en ;
    # Source: inferred from the context of freight operations

    sh:message "freightFlag: This error is due to the PrimaryLocation {?locationLabel}, violating the rule: Applicable only when the primary location is designated for freight operations."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?locationLabel WHERE {
        $this rdfs:label ?locationLabel .
        FILTER NOT EXISTS { $this era:freightFlag ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:designatedForFreight ?freightValue . FILTER(?freightValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:FrenchTrainDetectionSystemLimitationApplicable a sh:PropertyShape ;
    era:affectedClass era:FrenchTrainDetectionSystemLimitation ;
    era:affectedProperty era:frenchTrainDetectionSystemLimitationApplicable ;
    era:scope "local" ;

    rdfs:comment "Part of the section with train detection limitation that indicates if it is applicable. Only for the French network."@en ;
    # Source: ontology comment for frenchTrainDetectionSystemLimitationApplicable

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "frenchTrainDetectionSystemLimitationApplicable: The FrenchTrainDetectionSystemLimitation must have at most one frenchTrainDetectionSystemLimitationApplicable value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:frenchTrainDetectionSystemLimitationApplicable ;

    sh:severity sh:Violation .

era-sh:FrenchTrainDetectionSystemLimitationApplicableApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:FrenchTrainDetectionSystemLimitation ;
    era:affectedProperty era:frenchTrainDetectionSystemLimitationApplicable ;
    era:scope "local" ;

    rdfs:comment "Applicable only for sections with specific train detection limitations on the French network."@en ;
    # Source: inferred from the definition and applicability conditions in RAG

    sh:message "frenchTrainDetectionSystemLimitationApplicable: This error is due to the FrenchTrainDetectionSystemLimitation {?limitationLabel}, violating the rule: Applicable only for sections with specific train detection limitations."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?limitationLabel WHERE {
        $this rdfs:label ?limitationLabel .
        FILTER NOT EXISTS { $this era:frenchTrainDetectionSystemLimitationApplicable ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:hasTrainDetectionLimitation ?limitationValue . FILTER(?limitationValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GaugingCheckLocation a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gaugingCheckLocation ;
    era:rinfIndex "1.1.1.1.3.1.2" ;
    era:scope "local" ;

    rdfs:comment "Location of particular points requiring specific checks due to deviations from gauging."@en ;
    # Source: ontology comment for gaugingCheckLocation

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one gauging check location value expected based on the nature of the property

    sh:message "gaugingCheckLocation: The CommonCharacteristicsSubset must have at most one gaugingCheckLocation value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:gaugingCheckLocation ;

    sh:severity sh:Violation .

era-sh:GaugingCheckLocationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gaugingCheckLocation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the conditions for the application of the gauge are not fulfilled."@en ;
    # Source: inferred from the applicability explanation in RAG

    sh:message "gaugingCheckLocation: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when the conditions for the application of the gauge are not fulfilled."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gaugingCheckLocation ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:gaugeConditionsFulfilled ?gaugeValue . FILTER(?gaugeValue = "N") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GprsForETCS a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gprsForETCS ;
    era:rinfIndex "1.1.1.3.3.3.2" ;
    era:scope "local" ;

    rdfs:comment "Indication if GPRS can be used for ETCS."@en ;
    # Source: ontology comment for gprsForETCS

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "gprsForETCS: The CommonCharacteristicsSubset must have at most one gprsForETCS value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:gprsForETCS ;

    sh:severity sh:Violation .

era-sh:GprsForETCSApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gprsForETCS ;
    era:scope "local" ;

    rdfs:comment "Applicable when the conditions for GPRS usage are met, specifically when GSM-R and ETCS L2 are installed."@en ;
    # Source: dependency note in the ontology

    sh:message "gprsForETCS: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when GSM-R and ETCS L2 are installed."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gprsForETCS ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { $this era:gsmRInstalled ?gsmValue . FILTER(?gsmValue = "Y") } .
        FILTER NOT EXISTS { $this era:etcsL2Installed ?etcsValue . FILTER(?etcsValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GprsImplementationArea a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gprsImplementationArea ;
    era:rinfIndex "1.1.1.3.3.3.3" ;
    era:scope "local" ;

    rdfs:comment "Indication of the area in which GPRS can be used for ETCS, expressed as a list of GPRS-enabled RBCs."@en ;
    # Source: ontology comment for gprsImplementationArea

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one gprsImplementationArea value expected based on the nature of the property

    sh:message "gprsImplementationArea: The CommonCharacteristicsSubset must have at most one gprsImplementationArea value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:gprsImplementationArea ;

    sh:severity sh:Violation .

era-sh:GprsImplementationAreaApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gprsImplementationArea ;
    era:scope "local" ;

    rdfs:comment "Applicable when GPRS can be used for ETCS, specifically when GPRS for ETCS is applicable."@en ;
    # Source: inferred from the applicability explanation in RAG

    sh:message "gprsImplementationArea: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when GPRS for ETCS is applicable."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gprsImplementationArea ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:gprsForETCS ?gprsValue . FILTER(?gprsValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Gradient a sh:PropertyShape ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:gradient ;
    era:rinfIndex "1.2.2.0.3.1" ;
    era:scope "local" ;

    rdfs:comment "Maximum value of the gradient for stabling tracks expressed in millimetres per metre."@en ;
    # Source: ontology comment for gradient

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one gradient value expected based on the nature of the property

    sh:message "gradient: The Siding must have at most one gradient value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:gradient ;

    sh:severity sh:Violation .

era-sh:GradientApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:gradient ;
    era:scope "local" ;

    rdfs:comment "Applicable when the gradient exceeds the TSI limit of 2.5 expressed in millimetres per metre."@en ;
    # Source: inferred from the applicability explanation in RAG

    sh:message "gradient: This error is due to the Siding {?sidingLabel}, violating the rule: Applicable only when the gradient exceeds the TSI limit of 2.5."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sidingLabel WHERE {
        $this rdfs:label ?sidingLabel .
        FILTER NOT EXISTS { $this era:gradient ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:gradient ?gradientValue . FILTER(?gradientValue > 2.5) } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GradientProfile a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gradientProfile ;
    era:rinfIndex "1.1.1.1.3.6" ;
    era:scope "local" ;

    rdfs:comment "Sequence of gradient values and locations of change in gradient."@en ;
    # Source: ontology comment for gradientProfile

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one gradientProfile value expected based on the nature of the property

    sh:message "gradientProfile: The CommonCharacteristicsSubset must have at most one gradientProfile value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:gradientProfile ;

    sh:severity sh:Violation .

era-sh:GradientProfileApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gradientProfile ;
    era:scope "local" ;

    rdfs:comment "Applicable when the gradient profile is defined for the section of line."@en ;
    # Source: inferred from the context of gradient profile applicability

    sh:message "gradientProfile: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when the gradient profile is defined for the section of line."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gradientProfile ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:gradientProfileDefined ?definedValue . FILTER(?definedValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GsmRNoCoverage a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gsmRNoCoverage ;
    era:rinfIndex "1.1.1.3.3.8" ;
    era:scope "local" ;

    rdfs:comment "Indication if there is no GSMR coverage."@en ;
    # Source: ontology comment for gsmRNoCoverage

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one gsmRNoCoverage value expected based on the nature of the property

    sh:message "gsmRNoCoverage: The CommonCharacteristicsSubset must have at most one gsmRNoCoverage value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:gsmRNoCoverage ;

    sh:severity sh:Violation .

era-sh:GsmRNoCoverageApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:gsmRNoCoverage ;
    era:scope "local" ;

    rdfs:comment "Applicable when GSM-R is installed; not applicable when 'none' is selected in parameter 1.1.1.3.3.1."@en ;
    # Source: applicability explanation in RAG

    sh:message "gsmRNoCoverage: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when GSM-R is installed."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gsmRNoCoverage ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { $this era:gsmRInstalled ?gsmValue . FILTER(?gsmValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GsmRSetsInDrivingCab a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:gsmRSetsInDrivingCab ;
    era:rinfIndex "4.13.2.2" ;
    era:scope "local" ;

    rdfs:comment "Number of GSM-R mobile sets in driving cab for data transmission."@en ;
    # Source: ontology comment for gsmRSetsInDrivingCab

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "gsmRSetsInDrivingCab: The VehicleType must have at most one gsmRSetsInDrivingCab value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:gsmRSetsInDrivingCab ;

    sh:severity sh:Violation .

era-sh:GsmRSetsInDrivingCabApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:gsmRSetsInDrivingCab ;
    era:scope "local" ;

    rdfs:comment "Applicable when GSM-R communication is required for the vehicle type."@en ;
    # Source: inferred from the context of GSM-R requirements

    sh:message "gsmRSetsInDrivingCab: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when GSM-R communication is required."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:gsmRSetsInDrivingCab ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:gsmRCommunicationRequired ?gsmValue . FILTER(?gsmValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:GsmrForcedDeregistrationFunctionalNumber a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:gsmrForcedDeregistrationFunctionalNumber ; 
    era:rinfIndex "1.1.1.3.3.11" ; 
    era:scope "local" ; 

    rdfs:comment "Indicates whether the GSM-R network is configured to allow forced de-registration of a functional number by another driver."@en ; 
    # Source: ontology comment for gsmrForcedDeregistrationFunctionalNumber

    sh:datatype xsd:boolean ; 
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ; 
    # Inferred: only one value expected based on the nature of the property

    sh:message "gsmrForcedDeregistrationFunctionalNumber: The CommonCharacteristicsSubset must have at most one gsmrForcedDeregistrationFunctionalNumber value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ; 

    sh:path era:gsmrForcedDeregistrationFunctionalNumber ; 

    sh:severity sh:Violation .

era-sh:GsmrForcedDeregistrationFunctionalNumberApplicability a sh:SPARQLConstraint ; 
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:gsmrForcedDeregistrationFunctionalNumber ; 
    era:scope "local" ; 

    rdfs:comment "Applicable when GSM-R and ETCS L2 must be installed for this parameter to be applicable."@en ; 
    # Source: dependency note in the ontology

    sh:message "gsmrForcedDeregistrationFunctionalNumber: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when GSM-R and ETCS L2 are installed."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:gsmrForcedDeregistrationFunctionalNumber ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { $this era:gsmRInstalled ?gsmValue . FILTER(?gsmValue = "Y") } .
        FILTER NOT EXISTS { $this era:etcsL2Installed ?etcsValue . FILTER(?etcsValue = "Y") } .
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:HandoverPointFlag a sh:PropertyShape ;
    era:affectedClass era:PrimaryLocation ;
    era:affectedProperty era:handoverPointFlag ;
    era:scope "local" ;

    rdfs:comment "Indicates whether the primary location is designated as a handover point."@en ;
    # Source: inferred from the context of handover points in rail operations

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one handover point flag value expected based on the nature of the property

    sh:message "handoverPointFlag: The PrimaryLocation must have at most one handoverPointFlag value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:handoverPointFlag ;

    sh:severity sh:Violation .

era-sh:HandoverPointFlagApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PrimaryLocation ;
    era:affectedProperty era:handoverPointFlag ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the primary location is designated as a handover point."@en ;
    # Source: inferred from the context of handover points in rail operations

    sh:message "handoverPointFlag: This error is due to the PrimaryLocation {?locationLabel}, violating the rule: Applicable only when the primary location is designated as a handover point."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?locationLabel WHERE {
        $this rdfs:label ?locationLabel .
        FILTER NOT EXISTS { $this era:handoverPointFlag ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:designatedAsHandoverPoint ?handoverValue . FILTER(?handoverValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasAdditionalBrakingInformation a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasAdditionalBrakingInformation ;
    era:rinfIndex "1.1.1.3.11.2" ;
    era:scope "local" ;

    rdfs:comment "Availability by the IM of additional information as defined in point (2) of point 4.2.2.6.2 of Regulation (EU) 2023/1693 - TSI OPE."@en ;
    # Source: ontology comment for hasAdditionalBrakingInformation

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasAdditionalBrakingInformation: The CommonCharacteristicsSubset must have at most one hasAdditionalBrakingInformation value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasAdditionalBrakingInformation ;

    sh:severity sh:Violation .

era-sh:HasAdditionalBrakingInformationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasAdditionalBrakingInformation ;
    era:scope "local" ;

    rdfs:comment "Applicable when additional braking information is available as defined in TSI OPE."@en ;
    # Source: inferred from the applicability explanation in RAG

    sh:message "hasAdditionalBrakingInformation: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when additional braking information is available."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:hasAdditionalBrakingInformation ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:additionalBrakingInformationAvailable ?infoValue . FILTER(?infoValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasAutomaticDroppingDevice a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasAutomaticDroppingDevice ;
    era:rinfIndex "4.10.11" ;
    era:scope "local" ;

    rdfs:comment "Automatic dropping device (ADD) fitted (to be indicated for each energy supply system the vehicle is equipped for)."@en ;
    # Source: ontology comment for hasAutomaticDroppingDevice

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasAutomaticDroppingDevice: The VehicleType must have at most one hasAutomaticDroppingDevice value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasAutomaticDroppingDevice ;

    sh:severity sh:Violation .

era-sh:HasAutomaticDroppingDeviceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasAutomaticDroppingDevice ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type requires an automatic dropping device based on the energy supply system."@en ;
    # Source: inferred from the applicability explanation in RAG

    sh:message "hasAutomaticDroppingDevice: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the energy supply system requires an automatic dropping device."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:hasAutomaticDroppingDevice ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:energySupplySystem ?systemValue . FILTER(?systemValue = "Overhead contact line (OCL)") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasBallast a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:hasBallast ;
    era:rinfIndex "1.1.1.1.4.4" ;
    era:scope "local" ;

    rdfs:comment "Specifies whether track construction is with sleepers embedded in ballast or not."@en ;
    # Source: ontology comment for hasBallast

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasBallast: The RunningTrack must have at most one hasBallast value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasBallast ;

    sh:severity sh:Violation .

era-sh:HasBallastApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:hasBallast ;
    era:scope "local" ;

    rdfs:comment "Mandatory for tracks with permitted speed (parameter 1.1.1.1.2.5) greater than 250km/h."@en ;
    # Source: dependency note in the ontology

    sh:message "hasBallast: This error is due to the RunningTrack {?trackLabel}, violating the rule: Applicable only for tracks with permitted speed greater than 250 km/h."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:hasBallast ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:maximumPermittedSpeed ?speedValue . FILTER(xsd:integer(?speedValue) > 250) } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasBridge a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:hasBridge ;
    era:scope "local" ;

    rdfs:comment "Whether or not a track is associated with a bridge."@en ;
    # Source: ontology comment for hasBridge

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one hasBridge value expected based on the nature of the property

    sh:message "hasBridge: The RunningTrack must have at most one hasBridge value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasBridge ;

    sh:severity sh:Violation .

era-sh:HasBridgeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:hasBridge ;
    era:scope "local" ;

    rdfs:comment "Applicable when the track is associated with a bridge."@en ;
    # Source: inferred from the context of bridge applicability

    sh:message "hasBridge: This error is due to the RunningTrack {?trackLabel}, violating the rule: Applicable only when the track is associated with a bridge."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:hasBridge ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:associatedWithBridge ?bridgeValue . FILTER(?bridgeValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasCantDefficiencyCompensation a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasCantDefficiencyCompensation ;
    era:rinfIndex "4.6.2" ;
    era:scope "local" ;

    rdfs:comment "Vehicle equipped with a cant deficiency compensation system (tilting vehicle)."@en ;
    # Source: ontology comment for hasCantDefficiencyCompensation

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasCantDefficiencyCompensation: The VehicleType must have at most one hasCantDefficiencyCompensation value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasCantDefficiencyCompensation ;

    sh:severity sh:Violation .

era-sh:HasCantDefficiencyCompensationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasCantDefficiencyCompensation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is designed to operate with cant deficiency compensation."@en ;
    # Source: inferred from the context of cant deficiency compensation in vehicle design

    sh:message "hasCantDefficiencyCompensation: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the vehicle type is designed to operate with cant deficiency compensation."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:hasCantDefficiencyCompensation ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:designedForCantCompensation ?compensationValue . FILTER(?compensationValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasCurrentLimitation a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasCurrentLimitation ;
    era:rinfIndex "4.10.14" ;
    era:scope "local" ;

    rdfs:comment "Electric units equipped with power or current limitation function."@en ;
    # Source: ontology comment for hasCurrentLimitation

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasCurrentLimitation: The VehicleType must have at most one hasCurrentLimitation value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasCurrentLimitation ;

    sh:severity sh:Violation .

era-sh:HasCurrentLimitationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasCurrentLimitation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type requires a current limitation function."@en ;
    # Source: inferred from the context of current limitation applicability

    sh:message "hasCurrentLimitation: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when a current limitation function is required."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:hasCurrentLimitation ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:currentLimitationRequired ?limitationValue . FILTER(?limitationValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasETCSRestrictionsConditions a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasETCSRestrictionsConditions ;
    era:rinfIndex "1.1.1.3.2.6" ;
    era:scope "local" ;

    rdfs:comment "Indication whether restrictions or conditions due to partial compliance with the TSI CCS exist."@en ;
    # Source: ontology comment for hasETCSRestrictionsConditions

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasETCSRestrictionsConditions: The CommonCharacteristicsSubset must have at most one hasETCSRestrictionsConditions value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasETCSRestrictionsConditions ;

    sh:severity sh:Violation .

era-sh:HasETCSRestrictionsConditionsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasETCSRestrictionsConditions ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the selected value for 1.1.1.3.2.1 is not 'N'."@en ;
    # Source: applicability explanation in RAG

    sh:message "hasETCSRestrictionsConditions: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when the selected value for 1.1.1.3.2.1 is not 'N'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:hasETCSRestrictionsConditions ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:etcsComplianceValue ?complianceValue . FILTER(?complianceValue != "N") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasElectricShoreSupply a sh:PropertyShape ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:hasElectricShoreSupply ;
    era:rinfIndex "1.2.2.0.4.6" ;
    era:scope "local" ;

    rdfs:comment "Indication whether an installation for electric shore supply exists (fixed installation for servicing trains)."@en ;
    # Source: ontology comment for hasElectricShoreSupply

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasElectricShoreSupply: The Siding must have at most one hasElectricShoreSupply value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasElectricShoreSupply ;

    sh:severity sh:Violation .

era-sh:HasElectricShoreSupplyApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:hasElectricShoreSupply ;
    era:scope "local" ;

    rdfs:comment "Applicable when the siding is equipped with an electric shore supply installation."@en ;
    # Source: inferred from the context of electric shore supply requirements

    sh:message "hasElectricShoreSupply: This error is due to the Siding {?sidingLabel}, violating the rule: Applicable only when the siding is equipped with an electric shore supply installation."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sidingLabel WHERE {
        $this rdfs:label ?sidingLabel .
        FILTER NOT EXISTS { $this era:hasElectricShoreSupply ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:electricShoreSupplyRequired ?supplyValue . FILTER(?supplyValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasEmergencyPlan a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:hasEmergencyPlan ;
    era:rinfIndex "1.1.1.1.8.9" ;
    era:scope "local" ;

    rdfs:comment "Indication whether an emergency plan exists."@en ;
    # Source: ontology comment for hasEmergencyPlan

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasEmergencyPlan: The Tunnel must have at most one hasEmergencyPlan value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasEmergencyPlan ;

    sh:severity sh:Violation .

era-sh:HasEmergencyPlanApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:hasEmergencyPlan ;
    era:scope "local" ;

    rdfs:comment "Applicable for tunnels longer than 1 km, in accordance with section 4.4.2 of SRT TSI."@en ;
    # Source: applicability explanation in RAG

    sh:message "hasEmergencyPlan: This error is due to the Tunnel {?tunnelLabel}, violating the rule: Applicable only for tunnels longer than 1 km."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:hasEmergencyPlan ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:length ?lengthValue . FILTER(xsd:integer(?lengthValue) <= 1000) } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasEtcsNationalPacket44 a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:hasEtcsNationalPacket44 ; 
    era:rinfIndex "1.1.1.3.2.5" ; 
    era:scope "local" ;

    rdfs:comment "Indication whether data for national packet 44 applications is transmitted between track and train."@en ; 
    # Source: ontology comment for hasEtcsNationalPacket44

    sh:datatype xsd:boolean ; 
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ; 
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasEtcsNationalPacket44: The CommonCharacteristicsSubset must have at most one hasEtcsNationalPacket44 value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasEtcsNationalPacket44 ;

    sh:severity sh:Violation .

era-sh:HasEtcsNationalPacket44Applicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:hasEtcsNationalPacket44 ; 
    era:scope "local" ;

    rdfs:comment "Applicable only when the selected value for 1.1.1.3.2.1 (ETCS present) is not 'N'."@en ; 
    # Source: dependency note in the ontology

    sh:message "hasEtcsNationalPacket44: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when the selected value for 1.1.1.3.2.1 is not 'N'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:hasEtcsNationalPacket44 ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:etcsComplianceValue ?complianceValue . FILTER(?complianceValue != "N") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasEvacuationAndRescuePoints a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:hasEvacuationAndRescuePoints ;
    era:rinfIndex "1.1.1.1.8.13" ;
    era:scope "local" ;

    rdfs:comment "Indication of existence of evacuation and rescue points."@en ;
    # Source: ontology comment for hasEvacuationAndRescuePoints

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasEvacuationAndRescuePoints: The Tunnel must have at most one hasEvacuationAndRescuePoints value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasEvacuationAndRescuePoints ;

    sh:severity sh:Violation .

era-sh:HasEvacuationAndRescuePointsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:hasEvacuationAndRescuePoints ;
    era:scope "local" ;

    rdfs:comment "Mandatory for TSI-compliant tunnels longer than 0.5 km."@en ;
    # Source: dependency note in the ontology

    sh:message "hasEvacuationAndRescuePoints: This error is due to the Tunnel {?tunnelLabel}, violating the rule: Applicable only for TSI-compliant tunnels longer than 0.5 km."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:hasEvacuationAndRescuePoints ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:length ?lengthValue . FILTER(xsd:integer(?lengthValue) > 500) } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasExternalCleaning a sh:PropertyShape ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:hasExternalCleaning ;
    era:rinfIndex "1.2.2.0.4.2" ;
    era:scope "local" ;

    rdfs:comment "Indication whether exists an installation of external cleaning facility (fixed installation for servicing trains) as defined in TSI INF."@en ;
    # Source: ontology comment for hasExternalCleaning

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasExternalCleaning: The Siding must have at most one hasExternalCleaning value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasExternalCleaning ;

    sh:severity sh:Violation .

era-sh:HasExternalCleaningApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:hasExternalCleaning ;
    era:scope "local" ;

    rdfs:comment "Applicable when the siding is equipped with an external cleaning facility."@en ;
    # Source: inferred from the context of external cleaning facility requirements

    sh:message "hasExternalCleaning: This error is due to the Siding {?sidingLabel}, violating the rule: Applicable only when the siding is equipped with an external cleaning facility."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sidingLabel WHERE {
        $this rdfs:label ?sidingLabel .
        FILTER NOT EXISTS { $this era:hasExternalCleaning ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:externalCleaningRequired ?cleaningValue . FILTER(?cleaningValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasHotAxleBoxDetector a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasHotAxleBoxDetector ;
    era:rinfIndex "1.1.1.1.7.4" ;
    era:scope "local" ;

    rdfs:comment "Indicates the existence of trackside hot axle box detector (HABD)."@en ;
    # Source: ontology comment for hasHotAxleBoxDetector

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasHotAxleBoxDetector: The CommonCharacteristicsSubset must have at most one hasHotAxleBoxDetector value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasHotAxleBoxDetector ;

    sh:severity sh:Violation .

era-sh:HasHotAxleBoxDetectorApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasHotAxleBoxDetector ;
    era:scope "local" ;

    rdfs:comment "Applicable when the section of line is equipped with trackside hot axle box detector (HABD)."@en ;
    # Source: inferred from the context of HABD applicability

    sh:message "hasHotAxleBoxDetector: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when the section of line is equipped with trackside hot axle box detector."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:hasHotAxleBoxDetector ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:equippedWithHABD ?habdValue . FILTER(?habdValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasLevelCrossings a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:hasLevelCrossings ;
    era:scope "local" ;

    rdfs:comment "Indication whether level crossings (including pedestrian track crossing) exist on the section of line."@en ;
    # Source: ontology comment for hasLevelCrossings

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one hasLevelCrossings value expected based on the nature of the property

    sh:message "hasLevelCrossings: The RunningTrack must have at most one hasLevelCrossings value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasLevelCrossings ;

    sh:severity sh:Violation .

era-sh:HasLevelCrossingsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:hasLevelCrossings ;
    era:scope "local" ;

    rdfs:comment "Applicable when the section of line has level crossings."@en ;
    # Source: inferred from the context of level crossings applicability

    sh:message "hasLevelCrossings: This error is due to the RunningTrack {?trackLabel}, violating the rule: Applicable only when the section of line has level crossings."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:hasLevelCrossings ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:levelCrossingsExist ?existValue . FILTER(?existValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasLubricationDevicePrevention a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasLubricationDevicePrevention ;
    era:rinfIndex "4.9.3.2" ;
    era:scope "local" ;

    rdfs:comment "Possibility of preventing the use of the lubrication device (only if fitted with flange lubrication)."@en ;
    # Source: ontology comment for hasLubricationDevicePrevention

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasLubricationDevicePrevention: The VehicleType must have at most one hasLubricationDevicePrevention value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasLubricationDevicePrevention ;

    sh:severity sh:Violation .

era-sh:HasLubricationDevicePreventionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasLubricationDevicePrevention ;
    era:scope "local" ;

    rdfs:comment "Applicable only if the vehicle type is fitted with flange lubrication."@en ;
    # Source: inferred from the context of lubrication device prevention

    sh:message "hasLubricationDevicePrevention: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only if the vehicle type is fitted with flange lubrication."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:hasLubricationDevicePrevention ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:flangeLubricationFitted ?lubricationValue . FILTER(?lubricationValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasParkingBrake a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasParkingBrake ;
    era:rinfIndex "4.7.3.4" ;
    era:scope "local" ;

    rdfs:comment "Indicates if a vehicle type has parking brake."@en ;
    # Source: ontology comment for hasParkingBrake

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasParkingBrake: The VehicleType must have at most one hasParkingBrake value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasParkingBrake ;

    sh:severity sh:Violation .

era-sh:HasParkingBrakeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasParkingBrake ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type requires a parking brake."@en ;
    # Source: inferred from the context of parking brake requirements

    sh:message "hasParkingBrake: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the vehicle type requires a parking brake."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:hasParkingBrake ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:parkingBrakeRequired ?brakeValue . FILTER(?brakeValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasPhaseSeparation a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasPhaseSeparation ;
    era:rinfIndex "1.1.1.2.4.1.1" ;
    era:scope "local" ;

    rdfs:comment "Indication of existence of phase separation and required information."@en ;
    # Source: ontology comment for hasPhaseSeparation

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasPhaseSeparation: The CommonCharacteristicsSubset must have at most one hasPhaseSeparation value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasPhaseSeparation ;

    sh:severity sh:Violation .

era-sh:HasPhaseSeparationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasPhaseSeparation ;
    era:scope "local" ;

    rdfs:comment "Applicable only if 'Overhead contact line (OCL)' is selected in parameter 1.1.1.2.2.1.1."@en ;
    # Source: applicability explanation in RAG

    sh:message "hasPhaseSeparation: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only if 'Overhead contact line (OCL)' is selected in parameter 1.1.1.2.2.1.1."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:hasPhaseSeparation ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:overheadContactLineSelection ?oclValue . FILTER(?oclValue = "OCL") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasPlatformCurvature a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasPlatformCurvature ;
    era:rinfIndex "1.2.1.0.6.8" ;
    era:scope "local" ;

    rdfs:comment "Indication of the existence of the curvature of the platform."@en ;
    # Source: ontology comment for hasPlatformCurvature

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasPlatformCurvature: The CommonCharacteristicsSubset must have at most one hasPlatformCurvature value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasPlatformCurvature ;

    sh:severity sh:Violation .

era-sh:HasPlatformCurvatureApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasPlatformCurvature ;
    era:scope "local" ;

    rdfs:comment "Applicable when the platform has curvature."@en ;
    # Source: inferred from the context of platform curvature requirements

    sh:message "hasPlatformCurvature: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when the platform has curvature."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:hasPlatformCurvature ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:platformCurvatureExists ?curvatureValue . FILTER(?curvatureValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasRefuelling a sh:PropertyShape ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:hasRefuelling ;
    era:rinfIndex "1.2.2.0.4.4" ;
    era:scope "local" ;

    rdfs:comment "Indication whether exists an installation for refuelling (fixed installation for servicing trains) as defined in INF TSIs."@en ;
    # Source: ontology comment for hasRefuelling

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasRefuelling: The Siding must have at most one hasRefuelling value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasRefuelling ;

    sh:severity sh:Violation .

era-sh:HasRefuellingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:hasRefuelling ;
    era:scope "local" ;

    rdfs:comment "Applicable when the siding is equipped with a refuelling installation."@en ;
    # Source: inferred from the context of refuelling installation requirements

    sh:message "hasRefuelling: This error is due to the Siding {?sidingLabel}, violating the rule: Applicable only when the siding is equipped with a refuelling installation."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sidingLabel WHERE {
        $this rdfs:label ?sidingLabel .
        FILTER NOT EXISTS { $this era:hasRefuelling ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:refuellingInstallationRequired ?refuellingValue . FILTER(?refuellingValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasRegenerativeBrake a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasRegenerativeBrake ;
    era:rinfIndex "4.7.4.3.1" ;
    era:scope "local" ;

    rdfs:comment "Indication whether regenerative braking is permitted or not."@en ;
    # Source: ontology comment for hasRegenerativeBrake

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasRegenerativeBrake: The VehicleType must have at most one hasRegenerativeBrake value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasRegenerativeBrake ;

    sh:severity sh:Violation .

era-sh:HasRegenerativeBrakeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasRegenerativeBrake ;
    era:scope "local" ;

    rdfs:comment "Applicable when regenerative braking is considered; not applicable when 'not electrified' is chosen in parameter 1.1.1.2.2.1.1."@en ;
    # Source: applicability explanation in RAG

    sh:message "hasRegenerativeBrake: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when regenerative braking is considered."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:hasRegenerativeBrake ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:systemType ?systemValue . FILTER(?systemValue != "not electrified") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasSandRestocking a sh:PropertyShape ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:hasSandRestocking ;
    era:rinfIndex "1.2.2.0.4.5" ;
    era:scope "local" ;

    rdfs:comment "Indication whether an installation for sand restocking exists (fixed installation for servicing trains)."@en ;
    # Source: ontology comment for hasSandRestocking

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasSandRestocking: The Siding must have at most one hasSandRestocking value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasSandRestocking ;

    sh:severity sh:Violation .

era-sh:HasSandRestockingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:hasSandRestocking ;
    era:scope "local" ;

    rdfs:comment "Applicable when the siding is equipped with a sand restocking installation."@en ;
    # Source: inferred from the context of sand restocking requirements

    sh:message "hasSandRestocking: This error is due to the Siding {?sidingLabel}, violating the rule: Applicable only when the siding is equipped with a sand restocking installation."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sidingLabel WHERE {
        $this rdfs:label ?sidingLabel .
        FILTER NOT EXISTS { $this era:hasSandRestocking ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:sandRestockingRequired ?restockingValue . FILTER(?restockingValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasSandingPrevention a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasSandingPrevention ;
    era:rinfIndex "4.14.2.12" ;
    era:scope "local" ;

    rdfs:comment "Possibility of preventing the use of sanding."@en ;
    # Source: ontology comment for hasSandingPrevention

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasSandingPrevention: The VehicleType must have at most one hasSandingPrevention value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasSandingPrevention ;

    sh:severity sh:Violation .

era-sh:HasSandingPreventionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasSandingPrevention ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type has the capability to prevent sanding."@en ;
    # Source: inferred from the context of sanding prevention requirements

    sh:message "hasSandingPrevention: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the vehicle type has the capability to prevent sanding."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:hasSandingPrevention ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:sandingPreventionCapability ?capabilityValue . FILTER(?capabilityValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasSchematicOverviewOPDigitalForm a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:hasSchematicOverviewOPDigitalForm ;
    era:rinfIndex "1.2.0.0.0.7" ;
    era:scope "local" ;

    rdfs:comment "The existence of a schematic overview of the operational point in digital form."@en ;
    # Source: ontology comment for hasSchematicOverviewOPDigitalForm

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasSchematicOverviewOPDigitalForm: The OperationalPoint must have at most one hasSchematicOverviewOPDigitalForm value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasSchematicOverviewOPDigitalForm ;

    sh:severity sh:Violation .

era-sh:HasSchematicOverviewOPDigitalFormApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:hasSchematicOverviewOPDigitalForm ;
    era:scope "local" ;

    rdfs:comment "Applicable when the operational point has a schematic overview in digital form."@en ;
    # Source: inferred from the context of operational points and their schematic overviews

    sh:message "hasSchematicOverviewOPDigitalForm: This error is due to the OperationalPoint {?opLabel}, violating the rule: Applicable only when the operational point has a schematic overview in digital form."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?opLabel WHERE {
        $this rdfs:label ?opLabel .
        FILTER NOT EXISTS { $this era:hasSchematicOverviewOPDigitalForm ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:schematicOverviewExists ?overviewValue . FILTER(?overviewValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasSevereWeatherConditions a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasSevereWeatherConditions ;
    era:rinfIndex "1.1.1.1.2.8" ;
    era:scope "local" ;

    rdfs:comment "Climatic conditions on the line are severe according to European standard."@en ;
    # Source: ontology comment for hasSevereWeatherConditions

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasSevereWeatherConditions: The CommonCharacteristicsSubset must have at most one hasSevereWeatherConditions value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasSevereWeatherConditions ;

    sh:severity sh:Violation .

era-sh:HasSevereWeatherConditionsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasSevereWeatherConditions ;
    era:scope "local" ;

    rdfs:comment "Applicable when climatic conditions indicate severe weather as defined in TSI."@en ;
    # Source: inferred from the context of severe weather conditions applicability

    sh:message "hasSevereWeatherConditions: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when climatic conditions indicate severe weather."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:hasSevereWeatherConditions ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:severeWeatherConditionsExpected ?weatherValue . FILTER(?weatherValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasShuntingRestrictions a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasShuntingRestrictions ;
    era:rinfIndex "4.8.3" ;
    era:scope "local" ;

    rdfs:comment "Indicates the presence of shunting restrictions."@en ;
    # Source: ontology comment for hasShuntingRestrictions

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasShuntingRestrictions: The VehicleType must have at most one hasShuntingRestrictions value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasShuntingRestrictions ;

    sh:severity sh:Violation .

era-sh:HasShuntingRestrictionsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasShuntingRestrictions ;
    era:scope "local" ;

    rdfs:comment "Applicable when there are specific operational conditions or restrictions related to shunting."@en ;
    # Source: inferred from the context of shunting restrictions

    sh:message "hasShuntingRestrictions: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when there are specific operational conditions or restrictions related to shunting."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:hasShuntingRestrictions ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:operationalConditions ?conditionsValue . FILTER(?conditionsValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasSystemSeparation a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:hasSystemSeparation ; 
    era:rinfIndex "1.1.1.2.4.2.1" ; 
    era:scope "local" ; 

    rdfs:comment "Indication of existence of system separation."@en ; 
    # Source: ontology comment for hasSystemSeparation

    sh:datatype xsd:boolean ; 
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ; 
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasSystemSeparation: The CommonCharacteristicsSubset must have at most one hasSystemSeparation value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ; 

    sh:path era:hasSystemSeparation ; 

    sh:severity sh:Violation .

era-sh:HasSystemSeparationApplicability a sh:SPARQLConstraint ; 
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:hasSystemSeparation ; 
    era:scope "local" ; 

    rdfs:comment "Applicable only if 'Overhead contact line (OCL)' is selected for parameter 1.1.1.2.2.1.1."@en ; 
    # Source: applicability explanation in RAG

    sh:message "hasSystemSeparation: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only if 'Overhead contact line (OCL)' is selected for parameter 1.1.1.2.2.1.1."@en ; 

    sh:prefixes era: ; 

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:hasSystemSeparation ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:overheadContactLineSelection ?oclValue . FILTER(?oclValue = "OCL") } .
    }
    """ ; 

    sh:severity sh:Violation .

era-sh:HasTSITrainDetection a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasTSITrainDetection ;
    era:rinfIndex "1.1.1.3.4.1" ;
    era:scope "local" ;

    rdfs:comment "Indication if there is any train detection system installed and fully compliant with the TSI CCS."@en ;
    # Source: ontology comment for hasTSITrainDetection

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasTSITrainDetection: The CommonCharacteristicsSubset must have at most one hasTSITrainDetection value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasTSITrainDetection ;

    sh:severity sh:Violation .

era-sh:HasTSITrainDetectionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:hasTSITrainDetection ;
    era:scope "local" ;

    rdfs:comment "Applicable only if there is a requirement for train detection systems as defined in the TSI."@en ;
    # Source: inferred from the context of TSI compliance requirements

    sh:message "hasTSITrainDetection: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only if there is a requirement for train detection systems."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:hasTSITrainDetection ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:trainDetectionSystemRequirement ?requirementValue . FILTER(?requirementValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasToiletDischarge a sh:PropertyShape ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:hasToiletDischarge ;
    era:rinfIndex "1.2.2.0.4.1" ;
    era:scope "local" ;

    rdfs:comment "Indication whether exists an installation for toilet discharge (fixed installation for servicing trains) as defined in INF TSIs."@en ;
    # Source: ontology comment for hasToiletDischarge

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasToiletDischarge: The Siding must have at most one hasToiletDischarge value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasToiletDischarge ;

    sh:severity sh:Violation .

era-sh:HasToiletDischargeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:hasToiletDischarge ;
    era:scope "local" ;

    rdfs:comment "Applicable when there is a requirement for toilet discharge facilities as defined in the TSI."@en ;
    # Source: inferred from the context of toilet discharge requirements

    sh:message "hasToiletDischarge: This error is due to the Siding {?sidingLabel}, violating the rule: Applicable only when there is a requirement for toilet discharge facilities."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sidingLabel WHERE {
        $this rdfs:label ?sidingLabel .
        FILTER NOT EXISTS { $this era:hasToiletDischarge ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:toiletDischargeRequired ?dischargeValue . FILTER(?dischargeValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasTrainIntegrityConfirmation a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasTrainIntegrityConfirmation ;
    era:rinfIndex "4.13.1.9" ;
    era:scope "local" ;

    rdfs:comment "Indication of on-board management system about completeness of the train information."@en ;
    # Source: ontology comment for hasTrainIntegrityConfirmation

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasTrainIntegrityConfirmation: The VehicleType must have at most one hasTrainIntegrityConfirmation value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasTrainIntegrityConfirmation ;

    sh:severity sh:Violation .

era-sh:HasTrainIntegrityConfirmationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasTrainIntegrityConfirmation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the train integrity monitoring system (TIMS) is required for line access."@en ;
    # Source: inferred from RAG applicability explanation

    sh:message "hasTrainIntegrityConfirmation: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the train integrity monitoring system (TIMS) is required for line access."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:hasTrainIntegrityConfirmation ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:trainIntegrityMonitoringRequired ?timValue . FILTER(?timValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasWalkway a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:hasWalkway ;
    era:rinfIndex "1.1.1.1.8.12" ;
    era:scope "local" ;

    rdfs:comment "Indication of existence of walkways."@en ;
    # Source: ontology comment for hasWalkway

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasWalkway: The Tunnel must have at most one hasWalkway value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasWalkway ;

    sh:severity sh:Violation .

era-sh:HasWalkwayApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:hasWalkway ;
    era:scope "local" ;

    rdfs:comment "Mandatory for TSI-compliant tunnels longer than 0.5 km."@en ;
    # Source: dependency note in ontology

    sh:message "hasWalkway: This error is due to the Tunnel {?tunnelLabel}, violating the rule: Applicable only for TSI-compliant tunnels longer than 0.5 km."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:hasWalkway ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:tunnelLength ?lengthValue . FILTER(xsd:decimal(?lengthValue) > 0.5) } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasWaterRestocking a sh:PropertyShape ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:hasWaterRestocking ;
    era:rinfIndex "1.2.2.0.4.3" ;
    era:scope "local" ;

    rdfs:comment "Indication whether exists an installation for water restocking (fixed installation for servicing trains) as defined in INF TSIs."@en ;
    # Source: ontology comment for hasWaterRestocking

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasWaterRestocking: The Siding must have at most one hasWaterRestocking value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasWaterRestocking ;

    sh:severity sh:Violation .

era-sh:HasWaterRestockingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:hasWaterRestocking ;
    era:scope "local" ;

    rdfs:comment "Applicable when the siding is equipped with a water restocking installation."@en ;
    # Source: inferred from the context of water restocking requirements

    sh:message "hasWaterRestocking: This error is due to the Siding {?sidingLabel}, violating the rule: Applicable only when the siding is equipped with a water restocking installation."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sidingLabel WHERE {
        $this rdfs:label ?sidingLabel .
        FILTER NOT EXISTS { $this era:hasWaterRestocking ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:waterRestockingRequired ?restockingValue . FILTER(?restockingValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HasWheelSlideProtectionSystem a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasWheelSlideProtectionSystem ;
    era:rinfIndex "4.7.8" ;
    era:scope "local" ;

    rdfs:comment "Indicates the presence of a wheel slide protection system."@en ;
    # Source: ontology comment for hasWheelSlideProtectionSystem

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hasWheelSlideProtectionSystem: The VehicleType must have at most one hasWheelSlideProtectionSystem value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hasWheelSlideProtectionSystem ;

    sh:severity sh:Violation .

era-sh:HasWheelSlideProtectionSystemApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:hasWheelSlideProtectionSystem ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is required to have a wheel slide protection system."@en ;
    # Source: inferred from the context of wheel slide protection system requirements

    sh:message "hasWheelSlideProtectionSystem: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the vehicle type is required to have a wheel slide protection system."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:hasWheelSlideProtectionSystem ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:wheelSlideProtectionRequired ?requirementValue . FILTER(?requirementValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HighSpeedLoadModelCompliance a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:highSpeedLoadModelCompliance ;
    era:rinfIndex "1.1.1.1.2.4.2" ;
    era:scope "local" ;

    rdfs:comment "For sections of line with a maximum permitted speed of 200 km/h or more. Information regarding the procedure to be used to perform the dynamic compatibility check."@en ;
    # Source: ontology comment for highSpeedLoadModelCompliance

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "highSpeedLoadModelCompliance: The RunningTrack must have at most one highSpeedLoadModelCompliance value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:highSpeedLoadModelCompliance ;

    sh:severity sh:Violation .

era-sh:HighSpeedLoadModelComplianceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:highSpeedLoadModelCompliance ;
    era:scope "local" ;

    rdfs:comment "Applicable if the maximum permitted speed of the running track is more than 200 km/h and the structures within the section of line are all compatible with the High Speed Load Model (HSLM)."@en ;
    # Source: applicability explanation in RAG

    sh:message "highSpeedLoadModelCompliance: This error is due to the RunningTrack {?trackLabel}, violating the rule: Applicable only if the maximum permitted speed is greater than 200 km/h."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:highSpeedLoadModelCompliance ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:maximumPermittedSpeed ?speedValue . FILTER(xsd:integer(?speedValue) > 200) } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HotAxleBoxDetectorGeneration a sh:PropertyShape ;
    era:affectedClass era:HABD ;
    era:affectedProperty era:hotAxleBoxDetectorGeneration ;
    era:rinfIndex "1.1.1.1.7.7" ;
    era:scope "local" ;

    rdfs:comment "Specific for the French, Italian, and Swedish networks. Generation of trackside hot axle box detector."@en ;
    # Source: ontology comment for hotAxleBoxDetectorGeneration

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hotAxleBoxDetectorGeneration: The HABD must have at most one hotAxleBoxDetectorGeneration value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:hotAxleBoxDetectorGeneration ;

    sh:severity sh:Violation .

era-sh:HotAxleBoxDetectorGenerationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:HABD ;
    era:affectedProperty era:hotAxleBoxDetectorGeneration ;
    era:scope "local" ;

    rdfs:comment "Applicable if the HABD is not TSI compliant (the value of parameter 1.1.1.1.7.5 (Trackside HABD TSI compliant) is 'False')."@en ;
    # Source: applicability explanation in RAG

    sh:message "hotAxleBoxDetectorGeneration: This error is due to the HABD {?habdLabel}, violating the rule: Applicable only if the HABD is not TSI compliant."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?habdLabel WHERE {
        $this rdfs:label ?habdLabel .
        FILTER NOT EXISTS { $this era:hotAxleBoxDetectorGeneration ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:tsiCompliance ?tsiValue . FILTER(?tsiValue = "False") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HotAxleBoxDetectorIdentification a sh:PropertyShape ;
    era:affectedClass era:HABD ;
    era:affectedProperty era:hotAxleBoxDetectorIdentification ;
    era:rinfIndex "1.1.1.1.7.6" ;
    era:scope "local" ;

    rdfs:comment "Specific for the French, Italian, and Swedish networks. Applicable if trackside HABD is not TSI compliant, identification of trackside hot axle box detector."@en ;
    # Source: ontology comment for hotAxleBoxDetectorIdentification

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hotAxleBoxDetectorIdentification: The HABD must have at most one hotAxleBoxDetectorIdentification value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:hotAxleBoxDetectorIdentification ;

    sh:severity sh:Violation .

era-sh:HotAxleBoxDetectorIdentificationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:HABD ;
    era:affectedProperty era:hotAxleBoxDetectorIdentification ;
    era:scope "local" ;

    rdfs:comment "Applicable if trackside HABD is not TSI compliant (the value of parameter 1.1.1.1.7.5 (Trackside HABD TSI compliant) is 'False')."@en ;
    # Source: applicability explanation in RAG

    sh:message "hotAxleBoxDetectorIdentification: This error is due to the HABD {?habdLabel}, violating the rule: Applicable only if the HABD is not TSI compliant."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?habdLabel WHERE {
        $this rdfs:label ?habdLabel .
        FILTER NOT EXISTS { $this era:hotAxleBoxDetectorIdentification ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:tsiCompliance ?tsiValue . FILTER(?tsiValue = "False") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HotAxleBoxDetectorLocation a sh:PropertyShape ;
    era:affectedClass era:HABD ;
    era:affectedProperty era:hotAxleBoxDetectorLocation ;
    era:rinfIndex "1.1.1.1.7.8" ;
    era:scope "local" ;

    rdfs:comment "Specific for the French, Italian, and Swedish networks. Applicable if trackside HABD is not TSI compliant, localisation of trackside hot axle box detector."@en ;
    # Source: ontology comment for hotAxleBoxDetectorLocation

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hotAxleBoxDetectorLocation: The HABD must have at most one hotAxleBoxDetectorLocation value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:hotAxleBoxDetectorLocation ;

    sh:severity sh:Violation .

era-sh:HotAxleBoxDetectorLocationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:HABD ;
    era:affectedProperty era:hotAxleBoxDetectorLocation ;
    era:scope "local" ;

    rdfs:comment "Applicable if trackside HABD is not TSI compliant (the value of parameter 1.1.1.1.7.5 (Trackside HABD TSI compliant) is 'False')."@en ;
    # Source: applicability explanation in RAG

    sh:message "hotAxleBoxDetectorLocation: This error is due to the HABD {?habdLabel}, violating the rule: Applicable only if the HABD is not TSI compliant."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?habdLabel WHERE {
        $this rdfs:label ?habdLabel .
        FILTER NOT EXISTS { $this era:hotAxleBoxDetectorLocation ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:tsiCompliance ?tsiValue . FILTER(?tsiValue = "False") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:HotAxleBoxDetectorTSICompliant a sh:PropertyShape ;
    era:affectedClass era:HABD ;
    era:affectedProperty era:hotAxleBoxDetectorTSICompliant ;
    era:rinfIndex "1.1.1.1.7.5" ;
    era:scope "local" ;

    rdfs:comment "Specific for the French, Italian and Swedish networks. Trackside HABD compliant to TSI means that the HABD Trackside is compliant with EN 15437-1:2009 referred in TSIs."@en ;
    # Source: ontology comment for hotAxleBoxDetectorTSICompliant

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "hotAxleBoxDetectorTSICompliant: The HABD must have at most one hotAxleBoxDetectorTSICompliant value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:hotAxleBoxDetectorTSICompliant ;

    sh:severity sh:Violation .

era-sh:HotAxleBoxDetectorTSICompliantApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:HABD ;
    era:affectedProperty era:hotAxleBoxDetectorTSICompliant ;
    era:scope "local" ;

    rdfs:comment "Applicable if the trackside HABD is required to be compliant with TSI standards."@en ;
    # Source: inferred from the context of TSI compliance requirements

    sh:message "hotAxleBoxDetectorTSICompliant: This error is due to the HABD {?habdLabel}, violating the rule: Applicable only if the HABD is required to be compliant with TSI standards."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?habdLabel WHERE {
        $this rdfs:label ?habdLabel .
        FILTER NOT EXISTS { $this era:hotAxleBoxDetectorTSICompliant ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:tsiCompliance ?tsiValue . FILTER(?tsiValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:IMCode a sh:PropertyShape ;
    era:affectedClass era:OrganisationRole ;
    era:affectedProperty era:imCode ;
    era:rinfIndex "1.2.2.0.5.1" ;
    era:scope "local" ;

    rdfs:comment "Infrastructure Manager means any body or undertaking that is responsible in particular for establishing and maintaining railway infrastructure or a part thereof."@en ;
    # Source: RAG definition for IM's Code

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:pattern "^[A-Z]{4}$" ;
    # Inferred: based on the data presentation format [AAAA] indicating a 4-character code

    sh:message "imCode: The OrganisationRole must have at most one imCode value that is a string matching the pattern of four uppercase letters. This error may be due to having more than one value or having a value that does not conform to the expected format."@en ;

    sh:path era:imCode ;

    sh:severity sh:Violation .

era-sh:IMCodeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OrganisationRole ;
    era:affectedProperty era:imCode ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OrganisationRole is defined as an Infrastructure Manager."@en ;
    # Source: inferred from the context of IM's Code requirements

    sh:message "imCode: This error is due to the OrganisationRole {?roleLabel}, violating the rule: Applicable only when the OrganisationRole is defined as an Infrastructure Manager."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?roleLabel WHERE {
        $this rdfs:label ?roleLabel .
        FILTER NOT EXISTS { $this era:imCode ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this a era:InfrastructureManager } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:IsOnOriginOfElementA a sh:PropertyShape ;
    era:affectedClass era:NetRelation ;
    era:affectedProperty era:isOnOriginOfElementA ;
    era:rinfIndex "1.1.1.0.1.2" ;
    era:scope "local" ;

    rdfs:comment "Determines if the position of the topological element A in a relation is the origin of element A."@en ;
    # Source: ontology comment for isOnOriginOfElementA

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "isOnOriginOfElementA: The NetRelation must have at most one isOnOriginOfElementA value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:isOnOriginOfElementA ;

    sh:severity sh:Violation .

era-sh:IsOnOriginOfElementAApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:NetRelation ;
    era:affectedProperty era:isOnOriginOfElementA ;
    era:scope "local" ;

    rdfs:comment "Applicable when the topological element A is defined in the context of a net relation."@en ;
    # Source: inferred from the context of net relations

    sh:message "isOnOriginOfElementA: This error is due to the NetRelation {?relationLabel}, violating the rule: Applicable only when the topological element A is defined in the context of a net relation."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?relationLabel WHERE {
        $this rdfs:label ?relationLabel .
        FILTER NOT EXISTS { $this era:isOnOriginOfElementA ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this a era:NetRelation } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:IsOnOriginOfElementB a sh:PropertyShape ;
    era:affectedClass era:NetRelation ;
    era:affectedProperty era:isOnOriginOfElementB ;
    era:rinfIndex "1.1.1.0.1.2" ;
    era:scope "local" ;

    rdfs:comment "Determines if the position of the topological element B in a relation is the origin of element B."@en ;
    # Source: ontology comment for isOnOriginOfElementB

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "isOnOriginOfElementB: The NetRelation must have at most one isOnOriginOfElementB value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:isOnOriginOfElementB ;

    sh:severity sh:Violation .

era-sh:IsOnOriginOfElementBApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:NetRelation ;
    era:affectedProperty era:isOnOriginOfElementB ;
    era:scope "local" ;

    rdfs:comment "Applicable when the topological element B is defined in the context of a net relation."@en ;
    # Source: inferred from the context of net relations

    sh:message "isOnOriginOfElementB: This error is due to the NetRelation {?relationLabel}, violating the rule: Applicable only when the topological element B is defined in the context of a net relation."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?relationLabel WHERE {
        $this rdfs:label ?relationLabel .
        FILTER NOT EXISTS { $this era:isOnOriginOfElementB ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this a era:NetRelation } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:IsQuietRoute a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:isQuietRoute ;
    era:rinfIndex "1.1.1.1.7.11" ;
    era:scope "local" ;

    rdfs:comment "Belonging to a 'quieter route' in accordance with Article 5b of TSI NOI."@en ;
    # Source: ontology comment for isQuietRoute

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "isQuietRoute: The CommonCharacteristicsSubset must have at most one isQuietRoute value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:isQuietRoute ;

    sh:severity sh:Violation .

era-sh:IsQuietRouteApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:isQuietRoute ;
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined as part of a quieter route."@en ;
    # Source: inferred from the context of quieter route requirements

    sh:message "isQuietRoute: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when the CommonCharacteristicsSubset is defined as part of a quieter route."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:isQuietRoute ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:quieterRouteRequirement ?requirementValue . FILTER(?requirementValue = "Y") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Kilometer a sh:PropertyShape ;
    era:affectedClass era:KilometricPost ;
    era:affectedProperty era:kilometer ;
    era:rinfIndex "1.2.0.0.0.6" ;
    era:scope "local" ;

    rdfs:comment "Kilometer number of the kilometric post related to line identification defining the location of an infrastructure element."@en ;
    # Source: ontology comment for kilometer

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "kilometer: The KilometricPost must have at most one kilometer value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:kilometer ;

    sh:severity sh:Violation .

era-sh:KilometerApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:KilometricPost ;
    era:affectedProperty era:kilometer ;
    era:scope "local" ;

    rdfs:comment "Applicable when the kilometric post is used to define the location of an infrastructure element."@en ;
    # Source: inferred from the context of kilometer usage in infrastructure

    sh:message "kilometer: This error is due to the KilometricPost {?postLabel}, violating the rule: Applicable only when the kilometric post is used to define the location of an infrastructure element."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?postLabel WHERE {
        $this rdfs:label ?postLabel .
        FILTER NOT EXISTS { $this era:kilometer ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this a era:KilometricPost } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:KmPostName a sh:PropertyShape ;
    era:affectedClass era:KilometricPost ;
    era:affectedProperty era:kmPostName ;
    era:scope "local" ;

    rdfs:comment "Name of the kilometric post (optional)."@en ;
    # Source: ontology comment for kmPostName

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "kmPostName: The KilometricPost must have at most one kmPostName value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:kmPostName ;

    sh:severity sh:Violation .

era-sh:KmPostNameApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:KilometricPost ;
    era:affectedProperty era:kmPostName ;
    era:scope "local" ;

    rdfs:comment "Applicable when the kilometric post is defined within the context of infrastructure elements."@en ;
    # Source: inferred from the context of kmPostName usage in infrastructure

    sh:message "kmPostName: This error is due to the KilometricPost {?postLabel}, violating the rule: Applicable only when the kilometric post is defined within the context of infrastructure elements."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?postLabel WHERE {
        $this rdfs:label ?postLabel .
        FILTER NOT EXISTS { $this era:kmPostName ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this a era:KilometricPost } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LengthOfNetLinearElement a sh:PropertyShape ;
    era:affectedClass era:LinearElement ;
    era:affectedProperty era:lengthOfNetLinearElement ;
    era:scope "local" ;

    rdfs:comment "Length of a topological linear element."@en ;
    # Source: ontology comment for lengthOfNetLinearElement

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "lengthOfNetLinearElement: The LinearElement must have at most one lengthOfNetLinearElement value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:lengthOfNetLinearElement ;

    sh:severity sh:Violation .

era-sh:LengthOfNetLinearElementApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LinearElement ;
    era:affectedProperty era:lengthOfNetLinearElement ;
    era:scope "local" ;

    rdfs:comment "Applicable when the LinearElement is defined and has a specified length."@en ;
    # Source: inferred from the context of length requirements for linear elements

    sh:message "lengthOfNetLinearElement: This error is due to the LinearElement {?elementLabel}, violating the rule: Applicable only when the LinearElement is defined and has a specified length."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?elementLabel WHERE {
        $this rdfs:label ?elementLabel .
        FILTER NOT EXISTS { $this era:lengthOfNetLinearElement ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this a era:LinearElement } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LengthOfPlatform a sh:PropertyShape ;
    era:affectedClass era:PlatformEdge ;
    era:affectedProperty era:lengthOfPlatform ;
    era:rinfIndex "1.2.1.0.6.4" ;
    era:scope "local" ;

    rdfs:comment "The maximum continuous length (expressed in metres) of that part of platform in front of which a train is intended to remain stationary in normal operating conditions for passengers to board and alight from the train, making appropriate allowance for stopping tolerances."@en ;
    # Source: ontology comment for lengthOfPlatform

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "lengthOfPlatform: The PlatformEdge must have at most one lengthOfPlatform value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:lengthOfPlatform ;

    sh:severity sh:Violation .

era-sh:LengthOfPlatformApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PlatformEdge ;
    era:affectedProperty era:lengthOfPlatform ;
    era:scope "local" ;

    rdfs:comment "Applicable when the platform edge is defined and has a specified usable length."@en ;
    # Source: inferred from the context of platform length requirements

    sh:message "lengthOfPlatform: This error is due to the PlatformEdge {?platformLabel}, violating the rule: Applicable only when the platform edge is defined and has a specified usable length."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?platformLabel WHERE {
        $this rdfs:label ?platformLabel .
        FILTER NOT EXISTS { $this era:lengthOfPlatform ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this a era:PlatformEdge } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LengthOfSectionOfLine a sh:PropertyShape ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:lengthOfSectionOfLine ;
    era:rinfIndex "1.1.0.0.0.5" ;
    era:scope "local" ;

    rdfs:comment "Length between operational points at start and end of section of line."@en ;
    # Source: ontology comment for lengthOfSectionOfLine

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "lengthOfSectionOfLine: The SectionOfLine must have at most one lengthOfSectionOfLine value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:lengthOfSectionOfLine ;

    sh:severity sh:Violation .

era-sh:LengthOfSectionOfLineApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SectionOfLine ;
    era:affectedProperty era:lengthOfSectionOfLine ;
    era:scope "local" ;

    rdfs:comment "Applicable when the SectionOfLine is defined and has a specified length."@en ;
    # Source: inferred from the context of length requirements for sections of line

    sh:message "lengthOfSectionOfLine: This error is due to the SectionOfLine {?sectionLabel}, violating the rule: Applicable only when the SectionOfLine is defined and has a specified length."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sectionLabel WHERE {
        $this rdfs:label ?sectionLabel .
        FILTER NOT EXISTS { $this era:lengthOfSectionOfLine ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this a era:SectionOfLine } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LengthOfSiding a sh:PropertyShape ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:lengthOfSiding ;
    era:rinfIndex "1.2.2.0.2.1" ;
    era:scope "local" ;

    rdfs:comment "Total length of the siding/stabling track expressed in metres where trains can be parked safely."@en ;
    # Source: ontology comment for lengthOfSiding

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "lengthOfSiding: The Siding must have at most one lengthOfSiding value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:lengthOfSiding ;

    sh:severity sh:Violation .

era-sh:LengthOfSidingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:lengthOfSiding ;
    era:scope "local" ;

    rdfs:comment "Applicable when the siding is defined and has a specified usable length."@en ;
    # Source: inferred from the context of siding length requirements

    sh:message "lengthOfSiding: This error is due to the Siding {?sidingLabel}, violating the rule: Applicable only when the siding is defined and has a specified usable length."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?sidingLabel WHERE {
        $this rdfs:label ?sidingLabel .
        FILTER NOT EXISTS { $this era:lengthOfSiding ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this a era:Siding } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LengthOfTunnel a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:lengthOfTunnel ;
    era:rinfIndex "1.1.1.1.8.7" ;
    era:scope "local" ;

    rdfs:comment "Length of a tunnel in metres from entrance portal to exit portal."@en ;
    # Source: RAG definition for length of tunnel

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "lengthOfTunnel: The Tunnel must have at most one lengthOfTunnel value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:lengthOfTunnel ;

    sh:severity sh:Violation .

era-sh:LengthOfTunnelApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:lengthOfTunnel ;
    era:scope "local" ;

    rdfs:comment "Applicable only for a tunnel with length of 100 metres or more."@en ;
    # Source: RAG applicability note

    sh:message "lengthOfTunnel: This error is due to the Tunnel {?tunnelLabel}, violating the rule: Applicable only when the length of the tunnel is 100 metres or more."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?tunnelLabel WHERE {
        $this rdfs:label ?tunnelLabel .
        FILTER NOT EXISTS { $this era:lengthOfTunnel ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this a era:Tunnel } .
        FILTER (?value < 100) .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LengthOfVehicle a sh:PropertyShape ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:lengthOfVehicle ;
    era:rinfIndex "4.8.1" ;
    era:scope "local" ;

    rdfs:comment "Part of the combination of maximum speed and maximum cant deficiency for which the vehicle was assessed. Corresponds to the maximum speed."@en ;
    # Source: ontology comment for lengthOfVehicle

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "lengthOfVehicle: The Vehicle must have at most one lengthOfVehicle value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:lengthOfVehicle ;

    sh:severity sh:Violation .

era-sh:LengthOfVehicleApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:lengthOfVehicle ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle is defined and has a specified length."@en ;
    # Source: inferred from the context of vehicle length requirements

    sh:message "lengthOfVehicle: This error is due to the Vehicle {?vehicleLabel}, violating the rule: Applicable only when the Vehicle is defined and has a specified length."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:lengthOfVehicle ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this a era:Vehicle } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LetterMarking a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:letterMarking ;
    era:rinfIndex "4.1.7" ;
    era:scope "local" ;

    rdfs:comment "Letter marking associated with the vehicle type."@en ;
    # Source: ontology comment for letterMarking

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "letterMarking: The VehicleType must have at most one letterMarking value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:letterMarking ;

    sh:severity sh:Violation .

era-sh:LetterMarkingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:letterMarking ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and has a specified letter marking."@en ;
    # Source: inferred from the context of letter marking usage in vehicle types

    sh:message "letterMarking: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the VehicleType is defined and has a specified letter marking."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:letterMarking ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LineId a sh:PropertyShape ;
    era:affectedClass era:LinearPositioningSystem ;
    era:affectedProperty era:lineId ;
    era:rinfIndex "1.1.0.0.0.2" ;
    era:scope "local" ;

    rdfs:comment "Unique line identification or unique line number within Member State."@en ;
    # Source: ontology comment for lineId

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "lineId: The LinearPositioningSystem must have at most one lineId value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:lineId ;

    sh:severity sh:Violation .

era-sh:LineIdApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LinearPositioningSystem ;
    era:affectedProperty era:lineId ;
    era:scope "local" ;

    rdfs:comment "Applicable when the LinearPositioningSystem is defined and requires a unique line identification."@en ;
    # Source: inferred from the context of line identification requirements

    sh:message "lineId: This error is due to the LinearPositioningSystem {?systemLabel}, violating the rule: Applicable only when the LinearPositioningSystem is defined and requires a unique line identification."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:lineId ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this a era:LinearPositioningSystem } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LinesideDistanceIndicationFrequency a sh:PropertyShape ;
    era:affectedClass era:LinesideDistanceIndication ;
    era:affectedProperty era:linesideDistanceIndicationFrequency ;
    era:scope "local" ;

    rdfs:comment "Frequency of track lineside distance indications."@en ;
    # Source: ontology comment for linesideDistanceIndicationFrequency

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "linesideDistanceIndicationFrequency: The LinesideDistanceIndication must have at most one linesideDistanceIndicationFrequency value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:linesideDistanceIndicationFrequency ;

    sh:severity sh:Violation .

era-sh:LinesideDistanceIndicationFrequencyApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LinesideDistanceIndication ;
    era:affectedProperty era:linesideDistanceIndicationFrequency ;
    era:scope "local" ;

    rdfs:comment "Applicable when the LinesideDistanceIndication is defined and requires a specified frequency for distance indications."@en ;
    # Source: inferred from the context of lineside distance indication usage

    sh:message "linesideDistanceIndicationFrequency: This error is due to the LinesideDistanceIndication {?indicationLabel}, violating the rule: Applicable only when the LinesideDistanceIndication is defined and requires a specified frequency for distance indications."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?indicationLabel WHERE {
        $this rdfs:label ?indicationLabel .
        FILTER NOT EXISTS { $this era:linesideDistanceIndicationFrequency ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this a era:LinesideDistanceIndication } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LoadCapabilitySpeed a sh:PropertyShape ;
    era:affectedClass era:LoadCapability ;
    era:affectedProperty era:loadCapabilitySpeed ;
    era:scope "local" ;

    rdfs:comment "Part of the load capability of a track that corresponds to the speed of the load model."@en ;
    # Source: ontology comment for loadCapabilitySpeed

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "loadCapabilitySpeed: The LoadCapability must have at most one loadCapabilitySpeed value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:loadCapabilitySpeed ;

    sh:severity sh:Violation .

era-sh:LoadCapabilitySpeedApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LoadCapability ;
    era:affectedProperty era:loadCapabilitySpeed ;
    era:scope "local" ;

    rdfs:comment "Applicable when the LoadCapability is defined and has a specified load capability speed."@en ;
    # Source: inferred from the context of load capability speed requirements

    sh:message "loadCapabilitySpeed: This error is due to the LoadCapability {?capabilityLabel}, violating the rule: Applicable only when the LoadCapability is defined and has a specified load capability speed."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?capabilityLabel WHERE {
        $this rdfs:label ?capabilityLabel .
        FILTER NOT EXISTS { $this era:loadCapabilitySpeed ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this a era:LoadCapability } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LoadingPlatformHeight a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:loadingPlatformHeight ;
    era:rinfIndex "4.8.7" ;
    era:scope "local" ;

    rdfs:comment "Height of loading platform (for flat wagons and combined transport), given in mm."@en ;
    # Source: ontology comment for loadingPlatformHeight

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "loadingPlatformHeight: The VehicleType must have at most one loadingPlatformHeight value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:loadingPlatformHeight ;

    sh:severity sh:Violation .

era-sh:LoadingPlatformHeightApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:loadingPlatformHeight ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and has a specified loading platform height."@en ;
    # Source: inferred from the context of loading platform height requirements

    sh:message "loadingPlatformHeight: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the VehicleType is defined and has a specified loading platform height."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:loadingPlatformHeight ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:LocalRulesOrRestrictions a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:localRulesOrRestrictions ; 
    era:rinfIndex "1.1.1.4.1" ; 
    era:scope "local" ;

    rdfs:comment "Existence of rules and restrictions of a strictly local nature."@en ;
    # Source: ontology comment for localRulesOrRestrictions

    sh:datatype xsd:string ; 
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ; 
    # Inferred: only one value expected based on the nature of the property

    sh:message "localRulesOrRestrictions: The CommonCharacteristicsSubset must have at most one localRulesOrRestrictions value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:localRulesOrRestrictions ;

    sh:severity sh:Violation .

era-sh:LocalRulesOrRestrictionsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:localRulesOrRestrictions ; 
    era:scope "local" ;

    rdfs:comment "Applicable when the CommonCharacteristicsSubset is defined and has a specified local rules or restrictions."@en ;
    # Source: inferred from the context of local rules applicability

    sh:message "localRulesOrRestrictions: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when the CommonCharacteristicsSubset is defined and has a specified local rules or restrictions."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:localRulesOrRestrictions ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this a era:CommonCharacteristicsSubset } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MNvderun a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:mNvderun ;
    era:rinfIndex "1.1.1.3.2.16.10" ;
    era:scope "local" ;

    rdfs:comment "Entry of Driver ID permitted while running according to the specification referenced in Appendix A-1, index [C]."@en ;
    # Source: ontology comment for mNvderun

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "mNvderun: The CommonCharacteristicsSubset must have at most one mNvderun value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:mNvderun ;

    sh:severity sh:Violation .

era-sh:MNvderunApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:mNvderun ;
    era:scope "local" ;

    rdfs:comment "Applicable when ETCS is present; a value must be provided."@en ;
    # Source: dependency note in ontology

    sh:message "mNvderun: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when ETCS is present and a value is provided."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:mNvderun ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:etcsPresent ?etcsValue } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MagneticBrakePrevention a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:magneticBrakePrevention ;
    era:rinfIndex "4.7.4.2.2" ;
    era:scope "local" ;

    rdfs:comment "Possibility of preventing the use of the magnetic track brake (only if fitted with magnetic brake)."@en ;
    # Source: ontology comment for magneticBrakePrevention

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "magneticBrakePrevention: The VehicleType must have at most one magneticBrakePrevention value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:magneticBrakePrevention ;

    sh:severity sh:Violation .

era-sh:MagneticBrakePreventionApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:magneticBrakePrevention ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and has a specified magnetic brake prevention status."@en ;
    # Source: inferred from the context of magnetic brake usage

    sh:message "magneticBrakePrevention: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the VehicleType is defined and has a specified magnetic brake prevention status."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:magneticBrakePrevention ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MagneticBrakingFitted a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:magneticBrakingFitted ;
    era:rinfIndex "4.7.4.2.1" ;
    era:scope "local" ;

    rdfs:comment "Magnetic track brake fitted. New property defined to distinguish it from magneticBraking which is a RINF SKOS property."@en ;
    # Source: ontology comment for magneticBrakingFitted

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "magneticBrakingFitted: The VehicleType must have at most one magneticBrakingFitted value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:magneticBrakingFitted ;

    sh:severity sh:Violation .

era-sh:MagneticBrakingFittedApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:magneticBrakingFitted ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and has a specified magnetic braking fitted status."@en ;
    # Source: inferred from the context of magnetic braking usage

    sh:message "magneticBrakingFitted: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the VehicleType is defined and has a specified magnetic braking fitted status."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:magneticBrakingFitted ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MassPerWheel a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:massPerWheel ;
    era:rinfIndex "4.5.6" ;
    era:scope "local" ;

    rdfs:comment "Mass per wheel, given in kg."@en ;
    # Source: ontology comment for massPerWheel

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "massPerWheel: The VehicleType must have at most one massPerWheel value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:massPerWheel ;

    sh:severity sh:Violation .

era-sh:MassPerWheelApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:massPerWheel ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and has a specified mass per wheel."@en ;
    # Source: inferred from the context of vehicle mass requirements

    sh:message "massPerWheel: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the VehicleType is defined and has a specified mass per wheel."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:massPerWheel ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaxCurrentStandstillPantograph a sh:PropertyShape ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:maxCurrentStandstillPantograph ;
    era:rinfIndex "1.1.1.2.2.3" ;
    era:scope "local" ;

    rdfs:comment "Indication of the maximum allowable train current at standstill expressed in amperes."@en ;
    # Source: ontology comment for maxCurrentStandstillPantograph

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "maxCurrentStandstillPantograph: The ContactLineSystem must have at most one maxCurrentStandstillPantograph value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:maxCurrentStandstillPantograph ;

    sh:severity sh:Violation .

era-sh:MaxCurrentStandstillPantographApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:maxCurrentStandstillPantograph ;
    era:scope "local" ;

    rdfs:comment "Applicable only if 'Overhead contact line (OCL)' is selected for parameter 1.1.1.2.2.1.1 and if DC system is selected in 1.1.1.2.2.1.2."@en ;
    # Source: dependency note in ontology

    sh:message "maxCurrentStandstillPantograph: This error is due to the ContactLineSystem {?systemLabel}, violating the rule: Applicable only if 'Overhead contact line (OCL)' is selected and if DC system is selected."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:maxCurrentStandstillPantograph ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:systemType "Overhead contact line (OCL)" } .
        FILTER EXISTS { $this era:dcSystem ?dcValue } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaxDistConsecutiveAxles a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maxDistConsecutiveAxles ;
    era:rinfIndex "1.1.1.3.7.2.2" ;
    era:scope "local" ;

    rdfs:comment "Indication of maximum permitted distance between two consecutive axles in case of TSI non-compliance, given in millimetres."@en ;
    # Source: ontology comment for maxDistConsecutiveAxles

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "maxDistConsecutiveAxles: The VehicleType must have at most one maxDistConsecutiveAxles value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:maxDistConsecutiveAxles ;

    sh:severity sh:Violation .

era-sh:MaxDistConsecutiveAxlesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maxDistConsecutiveAxles ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and the selected option in parameter 1.1.1.3.7.2.1 is ‘Not TSI compliant’."@en ;
    # Source: RAG indicates applicability based on TSI compliance

    sh:message "maxDistConsecutiveAxles: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the VehicleType is defined and the selected option is ‘Not TSI compliant’."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:maxDistConsecutiveAxles ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:tsiCompliance "Not TSI compliant" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaxDistEndTrainFirstAxle a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:maxDistEndTrainFirstAxle ;
    era:rinfIndex "1.1.1.3.7.5" ;
    era:scope "local" ;

    rdfs:comment "Indication of maximum distance between end of train and first axle, given in millimetres, applicable for both sides (front and rear) of a vehicle or train."@en ;
    # Source: ontology comment for maxDistEndTrainFirstAxle

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "maxDistEndTrainFirstAxle: The TrainDetectionSystem must have at most one maxDistEndTrainFirstAxle value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:maxDistEndTrainFirstAxle ;

    sh:severity sh:Violation .

era-sh:MaxDistEndTrainFirstAxleApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:maxDistEndTrainFirstAxle ;
    era:scope "local" ;

    rdfs:comment "Applicable only when for parameter 1.1.1.3.7.1.1 the selected option is ‘track circuits’ or ‘wheel detector’."@en ;
    # Source: RAG indicates applicability based on detection system type

    sh:message "maxDistEndTrainFirstAxle: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: Applicable only when the selected option is ‘track circuits’ or ‘wheel detector’."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:maxDistEndTrainFirstAxle ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:detectionSystemType ?typeValue } .
        FILTER (?typeValue IN ("track circuits", "wheel detector")) .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaxFlangeHeight a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maxFlangeHeight ;
    era:rinfIndex "1.1.1.3.7.10" ;
    era:scope "local" ;

    rdfs:comment "Indication of maximum permitted height of the flange, given in millimetres."@en ;
    # Source: ontology comment for maxFlangeHeight

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "maxFlangeHeight: The VehicleType must have at most one maxFlangeHeight value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:maxFlangeHeight ;

    sh:severity sh:Violation .

era-sh:MaxFlangeHeightApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maxFlangeHeight ;
    era:scope "local" ;

    rdfs:comment "Applicable only when for parameter 1.1.1.3.7.1.1 the selected option is ‘wheel detector’."@en ;
    # Source: RAG indicates applicability based on wheel detection type

    sh:message "maxFlangeHeight: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the selected option for the detection type is ‘wheel detector’."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:maxFlangeHeight ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:detectionType "wheel detector" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaxImpedanceWheelset a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ; 
    era:affectedProperty era:maxImpedanceWheelset ; 
    era:rinfIndex "1.1.1.3.7.15.2" ; 
    era:scope "local" ;

    rdfs:comment "The value of maximum permitted impedance given in ohm in case of TSI non-compliance."@en ;
    # Source: ontology comment for maxImpedanceWheelset

    sh:datatype xsd:double ; 
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ; 
    # Inferred: only one value expected based on the nature of the property

    sh:message "maxImpedanceWheelset: The TrainDetectionSystem must have at most one maxImpedanceWheelset value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:maxImpedanceWheelset ;

    sh:severity sh:Violation .

era-sh:MaxImpedanceWheelsetApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ; 
    era:affectedProperty era:maxImpedanceWheelset ; 
    era:scope "local" ;

    rdfs:comment "Applicable only when for parameter 1.1.1.3.7.15.1 the selected option is ‘Not TSI compliant’."@en ;
    # Source: RAG indicates applicability based on TSI compliance

    sh:message "maxImpedanceWheelset: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: Applicable only when the selected option is ‘Not TSI compliant’."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:maxImpedanceWheelset ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:tsiCompliance "Not TSI compliant" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaxLengthVehicleNose a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maxLengthVehicleNose ;
    era:rinfIndex "4.14.2.4" ;
    era:scope "local" ;

    rdfs:comment "Maximum length of the vehicle nose."@en ;
    # Source: ontology comment for maxLengthVehicleNose

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "maxLengthVehicleNose: The VehicleType must have at most one maxLengthVehicleNose value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:maxLengthVehicleNose ;

    sh:severity sh:Violation .

era-sh:MaxLengthVehicleNoseApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maxLengthVehicleNose ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and has a specified maximum length of the vehicle nose."@en ;
    # Source: inferred from the context of vehicle specifications

    sh:message "maxLengthVehicleNose: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the VehicleType is defined and has a specified maximum length of the vehicle nose."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:maxLengthVehicleNose ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaxTrainCurrent a sh:PropertyShape ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:maxTrainCurrent ;
    era:rinfIndex "1.1.1.2.2.2" ;
    era:scope "local" ;

    rdfs:comment "Indication of the maximum allowable train current expressed in amperes."@en ;
    # Source: ontology comment for maxTrainCurrent

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "maxTrainCurrent: The ContactLineSystem must have at most one maxTrainCurrent value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:maxTrainCurrent ;

    sh:severity sh:Violation .

era-sh:MaxTrainCurrentApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:maxTrainCurrent ;
    era:scope "local" ;

    rdfs:comment "Applicable when 'not electrified' is not chosen in parameter 1.1.1.2.2.1.1."@en ;
    # Source: dependency note in ontology

    sh:message "maxTrainCurrent: This error is due to the ContactLineSystem {?systemLabel}, violating the rule: Applicable only when 'not electrified' is not chosen."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:maxTrainCurrent ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:electrificationStatus "not electrified" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumAltitude a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:maximumAltitude ; 
    era:rinfIndex "1.1.1.1.2.7" ; 
    era:scope "local" ;

    rdfs:comment "Highest point of the section of line above sea level in reference to Normal Amsterdam's Peil (NAP)."@en ;
    # Source: ontology comment for maximumAltitude

    sh:datatype xsd:double ; 
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ; 
    # Inferred: only one value expected based on the nature of the property

    sh:message "maximumAltitude: The CommonCharacteristicsSubset must have at most one maximumAltitude value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:maximumAltitude ;

    sh:severity sh:Violation .

era-sh:MaximumAltitudeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:maximumAltitude ; 
    era:scope "local" ;

    rdfs:comment "Applicable when the maximum altitude is defined and the value is within the specified range."@en ;
    # Source: inferred from the context of maximum altitude requirements

    sh:message "maximumAltitude: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when the maximum altitude is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:maximumAltitude ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:CommonCharacteristicsSubset } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumAverageDeceleration a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumAverageDeceleration ;
    era:rinfIndex "4.7.1" ;
    era:scope "local" ;

    rdfs:comment "Maximum train deceleration given in m/s²."@en ;
    # Source: ontology comment for maximumAverageDeceleration

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "maximumAverageDeceleration: The VehicleType must have at most one maximumAverageDeceleration value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:maximumAverageDeceleration ;

    sh:severity sh:Violation .

era-sh:MaximumAverageDecelerationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumAverageDeceleration ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and has a specified maximum average deceleration."@en ;
    # Source: inferred from the context of vehicle specifications

    sh:message "maximumAverageDeceleration: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the VehicleType is defined and has a specified maximum average deceleration."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:maximumAverageDeceleration ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumBrakeThermalEnergyCapacity a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumBrakeThermalEnergyCapacity ;
    era:rinfIndex "4.7.2.1.6" ;
    era:scope "local" ;

    rdfs:comment "Maximum brake thermal energy capacity given in kJ."@en ;
    # Source: ontology comment for maximumBrakeThermalEnergyCapacity

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "maximumBrakeThermalEnergyCapacity: The VehicleType must have at most one maximumBrakeThermalEnergyCapacity value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:maximumBrakeThermalEnergyCapacity ;

    sh:severity sh:Violation .

era-sh:MaximumBrakeThermalEnergyCapacityApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumBrakeThermalEnergyCapacity ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and has a specified maximum brake thermal energy capacity."@en ;
    # Source: inferred from the context of vehicle specifications

    sh:message "maximumBrakeThermalEnergyCapacity: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the VehicleType is defined and has a specified maximum brake thermal energy capacity."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:maximumBrakeThermalEnergyCapacity ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumBrakingDistance a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:maximumBrakingDistance ; 
    era:rinfIndex "1.1.1.3.11.1" ; 
    era:scope "local" ;

    rdfs:comment "The maximum value of the braking distance [in metres] of a train shall be given for the maximum line speed."@en ;
    # Source: ontology comment for maximumBrakingDistance

    sh:datatype xsd:integer ; 
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ; 
    # Inferred: only one value expected based on the nature of the property

    sh:message "maximumBrakingDistance: The CommonCharacteristicsSubset must have at most one maximumBrakingDistance value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:maximumBrakingDistance ;

    sh:severity sh:Violation .

era-sh:MaximumBrakingDistanceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:maximumBrakingDistance ; 
    era:scope "local" ;

    rdfs:comment "Applicable only when the maximum braking distance is defined and the value is within the specified range."@en ;
    # Source: inferred from the context of maximum braking distance requirements

    sh:message "maximumBrakingDistance: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when the maximum braking distance is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:maximumBrakingDistance ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:CommonCharacteristicsSubset } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumContactWireHeight a sh:PropertyShape ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:maximumContactWireHeight ;
    era:rinfIndex "1.1.1.2.2.5" ;
    era:scope "local" ;

    rdfs:comment "Indication of the maximum contact wire height expressed in metres."@en ;
    # Source: ontology comment for maximumContactWireHeight

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "maximumContactWireHeight: The ContactLineSystem must have at most one maximumContactWireHeight value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:maximumContactWireHeight ;

    sh:severity sh:Violation .

era-sh:MaximumContactWireHeightApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:maximumContactWireHeight ;
    era:scope "local" ;

    rdfs:comment "Applicable only if 'Overhead contact line (OCL)' is selected in parameter 1.1.1.2.2.1.1."@en ;
    # Source: RAG indicates applicability based on the selection of overhead contact line

    sh:message "maximumContactWireHeight: This error is due to the ContactLineSystem {?systemLabel}, violating the rule: Applicable only if 'Overhead contact line (OCL)' is selected."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:maximumContactWireHeight ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:overheadContactLine "OCL" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumDesignSpeed a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumDesignSpeed ;
    era:rinfIndex "4.1.2.1" ;
    era:scope "local" ;

    rdfs:comment "Maximum design speed."@en ;
    # Source: ontology comment for maximumDesignSpeed

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "maximumDesignSpeed: The VehicleType must have at most one maximumDesignSpeed value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:maximumDesignSpeed ;

    sh:severity sh:Violation .

era-sh:MaximumDesignSpeedApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumDesignSpeed ;
    era:scope "local" ;

    rdfs:comment "Applicable when the maximum design speed is defined and the value is within the specified range."@en ;
    # Source: inferred from the context of maximum design speed requirements

    sh:message "maximumDesignSpeed: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the maximum design speed is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:maximumDesignSpeed ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumInterferenceCurrent a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:maximumInterferenceCurrent ;
    era:rinfIndex "1.1.1.3.4.2.1" ;
    era:scope "local" ;

    rdfs:comment "Maximum interference current limits allowed for track circuits for a defined frequency band."@en ;
    # Source: ontology comment for maximumInterferenceCurrent

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "maximumInterferenceCurrent: The TrainDetectionSystem must have at most one maximumInterferenceCurrent value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:maximumInterferenceCurrent ;

    sh:severity sh:Violation .

era-sh:MaximumInterferenceCurrentApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:maximumInterferenceCurrent ;
    era:scope "local" ;

    rdfs:comment "Applicable when the preferred frequency bands are defined and the evaluation parameters are added if preferred bands are not used."@en ;
    # Source: dependency note in ontology

    sh:message "maximumInterferenceCurrent: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: Applicable only when preferred frequency bands are defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:maximumInterferenceCurrent ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { $this era:preferredFrequencyBands ?bands } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumInterferenceCurrentEvaluation a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:maximumInterferenceCurrentEvaluation ;
    era:rinfIndex "1.1.1.3.4.2.1" ;
    era:scope "local" ;

    rdfs:comment "Maximum interference current limits allowed for track circuits for a defined frequency band."@en ;
    # Source: ontology comment for maximumInterferenceCurrentEvaluation

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "maximumInterferenceCurrentEvaluation: The TrainDetectionSystem must have at most one maximumInterferenceCurrentEvaluation value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:maximumInterferenceCurrentEvaluation ;

    sh:severity sh:Violation .

era-sh:MaximumInterferenceCurrentEvaluationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:maximumInterferenceCurrentEvaluation ;
    era:scope "local" ;

    rdfs:comment "Applicable if the preferred frequency bands are not used, a mandatory description of the parameters for evaluation of compliance must be provided."@en ;
    # Source: dependency note in ontology

    sh:message "maximumInterferenceCurrentEvaluation: This error is due to the TrainDetectionSystem {?systemLabel}, violating the rule: Applicable only when the preferred frequency bands are not used."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:maximumInterferenceCurrentEvaluation ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:preferredFrequencyBands ?bands } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumLocomotivesCoupled a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumLocomotivesCoupled ;
    era:rinfIndex "4.1.5" ;
    era:scope "local" ;

    rdfs:comment "Maximum number of trainsets or locomotives coupled together in multiple operation."@en ;
    # Source: ontology comment for maximumLocomotivesCoupled

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "maximumLocomotivesCoupled: The VehicleType must have at most one maximumLocomotivesCoupled value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:maximumLocomotivesCoupled ;

    sh:severity sh:Violation .

era-sh:MaximumLocomotivesCoupledApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumLocomotivesCoupled ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and has a specified maximum number of locomotives that can be coupled."@en ;
    # Source: inferred from the context of vehicle specifications

    sh:message "maximumLocomotivesCoupled: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the VehicleType is defined and has a specified maximum number of locomotives that can be coupled."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:maximumLocomotivesCoupled ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumMagneticFieldDirectionX a sh:PropertyShape ;
    era:affectedClass era:MaximumMagneticField ;
    era:affectedProperty era:maximumMagneticFieldDirectionX ;
    era:rinfIndex "1.1.1.1.7.9" ;
    era:scope "local" ;

    rdfs:comment "The maximum magnetic field limits allowed for axle counters (in dB µA/m) for a defined frequency band. Direction X."@en ;
    # Source: ontology comment for maximumMagneticFieldDirectionX

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "maximumMagneticFieldDirectionX: The MaximumMagneticField must have at most one maximumMagneticFieldDirectionX value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:maximumMagneticFieldDirectionX ;

    sh:severity sh:Violation .

era-sh:MaximumMagneticFieldDirectionXApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:MaximumMagneticField ;
    era:affectedProperty era:maximumMagneticFieldDirectionX ;
    era:scope "local" ;

    rdfs:comment "Applicable only for axle counters."@en ;
    # Source: inferred from the scope note in the ontology

    sh:message "maximumMagneticFieldDirectionX: This error is due to the MaximumMagneticField {?fieldLabel}, violating the rule: Applicable only for axle counters."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?fieldLabel WHERE {
        $this rdfs:label ?fieldLabel .
        FILTER NOT EXISTS { $this era:maximumMagneticFieldDirectionX ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:relatedToAxleCounters "true" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumMagneticFieldDirectionY a sh:PropertyShape ;
    era:affectedClass era:MaximumMagneticField ;
    era:affectedProperty era:maximumMagneticFieldDirectionY ;
    era:rinfIndex "1.1.1.1.7.9" ;
    era:scope "local" ;

    rdfs:comment "The maximum magnetic field limits allowed for axle counters (in dB µA/m) for a defined frequency band. Direction Y."@en ;
    # Source: ontology comment for maximumMagneticFieldDirectionY

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "maximumMagneticFieldDirectionY: The MaximumMagneticField must have at most one maximumMagneticFieldDirectionY value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:maximumMagneticFieldDirectionY ;

    sh:severity sh:Violation .

era-sh:MaximumMagneticFieldDirectionYApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:MaximumMagneticField ;
    era:affectedProperty era:maximumMagneticFieldDirectionY ;
    era:scope "local" ;

    rdfs:comment "Applicable only for axle counters."@en ;
    # Source: inferred from the scope note in the ontology

    sh:message "maximumMagneticFieldDirectionY: This error is due to the MaximumMagneticField {?fieldLabel}, violating the rule: Applicable only for axle counters."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?fieldLabel WHERE {
        $this rdfs:label ?fieldLabel .
        FILTER NOT EXISTS { $this era:maximumMagneticFieldDirectionY ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:relatedToAxleCounters "true" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumMagneticFieldDirectionZ a sh:PropertyShape ;
    era:affectedClass era:MaximumMagneticField ;
    era:affectedProperty era:maximumMagneticFieldDirectionZ ;
    era:rinfIndex "1.1.1.1.7.9" ;
    era:scope "local" ;

    rdfs:comment "The maximum magnetic field limits allowed for axle counters (in dB µA/m) for a defined frequency band. Direction Z."@en ;
    # Source: ontology comment for maximumMagneticFieldDirectionZ

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "maximumMagneticFieldDirectionZ: The MaximumMagneticField must have at most one maximumMagneticFieldDirectionZ value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:maximumMagneticFieldDirectionZ ;

    sh:severity sh:Violation .

era-sh:MaximumMagneticFieldDirectionZApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:MaximumMagneticField ;
    era:affectedProperty era:maximumMagneticFieldDirectionZ ;
    era:scope "local" ;

    rdfs:comment "Applicable only for axle counters."@en ;
    # Source: inferred from the scope note in the ontology

    sh:message "maximumMagneticFieldDirectionZ: This error is due to the MaximumMagneticField {?fieldLabel}, violating the rule: Applicable only for axle counters."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?fieldLabel WHERE {
        $this rdfs:label ?fieldLabel .
        FILTER NOT EXISTS { $this era:maximumMagneticFieldDirectionZ ?value } .
        # Additional check for the applicability condition
        FILTER EXISTS { $this era:relatedToAxleCounters "true" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumPermittedSpeed a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:maximumPermittedSpeed ;
    era:rinfIndex "1.1.1.1.2.5" ;
    era:scope "local" ;

    rdfs:comment "Nominal maximum operational speed on the line as a result of INF, ENE and CCS subsystem characteristics expressed in kilometres/hour."@en ;
    # Source: ontology comment for maximumPermittedSpeed

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "maximumPermittedSpeed: The CommonCharacteristicsSubset must have at most one maximumPermittedSpeed value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:maximumPermittedSpeed ;

    sh:severity sh:Violation .

era-sh:MaximumPermittedSpeedApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:maximumPermittedSpeed ;
    era:scope "local" ;

    rdfs:comment "Applicable when the maximum permitted speed is defined and the value is within the specified range."@en ;
    # Source: inferred from the context of maximum permitted speed requirements

    sh:message "maximumPermittedSpeed: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when the maximum permitted speed is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:maximumPermittedSpeed ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:CommonCharacteristicsSubset } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumServiceBrake a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumServiceBrake ;
    era:rinfIndex "4.7.7" ;
    era:scope "local" ;

    rdfs:comment "At maximum service brake: Stopping distance, Maximum deceleration, for the load condition 'design mass under normal payload' at the design maximum speed."@en ;
    # Source: ontology comment for maximumServiceBrake

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "maximumServiceBrake: The VehicleType must have at most one maximumServiceBrake value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:maximumServiceBrake ;

    sh:severity sh:Violation .

era-sh:MaximumServiceBrakeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumServiceBrake ;
    era:scope "local" ;

    rdfs:comment "Applicable when the maximum service brake is defined and the value is within the specified range."@en ;
    # Source: inferred from the context of maximum service brake requirements

    sh:message "maximumServiceBrake: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the maximum service brake is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:maximumServiceBrake ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumSpeedAndCantDeficiency a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumSpeedAndCantDeficiency ;
    era:rinfIndex "4.6.4" ;
    era:scope "local" ;

    rdfs:comment "Combination of maximum speed and maximum cant deficiency for which the vehicle was assessed."@en ;
    # Source: ontology comment for maximumSpeedAndCantDeficiency

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "maximumSpeedAndCantDeficiency: The VehicleType must have at most one maximumSpeedAndCantDeficiency value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:maximumSpeedAndCantDeficiency ;

    sh:severity sh:Violation .

era-sh:MaximumSpeedAndCantDeficiencyApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumSpeedAndCantDeficiency ;
    era:scope "local" ;

    rdfs:comment "Applicable when both maximum speed and maximum cant deficiency are defined and provided in the correct format."@en ;
    # Source: inferred from the context of maximum speed and cant deficiency requirements

    sh:message "maximumSpeedAndCantDeficiency: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when both maximum speed and maximum cant deficiency are defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:maximumSpeedAndCantDeficiency ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:maximumPermittedSpeed ?speed } .
        FILTER EXISTS { $this era:maximumCantDeficiency ?cant } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumSpeedEmpty a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumSpeedEmpty ;
    era:rinfIndex "4.1.2.2" ;
    era:scope "local" ;

    rdfs:comment "Maximum speed when empty."@en ;
    # Source: ontology comment for maximumSpeedEmpty

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "maximumSpeedEmpty: The VehicleType must have at most one maximumSpeedEmpty value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:maximumSpeedEmpty ;

    sh:severity sh:Violation .

era-sh:MaximumSpeedEmptyApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:maximumSpeedEmpty ;
    era:scope "local" ;

    rdfs:comment "Applicable when the maximum speed when empty is defined and the value is within the specified range."@en ;
    # Source: inferred from the context of maximum speed requirements

    sh:message "maximumSpeedEmpty: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the maximum speed when empty is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:maximumSpeedEmpty ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MaximumTrainDeceleration a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:maximumTrainDeceleration ;
    era:rinfIndex "1.1.1.1.6.1" ;
    era:scope "local" ;

    rdfs:comment "Limit for longitudinal track resistance given as a maximum allowed train deceleration and expressed in metres per square second."@en ;
    # Source: ontology comment for maximumTrainDeceleration

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "maximumTrainDeceleration: The RunningTrack must have at most one maximumTrainDeceleration value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:maximumTrainDeceleration ;

    sh:severity sh:Violation .

era-sh:MaximumTrainDecelerationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:maximumTrainDeceleration ;
    era:scope "local" ;

    rdfs:comment "Applicable when the specific track is part of a line that is in the scope of INF TSI."@en ;
    # Source: dependency note in ontology

    sh:message "maximumTrainDeceleration: This error is due to the RunningTrack {?trackLabel}, violating the rule: Applicable only when the specific track is part of a line which is in the scope of TSI."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:maximumTrainDeceleration ?value } .
        # Additional check for the applicability condition
        FILTER NOT EXISTS { $this era:inScopeOfTSI "true" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MeasuredDistance a sh:PropertyShape ;
    era:affectedClass era:KilometricPost ;
    era:affectedProperty era:measuredDistance ;
    era:scope "local" ;

    rdfs:comment "Measured distance from the origin of the LRS."@en ;
    # Source: ontology comment for measuredDistance

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "measuredDistance: The KilometricPost must have at most one measuredDistance value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:measuredDistance ;

    sh:severity sh:Violation .

era-sh:MeasuredDistanceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:KilometricPost ;
    era:affectedProperty era:measuredDistance ;
    era:scope "local" ;

    rdfs:comment "Applicable when the measured distance is defined and corresponds to the correct format."@en ;
    # Source: inferred from the context of measured distance requirements

    sh:message "measuredDistance: This error is due to the KilometricPost {?postLabel}, violating the rule: Applicable only when the measured distance is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?postLabel WHERE {
        $this rdfs:label ?postLabel .
        FILTER NOT EXISTS { $this era:measuredDistance ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:KilometricPost } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MeetsRequirementVehicleAuthorisation a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:meetsRequirementVehicleAuthorisation ;
    era:rinfIndex "4.1.8" ;
    era:scope "local" ;

    rdfs:comment "Type meets the requirements necessary for validity of the vehicle authorisation granted by one Member State in other MSs."@en ;
    # Source: ontology comment for meetsRequirementVehicleAuthorisation

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "meetsRequirementVehicleAuthorisation: The VehicleType must have at most one meetsRequirementVehicleAuthorisation value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:meetsRequirementVehicleAuthorisation ;

    sh:severity sh:Violation .

era-sh:MeetsRequirementVehicleAuthorisationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:meetsRequirementVehicleAuthorisation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the VehicleType is defined and has a specified requirement for vehicle authorisation."@en ;
    # Source: inferred from the context of vehicle authorisation requirements

    sh:message "meetsRequirementVehicleAuthorisation: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the VehicleType is defined and has a specified requirement for vehicle authorisation."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:meetsRequirementVehicleAuthorisation ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinAxleLoad a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minAxleLoad ;
    era:rinfIndex "4.14.2.10" ;
    era:scope "local" ;

    rdfs:comment "Minimum permitted axle load, given in tons."@en ;
    # Source: ontology comment for minAxleLoad

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "minAxleLoad: The VehicleType must have at most one minAxleLoad value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:minAxleLoad ;

    sh:severity sh:Violation .

era-sh:MinAxleLoadApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minAxleLoad ;
    era:scope "local" ;

    rdfs:comment "Applicable when the minimum axle load is defined and corresponds to the correct format."@en ;
    # Source: inferred from the context of minimum axle load requirements

    sh:message "minAxleLoad: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the minimum axle load is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:minAxleLoad ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinDistConsecutiveAxles a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minDistConsecutiveAxles ;
    era:rinfIndex "4.14.2.2" ;
    era:scope "local" ;

    rdfs:comment "Indication of minimum permitted distance between two consecutive axles, given in millimetres."@en ;
    # Source: ontology comment for minDistConsecutiveAxles

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "minDistConsecutiveAxles: The VehicleType must have at most one minDistConsecutiveAxles value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:minDistConsecutiveAxles ;

    sh:severity sh:Violation .

era-sh:MinDistConsecutiveAxlesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minDistConsecutiveAxles ;
    era:scope "local" ;

    rdfs:comment "Applicable when the minimum distance between consecutive axles is defined and corresponds to the correct format."@en ;
    # Source: inferred from the context of minimum distance requirements

    sh:message "minDistConsecutiveAxles: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the minimum distance between consecutive axles is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:minDistConsecutiveAxles ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinDistFirstLastAxle a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minDistFirstLastAxle ;
    era:rinfIndex "4.14.2.3" ;
    era:scope "local" ;

    rdfs:comment "Indication of minimum permitted distance between first and last axles, given in millimetres."@en ;
    # Source: ontology comment for minDistFirstLastAxle

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "minDistFirstLastAxle: The VehicleType must have at most one minDistFirstLastAxle value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:minDistFirstLastAxle ;

    sh:severity sh:Violation .

era-sh:MinDistFirstLastAxleApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minDistFirstLastAxle ;
    era:scope "local" ;

    rdfs:comment "Applicable when the minimum distance between first and last axles is defined and corresponds to the correct format."@en ;
    # Source: inferred from the context of minimum distance requirements

    sh:message "minDistFirstLastAxle: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the minimum distance between first and last axles is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:minDistFirstLastAxle ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinFlangeHeight a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minFlangeHeight ;
    era:rinfIndex "1.1.1.3.7.9" ;
    era:scope "local" ;

    rdfs:comment "Indication of height of flange given in millimetres."@en ;
    # Source: ontology comment for minFlangeHeight

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "minFlangeHeight: The VehicleType must have at most one minFlangeHeight value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:minFlangeHeight ;

    sh:severity sh:Violation .

era-sh:MinFlangeHeightApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minFlangeHeight ;
    era:scope "local" ;

    rdfs:comment "Applicable when the minimum flange height is defined and corresponds to the correct format."@en ;
    # Source: inferred from the context of minimum flange height requirements

    sh:message "minFlangeHeight: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the minimum flange height is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:minFlangeHeight ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinFlangeThickness a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minFlangeThickness ;
    era:rinfIndex "4.14.2.7" ;
    era:scope "local" ;

    rdfs:comment "Indication of flange thickness given in millimetres."@en ;
    # Source: ontology comment for minFlangeThickness

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "minFlangeThickness: The VehicleType must have at most one minFlangeThickness value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:minFlangeThickness ;

    sh:severity sh:Violation .

era-sh:MinFlangeThicknessApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minFlangeThickness ;
    era:scope "local" ;

    rdfs:comment "Applicable when the minimum flange thickness is defined and corresponds to the correct format."@en ;
    # Source: inferred from the context of minimum flange thickness requirements

    sh:message "minFlangeThickness: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the minimum flange thickness is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:minFlangeThickness ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinRimWidth a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minRimWidth ;
    era:rinfIndex "4.14.2.5" ;
    era:scope "local" ;

    rdfs:comment "Indication of minimum permitted width of the rim given in millimetres."@en ;
    # Source: ontology comment for minRimWidth

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "minRimWidth: The VehicleType must have at most one minRimWidth value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:minRimWidth ;

    sh:severity sh:Violation .

era-sh:MinRimWidthApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minRimWidth ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the selected option for parameter 1.1.1.3.7.1.1 is ‘wheel detector’."@en ;
    # Source: inferred from the applicability conditions in RAG

    sh:message "minRimWidth: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the selected option for wheel detection is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:minRimWidth ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:wheelDetectionType "wheel detector" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinVehicleInputCapacitance a sh:PropertyShape ;
    era:affectedClass era:MinVehicleImpedance ;
    era:affectedProperty era:minVehicleInputCapacitance ;
    era:rinfIndex "4.6.4" ;
    era:scope "local" ;

    rdfs:comment "Indication of minimum vehicle input capacitance for the selected DC voltage."@en ;
    # Source: ontology comment for minVehicleInputCapacitance

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "minVehicleInputCapacitance: The MinVehicleImpedance must have at most one minVehicleInputCapacitance value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:minVehicleInputCapacitance ;

    sh:severity sh:Violation .

era-sh:MinVehicleInputCapacitanceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:MinVehicleImpedance ;
    era:affectedProperty era:minVehicleInputCapacitance ;
    era:scope "local" ;

    rdfs:comment "Applicable when the minimum vehicle input capacitance is defined and corresponds to the correct format."@en ;
    # Source: inferred from the context of minimum vehicle input capacitance requirements

    sh:message "minVehicleInputCapacitance: This error is due to the MinVehicleImpedance {?impedanceLabel}, violating the rule: Applicable only when the minimum vehicle input capacitance is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?impedanceLabel WHERE {
        $this rdfs:label ?impedanceLabel .
        FILTER NOT EXISTS { $this era:minVehicleInputCapacitance ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:MinVehicleImpedance } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinVehicleInputImpedance a sh:PropertyShape ;
    era:affectedClass era:MinVehicleImpedance ;
    era:affectedProperty era:minVehicleInputImpedance ;
    era:rinfIndex "4.6.4" ;
    era:scope "local" ;

    rdfs:comment "Indication of minimum vehicle input impedance for the selected DC voltage."@en ;
    # Source: ontology comment for minVehicleInputImpedance

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "minVehicleInputImpedance: The MinVehicleImpedance must have at most one minVehicleInputImpedance value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:minVehicleInputImpedance ;

    sh:severity sh:Violation .

era-sh:MinVehicleInputImpedanceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:MinVehicleImpedance ;
    era:affectedProperty era:minVehicleInputImpedance ;
    era:scope "local" ;

    rdfs:comment "Applicable when the minimum vehicle input impedance is defined and corresponds to the correct format."@en ;
    # Source: inferred from the context of minimum vehicle input impedance requirements

    sh:message "minVehicleInputImpedance: This error is due to the MinVehicleImpedance {?impedanceLabel}, violating the rule: Applicable only when the minimum vehicle input impedance is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?impedanceLabel WHERE {
        $this rdfs:label ?impedanceLabel .
        FILTER NOT EXISTS { $this era:minVehicleInputImpedance ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:MinVehicleImpedance } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinWheelDiameter a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minWheelDiameter ;
    era:rinfIndex "4.14.2.6" ;
    era:scope "local" ;

    rdfs:comment "Indication of minimum permitted wheel diameter, given in millimetres."@en ;
    # Source: ontology comment for minWheelDiameter

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "minWheelDiameter: The VehicleType must have at most one minWheelDiameter value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:minWheelDiameter ;

    sh:severity sh:Violation .

era-sh:MinWheelDiameterApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minWheelDiameter ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the selected option for parameter 1.1.1.3.7.1.1 is ‘wheel detector’."@en ;
    # Source: inferred from the applicability conditions in RAG

    sh:message "minWheelDiameter: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the selected option for wheel detection is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:minWheelDiameter ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:wheelDetectionType "wheel detector" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinimumConcaveVerticalRadius a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minimumConcaveVerticalRadius ;
    era:rinfIndex "4.8.6" ;
    era:scope "local" ;

    rdfs:comment "Minimum vertical concave curve radius capability."@en ;
    # Source: ontology comment for minimumConcaveVerticalRadius

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "minimumConcaveVerticalRadius: The VehicleType must have at most one minimumConcaveVerticalRadius value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:minimumConcaveVerticalRadius ;

    sh:severity sh:Violation .

era-sh:MinimumConcaveVerticalRadiusApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minimumConcaveVerticalRadius ;
    era:scope "local" ;

    rdfs:comment "Applicable when the minimum concave vertical radius is defined and corresponds to the correct format."@en ;
    # Source: inferred from the context of minimum concave vertical radius requirements

    sh:message "minimumConcaveVerticalRadius: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the minimum concave vertical radius is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:minimumConcaveVerticalRadius ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinimumContactWireHeight a sh:PropertyShape ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:minContactWireHeight ;
    era:rinfIndex "4.10.5" ;
    era:scope "local" ;

    rdfs:comment "Indication of the minimum contact wire height expressed in metres."@en ;
    # Source: ontology comment for minimumContactWireHeight

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "minimumContactWireHeight: The ContactLineSystem must have at most one minimumContactWireHeight value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:minContactWireHeight ;

    sh:severity sh:Violation .

era-sh:MinimumContactWireHeightApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:minContactWireHeight ;
    era:scope "local" ;

    rdfs:comment "Applicable only if 'Overhead contact line (OCL)' is selected in parameter 1.1.1.2.2.1.1."@en ;
    # Source: RAG applicability condition for minimum contact wire height

    sh:message "minimumContactWireHeight: This error is due to the ContactLineSystem {?systemLabel}, violating the rule: Applicable only if 'Overhead contact line (OCL)' is selected."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:minContactWireHeight ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:overheadContactLine "OCL" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinimumConvexVerticalRadius a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minimumConvexVerticalRadius ;
    era:rinfIndex "4.8.5" ;
    era:scope "local" ;

    rdfs:comment "Minimum vertical convex curve radius capability."@en ;
    # Source: ontology comment for minimumConvexVerticalRadius

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "minimumConvexVerticalRadius: The VehicleType must have at most one minimumConvexVerticalRadius value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:minimumConvexVerticalRadius ;

    sh:severity sh:Violation .

era-sh:MinimumConvexVerticalRadiusApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minimumConvexVerticalRadius ;
    era:scope "local" ;

    rdfs:comment "Applicable when the minimum convex vertical radius is defined and corresponds to the correct format."@en ;
    # Source: inferred from the context of minimum convex vertical radius requirements

    sh:message "minimumConvexVerticalRadius: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the minimum convex vertical radius is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:minimumConvexVerticalRadius ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinimumHorizontalRadius a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:minimumHorizontalRadius ;
    era:rinfIndex "4.8.4" ;
    era:scope "local" ;

    rdfs:comment "Radius of the smallest horizontal curve, expressed in metres."@en ;
    # Source: ontology comment for minimumHorizontalRadius

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "minimumHorizontalRadius: The RunningTrack must have at most one minimumHorizontalRadius value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:minimumHorizontalRadius ;

    sh:severity sh:Violation .

era-sh:MinimumHorizontalRadiusApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:minimumHorizontalRadius ;
    era:scope "local" ;

    rdfs:comment "Applicable when the minimum horizontal radius is defined and corresponds to the correct format."@en ;
    # Source: inferred from the context of minimum horizontal radius requirements

    sh:message "minimumHorizontalRadius: This error is due to the RunningTrack {?trackLabel}, violating the rule: Applicable only when the minimum horizontal radius is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?trackLabel WHERE {
        $this rdfs:label ?trackLabel .
        FILTER NOT EXISTS { $this era:minimumHorizontalRadius ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:RunningTrack } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinimumVerticalRadiusCrest a sh:PropertyShape ;
    era:affectedClass era:MinimumVerticalRadius ;
    era:affectedProperty era:minimumVerticalRadiusCrest ;
    era:rinfIndex "1.2.2.0.3.3" ;
    era:scope "local" ;

    rdfs:comment "Part of the minimum radius of vertical curve that indicates the crest."@en ;
    # Source: ontology comment for minimumVerticalRadiusCrest

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "minimumVerticalRadiusCrest: The MinimumVerticalRadius must have at most one minimumVerticalRadiusCrest value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:minimumVerticalRadiusCrest ;

    sh:severity sh:Violation .

era-sh:MinimumVerticalRadiusCrestApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:MinimumVerticalRadius ;
    era:affectedProperty era:minimumVerticalRadiusCrest ;
    era:scope "local" ;

    rdfs:comment "Applicable when the minimum vertical radius crest is defined and corresponds to the correct format."@en ;
    # Source: inferred from the context of minimum vertical radius requirements

    sh:message "minimumVerticalRadiusCrest: This error is due to the MinimumVerticalRadius {?radiusLabel}, violating the rule: Applicable only when the minimum vertical radius crest is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?radiusLabel WHERE {
        $this rdfs:label ?radiusLabel .
        FILTER NOT EXISTS { $this era:minimumVerticalRadiusCrest ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:MinimumVerticalRadius } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinimumVerticalRadiusHollow a sh:PropertyShape ;
    era:affectedClass era:MinimumVerticalRadius ;
    era:affectedProperty era:minimumVerticalRadiusHollow ;
    era:scope "local" ;

    rdfs:comment "Part of the minimum radius of vertical curve that indicates the hollow."@en ;
    # Source: ontology comment for minimumVerticalRadiusHollow

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "minimumVerticalRadiusHollow: The MinimumVerticalRadius must have at most one minimumVerticalRadiusHollow value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:minimumVerticalRadiusHollow ;

    sh:severity sh:Violation .

era-sh:MinimumVerticalRadiusHollowApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:MinimumVerticalRadius ;
    era:affectedProperty era:minimumVerticalRadiusHollow ;
    era:scope "local" ;

    rdfs:comment "Applicable when the minimum vertical radius hollow is defined and corresponds to the correct format."@en ;
    # Source: inferred from the context of minimum vertical radius requirements

    sh:message "minimumVerticalRadiusHollow: This error is due to the MinimumVerticalRadius {?radiusLabel}, violating the rule: Applicable only when the minimum vertical radius hollow is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?radiusLabel WHERE {
        $this rdfs:label ?radiusLabel .
        FILTER NOT EXISTS { $this era:minimumVerticalRadiusHollow ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:MinimumVerticalRadius } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:MinimumWheelDiameter a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minWheelDiameter ;
    era:rinfIndex "4.8.2" ;
    era:scope "local" ;

    rdfs:comment "Indication of minimum permitted wheel diameter, given in millimetres."@en ;
    # Source: ontology comment for minimumWheelDiameter

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "minWheelDiameter: The VehicleType must have at most one minWheelDiameter value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:minWheelDiameter ;

    sh:severity sh:Violation .

era-sh:MinimumWheelDiameterApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:minWheelDiameter ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the selected option for parameter 1.1.1.1.5.2 is ‘Y’."@en ;
    # Source: inferred from the applicability conditions in RAG

    sh:message "minWheelDiameter: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the minimum wheel diameter is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:minWheelDiameter ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:minimumWheelDiameter "Y" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:NationalLoadCapability a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:nationalLoadCapability ;
    era:rinfIndex "1.1.1.1.2.4.1" ;
    era:scope "local" ;

    rdfs:comment "National classification for load capability."@en ;
    # Source: ontology comment for nationalLoadCapability

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "nationalLoadCapability: The CommonCharacteristicsSubset or RunningTrack must have at most one nationalLoadCapability value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:nationalLoadCapability ;

    sh:severity sh:Violation .

era-sh:NationalLoadCapabilityApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:nationalLoadCapability ;
    era:scope "local" ;

    rdfs:comment "Applicable when the national load capability is defined and corresponds to the correct format."@en ;
    # Source: inferred from the context of national load capability requirements

    sh:message "nationalLoadCapability: This error is due to the CommonCharacteristicsSubset or RunningTrack {?label}, violating the rule: Applicable only when the national load capability is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:nationalLoadCapability ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:CommonCharacteristicsSubset } .
        # or
        FILTER EXISTS { $this a era:RunningTrack } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:NationalRollingStockFireCategory a sh:PropertyShape ;
    era:affectedClass era:Tunnel, era:CommonCharacteristicsSubset ;
    era:affectedProperty era:nationalRollingStockFireCategory ;
    era:rinfIndex "1.1.1.1.8.11" ;
    era:scope "local" ;

    rdfs:comment "Categorisation of how a passenger train with a fire on board will continue to operate for a defined time period - according to national rules if they exist."@en ;
    # Source: ontology comment for nationalRollingStockFireCategory

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "nationalRollingStockFireCategory: The Tunnel or CommonCharacteristicsSubset must have at most one nationalRollingStockFireCategory value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:nationalRollingStockFireCategory ;

    sh:severity sh:Violation .

era-sh:NationalRollingStockFireCategoryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel, era:CommonCharacteristicsSubset ;
    era:affectedProperty era:nationalRollingStockFireCategory ;
    era:scope "local" ;

    rdfs:comment "Applicable when the national rolling stock fire category is defined and corresponds to the correct format."@en ;
    # Source: inferred from the context of national rolling stock fire category requirements

    sh:message "nationalRollingStockFireCategory: This error is due to the Tunnel or CommonCharacteristicsSubset {?label}, violating the rule: Applicable only when the national rolling stock fire category is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:nationalRollingStockFireCategory ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:Tunnel } .
        # or
        FILTER EXISTS { $this a era:CommonCharacteristicsSubset } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:NationalValuesBrakeModel a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:nationalValuesBrakeModel ;
    era:rinfIndex "1.1.1.3.2.16.13" ;
    era:scope "local" ;

    rdfs:comment "Set of parameters for adapting the braking curves calculated by the ETCS on-board system to match accuracy, performance and safety margins imposed by the infrastructure manager."@en ;
    # Source: ontology comment for nationalValuesBrakeModel

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "nationalValuesBrakeModel: The CommonCharacteristicsSubset or RunningTrack must have at most one nationalValuesBrakeModel value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:nationalValuesBrakeModel ;

    sh:severity sh:Violation .

era-sh:NationalValuesBrakeModelApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:nationalValuesBrakeModel ;
    era:scope "local" ;

    rdfs:comment "Applicable when ETCS is present; a value must be provided."@en ;
    # Source: inferred from the dependency note in the ontology

    sh:message "nationalValuesBrakeModel: This error is due to the CommonCharacteristicsSubset or RunningTrack {?label}, violating the rule: A value has to be provided when ETCS is present."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:nationalValuesBrakeModel ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:etcsPresent "Y" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:NonCodedRestrictions a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:nonCodedRestrictions ;
    era:scope "local" ;

    rdfs:comment "Denotes a non-coded restrictions for this particular vehicleType, as a set of sentences."@en ;
    # Source: ontology comment for nonCodedRestrictions

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "nonCodedRestrictions: The VehicleType must have at most one nonCodedRestrictions value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:nonCodedRestrictions ;

    sh:severity sh:Violation .

era-sh:NonCodedRestrictionsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:nonCodedRestrictions ;
    era:scope "local" ;

    rdfs:comment "Applicable when the non-coded restrictions are defined and correspond to the correct format."@en ;
    # Source: inferred from the context of non-coded restrictions requirements

    sh:message "nonCodedRestrictions: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the non-coded restrictions are defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:nonCodedRestrictions ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:NumberElementsRakeFreightWagons a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:numberElementsRakeFreightWagons ;
    era:rinfIndex "4.1.6" ;
    era:scope "local" ;

    rdfs:comment "Number of elements in the rake of freight wagons (only for subcategory 'rake of freight wagons')."@en ;
    # Source: ontology comment for numberElementsRakeFreightWagons

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "numberElementsRakeFreightWagons: The VehicleType must have at most one numberElementsRakeFreightWagons value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:numberElementsRakeFreightWagons ;

    sh:severity sh:Violation .

era-sh:NumberElementsRakeFreightWagonsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:numberElementsRakeFreightWagons ;
    era:scope "local" ;

    rdfs:comment "Applicable only for the subcategory 'rake of freight wagons'."@en ;
    # Source: inferred from the context of the property usage

    sh:message "numberElementsRakeFreightWagons: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only for the subcategory 'rake of freight wagons'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:numberElementsRakeFreightWagons ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
        # Assuming there is a property indicating the subcategory
        FILTER EXISTS { $this era:subcategory "rake of freight wagons" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:NumberOfPantographsInContactWithOCL a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:numberOfPantographsInContactWithOCL ;
    era:rinfIndex "4.10.7" ;
    era:scope "local" ;

    rdfs:comment "Number of pantographs in contact with the overhead contact line (OCL) (to be indicated for each energy supply system the vehicle is equipped for)."@en ;
    # Source: ontology comment for numberOfPantographsInContactWithOCL

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "numberOfPantographsInContactWithOCL: The VehicleType must have at most one numberOfPantographsInContactWithOCL value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:numberOfPantographsInContactWithOCL ;

    sh:severity sh:Violation .

era-sh:NumberOfPantographsInContactWithOCLApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:numberOfPantographsInContactWithOCL ;
    era:scope "local" ;

    rdfs:comment "Applicable when the number of pantographs in contact with OCL is defined and corresponds to the correct format."@en ;
    # Source: inferred from the context of number of pantographs requirements

    sh:message "numberOfPantographsInContactWithOCL: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the number of pantographs in contact with OCL is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:numberOfPantographsInContactWithOCL ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:NumberOfToilets a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:numberOfToilets ;
    era:rinfIndex "4.12.1.2" ;
    era:scope "local" ;

    rdfs:comment "Number of toilets."@en ;
    # Source: ontology comment for numberOfToilets

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "numberOfToilets: The VehicleType must have at most one numberOfToilets value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:numberOfToilets ;

    sh:severity sh:Violation .

era-sh:NumberOfToiletsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:numberOfToilets ;
    era:scope "local" ;

    rdfs:comment "Applicable when the number of toilets is defined and corresponds to the correct format."@en ;
    # Source: inferred from the context of number of toilets requirements

    sh:message "numberOfToilets: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the number of toilets is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:numberOfToilets ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OclType a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:oclType ;
    era:rinfIndex "4.10.9" ;
    era:scope "local" ;

    rdfs:comment "OCL type."@en ;
    # Source: ontology comment for oclType

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "oclType: The VehicleType must have at most one oclType value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:oclType ;

    sh:severity sh:Violation .

era-sh:OclTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:oclType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OCL type is defined and corresponds to the correct format."@en ;
    # Source: inferred from the context of OCL type requirements

    sh:message "oclType: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the OCL type is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:oclType ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OffsetFromKilometricPost a sh:PropertyShape ;
    era:affectedClass era:LinearPositioningSystemCoordinate ;
    era:affectedProperty era:offsetFromKilometricPost ;
    era:scope "local" ;

    rdfs:comment "Relative distance from a reference kilometric post."@en ;
    # Source: ontology comment for offsetFromKilometricPost

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "offsetFromKilometricPost: The LinearPositioningSystemCoordinate must have at most one offsetFromKilometricPost value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:offsetFromKilometricPost ;

    sh:severity sh:Violation .

era-sh:OffsetFromKilometricPostApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LinearPositioningSystemCoordinate ;
    era:affectedProperty era:offsetFromKilometricPost ;
    era:scope "local" ;

    rdfs:comment "Applicable when the offset from kilometric post is defined and corresponds to the correct format."@en ;
    # Source: inferred from the context of offset requirements

    sh:message "offsetFromKilometricPost: This error is due to the LinearPositioningSystemCoordinate {?coordinateLabel}, violating the rule: Applicable only when the offset from kilometric post is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?coordinateLabel WHERE {
        $this rdfs:label ?coordinateLabel .
        FILTER NOT EXISTS { $this era:offsetFromKilometricPost ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:LinearPositioningSystemCoordinate } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OffsetFromOrigin a sh:PropertyShape ;
    era:affectedClass era:TopologicalCoordinate ;
    era:affectedProperty era:offsetFromOrigin ;
    era:scope "local" ;

    rdfs:comment "Offset from the origin in a topological coordinate."@en ;
    # Source: ontology comment for offsetFromOrigin

    sh:datatype xsd:positiveInteger ;
    # Source: ontology range is xsd:positiveInteger (http://www.w3.org/2001/XMLSchema#positiveInteger)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "offsetFromOrigin: The TopologicalCoordinate must have at most one offsetFromOrigin value that is a positive integer. This error may be due to having more than one value or having a value that is not a valid positive integer."@en ;

    sh:path era:offsetFromOrigin ;

    sh:severity sh:Violation .

era-sh:OffsetFromOriginApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TopologicalCoordinate ;
    era:affectedProperty era:offsetFromOrigin ;
    era:scope "local" ;

    rdfs:comment "Applicable when the offset from origin is defined and corresponds to the correct format."@en ;
    # Source: inferred from the context of offset requirements in RAG

    sh:message "offsetFromOrigin: This error is due to the TopologicalCoordinate {?coordinateLabel}, violating the rule: Applicable only when the offset from origin is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?coordinateLabel WHERE {
        $this rdfs:label ?coordinateLabel .
        FILTER NOT EXISTS { $this era:offsetFromOrigin ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:TopologicalCoordinate } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OpName a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:opName ;
    era:rinfIndex "1.2.0.0.0.1" ;
    era:scope "local" ;

    rdfs:comment "Name normally related to the town or village or to traffic control purpose."@en ;
    # Source: ontology comment for opName

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "opName: The OperationalPoint must have at most one opName value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:opName ;

    sh:severity sh:Violation .

era-sh:OpNameApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:opName ;
    era:scope "local" ;

    rdfs:comment "Applicable when the name of the operational point is defined and corresponds to the correct format."@en ;
    # Source: inferred from the context of operational point naming requirements

    sh:message "opName: This error is due to the OperationalPoint {?opLabel}, violating the rule: Applicable only when the name of the operational point is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?opLabel WHERE {
        $this rdfs:label ?opLabel .
        FILTER NOT EXISTS { $this era:opName ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:OperationalPoint } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OpTypeGaugeChangeover a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:opTypeGaugeChangeover ;
    era:rinfIndex "1.2.0.0.0.4.1" ;
    era:scope "local" ;

    rdfs:comment "Type of track gauge changeover facility."@en ;
    # Source: ontology comment for opTypeGaugeChangeover

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "opTypeGaugeChangeover: The OperationalPoint must have at most one opTypeGaugeChangeover value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:opTypeGaugeChangeover ;

    sh:severity sh:Violation .

era-sh:OpTypeGaugeChangeoverApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:opTypeGaugeChangeover ;
    era:scope "local" ;

    rdfs:comment "Applicable when the type of track gauge changeover facility is defined and corresponds to the correct format."@en ;
    # Source: inferred from the context of track gauge changeover requirements

    sh:message "opTypeGaugeChangeover: This error is due to the OperationalPoint {?opLabel}, violating the rule: Applicable only when the type of track gauge changeover facility is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?opLabel WHERE {
        $this rdfs:label ?opLabel .
        FILTER NOT EXISTS { $this era:opTypeGaugeChangeover ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:OperationalPoint } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:OrganisationCode a sh:PropertyShape ;
    era:affectedClass era:OrganisationRole ;
    era:affectedProperty era:organisationCode ;
    era:rinfIndex "1.2.1.0.5.1" ; # Example index, can vary based on specific context
    era:scope "local" ;

    rdfs:comment "Four alpha-numeric code allocated by ERA to a body. It represents the Infrastructure Manager (IM) code in RINF."@en ;
    # Source: ontology comment for organisationCode

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:pattern "^[A-Z0-9]{4}$" ;
    # Inferred: format for the organisation code is a four-character alphanumeric string

    sh:message "organisationCode: The OrganisationRole must have at most one organisationCode value that is a valid four-character alphanumeric string. This error may be due to having more than one value or having a value that does not match the required format."@en ;

    sh:path era:organisationCode ;

    sh:severity sh:Violation .

era-sh:OrganisationCodeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OrganisationRole ;
    era:affectedProperty era:organisationCode ;
    era:scope "local" ;

    rdfs:comment "Applicable when the organisation code is defined and corresponds to the correct format."@en ;
    # Source: inferred from the context of organisation code requirements

    sh:message "organisationCode: This error is due to the OrganisationRole {?roleLabel}, violating the rule: Applicable only when the organisation code is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?roleLabel WHERE {
        $this rdfs:label ?roleLabel .
        FILTER NOT EXISTS { $this era:organisationCode ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:OrganisationRole } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ParameterValue a sh:PropertyShape ;
    era:affectedClass era:ParameterApplicability ;
    era:affectedProperty era:parameterValue ;
    era:scope "local" ;

    rdfs:comment "This property is used to link an applicability to the precise value of the parameter."@en ;
    # Source: ontology comment for parameterValue

    sh:datatype rdfs:Literal ;
    # Source: ontology range is rdfs:Literal

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "parameterValue: The ParameterApplicability must have at most one parameterValue that is a literal. This error may be due to having more than one value or having a value that is not a valid literal."@en ;

    sh:path era:parameterValue ;

    sh:severity sh:Violation .

era-sh:ParameterValueApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ParameterApplicability ;
    era:affectedProperty era:parameterValue ;
    era:scope "local" ;

    rdfs:comment "Applicable when the parameter value is defined and corresponds to the correct format."@en ;
    # Source: inferred from the context of parameter value requirements

    sh:message "parameterValue: This error is due to the ParameterApplicability {?applicabilityLabel}, violating the rule: Applicable only when the parameter value is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?applicabilityLabel WHERE {
        $this rdfs:label ?applicabilityLabel .
        FILTER NOT EXISTS { $this era:parameterValue ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:ParameterApplicability } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ParkingBrake a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:parkingBrake ;
    era:rinfIndex "4.7.3" ;
    era:scope "local" ;

    rdfs:comment "Indicates whether all vehicles of this type must be equipped with a parking brake."@en ;
    # Source: ontology comment for parkingBrake

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "parkingBrake: The VehicleType must have at most one parkingBrake value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:parkingBrake ;

    sh:severity sh:Violation .

era-sh:ParkingBrakeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:parkingBrake ;
    era:scope "local" ;

    rdfs:comment "Applicable when the parking brake requirement is defined for the vehicle type."@en ;
    # Source: inferred from the context of parking brake requirements

    sh:message "parkingBrake: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the parking brake requirement is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:parkingBrake ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ParkingBrakeMandatory a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:parkingBrakeMandatory ;
    era:rinfIndex "4.7.3.1" ;
    era:scope "local" ;

    rdfs:comment "All vehicles of this type must be equipped with a parking brake (parking brake mandatory for vehicles of this type)."@en ;
    # Source: ontology comment for parkingBrakeMandatory

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "parkingBrakeMandatory: The VehicleType must have at most one parkingBrakeMandatory value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:parkingBrakeMandatory ;

    sh:severity sh:Violation .

era-sh:ParkingBrakeMandatoryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:parkingBrakeMandatory ;
    era:scope "local" ;

    rdfs:comment "Applicable when the parking brake mandatory requirement is defined for the vehicle type."@en ;
    # Source: inferred from the context of parking brake requirements

    sh:message "parkingBrakeMandatory: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the parking brake mandatory requirement is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:parkingBrakeMandatory ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ParkingBrakeMaximumGradient a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:parkingBrakeMaximumGradient ;
    era:rinfIndex "4.7.3.3" ;
    era:scope "local" ;

    rdfs:comment "Maximum gradient on which the unit is kept immobilised by the parking brake alone (if the vehicle is fitted with it)."@en ;
    # Source: ontology comment for parkingBrakeMaximumGradient

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "parkingBrakeMaximumGradient: The VehicleType must have at most one parkingBrakeMaximumGradient value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:parkingBrakeMaximumGradient ;

    sh:severity sh:Violation .

era-sh:ParkingBrakeMaximumGradientApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:parkingBrakeMaximumGradient ;
    era:scope "local" ;

    rdfs:comment "Applicable when the parking brake maximum gradient requirement is defined for the vehicle type."@en ;
    # Source: inferred from the context of parking brake maximum gradient requirements

    sh:message "parkingBrakeMaximumGradient: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the parking brake maximum gradient requirement is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:parkingBrakeMaximumGradient ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PassByNoiseLevel a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:passByNoiseLevel ;
    era:rinfIndex "4.11.1" ;
    era:scope "local" ;

    rdfs:comment "Pass-by noise level given in dB(A)."@en ;
    # Source: ontology comment for passByNoiseLevel

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "passByNoiseLevel: The VehicleType must have at most one passByNoiseLevel value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:passByNoiseLevel ;

    sh:severity sh:Violation .

era-sh:PassByNoiseLevelApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:passByNoiseLevel ;
    era:scope "local" ;

    rdfs:comment "Applicable when the pass-by noise level requirement is defined for the vehicle type."@en ;
    # Source: inferred from the context of pass-by noise level requirements

    sh:message "passByNoiseLevel: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the pass-by noise level requirement is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:passByNoiseLevel ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PermissiblePayload a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:permissiblePayload ;
    era:rinfIndex "4.5.1" ;
    era:scope "local" ;

    rdfs:comment "Permissible payload for different line categories."@en ;
    # Source: ontology comment for permissiblePayload

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "permissiblePayload: The VehicleType must have at most one permissiblePayload value that is a string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:permissiblePayload ;

    sh:severity sh:Violation .

era-sh:PermissiblePayloadApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:permissiblePayload ;
    era:scope "local" ;

    rdfs:comment "Applicable when the permissible payload requirement is defined for the vehicle type."@en ;
    # Source: inferred from the context of permissible payload requirements

    sh:message "permissiblePayload: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the permissible payload requirement is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:permissiblePayload ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PermissionChargingElectricEnergyTractionStandstill a sh:PropertyShape ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:permissionChargingElectricEnergyTractionStandstill ;
    era:rinfIndex "1.2.1.0.7.1" ;
    era:scope "local" ;

    rdfs:comment "Point at which IM authorises charging of electric energy storage for traction purposes at standstill."@en ;
    # Source: ontology comment for permissionChargingElectricEnergyTractionStandstill

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "permissionChargingElectricEnergyTractionStandstill: The ContactLineSystem must have at most one permissionChargingElectricEnergyTractionStandstill value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:permissionChargingElectricEnergyTractionStandstill ;

    sh:severity sh:Violation .

era-sh:PermissionChargingElectricEnergyTractionStandstillApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:permissionChargingElectricEnergyTractionStandstill ;
    era:scope "local" ;

    rdfs:comment "Applicable when the permission for charging electric energy storage for traction purposes at standstill is defined and corresponds to the correct format."@en ;
    # Source: inferred from the context of charging permissions in RAG

    sh:message "permissionChargingElectricEnergyTractionStandstill: This error is due to the ContactLineSystem {?systemLabel}, violating the rule: Applicable only when the permission for charging electric energy storage is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?systemLabel WHERE {
        $this rdfs:label ?systemLabel .
        FILTER NOT EXISTS { $this era:permissionChargingElectricEnergyTractionStandstill ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:ContactLineSystem } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PermitUseReflectivePlates a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:permitUseReflectivePlates ;
    era:rinfIndex "1.1.1.1.7.12" ;
    era:scope "local" ;

    rdfs:comment "Sections where it is permitted to use reflective plates on rail freight corridors, with a view to prioritise the current bottlenecks. Specific case for Belgium, France, Italy, Portugal, and Spain until 1.1.2026."@en ;
    # Source: ontology comment for permitUseReflectivePlates

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "permitUseReflectivePlates: The CommonCharacteristicsSubset must have at most one permitUseReflectivePlates value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:permitUseReflectivePlates ;

    sh:severity sh:Violation .

era-sh:PermitUseReflectivePlatesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:permitUseReflectivePlates ;
    era:scope "local" ;

    rdfs:comment "Applicable when the use of reflective plates is defined for the sections on rail freight corridors."@en ;
    # Source: inferred from the context of reflective plates usage requirements

    sh:message "permitUseReflectivePlates: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only when the use of reflective plates is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:permitUseReflectivePlates ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:CommonCharacteristicsSubset } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PermittedContactForce a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:permittedContactForce ;
    era:rinfIndex "1.1.1.2.5.2" ;
    era:scope "local" ;

    rdfs:comment "Indication of contact force allowed expressed in newtons."@en ;
    # Source: ontology comment for permittedContactForce

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:pattern "^(\\d+\\s*N|\\d+\\s*N\\s*\\+\\s*\\d+\\s*N|\\d+\\s*N\\s*-\\s*\\d+\\s*N|\\w+)$" ;
    # Inferred: format for the permitted contact force can be a numeric value followed by N or a formula

    sh:message "permittedContactForce: The CommonCharacteristicsSubset must have at most one permittedContactForce value that is a valid string representing the contact force in newtons or a formula. This error may be due to having more than one value or having a value that does not match the required format."@en ;

    sh:path era:permittedContactForce ;

    sh:severity sh:Violation .

era-sh:PermittedContactForceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:permittedContactForce ;
    era:scope "local" ;

    rdfs:comment "Applicable when the parameter is applicable only if the value 'Overhead contact line (OCL)' is selected for parameter 1.1.1.2.2.1."@en ;
    # Source: RAG applicability condition

    sh:message "permittedContactForce: This error is due to the CommonCharacteristicsSubset {?subsetLabel}, violating the rule: Applicable only if 'Overhead contact line (OCL)' is selected for the relevant parameter."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?subsetLabel WHERE {
        $this rdfs:label ?subsetLabel .
        FILTER NOT EXISTS { $this era:permittedContactForce ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:typeOfContactLineSystem "Overhead contact line (OCL)" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PhaseInfoChangeSupplySystem a sh:PropertyShape ;
    era:affectedClass era:PhaseInfo ;
    era:affectedProperty era:phaseInfoChangeSupplySystem ;
    era:scope "local" ;

    rdfs:comment "Part of the phase info of a track that corresponds to the single selection of Y=yes or N=no to show if the energy supply system changes."@en ;
    # Source: ontology comment for phaseInfoChangeSupplySystem

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "phaseInfoChangeSupplySystem: The PhaseInfo must have at most one phaseInfoChangeSupplySystem value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:phaseInfoChangeSupplySystem ;

    sh:severity sh:Violation .

era-sh:PhaseInfoChangeSupplySystemApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PhaseInfo ;
    era:affectedProperty era:phaseInfoChangeSupplySystem ;
    era:scope "local" ;

    rdfs:comment "Applicable when the phase info change supply system requirement is defined for the phase info."@en ;
    # Source: inferred from the context of phase info requirements

    sh:message "phaseInfoChangeSupplySystem: This error is due to the PhaseInfo {?phaseLabel}, violating the rule: Applicable only when the phase info change supply system requirement is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?phaseLabel WHERE {
        $this rdfs:label ?phaseLabel .
        FILTER NOT EXISTS { $this era:phaseInfoChangeSupplySystem ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:PhaseInfo } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PhaseInfoDistanceType a sh:PropertyShape ;
    era:affectedClass era:PhaseInfo ;
    era:affectedProperty era:phaseInfoDistanceType ;
    era:scope "local" ;

    rdfs:comment "Part of the phase info of a track that corresponds to the single selection of 'MIN=minimum' or 'MAX=maximum' to show whether the length is a minimum distance between the inner contact strips of the pantographs or a maximum distance between the outer contact strips of the pantographs. Multiple strings for this parameter are accepted."@en ;
    # Source: ontology comment for phaseInfoDistanceType

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:pattern "^(MIN=\\d+|MAX=\\d+)$" ;
    # Inferred: format for the phase info distance type must conform to 'MIN=value' or 'MAX=value'

    sh:message "phaseInfoDistanceType: The PhaseInfo must have at most one phaseInfoDistanceType value that is a valid string. This error may be due to having more than one value or having a value that does not match the required format."@en ;

    sh:path era:phaseInfoDistanceType ;

    sh:severity sh:Violation .

era-sh:PhaseInfoDistanceTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PhaseInfo ;
    era:affectedProperty era:phaseInfoDistanceType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the phase info distance type requirement is defined for the phase info."@en ;
    # Source: inferred from the context of phase info requirements

    sh:message "phaseInfoDistanceType: This error is due to the PhaseInfo {?phaseLabel}, violating the rule: Applicable only when the phase info distance type requirement is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?phaseLabel WHERE {
        $this rdfs:label ?phaseLabel .
        FILTER NOT EXISTS { $this era:phaseInfoDistanceType ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:PhaseInfo } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PhaseInfoKm a sh:PropertyShape ;
    era:affectedClass era:PhaseInfo ;
    era:affectedProperty era:phaseInfoKm ;
    era:scope "local" ;

    rdfs:comment "Part of the phase info of a track that indicates the location from the start of the line where the new value is valid."@en ;
    # Source: ontology comment for phaseInfoKm

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "phaseInfoKm: The PhaseInfo must have at most one phaseInfoKm value that is a double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:phaseInfoKm ;

    sh:severity sh:Violation .

era-sh:PhaseInfoKmApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PhaseInfo ;
    era:affectedProperty era:phaseInfoKm ;
    era:scope "local" ;

    rdfs:comment "Applicable when the phase info km requirement is defined for the phase info."@en ;
    # Source: inferred from the context of phase info requirements

    sh:message "phaseInfoKm: This error is due to the PhaseInfo {?phaseLabel}, violating the rule: Applicable only when the phase info km requirement is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?phaseLabel WHERE {
        $this rdfs:label ?phaseLabel .
        FILTER NOT EXISTS { $this era:phaseInfoKm ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:PhaseInfo } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PhaseInfoLength a sh:PropertyShape ;
    era:affectedClass era:PhaseInfo ;
    era:affectedProperty era:phaseInfoLength ;
    era:scope "local" ;

    rdfs:comment "Part of the phase info of a track that corresponds to the length of the phase separation in metres."@en ;
    # Source: ontology comment for phaseInfoLength

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "phaseInfoLength: The PhaseInfo must have at most one phaseInfoLength value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:phaseInfoLength ;

    sh:severity sh:Violation .

era-sh:PhaseInfoLengthApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PhaseInfo ;
    era:affectedProperty era:phaseInfoLength ;
    era:scope "local" ;

    rdfs:comment "Applicable when the phase info length requirement is defined for the phase info."@en ;
    # Source: inferred from the context of phase info requirements

    sh:message "phaseInfoLength: This error is due to the PhaseInfo {?phaseLabel}, violating the rule: Applicable only when the phase info length requirement is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?phaseLabel WHERE {
        $this rdfs:label ?phaseLabel .
        FILTER NOT EXISTS { $this era:phaseInfoLength ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:PhaseInfo } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PhaseInfoPantographLowered a sh:PropertyShape ;
    era:affectedClass era:PhaseInfo ;
    era:affectedProperty era:phaseInfoPantographLowered ;
    era:scope "local" ;

    rdfs:comment "Part of the phase info of a track that shows whether a pantograph has to be lowered."@en ;
    # Source: ontology comment for phaseInfoPantographLowered

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "phaseInfoPantographLowered: The PhaseInfo must have at most one phaseInfoPantographLowered value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:phaseInfoPantographLowered ;

    sh:severity sh:Violation .

era-sh:PhaseInfoPantographLoweredApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PhaseInfo ;
    era:affectedProperty era:phaseInfoPantographLowered ;
    era:scope "local" ;

    rdfs:comment "Applicable when the phase info pantograph lowered requirement is defined for the phase info."@en ;
    # Source: inferred from the context of phase info requirements

    sh:message "phaseInfoPantographLowered: This error is due to the PhaseInfo {?phaseLabel}, violating the rule: Applicable only when the phase info pantograph lowered requirement is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?phaseLabel WHERE {
        $this rdfs:label ?phaseLabel .
        FILTER NOT EXISTS { $this era:phaseInfoPantographLowered ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:PhaseInfo } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PhaseInfoSwitchOffBreaker a sh:PropertyShape ;
    era:affectedClass era:PhaseInfo ;
    era:affectedProperty era:phaseInfoSwitchOffBreaker ;
    era:scope "local" ;

    rdfs:comment "Part of the phase info of a track that shows whether the breaker has to be switched off."@en ;
    # Source: ontology comment for phaseInfoSwitchOffBreaker

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "phaseInfoSwitchOffBreaker: The PhaseInfo must have at most one phaseInfoSwitchOffBreaker value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:phaseInfoSwitchOffBreaker ;

    sh:severity sh:Violation .

era-sh:PhaseInfoSwitchOffBreakerApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PhaseInfo ;
    era:affectedProperty era:phaseInfoSwitchOffBreaker ;
    era:scope "local" ;

    rdfs:comment "Applicable when the phase info switch off breaker requirement is defined for the phase info."@en ;
    # Source: inferred from the context of phase info requirements

    sh:message "phaseInfoSwitchOffBreaker: This error is due to the PhaseInfo {?phaseLabel}, violating the rule: Applicable only when the phase info switch off breaker requirement is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?phaseLabel WHERE {
        $this rdfs:label ?phaseLabel .
        FILTER NOT EXISTS { $this era:phaseInfoSwitchOffBreaker ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:PhaseInfo } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PlatformId a sh:PropertyShape ;
    era:affectedClass era:PlatformEdge ;
    era:affectedProperty era:platformId ;
    era:rinfIndex "1.2.1.0.6.2" ;
    era:scope "local" ;

    rdfs:comment "Unique platform identification or unique platform number within an Operational Point."@en ;
    # Source: ontology comment for platformId

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:pattern "^[A-Z]{2}[A-Za-z0-9]{1,10}$" ;
    # Inferred: format for platformId must conform to the specified character string pattern

    sh:message "platformId: The PlatformEdge must have at most one platformId value that is a valid string. This error may be due to having more than one value or having a value that does not match the required format."@en ;

    sh:path era:platformId ;

    sh:severity sh:Violation .

era-sh:PlatformIdApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PlatformEdge ;
    era:affectedProperty era:platformId ;
    era:scope "local" ;

    rdfs:comment "Applicable when the platform identification is defined for the platform edge."@en ;
    # Source: inferred from the context of platform identification requirements

    sh:message "platformId: This error is due to the PlatformEdge {?platformLabel}, violating the rule: Applicable only when the platform identification is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?platformLabel WHERE {
        $this rdfs:label ?platformLabel .
        FILTER NOT EXISTS { $this era:platformId ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:PlatformEdge } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PortableBoardingAids a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:portableBoardingAids ;
    era:rinfIndex "4.12.3.3" ;
    era:scope "local" ;

    rdfs:comment "Description of any portable boarding aids if considered in the design of the vehicle for meeting the PRM TSI requirements."@en ;
    # Source: ontology comment for portableBoardingAids

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "portableBoardingAids: The VehicleType must have at most one portableBoardingAids value that is a string. This error may be due to having more than one value or having a value that does not match the required format."@en ;

    sh:path era:portableBoardingAids ;

    sh:severity sh:Violation .

era-sh:PortableBoardingAidsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:portableBoardingAids ;
    era:scope "local" ;

    rdfs:comment "Applicable when portable boarding aids are considered in the design of the vehicle."@en ;
    # Source: inferred from the context of portable boarding aids requirements

    sh:message "portableBoardingAids: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when portable boarding aids are considered in the design."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:portableBoardingAids ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PreventRegenerativeBrakeUse a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:preventRegenerativeBrakeUse ;
    era:rinfIndex "4.7.4.3.2" ;
    era:scope "local" ;

    rdfs:comment "Possibility of preventing the use of the regenerative brake (only if fitted with regenerative brake)."@en ;
    # Source: ontology comment for preventRegenerativeBrakeUse

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "preventRegenerativeBrakeUse: The VehicleType must have at most one preventRegenerativeBrakeUse value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:preventRegenerativeBrakeUse ;

    sh:severity sh:Violation .

era-sh:PreventRegenerativeBrakeUseApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:preventRegenerativeBrakeUse ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is fitted with a regenerative brake."@en ;
    # Source: inferred from the context of regenerative brake usage requirements

    sh:message "preventRegenerativeBrakeUse: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the vehicle type is fitted with a regenerative brake."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:preventRegenerativeBrakeUse ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:isFittedWithRegenerativeBrake true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PrimaryLocationCode a sh:PropertyShape ;
    era:affectedClass era:PrimaryLocation ;
    era:affectedProperty era:primaryLocationCode ;
    era:rinfIndex "1.2.0.0.0.3" ;
    era:scope "local" ;

    rdfs:comment "Primary location code developed for information exchange in accordance with the TSIs relating to the telematics applications subsystem."@en ;
    # Source: ontology comment for primaryLocationCode

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:pattern "^[A-Z]{2}\\d{5}$" ;
    # Inferred: format for primaryLocationCode must conform to the specified character string pattern (Country Code + Location Code)

    sh:message "primaryLocationCode: The PrimaryLocation must have at most one primaryLocationCode value that is a valid string. This error may be due to having more than one value or having a value that does not match the required format."@en ;

    sh:path era:primaryLocationCode ;

    sh:severity sh:Violation .

era-sh:PrimaryLocationCodeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PrimaryLocation ;
    era:affectedProperty era:primaryLocationCode ;
    era:scope "local" ;

    rdfs:comment "Applicable when the Primary Location Code exists for the operational point."@en ;
    # Source: RAG applicability condition

    sh:message "primaryLocationCode: This error is due to the PrimaryLocation {?locationLabel}, violating the rule: Applicable only when the Primary Location Code exists."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?locationLabel WHERE {
        $this rdfs:label ?locationLabel .
        FILTER NOT EXISTS { $this era:primaryLocationCode ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:primaryLocationCode ?code } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PrimaryLocationName a sh:PropertyShape ;
    era:affectedClass era:PrimaryLocation ;
    era:affectedProperty era:primaryLocationName ;
    era:scope "local" ;

    rdfs:comment "Part of the primary location that specifies the name of the location used by the Infrastructure Manager (IM) to define a path for a train."@en ;
    # Source: ontology comment for primaryLocationName

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "primaryLocationName: The PrimaryLocation must have at most one primaryLocationName value that is a valid string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:primaryLocationName ;

    sh:severity sh:Violation .

era-sh:PrimaryLocationNameApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:PrimaryLocation ;
    era:affectedProperty era:primaryLocationName ;
    era:scope "local" ;

    rdfs:comment "Applicable when the primary location name is defined for the primary location."@en ;
    # Source: inferred from the context of primary location requirements

    sh:message "primaryLocationName: This error is due to the PrimaryLocation {?locationLabel}, violating the rule: Applicable only when the primary location name is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?locationLabel WHERE {
        $this rdfs:label ?locationLabel .
        FILTER NOT EXISTS { $this era:primaryLocationName ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:PrimaryLocation } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PrioritySeats a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:prioritySeats ;
    era:scope "local" ;

    rdfs:comment "Number of priority seats."@en ;
    # Source: ontology comment for prioritySeats

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "prioritySeats: The VehicleType must have at most one prioritySeats value that is a valid string. This error may be due to having more than one value or having a value that does not match the required format."@en ;

    sh:path era:prioritySeats ;

    sh:severity sh:Violation .

era-sh:PrioritySeatsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:prioritySeats ;
    era:scope "local" ;

    rdfs:comment "Applicable when the priority seats requirement is defined for the vehicle type."@en ;
    # Source: inferred from the context of priority seats requirements

    sh:message "prioritySeats: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the priority seats requirement is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:prioritySeats ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PrmAccessibleToilets a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:prmAccessibleToilets ;
    era:scope "local" ;

    rdfs:comment "Number of PRM accessible toilets."@en ;
    # Source: ontology comment for prmAccessibleToilets

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "prmAccessibleToilets: The VehicleType must have at most one prmAccessibleToilets value that is an integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:prmAccessibleToilets ;

    sh:severity sh:Violation .

era-sh:PrmAccessibleToiletsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:prmAccessibleToilets ;
    era:scope "local" ;

    rdfs:comment "Applicable when the PRM accessible toilets requirement is defined for the vehicle type."@en ;
    # Source: inferred from the context of PRM accessibility requirements

    sh:message "prmAccessibleToilets: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the PRM accessible toilets requirement is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:prmAccessibleToilets ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PublicNetworkRoaming a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:publicNetworkRoaming ;
    era:rinfIndex "1.1.1.3.3.6" ;
    era:scope "local" ;

    rdfs:comment "Existence of roaming to a public network. In case of Y, provide the name of the public network(s) under parameter 'Details on GSM-R roaming to public networks'."@en ;
    # Source: ontology comment for publicNetworkRoaming

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "publicNetworkRoaming: The CommonCharacteristicsSubset or RunningTrack must have at most one publicNetworkRoaming value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:publicNetworkRoaming ;

    sh:severity sh:Violation .

era-sh:PublicNetworkRoamingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:publicNetworkRoaming ;
    era:scope "local" ;

    rdfs:comment "Applicable if the answer to parameter 1.1.1.3.3.6 / Existence of roaming to public networks is ‘Y’."@en ;
    # Source: RAG applicability condition

    sh:message "publicNetworkRoaming: This error is due to the CommonCharacteristicsSubset or RunningTrack {?label}, violating the rule: Applicable only if the answer to parameter 1.1.1.3.3.6 is ‘Y’."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:publicNetworkRoaming ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:existenceOfRoamingToPublicNetworks "Y" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:PublicNetworkRoamingDetails a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:publicNetworkRoamingDetails ;
    era:rinfIndex "1.1.1.3.3.7" ;
    era:scope "local" ;

    rdfs:comment "If roaming to public networks is configured, please indicate to which networks, for which users and in which areas."@en ;
    # Source: ontology comment for publicNetworkRoamingDetails

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "publicNetworkRoamingDetails: The CommonCharacteristicsSubset or RunningTrack must have at most one publicNetworkRoamingDetails value that is a string. This error may be due to having more than one value or having a value that does not match the required format."@en ;

    sh:path era:publicNetworkRoamingDetails ;

    sh:severity sh:Violation .

era-sh:PublicNetworkRoamingDetailsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:publicNetworkRoamingDetails ;
    era:scope "local" ;

    rdfs:comment "Mandatory if GSM-R is installed and publicNetworkRoaming is 'true'."@en ;
    # Source: dependency note in ontology

    sh:message "publicNetworkRoamingDetails: This error is due to the CommonCharacteristicsSubset or RunningTrack {?label}, violating the rule: Mandatory if GSM-R is installed and publicNetworkRoaming is 'true'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:publicNetworkRoamingDetails ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:publicNetworkRoaming true } .
        FILTER EXISTS { $this era:gsmRInstalled true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:QNvsbtsmperm a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:qNvsbtsmperm ;
    era:rinfIndex "1.1.1.3.2.16.12" ;
    era:scope "local" ;

    rdfs:comment "Permission to use service brake in target speed monitoring."@en ;
    # Source: ontology comment for qNvsbtsmperm

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "qNvsbtsmperm: The CommonCharacteristicsSubset or RunningTrack must have at most one qNvsbtsmperm value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:qNvsbtsmperm ;

    sh:severity sh:Violation .

era-sh:QNvsbtsmpermApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:qNvsbtsmperm ;
    era:scope "local" ;

    rdfs:comment "A value has to be provided when ETCS is present."@en ;
    # Source: dependency note in ontology

    sh:message "qNvsbtsmperm: This error is due to the CommonCharacteristicsSubset or RunningTrack {?label}, violating the rule: A value has to be provided when ETCS is present."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:qNvsbtsmperm ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:etcsPresent true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:QuasiStaticGuidingForce a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:quasiStaticGuidingForce ;
    era:rinfIndex "4.5.4" ;
    era:scope "local" ;

    rdfs:comment "Quasi-static guiding force (if exceeds the limit defined in TSI or not defined in the TSI), given in kN."@en ;
    # Source: ontology comment for quasiStaticGuidingForce

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "quasiStaticGuidingForce: The VehicleType must have at most one quasiStaticGuidingForce value that is a valid double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:quasiStaticGuidingForce ;

    sh:severity sh:Violation .

era-sh:QuasiStaticGuidingForceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:quasiStaticGuidingForce ;
    era:scope "local" ;

    rdfs:comment "Applicable when the quasi-static guiding force is defined for the vehicle type."@en ;
    # Source: inferred from the context of quasi-static guiding force requirements

    sh:message "quasiStaticGuidingForce: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the quasi-static guiding force is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:quasiStaticGuidingForce ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RadioNetworkId a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:radioNetworkId ;
    era:rinfIndex "1.1.1.3.3" ;
    era:scope "local" ;

    rdfs:comment "Unique identification of the GSM-R network the calling mobile station has to register with, as defined in the specification referenced in Appendix A-1, index [C]."@en ;
    # Source: ontology comment for radioNetworkId

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:pattern "^[0-9]{6}$" ;
    # Inferred: format for radioNetworkId must conform to the specified character string pattern (6 decimal digits)

    sh:message "radioNetworkId: The CommonCharacteristicsSubset or RunningTrack must have at most one radioNetworkId value that is a valid string. This error may be due to having more than one value or having a value that does not match the required format."@en ;

    sh:path era:radioNetworkId ;

    sh:severity sh:Violation .

era-sh:RadioNetworkIdApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:radioNetworkId ;
    era:scope "local" ;

    rdfs:comment "Applicable when GSM-R is installed for the infrastructure."@en ;
    # Source: dependency note in ontology

    sh:message "radioNetworkId: This error is due to the CommonCharacteristicsSubset or RunningTrack {?label}, violating the rule: Applicable only when GSM-R is installed."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:radioNetworkId ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:gsmRInstalled true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RadioSwitchOverSpecialConditions a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:radioSwitchOverSpecialConditions ;
    era:rinfIndex "4.13.2.4" ;
    era:scope "local" ;

    rdfs:comment "Special conditions implemented on-board to switch over between different radio systems. Given as combination of systems installed on board ('System XX'_'System YY')."@en ;
    # Source: ontology comment for radioSwitchOverSpecialConditions

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "radioSwitchOverSpecialConditions: The VehicleType must have at most one radioSwitchOverSpecialConditions value that is a valid string. This error may be due to having more than one value or having a value that does not match the required format."@en ;

    sh:path era:radioSwitchOverSpecialConditions ;

    sh:severity sh:Violation .

era-sh:RadioSwitchOverSpecialConditionsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:radioSwitchOverSpecialConditions ;
    era:scope "local" ;

    rdfs:comment "Applicable when at least two different radio systems exist."@en ;
    # Source: RAG applicability condition

    sh:message "radioSwitchOverSpecialConditions: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when at least two different radio systems exist."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:radioSwitchOverSpecialConditions ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:otherRadioSystemsInstalled ?systems . FILTER (COUNT(?systems) >= 2) } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RailSystemType a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:railSystemType ;
    era:scope "local" ;

    rdfs:comment "(deprecated) Type of railway system. Can be 'High-speed' or 'Conventional'. Deprecated due to the lack of information."@en ;
    # Source: ontology comment for railSystemType

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:in ( "High-speed" "Conventional" ) ;
    # Source: inferred from the comment indicating possible values

    sh:message "railSystemType: The CommonCharacteristicsSubset must have at most one railSystemType value that is a valid string. This error may be due to having more than one value or having a value that is not one of the allowed types."@en ;

    sh:path era:railSystemType ;

    sh:severity sh:Violation .

era-sh:RailSystemTypeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:railSystemType ;
    era:scope "local" ;

    rdfs:comment "Applicable when the rail system type is defined for the common characteristics subset."@en ;
    # Source: inferred from the context of rail system type requirements

    sh:message "railSystemType: This error is due to the CommonCharacteristicsSubset {?label}, violating the rule: Applicable only when the rail system type is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:railSystemType ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:CommonCharacteristicsSubset } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RaisedPantographsDistance a sh:PropertyShape ;
    era:affectedClass era:RaisedPantographsDistanceAndSpeed ;
    era:affectedProperty era:raisedPantographsDistance ;
    era:scope "local" ;

    rdfs:comment "Part of the raised pantographs distance and speed of a track that corresponds to the minimum distance between pantographs, in metres."@en ;
    # Source: ontology comment for raisedPantographsDistance

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "raisedPantographsDistance: The RaisedPantographsDistanceAndSpeed must have at most one raisedPantographsDistance value that is a valid integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:raisedPantographsDistance ;

    sh:severity sh:Violation .

era-sh:RaisedPantographsDistanceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RaisedPantographsDistanceAndSpeed ;
    era:affectedProperty era:raisedPantographsDistance ;
    era:scope "local" ;

    rdfs:comment "Applicable only if 'Overhead contact line (OCL)' is selected in the relevant parameter."@en ;
    # Source: RAG applicability condition

    sh:message "raisedPantographsDistance: This error is due to the RaisedPantographsDistanceAndSpeed {?label}, violating the rule: Applicable only if 'Overhead contact line (OCL)' is selected."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:raisedPantographsDistance ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:overheadContactLine "Y" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RaisedPantographsNumber a sh:PropertyShape ;
    era:affectedClass era:RaisedPantographsDistanceAndSpeed ;
    era:affectedProperty era:raisedPantographsNumber ;
    era:scope "local" ;

    rdfs:comment "Part of the raised pantographs distance and speed of a track that corresponds to the number of pantographs."@en ;
    # Source: ontology comment for raisedPantographsNumber

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "raisedPantographsNumber: The RaisedPantographsDistanceAndSpeed must have at most one raisedPantographsNumber value that is a valid integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:raisedPantographsNumber ;

    sh:severity sh:Violation .

era-sh:RaisedPantographsNumberApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RaisedPantographsDistanceAndSpeed ;
    era:affectedProperty era:raisedPantographsNumber ;
    era:scope "local" ;

    rdfs:comment "Applicable only if 'Overhead contact line (OCL)' is selected in the relevant parameter."@en ;
    # Source: RAG applicability condition

    sh:message "raisedPantographsNumber: This error is due to the RaisedPantographsDistanceAndSpeed {?label}, violating the rule: Applicable only if 'Overhead contact line (OCL)' is selected."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:raisedPantographsNumber ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:overheadContactLine "Y" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RaisedPantographsSpeed a sh:PropertyShape ;
    era:affectedClass era:RaisedPantographsDistanceAndSpeed ;
    era:affectedProperty era:raisedPantographsSpeed ;
    era:scope "local" ;

    rdfs:comment "Indication of maximum number of raised pantographs per train allowed and minimum spacing centre line to centre line of adjacent pantograph heads, expressed in metres, at the given speed."@en ;
    # Source: ontology comment for raisedPantographsSpeed

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "raisedPantographsSpeed: The RaisedPantographsDistanceAndSpeed must have at most one raisedPantographsSpeed value that is a valid integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:raisedPantographsSpeed ;

    sh:severity sh:Violation .

era-sh:RaisedPantographsSpeedApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RaisedPantographsDistanceAndSpeed ;
    era:affectedProperty era:raisedPantographsSpeed ;
    era:scope "local" ;

    rdfs:comment "Applicable only if 'Overhead contact line (OCL)' is selected in the relevant parameter."@en ;
    # Source: RAG applicability condition

    sh:message "raisedPantographsSpeed: This error is due to the RaisedPantographsDistanceAndSpeed {?label}, violating the rule: Applicable only if 'Overhead contact line (OCL)' is selected."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:raisedPantographsSpeed ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:overheadContactLine "Y" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RbcID a sh:PropertyShape ;
    era:affectedClass era:RadioBlockCenter ;
    era:affectedProperty era:rbcID ;
    era:rinfIndex "1.1.1.3.2.17" ;
    era:scope "local" ;

    rdfs:comment "Unique RBC identification (NID_C+NID_RBC) as defined in the specification referenced in Appendix A-1, index [C] (TSI CCS)."@en ;
    # Source: ontology comment for rbcID

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:pattern "^[0-9]{4}$" ;
    # Inferred: format for rbcID must conform to the specified character string pattern (4 decimal digits)

    sh:message "rbcID: The RadioBlockCenter must have at most one rbcID value that is a valid string. This error may be due to having more than one value or having a value that does not match the required format."@en ;

    sh:path era:rbcID ;

    sh:severity sh:Violation .

era-sh:RbcIDApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RadioBlockCenter ;
    era:affectedProperty era:rbcID ;
    era:scope "local" ;

    rdfs:comment "Applicable if the Radio Block Center is defined and requires a unique identification."@en ;
    # Source: inferred from the context of unique identification requirements

    sh:message "rbcID: This error is due to the RadioBlockCenter {?label}, violating the rule: Applicable only if a unique identification is required."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:rbcID ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:RadioBlockCenter } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RbcPhone a sh:PropertyShape ;
    era:affectedClass era:RadioBlockCenter ;
    era:affectedProperty era:rbcPhone ;
    era:rinfIndex "1.1.1.3.2.17" ;
    era:scope "local" ;

    rdfs:comment "Unique RBC calling number (NID_RADIO) as defined in the specification referenced in Appendix A-1, index [C]."@en ;
    # Source: ontology comment for rbcPhone

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:pattern "^[0-9]{4,15}$" ;
    # Inferred: format for rbcPhone must conform to the specified character string pattern (4 to 15 decimal digits)

    sh:message "rbcPhone: The RadioBlockCenter must have at most one rbcPhone value that is a valid string. This error may be due to having more than one value or having a value that does not match the required format."@en ;

    sh:path era:rbcPhone ;

    sh:severity sh:Violation .

era-sh:RbcPhoneApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RadioBlockCenter ;
    era:affectedProperty era:rbcPhone ;
    era:scope "local" ;

    rdfs:comment "Mandatory if the Radio Block Center is defined and requires a unique calling number."@en ;
    # Source: inferred from the context of unique identification requirements

    sh:message "rbcPhone: This error is due to the RadioBlockCenter {?label}, violating the rule: Mandatory if the Radio Block Center requires a unique calling number."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:rbcPhone ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:RadioBlockCenter } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RedLightsRequired a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:redLightsRequired ;
    era:rinfIndex "1.1.1.1.7.10" ;
    era:scope "local" ;

    rdfs:comment "Sections where two steady red lights are required in accordance with TSI OPE."@en ;
    # Source: ontology comment for redLightsRequired

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "redLightsRequired: The CommonCharacteristicsSubset or RunningTrack must have at most one redLightsRequired value that is a boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:redLightsRequired ;

    sh:severity sh:Violation .

era-sh:RedLightsRequiredApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:redLightsRequired ;
    era:scope "local" ;

    rdfs:comment "Applicable if the section of line requires two steady red lights as per national rules."@en ;
    # Source: inferred from RAG and ontology context

    sh:message "redLightsRequired: This error is due to the CommonCharacteristicsSubset or RunningTrack {?label}, violating the rule: Applicable only if the section of line requires two steady red lights."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:redLightsRequired ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:requiresRedLights true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ReferencePassByNoiseLevel a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:referencePassByNoiseLevel ;
    era:rinfIndex "4.11.2" ;
    era:scope "local" ;

    rdfs:comment "Pass-by noise level was measured under reference conditions."@en ;
    # Source: ontology comment for referencePassByNoiseLevel

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "referencePassByNoiseLevel: The VehicleType must have at most one referencePassByNoiseLevel value that is a valid boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:referencePassByNoiseLevel ;

    sh:severity sh:Violation .

era-sh:ReferencePassByNoiseLevelApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:referencePassByNoiseLevel ;
    era:scope "local" ;

    rdfs:comment "Applicable when the pass-by noise level is defined for the vehicle type."@en ;
    # Source: inferred from the context of pass-by noise level requirements

    sh:message "referencePassByNoiseLevel: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the pass-by noise level is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:referencePassByNoiseLevel ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RelativeDistanceDangerPoint a sh:PropertyShape ;
    era:affectedClass era:Signal ;
    era:affectedProperty era:relativeDistanceDangerPoint ;
    era:rinfIndex "1.1.1.3.14.4" ;
    era:scope "local" ;

    rdfs:comment "Distance in meters to the danger point."@en ;
    # Source: ontology comment for relativeDistanceDangerPoint

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "relativeDistanceDangerPoint: The Signal must have at most one relativeDistanceDangerPoint value that is a valid integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:relativeDistanceDangerPoint ;

    sh:severity sh:Violation .

era-sh:RelativeDistanceDangerPointApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Signal ;
    era:affectedProperty era:relativeDistanceDangerPoint ;
    era:scope "local" ;

    rdfs:comment "Applicable when the signal is defined and requires a relative distance to the danger point."@en ;
    # Source: inferred from the context of signal requirements

    sh:message "relativeDistanceDangerPoint: This error is due to the Signal {?label}, violating the rule: Applicable only when the signal requires a relative distance to the danger point."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:relativeDistanceDangerPoint ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:requiresRelativeDistanceToDangerPoint true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:RequiredSandingOverride a sh:PropertyShape ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:requiredSandingOverride ;
    era:scope "local" ;

    rdfs:comment "Indication whether possibility to activate/deactivate sanding devices by driver, according to instructions from the Infrastructure Manager, is required or not."@en ;
    # Source: ontology comment for requiredSandingOverride

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "requiredSandingOverride: The TrainDetectionSystem must have at most one requiredSandingOverride value that is a valid boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:requiredSandingOverride ;

    sh:severity sh:Violation .

era-sh:RequiredSandingOverrideApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:TrainDetectionSystem ;
    era:affectedProperty era:requiredSandingOverride ;
    era:scope "local" ;

    rdfs:comment "Applicable only when the possibility to activate/deactivate sanding devices by driver is defined."@en ;
    # Source: inferred from the context of required sanding override requirements

    sh:message "requiredSandingOverride: This error is due to the TrainDetectionSystem {?label}, violating the rule: Applicable only when the possibility to activate/deactivate sanding devices is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:requiredSandingOverride ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:activationSandingDefined true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SchematicOverviewOP a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:schematicOverviewOP ;
    era:rinfIndex "1.2.0.0.0.7.1" ;
    era:scope "local" ;

    rdfs:comment "Document providing the schematic overview of the operational point."@en ;
    # Source: ontology comment for schematicOverviewOP

    sh:datatype xsd:anyURI ;
    # Source: ontology range is xsd:anyURI (http://www.w3.org/2001/XMLSchema#anyURI)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "schematicOverviewOP: The OperationalPoint must have at most one schematicOverviewOP value that is a valid URI. This error may be due to having more than one value or having a value that is not a valid URI."@en ;

    sh:path era:schematicOverviewOP ;

    sh:severity sh:Violation .

era-sh:SchematicOverviewOPApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:schematicOverviewOP ;
    era:scope "local" ;

    rdfs:comment "Applicable when the schematic overview of the operational point is defined."@en ;
    # Source: inferred from the context of operational point requirements

    sh:message "schematicOverviewOP: This error is due to the OperationalPoint {?label}, violating the rule: Applicable only when the schematic overview is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:schematicOverviewOP ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:OperationalPoint } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ShortestDistanceBetweenPantographsInContactWithOCL a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:shortestDistanceBetweenPantographsInContactWithOCL ;
    era:scope "local" ;

    rdfs:comment "Shortest distance between two pantographs in contact with the OCL (to be indicated for each energy supply system the vehicle is equipped for; to be indicated for single and, if applicable, multiple operation)."@en ;
    # Source: ontology comment for shortestDistanceBetweenPantographsInContactWithOCL

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "shortestDistanceBetweenPantographsInContactWithOCL: The VehicleType must have at most one shortestDistanceBetweenPantographsInContactWithOCL value that is a valid string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:shortestDistanceBetweenPantographsInContactWithOCL ;

    sh:severity sh:Violation .

era-sh:ShortestDistanceBetweenPantographsInContactWithOCLApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:shortestDistanceBetweenPantographsInContactWithOCL ;
    era:scope "local" ;

    rdfs:comment "Applicable only if 'Overhead contact line (OCL)' is selected in the relevant parameter."@en ;
    # Source: RAG applicability condition

    sh:message "shortestDistanceBetweenPantographsInContactWithOCL: This error is due to the VehicleType {?label}, violating the rule: Applicable only if 'Overhead contact line (OCL)' is selected."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:shortestDistanceBetweenPantographsInContactWithOCL ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:overheadContactLine "Y" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SidingId a sh:PropertyShape ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:sidingId ;
    era:rinfIndex "1.2.2.0.0.2" ;
    era:scope "local" ;

    rdfs:comment "Unique siding identification or unique siding number within an Operational Point."@en ;
    # Source: ontology comment for sidingId

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "sidingId: The Siding must have at most one sidingId value that is a valid string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:sidingId ;

    sh:severity sh:Violation .

era-sh:SidingIdApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Siding ;
    era:affectedProperty era:sidingId ;
    era:scope "local" ;

    rdfs:comment "Applicable only if sidings exist in the Operational Point."@en ;
    # Source: RAG applicability condition

    sh:message "sidingId: This error is due to the Siding {?label}, violating the rule: Applicable only if sidings exist in the Operational Point."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:sidingId ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:Siding } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SignalId a sh:PropertyShape ;
    era:affectedClass era:Signal ;
    era:affectedProperty era:signalId ;
    era:rinfIndex "1.1.1.3.14.1" ;
    era:scope "local" ;

    rdfs:comment "Identifier of signal."@en ;
    # Source: ontology comment for signalId

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "signalId: The Signal must have at most one signalId value that is a valid string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:signalId ;

    sh:severity sh:Violation .

era-sh:SignalIdApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Signal ;
    era:affectedProperty era:signalId ;
    era:scope "local" ;

    rdfs:comment "Applicable when the signal is defined and requires a unique identifier."@en ;
    # Source: inferred from the context of signal requirements

    sh:message "signalId: This error is due to the Signal {?label}, violating the rule: Applicable only when the signal requires a unique identifier."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:signalId ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:Signal } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SleepingPlaces a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:sleepingPlaces ;
    era:rinfIndex "4.12.1.3" ;
    era:scope "local" ;

    rdfs:comment "Number of sleeping places."@en ;
    # Source: ontology comment for sleepingPlaces

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:pattern "^[0-9]{1,3}$" ;
    # Inferred: format for sleepingPlaces must conform to the specified character string pattern (1 to 3 digits)

    sh:message "sleepingPlaces: The VehicleType must have at most one sleepingPlaces value that is a valid string representing the number of sleeping places. This error may be due to having more than one value or having a value that does not match the required format."@en ;

    sh:path era:sleepingPlaces ;

    sh:severity sh:Violation .

era-sh:SleepingPlacesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:sleepingPlaces ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type includes sleeping places."@en ;
    # Source: inferred from the context of vehicle type characteristics

    sh:message "sleepingPlaces: This error is due to the VehicleType {?label}, violating the rule: Applicable only when the vehicle type includes sleeping places."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:sleepingPlaces ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:includesSleepingPlaces true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SpecificInformation a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:specificInformation ;
    era:rinfIndex "1.1.1.1.3.5.1" ;
    era:scope "local" ;

    rdfs:comment "Any relevant information from the IM relating to the line layout."@en ;
    # Source: ontology comment for specificInformation

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "specificInformation: The CommonCharacteristicsSubset or RunningTrack must have at most one specificInformation value that is a valid string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:specificInformation ;

    sh:severity sh:Violation .

era-sh:SpecificInformationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:specificInformation ;
    era:scope "local" ;

    rdfs:comment "Applicable when specific information about the line layout is provided."@en ;
    # Source: inferred from the context of specific information requirements

    sh:message "specificInformation: This error is due to the CommonCharacteristicsSubset or RunningTrack {?label}, violating the rule: Applicable only when specific information about the line layout is provided."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:specificInformation ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:CommonCharacteristicsSubset } .
        # Or for RunningTrack
        FILTER EXISTS { $this a era:RunningTrack } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:StartOffsetFromOrigin a sh:PropertyShape ;
    era:affectedClass era:LinearElementSection ;
    era:affectedProperty era:startOffsetFromOrigin ;
    era:scope "local" ;

    rdfs:comment "Start offset of the section from the origin."@en ;
    # Source: ontology comment for startOffsetFromOrigin

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "startOffsetFromOrigin: The LinearElementSection must have at most one startOffsetFromOrigin value that is a valid double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:startOffsetFromOrigin ;

    sh:severity sh:Violation .

era-sh:StartOffsetFromOriginApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:LinearElementSection ;
    era:affectedProperty era:startOffsetFromOrigin ;
    era:scope "local" ;

    rdfs:comment "Applicable when the section of line has a defined start offset from the origin."@en ;
    # Source: inferred from the context of linear element sections

    sh:message "startOffsetFromOrigin: This error is due to the LinearElementSection {?label}, violating the rule: Applicable only when the section of line has a defined start offset from the origin."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:startOffsetFromOrigin ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:LinearElementSection } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:StartingNoiseLevel a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:startingNoiseLevel ;
    era:rinfIndex "4.11.4" ;
    era:scope "local" ;

    rdfs:comment "Starting noise level given in dB(A)."@en ;
    # Source: ontology comment for startingNoiseLevel

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "startingNoiseLevel: The VehicleType must have at most one startingNoiseLevel value that is a valid double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:startingNoiseLevel ;

    sh:severity sh:Violation .

era-sh:StartingNoiseLevelApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:startingNoiseLevel ;
    era:scope "local" ;

    rdfs:comment "Applicable when the starting noise level is defined for the vehicle type."@en ;
    # Source: inferred from the context of starting noise level requirements

    sh:message "startingNoiseLevel: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the starting noise level is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:startingNoiseLevel ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:StaticAxleLoadExceptionalPayload a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:staticAxleLoadExceptionalPayload ;
    era:rinfIndex "4.5.3.3" ;
    era:scope "local" ;

    rdfs:comment "Static axle load under exceptional payload."@en ;
    # Source: ontology comment for staticAxleLoadExceptionalPayload

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "staticAxleLoadExceptionalPayload: The VehicleType must have at most one staticAxleLoadExceptionalPayload value that is a valid double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:staticAxleLoadExceptionalPayload ;

    sh:severity sh:Violation .

era-sh:StaticAxleLoadExceptionalPayloadApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:staticAxleLoadExceptionalPayload ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type has defined static axle load under exceptional payload."@en ;
    # Source: inferred from the context of vehicle type characteristics

    sh:message "staticAxleLoadExceptionalPayload: This error is due to the VehicleType {?label}, violating the rule: Applicable only when the vehicle type has defined static axle load under exceptional payload."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:staticAxleLoadExceptionalPayload ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:hasExceptionalPayload true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:StaticAxleLoadNormalPayload a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:staticAxleLoadNormalPayload ;
    era:rinfIndex "4.5.3.2" ;
    era:scope "local" ;

    rdfs:comment "Static axle load under normal payload."@en ;
    # Source: ontology comment for staticAxleLoadNormalPayload

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "staticAxleLoadNormalPayload: The VehicleType must have at most one staticAxleLoadNormalPayload value that is a valid double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:staticAxleLoadNormalPayload ;

    sh:severity sh:Violation .

era-sh:StaticAxleLoadNormalPayloadApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:staticAxleLoadNormalPayload ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type has defined static axle load under normal payload."@en ;
    # Source: inferred from the context of vehicle type characteristics

    sh:message "staticAxleLoadNormalPayload: This error is due to the VehicleType {?label}, violating the rule: Applicable only when the vehicle type has defined static axle load under normal payload."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:staticAxleLoadNormalPayload ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:hasNormalPayload true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:StaticAxleLoadWorkingOrder a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:staticAxleLoadWorkingOrder ;
    era:rinfIndex "4.5.3.1" ;
    era:scope "local" ;

    rdfs:comment "Static axle load in working order."@en ;
    # Source: ontology comment for staticAxleLoadWorkingOrder

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "staticAxleLoadWorkingOrder: The VehicleType must have at most one staticAxleLoadWorkingOrder value that is a valid double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:staticAxleLoadWorkingOrder ;

    sh:severity sh:Violation .

era-sh:StaticAxleLoadWorkingOrderApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:staticAxleLoadWorkingOrder ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type has defined static axle load in working order."@en ;
    # Source: inferred from the context of vehicle type characteristics

    sh:message "staticAxleLoadWorkingOrder: This error is due to the VehicleType {?label}, violating the rule: Applicable only when the vehicle type has defined static axle load in working order."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:staticAxleLoadWorkingOrder ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:hasStaticAxleLoadWorkingOrder true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:StationaryNoiseLevel a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:stationaryNoiseLevel ;
    era:rinfIndex "4.11.3" ;
    era:scope "local" ;

    rdfs:comment "Stationary noise level given in dB(A)."@en ;
    # Source: ontology comment for stationaryNoiseLevel

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "stationaryNoiseLevel: The VehicleType must have at most one stationaryNoiseLevel value that is a valid double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:stationaryNoiseLevel ;

    sh:severity sh:Violation .

era-sh:StationaryNoiseLevelApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:stationaryNoiseLevel ;
    era:scope "local" ;

    rdfs:comment "Applicable when the stationary noise level is defined for the vehicle type."@en ;
    # Source: inferred from the context of stationary noise level requirements

    sh:message "stationaryNoiseLevel: This error is due to the VehicleType {?vehicleLabel}, violating the rule: Applicable only when the stationary noise level is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?vehicleLabel WHERE {
        $this rdfs:label ?vehicleLabel .
        FILTER NOT EXISTS { $this era:stationaryNoiseLevel ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:StructuralCategory a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:structuralCategory ;
    era:rinfIndex "4.1.10" ;
    era:scope "local" ;

    rdfs:comment "Structural category."@en ;
    # Source: ontology comment for structuralCategory

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "structuralCategory: The VehicleType must have at most one structuralCategory value that is a valid string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:structuralCategory ;

    sh:severity sh:Violation .

era-sh:StructuralCategoryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:structuralCategory ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type has a defined structural category."@en ;
    # Source: inferred from the context of vehicle type characteristics

    sh:message "structuralCategory: This error is due to the VehicleType {?label}, violating the rule: Applicable only when the vehicle type has a defined structural category."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:structuralCategory ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:VehicleType } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:StructureCheckLocation a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:structureCheckLocation ;
    era:rinfIndex "1.1.1.1.2.4.3" ;
    era:scope "local" ;

    rdfs:comment "Localisation of structures requiring specific checks."@en ;
    # Source: ontology comment for structureCheckLocation

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:pattern "^[+-]?\\d+(\\.\\d{1,3})?$" ;
    # Inferred: format for structureCheckLocation must conform to the specified character string pattern (±NNNN.NNN)

    sh:message "structureCheckLocation: The CommonCharacteristicsSubset or RunningTrack must have at most one structureCheckLocation value that is a valid double representing the location. This error may be due to having more than one value or having a value that does not match the required format."@en ;

    sh:path era:structureCheckLocation ;

    sh:severity sh:Violation .

era-sh:StructureCheckLocationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:structureCheckLocation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the structure check location is defined for the common characteristics subset or running track."@en ;
    # Source: inferred from the context of structure check requirements

    sh:message "structureCheckLocation: This error is due to the CommonCharacteristicsSubset or RunningTrack {?label}, violating the rule: Applicable only when the structure check location is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:structureCheckLocation ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:CommonCharacteristicsSubset } .
        # Or for RunningTrack
        FILTER EXISTS { $this a era:RunningTrack } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SubsetName a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:subsetName ;
    era:scope "local" ;

    rdfs:comment "A subset of elements with common characteristics could be identified with a unique name/id."@en ;
    # Source: ontology comment for subsetName

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:pattern "^(?!.*\\s{2})[\\S\\s]{1,}$" ;
    # Inferred: value must be a non-empty string without leading or trailing spaces

    sh:message "subsetName: The CommonCharacteristicsSubset must have at most one subsetName value that is a valid string. This error may be due to having more than one value or having a value that does not match the required format."@en ;

    sh:path era:subsetName ;

    sh:severity sh:Violation .

era-sh:SubsetNameApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:subsetName ;
    era:scope "local" ;

    rdfs:comment "Applicable when the subset of elements with common characteristics is defined."@en ;
    # Source: inferred from the context of common characteristics subsets

    sh:message "subsetName: This error is due to the CommonCharacteristicsSubset {?label}, violating the rule: Applicable only when the subset of elements with common characteristics is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:subsetName ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:CommonCharacteristicsSubset } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SubsidiaryLocationCode a sh:PropertyShape ;
    era:affectedClass era:SubsidiaryLocation ;
    era:affectedProperty era:subsidiaryLocationCode ;
    era:scope "local" ;

    rdfs:comment "The numeric code for the subsidiary location."@en ;
    # Source: ontology comment for subsidiaryLocationCode

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:pattern "^[0-9]+$" ;
    # Inferred: assuming the code is numeric, it should match a pattern of digits

    sh:message "subsidiaryLocationCode: The SubsidiaryLocation must have at most one subsidiaryLocationCode value that is a valid numeric string. This error may be due to having more than one value or having a value that does not match the required numeric format."@en ;

    sh:path era:subsidiaryLocationCode ;

    sh:severity sh:Violation .

era-sh:SubsidiaryLocationCodeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SubsidiaryLocation ;
    era:affectedProperty era:subsidiaryLocationCode ;
    era:scope "local" ;

    rdfs:comment "Applicable when the subsidiary location code is defined for a subsidiary location."@en ;
    # Source: inferred from the context of subsidiary location requirements

    sh:message "subsidiaryLocationCode: This error is due to the SubsidiaryLocation {?label}, violating the rule: Applicable only when the subsidiary location code is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:subsidiaryLocationCode ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:SubsidiaryLocation } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SubsidiaryLocationName a sh:PropertyShape ;
    era:affectedClass era:SubsidiaryLocation ;
    era:affectedProperty era:subsidiaryLocationName ;
    era:scope "local" ;

    rdfs:comment "The common name given to the subsidiary location."@en ;
    # Source: ontology comment for subsidiaryLocationName

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:pattern "^(?!\\s)[\\S\\s]{1,}$" ;
    # Inferred: value must be a non-empty string without leading or trailing spaces

    sh:message "subsidiaryLocationName: The SubsidiaryLocation must have at most one subsidiaryLocationName value that is a valid string. This error may be due to having more than one value or having a value that does not match the required format."@en ;

    sh:path era:subsidiaryLocationName ;

    sh:severity sh:Violation .

era-sh:SubsidiaryLocationNameApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SubsidiaryLocation ;
    era:affectedProperty era:subsidiaryLocationName ;
    era:scope "local" ;

    rdfs:comment "Applicable when the subsidiary location name is defined for a subsidiary location."@en ;
    # Source: inferred from the context of subsidiary location requirements

    sh:message "subsidiaryLocationName: This error is due to the SubsidiaryLocation {?label}, violating the rule: Applicable only when the subsidiary location name is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:subsidiaryLocationName ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this a era:SubsidiaryLocation } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SwitchProtectControlWarning a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset, era:Track ;
    era:affectedProperty era:switchProtectControlWarning ;
    era:rinfIndex "1.1.1.3.8.1" ;
    era:scope "local" ;

    rdfs:comment "Indication whether a switch over between different systems whilst running exists."@en ;
    # Source: ontology comment for switchProtectControlWarning

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "switchProtectControlWarning: The CommonCharacteristicsSubset or Track must have at most one switchProtectControlWarning value that is a valid boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:switchProtectControlWarning ;

    sh:severity sh:Violation .

era-sh:SwitchProtectControlWarningApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset, era:Track ;
    era:affectedProperty era:switchProtectControlWarning ;
    era:scope "local" ;

    rdfs:comment "Applicable when at least two different protection, control, and warning systems exist."@en ;
    # Source: inferred from the dependency note in the ontology

    sh:message "switchProtectControlWarning: This error is due to the CommonCharacteristicsSubset or Track {?label}, violating the rule: Applicable only when at least two different protection, control, and warning systems exist."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:switchProtectControlWarning ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:hasMultipleProtectionSystems true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SwitchRadioSystem a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset, era:Track ;
    era:affectedProperty era:switchRadioSystem ;
    era:rinfIndex "1.1.1.3.8.2" ;
    era:scope "local" ;

    rdfs:comment "Indication whether a switch over between different radio systems and no communication system whilst running exists."@en ;
    # Source: ontology comment for switchRadioSystem

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "switchRadioSystem: The CommonCharacteristicsSubset or Track must have at most one switchRadioSystem value that is a valid boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:switchRadioSystem ;

    sh:severity sh:Violation .

era-sh:SwitchRadioSystemApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset, era:Track ;
    era:affectedProperty era:switchRadioSystem ;
    era:scope "local" ;

    rdfs:comment "Applicable when at least two different radio systems exist."@en ;
    # Source: inferred from the dependency note in the ontology

    sh:message "switchRadioSystem: This error is due to the CommonCharacteristicsSubset or Track {?label}, violating the rule: Applicable only when at least two different radio systems exist."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:switchRadioSystem ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:hasMultipleRadioSystems true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SystemSeparationInfoChangeSupplySystem a sh:PropertyShape ;
    era:affectedClass era:SystemSeparationInfo ;
    era:affectedProperty era:systemSeparationInfoChangeSupplySystem ;
    era:scope "local" ;

    rdfs:comment "Part of the system separation info of a track that shows whether the supply system has to be changed. The system separation info is the indication of required several information on system separation."@en ;
    # Source: ontology comment for systemSeparationInfoChangeSupplySystem

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "systemSeparationInfoChangeSupplySystem: The SystemSeparationInfo must have at most one systemSeparationInfoChangeSupplySystem value that is a valid string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:systemSeparationInfoChangeSupplySystem ;

    sh:severity sh:Violation .

era-sh:SystemSeparationInfoChangeSupplySystemApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SystemSeparationInfo ;
    era:affectedProperty era:systemSeparationInfoChangeSupplySystem ;
    era:scope "local" ;

    rdfs:comment "Applicable when the system separation info indicates whether the supply system has to be changed."@en ;
    # Source: inferred from the context of system separation requirements

    sh:message "systemSeparationInfoChangeSupplySystem: This error is due to the SystemSeparationInfo {?label}, violating the rule: Applicable only when the system separation info indicates whether the supply system has to be changed."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:systemSeparationInfoChangeSupplySystem ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:systemSeparationInfo ?info } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SystemSeparationInfoKm a sh:PropertyShape ;
    era:affectedClass era:SystemSeparationInfo ;
    era:affectedProperty era:systemSeparationInfoKm ;
    era:scope "local" ;

    rdfs:comment "Part of the system separation info of a track. Indicates the location from the start of the line where the new value is valid."@en ;
    # Source: ontology comment for systemSeparationInfoKm

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "systemSeparationInfoKm: The SystemSeparationInfo must have at most one systemSeparationInfoKm value that is a valid double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:systemSeparationInfoKm ;

    sh:severity sh:Violation .

era-sh:SystemSeparationInfoKmApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SystemSeparationInfo ;
    era:affectedProperty era:systemSeparationInfoKm ;
    era:scope "local" ;

    rdfs:comment "Applicable when the system separation info indicates the location from the start of the line where the new value is valid."@en ;
    # Source: inferred from the context of system separation requirements

    sh:message "systemSeparationInfoKm: This error is due to the SystemSeparationInfo {?label}, violating the rule: Applicable only when the system separation info indicates the location from the start of the line."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:systemSeparationInfoKm ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:systemSeparationInfo ?info } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SystemSeparationInfoLength a sh:PropertyShape ;
    era:affectedClass era:SystemSeparationInfo ;
    era:affectedProperty era:systemSeparationInfoLength ;
    era:scope "local" ;

    rdfs:comment "Part of the system separation info of a track that shows the length of the system separation in metres."@en ;
    # Source: ontology comment for systemSeparationInfoLength

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "systemSeparationInfoLength: The SystemSeparationInfo must have at most one systemSeparationInfoLength value that is a valid integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:systemSeparationInfoLength ;

    sh:severity sh:Violation .

era-sh:SystemSeparationInfoLengthApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SystemSeparationInfo ;
    era:affectedProperty era:systemSeparationInfoLength ;
    era:scope "local" ;

    rdfs:comment "Applicable when the system separation info indicates the length of the system separation."@en ;
    # Source: inferred from the context of system separation requirements

    sh:message "systemSeparationInfoLength: This error is due to the SystemSeparationInfo {?label}, violating the rule: Applicable only when the system separation info indicates the length of the system separation."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:systemSeparationInfoLength ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:systemSeparationInfo ?info } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SystemSeparationInfoPantographLowered a sh:PropertyShape ;
    era:affectedClass era:SystemSeparationInfo ;
    era:affectedProperty era:systemSeparationInfoPantographLowered ;
    era:scope "local" ;

    rdfs:comment "Part of the system separation info of a track that shows whether the pantograph has to be lowered. The system separation info is the indication of required several information on system separation."@en ;
    # Source: ontology comment for systemSeparationInfoPantographLowered

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "systemSeparationInfoPantographLowered: The SystemSeparationInfo must have at most one systemSeparationInfoPantographLowered value that is a valid boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:systemSeparationInfoPantographLowered ;

    sh:severity sh:Violation .

era-sh:SystemSeparationInfoPantographLoweredApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SystemSeparationInfo ;
    era:affectedProperty era:systemSeparationInfoPantographLowered ;
    era:scope "local" ;

    rdfs:comment "Applicable when the system separation info indicates whether the pantograph has to be lowered."@en ;
    # Source: inferred from the context of system separation requirements

    sh:message "systemSeparationInfoPantographLowered: This error is due to the SystemSeparationInfo {?label}, violating the rule: Applicable only when the system separation info indicates whether the pantograph has to be lowered."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:systemSeparationInfoPantographLowered ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:systemSeparationInfo ?info } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:SystemSeparationInfoSwitchOffBreaker a sh:PropertyShape ;
    era:affectedClass era:SystemSeparationInfo ;
    era:affectedProperty era:systemSeparationInfoSwitchOffBreaker ;
    era:scope "local" ;

    rdfs:comment "Part of the system separation info of a track that shows whether the breaker has to be switched off. The system separation info is the indication of required several information on system separation."@en ;
    # Source: ontology comment for systemSeparationInfoSwitchOffBreaker

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "systemSeparationInfoSwitchOffBreaker: The SystemSeparationInfo must have at most one systemSeparationInfoSwitchOffBreaker value that is a valid boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:systemSeparationInfoSwitchOffBreaker ;

    sh:severity sh:Violation .

era-sh:SystemSeparationInfoSwitchOffBreakerApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:SystemSeparationInfo ;
    era:affectedProperty era:systemSeparationInfoSwitchOffBreaker ;
    era:scope "local" ;

    rdfs:comment "Applicable when the system separation info indicates whether the breaker has to be switched off."@en ;
    # Source: inferred from the context of system separation requirements

    sh:message "systemSeparationInfoSwitchOffBreaker: This error is due to the SystemSeparationInfo {?label}, violating the rule: Applicable only when the system separation info indicates whether the breaker has to be switched off."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:systemSeparationInfoSwitchOffBreaker ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:systemSeparationInfo ?info } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TNvcontact a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:tNvcontact ;
    era:rinfIndex "1.1.1.3.2.16.8" ; 
    era:scope "local" ;

    rdfs:comment "Maximum time without a safe message from Radio Block Center before train reacts in seconds."@en ;
    # Source: ontology comment for tNvcontact

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:pattern "^(255|[0-2][0-9]{0,2}|[0-9]{1,2})$" ;
    # Inferred: values must be integers between 0 and 255, where 255 represents infinity

    sh:message "tNvcontact: The CommonCharacteristicsSubset or RunningTrack must have at most one tNvcontact value that is a valid integer. This error may be due to having more than one value or having a value that does not match the required format."@en ;

    sh:path era:tNvcontact ;

    sh:severity sh:Violation .

era-sh:TNvcontactApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:tNvcontact ;
    era:scope "local" ;

    rdfs:comment "Applicable when ETCS is present and a value is provided."@en ;
    # Source: inferred from the dependency note in the ontology

    sh:message "tNvcontact: This error is due to the CommonCharacteristicsSubset or RunningTrack {?label}, violating the rule: Applicable only when ETCS is present and a value is provided."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:tNvcontact ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:etcsPresent true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TNvovtrp a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:tNvovtrp ;
    era:rinfIndex "1.1.1.3.2.16.6" ;
    era:scope "local" ;

    rdfs:comment "Maximum time for overriding the train trip in seconds."@en ;
    # Source: ontology comment for tNvovtrp

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:pattern "^(255|[0-2][0-9]{0,2}|[0-9]{1,2})$" ;
    # Inferred: values must be integers between 0 and 255, where 255 represents infinity

    sh:message "tNvovtrp: The CommonCharacteristicsSubset or RunningTrack must have at most one tNvovtrp value that is a valid integer. This error may be due to having more than one value or having a value that does not match the required format."@en ;

    sh:path era:tNvovtrp ;

    sh:severity sh:Violation .

era-sh:TNvovtrpApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:tNvovtrp ;
    era:scope "local" ;

    rdfs:comment "Applicable when ETCS is present and a value is provided."@en ;
    # Source: inferred from the dependency note in the ontology

    sh:message "tNvovtrp: This error is due to the CommonCharacteristicsSubset or RunningTrack {?label}, violating the rule: Applicable only when ETCS is present and a value is provided."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:tNvovtrp ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:etcsPresent true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TAFTAPCode a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:tafTAPCode ;
    era:scope "local" ;

    rdfs:comment "Primary location code developed for information exchange in accordance with the TSIs relating to the telematics applications subsystem."@en ;
    # Source: ontology comment for tafTAPCode

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:pattern "^[A-Z]{2}[0-9]{5}$" ;
    # Inferred: value must match the predefined character string format [AANNNNN]

    sh:message "tafTAPCode: The OperationalPoint must have at most one tafTAPCode value that is a valid string matching the format [AANNNNN]. This error may be due to having more than one value or having a value that does not match the required format."@en ;

    sh:path era:tafTAPCode ;

    sh:severity sh:Violation .

era-sh:TAFTAPCodeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:tafTAPCode ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OP TAF TAP primary code exists."@en ;
    # Source: RAG indicates applicability based on existence of OP TAF TAP primary code

    sh:message "tafTAPCode: This error is due to the OperationalPoint {?label}, violating the rule: Applicable only when the OP TAF TAP primary code exists."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:tafTAPCode ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:hasTAFTAPCode true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TenGISId a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:tenGISId ;
    era:rinfIndex "1.1.1.1.2.1.2" ;
    era:scope "local" ;

    rdfs:comment "Indication of the GIS identity (GIS ID) of the section of TEN-T database to which the track belongs."@en ;
    # Source: ontology comment for tenGISId

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "tenGISId: The CommonCharacteristicsSubset or RunningTrack must have at most one tenGISId value that is a valid string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:tenGISId ;

    sh:severity sh:Violation .

era-sh:TenGISIdApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:tenGISId ;
    era:scope "local" ;

    rdfs:comment "Applicable if the answer to parameter 1.1.1.1.2.1 is not off TEN."@en ;
    # Source: RAG indicates applicability based on the answer to related parameter

    sh:message "tenGISId: This error is due to the CommonCharacteristicsSubset or RunningTrack {?label}, violating the rule: Applicable only if the answer to parameter 1.1.1.1.2.1 is not off TEN."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:tenGISId ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:tenClassification ?classification . FILTER(?classification != "Off-TEN") } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ThermalCapacityDistance a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:thermalCapacityDistance ;
    era:rinfIndex "4.7.2.1.4" ;
    era:scope "local" ;

    rdfs:comment "Thermal capacity distance. If no reference case is indicated."@en ;
    # Source: ontology comment for thermalCapacityDistance

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "thermalCapacityDistance: The VehicleType must have at most one thermalCapacityDistance value that is a valid double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:thermalCapacityDistance ;

    sh:severity sh:Violation .

era-sh:ThermalCapacityDistanceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:thermalCapacityDistance ;
    era:scope "local" ;

    rdfs:comment "Applicable when the thermal capacity distance is defined for the vehicle type."@en ;
    # Source: inferred from the context of thermal capacity requirements

    sh:message "thermalCapacityDistance: This error is due to the VehicleType {?label}, violating the rule: Applicable only when the thermal capacity distance is defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:thermalCapacityDistance ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:thermalCapacityDistanceDefined true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ThermalCapacityGradient a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:thermalCapacityGradient ;
    era:rinfIndex "4.7.2.1.3" ;
    era:scope "local" ;

    rdfs:comment "Thermal capacity gradient. If no reference case is indicated."@en ;
    # Source: ontology comment for thermalCapacityGradient

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "thermalCapacityGradient: The VehicleType must have at most one thermalCapacityGradient value that is a valid double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:thermalCapacityGradient ;

    sh:severity sh:Violation .

era-sh:ThermalCapacityGradientApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:thermalCapacityGradient ;
    era:scope "local" ;

    rdfs:comment "Applicable when the thermal capacity gradient is defined for the vehicle type."@en ;
    # Source: inferred from the context of thermal capacity requirements

    sh:message "thermalCapacityGradient: This error is due to the VehicleType {?label}, violating the rule: Applicable only when the thermal capacity gradient is defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:thermalCapacityGradient ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:thermalCapacityGradientDefined true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ThermalCapacitySpeed a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:thermalCapacitySpeed ;
    era:rinfIndex "4.7.2.1.2" ;
    era:scope "local" ;

    rdfs:comment "Thermal capacity speed. If no reference case is indicated."@en ;
    # Source: ontology comment for thermalCapacitySpeed

    sh:datatype xsd:double ;
    # Source: ontology range is xsd:double (http://www.w3.org/2001/XMLSchema#double)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "thermalCapacitySpeed: The VehicleType must have at most one thermalCapacitySpeed value that is a valid double. This error may be due to having more than one value or having a value that is not a valid double."@en ;

    sh:path era:thermalCapacitySpeed ;

    sh:severity sh:Violation .

era-sh:ThermalCapacitySpeedApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:thermalCapacitySpeed ;
    era:scope "local" ;

    rdfs:comment "Applicable when the thermal capacity speed is defined for the vehicle type."@en ;
    # Source: inferred from the context of thermal capacity requirements

    sh:message "thermalCapacitySpeed: This error is due to the VehicleType {?label}, violating the rule: Applicable only when the thermal capacity speed is defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:thermalCapacitySpeed ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:thermalCapacitySpeedDefined true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ThermalCapacityTime a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:thermalCapacityTime ;
    era:rinfIndex "4.7.2.1.5" ;
    era:scope "local" ;

    rdfs:comment "Thermal capacity time. If no reference case is indicated."@en ;
    # Source: ontology comment for thermalCapacityTime

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "thermalCapacityTime: The VehicleType must have at most one thermalCapacityTime value that is a valid integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:thermalCapacityTime ;

    sh:severity sh:Violation .

era-sh:ThermalCapacityTimeApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:thermalCapacityTime ;
    era:scope "local" ;

    rdfs:comment "Applicable when the thermal capacity time is defined for the vehicle type."@en ;
    # Source: inferred from the context of thermal capacity requirements

    sh:message "thermalCapacityTime: This error is due to the VehicleType {?label}, violating the rule: Applicable only when the thermal capacity time is defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:thermalCapacityTime ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:thermalCapacityTimeDefined true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TotalVehicleMass a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:totalVehicleMass ;
    era:rinfIndex "4.5.5" ;
    era:scope "local" ;

    rdfs:comment "Total vehicle mass (for each vehicle of the unit), given in kg."@en ;
    # Source: ontology comment for totalVehicleMass

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "totalVehicleMass: The VehicleType must have at most one totalVehicleMass value that is a valid integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:totalVehicleMass ;

    sh:severity sh:Violation .

era-sh:TotalVehicleMassApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:totalVehicleMass ;
    era:scope "local" ;

    rdfs:comment "Applicable when the total vehicle mass is defined for the vehicle type."@en ;
    # Source: inferred from the context of vehicle mass requirements

    sh:message "totalVehicleMass: This error is due to the VehicleType {?label}, violating the rule: Applicable only when the total vehicle mass is defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:totalVehicleMass ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:totalVehicleMassDefined true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TrackId a sh:PropertyShape ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:trackId ;
    era:scope "local" ;

    rdfs:comment "Unique track identification or unique track number within operational point or section of line."@en ;
    # Source: ontology comment for trackId

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "trackId: The RunningTrack must have at most one trackId value that is a valid string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:trackId ;

    sh:severity sh:Violation .

era-sh:TrackIdApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:RunningTrack ;
    era:affectedProperty era:trackId ;
    era:scope "local" ;

    rdfs:comment "Applicable when each track shall have unique identification or number within the Section of Line. This number cannot be used for naming any other track in the same SoL."@en ;
    # Source: RAG states that the ID must be unique within the SoL

    sh:message "trackId: This error is due to the RunningTrack {?label}, violating the rule: Applicable only when the track ID is unique within the Section of Line."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:trackId ?value } .
        # Additional checks for uniqueness conditions
        FILTER EXISTS { $this era:sectionOfLine ?sol } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TrainControlSwitchOverSpecialConditions a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:trainControlSwitchOverSpecialConditions ;
    era:rinfIndex "4.13.1.6" ;
    era:scope "local" ;

    rdfs:comment "Special conditions implemented on-board to switch over between different train protection control and warning systems. Given as combination of systems installed on board ('System XX'_'System YY')."@en ;
    # Source: ontology comment for trainControlSwitchOverSpecialConditions

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "trainControlSwitchOverSpecialConditions: The VehicleType must have at most one trainControlSwitchOverSpecialConditions value that is a valid string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:trainControlSwitchOverSpecialConditions ;

    sh:severity sh:Violation .

era-sh:TrainControlSwitchOverSpecialConditionsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:trainControlSwitchOverSpecialConditions ;
    era:scope "local" ;

    rdfs:comment "Applicable when at least two different protection, control and warning systems exist."@en ;
    # Source: RAG indicates applicability based on the existence of multiple systems

    sh:message "trainControlSwitchOverSpecialConditions: This error is due to the VehicleType {?label}, violating the rule: Applicable only when at least two different protection, control and warning systems exist."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:trainControlSwitchOverSpecialConditions ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:protectionControlWarningSystems ?system . FILTER(?system >= 2) } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TrainIntegrityOnBoardRequired a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:trainIntegrityOnBoardRequired ;
    era:rinfIndex "1.1.1.3.2.8" ;
    era:scope "local" ;

    rdfs:comment "Indication whether train confirmation from on-board is required to access the line for safety reasons."@en ;
    # Source: ontology comment for trainIntegrityOnBoardRequired

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:in ("Y" "N" "NYA") ;
    # Source: RAG indicates applicable values are Y/N/NYA

    sh:message "trainIntegrityOnBoardRequired: The CommonCharacteristicsSubset or RunningTrack must have at most one trainIntegrityOnBoardRequired value that is a valid selection from the predefined list: Y, N, or NYA. This error may be due to having more than one value or having a value that is not in the allowed set."@en ;

    sh:path era:trainIntegrityOnBoardRequired ;

    sh:severity sh:Violation .

era-sh:TrainIntegrityOnBoardRequiredApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:trainIntegrityOnBoardRequired ;
    era:scope "local" ;

    rdfs:comment "Applicable only when selected value for 1.1.1.3.2.1 is ‘3’."@en ;
    # Source: RAG indicates applicability based on the value of another parameter

    sh:message "trainIntegrityOnBoardRequired: This error is due to the CommonCharacteristicsSubset or RunningTrack {?label}, violating the rule: Applicable only when selected value for 1.1.1.3.2.1 is ‘3’."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:trainIntegrityOnBoardRequired ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:parameter1_1_1_3_2_1 "3" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TransportableOnFerry a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:transportableOnFerry ;
    era:scope "local" ;

    rdfs:comment "Indicates the suitability for transport on ferries."@en ;
    # Source: ontology comment for transportableOnFerry

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "transportableOnFerry: The VehicleType must have at most one transportableOnFerry value that is a valid boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:transportableOnFerry ;

    sh:severity sh:Violation .

era-sh:TransportableOnFerryApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:transportableOnFerry ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is assessed for suitability for transport on ferries."@en ;
    # Source: inferred from the context of vehicle type assessments

    sh:message "transportableOnFerry: This error is due to the VehicleType {?label}, violating the rule: Applicable only when the vehicle type is assessed for suitability for transport on ferries."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:transportableOnFerry ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:assessedForFerryTransport true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TsiMagneticFields a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:tsiMagneticFields ;
    era:rinfIndex "1.1.1.3.9.1" ;
    era:scope "local" ;

    rdfs:comment "Indication whether rules exist and are compliant with the TSI for magnetic fields emitted by a vehicle."@en ;
    # Source: ontology comment for tsiMagneticFields

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:in ("none" "TSI compliant" "Not TSI compliant") ;
    # Source: RAG indicates applicable values are none, TSI compliant, Not TSI compliant

    sh:message "tsiMagneticFields: The CommonCharacteristicsSubset or RunningTrack must have at most one tsiMagneticFields value that is a valid selection from the predefined list: none, TSI compliant, or Not TSI compliant. This error may be due to having more than one value or having a value that is not in the allowed set."@en ;

    sh:path era:tsiMagneticFields ;

    sh:severity sh:Violation .

era-sh:TsiMagneticFieldsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:tsiMagneticFields ;
    era:scope "local" ;

    rdfs:comment "Applicable only when for parameter 1.1.1.3.7.1 the selected option is 'wheel detector'."@en ;
    # Source: RAG indicates applicability based on the value of another parameter

    sh:message "tsiMagneticFields: This error is due to the CommonCharacteristicsSubset or RunningTrack {?label}, violating the rule: Applicable only when for parameter 1.1.1.3.7.1 the selected option is 'wheel detector'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:tsiMagneticFields ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:parameter1_1_1_3_7_1 "wheel detector" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TSISwitchCrossing a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:tsiSwitchCrossing ;
    era:rinfIndex "1.1.1.1.5.1" ;
    era:scope "local" ;

    rdfs:comment "Indicates TSI compliance of in-service values for switches and crossings."@en ;
    # Source: ontology comment for tsiSwitchCrossing

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "tsiSwitchCrossing: The CommonCharacteristicsSubset or RunningTrack must have at most one tsiSwitchCrossing value that is a valid boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:tsiSwitchCrossing ;

    sh:severity sh:Violation .

era-sh:TSISwitchCrossingApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:tsiSwitchCrossing ;
    era:scope "local" ;

    rdfs:comment "Applicable if for existing track at least one parameter has less strict value than specified in the TSI."@en ;
    # Source: RAG indicates applicability based on compliance with TSI standards

    sh:message "tsiSwitchCrossing: This error is due to the CommonCharacteristicsSubset or RunningTrack {?label}, violating the rule: Applicable only if for existing track at least one parameter has less strict value than specified in the TSI."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:tsiSwitchCrossing ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:parameterWithLessStrictValue true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TsiTractionHarmonics a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:tsiTractionHarmonics ;
    era:rinfIndex "1.1.1.3.9.2" ;
    era:scope "local" ;

    rdfs:comment "Indication whether rules exist and are compliant with the TSI for harmonics in the traction current of vehicles."@en ;
    # Source: ontology comment for tsiTractionHarmonics

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:in ("none" "TSI compliant" "Not TSI compliant") ;
    # Source: RAG indicates applicable values are none, TSI compliant, Not TSI compliant

    sh:message "tsiTractionHarmonics: The CommonCharacteristicsSubset or RunningTrack must have at most one tsiTractionHarmonics value that is a valid selection from the predefined list: none, TSI compliant, or Not TSI compliant. This error may be due to having more than one value or having a value that is not in the allowed set."@en ;

    sh:path era:tsiTractionHarmonics ;

    sh:severity sh:Violation .

era-sh:TsiTractionHarmonicsApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:tsiTractionHarmonics ;
    era:scope "local" ;

    rdfs:comment "Applicable only when for parameter 1.1.1.3.7.1 the selected option is 'wheel detector' or 'track circuit'."@en ;
    # Source: RAG indicates applicability based on the value of another parameter

    sh:message "tsiTractionHarmonics: This error is due to the CommonCharacteristicsSubset or RunningTrack {?label}, violating the rule: Applicable only when for parameter 1.1.1.3.7.1 the selected option is 'wheel detector' or 'track circuit'."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:tsiTractionHarmonics ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:parameter1_1_1_3_7_1 ?option . FILTER(?option IN ("wheel detector", "track circuit")) } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TunnelIdentification a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:tunnelIdentification ;
    era:rinfIndex "1.1.1.1.8.2" ;
    era:scope "local" ;

    rdfs:comment "Unique tunnel identification or unique number within Member State."@en ;
    # Source: RAG definition for tunnel identification

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "tunnelIdentification: The Tunnel must have at most one tunnelIdentification value that is a valid string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:tunnelIdentification ;

    sh:severity sh:Violation .

era-sh:TunnelIdentificationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:tunnelIdentification ;
    era:scope "local" ;

    rdfs:comment "Applicable when the tunnel does not have its own identification within the Member State."@en ;
    # Source: RAG indicates that the IM should deliver it if no identification exists

    sh:message "tunnelIdentification: This error is due to the Tunnel {?label}, violating the rule: Applicable only when the tunnel does not have its own identification within the Member State."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:tunnelIdentification ?value } .
        # Additional checks for applicability conditions
        FILTER NOT EXISTS { $this era:hasOwnIdentification true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:TypeVersionNumber a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:typeVersionNumber ;
    era:scope "local" ;

    rdfs:comment "Serial number that identifies a vehicle type."@en ;
    # Source: ontology comment for typeVersionNumber

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "typeVersionNumber: The VehicleType must have at most one typeVersionNumber value that is a valid string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:typeVersionNumber ;

    sh:severity sh:Violation .

era-sh:TypeVersionNumberApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:typeVersionNumber ;
    era:scope "local" ;

    rdfs:comment "Applicable when the type version number is defined for the vehicle type."@en ;
    # Source: inferred from the context of vehicle type identification

    sh:message "typeVersionNumber: This error is due to the VehicleType {?label}, violating the rule: Applicable only when the type version number is defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:typeVersionNumber ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:typeVersionNumberDefined true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Umax2 a sh:PropertyShape ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:umax2 ;
    era:rinfIndex "1.1.1.2.2.1.3" ;
    era:scope "local" ;

    rdfs:comment "Highest non-permanent voltage (Umax2) for France on lines not compliant with values defined in the EN50163:2004+A1:2007+A2:2020+A3:2022."@en ;
    # Source: ontology comment for umax2

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "umax2: The ContactLineSystem must have at most one umax2 value that is a valid integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:umax2 ;

    sh:severity sh:Violation .

era-sh:Umax2Applicability a sh:SPARQLConstraint ;
    era:affectedClass era:ContactLineSystem ;
    era:affectedProperty era:umax2 ;
    era:scope "local" ;

    rdfs:comment "Applicable when 'not electrified' is not chosen in parameter 1.1.1.2.2.1.1."@en ;
    # Source: RAG indicates applicability based on the choice of electrification

    sh:message "umax2: This error is due to the ContactLineSystem {?label}, violating the rule: Applicable only when 'not electrified' is not chosen."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:umax2 ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:parameter1_1_1_2_2_1_1 "not electrified" } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:Uopid a sh:PropertyShape ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:uopid ;
    era:rinfIndex "1.2.0.0.0.2" ;
    era:scope "local" ;

    rdfs:comment "Code composed of country code and alphanumeric operational point code."@en ;
    # Source: ontology comment for uopid

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:pattern "^[A-Z]{2}[A-Za-z0-9]{1,10}$" ;
    # Source: RAG specifies format as [AA+AAAAAAAAAA] where AA is country code and AAAAAAAAAA is OP code

    sh:message "uopid: The OperationalPoint must have at most one uopid value that is a valid string matching the format [AA+AAAAAAAAAA]. This error may be due to having more than one value or having a value that does not conform to the expected format."@en ;

    sh:path era:uopid ;

    sh:severity sh:Violation .

era-sh:UopidApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:OperationalPoint ;
    era:affectedProperty era:uopid ;
    era:scope "local" ;

    rdfs:comment "Applicable when the OP ID must be unique within each Member State and must be selected from the corresponding list in annex 5.1 or 5.2."@en ;
    # Source: RAG indicates applicability based on uniqueness and reference lists

    sh:message "uopid: This error is due to the OperationalPoint {?label}, violating the rule: Applicable only when the uopid is unique within the Member State and referenced correctly."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:uopid ?value } .
        # Additional checks for uniqueness conditions
        FILTER EXISTS { $this era:countryCode ?country . FILTER(?country IN (SELECT ?c WHERE { ?c a era:Country })) } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:UsesGroup555 a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:usesGroup555 ;
    era:rinfIndex "4.13.2.12" ;
    era:scope "local" ;

    rdfs:comment "Indication if group 555 is needed."@en ;
    # Source: ontology comment for usesGroup555

    sh:datatype xsd:boolean ;
    # Source: ontology range is xsd:boolean (http://www.w3.org/2001/XMLSchema#boolean)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:in ("true" "false") ;
    # Source: RAG indicates applicable values are Y/N/NYA, interpreted as boolean

    sh:message "usesGroup555: The CommonCharacteristicsSubset or RunningTrack must have at most one usesGroup555 value that is a valid boolean. This error may be due to having more than one value or having a value that is not a valid boolean."@en ;

    sh:path era:usesGroup555 ;

    sh:severity sh:Violation .

era-sh:UsesGroup555Applicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:usesGroup555 ;
    era:scope "local" ;

    rdfs:comment "Applicable when GSM-R and ETCS L2 must be installed for this parameter to be applicable."@en ;
    # Source: RAG indicates applicability based on the installation of specific systems

    sh:message "usesGroup555: This error is due to the CommonCharacteristicsSubset or RunningTrack {?label}, violating the rule: Applicable only when GSM-R and ETCS L2 must be installed."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:usesGroup555 ?value } .
        # Additional checks for applicability conditions
        FILTER NOT EXISTS { $this era:gsmRInstalled false } .
        FILTER NOT EXISTS { $this era:etcsL2Installed false } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VNallowovtrp a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:vNvallowovtrp ;
    era:rinfIndex "1.1.1.3.2.16.3" ;
    era:scope "local" ;

    rdfs:comment "Speed limit allowing the driver to select the override function in km/h according to the specification referenced in Appendix A-1."@en ;
    # Source: ontology comment for vNvallowovtrp

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "vNvallowovtrp: The CommonCharacteristicsSubset or RunningTrack must have at most one vNvallowovtrp value that is a valid integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:vNvallowovtrp ;

    sh:severity sh:Violation .

era-sh:VNallowovtrpApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:vNvallowovtrp ;
    era:scope "local" ;

    rdfs:comment "Applicable when ETCS is present, and a value must be provided."@en ;
    # Source: RAG indicates dependency on ETCS presence

    sh:message "vNvallowovtrp: This error is due to the CommonCharacteristicsSubset or RunningTrack {?label}, violating the rule: Applicable only when ETCS is present."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:vNvallowovtrp ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:etcsPresent true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VNvsupovtrp a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:vNvsupovtrp ;
    era:rinfIndex "1.1.1.3.2.16.4" ;
    era:scope "local" ;

    rdfs:comment "Override speed limit to be supervised when the 'override' function is active in km/h."@en ;
    # Source: ontology comment for vNvsupovtrp

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "vNvsupovtrp: The CommonCharacteristicsSubset or RunningTrack must have at most one vNvsupovtrp value that is a valid integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:vNvsupovtrp ;

    sh:severity sh:Violation .

era-sh:VNvsupovtrpApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset, era:RunningTrack ;
    era:affectedProperty era:vNvsupovtrp ;
    era:scope "local" ;

    rdfs:comment "Applicable when ETCS is present, and a value has to be provided."@en ;
    # Source: RAG indicates a value must be provided when ETCS is present

    sh:message "vNvsupovtrp: This error is due to the CommonCharacteristicsSubset or RunningTrack {?label}, violating the rule: Applicable only when ETCS is present and a value is provided."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:vNvsupovtrp ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:etcsPresent true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ValidityEndDate a sh:PropertyShape ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:validityEndDate ;
    era:scope "local" ;

    rdfs:comment "The validity end date for the InfrastructureElement, indicating when the data is no longer applicable."@en ;
    # Source: ontology comment for validityEndDate

    sh:datatype xsd:date ;
    # Source: ontology range is xsd:date (http://www.w3.org/2001/XMLSchema#date)

    sh:maxCount 1 ;
    # Inferred: only one validity end date expected based on the nature of the property

    sh:message "validityEndDate: The InfrastructureElement must have at most one validityEndDate value that is a valid date. This error may be due to having more than one value or having a value that is not a valid date."@en ;

    sh:path era:validityEndDate ;

    sh:severity sh:Violation .

era-sh:ValidityEndDateApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:validityEndDate ;
    era:scope "local" ;

    rdfs:comment "Applicable when the InfrastructureElement has a defined future validity date."@en ;
    # Source: RAG indicates that validity dates can be defined for future values

    sh:message "validityEndDate: This error is due to the InfrastructureElement {?label}, violating the rule: Applicable only when a future validity date is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:validityEndDate ?value } .
        # Additional checks for future validity date
        FILTER EXISTS { $this era:futureValidity true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:ValidityStartDate a sh:PropertyShape ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:validityStartDate ;
    era:scope "local" ;

    rdfs:comment "The validity start date for the InfrastructureElement, indicating when the data becomes applicable."@en ;
    # Source: inferred from the context of validity dates

    sh:datatype xsd:date ;
    # Source: ontology range is xsd:date (http://www.w3.org/2001/XMLSchema#date)

    sh:maxCount 1 ;
    # Inferred: only one validity start date expected based on the nature of the property

    sh:message "validityStartDate: The InfrastructureElement must have at most one validityStartDate value that is a valid date. This error may be due to having more than one value or having a value that is not a valid date."@en ;

    sh:path era:validityStartDate ;

    sh:severity sh:Violation .

era-sh:ValidityStartDateApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:InfrastructureElement ;
    era:affectedProperty era:validityStartDate ;
    era:scope "local" ;

    rdfs:comment "Applicable when the InfrastructureElement has a defined future validity date."@en ;
    # Source: RAG indicates that validity dates can be defined for future values

    sh:message "validityStartDate: This error is due to the InfrastructureElement {?label}, violating the rule: Applicable only when a future validity date is defined."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:validityStartDate ?value } .
        # Additional checks for future validity date
        FILTER EXISTS { $this era:futureValidity true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehicleContactForce a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehicleContactForce ;
    era:rinfIndex "4.10.15" ;
    era:scope "local" ;

    rdfs:comment "Mean contact force."@en ;
    # Source: ontology comment for vehicleContactForce

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "vehicleContactForce: The VehicleType must have at most one vehicleContactForce value that is a valid integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:vehicleContactForce ;

    sh:severity sh:Violation .

era-sh:VehicleContactForceApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehicleContactForce ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type has defined contact force parameters."@en ;
    # Source: inferred from the context of vehicle type characteristics

    sh:message "vehicleContactForce: This error is due to the VehicleType {?label}, violating the rule: Applicable only when the vehicle type has defined contact force parameters."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:vehicleContactForce ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:contactForceDefined true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehicleKinematicGaugeOther a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehicleKinematicGaugeOther ;
    era:rinfIndex "4.2.2" ;
    era:scope "local" ;

    rdfs:comment "Vehicle kinematic gauge that corresponds to other gauges assessed using the kinematic method."@en ;
    # Source: ontology comment for vehicleKinematicGaugeOther

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "vehicleKinematicGaugeOther: The VehicleType must have at most one vehicleKinematicGaugeOther value that is a valid string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:vehicleKinematicGaugeOther ;

    sh:severity sh:Violation .

era-sh:VehicleKinematicGaugeOtherApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehicleKinematicGaugeOther ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type has defined kinematic gauge parameters."@en ;
    # Source: inferred from the context of vehicle type characteristics

    sh:message "vehicleKinematicGaugeOther: This error is due to the VehicleType {?label}, violating the rule: Applicable only when the vehicle type has defined kinematic gauge parameters."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:vehicleKinematicGaugeOther ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:kinematicGaugeDefined true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehicleMaxSandingOutput a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehicleMaxSandingOutput ;
    era:rinfIndex "4.14.2.13" ;
    era:scope "local" ;

    rdfs:comment "Vehicle Maximum sanding output, given in grams per second."@en ;
    # Source: ontology comment for vehicleMaxSandingOutput

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "vehicleMaxSandingOutput: The VehicleType must have at most one vehicleMaxSandingOutput value that is a valid string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:vehicleMaxSandingOutput ;

    sh:severity sh:Violation .

era-sh:VehicleMaxSandingOutputApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehicleMaxSandingOutput ;
    era:scope "local" ;

    rdfs:comment "Applicable when the maximum sanding output is defined for the vehicle type."@en ;
    # Source: inferred from the context of vehicle type characteristics

    sh:message "vehicleMaxSandingOutput: This error is due to the VehicleType {?label}, violating the rule: Applicable only when the maximum sanding output is defined for the vehicle type."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:vehicleMaxSandingOutput ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:maxSandingOutputDefined true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehicleNumber a sh:PropertyShape ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:vehicleNumber ;
    era:scope "local" ;

    rdfs:comment "Identification number of a vehicle or wagon."@en ;
    # Source: ontology comment for vehicleNumber

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:pattern "^[A-Z0-9-]+$" ;
    # Inferred: common pattern for vehicle numbers, allowing alphanumeric characters and hyphens

    sh:message "vehicleNumber: The Vehicle must have at most one vehicleNumber value that is a valid string matching the expected format. This error may be due to having more than one value or having a value that does not conform to the expected format."@en ;

    sh:path era:vehicleNumber ;

    sh:severity sh:Violation .

era-sh:VehiclePantographHead a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehiclePantographHead ;
    era:rinfIndex "4.10.6" ;
    era:scope "local" ;

    rdfs:comment "Pantograph head geometry (to be indicated for each energy supply system the vehicle is equipped for)."@en ;
    # Source: ontology comment for vehiclePantographHead

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "vehiclePantographHead: The VehicleType must have at most one vehiclePantographHead value that is a valid string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:vehiclePantographHead ;

    sh:severity sh:Violation .

era-sh:VehiclePantographHeadApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehiclePantographHead ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type has defined pantograph head parameters."@en ;
    # Source: inferred from the context of vehicle type characteristics

    sh:message "vehiclePantographHead: This error is due to the VehicleType {?label}, violating the rule: Applicable only when the vehicle type has defined pantograph head parameters."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:vehiclePantographHead ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:pantographHeadDefined true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehicleSeries a sh:PropertyShape ;
    era:affectedClass era:Vehicle ;
    era:affectedProperty era:vehicleSeries ;
    era:scope "local" ;

    rdfs:comment "Manufacturing series of a vehicle."@en ;
    # Source: ontology comment for vehicleSeries

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "vehicleSeries: The Vehicle must have at most one vehicleSeries value that is a valid string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:vehicleSeries ;

    sh:severity sh:Violation .

era-sh:VehicleTypeMaximumCantDeficiency a sh:PropertyShape ;
    era:affectedClass era:MaximumSpeedAndCantDeficiency ;
    era:affectedProperty era:vehicleTypeMaximumCantDeficiency ;
    era:rinfIndex "4.6.4" ;
    era:scope "local" ;

    rdfs:comment "Part of the combination of maximum speed and maximum cant deficiency for which the vehicle was assessed. Corresponds to the maximum cant deficiency."@en ;
    # Source: ontology comment for vehicleTypeMaximumCantDeficiency

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "vehicleTypeMaximumCantDeficiency: The MaximumSpeedAndCantDeficiency must have at most one vehicleTypeMaximumCantDeficiency value that is a valid integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:vehicleTypeMaximumCantDeficiency ;

    sh:severity sh:Violation .

era-sh:VehicleTypeMaximumCantDeficiencyApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:MaximumSpeedAndCantDeficiency ;
    era:affectedProperty era:vehicleTypeMaximumCantDeficiency ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type has defined cant deficiency parameters."@en ;
    # Source: inferred from the context of vehicle type characteristics

    sh:message "vehicleTypeMaximumCantDeficiency: This error is due to the MaximumSpeedAndCantDeficiency {?label}, violating the rule: Applicable only when the vehicle type has defined cant deficiency parameters."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:vehicleTypeMaximumCantDeficiency ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:cantDeficiencyDefined true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehicleTypeMaximumSpeed a sh:PropertyShape ;
    era:affectedClass era:MaximumSpeedAndCantDeficiency ;
    era:affectedProperty era:vehicleTypeMaximumSpeed ;
    era:rinfIndex "4.6.4" ;
    era:scope "local" ;

    rdfs:comment "Part of the combination of maximum speed and maximum cant deficiency for which the vehicle was assessed. Corresponds to the maximum speed."@en ;
    # Source: ontology comment for vehicleTypeMaximumSpeed

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "vehicleTypeMaximumSpeed: The MaximumSpeedAndCantDeficiency must have at most one vehicleTypeMaximumSpeed value that is a valid integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:vehicleTypeMaximumSpeed ;

    sh:severity sh:Violation .

era-sh:VehicleTypeMaximumSpeedApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:MaximumSpeedAndCantDeficiency ;
    era:affectedProperty era:vehicleTypeMaximumSpeed ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type has defined maximum speed parameters."@en ;
    # Source: inferred from the context of vehicle type characteristics

    sh:message "vehicleTypeMaximumSpeed: This error is due to the MaximumSpeedAndCantDeficiency {?label}, violating the rule: Applicable only when the vehicle type has defined maximum speed parameters."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:vehicleTypeMaximumSpeed ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:maxSpeedDefined true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VehiclesComposingFixedFormation a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehiclesComposingFixedFormation ;
    era:rinfIndex "4.1.12" ;
    era:scope "local" ;

    rdfs:comment "Number of vehicles composing the fixed formation (for fixed formation only). Note: the value is mandatory for all vehicle categories. If the vehicle is composed of only one car, the indicated value shall be '1'."@en ;
    # Source: ontology comment for vehiclesComposingFixedFormation

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:minCount 1 ;
    # Inferred: at least one vehicle must be specified for fixed formations

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "vehiclesComposingFixedFormation: The VehicleType must have at most one vehiclesComposingFixedFormation value that is a valid integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:vehiclesComposingFixedFormation ;

    sh:severity sh:Violation .

era-sh:VehiclesComposingFixedFormationApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:vehiclesComposingFixedFormation ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type is defined as a fixed formation."@en ;
    # Source: inferred from the context of vehicle type characteristics

    sh:message "vehiclesComposingFixedFormation: This error is due to the VehicleType {?label}, violating the rule: Applicable only when the vehicle type is defined as a fixed formation."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:vehiclesComposingFixedFormation ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:isFixedFormation true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VerificationCCS a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:verificationCCS ; 
    era:rinfIndex "1.1.1.3.1.1" ; 
    era:scope "local" ; 

    rdfs:comment "Unique number for EC declarations in accordance with Commission Implementing Regulation (EU) 2019/250."@en ; 
    # Source: ontology comment for verificationCCS

    sh:datatype xsd:string ; 
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ; 
    # Inferred: only one value expected based on the nature of the property

    sh:pattern "^[A-Z]{2}/[0-9]{13}/[0-9]{4}/[0-9]{6}$" ; 
    # Inferred: pattern based on data presentation format CC/RRRRRRRRRRRRRR/YYYY/NNNNNN

    sh:message "verificationCCS: The CommonCharacteristicsSubset must have at most one verificationCCS value that is a valid string matching the expected format. This error may be due to having more than one value or having a value that does not conform to the expected format."@en ; 

    sh:path era:verificationCCS ; 

    sh:severity sh:Violation .

era-sh:VerificationCCSApplicability a sh:SPARQLConstraint ; 
    era:affectedClass era:CommonCharacteristicsSubset ; 
    era:affectedProperty era:verificationCCS ; 
    era:scope "local" ; 

    rdfs:comment "Applicable when the EC declaration of verification is issued."@en ; 
    # Source: RAG indicates applicability based on the issuance of EC declaration

    sh:message "verificationCCS: This error is due to the CommonCharacteristicsSubset {?label}, violating the rule: Applicable only when the EC declaration of verification is issued."@en ; 

    sh:prefixes era: ; 

    sh:select """ 
    PREFIX era: <http://data.europa.eu/949/> 
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#> 
    SELECT DISTINCT $this ?label WHERE { 
        $this rdfs:label ?label . 
        FILTER NOT EXISTS { $this era:verificationCCS ?value } . 
        # Additional checks for applicability conditions 
        FILTER EXISTS { $this era:ecDeclarationIssued true } . 
    } 
    """ ; 

    sh:severity sh:Violation .

era-sh:VerificationENE a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:verificationENE ;
    era:rinfIndex "1.1.1.2.1.1" ;
    era:scope "local" ;

    rdfs:comment "Unique number for EC declarations in accordance with Commission Implementing Regulation (EU) 2019/250."@en ;
    # Source: ontology comment for verificationENE

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:pattern "^[A-Z]{2}/[0-9]{13}/[0-9]{4}/[0-9]{6}$" ;
    # Inferred: pattern based on data presentation format CC/RRRRRRRRRRRRRR/YYYY/NNNNNN

    sh:message "verificationENE: The CommonCharacteristicsSubset must have at most one verificationENE value that is a valid string matching the expected format. This error may be due to having more than one value or having a value that does not conform to the expected format."@en ;

    sh:path era:verificationENE ;

    sh:severity sh:Violation .

era-sh:VerificationENEApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:verificationENE ;
    era:scope "local" ;

    rdfs:comment "Applicable when the EC declaration of verification is issued."@en ;
    # Source: RAG indicates applicability based on the issuance of EC declaration

    sh:message "verificationENE: This error is due to the CommonCharacteristicsSubset {?label}, violating the rule: Applicable only when the EC declaration of verification is issued."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:verificationENE ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:ecDeclarationIssued true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VerificationINF a sh:PropertyShape ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:verificationINF ;
    era:rinfIndex "1.2.1.0.1.1" ;
    era:scope "local" ;

    rdfs:comment "Unique number for EC declarations in accordance with Commission Implementing Regulation (EU) 2019/250."@en ;
    # Source: ontology comment for verificationINF

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:pattern "^[A-Z]{2}/[0-9]{13}/[0-9]{4}/[0-9]{6}$" ;
    # Inferred: pattern based on data presentation format CC/RRRRRRRRRRRRRR/YYYY/NNNNNN

    sh:message "verificationINF: The CommonCharacteristicsSubset must have at most one verificationINF value that is a valid string matching the expected format. This error may be due to having more than one value or having a value that does not conform to the expected format."@en ;

    sh:path era:verificationINF ;

    sh:severity sh:Violation .

era-sh:VerificationINFApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:CommonCharacteristicsSubset ;
    era:affectedProperty era:verificationINF ;
    era:scope "local" ;

    rdfs:comment "Applicable when the EC declaration of verification is issued."@en ;
    # Source: RAG indicates applicability based on the issuance of EC declaration

    sh:message "verificationINF: This error is due to the CommonCharacteristicsSubset {?label}, violating the rule: Applicable only when the EC declaration of verification is issued."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:verificationINF ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:ecDeclarationIssued true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VerificationSRT a sh:PropertyShape ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:verificationSRT ;
    era:rinfIndex "1.1.1.1.8.5" ;
    era:scope "local" ;

    rdfs:comment "Unique number for EC declarations in accordance with Commission Implementing Regulation (EU) 2019/250."@en ;
    # Source: ontology comment for verificationSRT

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:pattern "^[A-Z]{2}/[0-9]{13}/[0-9]{4}/[0-9]{6}$" ;
    # Inferred: pattern based on data presentation format CC/RRRRRRRRRRRRRR/YYYY/NNNNNN

    sh:message "verificationSRT: The Tunnel must have at most one verificationSRT value that is a valid string matching the expected format. This error may be due to having more than one value or having a value that does not conform to the expected format."@en ;

    sh:path era:verificationSRT ;

    sh:severity sh:Violation .

era-sh:VerificationSRTApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:Tunnel ;
    era:affectedProperty era:verificationSRT ;
    era:scope "local" ;

    rdfs:comment "Applicable when the EC declaration of verification is issued."@en ;
    # Source: RAG indicates applicability based on the issuance of EC declaration

    sh:message "verificationSRT: This error is due to the Tunnel {?label}, violating the rule: Applicable only when the EC declaration of verification is issued."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:verificationSRT ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:ecDeclarationIssued true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:VoiceOperationalCommImpl a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:voiceOperationalCommImpl ;
    era:scope "local" ;

    rdfs:comment "Voice and operational communication implementation."@en ;
    # Source: ontology comment for voiceOperationalCommImpl

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "voiceOperationalCommImpl: The VehicleType must have at most one voiceOperationalCommImpl value that is a valid string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:voiceOperationalCommImpl ;

    sh:severity sh:Violation .

era-sh:VoiceOperationalCommImplApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:voiceOperationalCommImpl ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type has defined voice operational communication parameters."@en ;
    # Source: inferred from the context of vehicle type characteristics

    sh:message "voiceOperationalCommImpl: This error is due to the VehicleType {?label}, violating the rule: Applicable only when the vehicle type has defined voice operational communication parameters."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:voiceOperationalCommImpl ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:voiceCommDefined true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:WheelSetGaugeTransformationMethod a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:wheelSetGaugeTransformationMethod ;
    era:rinfIndex "4.1.3.b" ;
    era:scope "local" ;

    rdfs:comment "Wheel gauge transformation method. Defined if more than one wheel gauges have been selected."@en ;
    # Source: ontology comment for wheelSetGaugeTransformationMethod

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "wheelSetGaugeTransformationMethod: The VehicleType must have at most one wheelSetGaugeTransformationMethod value that is a valid string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:wheelSetGaugeTransformationMethod ;

    sh:severity sh:Violation .

era-sh:WheelSetGaugeTransformationMethodApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:wheelSetGaugeTransformationMethod ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type has defined wheel gauge transformation parameters."@en ;
    # Source: inferred from the context of vehicle type characteristics

    sh:message "wheelSetGaugeTransformationMethod: This error is due to the VehicleType {?label}, violating the rule: Applicable only when the vehicle type has defined wheel gauge transformation parameters."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:wheelSetGaugeTransformationMethod ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:wheelGaugeTransformationDefined true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:WheelchairSleepingPlaces a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:wheelchairSleepingPlaces ;
    era:scope "local" ;

    rdfs:comment "Number of wheelchair accessible sleeping places."@en ;
    # Source: ontology comment for wheelchairSleepingPlaces

    sh:datatype xsd:string ;
    # Source: ontology range is xsd:string (http://www.w3.org/2001/XMLSchema#string)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "wheelchairSleepingPlaces: The VehicleType must have at most one wheelchairSleepingPlaces value that is a valid string. This error may be due to having more than one value or having a value that is not a valid string."@en ;

    sh:path era:wheelchairSleepingPlaces ;

    sh:severity sh:Violation .

era-sh:WheelchairSleepingPlacesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:wheelchairSleepingPlaces ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type has defined wheelchair accessibility parameters."@en ;
    # Source: inferred from the context of vehicle type characteristics

    sh:message "wheelchairSleepingPlaces: This error is due to the VehicleType {?label}, violating the rule: Applicable only when the vehicle type has defined wheelchair accessibility parameters."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:wheelchairSleepingPlaces ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:wheelchairAccessibilityDefined true } .
    }
    """ ;

    sh:severity sh:Violation .

era-sh:WheelchairSpaces a sh:PropertyShape ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:wheelchairSpaces ;
    era:rinfIndex "4.12.2.2" ;
    era:scope "local" ;

    rdfs:comment "Number of wheelchair spaces."@en ;
    # Source: ontology comment for wheelchairSpaces

    sh:datatype xsd:integer ;
    # Source: ontology range is xsd:integer (http://www.w3.org/2001/XMLSchema#integer)

    sh:maxCount 1 ;
    # Inferred: only one value expected based on the nature of the property

    sh:message "wheelchairSpaces: The VehicleType must have at most one wheelchairSpaces value that is a valid integer. This error may be due to having more than one value or having a value that is not a valid integer."@en ;

    sh:path era:wheelchairSpaces ;

    sh:severity sh:Violation .

era-sh:WheelchairSpacesApplicability a sh:SPARQLConstraint ;
    era:affectedClass era:VehicleType ;
    era:affectedProperty era:wheelchairSpaces ;
    era:scope "local" ;

    rdfs:comment "Applicable when the vehicle type has defined wheelchair accessibility parameters."@en ;
    # Source: inferred from the context of vehicle type characteristics

    sh:message "wheelchairSpaces: This error is due to the VehicleType {?label}, violating the rule: Applicable only when the vehicle type has defined wheelchair accessibility parameters."@en ;

    sh:prefixes era: ;

    sh:select """
    PREFIX era: <http://data.europa.eu/949/>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT DISTINCT $this ?label WHERE {
        $this rdfs:label ?label .
        FILTER NOT EXISTS { $this era:wheelchairSpaces ?value } .
        # Additional checks for applicability conditions
        FILTER EXISTS { $this era:wheelchairAccessibilityDefined true } .
    }
    """ ;

    sh:severity sh:Violation .